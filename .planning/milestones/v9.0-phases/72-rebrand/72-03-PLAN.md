---
phase: 72-rebrand
plan: 03
type: execute
wave: 3
depends_on: [72-01, 72-02]
files_modified:
  - workflows/*.md
  - commands/*.md
  - templates/*.md
  - skills/*/SKILL.md
  - AGENTS.md
autonomous: true
requirements: [RBND-08]
outcome_ids: []

must_haves:
  truths:
    - "All workflow files reference bgsd-oc, bgsd-tools, __OPENCODE_CONFIG__/bgsd-oc (no old gsd patterns)"
    - "All command files reference bgsd-oc (no get-shit-done patterns)"
    - "All template files reference bgsd-oc (no get-shit-done patterns)"
    - "All skill files reference bgsd-tools, __OPENCODE_CONFIG__/bgsd-oc (no old gsd patterns)"
    - "AGENTS.md references bgsd-tools and bgsd-oc throughout"
  artifacts:
    - path: "workflows/execute-plan.md"
      provides: "Execute plan workflow with bgsd-tools references"
    - path: "commands/bgsd-execute-phase.md"
      provides: "Execute phase command with bgsd-oc references"
    - path: "AGENTS.md"
      provides: "Project description with bgsd-tools and bgsd-oc naming"
  key_links:
    - from: "workflows/*.md"
      to: "__OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs"
      via: "CLI invocations in workflow steps"
      pattern: "bgsd-tools\\.cjs"
    - from: "commands/*.md"
      to: "bgsd-oc"
      via: "config path references"
      pattern: "bgsd-oc"
---

<objective>
Update all workflow, command, template, and skill files to use the new bgsd-* naming consistently. This is the bulk rename covering ~400+ references across ~130+ markdown files.

Purpose: These files are the user-facing and agent-facing interface. They reference the CLI binary, config paths, and agent names extensively. After source (Plan 01) and agents (Plan 02) are renamed, these must follow.
Output: All .md files in workflows/, commands/, templates/, skills/, and AGENTS.md updated
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-rebrand/72-RESEARCH.md
@.planning/phases/72-rebrand/72-01-SUMMARY.md
@.planning/phases/72-rebrand/72-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all workflow and command files</name>
  <files>workflows/*.md (44 files), commands/*.md (41 files)</files>
  <action>
Perform bulk text replacement across all workflow and command markdown files:

**Workflows (44 files, ~390 references):**
1. `get-shit-done` -> `bgsd-oc` (config path references, ~121 matches)
2. `gsd-tools` -> `bgsd-tools` (CLI binary references, ~140 matches)
3. `GSD_HOME` -> `__OPENCODE_CONFIG__/bgsd-oc` (env var references, if any)
4. `gsd-executor` -> `bgsd-executor`, `gsd-planner` -> `bgsd-planner`, `gsd-verifier` -> `bgsd-verifier`, etc. (agent name references, ~1 match)

**Commands (41 files, ~80 references):**
1. `get-shit-done` -> `bgsd-oc` (config path references, ~80 matches)
2. `gsd-tools` -> `bgsd-tools` (if any CLI references)
3. Agent name references: `gsd-{name}` -> `bgsd-{name}`

Use `sed -i` for bulk operations:
```bash
sed -i 's/get-shit-done/bgsd-oc/g' workflows/*.md commands/*.md
sed -i 's/gsd-tools/bgsd-tools/g' workflows/*.md commands/*.md
sed -i 's/GSD_HOME/__OPENCODE_CONFIG__/bgsd-oc/g' workflows/*.md commands/*.md
sed -i 's/gsd-executor/bgsd-executor/g' workflows/*.md commands/*.md
sed -i 's/gsd-planner/bgsd-planner/g' workflows/*.md commands/*.md
sed -i 's/gsd-verifier/bgsd-verifier/g' workflows/*.md commands/*.md
sed -i 's/gsd-debugger/bgsd-debugger/g' workflows/*.md commands/*.md
sed -i 's/gsd-roadmapper/bgsd-roadmapper/g' workflows/*.md commands/*.md
sed -i 's/gsd-codebase-mapper/bgsd-codebase-mapper/g' workflows/*.md commands/*.md
sed -i 's/gsd-github-ci/bgsd-github-ci/g' workflows/*.md commands/*.md
sed -i 's/gsd-phase-researcher/bgsd-phase-researcher/g' workflows/*.md commands/*.md
sed -i 's/gsd-project-researcher/bgsd-project-researcher/g' workflows/*.md commands/*.md
sed -i 's/gsd-plan-checker/bgsd-plan-checker/g' workflows/*.md commands/*.md
```

IMPORTANT: Run agent-name replacements BEFORE generic `gsd-` replacements to avoid partial matches.
  </action>
  <verify>
grep -rl 'get-shit-done\|gsd-tools\|GSD_HOME\|gsd-executor\|gsd-planner\|gsd-verifier' workflows/ commands/ | head -20 — should return nothing
grep -c 'bgsd-oc' workflows/execute-plan.md — should return several matches
  </verify>
  <done>All 44 workflow files and 41 command files use bgsd-oc, bgsd-tools, __OPENCODE_CONFIG__/bgsd-oc naming consistently</done>
</task>

<task type="auto">
  <name>Task 2: Update templates, skills, and AGENTS.md</name>
  <files>templates/*.md (~20 files), skills/*/SKILL.md (~28 directories), AGENTS.md</files>
  <action>
**Templates (20 files, ~20 references):**
1. `get-shit-done` -> `bgsd-oc` (config path references)
2. `gsd-tools` -> `bgsd-tools` (if any CLI references)
3. `GSD_HOME` -> `__OPENCODE_CONFIG__/bgsd-oc` (if any)

```bash
sed -i 's/get-shit-done/bgsd-oc/g' templates/*.md
sed -i 's/gsd-tools/bgsd-tools/g' templates/*.md
sed -i 's/GSD_HOME/__OPENCODE_CONFIG__/bgsd-oc/g' templates/*.md
```

**Skills (~28 directories, ~57 references):**
Skills are in `skills/*/SKILL.md` format. Update all SKILL.md files:
1. `gsd-tools` -> `bgsd-tools` (~36 matches)
2. `GSD_HOME` -> `__OPENCODE_CONFIG__/bgsd-oc` (~20 matches)
3. `gsd_home` -> `bgsd_home` (lowercase var refs)
4. `get-shit-done` -> `bgsd-oc` (~1 match)
5. Agent name references where present

```bash
find skills/ -name 'SKILL.md' -exec sed -i 's/gsd-tools/bgsd-tools/g; s/GSD_HOME/__OPENCODE_CONFIG__/bgsd-oc/g; s/gsd_home/bgsd_home/g; s/get-shit-done/bgsd-oc/g' {} +
```

Also update any other .md files in skills/ directories (reference files, etc.).

**AGENTS.md:**
1. `gsd-tools` -> `bgsd-tools` (CLI references)
2. `get-shit-done` -> `bgsd-oc` (config path references)
3. `gsd-tools.cjs` -> `bgsd-tools.cjs` (binary name)
4. Update version reference if still saying "v8.0" to current state

```bash
sed -i 's/gsd-tools/bgsd-tools/g; s/get-shit-done/bgsd-oc/g' AGENTS.md
```
  </action>
  <verify>
grep -rl 'get-shit-done\|gsd-tools\|GSD_HOME' templates/ — should return nothing
find skills/ -name '*.md' -exec grep -l 'gsd-tools\|GSD_HOME\|gsd_home\|get-shit-done' {} + — should return nothing
grep -c 'gsd-tools\|get-shit-done' AGENTS.md — should return 0
  </verify>
  <done>All template, skill, and AGENTS.md files use new bgsd-* naming consistently</done>
</task>

</tasks>

<verification>
- `grep -r 'gsd-tools\|get-shit-done\|GSD_HOME\|GSD_DEBUG\|GSD_PROFILE\|gsd-executor\|gsd-planner\|gsd-verifier' workflows/ commands/ templates/ skills/ AGENTS.md` returns zero matches
- Spot-check 3-5 files to verify replacements look correct (no broken paths, no partial replacements)
</verification>

<success_criteria>
1. Zero remaining old-pattern references in workflows/, commands/, templates/, skills/, AGENTS.md
2. All replacements are semantically correct (paths resolve, binary names match)
3. No files corrupted by bulk replacement
</success_criteria>

<output>
After completion, create `.planning/phases/72-rebrand/72-03-SUMMARY.md`
</output>
