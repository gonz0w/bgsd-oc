---
phase: 72-rebrand
plan: 02
type: execute
wave: 2
depends_on: [72-01]
files_modified:
  - agents/gsd-codebase-mapper.md
  - agents/gsd-debugger.md
  - agents/gsd-executor.md
  - agents/gsd-github-ci.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-plan-checker.md
  - agents/gsd-planner.md
  - agents/gsd-project-researcher.md
  - agents/gsd-roadmapper.md
  - agents/gsd-verifier.md
  - deploy.sh
  - install.js
autonomous: true
requirements: [RBND-06, RBND-07]
outcome_ids: []

must_haves:
  truths:
    - "All agent files are named bgsd-*.md (no gsd-*.md files remain)"
    - "Every agent PATH SETUP resolves __OPENCODE_CONFIG__/bgsd-oc from bgsd-oc directory"
    - "Every agent uses __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs for CLI invocations"
    - "install.js detects existing get-shit-done install and migrates to bgsd-oc"
    - "install.js cleans up old gsd-*.md agent files during migration"
    - "deploy.sh deploys to bgsd-oc destination path"
  artifacts:
    - path: "agents/bgsd-planner.md"
      provides: "Renamed planner agent with __OPENCODE_CONFIG__/bgsd-oc path setup"
    - path: "agents/bgsd-executor.md"
      provides: "Renamed executor agent with __OPENCODE_CONFIG__/bgsd-oc path setup"
    - path: "agents/bgsd-verifier.md"
      provides: "Renamed verifier agent with __OPENCODE_CONFIG__/bgsd-oc path setup"
    - path: "install.js"
      provides: "Migration logic for get-shit-done -> bgsd-oc"
    - path: "deploy.sh"
      provides: "Deploy destination updated to bgsd-oc"
  key_links:
    - from: "install.js"
      to: "~/.config/opencode/bgsd-oc/"
      via: "migration logic (copy + delete old)"
      pattern: "bgsd-oc"
    - from: "deploy.sh"
      to: "~/.config/opencode/bgsd-oc/"
      via: "DEST variable"
      pattern: "bgsd-oc"
    - from: "agents/bgsd-*.md"
      to: "__OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs"
      via: "PATH SETUP block"
      pattern: "__OPENCODE_CONFIG__/bgsd-oc.*bgsd-tools"
---

<objective>
Rename all 10 agent files from gsd-*.md to bgsd-*.md, update their internal references, add migration logic to install.js, and update deploy.sh paths.

Purpose: Agents are the next dependency layer — they reference the CLI binary and config paths renamed in Plan 01. install.js migration logic (RBND-07) ensures existing users transition seamlessly. deploy.sh must target the new path for dev workflow.
Output: 10 renamed agent files, install.js with migration, deploy.sh with new paths
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-rebrand/72-RESEARCH.md
@.planning/phases/72-rebrand/72-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename agent files and update internal references</name>
  <files>agents/gsd-codebase-mapper.md, agents/gsd-debugger.md, agents/gsd-executor.md, agents/gsd-github-ci.md, agents/gsd-phase-researcher.md, agents/gsd-plan-checker.md, agents/gsd-planner.md, agents/gsd-project-researcher.md, agents/gsd-roadmapper.md, agents/gsd-verifier.md</files>
  <action>
For each of the 10 agent files:

1. **Rename** using `git mv agents/gsd-{name}.md agents/bgsd-{name}.md` to preserve git history
2. **Update PATH SETUP** in each file:
   - `GSD_HOME=$(ls -d $HOME/.config/*/get-shit-done 2>/dev/null | head -1)` -> `__OPENCODE_CONFIG__/bgsd-oc=$(ls -d $HOME/.config/*/bgsd-oc 2>/dev/null | head -1)`
   - All `$GSD_HOME` references -> `$__OPENCODE_CONFIG__/bgsd-oc`
   - All `gsd-tools.cjs` references -> `bgsd-tools.cjs`
   - All `get-shit-done` references -> `bgsd-oc`
3. **Update agent cross-references** within each file:
   - `gsd-executor` -> `bgsd-executor`, `gsd-planner` -> `bgsd-planner`, etc.
   - `gsd-verifier` -> `bgsd-verifier`, `gsd-debugger` -> `bgsd-debugger`, etc.

Agent files to rename (all 10):
- gsd-codebase-mapper.md -> bgsd-codebase-mapper.md
- gsd-debugger.md -> bgsd-debugger.md
- gsd-executor.md -> bgsd-executor.md
- gsd-github-ci.md -> bgsd-github-ci.md
- gsd-phase-researcher.md -> bgsd-phase-researcher.md
- gsd-plan-checker.md -> bgsd-plan-checker.md
- gsd-planner.md -> bgsd-planner.md
- gsd-project-researcher.md -> bgsd-project-researcher.md
- gsd-roadmapper.md -> bgsd-roadmapper.md
- gsd-verifier.md -> bgsd-verifier.md
  </action>
  <verify>
ls agents/gsd-*.md 2>/dev/null — should return nothing (no old files)
ls agents/bgsd-*.md | wc -l — should return 10
grep -l 'get-shit-done\|GSD_HOME\|gsd-tools' agents/bgsd-*.md — should return nothing
grep -c '__OPENCODE_CONFIG__/bgsd-oc' agents/bgsd-executor.md — should return several matches
  </verify>
  <done>All 10 agent files renamed to bgsd-*.md with all internal references updated to __OPENCODE_CONFIG__/bgsd-oc, bgsd-tools.cjs, bgsd-oc, and bgsd-{agent-name} patterns</done>
</task>

<task type="auto">
  <name>Task 2: Add migration logic to install.js and update deploy.sh</name>
  <files>install.js, deploy.sh</files>
  <action>
**install.js:**
1. Update `DEST` path from `get-shit-done` to `bgsd-oc`:
   `join(homedir(), ".config", "opencode", "bgsd-oc")`
2. Add migration logic BEFORE the main install logic:
   - Check if old `get-shit-done` directory exists AND new `bgsd-oc` doesn't
   - If so: `cpSync(oldDest, newDest, { recursive: true })` then `rmSync(oldDest, { recursive: true, force: true })`
   - Log migration steps to console
3. Add old agent file cleanup during install:
   - After deploying new agents, scan for `gsd-*.md` files in agent dir
   - Remove any old `gsd-*.md` agent files that have a corresponding `bgsd-*.md`
4. Update uninstall pattern:
   - Remove both `gsd-*.md` AND `bgsd-*.md` agent files
5. Update all `gsd-tools` references -> `bgsd-tools`

**deploy.sh:**
1. Update `DEST=` path from `get-shit-done` to `bgsd-oc`
2. Update agent glob from `gsd-*.md` to `bgsd-*.md`
3. Update `gsd-tools.cjs` -> `bgsd-tools.cjs` references
4. Add cleanup: remove old `gsd-*.md` agent files from deploy target after deploying new ones
5. Update any `get-shit-done` path references -> `bgsd-oc`

See 72-RESEARCH.md Pattern 5 for migration code example.
  </action>
  <verify>
grep -c 'get-shit-done\|gsd-tools\|gsd-\*\.md' install.js — should return 0 (except migration detection logic which references old name intentionally)
grep 'bgsd-oc' install.js — should show new config path
grep 'get-shit-done' install.js — should only appear in migration detection context
grep -c 'get-shit-done' deploy.sh — should return 0
grep 'DEST=.*bgsd-oc' deploy.sh — should match
  </verify>
  <done>install.js has migration logic that detects get-shit-done and moves to bgsd-oc, deploy.sh targets bgsd-oc path, both clean up old agent files</done>
</task>

</tasks>

<verification>
- `ls agents/bgsd-*.md | wc -l` returns 10
- `ls agents/gsd-*.md 2>/dev/null | wc -l` returns 0
- `grep -r 'GSD_HOME\|gsd-tools\.cjs' agents/*.md | grep -v BGSD | grep -v bgsd` returns zero matches
- install.js migration logic handles: (a) fresh install, (b) old install migration, (c) agent file cleanup
- deploy.sh DEST points to bgsd-oc
</verification>

<success_criteria>
1. All 10 agent files are renamed to bgsd-*.md with updated internal references
2. install.js detects existing get-shit-done installs and migrates automatically
3. deploy.sh deploys to ~/.config/opencode/bgsd-oc/
4. No gsd-*.md agent files remain in agents/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/72-rebrand/72-02-SUMMARY.md`
</output>
