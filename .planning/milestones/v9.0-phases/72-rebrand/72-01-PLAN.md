---
phase: 72-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/output.js
  - src/lib/profiler.js
  - src/router.js
  - src/commands/agent.js
  - src/commands/features.js
  - src/commands/codebase.js
  - src/commands/profiler.js
  - src/commands/trajectory.js
  - src/commands/worktree.js
  - src/commands/verify.js
  - src/lib/constants.js
  - src/plugin/index.js
  - build.cjs
  - package.json
autonomous: true
requirements: [RBND-01, RBND-02, RBND-03, RBND-04, RBND-05]
outcome_ids: []

must_haves:
  truths:
    - "process.env.BGSD_DEBUG triggers debug output in CLI source (GSD_DEBUG no longer recognized)"
    - "process.env.BGSD_PROFILE enables profiling (GSD_PROFILE no longer recognized)"
    - "process.env.BGSD_HOME resolves config home (GSD_HOME no longer recognized)"
    - "Config path resolves to bgsd-oc, not get-shit-done"
    - "npm run build produces bin/bgsd-tools.cjs (not bin/gsd-tools.cjs)"
    - "All usage strings in constants.js show bgsd-tools"
  artifacts:
    - path: "src/lib/output.js"
      provides: "BGSD_DEBUG env check and [BGSD_DEBUG] prefix"
    - path: "src/lib/profiler.js"
      provides: "BGSD_DEBUG and BGSD_PROFILE env checks"
    - path: "src/router.js"
      provides: "BGSD_PROFILE check and bgsd-tools usage"
    - path: "src/commands/agent.js"
      provides: "BGSD_HOME resolution and bgsd-oc config path"
    - path: "src/lib/constants.js"
      provides: "All usage strings reference bgsd-tools"
    - path: "build.cjs"
      provides: "outfile: bin/bgsd-tools.cjs"
    - path: "bin/bgsd-tools.cjs"
      provides: "Built CLI binary with new name"
  key_links:
    - from: "src/lib/output.js"
      to: "process.env.BGSD_DEBUG"
      via: "env check in debugLog()"
      pattern: "process\\.env\\.BGSD_DEBUG"
    - from: "build.cjs"
      to: "bin/bgsd-tools.cjs"
      via: "esbuild outfile"
      pattern: "bgsd-tools\\.cjs"
---

<objective>
Rename all environment variables, config paths, CLI binary name, and usage strings in source code and build pipeline — the foundation that all other plans depend on.

Purpose: Source code is the foundation of the rename. Build pipeline produces the new binary. All downstream renames (agents, workflows, commands, tests) depend on source + build being correct first.
Output: Updated src/ files, build.cjs, package.json; `npm run build` produces `bin/bgsd-tools.cjs`
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-rebrand/72-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename environment variables and config paths in source</name>
  <files>src/lib/output.js, src/lib/profiler.js, src/router.js, src/commands/agent.js, src/commands/features.js, src/commands/codebase.js, src/commands/profiler.js, src/commands/trajectory.js, src/commands/worktree.js, src/commands/verify.js, src/lib/constants.js, src/plugin/index.js</files>
  <action>
Rename all old naming patterns to new in source files:

1. **src/lib/output.js** — `GSD_DEBUG` -> `BGSD_DEBUG` (env check AND `[GSD_DEBUG]` prefix string, ~2 lines)
2. **src/lib/profiler.js** — `GSD_DEBUG` -> `BGSD_DEBUG` (~2 lines), `GSD_PROFILE` -> `BGSD_PROFILE` (~3 lines), usage comment `gsd-tools` -> `bgsd-tools`
3. **src/router.js** — `GSD_PROFILE` -> `BGSD_PROFILE` (~2 lines), `gsd-tools` -> `bgsd-tools` usage strings (~5 lines)
4. **src/commands/agent.js** — `GSD_HOME` -> `BGSD_HOME` (~7 lines), `get-shit-done` -> `bgsd-oc` (~1 line)
5. **src/commands/features.js** — `get-shit-done` -> `bgsd-oc` (~1 line), `gsd-tools` -> `bgsd-tools` (~1 line)
6. **src/commands/codebase.js** — `gsd-tools` -> `bgsd-tools` (~1 line)
7. **src/commands/profiler.js** — `gsd-tools` -> `bgsd-tools` (~7 lines)
8. **src/commands/trajectory.js** — `gsd-tools` -> `bgsd-tools` (~2 lines)
9. **src/commands/worktree.js** — `gsd-tools` -> `bgsd-tools` (~4 lines)
10. **src/commands/verify.js** — `gsd-tools` -> `bgsd-tools` (~2 lines)
11. **src/lib/constants.js** — `gsd-tools` -> `bgsd-tools` globally (~149 usage strings). Use replaceAll.
12. **src/plugin/index.js** — `get-shit-done` -> `bgsd-oc` (config path), `GSD_HOME` -> `BGSD_HOME` (~3 lines), variable name `gsdHome` -> `bgsdHome`

CRITICAL: In output.js, change BOTH the env check (`process.env.GSD_DEBUG`) AND the output format string (`[GSD_DEBUG]`) — tests assert the prefix format.
  </action>
  <verify>
grep -r 'GSD_HOME\|GSD_DEBUG\|GSD_PROFILE' src/ --include='*.js' | grep -v BGSD | grep -v node_modules — should return zero matches
grep -r 'get-shit-done' src/ --include='*.js' — should return zero matches
grep -c 'gsd-tools' src/lib/constants.js — should return 0
  </verify>
  <done>All env vars use BGSD_ prefix, all config paths use bgsd-oc, all usage strings show bgsd-tools in src/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Update build pipeline and package.json, rebuild</name>
  <files>build.cjs, package.json</files>
  <action>
1. **build.cjs** — Update all references:
   - `outfile: 'bin/gsd-tools.cjs'` -> `outfile: 'bin/bgsd-tools.cjs'` (main CLI build target)
   - Agent filter: `name.startsWith('gsd-')` -> `name.startsWith('bgsd-')` (manifest generation)
   - Any other `gsd-tools` references -> `bgsd-tools`
   - Any `get-shit-done` references -> `bgsd-oc`

2. **package.json** — Update:
   - `"name": "get-shit-done-oc"` -> `"name": "bgsd-oc"`
   - `"bin": { "get-shit-done-oc": "./install.js" }` -> `"bin": { "bgsd-oc": "./install.js" }`
   - `"test": "node --test bin/gsd-tools.test.cjs"` -> `"test": "node --test bin/bgsd-tools.test.cjs"`
   - `"files"` array: `"bin/gsd-tools.cjs"` -> `"bin/bgsd-tools.cjs"`

3. Run `npm run build` to verify the build pipeline produces `bin/bgsd-tools.cjs`

NOTE: The old bin/gsd-tools.cjs can be removed after the build succeeds. The test file rename happens in Plan 04.
  </action>
  <verify>
ls -la bin/bgsd-tools.cjs — file exists
node bin/bgsd-tools.cjs --help — produces help output with bgsd-tools in usage
grep -c 'gsd-tools\|get-shit-done' build.cjs — should return 0
grep -c 'gsd-tools\|get-shit-done' package.json — should return 0
  </verify>
  <done>build.cjs produces bin/bgsd-tools.cjs, package.json uses bgsd-oc naming throughout, npm run build succeeds</done>
</task>

</tasks>

<verification>
- `grep -r 'GSD_HOME\|GSD_DEBUG\|GSD_PROFILE' src/ --include='*.js' | grep -v BGSD` returns zero matches
- `grep -r 'get-shit-done\|gsd-tools' src/ --include='*.js'` returns zero matches
- `npm run build` succeeds and produces `bin/bgsd-tools.cjs`
- `node bin/bgsd-tools.cjs --help` runs without error
- `BGSD_DEBUG=1 node bin/bgsd-tools.cjs init:state 2>&1 | head -5` produces debug output with `[BGSD_DEBUG]` prefix
</verification>

<success_criteria>
1. All source files in src/ use BGSD_ prefix for environment variables
2. All source files reference bgsd-oc config path and bgsd-tools binary
3. Build pipeline produces bin/bgsd-tools.cjs successfully
4. CLI binary is functional with new name
</success_criteria>

<output>
After completion, create `.planning/phases/72-rebrand/72-01-SUMMARY.md`
</output>
