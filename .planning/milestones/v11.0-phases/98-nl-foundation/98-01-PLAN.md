---
phase: "98"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []
requirements:
  - "NL-01"
  - "NL-02"
must_haves:
  truths:
    - "User input is parsed into intent (plan/execute/verify/query)"
    - "Phase numbers, flags, and targets are extracted from natural language"
    - "Command registry maps phrases to canonical commands"
  artifacts:
    - path: "src/lib/nl/intent-classifier.js"
      provides: "Intent classification from user input"
      exports: ["classifyIntent"]
    - path: "src/lib/nl/parameter-extractor.js"
      provides: "Parameter extraction (phase, flags, target)"
      exports: ["extractPhaseNumber", "extractFlags", "extractTarget"]
    - path: "src/lib/nl/command-registry.js"
      provides: "Phrase-to-command mapping registry"
      exports: ["PHRASES", "ALIASES", "INTENTS"]
  key_links:
    - from: "intent-classifier.js"
      to: "command-registry.js"
      via: "INTENTS lookup"
      pattern: "INTENTS\\[.*\\]"
    - from: "parameter-extractor.js"
      to: "command-registry.js"
      via: "INTENTS reference for target extraction"
      pattern: "INTENTS"
---

# Phase 98 Plan 01: Core NL Parsing Infrastructure

## Objective

- **What:** Implement foundational natural language parsing modules — intent classification, parameter extraction, and command registry
- **Why:** Provides the core data structures and logic needed to parse natural language input into executable CLI commands
- **Output:** Three new modules in `src/lib/nl/` ready for fuzzy matching integration

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Requirements Addressed

| Requirement | Description |
|-------------|-------------|
| NL-01 | Intent classification — parse input into command types |
| NL-02 | Parameter extraction — extract phase numbers, flags, targets |

## Decisions (from context)

- Fuzzy threshold: 0.4 (moderate)
- Alias priority: short aliases first (backward compatibility)
- Learning: disabled (stateless)

## Tasks

<task type="auto">
  <name>Install Fuse.js dependency</name>
  <files>package.json</files>
  <action>
    Add fuse.js ^7.x to dependencies in package.json:
    - Run: npm install fuse.js@^7.0.0 --save
    - This provides fuzzy string matching for NL-03
  </action>
  <verify>npm list fuse.js shows version ^7.x</verify>
  <done>fuse.js is installed and importable via require('fuse.js')</done>
</task>

<task type="auto">
  <name>Create intent-classifier.js</name>
  <files>src/lib/nl/intent-classifier.js</files>
  <action>
    Create src/lib/nl/intent-classifier.js with classifyIntent function:
    - Define INTENTS mapping (plan, execute, verify, query) with keyword arrays
    - Implement classifyIntent(input) returning {intent, confidence}
    - Support exact match (confidence 1.0), keyword match (0.8), no match (0)
    - Export as CommonJS module for compatibility with bgsd-tools.cjs
  </action>
  <verify>node -e "const{classifyIntent}=require('./src/lib/nl/intent-classifier.js');console.log(classifyIntent('plan phase 5'))"</verify>
  <done>classifyIntent returns {intent:'plan',confidence:1} for "plan phase 5"</done>
</task>

<task type="auto">
  <name>Create parameter-extractor.js</name>
  <files>src/lib/nl/parameter-extractor.js</files>
  <action>
    Create src/lib/nl/parameter-extractor.js with extraction functions:
    - extractPhaseNumber(input): Match patterns like "phase 5", "p5", standalone 2-digit numbers
    - extractFlags(input): Extract --flag, -f, and keyword flags (force, verbose, debug, auto)
    - extractTarget(input): Extract remaining text after intent/command words
    - Return null for missing values (not errors)
  </action>
  <verify>node -e "const{extractPhaseNumber,extractFlags}=require('./src/lib/nl/parameter-extractor.js');console.log(extractPhaseNumber('plan phase 98 --force'))"</verify>
  <done>extractPhaseNumber returns 98, extractFlags returns ['force'] for "plan phase 98 --force"</done>
</task>

<task type="auto">
  <name>Create command-registry.js</name>
  <files>src/lib/nl/command-registry.js</files>
  <action>
    Create src/lib/nl/command-registry.js with registry data:
    - PHRASES: Array of {phrase, command, intent} objects (15-20 common phrases)
    - ALIASES: Short alias map (p→plan, e→execute, etc.) — extend existing jr map
    - INTENTS: Keyword arrays for each intent type
    - Include phrases like: "plan phase", "show progress", "execute phase", "verify work", etc.
  </action>
  <verify>node -e "const{PHRASES}=require('./src/lib/nl/command-registry.js');console.log(PHRASES.length)"</verify>
  <done>command-registry.js exports PHRASES (15+ entries), ALIASES, INTENTS</done>
</task>

<task type="auto">
  <name>Wire intent classifier into CLI</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
    Extend bgsd-tools.cjs to use the new NL modules:
    - Import the new modules at the top (lazy-require pattern)
    - Modify command parsing to first try NL input detection
    - Ensure backward compatibility: short aliases (p, e) still work
    - Keep existing oT function for suggestions, extend with new data
  </action>
  <verify>node bin/bgsd-tools.cjs --help | head -20</verify>
  <done>CLI still works, NL modules are wire-reachable but not yet active</done>
</task>

## Verification

Run manual tests:
- `classifyIntent("plan phase 5")` → {intent:'plan',confidence:1}
- `classifyIntent("run the tests")` → {intent:'execute',confidence:0.8}
- `extractPhaseNumber("plan phase 98")` → 98
- `extractFlags("--force --verbose")` → ['force','verbose']
- CLI startup: `node bin/bgsd-tools.cjs --help` works

## Success Criteria

- [ ] Fuse.js installed
- [ ] intent-classifier.js created and exportable
- [ ] parameter-extractor.js created with all three extractors
- [ ] command-registry.js created with 15+ phrases
- [ ] Existing CLI functionality preserved
- [ ] All tasks complete without checkpoints (fully autonomous)
