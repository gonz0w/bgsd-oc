---
phase: 101-rich-visualization
plan: 03
type: execute
wave: 2
depends_on: [101-01, 101-02]
files_modified: []
autonomous: true
requirements: [VIS-06]
user_setup: []

must_haves:
  truths:
    - "User can run dashboard command to see overview of key metrics in terminal"
    - "Dashboard displays progress, phase completion, and quality metrics"
    - "Dashboard supports keyboard navigation (arrows, enter, q)"
  artifacts:
    - path: "src/lib/viz/dashboard.js"
      provides: "Terminal dashboard rendering with keyboard handling"
      exports: ["renderDashboard", "createDashboard", "DashboardView"]
  key_links:
    - from: "src/lib/viz/dashboard.js"
      to: "src/lib/viz/index.js"
      via: "module export"
      pattern: "require.*dashboard"
---

<objective>
Create terminal dashboard with keyboard navigation for project metrics overview
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-rich-visualization/101-CONTEXT.md
@.planning/ROADMAP.md
@src/lib/viz/index.js
@src/lib/viz/progress.js
@src/lib/viz/milestone.js
@src/lib/viz/quality.js
@src/lib/viz/burndown.js
@src/lib/viz/sparkline.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create terminal dashboard module</name>
  <files>src/lib/viz/dashboard.js</files>
  <action>
Create src/lib/viz/dashboard.js with btop-style full-screen dashboard:
- renderDashboard(projectData, options): Render dashboard layout
  - Options: theme (auto/dark/light), refreshRate (ms)
  - Layout: Header with project name, 3-column grid for metrics
  - Sections: Progress (task completion %), Milestone (phase progress), Quality (grade)
  - Footer with keyboard hints: "←→ Navigate | Enter Expand | Q Quit"
- createDashboard(): Factory for dashboard instance with state
- DashboardView class:
  - render(): Main render loop, clears screen, draws UI, positions cursor
  - handleInput(key): Process arrow keys, enter, q
  - getMetricsSection(): Fetch current metrics from STATE.md/ROADMAP.md
  - getExpandedView(selected): Drill-down view when Enter pressed
Terminal handling:
  - Use readline for keyboard input (setRawMode)
  - Clear screen: process.stdout.write('\x1b[2J\x1b[H') 
  - Position cursor: \x1b[row;colH
  - Detect terminal size: process.stdout.columns-rows
  - If terminal too small (<80x24), exit with message
Follow tiered approach from context: better layouts on modern terminals.
  </action>
<verify>node -e "const {renderDashboard} = require('./src/lib/viz/dashboard'); const data = {milestone:'v11.0',progress:{total:10,completed:6},phases:{total:5,completed:3},quality:{grade:'A',score:95}}; console.log(renderDashboard(data, {width:80,height:24}));"</verify>
  <done>Dashboard module renders ASCII layout with metrics sections</done>
</task>

<task type="auto">
  <name>Task 2: Export dashboard module and verify all modules integrated</name>
  <files>src/lib/viz/index.js</files>
  <action>
Update src/lib/viz/index.js to export all Phase 101 modules:
  module.exports = {
    progress: require('./progress'),
    milestone: require('./milestone'),
    quality: require('./quality'),
    burndown: require('./burndown'),
    sparkline: require('./sparkline'),
    dashboard: require('./dashboard')
  };
Then verify all exports are present and functional.
  </action>
  <verify>node -e "const viz = require('./src/lib/viz'); console.log('Exports:', Object.keys(viz).join(', ')); console.log('All have render functions:', Object.values(viz).every(m => typeof m.render === 'function' || typeof m.renderChart === 'function' || typeof m.renderDashboard === 'function'));"</verify>
  <done>All 6 viz modules exported from index.js</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end verification of all three success criteria</name>
  <files></files>
  <action>
Run verification tests for all three Phase 101 success criteria:
1. Burndown chart: Verify ASCII burndown renders with ideal and actual lines
2. Sparkline: Verify velocity sparkline renders inline 
3. Dashboard: Verify dashboard layout renders with metrics

Create a test script that exercises all three and outputs confirmation.
  </action>
  <verify>node -e "
const viz = require('./src/lib/viz');
// Test burndown
const burndownData = viz.burndown.calculateBurndownData({totalTasks:10,completedTasks:4,totalDays:14,daysElapsed:7});
console.log('✓ Burndown chart:', viz.burndown.renderBurndownChart(burndownData).slice(0,50)+'...');

// Test sparkline  
const sessions = [{tasksCompleted:3},{tasksCompleted:5},{tasksCompleted:2},{tasksCompleted:7},{tasksCompleted:4}];
const trend = viz.sparkline.calculateVelocityTrend(sessions);
console.log('✓ Sparkline:', viz.sparkline.renderSparkline(trend));

// Test dashboard
const dashboardData = {milestone:'v11.0',progress:{total:10,completed:6},phases:{total:5,completed:3},quality:{grade:'A',score:95}};
console.log('✓ Dashboard renders:', viz.dashboard.renderDashboard(dashboardData).slice(0,50)+'...');
console.log('\\nAll three success criteria verified!');
"</verify>
  <done>All three success criteria (burndown, sparkline, dashboard) verified working</done>
</task>

</tasks>

<verification>
- Run each visualization module directly to verify output
- Verify keyboard handling in dashboard (arrows, enter, q)
- Check terminal size detection and graceful exit for small terminals
- Verify all modules integrate properly through viz index
</verification>

<success_criteria>
All three Phase 101 success criteria verified: burndown chart, sparkline, and dashboard work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/101-rich-visualization/101-03-SUMMARY.md`
</output>
