---
phase: 101-rich-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VIS-05]
user_setup: []

must_haves:
  truths:
    - "Velocity sparkline displays inline with session output showing trend"
    - "Sparkline shows last 7 sessions as trend indicator"
    - "Sparkline uses Unicode or ASCII characters for visual bars"
  artifacts:
    - path: "src/lib/viz/sparkline.js"
      provides: "Velocity sparkline rendering"
      exports: ["renderSparkline", "calculateVelocityTrend"]
  key_links:
    - from: "src/lib/viz/sparkline.js"
      to: "src/lib/viz/index.js"
      via: "module export"
      pattern: "require.*sparkline"
---

<objective>
Create velocity sparkline visualization module for session trend display
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-rich-visualization/101-CONTEXT.md
@.planning/ROADMAP.md
@src/lib/viz/progress.js
@src/lib/viz/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sparkline visualization module</name>
  <files>src/lib/viz/sparkline.js</files>
  <action>
Create src/lib/viz/sparkline.js with:
- calculateVelocityTrend(sessionHistory): Compute velocity from session data
  - Input: Array of {tasksCompleted, duration} objects
  - Output: Array of normalized values (0-1 scale) for sparkline
- renderSparkline(data, options): Render inline sparkline
  - Options: width (default 10), showValues (default false), chars (customizable)
  - Use Unicode block chars: ▁▂▃▄▅▆▇█ (or ASCII: _-=+#)
  - Min 3 bars, max 15 bars
  - Compact inline format: "▁▃▅▇▄▆" without labels
  - Expanded format: includes current value label
Follow pattern from existing viz modules.
  </action>
  <verify>node -e "const {renderSparkline, calculateVelocityTrend} = require('./src/lib/viz/sparkline'); const sessions = [{tasksCompleted:3,duration:15},{tasksCompleted:5,duration:20},{tasksCompleted:2,duration:12},{tasksCompleted:7,duration:25},{tasksCompleted:4,duration:18}]; const trend = calculateVelocityTrend(sessions); console.log('Inline:', renderSparkline(trend)); console.log('Expanded:', renderSparkline(trend, {showValues:true}));"</verify>
  <done>Sparkline renders as inline visual bars showing velocity trend</done>
</task>

<task type="auto">
  <name>Task 2: Export sparkline module from viz index</name>
  <files>src/lib/viz/index.js</files>
  <action>
Update src/lib/viz/index.js to export the sparkline module:
  module.exports = {
    progress: require('./progress'),
    milestone: require('./milestone'),
    quality: require('./quality'),
    burndown: require('./burndown'),
    sparkline: require('./sparkline')
  };
  </action>
  <verify>node -e "const viz = require('./src/lib/viz'); console.log(Object.keys(viz));"</verify>
  <done>viz index exports sparkline module</done>
</task>

</tasks>

<verification>
- Run sparkline module directly to verify inline visualization
- Verify Unicode block characters render in terminal
- Check expanded view shows numeric values
</verification>

<success_criteria>
Sparkline visualization module created and exported. Shows velocity trend inline with output.
</success_criteria>

<output>
After completion, create `.planning/phases/101-rich-visualization/101-02-SUMMARY.md`
</output>
