---
phase: 101-rich-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VIS-04]
user_setup: []

must_haves:
  truths:
    - "User can run command to view ASCII burndown chart showing planned vs actual progress"
    - "Burndown chart displays milestone timeline on X-axis and task count on Y-axis"
    - "Chart shows both ideal burndown line and actual progress line"
  artifacts:
    - path: "src/lib/viz/burndown.js"
      provides: "Burndown chart rendering"
      exports: ["renderBurndownChart", "calculateBurndownData"]
  key_links:
    - from: "src/lib/viz/burndown.js"
      to: "src/lib/viz/index.js"
      via: "module export"
      pattern: "require.*burndown"
---

<objective>
Create ASCII burndown chart visualization module for milestone progress tracking
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-rich-visualization/101-CONTEXT.md
@.planning/ROADMAP.md
@src/lib/viz/progress.js
@src/lib/viz/milestone.js
@src/lib/viz/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create burndown chart visualization module</name>
  <files>src/lib/viz/burndown.js</files>
  <action>
Create src/lib/viz/burndown.js with:
- calculateBurndownData(milestoneData): Compute ideal and actual burndown lines from milestone phases
  - Input: { totalTasks, completedTasks, totalDays, daysElapsed, phases: [{completed, total}, ...] }
  - Output: { ideal: [{day, remaining}], actual: [{day, remaining}], remainingDays }
- renderBurndownChart(burndownData, options): Render ASCII chart
  - Options: width (default 50), height (default 15), showLegend (default true)
  - Use Unicode box-drawing chars (┌┐└┘│─) with ASCII fallback (+++|)
  - Y-axis: remaining tasks (0 to max), X-axis: timeline (days)
  - Two lines: ideal (dashed) and actual (solid)
  - Include axis labels and legend
Follow pattern from progress.js and milestone.js for module structure.
  </action>
  <verify>node -e "const {renderBurndownChart, calculateBurndownData} = require('./src/lib/viz/burndown'); const data = calculateBurndownData({totalTasks: 10, completedTasks: 4, totalDays: 14, daysElapsed: 7, phases:[{completed:2,total:3},{completed:2,total:3},{completed:0,total:4}]}); console.log(renderBurndownChart(data));"</verify>
  <done>Burndown chart renders with both ideal and actual lines visible</done>
</task>

<task type="auto">
  <name>Task 2: Export burndown module from viz index</name>
  <files>src/lib/viz/index.js</files>
  <action>
Update src/lib/viz/index.js to export the burndown module alongside existing modules:
  module.exports = {
    progress: require('./progress'),
    milestone: require('./milestone'),
    quality: require('./quality'),
    burndown: require('./burndown')
  };
  </action>
  <verify>node -e "const viz = require('./src/lib/viz'); console.log(Object.keys(viz));"</verify>
  <done>viz index exports burndown module</done>
</task>

</tasks>

<verification>
- Run burndown module directly to verify ASCII chart renders
- Verify chart shows two lines: ideal (planned pace) and actual (current progress)
- Check graceful fallback to ASCII if Unicode unavailable
</verification>

<success_criteria>
Burndown chart visualization module created and exported. User can run command to view ASCII burndown chart.
</success_criteria>

<output>
After completion, create `.planning/phases/101-rich-visualization/101-01-SUMMARY.md`
</output>
