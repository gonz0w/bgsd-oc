---
phase: "100"
name: "visualization-core"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "100-01"
files_modified:
  - "src/lib/viz/index.js"
  - "bin/bgsd-tools.cjs"
autonomous: true
requirements:
  - "VIS-01"
  - "VIS-02"
  - "VIS-03"
user_setup: []
must_haves:
  truths:
    - "User sees ASCII progress bar showing task completion in current phase (e.g., [███░░░] 3/8 tasks)"
    - "User sees milestone completion percentage with visual indicator (e.g., Milestone: 65% complete)"
    - "User sees quality grades displayed with visual indicators (e.g., Quality: A ████████)"
  artifacts:
    - path: "src/lib/viz/index.js"
      provides: "Unified visualization API"
      exports: ["progress", "milestone", "quality"]
  key_links:
    - from: "src/lib/viz/index.js"
      to: "src/lib/viz/progress.js"
      via: "Requires progress module"
      pattern: "require.*progress"
    - from: "src/lib/viz/index.js"
      to: "src/lib/viz/milestone.js"
      via: "Requires milestone module"
      pattern: "require.*milestone"
    - from: "src/lib/viz/index.js"
      to: "src/lib/viz/quality.js"
      via: "Requires quality module"
      pattern: "require.*quality"
---

<objective>
Create unified visualization API and integrate with the bGSD CLI.

Purpose: Expose all visualization functions through a single module entry point and verify end-to-end functionality.

Output: Unified viz/index.js module with end-to-end testing
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-visualization-core/100-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified viz API index</name>
  <files>
    - src/lib/viz/index.js
  </files>
  <action>
Create src/lib/viz/index.js that exports all visualization functions in a unified API:

```js
module.exports = {
  progress: require('./progress'),
  milestone: require('./milestone'),
  quality: require('./quality')
};
```

This allows single require('./lib/viz') to access all visualization functions.
  </action>
  <verify>
node -e "const viz = require('./src/lib/viz'); console.log(viz.progress.renderTaskProgress(3,8)); console.log(viz.milestone.renderMilestoneProgress(65,'v11.0')); console.log(viz.quality.renderQualityScore(95));"
  </verify>
  <done>
- viz.progress.renderTaskProgress(3, 8) works
- viz.milestone.renderMilestoneProgress(65, 'v11.0') works
- viz.quality.renderQualityScore(95) works
- All three exported from single require('./lib/viz')
  </done>
</task>

<task type="auto">
  <name>Task 2: Test end-to-end visualization output</name>
  <files>
    - bin/bgsd-tools.cjs
  </files>
  <action>
Run a test to verify all visualization components work together:

1. Import the viz module in the CLI context
2. Verify the output format matches success criteria exactly:
   - "[███░░░] 3/8 tasks" - task progress
   - "Milestone: 65% complete" - milestone progress  
   - "Quality: A ████████" - quality grade with bar

Test all three success criteria from ROADMAP.md.
  </verify>
  <verify>
node -e "const viz = require('./src/lib/viz'); console.log('=== Task Progress ==='); console.log(viz.progress.renderTaskProgress(3, 8)); console.log('=== Milestone Progress ==='); console.log(viz.milestone.renderMilestoneProgress(65, 'v11.0')); console.log('=== Quality Score ==='); console.log(viz.quality.renderQualityScore(95));"
  </verify>
  <done>
- All three success criteria verified in terminal output:
  - Task: "[███░░░] 3/8 tasks"
  - Milestone: "Milestone v11.0: 65% complete [...]"
  - Quality: "Quality: A ████████ (95%)"
- ASCII characters render correctly in terminal
  </done>
</task>

</tasks>

<verification>
All three visualization components render correct ASCII output that matches ROADMAP.md success criteria exactly.
</verification>

<success_criteria>
Plan 100-02 is complete when:
- [x] src/lib/viz/index.js provides unified API
- [x] All three success criteria from ROADMAP.md verified:
  - Task progress: "[███░░░] 3/8 tasks"
  - Milestone: "Milestone: 65% complete"
  - Quality: "Quality: A ████████"
</success_criteria>

<output>
After completion, create `.planning/phases/100-visualization-core/100-02-SUMMARY.md`
</output>
