---
phase: "100"
name: "visualization-core"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "src/lib/viz/progress.js"
  - "src/lib/viz/quality.js"
  - "src/lib/viz/milestone.js"
autonomous: true
requirements:
  - "VIS-01"
  - "VIS-02"
  - "VIS-03"
user_setup: []
must_haves:
  truths:
    - "User sees ASCII progress bar showing task completion in current phase (e.g., [███░░░] 3/8 tasks)"
    - "User sees milestone completion percentage with visual indicator (e.g., Milestone: 65% complete)"
    - "User sees quality grades displayed with visual indicators (e.g., Quality: A ████████)"
  artifacts:
    - path: "src/lib/viz/progress.js"
      provides: "ASCII progress bar rendering for tasks and phases"
      exports: ["renderProgressBar", "renderTaskProgress", "renderPhaseProgress"]
    - path: "src/lib/viz/milestone.js"
      provides: "Milestone progress visualization"
      exports: ["renderMilestoneProgress", "calculateMilestonePercentage", "renderMilestoneSummary"]
    - path: "src/lib/viz/quality.js"
      provides: "Quality score visualization"
      exports: ["renderQualityScore", "getQualityGrade", "renderQualityBar"]
  key_links:
    - from: "src/lib/viz/milestone.js"
      to: "src/lib/viz/progress.js"
      via: "Uses progress bar in milestone rendering"
      pattern: "renderProgressBar"
    - from: "src/lib/viz/quality.js"
      to: "src/lib/viz/progress.js"
      via: "Uses progress bar for quality bar display"
      pattern: "renderProgressBar"
---

<objective>
Create core visualization modules for the bGSD plugin — progress bars, milestone progress indicators, and quality score displays.

Purpose: Enable users to see visual progress indicators for tasks, phases, and quality metrics in their terminal. This is the foundation for future rich visualization (Phase 101-102).

Output: Three viz modules: progress.js, milestone.js, quality.js
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/99-conversational-planning/99-01-SUMMARY.md
@src/lib/output.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create progress bar visualization module</name>
  <files>
    - src/lib/viz/progress.js
  </files>
  <action>
Create src/lib/viz/progress.js with ASCII progress bar rendering.

Implement:
- renderProgressBar(current, total, options) - Renders ASCII progress bar with customizable chars
  - Default: filled='█', empty='░', width=10
  - Example output: "[███░░░░░░] 30%"
  - Handle edge cases: 0, 100%, partial percentages
- renderTaskProgress(completed, total) - Task-specific progress with format "[███░░░] 3/8 tasks"
- renderPhaseProgress(completed, total) - Phase-specific with percentage

Use module.exports for CommonJS compatibility. Keep functions pure and testable.
  </action>
  <verify>
node -e "const p = require('./src/lib/viz/progress'); console.log(p.renderProgressBar(3, 8)); console.log(p.renderTaskProgress(3, 8));"
  </verify>
  <done>
- renderProgressBar(3, 8) outputs "[███░░░░░░] 37.5%"
- renderTaskProgress(3, 8) outputs "[███░░░] 3/8 tasks"
- Edge cases handled: 0/0 shows "empty", 8/8 shows full bar
  </done>
</task>

<task type="auto">
  <name>Task 2: Create milestone progress visualization</name>
  <files>
    - src/lib/viz/milestone.js
  </files>
  <action>
Create src/lib/viz/milestone.js with milestone progress visualization.

Implement:
- calculateMilestonePercentage(completedPhases, totalPhases) - Returns percentage number
- renderMilestoneProgress(percentage, label) - Renders milestone bar with format "Milestone v11.0: 65% complete [█████████░░░░░░]"
- renderMilestoneSummary(name, completed, total) - Full summary with phases count

Integrate with progress.js - use renderProgressBar internally for consistent styling.
  </action>
  <verify>
node -e "const m = require('./src/lib/viz/milestone'); console.log(m.renderMilestoneProgress(65, 'v11.0')); console.log(m.renderMilestoneSummary('v11.0', 3, 5));"
  </verify>
  <done>
- renderMilestoneProgress(65, 'v11.0') outputs "Milestone v11.0: 65% complete [█████████░░░░░░]"
- renderMilestoneSummary('v11.0', 3, 5) outputs "Milestone v11.0: 3/5 phases complete (60%)"
  </done>
</task>

<task type="auto">
  <name>Task 3: Create quality score visualization</name>
  <files>
    - src/lib/viz/quality.js
  </files>
  <action>
Create src/lib/viz/quality.js with quality score visualization.

Implement:
- getQualityGrade(score) - Returns letter grade (A: 90+, B: 80+, C: 70+, D: 60+, F: <60)
- renderQualityBar(grade) - Renders quality bar like "Quality: A ████████" (8 bars for A,递减)
- renderQualityScore(score) - Combined: shows grade + bar + percentage

Quality grades: A (90-100), B (80-89), C (70-79), D (60-69), F (<60)
Bar length: A=8, B=7, C=5, D=3, F=1
  </action>
  <verify>
node -e "const q = require('./src/lib/viz/quality'); console.log(q.renderQualityScore(95)); console.log(q.renderQualityScore(72));"
  </verify>
  <done>
- renderQualityScore(95) outputs "Quality: A ████████ (95%)"
- renderQualityScore(72) outputs "Quality: C █████ (72%)"
- getQualityGrade(85) returns "B"
  </done>
</task>

</tasks>

<verification>
All visualization components render correct ASCII output:
1. Progress bar shows filled/empty characters in correct proportions
2. Milestone shows percentage and visual bar
3. Quality shows letter grade with appropriate bar length
</verification>

<success_criteria>
Plan 100-01 is complete when:
- [x] src/lib/viz/progress.js implements renderProgressBar, renderTaskProgress, renderPhaseProgress
- [x] src/lib/viz/milestone.js implements renderMilestoneProgress, renderMilestoneSummary
- [x] src/lib/viz/quality.js implements renderQualityScore, getQualityGrade, renderQualityBar
- [x] All three visualization modules export correct functions
</success_criteria>

<output>
After completion, create `.planning/phases/100-visualization-core/100-01-SUMMARY.md`
</output>
