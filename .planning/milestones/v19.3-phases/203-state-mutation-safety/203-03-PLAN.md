---
phase: 203-state-mutation-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 203-01
files_modified:
  - workflows/execute-phase.md
autonomous: true
requirements:
  - BUNDLE-02
verification_route: light
verification_route_reason: "CLI contract validation is structural check only; no testable behavior"
must_haves:
  truths:
    - "util:validate-commands --raw runs after any routing change to confirm CLI contract validity"
  artifacts:
    - path: "workflows/execute-phase.md"
      provides: "CLI contract validation after routing changes"
      contains: "util:validate-commands --raw"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "src/commands/misc/frontmatter.js"
      via: "util:validate-commands --raw call"
      pattern: "util:validate-commands"
---

<objective>
Wire util:validate-commands --raw after any routing change to catch CLI contract drift.

Purpose: BUNDLE-02 requires CLI contract validation after any routing change. This ensures subcommand integrity is maintained as the system evolves.

Output: Updated execute-phase workflow with explicit CLI validation step after routing changes.
</objective>

> **TDD Decision:** Skipped — CLI contract validation is a structural/command-integrity check, not testable business logic behavior.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-state-mutation-safety/203-CONTEXT.md
@.planning/phases/203-state-mutation-safety/203-RESEARCH.md
@workflows/execute-phase.md
@src/commands/misc/frontmatter.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add util:validate-commands --raw step after routing changes in execute-phase</name>
  <files>workflows/execute-phase.md</files>
  <action>
Add a step `cli_contract_validation` to execute-phase.md that runs after any wave that modifies routing or command discovery files.

Identify where routing changes occur in the phase. The validation should run:
- After any plan that touches routing files (bin/bgsd-tools.cjs, src/commands/, src/lib/commandDiscovery.js)
- Before verify_phase_goal

A reasonable placement is as a conditional step in the preflight section or as an explicit step after execute_waves if routing was modified.

The step:
```
step name="cli_contract_validation">
  Run util:validate-commands --raw to confirm CLI contract after routing changes:
  ```bash
  VALIDATE_OUTPUT=$(node __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs util:validate-commands --raw 2>&1)
  VALIDATE_EXIT=$?
  if [ $VALIDATE_EXIT -ne 0 ]; then
    echo "CLI CONTRACT DRIFT: $VALIDATE_OUTPUT"
    exit 1
  fi
  echo "CLI contract validation: PASS"
  ```
  If validation fails: exit 1, do not proceed to verify_phase_goal.
  ```

Per CONTEXT.md: Agent discretion for exact placement within execute-phase, but consistent with Phase 159 pattern.
  </action>
  <verify>rg "util:validate-commands" workflows/execute-phase.md</verify>
  <done>execute-phase.md has cli_contract_validation step that runs util:validate-commands --raw and fails closed on contract drift</done>
</task>

</tasks>

<verification>
Structural verification: verify cli_contract_validation step exists in execute-phase.md with correct util:validate-commands --raw call.
</verification>

<success_criteria>
- execute-phase.md has explicit CLI contract validation step with util:validate-commands --raw
- Step fails closed on validation failure
- Placement is consistent with Phase 159 pattern per CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/203-state-mutation-safety/203-03-SUMMARY.md`
</output>
