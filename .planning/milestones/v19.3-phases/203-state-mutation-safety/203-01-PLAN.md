---
phase: 203-state-mutation-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/planning-cache.js
  - src/lib/state-session-mutator.js
autonomous: true
requirements:
  - STATE-02
  - STATE-03
verification_route: light
verification_route_reason: "Infrastructure-only extension of existing transaction patterns; no new testable behavior introduced"
must_haves:
  truths:
    - "Non-sacred state mutations use batch transaction API when multiple bundles provided"
    - "Sacred stores (decisions, lessons, trajectories) always use single-write path, never batched"
    - "Batch transaction rolls back all writes on any failure"
  artifacts:
    - path: "src/lib/planning-cache.js"
      provides: "Batch transaction API for session bundles"
      contains: "storeSessionBundleBatch"
    - path: "src/lib/state-session-mutator.js"
      provides: "Sacred data guard on batch entry points"
      contains: "SACRED_STORES"
  key_links:
    - from: "src/lib/state-session-mutator.js"
      to: "src/lib/planning-cache.js"
      via: "storeSessionBundleBatch call"
      pattern: "storeSessionBundleBatch"
    - from: "src/lib/state-session-mutator.js"
      to: "src/commands/memory.js"
      via: "SACRED_STORES import"
      pattern: "SACRED_STORES"
---

<objective>
Add batch transaction API for non-sacred state mutations and enforce sacred data protection.

Purpose: Enable batch writes for non-critical state while guaranteeing sacred data (decisions, lessons, trajectories) always uses the canonical single-write path.

Output: Extended PlanningCache with storeSessionBundleBatch, sacred store guards in state-session-mutator.js.
</objective>

> **TDD Decision:** Skipped — Extension of existing transaction patterns (batchCheckFreshness, storeSessionBundle) with no new testable behavior; infrastructure-only work following established patterns.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-state-mutation-safety/203-CONTEXT.md
@.planning/phases/203-state-mutation-safety/203-RESEARCH.md
@src/lib/planning-cache.js
@src/lib/state-session-mutator.js
@src/commands/memory.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storeSessionBundleBatch to PlanningCache</name>
  <files>src/lib/planning-cache.js</files>
  <action>
Add `storeSessionBundleBatch(cwd, bundles)` method to the PlanningCache class following the existing `storeSessionBundle` pattern.

The method must:
- Accept `bundles` as an array of bundle objects
- Wrap all writes in a single BEGIN/COMMIT/ROLLBACK transaction
- Iterate each bundle and call the existing session write logic (state, decisions, blockers, continuity) per bundle
- Return `{ stored: true, count: bundles.length }` on success
- Return `null` on any failure (transaction rollback is automatic)
- Handle MapDatabase backend (return null immediately, no-op)

Implementation pattern: See existing `storeSessionBundle` (line 1214) and `batchCheckFreshness` (line 238) for transaction wrapping conventions.

Place the new method after `storeSessionBundle` (after line 1305).
  </action>
  <verify>rg "storeSessionBundleBatch" src/lib/planning-cache.js</verify>
  <done>PlanningCache has storeSessionBundleBatch method that accepts array of bundles and returns {stored, count} on success or null on failure</done>
</task>

<task type="auto">
  <name>Task 2: Add SACRED_STORES guard to state-session-mutator.js batch entry</name>
  <files>src/lib/state-session-mutator.js</files>
  <action>
Import `SACRED_STORES` from `src/commands/memory.js` at the top of state-session-mutator.js.

Add a `canBatch(store)` helper function that returns false if store is in SACRED_STORES, true otherwise. This guard ensures sacred data is never batched.

Update `applyStateSessionMutation` to check store type before batching — if the operation targets a sacred store, route to single-write path only. The sacred data boundary is: decisions, lessons, trajectories, requirements.

Note: `applyStateSessionMutation` currently handles single operations (patch, appendDecision, etc.). The batch guard applies when the batch API is called — future callers must check `canBatch(store)` before invoking batch operations on any store.
  </action>
  <verify>rg "SACRED_STORES" src/lib/state-session-mutator.js</verify>
  <done>SACRED_STORES guard exists in state-session-mutator.js preventing sacred data from being batched</done>
</task>

<task type="auto">
  <name>Task 3: Add batch write path to memory.js for non-sacred stores</name>
  <files>src/commands/memory.js</files>
  <action>
Extend `cmdMemoryWrite` in memory.js to support batch entry points for non-sacred stores.

For each write operation:
- If store is in SACRED_STORES (`['decisions', 'lessons', 'trajectories']`), use existing single-write path only — never batch
- If store is NOT sacred, batch-enabled writes may use the new storeSessionBundleBatch API

The sacred store check is:
```javascript
if (SACRED_STORES.includes(store)) {
  // Route to canonical single-write path — NEVER batch
  return singleWritePath(cwd, store, entry);
}
```

This enforces the locked decision: sacred data writes never batched, always canonical single-write path.
  </action>
  <verify>rg "SACRED_STORES.includes" src/commands/memory.js</verify>
  <done>memory.js batch write path includes sacred store guard preventing decisions, lessons, trajectories from being batched</done>
</task>

</tasks>

<verification>
Structural verification: verify storeSessionBundleBatch exists in planning-cache.js, SACRED_STORES guard exists in state-session-mutator.js and memory.js. No runtime tests needed — infrastructure extension only.
</verification>

<success_criteria>
- storeSessionBundleBatch method exists in PlanningCache with proper transaction wrapping
- SACRED_STORES guard prevents decisions, lessons, trajectories from being batched
- Batch writes for non-sacred stores (bookmarks, todos) use the batch transaction API
</success_criteria>

<output>
After completion, create `.planning/phases/203-state-mutation-safety/203-01-SUMMARY.md`
</output>
