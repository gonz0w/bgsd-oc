---
phase: 203-state-mutation-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 203-01
files_modified:
  - workflows/execute-plan.md
  - workflows/execute-phase.md
autonomous: true
requirements:
  - STATE-01
  - BUNDLE-01
verification_route: light
verification_route_reason: "Workflow integration (adding validation step + smoke test); no new testable business logic"
must_haves:
  truths:
    - "verify:state validate runs as a gating checkpoint after batched state writes in execute-plan"
    - "npm run build smoke test runs once after execute-phase completes all plans"
    - "Bundle parity failures cause execute-phase to fail closed"
  artifacts:
    - path: "workflows/execute-plan.md"
      provides: "State validation gate after batched writes"
      contains: "verify:state validate"
    - path: "workflows/execute-phase.md"
      provides: "Bundle smoke test after aggregate_results"
      contains: "npm run build"
  key_links:
    - from: "workflows/execute-plan.md"
      to: "src/commands/state.js"
      via: "verify:state validate command call"
      pattern: "verify:state validate"
    - from: "workflows/execute-phase.md"
      to: "bin/bgsd-tools.cjs"
      via: "npm run build"
      pattern: "npm run build"
---

<objective>
Wire verify:state validate as a post-batch checkpoint in execute-plan and add npm run build smoke test to execute-phase.

Purpose: STATE-01 requires regression validation after batched state writes. BUNDLE-01 requires smoke test after every plan to catch bundle parity issues.

Output: Updated workflows with state validation gate and bundle smoke test.
</objective>

> **TDD Decision:** Skipped — Workflow integration (adding a validation step + smoke test), not testable behavior. No business logic introduced.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-state-mutation-safety/203-CONTEXT.md
@.planning/phases/203-state-mutation-safety/203-RESEARCH.md
@workflows/execute-plan.md
@workflows/execute-phase.md
@src/commands/state.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire verify:state validate as post-batch checkpoint in execute-plan</name>
  <files>workflows/execute-plan.md</files>
  <action>
Add a new step `state_validation_gate` after the existing `update_position` step (around line 244) in execute-plan.md.

The step runs after batched state writes complete, before git_commit_metadata:

```
step name="state_validation_gate">
  Run `verify:state validate` as a regression checkpoint after all batched state writes:
  ```bash
  VALIDATION=$(node __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs verify:state validate --raw 2>/dev/null)
  echo "$VALIDATION"
  ```
  If status != 'clean': STOP, report issues, do not proceed to next plan or git commit.
  ```
```

Per CONTEXT.md: validation runs at plan end only (not per-batch) as the regression gate. Phase 202 mutex protection provides per-batch safety; this is the plan-end validation gate.

The step must appear after `update_position` (which performs batched state writes) and before `git_commit_metadata`.
  </action>
  <verify>rg "state_validation_gate" workflows/execute-plan.md</verify>
  <done>execute-plan.md has state_validation_gate step after update_position that runs verify:state validate and halts on non-clean status</done>
</task>

<task type="auto">
  <name>Task 2: Add npm run build smoke test to execute-phase after aggregate_results</name>
  <files>workflows/execute-phase.md</files>
  <action>
Add a new step `bundle_smoke_test` in the execute-phase workflow after `aggregate_results` (around line 384) and before `verify_phase_goal` (line 406).

The step runs once after all waves complete, serving as the final gate before phase verification:

```
step name="bundle_smoke_test">
  Run npm run build smoke test to verify bundle parity:
  ```bash
  BUILD_OUTPUT=$(npm run build 2>&1)
  BUILD_EXIT=$?
  if [ $BUILD_EXIT -ne 0 ]; then
    echo "BUNDLE PARITY FAILURE: $BUILD_OUTPUT"
    exit 1
  fi
  echo "Bundle smoke test: PASS"
  ```
  If build fails: exit 1, do not proceed to verify_phase_goal.
  ```

Per CONTEXT.md: smoke test placement is after execute-phase completes (not after planning). This is the final gate before phase verification.

Note: This step runs once per phase, not once per plan. It gates phase completion, not individual plan completion.
  </action>
  <verify>rg "bundle_smoke_test" workflows/execute-phase.md</verify>
  <done>execute-phase.md has bundle_smoke_test step after aggregate_results that runs npm run build and fails closed on bundle parity issues</done>
</task>

</tasks>

<verification>
Structural verification: verify state_validation_gate exists in execute-plan.md with correct verify:state validate call. Verify bundle_smoke_test exists in execute-phase.md with npm run build. No runtime tests needed — workflow integration only.
</verification>

<success_criteria>
- execute-plan.md has state_validation_gate step after update_position that gates on verify:state validate clean status
- execute-phase.md has bundle_smoke_test step after aggregate_results that fails closed on npm run build failure
- Both steps are placed per the locked decisions in CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/203-state-mutation-safety/203-02-SUMMARY.md`
</output>
