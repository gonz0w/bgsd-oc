---
phase: 204-wire-batch-state-api
plan: 03
type: execute
wave: 3
depends_on: ["204-01", "204-02"]
files_modified:
  - workflows/execute-plan.md
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - BUNDLE-01
  - BUNDLE-02
verification_route: light
verification_route_reason: "Workflow integration verification - structural checks confirm invoke pattern"
gap_closure: true

must_haves:
  truths:
    - execute-plan --dry-run shows batch path being selected for non-sacred stores
    - Batch state API is invoked from execute-plan workflow
  artifacts:
    - path: workflows/execute-plan.md
      contains: "verify:state complete-plan"
  key_links:
    - from: "workflows/execute-plan.md"
      to: "src/commands/state.js"
      via: "verify:state complete-plan command invocation"
      pattern: "verify:state complete-plan"
    - from: "src/commands/state.js"
      to: "src/lib/planning-cache.js"
      via: "storeSessionBundleBatch call"
      pattern: "storeSessionBundleBatch"
---

<objective>
Verify the end-to-end flow: execute-plan workflow calls cmdStateCompletePlan which routes to batch or single-write path based on canBatch.

Purpose: Confirm FLOW-002 is closed by verifying the execute-plan workflow correctly invokes the batch state API.
Output: Verified end-to-end flow from execute-plan to batch state API.
</objective>

> **TDD Decision:** Skipped — this is verification work, not behavioral code with testable outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/DEV/bgsd-oc/src/commands/state.js
</execution_context>

<context>
@.planning/phases/204-wire-batch-state-api/204-01-PLAN.md
@.planning/phases/204-wire-batch-state-api/204-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify execute-plan workflow integration</name>
  <files>workflows/execute-plan.md</files>
  <action>
    Review the execute-plan.md workflow to confirm it already correctly calls `verify:state complete-plan`:

    1. The `update_current_position` step (lines 231-244) calls:
       ```
       node __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs verify:state complete-plan \
         --phase "${PHASE}" --plan "${PLAN}" ...
       ```

    2. This is the correct integration point - the workflow already calls the command that now routes to batch or single-write.

    3. The `--dry-run` flag should be passed through from execute-plan if the user wants dry-run mode.

    4. If `--dry-run` is not being passed through, add it to the command invocation so the routing decision is visible.

    Verify the flow:
    - execute-plan.md calls `verify:state complete-plan`
    - `verify:state complete-plan` routes via canBatch and calls storeSessionBundleBatch
    - The --dry-run output shows the routing decision

    No changes may be needed if the workflow already passes through --dry-run. Confirm and document.
  </action>
  <verify>`rg "verify:state complete-plan" workflows/execute-plan.md` shows the command is called</verify>
  <done>execute-plan workflow correctly invokes batch state API via verify:state complete-plan</done>
</task>

</tasks>

<verification>
- `rg "verify:state complete-plan" workflows/execute-plan.md` finds the invocation
- `node bgsd-tools.cjs verify:state complete-plan --phase 204 --plan 1 --duration "1 min" --dry-run` shows BATCH path selected
</verification>

<success_criteria>
- execute-plan workflow calls `verify:state complete-plan`
- `verify:state complete-plan` routes via `canBatch` and calls `storeSessionBundleBatch` for non-sacred stores
- `--dry-run` shows batch path being selected
</success_criteria>

<output>
After completion, create `.planning/phases/204-wire-batch-state-api/204-03-SUMMARY.md`
</output>
