---
phase: 204-wire-batch-state-api
plan: 02
type: execute
wave: 2
depends_on: ["204-01"]
files_modified:
  - src/commands/state.js
autonomous: true
requirements:
  - STATE-02
  - STATE-03
verification_route: light
verification_route_reason: "Dry-run output addition is a logging feature - structural verification of flag handling suffices"
gap_closure: true

must_haves:
  truths:
    - execute-plan --dry-run shows batch path being selected for non-sacred stores
    - Dry-run output clearly indicates which routing path was chosen
  artifacts:
    - path: src/commands/state.js
      contains: "dry-run"
    - path: src/commands/state.js
      contains: "BATCH"
  key_links:
    - from: "src/commands/state.js"
      to: "cmdStateCompletePlan"
      via: "--dry-run flag handling"
      pattern: "dry-run"
    - from: "src/commands/state.js"
      to: "stderr output"
      via: "routing decision logging"
      pattern: "BATCH"
---

<objective>
Add `--dry-run` output to `cmdStateCompletePlan` showing which routing path (batch vs single-write) is selected based on `canBatch`.

Purpose: Close FLOW-002 by making the batch path selection visible during dry-run execution.
Output: When `--dry-run` flag is passed, the command outputs the routing decision.
</objective>

> **TDD Decision:** Skipped — this is wiring/integration work, not behavioral code with testable outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/DEV/bgsd-oc/src/commands/state.js
</execution_context>

<context>
@.planning/phases/204-wire-batch-state-api/204-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dry-run output showing batch path selection</name>
  <files>src/commands/state.js</files>
  <action>
    Modify `cmdStateCompletePlan` to detect and output routing decision when dry-run mode is active:

    1. Add `--dry-run` flag handling to `cmdStateCompletePlan` options:
       - Check for `options.dry_run` or `options['dry-run']` flag

    2. When dry-run is active, output routing decision BEFORE making any changes:
       ```
       [dry-run] batch-state: canBatch('state')={true|false} using={BATCH|SINGLE}-WRITE path
       [dry-run] batch-state: stores affected = state, metrics, session (non-sacred)
       ```

    3. If `canBatch('state')` is false (should not happen for state, but defensive):
       ```
       [dry-run] batch-state: SACRED store detected, forcing SINGLE-WRITE path
       ```

    4. Return early after outputting the dry-run info (no actual state changes)

    5. Also log at console.error for visibility: the dry-run output should go to stderr so it doesn't corrupt JSON output when `--raw` is used
  </action>
  <verify>`rg "dry-run|dry_run" src/commands/state.js | head -20` shows dry-run handling added</verify>
  <done>execute-plan --dry-run shows batch path selection before any state changes</done>
</task>

</tasks>

<verification>
- `rg "dry-run" src/commands/state.js` finds the flag handling
- Output contains "BATCH" or "SINGLE-WRITE" to indicate selected path
</verification>

<success_criteria>
- `--dry-run` flag is recognized by `cmdStateCompletePlan`
- Dry-run output shows `canBatch('state')` value and selected path
- No state mutations occur when `--dry-run` is passed
- Output goes to stderr to preserve JSON output on --raw
</success_criteria>

<output>
After completion, create `.planning/phases/204-wire-batch-state-api/204-02-SUMMARY.md`
</output>
