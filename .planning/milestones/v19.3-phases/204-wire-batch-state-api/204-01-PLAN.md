---
phase: 204-wire-batch-state-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/state.js
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - BUNDLE-01
  - BUNDLE-02
verification_route: light
verification_route_reason: "Integration-only changes to state.js - no test behavior to verify, structural checks suffice"
gap_closure: true

must_haves:
  truths:
    - storeSessionBundleBatch is called from cmdStateCompletePlan for non-sacred state mutations
    - canBatch guard routes between batch and single-write paths based on store type
    - Sacred data stores (decisions, lessons, trajectories, requirements) always use single-write path
    - Batch path uses BEGIN/COMMIT/ROLLBACK transaction support
  artifacts:
    - path: src/commands/state.js
      contains: "canBatch"
    - path: src/commands/state.js
      contains: "storeSessionBundleBatch"
  key_links:
    - from: "src/commands/state.js"
      to: "src/lib/state-session-mutator.js"
      via: "canBatch import and call"
      pattern: "canBatch"
    - from: "src/commands/state.js"
      to: "src/lib/planning-cache.js"
      via: "storeSessionBundleBatch call"
      pattern: "storeSessionBundleBatch"
---

<objective>
Wire `canBatch` routing and `storeSessionBundleBatch` invocation into `cmdStateCompletePlan` so non-sacred state mutations use the batch path.

Purpose: Close GAP-001 (storeSessionBundleBatch never called) and GAP-002 (canBatch never used) by integrating the batch API into the state completion workflow.
Output: Modified `cmdStateCompletePlan` that routes based on `canBatch('state')` and calls `storeSessionBundleBatch` for non-sacred stores.
</objective>

> **TDD Decision:** Skipped — this is wiring/integration work, not behavioral code with testable outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/DEV/bgsd-oc/src/commands/state.js
@/Users/cam/DEV/bgsd-oc/src/lib/state-session-mutator.js
@/Users/cam/DEV/bgsd-oc/src/lib/planning-cache.js
</execution_context>

<context>
@.planning/phases/204-wire-batch-state-api/204-RESEARCH.md
@.planning/phases/203-state-mutation-safety/203-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire canBatch routing and storeSessionBundleBatch into cmdStateCompletePlan</name>
  <files>src/commands/state.js</files>
  <action>
    Modify `cmdStateCompletePlan` function (around line 1029) to:

    1. Import `canBatch` from `state-session-mutator.js` and `storeSessionBundleBatch` from `planning-cache.js`

    2. After computing phase truth and progress, check `canBatch('state')`:
       - If true: Collect state bundle (phase, progress, status), metrics bundle, and session continuity bundle into an array, then call `cache.storeSessionBundleBatch(cwd, bundles)` instead of individual writes
       - If false (sacred): Use existing individual write path via `applyStateSessionMutation`

    3. The batch call signature: `cache.storeSessionBundleBatch(cwd, [
      { state: { phase_number, phase_name, current_plan, status, progress, ... } },
      { state: { /* metrics data */ } },  // if metrics exist
      { continuity: { last_session, stopped_at, next_step } }
    ])`

    4. Continue to call `applyStateSessionMutation` for sacred data (decisions) that cannot be batched - this preserves existing behavior for sacred writes

    5. Log the routing decision for debugging: `console.error('batch-state: canBatch=', canBatch('state'), 'using=', canBatch('state') ? 'BATCH' : 'SINGLE')`

    Key reference from research:
    - `canBatch(store)` returns `false` for `['decisions', 'lessons', 'trajectories', 'requirements']`
    - `canBatch('state')` returns `true` (state is not sacred)
    - `storeSessionBundleBatch` does BEGIN/COMMIT/ROLLBACK internally
  </action>
  <verify>`rg "canBatch|storeSessionBundleBatch" src/commands/state.js` shows both functions imported and used</verify>
  <done>cmdStateCompletePlan routes based on canBatch and calls storeSessionBundleBatch for non-sacred stores</done>
</task>

</tasks>

<verification>
- `rg "canBatch" src/commands/state.js` finds the guard being used
- `rg "storeSessionBundleBatch" src/commands/state.js` finds the batch invocation
</verification>

<success_criteria>
- `canBatch` is imported and used as a routing guard in `cmdStateCompletePlan`
- `storeSessionBundleBatch` is called when `canBatch('state')` returns true
- Sacred data (decisions) still uses single-write path via `applyStateSessionMutation`
- Batch path includes full transaction support (BEGIN/COMMIT/ROLLBACK) via existing `storeSessionBundleBatch` implementation
</success_criteria>

<output>
After completion, create `.planning/phases/204-wire-batch-state-api/204-01-SUMMARY.md`
</output>
