---
phase: "135"
plan: "02"
title: "Compress discuss-phase & execute-phase"
type: standard
requirements: ["COMP-01", "COMP-02"]
depends_on: ["135-01"]
files_modified:
  - workflows/discuss-phase.md
  - workflows/execute-phase.md
estimated_tasks: 2
wave: 2
---

# Plan 02: Compress discuss-phase & execute-phase

## Objective

Prose-tighten the two largest workflows (discuss-phase at 538 lines and execute-phase at 497 lines) to achieve 40%+ token reduction each, add `<!-- section: step_name -->` markers at every major process step, and replace inline shared blocks with `<skill:X />` references.

## Must-Haves

```yaml
must_haves:
  truths:
    - "discuss-phase.md is reduced from ~538 lines to ~320 lines or fewer (40%+ reduction)"
    - "execute-phase.md is reduced from ~497 lines to ~300 lines or fewer (40%+ reduction)"
    - "Both workflows have <!-- section: step_name --> markers at each major process step"
    - "Inline bgsd-context init blocks replaced with <skill:bgsd-context-init /> references"
    - "execute-phase CI quality gate block replaced with <skill:ci-quality-gate /> reference"
    - "All Task() calls, CLI commands, and question blocks are preserved (zero structural regressions)"
  artifacts:
    - path: "workflows/discuss-phase.md"
      provides: "Compressed discuss-phase workflow"
      contains: "<!-- section: markers, <skill: references, all original Task() calls"
    - path: "workflows/execute-phase.md"
      provides: "Compressed execute-phase workflow"
      contains: "<!-- section: markers, <skill: references, all original Task() calls"
  key_links:
    - from: "workflows/discuss-phase.md"
      to: "skills/bgsd-context-init/SKILL.md"
      via: "<skill:bgsd-context-init />"
      pattern: "<skill:bgsd-context-init"
    - from: "workflows/execute-phase.md"
      to: "skills/ci-quality-gate/SKILL.md"
      via: "<skill:ci-quality-gate />"
      pattern: "<skill:ci-quality-gate"
```

## Tasks

### Task 1: Compress discuss-phase.md

- **Type:** auto
- **Files:**
  - `workflows/discuss-phase.md` (modify)

- **Action:**

  **Add section markers** at each major step: `<!-- section: purpose -->`, `<!-- section: initialize -->`, `<!-- section: check_existing -->`, `<!-- section: analyze_phase -->`, `<!-- section: present_gray_areas -->`, `<!-- section: discuss_areas -->`, `<!-- section: customer_stress_test -->`, `<!-- section: write_context -->`, `<!-- section: confirm_creation -->`, `<!-- section: git_commit -->`, `<!-- section: update_state -->`, `<!-- section: auto_advance -->`, `<!-- section: success_criteria -->`. Close each section with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace the bgsd-context initialization preamble (2-paragraph block about auto-injection + plugin-required error) with `<skill:bgsd-context-init />` followed by the workflow-specific field list

  **Prose-tighten throughout:**
  - **Philosophy/preamble blocks** (`<purpose>`, `<downstream_awareness>`, `<philosophy>`, `<scope_guardrail>`): Compress from multi-paragraph explanations to 1-2 sentence directives. These explain the "why" to the agent but are currently overwritten — cut by 60%+
  - **Gray area examples**: Reduce the domain-specific example lists from 10+ items each to 3-4 representative examples. The agent generalizes from fewer examples
  - **Discussion loop instructions**: Compress the Socratic discussion protocol from verbose paragraphs to a numbered directive list (Question → Follow-up → Probe → Summarize)
  - **Customer stress test**: Reduce the adversarial review role-play instructions to essential prompts — cut character descriptions and elaborate scenarios
  - **CONTEXT.md template**: Keep the template structure but remove verbose field descriptions
  - **Remove redundant transitions** between steps ("Now that we have X, let's proceed to Y")
  - **Compress conditional blocks**: Simplify if/else prose into single-line conditionals where possible

  **Preservation rules** (do NOT remove/modify):
  - All Task() calls (1 auto_advance spawn)
  - All CLI commands (execute:commit, verify:state, util:config-get, util:config-set)
  - All question() blocks and their option structures
  - The CONTEXT.md file path and structure
  - Pre-computed decision references

- **Verify:** Count lines — target ≤320. Manually confirm all Task() calls, CLI commands, and question blocks are present. Run `workflow:verify-structure` against the saved baseline to confirm zero structural regressions.

- **Done:** discuss-phase.md reduced 40%+ with section markers at every step. All structural elements preserved. `<skill:bgsd-context-init />` replaces inline init block.

### Task 2: Compress execute-phase.md

- **Type:** auto
- **Files:**
  - `workflows/execute-phase.md` (modify)

- **Action:**

  **Add section markers** at each major step: `<!-- section: purpose -->`, `<!-- section: initialize -->`, `<!-- section: handle_branching -->`, `<!-- section: validate_phase -->`, `<!-- section: preflight -->`, `<!-- section: discover_plans -->`, `<!-- section: visualize_execution -->`, `<!-- section: execute_waves -->`, `<!-- section: checkpoint_handling -->`, `<!-- section: aggregate_results -->`, `<!-- section: close_artifacts -->`, `<!-- section: ci_quality_gate -->`, `<!-- section: verify_phase_goal -->`, `<!-- section: update_roadmap -->`, `<!-- section: offer_next -->`, `<!-- section: resumption -->`, `<!-- section: success_criteria -->`. Close each section with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace bgsd-context initialization preamble with `<skill:bgsd-context-init />`
  - Replace the CI quality gate block (~40 lines including config check, banner, Task() spawn, and conditional skip) with `<skill:ci-quality-gate scope="phase-{N}" />`
  - Replace classifyHandoffIfNeeded workaround note with `<skill:executor-workarounds />` or inline comment (assess whether a skill is warranted for a 2-line note)

  **Prose-tighten throughout:**
  - **Purpose/core_principle/required_reading** blocks: Compress from multi-paragraph to 1-2 sentences each
  - **Preflight checks** (4 separate steps): Merge into a single preflight section with numbered checks — remove verbose explanations of why each check matters
  - **Wave visualization**: Keep the ASCII diagram pattern but remove the prose explaining what waves are
  - **Execute waves section** (Mode A worktree + Mode B standard): This is the largest section — compress the worktree creation/merge/cleanup instructions to essential commands only. Remove paragraphs explaining git worktree concepts
  - **Codebase context injection**: Compress the util:frontmatter + util:codebase context gathering to command lists without verbose explanations
  - **Aggregate results**: Compress the table-building instructions
  - **Resumption appendix**: Compress STATE.md recovery instructions to essential steps
  - **Remove all transitional prose** between steps

  **Preservation rules** (do NOT remove/modify):
  - All 4 Task() calls (executor spawn x2, CI gate, verifier)
  - All ~17 CLI commands (verify:state, verify:validate-dependencies, util:phase-plan-index, execute:commit, plan:phase, etc.)
  - Pre-computed decision references (branch-handling, ci-gate)
  - Mode A vs Mode B routing logic (structural, not just prose)
  - Worktree command sequences (git worktree add/remove)

- **Verify:** Count lines — target ≤300. Manually confirm all 4 Task() calls, all CLI commands, and branching logic preserved. Run `workflow:verify-structure` against baseline to confirm zero regressions.

- **Done:** execute-phase.md reduced 40%+ with section markers at every step. CI quality gate replaced with `<skill:ci-quality-gate />`. All structural elements preserved.
