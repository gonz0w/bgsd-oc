---
phase: "135"
plan: "04"
title: "Compress transition, new-project & resume-project"
type: standard
requirements: ["COMP-01", "COMP-02"]
depends_on: ["135-01"]
files_modified:
  - workflows/transition.md
  - workflows/new-project.md
  - workflows/resume-project.md
estimated_tasks: 3
wave: 2
---

# Plan 04: Compress transition, new-project & resume-project

## Objective

Prose-tighten transition (519 lines), new-project (273 lines), and resume-project (286 lines) to achieve 40%+ token reduction each, add section markers, and replace inline shared blocks with skill references.

## Must-Haves

```yaml
must_haves:
  truths:
    - "transition.md is reduced from ~519 lines to ~310 lines or fewer"
    - "new-project.md is reduced from ~273 lines to ~165 lines or fewer"
    - "resume-project.md is reduced from ~286 lines to ~170 lines or fewer"
    - "All 3 workflows have <!-- section: step_name --> markers at each major process step"
    - "new-project research pipeline replaced with <skill:research-pipeline />"
    - "All Task() calls and CLI commands preserved (zero structural regressions)"
  artifacts:
    - path: "workflows/transition.md"
      provides: "Compressed transition workflow"
      contains: "<!-- section: markers, all CLI commands, requirement lifecycle logic"
    - path: "workflows/new-project.md"
      provides: "Compressed new-project workflow"
      contains: "<!-- section: markers, <skill:research-pipeline />, all 6 Task() calls"
    - path: "workflows/resume-project.md"
      provides: "Compressed resume-project workflow"
      contains: "<!-- section: markers, routing logic, Next Up blocks"
  key_links:
    - from: "workflows/new-project.md"
      to: "skills/research-pipeline/SKILL.md"
      via: "<skill:research-pipeline />"
      pattern: "<skill:research-pipeline"
```

## Tasks

### Task 1: Compress transition.md

- **Type:** auto
- **Files:**
  - `workflows/transition.md` (modify)

- **Action:**

  **Add section markers** at each major step: `<!-- section: purpose -->`, `<!-- section: load_state -->`, `<!-- section: verify_completion -->`, `<!-- section: cleanup_handoff -->`, `<!-- section: update_roadmap -->`, `<!-- section: archive -->`, `<!-- section: evolve_project -->`, `<!-- section: update_position -->`, `<!-- section: update_reference -->`, `<!-- section: review_context -->`, `<!-- section: update_session -->`, `<!-- section: offer_next -->`, `<!-- section: implicit_tracking -->`, `<!-- section: partial_completion -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace bgsd-context initialization with `<skill:bgsd-context-init />`

  **Prose-tighten throughout:**
  - **Purpose/required_reading**: Compress to 1-2 sentences
  - **Verify completion**: Compress the PLAN vs SUMMARY count logic and yolo/interactive routing — keep branching structure but remove explanation of what yolo mode means (agent already knows)
  - **Evolve project section** (largest section, ~100+ lines): This is the requirement validation/invalidation/emergence logic. Compress the requirement lifecycle explanations. Keep the decision tree (validated/invalidated/emerged/unchanged) but express it as a compact table or decision list instead of multi-paragraph prose. Compress the PROJECT.md description evolution instructions
  - **Three Next Up blocks**: Keep all 3 routing paths (next phase with context, next phase without context, milestone complete) but compress each block. Remove the elaborate "Also available" alternative lists — keep 1-2 key alternatives max
  - **Implicit tracking appendix**: Compress from verbose explanation to a bullet list of what gets tracked
  - **Partial completion appendix**: Compress to essential instructions
  - **Remove yolo/interactive conditional duplication** where the two paths say nearly the same thing

  **Preservation rules:**
  - 0 Task() calls (this workflow has none — preserve that)
  - All ~4 CLI commands (plan:phase complete, util:progress bar, plan:roadmap analyze, util:config-set)
  - Pre-computed decision references (auto-advance, branch-handling)
  - All 3 routing paths in offer_next
  - Requirement lifecycle logic (validated/invalidated/emerged)

- **Verify:** Count lines — target ≤310. Confirm all CLI commands preserved. Run `workflow:verify-structure` against baseline.

- **Done:** transition.md reduced 40%+ with section markers. All structural elements and routing paths preserved.

### Task 2: Compress new-project.md

- **Type:** auto
- **Files:**
  - `workflows/new-project.md` (modify)

- **Action:**

  **Add section markers** at each major step: `<!-- section: setup -->`, `<!-- section: brownfield_check -->`, `<!-- section: auto_mode -->`, `<!-- section: questioning -->`, `<!-- section: write_project -->`, `<!-- section: capture_intent -->`, `<!-- section: preferences -->`, `<!-- section: resolve_models -->`, `<!-- section: research -->`, `<!-- section: define_requirements -->`, `<!-- section: create_roadmap -->`, `<!-- section: done -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace bgsd-context initialization with `<skill:bgsd-context-init />`
  - Replace research pipeline (~45 lines: 4 researcher spawns + synthesizer + scoring) with `<skill:research-pipeline context="new-project" />`

  **Prose-tighten throughout:**
  - **Setup step**: Compress git init instructions to essential commands
  - **Brownfield offer**: Compress codebase mapping check to 2-3 lines
  - **Deep questioning**: Compress — reference the questioning skill pattern instead of inline instructions. Keep the question() patterns but remove verbose explanation of Socratic method
  - **Write PROJECT.md**: Keep the template structure but compress field descriptions
  - **Intent capture**: Compress INTENT.md creation to template + commit
  - **Preferences step**: Compress config.json setup — keep the setting keys but remove verbose explanations of each option
  - **Requirements definition**: Compress — this is already shorter than new-milestone's version
  - **Roadmap creation**: Compress roadmapper spawn to Task() params only

  **Preservation rules:**
  - All 6 Task() calls (4 researchers, 1 synthesizer, 1 roadmapper)
  - All ~7 CLI commands (execute:commit x5, plan:intent, verify:validate)
  - Auto mode conditionals (--auto flag)
  - Git init logic

- **Verify:** Count lines — target ≤165. Confirm all 6 Task() calls and CLI commands preserved. Run `workflow:verify-structure` against baseline.

- **Done:** new-project.md reduced 40%+ with section markers. Research pipeline replaced with skill reference. All structural elements preserved.

### Task 3: Compress resume-project.md

- **Type:** auto
- **Files:**
  - `workflows/resume-project.md` (modify)

- **Action:**

  **Add section markers** at each major step: `<!-- section: initialize -->`, `<!-- section: load_state -->`, `<!-- section: check_incomplete -->`, `<!-- section: present_status -->`, `<!-- section: determine_action -->`, `<!-- section: offer_options -->`, `<!-- section: route_to_workflow -->`, `<!-- section: update_session -->`, `<!-- section: reconstruction -->`, `<!-- section: quick_resume -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace bgsd-context initialization with `<skill:bgsd-context-init />`

  **Prose-tighten throughout:**
  - **Load state**: Compress STATE.md/PROJECT.md reading instructions to essential fields
  - **Check incomplete work**: Compress the 3-way check (.continue-here, incomplete plans, interrupted agents) to a compact decision table
  - **Present status**: Keep the ASCII box format but compress the explanation of what to display
  - **Determine next action**: Compress the state-based routing from verbose if/else prose to a compact routing table (state → action → command)
  - **Offer options**: Compress the numbered options list — keep the routing logic, remove verbose descriptions of each option
  - **Route to workflow**: Keep all Next Up blocks but compress each to 3 lines max
  - **Reconstruction appendix**: Compress STATE.md recovery from verbose instructions to essential steps — this is a fallback path that rarely executes
  - **Quick resume appendix**: Compress to essential command

  **Preservation rules:**
  - 0 Task() calls (this workflow has none — preserve that)
  - 0 CLI commands (this workflow reads files directly — preserve that pattern)
  - Pre-computed decision references (resume-route)
  - All routing paths and Next Up blocks
  - State reconstruction logic

- **Verify:** Count lines — target ≤170. Confirm routing logic and all Next Up blocks preserved. Run `workflow:verify-structure` against baseline.

- **Done:** resume-project.md reduced 40%+ with section markers. All routing paths and structural elements preserved.
