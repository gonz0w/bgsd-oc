---
phase: "135"
plan: "03"
title: "Compress new-milestone & execute-plan"
type: standard
requirements: ["COMP-01", "COMP-02", "COMP-03"]
depends_on: ["135-01"]
files_modified:
  - workflows/new-milestone.md
  - workflows/execute-plan.md
estimated_tasks: 2
wave: 2
---

# Plan 03: Compress new-milestone & execute-plan

## Objective

Prose-tighten new-milestone (505 lines) and execute-plan (376 lines) to achieve 40%+ token reduction each, add section markers, and replace inline shared blocks — particularly the research pipeline in new-milestone and the deviation rules/commit protocol/checkpoint blocks in execute-plan — with `<skill:X />` references.

## Must-Haves

```yaml
must_haves:
  truths:
    - "new-milestone.md is reduced from ~505 lines to ~300 lines or fewer"
    - "execute-plan.md is reduced from ~376 lines to ~225 lines or fewer"
    - "Both workflows have <!-- section: step_name --> markers at each major process step"
    - "new-milestone research pipeline replaced with <skill:research-pipeline />"
    - "execute-plan deviation rules replaced with <skill:deviation-rules />"
    - "execute-plan commit protocol replaced with <skill:commit-protocol />"
    - "execute-plan checkpoint handling replaced with <skill:checkpoint-protocol />"
    - "All Task() calls and CLI commands preserved (zero structural regressions)"
  artifacts:
    - path: "workflows/new-milestone.md"
      provides: "Compressed new-milestone workflow"
      contains: "<!-- section: markers, <skill:research-pipeline />, all 6 Task() calls"
    - path: "workflows/execute-plan.md"
      provides: "Compressed execute-plan workflow"
      contains: "<!-- section: markers, <skill:deviation-rules />, <skill:commit-protocol />, <skill:checkpoint-protocol />"
  key_links:
    - from: "workflows/new-milestone.md"
      to: "skills/research-pipeline/SKILL.md"
      via: "<skill:research-pipeline />"
      pattern: "<skill:research-pipeline"
    - from: "workflows/execute-plan.md"
      to: "skills/deviation-rules/SKILL.md"
      via: "<skill:deviation-rules />"
      pattern: "<skill:deviation-rules"
```

## Tasks

### Task 1: Compress new-milestone.md

- **Type:** auto
- **Files:**
  - `workflows/new-milestone.md` (modify)

- **Action:**

  **Add section markers** at each major step: `<!-- section: load_context -->`, `<!-- section: gather_goals -->`, `<!-- section: determine_version -->`, `<!-- section: update_project -->`, `<!-- section: review_intent -->`, `<!-- section: update_state -->`, `<!-- section: resolve_models -->`, `<!-- section: research -->`, `<!-- section: skill_discovery -->`, `<!-- section: define_requirements -->`, `<!-- section: create_roadmap -->`, `<!-- section: done -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace bgsd-context initialization with `<skill:bgsd-context-init />`
  - Replace the entire research pipeline (~70-80 lines: 4 parallel researcher spawns + synthesizer + quality scoring) with `<skill:research-pipeline context="milestone" />`. The skill contains the full spawn pattern; the workflow only needs to specify context type
  - Replace the roadmap creation + validation + commit block with a compressed inline version (this only appears in 2 workflows, so a skill may not be warranted — compress instead)

  **Prose-tighten throughout:**
  - **Steps 1-6 (project setup)**: These gather goals, determine version, update PROJECT.md/STATE.md — compress each from multi-paragraph to 3-5 line directives. Remove examples of version numbering schemes
  - **Intent review (Step 4.5)**: Compress INTENT.md evolution instructions — keep the template fields but remove verbose explanations
  - **Skill discovery (Step 8.5)**: Compress the optional skill installation flow — it's rarely used
  - **Requirements definition (Step 9)**: Keep the requirement quality criteria checklist but compress the feature scoping instructions. Remove the elaborate multiSelect UI description — the agent knows how to use question()
  - **Roadmap creation (Step 10)**: Compress the bgsd-roadmapper spawn instructions — keep Task() params but remove explanatory prose
  - **Banner blocks** (`bGSD ►`): Keep as structural markers but compress surrounding prose
  - **Remove all commit message templates** that duplicate the commit-protocol skill

  **Preservation rules:**
  - All 6 Task() calls (4 researchers, 1 synthesizer, 1 roadmapper)
  - All ~10 CLI commands (execute:commit, plan:intent, util:config-set, research:score, skills:list, verify:validate)
  - Requirement ID format (REQ-XX) and quality criteria structure
  - Version determination logic (major/minor/patch)

- **Verify:** Count lines — target ≤300. Confirm all 6 Task() calls and CLI commands preserved. Run `workflow:verify-structure` against baseline.

- **Done:** new-milestone.md reduced 40%+ with section markers. Research pipeline replaced with skill reference. All structural elements preserved.

### Task 2: Compress execute-plan.md

- **Type:** auto
- **Files:**
  - `workflows/execute-plan.md` (modify)

- **Action:**

  **Add section markers** at each major step: `<!-- section: init_context -->`, `<!-- section: identify_plan -->`, `<!-- section: context_budget -->`, `<!-- section: parse_segments -->`, `<!-- section: execute -->`, `<!-- section: deviation_rules -->`, `<!-- section: task_commit -->`, `<!-- section: checkpoint_protocol -->`, `<!-- section: verification_gate -->`, `<!-- section: post_execution -->`, `<!-- section: create_summary -->`, `<!-- section: update_position -->`, `<!-- section: update_roadmap -->`, `<!-- section: offer_next -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace bgsd-context initialization with `<skill:bgsd-context-init />`
  - Replace `<deviation_rules>` block (Rules 1-4 table + auto-capture instructions, ~30 lines) with `<skill:deviation-rules section="executor" />`
  - Replace `<task_commit>` block (atomic commits + staging + message format, ~20 lines) with `<skill:commit-protocol />`
  - Replace `checkpoint_protocol` block (checkpoint detection, handling, return format, ~25 lines) with `<skill:checkpoint-protocol />`
  - Replace `<authentication_gates>` block with a 2-line reference to checkpoint-protocol's auth gate section
  - Replace classifyHandoffIfNeeded workaround with a 1-line comment

  **Prose-tighten throughout:**
  - **Pattern A/B/C routing**: Compress the segment parsing explanation — keep the routing logic but remove paragraphs explaining why patterns exist
  - **Agent tracking init**: Compress agent-history.json initialization to essential commands
  - **Load prompt section**: Compress the previous-phase-check and context loading instructions
  - **Execute section**: This is the core task loop — compress but keep all CLI commands and the task iteration structure
  - **TDD plan execution**: Compress the TDD-specific instructions (reference tdd-execution skill instead)
  - **Auto-test after edit**: Compress to essential logic
  - **Post-execution review**: Compress the verification-failure-gate and review instructions
  - **Summary creation**: Keep the scaffold CLI command but remove verbose template instructions
  - **State update chain**: Keep all verify:state and plan:roadmap commands but compress surrounding prose
  - **Remove all transitional prose** and redundant explanations

  **Preservation rules:**
  - The 1 Task() call reference (Pattern A spawn) and all patterns for spawning
  - All ~15 CLI commands (verify:state, execute:commit, plan:roadmap, plan:requirements, util:summary-generate, verify:review, lessons:deviation-capture, execute:tdd, util:memory)
  - Pre-computed decision references (context-budget-gate, execution-pattern, previous-check-gate)
  - Pattern A/B/C routing structure
  - Task iteration loop structure

- **Verify:** Count lines — target ≤225. Confirm all CLI commands and task patterns preserved. Run `workflow:verify-structure` against baseline.

- **Done:** execute-plan.md reduced 40%+ with section markers. Deviation rules, commit protocol, and checkpoint handling replaced with skill references. All structural elements preserved.
