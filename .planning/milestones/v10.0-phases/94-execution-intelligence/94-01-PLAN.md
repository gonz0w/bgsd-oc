---
phase: 94-execution-intelligence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/bgsd-tools.cjs
  - workflows/execute-phase.md
  - workflows/execute-plan.md
autonomous: true
requirements:
  - AGENT-07
  - AGENT-08
  - AGENT-09

must_haves:
  truths:
    - "Executor handles common deviations autonomously with recovery"
    - "Checkpoint decisions made based on task complexity"
    - "Stuck/loop patterns detected and pivots suggested proactively"
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "Deviation recovery, checkpoint intelligence, loop detection"
      contains: "autoRecovery, checkpointAdvisor, loopDetector"
    - path: "workflows/execute-phase.md"
      provides: "Enhanced execution workflow with deviation handling"
      contains: "deviation handling, checkpoint decisions"
    - path: "workflows/execute-plan.md"
      provides: "Plan execution workflow with loop detection"
      contains: "stuck detection, pivot suggestions"
  key_links:
    - from: "execute-phase.md"
      to: "deviation-rules skill"
      via: "auto-fix decision framework"
      pattern: "Rule [1-4]"
    - from: "execute-plan.md"
      to: "checkpoint-protocol skill"
      via: "complexity-based checkpoint decisions"
      pattern: "task.complexity.*checkpoint"
---

<objective>
Implement execution intelligence with autonomous deviation handling, intelligent checkpoint decisions, and stuck/loop detection.

Purpose: Enable the executor to handle common issues without human intervention, make smart checkpoint decisions based on task complexity, and proactively detect when execution is stuck in a loop.

Output: Enhanced bgsd-tools.cjs with recovery capabilities, updated execution workflows with intelligence features
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-phase.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-verification-intelligence/93-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add autonomous deviation recovery to executor</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Enhance bgsd-tools.cjs with autoRecovery module that handles deviations autonomously:

1. Create autoRecovery object with:
   - attemptRecovery(deviation, context) - analyzes deviation type and applies appropriate recovery strategy
   - recoveryStrategies - map of deviation types to recovery functions
   - maxRetries - track retry attempts per deviation
   - recoveryLog - history of recovery actions for SUMMARY

2. Implement recovery strategies for common deviations:
   - RULE 1 (bugs): Attempt fix inline, verify, continue
   - RULE 2 (missing critical): Auto-add missing functionality
   - RULE 3 (blocking): Resolve blocking issue or suggest workaround
   - RULE 4 (architectural): Queue for checkpoint decision

3. Integrate with existing deviation-rules skill:
   - Use the 4-rule classification framework
   - Apply fix attempt limit (3 attempts per task)
   - Log all recovery actions with rule reference

4. Add recovery metrics to execution output:
   - deviations_handled count
   - autonomous_recoveries count
   - escalated_to_user count
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs exec quick --help to verify command exists
- Check that autoRecovery module is properly exported
- Verify recovery strategies map covers all 4 rules
</verify>
  <done>
Executor can handle at least 5 common deviation types autonomously
Recovery actions logged with rule numbers and descriptions
Metrics show deviation handling activity in execution output
</done>
</task>

<task type="auto">
  <name>Task 2: Add intelligent checkpoint decisions based on task complexity</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Enhance bgsd-tools.cjs with checkpointAdvisor module for intelligent checkpoint decisions:

1. Create checkpointAdvisor object with:
   - analyzeComplexity(task) - evaluates task complexity based on multiple factors
   - recommendCheckpoint(task, analysis) - suggests checkpoint type if needed
   - complexityFactors - scoring system for task attributes

2. Implement complexity analysis:
   - File count: tasks touching >3 files = higher complexity
   - Task type: checkpoint:decision > checkpoint:human-verify > auto
   - Dependencies: tasks with >2 dependencies = higher complexity
   - Estimated duration: tasks estimated >45 min = higher complexity
   - External services: tasks requiring user_setup = highest complexity

3. Implement checkpoint recommendation logic:
   - Complexity score 0-3: no checkpoint (autonomous)
   - Complexity score 4-6: checkpoint:human-verify
   - Complexity score 7-9: checkpoint:decision  
   - Complexity score 10+: checkpoint:human-action

4. Integrate with execute-phase workflow:
   - Call checkpointAdvisor before each task
   - Insert checkpoint if recommended
   - Pass complexity info to checkpoint for context

5. Override autonomy setting if complexity demands it:
   - If task has autonomous:false but complexity low, still execute
   - If task has autonomous:true but complexity high, add checkpoint
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs exec quick --help to verify command exists
- Check that checkpointAdvisor module is exported
- Test complexity scoring with sample tasks
</verify>
  <done>
Task complexity analysis produces consistent scores 0-10+
Checkpoint recommendations match complexity levels
Workflow respects complexity-based checkpoint decisions
</done>
</task>

<task type="auto">
  <name>Task 3: Add stuck/loop pattern detection and pivot suggestions</name>
  <files>bin/bgsd-tools.cjs, workflows/execute-phase.md, workflows/execute-plan.md</files>
  <action>
Enhance bgsd-tools.cjs with loopDetector module for detecting stuck execution patterns:

1. Create loopDetector object with:
   - trackProgress(taskId, attempt) - logs progress for pattern analysis
   - detectStuck(attemptHistory) - analyzes for stuck patterns
   - suggestPivot(stuckType) - recommends recovery actions
   - resetPattern(taskId) - clears history for new attempt

2. Implement stuck pattern detection:
   - Repetition detection: Same task attempted >3 times
   - No progress: Task output unchanged across attempts
   - Circular dependency: Task A waiting on B, B waiting on A
   - Resource exhaustion: Multiple failures with similar errors

3. Implement pivot suggestions:
   - For repetition: "Consider breaking task into smaller parts"
   - For no progress: "Try alternative approach or simplify requirements"
   - For circular deps: "Execute independent tasks first, revisit dependencies"
   - For exhaustion: "Check environment, restart from clean state"

4. Add loop detection to execute-phase workflow:
   - Track each task attempt with timestamp and outcome
   - Run detectStuck before each retry
   - If stuck detected, pause and suggest pivot
   - Allow user to accept pivot or force continue

5. Update execute-plan.md workflow:
   - Document loop detection integration
   - Add pivot suggestion presentation
   - Include force-continue option with warning
</action>
  <verify>
- Run: node bin/bgsd-tools.cjs exec quick --help to verify command exists
- Check that loopDetector module is exported
- Test pattern detection with simulated stuck scenarios
</verify>
  <done>
LoopDetector identifies repetition, no-progress, and circular patterns
Pivot suggestions are specific and actionable
User can accept pivot or force continue with warning
</done>
</task>

</tasks>

<verification>
1. All three modules (autoRecovery, checkpointAdvisor, loopDetector) are exported from bgsd-tools.cjs
2. Recovery strategies cover all 4 deviation rules from deviation-rules skill
3. Checkpoint recommendations align with complexity scores
4. Stuck detection triggers before 4th attempt of same task
5. Pivot suggestions appear in execution output when stuck detected
</verification>

<success_criteria>
- Executor handles at least 5 common deviation types autonomously
- Complexity-based checkpoint decisions are implemented
- Stuck/loop detection triggers at appropriate thresholds
- All three features integrate with existing execution workflows
- No existing functionality is broken
</success_criteria>

<output>
After completion, create `.planning/phases/94-execution-intelligence/94-01-SUMMARY.md`
</output>
