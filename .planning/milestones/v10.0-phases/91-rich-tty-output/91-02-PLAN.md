---
phase: 91-rich-tty-output
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/error.js]
autonomous: true
requirements: [UX-10, UX-12]
user_setup: []

must_haves:
  truths:
    - "Error messages include actionable recovery suggestions"
    - "Errors include relevant context (file path, line number)"
    - "Error output is color-coded (red/yellow) with [ERROR]/[WARN] prefixes"
    - "Recovery suggestions start with 'Try:' prefix for clarity"
  artifacts:
    - path: "src/lib/error.js"
      provides: "Error handling with context and recovery suggestions"
      contains: "BgsdError class, formatError function, error types"
  key_links:
    - from: "src/lib/error.js"
      to: "src/lib/format.js"
      via: "require for color/box formatting"
      pattern: "require.*format"
---

<objective>
Create src/lib/error.js module for context-rich error handling. This module provides structured error classes with file/line context and formatted error messages with recovery suggestions. Works with format.js for color-coded output.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-rich-tty-output/91-CONTEXT.md
@src/lib/format.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BgsdError base class</name>
  <files>src/lib/error.js</files>
  <action>
Create the error module with BgsdError class:
1. Create src/lib/error.js with exports
2. Define BgsdError class extending Error with properties:
   - type: error category (ValidationError, FileError, CommandError, etc.)
   - message: user-friendly error message
   - file: file path where error occurred (optional)
   - line: line number (optional)
   - suggestion: recovery action (optional)
   - code: optional error code for programmatic handling
3. Create specific error subclasses:
   - ValidationError (invalid input)
   - FileError (file not found, permissions)
   - CommandError (command failed)
   - ConfigError (configuration issues)
4. Export all error types

Example:
  throw new BgsdError({
    type: 'FileError',
    message: 'Config file not found',
    file: '.planning/config.json',
    suggestion: 'Run /bgsd init to create config'
  });
  </action>
  <verify>
ls -la src/lib/error.js && grep -n "class BgsdError" src/lib/error.js</verify>
  <done>BgsdError class and subclasses exist with proper properties</done>
</task>

<task type="auto">
  <name>Task 2: Create error formatting with recovery suggestions</name>
  <files>src/lib/error.js</files>
  <action>
Add error formatting functions:
1. Create formatError(error, options) function that:
   - Uses format.js color for red/yellow styling
   - Adds [ERROR] or [WARN] prefix based on severity
   - Shows: type, message, file:line (if present)
   - Shows "Try: {suggestion}" in green when suggestion present
2. Create formatErrors(errors[]) for multiple errors
3. Handle Error objects and BgsdError objects
4. Support 'verbose' option to show stack traces

Output format:
  [ERROR] FileError: Config file not found
         File: .planning/config.json
         Try: Run /bgsd init to create config
  </action>
  <verify>
grep -n "formatError" src/lib/error.js</verify>
  <done>formatError produces formatted output with Try: prefix</done>
</task>

<task type="auto">
  <name>Task 3: Add error handling utilities</name>
  <files>src/lib/error.js</files>
  <action>
Add error handling utilities:
1. createErrorHandler(commandName) - returns error handler for CLI commands
2. wrapAsync(fn) - wraps async functions with error handling
3. isBgsdError(error) - type guard for BgsdError
4. getErrorCode(error) - extracts error code or 'UNKNOWN'

The error handler should:
- Catch errors and format them nicely
- Exit with appropriate code (1 for errors, 0 for warnings)
- Log to stderr, not stdout
  </action>
  <verify>
grep -n "wrapAsync\|createErrorHandler" src/lib/error.js</verify>
  <done>Error utilities available for CLI command integration</done>
</task>

</tasks>

<verification>
- [ ] `node -e "const e = require('./src/lib/error.js'); console.log(e.BgsdError.name)"` works
- [ ] Error with suggestion formats with "Try:" prefix
- [ ] wrapAsync catches and formats errors properly
</verification>

<success_criteria>
Error.js module created with BgsdError class, formatting, and utilities. All exports functional.
</success_criteria>

<output>
After completion, create `.planning/phases/91-rich-tty-output/91-02-SUMMARY.md`
</output>
