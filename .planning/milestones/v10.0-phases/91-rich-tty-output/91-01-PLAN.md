---
phase: 91-rich-tty-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/format.js]
autonomous: true
requirements: [UX-01, UX-02, UX-03]
user_setup: []

must_haves:
  truths:
    - "Commands produce color-coded output when TTY detected or --color flag used"
    - "Commands respect NO_COLOR, --no-color, and --force-color flags"
    - "Progress indicators show spinner for indeterminate operations"
    - "Progress bars support nesting for multi-step operations"
    - "Ctrl+C gracefully cancels long operations"
  artifacts:
    - path: "src/lib/format.js"
      provides: "Enhanced TTY formatting, spinner, nested progress"
      contains: "isTTY, color.enabled, Spinner class, nested progress"
  key_links:
    - from: "src/lib/format.js"
      to: "bin/bgsd-tools.cjs"
      via: "require statement"
      pattern: "require.*format"
---

<objective>
Enhance src/lib/format.js with CLI flag support for color control and advanced progress indicators. Existing format.js already has color, symbols, table, and basic progress bar. This plan adds: (1) CLI flag support for --color/--no-color/--force-color, (2) Spinner for indeterminate progress, (3) Nested progress tracking, (4) Ctrl+C cancellation handling.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-rich-tty-output/91-CONTEXT.md
@src/lib/format.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI flag support for color control</name>
  <files>src/lib/format.js</files>
  <action>
Add CLI flag support to the existing color detection logic:
1. Parse process.argv for --color, --no-color, --force-color flags
2. Update _colorEnabled logic to respect these flags in order: --no-color > --force-color > NO_COLOR > auto-detect
3. Export a function setColorMode(mode) that commands can call to override
4. Keep backward compatibility with existing color.enabled property

Implementation should:
- Check for --no-color first (highest priority)
- Then check for --force-color (forces on)
- Then check NO_COLOR env var
- Then check isTTY
- Allow programmatic override via setColorMode('auto'|'force'|'disable')
  </action>
  <verify>
grep -n "colorEnabled\|setColorMode\|--color\|--no-color\|--force-color" src/lib/format.js</verify>
  <done>Color mode can be controlled via CLI flags and programmatically</done>
</task>

<task type="auto">
  <name>Task 2: Add Spinner class for indeterminate progress</name>
  <files>src/lib/format.js</files>
  <action>
Create a Spinner class that displays ASCII spinner animation:
1. Use classic ASCII characters: '|', '/', '-', '\' cycling
2. Support custom spinner frames if needed
3. Methods: start(message), stop(message), clear()
4. Auto-clears on stop to prevent orphaning
5. Uses process.stderr to not interfere with stdout table output

Example usage:
  const spinner = new Spinner();
  spinner.start('Loading...');
  // ... async work ...
  spinner.stop('Done!');
  </action>
  <verify>
grep -n "class Spinner" src/lib/format.js</verify>
  <done>Spinner class exists and exports from format.js</done>
</task>

<task type="auto">
  <name>Task 3: Add nested progress tracking with cancellation</name>
  <files>src/lib/format.js</files>
  <action>
Enhance progress tracking to support nesting:
1. Create ProgressTracker class that manages multiple nested tasks
2. Each task has: id, label, percent, parent
3. Output format shows hierarchy: "parent > child (45%)"
4. Add cancellation support via signal handler
5. Handle Ctrl+C gracefully - show "Cancelled" message and cleanup

The tracker should:
- Allow starting subtasks that inherit parent progress context
- Support up to 3 levels of nesting
- Show combined progress: parent at 50% with child at 75% shows "Task A > Subtask B (38%)"
- Export single instance as progress global for modules to use
  </action>
  <verify>
grep -n "ProgressTracker\|progress\." src/lib/format.js | head -20</verify>
  <done>ProgressTracker class supports nesting and Ctrl+C cancellation</done>
</task>

</tasks>

<verification>
- [ ] `node -e "const f = require('./src/lib/format.js'); console.log(f.color.enabled)"` works
- [ ] Spinner class can be instantiated and animated
- [ ] ProgressTracker supports nested tasks
- [ ] Ctrl+C handling works in long-running operations
</verification>

<success_criteria>
Enhanced format.js with color flag support, spinner, and nested progress. All new exports available and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/91-rich-tty-output/91-01-SUMMARY.md`
</output>
