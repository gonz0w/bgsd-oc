---
phase: 91-rich-tty-output
plan: 04
type: execute
wave: 2
depends_on: [91-01, 91-02, 91-03]
files_modified: [bin/bgsd-tools.cjs, commands/*.md]
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-10, UX-11, UX-12]
user_setup: []

must_haves:
  truths:
    - "Main CLI uses new format.js, error.js, and debug.js modules"
    - "Commands support --color/--no-color/--force-color flags"
    - "Commands support --debug and --trace flags"
    - "Errors are formatted with context and recovery suggestions"
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "Main CLI with new TTY and error handling"
      contains: "require format, require error, require debug"
  key_links:
    - from: "bin/bgsd-tools.cjs"
      to: "src/lib/format.js"
      via: "require"
      pattern: "require.*format"
---

<objective>
Integrate the new format.js, error.js, and debug.js modules into the main CLI and commands. Wire up CLI flags for color control and debug/trace modes.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-rich-tty-output/91-CONTEXT.md
@src/lib/format.js
@src/lib/error.js
@src/lib/debug.js
@bin/bgsd-tools.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate new modules into main CLI</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Update bin/bgsd-tools.cjs to use new modules:
1. Add require for format.js, error.js, debug.js at top
2. Parse CLI flags at startup:
   - Check for --color, --no-color, --force-color
   - Check for --debug, --trace
   - Call format.setColorMode() based on flags
3. Wrap main command handler with debug flag handling
4. Add global error handler using error.js wrapAsync
5. Ensure errors from commands show recovery suggestions

Integration should be minimal - just wire up the modules.
The actual command implementations will use the modules.
  </action>
  <verify>
grep -n "require.*format\|require.*error\|require.*debug" bin/bgsd-tools.cjs</verify>
  <done>Main CLI requires and uses new modules</done>
</task>

<task type="auto">
  <name>Task 2: Add color and debug flag documentation</name>
  <files>commands/bgsd.md</files>
  <action>
Update command documentation to reflect new flags:
1. Add --color, --no-color, --force-color to relevant commands
2. Add --debug and --trace flags
3. Document that errors now include recovery suggestions
4. Update help text if needed

Check commands/ directory for existing command files.
  </action>
  <verify>
grep -n "\-\-color\|\-\-debug\|\-\-trace" commands/*.md | head -10</verify>
  <help>
Check if commands directory exists and has command definition files.
</help>
  <done>Command documentation reflects new flags</done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end functionality</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Test the integration end-to-end:
1. Run `node bin/bgsd-tools.cjs --help` - should work
2. Run with --color flag - should enable colors
3. Run with --no-color - should disable colors  
4. Run a command that produces an error - should show formatted error
5. If any command uses progress, verify spinner works

Test a few core commands to ensure integration is correct.
  </action>
  <verify>
node bin/bgsd-tools.cjs --help | head -5</verify>
  <done>CLI runs with new flags and shows formatted output</done>
</task>

</tasks>

<verification>
- [ ] CLI starts without errors
- [ ] --color/--no-color flags work
- [ ] --debug/--trace flags work
- [ ] Error output is formatted with context
</verification>

<success_criteria>
All new modules integrated into main CLI with working flags. Commands produce enhanced output.
</success_criteria>

<output>
After completion, create `.planning/phases/91-rich-tty-output/91-04-SUMMARY.md`
</output>
