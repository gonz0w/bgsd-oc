---
phase: 91-rich-tty-output
plan: 03
type: execute
wave: 2
depends_on: [91-01, 91-02]
files_modified: [src/lib/debug.js]
autonomous: true
requirements: [UX-11]
user_setup: []

must_haves:
  truths:
    - "Debug helpers available via --trace and --debug flags"
    - "trace command shows stack traces with source context"
    - "context dump displays current execution state"
    - "state command inspects project planning state"
  artifacts:
    - path: "src/lib/debug.js"
      provides: "Debug utilities for troubleshooting"
      contains: "trace, dumpContext, inspectState, debug flag handlers"
  key_links:
    - from: "src/lib/debug.js"
      to: "src/lib/error.js"
      via: "require for error handling"
      pattern: "require.*error"
---

<objective>
Create src/lib/debug.js module for debugging and troubleshooting utilities. Provides trace, context dump, and state inspection capabilities. Integrates with error.js for consistent output.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-rich-tty-output/91-CONTEXT.md
@src/lib/format.js
@src/lib/error.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create debug module with trace functionality</name>
  <files>src/lib/debug.js</files>
  <action>
Create src/lib/debug.js with trace utilities:
1. Create trace(message, data) - logs with timestamp and optional data
2. Create traceError(error) - formats error with stack trace
3. Create traceStack(depth) - shows caller stack with source lines
4. Add getTrace() to capture current call stack as array
5. Support levels: debug, info, warn, error

Each trace entry includes:
- timestamp (ISO 8601)
- level (debug|info|warn|error)
- message
- optional data object
- stack trace (for errors)

Trace output uses format.js colors for level distinction.
  </action>
  <verify>
grep -n "function trace\|exports.trace" src/lib/debug.js</verify>
  <done>Trace functionality available with timestamps and stack traces</done>
</task>

<task type="auto">
  <name>Task 2: Create context dump utilities</name>
  <files>src/lib/debug.js</files>
  <action>
Add context dump functionality:
1. Create dumpContext(options) - captures and formats current execution context:
   - Working directory
   - Environment variables (filtered for secrets)
   - Command line arguments
   - Current phase/plan from STATE.md
   - Recent files modified
2. Create dumpState() - reads and formats .planning/STATE.md
3. Create dumpConfig() - shows current configuration
4. Output formatted with format.js table/box

Context is valuable for debugging issues reported by users.
  </action>
  <verify>
grep -n "dumpContext\|dumpState" src/lib/debug.js</verify>
  <done>Context dump shows relevant debugging information</done>
</task>

<task type="auto">
<parameter name>Task 3: Create state inspection utilities</name>
<files>src/lib/debug.js</files>
<action>
Add state inspection:
1. Create inspectState(path) - reads and parses STATE.md
2. Create inspectPhase(phaseNum) - reads phase details
3. Create inspectRoadmap() - shows roadmap overview
4. Create inspectPlans(phaseNum) - lists plans for a phase

Use format.js table for readable output.
All functions handle missing files gracefully (return helpful message).
  </action>
  <verify>
grep -n "inspectState\|inspectRoadmap" src/lib/debug.js</verify>
  <done>State inspection functions available for debugging</done>
</task>

<task type="auto">
  <name>Task 4: Create debug flag integration</name>
<files>src/lib/debug.js</files>
  <action>
Add CLI flag integration:
1. Create parseDebugFlags(argv) - extracts --trace, --debug from argv
2. Create isDebugEnabled() - returns debug state
3. Create isTraceEnabled() - returns trace state
4. Auto-enable verbose error output when debug is on

These integrate with format.js color detection so --debug enables colors too.
  </action>
  <verify>
grep -n "parseDebugFlags\|isDebugEnabled" src/lib/debug.js</verify>
  <done>Debug flags can control debug/trace output</done>
</task>

</tasks>

<verification>
- [ ] `node -e "const d = require('./src/lib/debug.js'); console.log(typeof d.trace)"` works
- [ ] dumpContext shows current state
- [ ] parseDebugFlags correctly identifies --debug/--trace
</verification>

<success_criteria>
Debug.js module created with trace, context dump, state inspection, and flag parsing. All exports functional.
</success_criteria>

<output>
After completion, create `.planning/phases/91-rich-tty-output/91-03-SUMMARY.md`
</output>
