---
phase: 92-planning-intelligence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/planner-dependency-graph/SKILL.md
  - skills/planner-task-breakdown/SKILL.md
  - skills/planner-scope-estimation/SKILL.md
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03

must_haves:
  truths:
    - Planner produces task decomposition with clear dependency chains
    - Tasks sized using 15-60 minute heuristic automatically
    - Independent tasks grouped for parallel execution
  artifacts:
    - Enhanced skill files with specific patterns
    - New CLI command for dependency detection
  key_links:
    - Skills loaded by planner agent during planning
    - CLI commands invoked to validate plans
---

<objective>
Enhance planner skills to produce better task decomposition with automatic dependency detection, task sizing, and parallelization analysis.

Purpose: Make the planner agent produce higher-quality execution plans with less manual intervention by codifying best practices into skills and validation tools.

Output: Enhanced skills and new validation commands.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-planning-intelligence/92-CONTEXT.md
@/home/cam/.config/opencode/skills/planner-dependency-graph/SKILL.md
@/home/cam/.config/opencode/skills/planner-task-breakdown/SKILL.md
@/home/cam/.config/opencode/skills/planner-scope-estimation/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance planner-dependency-graph skill with detection patterns</name>
  <files>
    - skills/planner-dependency-graph/SKILL.md
  </files>
  <action>
Enhance the planner-dependency-graph skill with specific file-level and pattern-based detection patterns:

1. Add "dependency_detection_patterns" section with:
   - File-level detection: Track reads/writes, infer from overlaps
   - Pattern-based: tests depend on source, configs depend on schemas
   - Confidence weighting rules: file-level is authoritative

2. Add "automatic_detection_cli" section:
   - Document new CLI command: `bgsd-tools util:analyze-deps <phase-dir>`
   - Shows suggested dependencies based on file analysis
   - Outputs confidence-weighted suggestions

3. Add "wave_computation_rules" section:
   - Wave 1: tasks with no dependencies
   - Wave N+1: tasks depending on Wave N
   - Special handling for checkpoint tasks

4. Update skill frontmatter sections to include: dependency_detection, wave_computation
  </action>
  <verify>
cat skills/planner-dependency-graph/SKILL.md | grep -E "(dependency_detection|wave_computation|automatic_detection)"
</verify>
  <done>
Skill enhanced with dependency detection patterns and wave computation rules. New sections document how to detect dependencies and compute waves automatically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance planner-task-breakdown skill with sizing feedback loop</name>
  <files>
    - skills/planner-task-breakdown/SKILL.md
  </files>
  <action>
Enhance the planner-task-breakdown skill with task sizing heuristics and execution feedback:

1. Expand "task_sizing" section with:
   - 15-60 minute target per task
   - Size indicators: file count, complexity, cross-subsystem
   - Split signals: >5 files, >1 subsystem, >1 paragraph action

2. Add "execution_feedback_loop" section:
   - Track actual execution time per task
   - Store in .planning/metrics/task-times.json
   - Use historical data to improve future sizing estimates

3. Add "sizing_cli_command" section:
   - Document: `bgsd-tools util:estimate-scope <plan-path>`
   - Returns estimated complexity, file count, context impact
   - Uses historical task timing data

4. Add "specificity_checklist" section:
   - Files: Exact paths, no "relevant files"
   - Action: Specific implementation, no vague "make it work"
   - Verify: Command to run, not "test it"
   - Done: Measurable criteria, not "complete"
  </action>
  <verify>
cat skills/planner-task-breakdown/SKILL.md | grep -E "(execution_feedback|sizing_cli|specificity_checklist)"
</verify>
  <done>
Skill enhanced with execution feedback loop documentation and CLI command references. Task sizing now includes historical data usage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance planner-scope-estimation with parallelization analysis</name>
  <files>
    - skills/planner-scope-estimation/SKILL.md
  </files>
  <action>
Enhance the planner-scope-estimation skill with parallelization detection:

1. Add "parallelization_detection" section:
   - File conflict analysis: same files = cannot parallelize
   - Resource conflict: shared state, global variables
   - Hybrid approach: analyze both file and resource conflicts

2. Add "wave_analysis_cli" section:
   - Document: `bgsd-tools verify:plan-wave <phase-dir>`
   - Shows file conflicts within each wave
   - Warns about unsafe parallelization

3. Add "vertical_slice_checklist" section:
   - Each plan is a feature, not a layer
   - Independent features = parallel execution
   - Avoid: separate model/API/UI plans

4. Add "context_budget_calculator" section:
   - Formula: context = files * complexity_factor
   - Target: 50% max per plan
   - Split if: >40% with high complexity
  </action>
  <verify>
cat skills/planner-scope-estimation/SKILL.md | grep -E "(parallelization_detection|wave_analysis|vertical_slice)"
</verify>
  <done>
Skill enhanced with parallelization detection and wave analysis documentation. Vertical slice principles codified.
  </done>
</task>

</tasks>

<verification>
- All three skills enhanced with new sections
- CLI commands documented in skills
- No circular references introduced
- Wave computation rules documented
</verification>

<success_criteria>
Skills enhanced with:
- Dependency detection patterns (file-level + pattern-based)
- Task sizing with execution feedback loop
- Parallelization analysis with conflict detection
- CLI commands for validation

Each skill documents how to apply the technique automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/92-planning-intelligence/92-01-SUMMARY.md`
</output>
