---
phase: 95-interactive-workflows
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/bgsd-tools.cjs
  - src/lib/prompts.js
  - src/lib/wizard.js
  - src/lib/abort-handler.js
  - src/plugin/context-builder.js
autonomous: true
requirements:
  - UX-04
  - UX-05
  - UX-06
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "Complex commands support guided prompts/wizards"
    - "Interactive mode available for multi-step tasks"
    - "User can abort interactive workflows gracefully"
    - "Compaction preserves full context (decisions, blockers, intent, trajectory)"
    - "Compaction automatically protects sacred project data"
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "Main CLI with interactive mode and enhanced compaction"
      contains: "interactivePrompt, wizard, abortHandler, buildTrajectoryBlock, buildSacredBlock"
    - path: "src/lib/prompts.js"
      provides: "Prompt factory and reusable question templates"
      contains: "createPrompt, promptTemplates, inputPrompt, confirmPrompt, listPrompt"
    - path: "src/lib/wizard.js"
      provides: "Multi-step wizard orchestration"
      contains: "WizardRunner, runWizard, abortWizard"
    - path: "src/lib/abort-handler.js"
      provides: "Graceful Ctrl+C and abort handling"
      contains: "setupAbortHandler, handleAbort, cleanupAndExit"
    - path: "src/plugin/context-builder.js"
      provides: "Enhanced compaction context building"
      contains: "buildTrajectoryBlock, buildSacredBlock"
  key_links:
    - from: "prompts.js"
      to: "inquirer v8"
      via: "CommonJS compatible prompt library"
      pattern: "require.*inquirer"
    - from: "context-builder.js"
      to: "existing compaction"
      via: "extend buildCompactionContext"
      pattern: "blocks.push.*trajectory"
---

<objective>
Implement interactive workflows with guided prompts/wizards and enhanced compaction context preservation.

Purpose: Enable complex CLI commands to guide users through multi-step tasks with prompts, while ensuring long-running sessions preserve full project context including decisions, blockers, intent, trajectory, and sacred project data.

Output: New prompt/wizard modules, enhanced compaction in context-builder.js
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/src/lib/prompts.js
@/home/cam/.config/opencode/bgsd-oc/src/lib/wizard.js
@/home/cam/.config/opencode/bgsd-oc/src/lib/abort-handler.js
@/home/cam/.config/opencode/bgsd-oc/src/plugin/context-builder.js
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-interactive-workflows/95-RESEARCH.md
@.planning/INTENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompts.js module with prompt factory</name>
  <files>src/lib/prompts.js</files>
  <action>
Create src/lib/prompts.js with reusable prompt utilities:

1. Add inquirer v8 import:
   ```javascript
   const inquirer = require('inquirer');
   ```

2. Create prompt factory functions:
   - inputPrompt(name, message, options) - text input with validation
   - confirmPrompt(name, message, default) - yes/no confirmation
   - listPrompt(name, message, choices) - single selection
   - checkboxPrompt(name, message, choices) - multi-selection
   - createPrompt(questions) - generic multi-question prompt

3. Add common prompt templates:
   - projectNamePrompt - with validation (3-50 chars, valid filename)
   - phaseSelectionPrompt - list of available phases
   - planSelectionPrompt - list of plans in current phase
   - confirmActionPrompt - confirmation with custom message

4. Add prompt utilities:
   - promptWithDefaults(questions, defaults) - merge defaults
   - validateInput(validator, value) - run validator function

5. Export all functions and templates

Use existing project patterns: export via module.exports, JSDoc comments, consistent error handling.
  </action>
  <verify>
- Check that inquirer is imported (require('inquirer'))
- Verify all prompt factory functions are exported
- Test with: node -e "const p = require('./src/lib/prompts.js'); console.log(typeof p.inputPrompt)"
  </verify>
  <done>
prompts.js exports inputPrompt, confirmPrompt, listPrompt, checkboxPrompt, createPrompt functions
projectNamePrompt template validates 3-50 char names
All functions have JSDoc comments
  </done>
</task>

<task type="auto">
  <name>Task 2: Create wizard.js for multi-step workflows</name>
  <files>src/lib/wizard.js</files>
  <action>
Create src/lib/wizard.js for orchestrating multi-step interactive workflows:

1. Import prompts module:
   ```javascript
   const { createPrompt } = require('./prompts');
   ```

2. Create WizardRunner class:
   - constructor(steps, options) - define wizard steps and options
   - run() - execute wizard steps sequentially
   - abort() - handle graceful getState() - abort
   - return current progress

3. Implement runWizard helper function:
   - Takes array of step definitions
   - Returns collected answers object
   - Handles abort gracefully

4. Add wizard utilities:
   - validateStep(answers, step) - validate current step
   - transformAnswers(answers, transformers) - format final answers
   - isAborted(error) - detect abort conditions

5. Add example wizards:
   - createProjectWizard() - full project creation flow
   - planPhaseWizard() - phase planning flow

Export WizardRunner class and helper functions.
  </action>
  <verify>
- Check WizardRunner class is exported
- Verify runWizard returns answers object on success
- Test abort detection with mock error
  </verify>
  <done>
WizardRunner class handles step-by-step execution
runWizard returns collected answers or null on abort
Example wizards (createProjectWizard, planPhaseWizard) defined
  </done>
</task>

<task type="auto">
  <name>Task 3: Create abort-handler.js for graceful cancellation</name>
  <files>src/lib/abort-handler.js</files>
  <action>
Create src/lib/abort-handler.js for handling Ctrl+C and abort signals:

1. Add SIGINT handler:
   ```javascript
   process.on('SIGINT', () => { /* cleanup */ });
   ```

2. Create abort handler functions:
   - setupAbortHandler(callbacks) - register cleanup callbacks
   - handleAbort(reason) - graceful shutdown with reason
   - cleanupAndExit(exitCode) - run cleanup and exit

3. Add prompt wrapper with abort handling:
   - safePrompt(questions) - wrap inquirer.prompt with abort handling
   - isAbortError(error) - detect inquirer abort conditions
   - formatAbortMessage(error) - user-friendly abort message

4. Add state preservation on abort:
   - saveProgress(state, path) - save current progress
   - loadProgress(path) - resume from saved state
   - clearProgress(path) - remove saved state

5. Export all functions with clear names
  </action>
  <verify>
- Check SIGINT handler is registered
- Verify safePrompt catches abort errors
- Test: node -e "process.kill('SIGINT')" should trigger handler
  </verify>
  <done>
Ctrl+C triggers graceful cleanup with message
safePrompt returns null on abort (not throw)
Progress can be saved/resumed across aborts
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend context-builder.js with trajectory block</name>
  <files>src/plugin/context-builder.js</files>
  <action>
Extend src/plugin/context-builder.js with trajectory preservation:

1. Find function (around line buildCompactionContext 140)

2. Add buildTrajectoryBlock function:
   ```javascript
   function buildTrajectoryBlock(state, plans) {
     // Extract recent completed tasks from plans
     // Include phase progression history
     // Return <trajectory> XML block
   }
   ```

3. Integrate trajectory block into buildCompactionContext:
   - After session block, add trajectory block
   - Include: completed phases, completed plans, task history
   - Keep under 200 tokens (trajectory is supplemental)

4. Define trajectory data to preserve:
   - Phase progression: which phases completed
   - Plan completion: which plans executed successfully
   - Key milestones: major achievements in session

Target: ~100-200 tokens for trajectory block.
  </action>
  <verify>
- Check buildTrajectoryBlock function exists
- Verify trajectory block appears in compaction output
- Test: call buildCompactionContext and check for &lt;trajectory&gt;
  </verify>
  <done>
buildTrajectoryBlock function created and exported
Trajectory block appears in compaction context
Preserves phase progression and plan completion history
  </done>
</task>

<task type="auto">
  <name>Task 5: Extend context-builder.js with sacred data protection</name>
  <files>src/plugin/context-builder.js</files>
  <action>
Extend src/plugin/context-builder.js with sacred data protection:

1. Add buildSacredBlock function:
   ```javascript
   function buildSacredBlock(intent, roadmap) {
     // Extract protected project data
     // Return <sacred> XML block
   }
   ```

2. Define sacred data (always preserve):
   - INTENT.md objectives (DO-XX items)
   - ROADMAP.md current phase and milestone
   - Critical decisions marked as permanent
   - Project core value from PROJECT.md

3. Add sacred data markers:
   - In intent parsing, mark certain outcomes as sacred
   - In roadmap, protect current phase info

4. Integrate sacred block into buildCompactionContext:
   - Add as first block (most important)
   - Include warning if block is missing

Target: ~150-250 tokens for sacred block.
  </action>
  <verify>
- Check buildSacredBlock function exists
- Verify sacred block includes INTENT objectives
- Verify sacred block is first in compaction output
  </verify>
  <done>
buildSacredBlock function created and exported
Sacred block includes INTENT objectives and roadmap phase
Sacred block appears first in compaction context
  </done>
</task>

<task type="auto">
  <name>Task 6: Integrate interactive modules into bgsd-tools.cjs</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Integrate new modules into the main CLI:

1. Add requires for new modules:
   ```javascript
   const prompts = require('./src/lib/prompts');
   const wizard = require('./src/lib/wizard');
   const abortHandler = require('./src/lib/abort-handler');
   ```

2. Add new commands or options:
   - Add --interactive flag to relevant commands
   - Create wizard command group (bgsd wizard *)

3. Update execution flow:
   - Check for interactive flag
   - If interactive, use wizard flow instead of direct execution

4. Ensure graceful abort works in all execution paths:
   - Register abort handler early in CLI startup
   - Wrap critical operations with safePrompt

5. Test integration:
   - Run bgsd-tools with --help to verify commands exist
   - Test interactive mode if TTY available
  </action>
  <verify>
- Run: node bin/bgsd-tools.cjs --help
- Check for new interactive-related output
- Verify modules are required without errors
  </verify>
  <done>
New modules required in main CLI
--interactive flag available on relevant commands
Abort handler active during CLI execution
  </done>
</task>

</tasks>

<verification>
1. All 6 tasks completed and verified
2. prompts.js exports inputPrompt, confirmPrompt, listPrompt, checkboxPrompt, createPrompt
3. wizard.js exports WizardRunner class and runWizard function
4. abort-handler.js handles Ctrl+C gracefully
5. context-builder.js includes <trajectory> block in output
6. context-builder.js includes <sacred> block as first block
7. Main CLI integrates interactive mode
8. All tests pass: npm test
</verification>

<success_criteria>
- Interactive prompts work via inquirer v8
- Multi-step wizards guide users through complex tasks
- Ctrl+C abort is graceful with cleanup
- Compaction preserves: decisions, blockers, intent, trajectory, sacred data
- All new functionality integrated into main CLI
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/95-interactive-workflows/95-01-SUMMARY.md`
