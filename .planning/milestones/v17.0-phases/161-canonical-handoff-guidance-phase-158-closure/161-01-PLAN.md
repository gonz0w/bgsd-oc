---
phase: 161-canonical-handoff-guidance-phase-158-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/init.js
  - bin/bgsd-tools.cjs
  - tests/init.test.cjs
  - tests/state.test.cjs
  - tests/integration.test.cjs
  - tests/guidance-command-integrity-workflows-handoffs.test.cjs
  - tests/workflow.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-02, CMD-03, CMD-05, CMD-06]
must_haves:
  truths:
    - "Generated resume-summary next-step guidance uses canonical planning-family commands for discuss and research handoffs, not legacy planning-prep aliases."
    - "Default restart and repair guidance points users back to canonical `/bgsd-plan discuss <phase>` re-entry with concrete phase arguments."
    - "Source and built CLI stay aligned on the canonical handoff contract, so runtime JSON output and shipped behavior match after rebuilds."
    - "Regression coverage proves dynamic handoff guidance stays validator-clean and keeps the remaining Phase 158 canonical-command blocker from reopening."
  artifacts:
    - path: "src/commands/init.js"
      provides: "Canonical next-safe-command and restart guidance generation for phase handoffs"
    - path: "bin/bgsd-tools.cjs"
      provides: "Built single-file CLI carrying the canonical handoff guidance contract"
    - path: "tests/init.test.cjs"
      provides: "Unit coverage for resume-summary and repair-guidance command defaults"
    - path: "tests/state.test.cjs"
      provides: "Contract coverage for persisted handoff next-command defaults"
    - path: "tests/integration.test.cjs"
      provides: "End-to-end proof that production-written handoffs resume through canonical planning-family commands"
    - path: "tests/guidance-command-integrity-workflows-handoffs.test.cjs"
      provides: "Validator-backed regression over workflow and persisted handoff guidance surfaces"
    - path: "tests/workflow.test.cjs"
      provides: "Workflow-level regression coverage for canonical planning-family alias and handoff wording"
  key_links:
    - from: "src/commands/init.js"
      to: "bin/bgsd-tools.cjs"
      via: "rebuilt CLI bundle preserves canonical handoff output"
    - from: "src/commands/init.js"
      to: "tests/init.test.cjs"
      via: "resume-summary and repair-guidance command assertions"
    - from: "src/commands/init.js"
      to: "tests/integration.test.cjs"
      via: "production handoff chain resumes through canonical commands"
    - from: "tests/guidance-command-integrity-workflows-handoffs.test.cjs"
      to: ".planning/phase-handoffs/159/discuss.json"
      via: "validator-backed proof over persisted canonical handoff guidance"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - Generated resume-summary next-step guidance uses canonical planning-family commands for discuss and research handoffs, not legacy planning-prep aliases.
      - Default restart and repair guidance points users back to canonical `/bgsd-plan discuss <phase>` re-entry with concrete phase arguments.
      - Source and built CLI stay aligned on the canonical handoff contract, so runtime JSON output and shipped behavior match after rebuilds.
      - Regression coverage proves dynamic handoff guidance stays validator-clean and keeps the remaining Phase 158 canonical-command blocker from reopening.
    artifacts:
      - src/commands/init.js
      - bin/bgsd-tools.cjs
      - tests/init.test.cjs
      - tests/state.test.cjs
      - tests/integration.test.cjs
      - tests/guidance-command-integrity-workflows-handoffs.test.cjs
      - tests/workflow.test.cjs
    key_links:
      - Init handoff helpers and the built CLI emit the same canonical planning-family guidance.
      - Dynamic handoff output is locked by direct regression and validator-backed tests.
---

<objective>
Close the remaining Phase 158 blocker by canonicalizing generated planning-prep handoff and restart guidance in the runtime handoff path.

Purpose: The milestone audit now shows static workflow/docs guidance is clean, but `src/commands/init.js` still emits legacy `/bgsd-discuss-phase` and `/bgsd-research-phase` routes in resume-summary and repair flows, which keeps CMD-03, CMD-05, and CMD-06 partially open.
Output: Canonical runtime handoff command generation, rebuilt shipped CLI output, and focused proof that generated guidance stays aligned with the planning-family contract.
</objective>

> **TDD Decision:** Selected — these handoff and restart commands are deterministic runtime outputs with exact expected strings, so the safest implementation path is to lock the desired canonical outputs in tests before or alongside the helper changes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/MILESTONE-INTENT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v17.0-MILESTONE-AUDIT.md
@.planning/phases/158-canonical-command-families/158-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phase-handoffs/159/discuss.json
@src/commands/init.js
@commands/bgsd-discuss-phase.md
@commands/bgsd-research-phase.md
@tests/init.test.cjs
@tests/state.test.cjs
@tests/integration.test.cjs
@tests/guidance-command-integrity-workflows-handoffs.test.cjs
@tests/workflow.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize generated handoff next-step and restart commands at the source</name>
  <files>src/commands/init.js, bin/bgsd-tools.cjs</files>
  <action>Update the phase-handoff helper logic so discuss-step next-safe commands resolve to `/bgsd-plan research ${phase}` and default restart guidance resolves to `/bgsd-plan discuss ${phase}` with the concrete safe phase token. Preserve alias compatibility elsewhere, but make the generated runtime output itself canonical-first. Rebuild `bin/bgsd-tools.cjs` immediately after the source change so CLI tests and shipped behavior stay aligned.</action>
  <verify>npm run build</verify>
  <done>- `src/commands/init.js` no longer emits legacy planning-prep aliases in generated handoff or repair guidance
- `bin/bgsd-tools.cjs` is rebuilt from the updated source
- Source and built CLI expose the same canonical handoff contract</done>
</task>

<task type="auto">
  <name>Task 2: Lock canonical handoff output across unit state and integration tests</name>
  <files>tests/init.test.cjs, tests/state.test.cjs, tests/integration.test.cjs</files>
  <action>Update the existing resume-summary, handoff-write, and end-to-end handoff-chain tests so they assert canonical `/bgsd-plan research` and `/bgsd-plan discuss` strings where the runtime generates or persists planning-prep next commands. Keep the coverage focused on actual runtime output and repair guidance, not static docs already closed in Phase 159.</action>
  <verify>node --test tests/init.test.cjs tests/state.test.cjs tests/integration.test.cjs</verify>
  <done>- Unit and integration tests fail if runtime handoff output regresses to `/bgsd-research-phase` or `/bgsd-discuss-phase`
- Persisted handoff defaults and resume summaries both use canonical planning-family commands with concrete phase arguments
- The production handoff chain still resumes correctly after the command changes</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen canonical-guidance proof for dynamic handoff surfaces</name>
  <files>tests/guidance-command-integrity-workflows-handoffs.test.cjs, tests/workflow.test.cjs</files>
  <action>Extend the focused workflow/handoff regression coverage so it explicitly treats persisted handoff guidance and workflow handoff wording as part of the canonical planning-family proof path for this blocker slice. Keep the assertions narrow: dynamic and persisted planning-prep next commands must stay canonical, legacy aliases remain compatibility-only in wrapper commands, and the shared validator continues to accept the canonical handoff examples with zero issues.</action>
  <verify>node --test tests/guidance-command-integrity-workflows-handoffs.test.cjs tests/workflow.test.cjs && node bin/bgsd-tools.cjs util:validate-commands</verify>
  <done>- Canonical handoff and workflow guidance are locked by direct regression coverage
- Shared validator proof includes the persisted canonical handoff example with zero issues
- Phase 158's remaining canonical-guidance blocker has an auditable regression path instead of relying on manual inspection</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `node --test tests/init.test.cjs tests/state.test.cjs tests/integration.test.cjs`
3. `node --test tests/guidance-command-integrity-workflows-handoffs.test.cjs tests/workflow.test.cjs`
4. `node bin/bgsd-tools.cjs util:validate-commands`
5. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/161-canonical-handoff-guidance-phase-158-closure/161-01-PLAN.md`
</verification>

<success_criteria>
- Generated handoff and repair commands use canonical `/bgsd-plan discuss|research` forms with concrete phase arguments.
- The built CLI and source helper stay aligned on canonical planning-prep runtime output.
- Unit, integration, and validator-backed regressions fail if legacy planning-prep aliases return in generated guidance.
- The remaining Phase 158 blocker has a concrete proof path that can verify canonical guidance across both shipped static surfaces and generated handoff output.
</success_criteria>

<output>
After completion, create `.planning/phases/161-canonical-handoff-guidance-phase-158-closure/161-01-SUMMARY.md`
</output>
