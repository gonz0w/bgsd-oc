---
phase: 158-canonical-command-families
plan: 08
type: tdd
wave: 1
depends_on: []
files_modified:
  - docs/getting-started.md
  - docs/workflows.md
  - tests/guidance-docs.test.cjs
autonomous: true
requirements: [CMD-02, CMD-03]
gap_closure: true
must_haves:
  truths:
    - Getting-started and workflow reference surfaces prefer canonical planning and settings family commands instead of legacy `/bgsd-plan-phase`, `/bgsd-plan-gaps`, or `/bgsd-set-profile` examples.
  artifacts:
    - "path: "docs/getting-started.md"
    - "path: "docs/workflows.md"
    - "path: "tests/guidance-docs.test.cjs"
  key_links: [from: "docs/getting-started.md, from: "docs/workflows.md]
---

<objective>
Close the docs-reference portion of GAP-158-01 by updating the remaining user-facing docs that still recommend legacy planning and settings commands as the preferred path.

Purpose: Phase 158 already shipped canonical routing, but CMD-03 remains incomplete until onboarding and workflow-reference docs consistently steer users toward the canonical family forms.
Output: Canonical `/bgsd-plan ...` and `/bgsd-settings ...` wording in the remaining docs reference surfaces plus focused doc-guidance regressions.
</objective>

> **TDD Decision:** Selected — This gap changes deterministic guidance and runtime next-step behavior with clear string-level outcomes that should be locked by targeted regression tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-VERIFICATION.md
@commands/bgsd-plan.md
@commands/bgsd-settings.md
@commands/bgsd-inspect.md
@docs/getting-started.md
@docs/workflows.md
@workflows/progress.md
@workflows/discuss-phase.md
@workflows/research-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the remaining preferred docs and workflow prompts to canonical family forms</name>
  <files>docs/getting-started.md, docs/workflows.md</files>
  <action>Update only the docs surfaces called out by GAP-158-01 so they recommend `/bgsd-plan phase`, `/bgsd-plan gaps`, `/bgsd-settings profile`, and other already-shipped canonical family forms. Preserve the locked family map from CONTEXT.md: `/bgsd-plan` remains the planning umbrella, `/bgsd-settings` stays separate, `/bgsd-inspect` remains read-only, and legacy commands can still be mentioned only as compatibility aliases rather than the preferred path.</action>
  <verify>npm run test:file -- tests/guidance-docs.test.cjs</verify>
  <done>- `docs/getting-started.md` and `docs/workflows.md` no longer present legacy planning or settings commands as the primary path
- Canonical family forms are used consistently for first-run planning, gap planning, and settings guidance in the touched docs
- The change stays scoped to the verification gap instead of broad Phase 159-style repo auditing</done>
</task>

<task type="auto">
  <name>Task 2: Lock canonical docs guidance and compatibility-only legacy mentions</name>
  <files>docs/getting-started.md, docs/workflows.md, tests/guidance-docs.test.cjs</files>
  <action>Create focused assertions for the touched docs so future edits fail fast if onboarding or workflow-reference examples drift back to `/bgsd-plan-phase`, `/bgsd-plan-gaps`, or `/bgsd-set-profile` as the preferred path. Where a touched doc still needs to mention a legacy command for migration context, rewrite it as compatibility-only wording instead of a preferred example. Do not add broader help-surface auditing or new command families.</action>
  <verify>npm run test:file -- tests/guidance-docs.test.cjs</verify>
  <done>- Tests fail if the touched docs regress to legacy-preferred guidance
- Any remaining legacy mentions in the touched docs are clearly compatibility-only
- The docs teach the canonical family forms first
- GAP-158-01 has direct automated proof instead of manual text review only</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-docs.test.cjs`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/158-canonical-command-families/158-08-PLAN.md`
</verification>

<success_criteria>
- The docs surfaces flagged in GAP-158-01 prefer canonical planning/settings family commands
- Alias compatibility remains intact because the change is guidance-only, not a routing redesign
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-08-SUMMARY.md`
</output>
