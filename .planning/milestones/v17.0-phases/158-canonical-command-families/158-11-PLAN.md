---
phase: 158-canonical-command-families
plan: 11
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/plugin/tools/bgsd-context.js
  - src/plugin/idle-validator.js
  - plugin.js
autonomous: true
requirements: [CMD-03]
gap_closure: true
must_haves:
  truths:
    - Plugin runtime guidance for missing plans and next-phase follow-up surfaces canonical planning-family commands instead of legacy planning names.
    - The shipped `plugin.js` bundle matches the updated source guidance rather than preserving stale legacy strings.
    - The runtime source and shipped bundle are both ready for follow-on plugin regression coverage against canonical guidance wording.
  artifacts:
    - "path: "src/plugin/tools/bgsd-context.js"
    - "path: "src/plugin/idle-validator.js"
    - "path: "plugin.js"
  key_links:
    - "from: "src/plugin/tools/bgsd-context.js"
    - "from: "src/plugin/idle-validator.js"
---

<objective>
Close the plugin-runtime portion of GAP-158-01 by replacing the remaining legacy planning guidance surfaced from plugin tools and idle notifications.

Purpose: CMD-03 is still visibly incomplete if runtime notices tell users to run legacy planning commands even after the canonical family rollout shipped elsewhere.
Output: Canonical planning-family guidance in plugin source and bundled output, ready for focused runtime regression coverage.
</objective>

> **TDD Decision:** Selected — The plugin emits deterministic user-facing guidance strings that should be changed under targeted regression protection.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-VERIFICATION.md
@commands/bgsd-plan.md
@src/plugin/tools/bgsd-context.js
@src/plugin/idle-validator.js
@plugin.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy planning guidance in plugin source notices</name>
  <files>src/plugin/tools/bgsd-context.js, src/plugin/idle-validator.js</files>
  <action>Update the missing-plan prompt and the next-phase idle notification action so they recommend canonical `/bgsd-plan phase` usage. Keep the change limited to surfaced runtime guidance; alias compatibility remains handled by the command wrappers, not by continuing to prefer legacy names in plugin notices.</action>
  <verify>npm run build</verify>
  <done>- Plugin source notices prefer canonical planning-family wording
- No touched runtime guidance string recommends `/bgsd-plan-phase` as the next action
- The change stays within the locked canonical family map</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the shipped plugin bundle after source guidance changes</name>
  <files>plugin.js</files>
  <action>Run the project build so `plugin.js` picks up the updated source strings and the committed bundle stays honest. Treat this as required whenever source-side plugin guidance changes, matching the established Phase 158 runtime parity pattern.</action>
  <verify>npm run build</verify>
  <done>- `plugin.js` contains the updated canonical planning guidance
- The committed runtime artifact matches the source tree
- No stale legacy guidance remains in the shipped bundle</done>
</task>

</tasks>

<verification>
1. `npm run build`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/158-canonical-command-families/158-11-PLAN.md`
</verification>

<success_criteria>
- Plugin runtime notices prefer canonical planning-family commands
- The built plugin artifact matches the updated source guidance
- Plugin source and bundle are ready for follow-on runtime regression assertions
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-11-SUMMARY.md`
</output>
