---
phase: 158-canonical-command-families
plan: 05
type: tdd
wave: 5
depends_on: [158-06, 158-07]
files_modified:
  - commands/bgsd-settings.md
  - commands/bgsd-set-profile.md
  - commands/bgsd-validate-config.md
  - src/plugin/command-enricher.js
  - docs/commands.md
  - workflows/help.md
  - tests/enricher.test.cjs
  - tests/workflow.test.cjs
  - tests/contracts.test.cjs
autonomous: true
requirements: [CMD-02, CMD-03]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - `/bgsd-settings` stays a separate canonical family while legacy settings aliases keep resolving as compatibility routes.
    - Runtime metadata recognizes the canonical command families so canonical names receive the same enrichment and agent routing as legacy names.
    - Directly touched help and command-reference surfaces prefer canonical names without spilling into Phase 159's broader audit scope.
  artifacts:
    - "path: "commands/bgsd-settings.md"
    - "path: "src/plugin/command-enricher.js"
    - "path: "workflows/help.md"
    - "path: "tests/enricher.test.cjs"
  key_links:
    - "from: "commands/bgsd-settings.md"
    - "from: "src/plugin/command-enricher.js"
    - "from: "workflows/help.md"
---

<objective>
Keep settings as its own canonical family and update the directly affected runtime/help surfaces once the planning and inspect families are in place.

Purpose: This final slice finishes the Phase 158 family map without mixing settings work with the larger inspect migration, and it defers broader help/reference cleanup to Phase 159 as intended.
Output: Canonical settings routing, command-enricher parity, and minimal help/reference updates for the new family names.
</objective>

> **TDD Decision:** Selected — This plan changes command routing, runtime enrichment, and guidance contracts with precise parity assertions.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-RESEARCH.md
@.planning/phases/158-canonical-command-families/158-06-SUMMARY.md
@.planning/phases/158-canonical-command-families/158-07-SUMMARY.md
@commands/bgsd-settings.md
@src/plugin/command-enricher.js
@workflows/help.md
@docs/commands.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route settings aliases through a separate canonical settings family</name>
  <files>commands/bgsd-settings.md, commands/bgsd-set-profile.md, commands/bgsd-validate-config.md</files>
  <action>Keep `/bgsd-settings` as its own canonical family and route `set-profile` and `validate-config` through it as thin compatibility shims. Preserve the locked separation from `/bgsd-plan` and `/bgsd-inspect`, and avoid expanding settings into unrelated command families.</action>
  <verify>npm run test:file -- tests/workflow.test.cjs tests/contracts.test.cjs</verify>
  <done>- `/bgsd-settings` remains the separate canonical settings family
- Covered legacy settings wrappers behave as compatibility routes
- Settings does not get folded into planning or inspect families</done>
</task>

<task type="auto">
  <name>Task 2: Update runtime metadata and only the directly contradicted guidance surfaces</name>
  <files>src/plugin/command-enricher.js, workflows/help.md, docs/commands.md</files>
  <action>Audit command-name keyed runtime metadata so canonical `/bgsd-plan`, `/bgsd-inspect`, and `/bgsd-settings` receive the same enrichment and agent-routing treatment as their legacy equivalents. In the same pass, update only directly contradicted help/reference text so these touched surfaces prefer the new canonical family names, while deliberately avoiding the broader command-reference audit and next-step cleanup reserved for Phase 159.</action>
  <verify>npm run test:file -- tests/enricher.test.cjs tests/workflow.test.cjs</verify>
  <done>- Canonical family names are recognized by runtime enrichment and agent-routing logic
- Touched help/reference surfaces no longer immediately contradict the new canonical map
- The change stays narrow and does not attempt Phase 159's repo-wide audit work</done>
</task>

<task type="auto">
  <name>Task 3: Add parity regressions for settings and canonical runtime metadata</name>
  <files>tests/enricher.test.cjs, tests/workflow.test.cjs, tests/contracts.test.cjs</files>
  <action>Add focused regressions that compare canonical and legacy settings entrypoints and assert that canonical family names receive the expected enricher metadata. Keep the checks on Phase 158's directly touched surfaces rather than broad command-reference validation.</action>
  <verify>npm run test:file -- tests/enricher.test.cjs tests/workflow.test.cjs tests/contracts.test.cjs</verify>
  <done>- Tests fail if canonical settings commands drift from legacy behavior
- Tests fail if canonical names miss command-enricher metadata
- Coverage stays on touched routing and runtime surfaces instead of Phase 159's full audit scope</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/enricher.test.cjs tests/workflow.test.cjs tests/contracts.test.cjs`
2. `node bin/bgsd-tools.cjs verify:references workflows/help.md`
3. `node bin/bgsd-tools.cjs verify:references docs/commands.md`
</verification>

<success_criteria>
- `/bgsd-settings` remains a separate canonical family with compatibility aliases preserved
- Canonical command names receive the same runtime treatment as legacy aliases
- Directly touched guidance surfaces prefer canonical family names without spilling into Phase 159's full audit scope
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-05-SUMMARY.md`
</output>
