---
phase: 158-canonical-command-families
plan: 12
type: tdd
wave: 2
depends_on: [158-11]
files_modified: [tests/plugin.test.cjs]
autonomous: true
requirements: [CMD-03]
gap_closure: true
must_haves:
  truths:
    - Focused plugin tests fail if canonical runtime guidance regresses back to `/bgsd-plan-phase` wording.
    - Plugin regression coverage protects both missing-plan and next-phase runtime notices introduced by Plan 11.
  artifacts: [path: "tests/plugin.test.cjs]
  key_links: [from: "tests/plugin.test.cjs, from: "tests/plugin.test.cjs]
---

<objective>
Add focused regression coverage for the canonical plugin runtime guidance introduced in the plugin gap-closure slice.

Purpose: GAP-158-01 is not durably closed if the plugin copy changes but there is no targeted test proving those runtime notices stay canonical-first.
Output: Plugin-level regressions for missing-plan and next-phase guidance strings.
</objective>

> **TDD Decision:** Selected — This plan adds deterministic regression proof for specific runtime guidance outputs.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-VERIFICATION.md
@.planning/phases/158-canonical-command-families/158-11-PLAN.md
@src/plugin/tools/bgsd-context.js
@src/plugin/idle-validator.js
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing-plan runtime guidance assertions</name>
  <files>tests/plugin.test.cjs</files>
  <action>Add targeted plugin assertions that fail if the missing-plan guidance exposed through `bgsd_context` reverts to legacy `/bgsd-plan-phase` wording instead of canonical `/bgsd-plan phase` guidance.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>- Tests fail if missing-plan runtime guidance reverts to legacy wording
- Assertions read as direct proof of canonical planning-family guidance</done>
</task>

<task type="auto">
  <name>Task 2: Add next-phase notification action assertions</name>
  <files>tests/plugin.test.cjs</files>
  <action>Add targeted assertions that fail if the idle-validator phase-complete notification action reverts to legacy `/bgsd-plan-phase` wording instead of canonical `/bgsd-plan phase` guidance.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>- Tests fail if next-phase notification actions revert to legacy wording
- Plugin runtime guidance has direct regression proof for both touched surfaces</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/plugin.test.cjs`
2. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/158-canonical-command-families/158-12-PLAN.md`
</verification>

<success_criteria>
- Plugin runtime guidance is protected by focused regression coverage
- Both touched plugin notices fail loudly if legacy-preferred guidance returns
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-12-SUMMARY.md`
</output>
