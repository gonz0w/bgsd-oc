---
phase: 158-canonical-command-families
plan: 06
type: tdd
wave: 4
depends_on: [158-03]
files_modified:
  - tests/plan-phase-workflow.test.cjs
  - tests/workflow.test.cjs
  - tests/contracts.test.cjs
autonomous: true
requirements: [CMD-02, CMD-03]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - Canonical and legacy roadmap, gap-planning, and plan-scoped todo entrypoints land on equivalent underlying behavior.
    - The expanded `/bgsd-plan` family stays one normalized contract rather than separate implementations per alias.
    - Parity coverage for planning-family aliases stays focused on Phase 158 routing behavior rather than Phase 159 help auditing.
  artifacts:
    - "path: "tests/plan-phase-workflow.test.cjs"
    - "path: "tests/workflow.test.cjs"
    - "path: "tests/contracts.test.cjs"
  key_links:
    - "from: "tests/plan-phase-workflow.test.cjs"
    - "from: "tests/contracts.test.cjs"
---

<objective>
Lock the expanded `/bgsd-plan` family behind targeted parity regressions once roadmap, gap, and todo alias routing is in place.

Purpose: Pulling regressions into their own slice removes the remaining scope warning from the routing plan while still proving CMD-03 parity for the grouped planning surface.
Output: Focused tests covering canonical versus legacy roadmap, gap, and plan-scoped todo behavior.
</objective>

> **TDD Decision:** Selected — This plan adds direct regression proof for deterministic routing and parity behavior.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-RESEARCH.md
@.planning/phases/158-canonical-command-families/158-03-SUMMARY.md
@commands/bgsd-plan.md
@workflows/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workflow-level parity coverage for roadmap, gap, and todo flows</name>
  <files>tests/plan-phase-workflow.test.cjs, tests/workflow.test.cjs</files>
  <action>Add focused tests that compare representative canonical and legacy invocations for roadmap mutation, gap planning, and plan-scoped todo flows. Assert shared routing targets and normalized arguments without turning this into a repo-wide command-reference audit.</action>
  <verify>npm run test:file -- tests/plan-phase-workflow.test.cjs tests/workflow.test.cjs</verify>
  <done>- Tests fail if `/bgsd-plan` and the migrated aliases diverge on workflow targets or normalized arguments
- Tests cover roadmap, gap, and todo concerns moved in Plan 03
- Coverage stays on Phase 158 routing behavior</done>
</task>

<task type="auto">
  <name>Task 2: Add contract-level regression checks for planning-family alias normalization</name>
  <files>tests/contracts.test.cjs</files>
  <action>Add contract-oriented checks that assert roadmap, gap, and todo aliases reuse the normalized planning-family contract rather than reintroducing command-specific implementations or preferred alias wording.</action>
  <verify>npm run test:file -- tests/contracts.test.cjs</verify>
  <done>- Contract tests fail if alias normalization drifts from the canonical planning family
- The checks stay limited to grouped planning concerns, not help-surface validation
- CMD-03 parity has direct automated proof</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/plan-phase-workflow.test.cjs tests/workflow.test.cjs tests/contracts.test.cjs`
2. Representative fixture checks show `/bgsd-plan roadmap add ...`, `/bgsd-plan gaps 158`, and `/bgsd-plan todo add ...` normalize to the expected existing workflow contracts
</verification>

<success_criteria>
- Canonical and legacy roadmap, gap, and todo entrypoints have targeted parity coverage
- The expanded `/bgsd-plan` family remains one shared contract
- Regression scope stays inside Phase 158 routing behavior
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-06-SUMMARY.md`
</output>
