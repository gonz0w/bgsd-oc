---
phase: 158-canonical-command-families
plan: 07
type: tdd
wave: 3
depends_on: [158-04]
files_modified:
  - commands/bgsd-velocity.md
  - commands/bgsd-context-budget.md
  - commands/bgsd-session-diff.md
  - commands/bgsd-rollback-info.md
  - commands/bgsd-validate-deps.md
  - tests/workflow.test.cjs
  - tests/contracts.test.cjs
autonomous: true
requirements: [CMD-02, CMD-03]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - The remaining read-only inspect aliases also resolve through `/bgsd-inspect` without changing the locked diagnostics boundary.
    - Canonical and legacy inspect entrypoints for those remaining aliases stay behaviorally equivalent.
    - Inspect-family parity coverage proves the canonical hub remains read-only and Phase 158-scoped.
  artifacts:
    - "path: "commands/bgsd-velocity.md"
    - "path: "commands/bgsd-validate-deps.md"
    - "path: "tests/contracts.test.cjs"
  key_links:
    - "from: "commands/bgsd-velocity.md"
    - "from: "tests/contracts.test.cjs"
---

<objective>
Finish the remaining inspect alias migration and add parity checks once the core `/bgsd-inspect` contract is established.

Purpose: This follow-on slice removes the remaining scope warning from the inspect plan by separating long-tail alias routing and regressions from the core diagnostics-hub migration.
Output: Remaining inspect alias shims and targeted parity tests for canonical versus legacy inspect behavior.
</objective>

> **TDD Decision:** Selected — This plan changes and verifies deterministic inspect routing behavior with clear parity and boundary outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-RESEARCH.md
@.planning/phases/158-canonical-command-families/158-04-SUMMARY.md
@commands/bgsd-inspect.md
@commands/bgsd-velocity.md
@commands/bgsd-validate-deps.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route the remaining inspect aliases through the canonical hub</name>
  <files>commands/bgsd-velocity.md, commands/bgsd-context-budget.md, commands/bgsd-session-diff.md, commands/bgsd-rollback-info.md, commands/bgsd-validate-deps.md</files>
  <action>Route the remaining read-only diagnostic aliases through `/bgsd-inspect` so velocity, context-budget, session-diff, rollback-info, and dependency-validation inspection all reuse the canonical family behavior. Preserve the same strict read-only boundary and keep the work additive rather than inventing new workflow surfaces.</action>
  <verify>node bin/bgsd-tools.cjs verify:references commands/bgsd-validate-deps.md</verify>
  <done>- The remaining targeted inspect aliases route through `/bgsd-inspect`
- The inspect family remains additive and read-only
- Alias wording stays compatibility-focused instead of preferred-surface focused</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical-versus-legacy parity and boundary regressions for the remaining inspect flows</name>
  <files>tests/workflow.test.cjs, tests/contracts.test.cjs</files>
  <action>Add focused regressions that compare canonical and legacy inspect entrypoints for the remaining migrated commands and assert the locked read-only boundary so `/bgsd-inspect` does not silently absorb out-of-scope mutating or domain-specific commands.</action>
  <verify>npm run test:file -- tests/workflow.test.cjs tests/contracts.test.cjs</verify>
  <done>- Tests fail if the remaining canonical inspect commands drift from legacy behavior
- Tests fail if `/bgsd-inspect` starts owning out-of-bound mutating or domain-specific flows
- Coverage stays on inspect-family routing rather than full help/reference cleanup</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/workflow.test.cjs tests/contracts.test.cjs`
2. Representative dry-run or fixture checks show `/bgsd-inspect velocity`, `/bgsd-inspect context-budget`, and `/bgsd-inspect validate-deps` normalize to the expected existing workflow contracts
</verification>

<success_criteria>
- Remaining inspect aliases stay behaviorally equivalent as compatibility shims
- The inspect family remains a strict read-only diagnostics hub
- Parity coverage exists for both core and long-tail inspect aliases without Phase 159 spillover
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-07-SUMMARY.md`
</output>
