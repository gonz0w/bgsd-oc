---
phase: 158-canonical-command-families
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md
  - docs/planning-system.md
  - docs/agents.md
  - docs/expert-guide.md
  - docs/configuration.md
  - docs/troubleshooting.md
  - tests/guidance-docs-phase-158-gap.test.cjs
autonomous: true
requirements: [CMD-03]
gap_closure: true
must_haves:
  truths:
    - Remaining shipped documentation examples in Phase 158 scope prefer canonical `/bgsd-plan ...` and `/bgsd-settings profile` commands instead of legacy aliases as the primary path.
    - Expert and architecture references still mention legacy commands only as compatibility notes when those aliases are worth preserving for migration context.
    - Focused regression coverage fails if these remaining doc surfaces regress to legacy-preferred planning, gap, or settings guidance.
  artifacts:
    - path: "docs/architecture.md"
      provides: "Canonical planning-family example in the architecture data-flow walkthrough"
    - path: "docs/planning-system.md"
      provides: "Canonical planning command reference in the planning-system overview"
    - path: "docs/agents.md"
      provides: "Canonical planner, checker, and researcher spawn examples"
    - path: "docs/expert-guide.md"
      provides: "Canonical deep-dive examples for planning, gap closure, and settings guidance"
    - path: "docs/configuration.md"
      provides: "Canonical settings-family profile-switch example"
    - path: "docs/troubleshooting.md"
      provides: "Canonical skip-verify recovery example"
    - path: "tests/guidance-docs-phase-158-gap.test.cjs"
      provides: "Focused regression coverage for the remaining Phase 158 doc evidence surfaces"
  key_links:
    - from: "docs/architecture.md"
      to: "commands/bgsd-plan.md"
      via: "architecture walkthrough using the canonical planning-family phase subcommand"
      pattern: "/bgsd-plan phase"
    - from: "docs/agents.md"
      to: "commands/bgsd-plan.md"
      via: "agent spawn examples naming canonical phase and research subcommands"
      pattern: "/bgsd-plan phase|/bgsd-plan research"
    - from: "docs/configuration.md"
      to: "commands/bgsd-settings.md"
      via: "configuration quick-switch example using the canonical settings profile subcommand"
      pattern: "/bgsd-settings profile"
    - from: "tests/guidance-docs-phase-158-gap.test.cjs"
      to: "docs/expert-guide.md"
      via: "focused regression assertions covering the remaining doc evidence surfaces"
      pattern: "/bgsd-plan phase|/bgsd-plan gaps|/bgsd-settings profile"
---

<objective>
Close the remaining documentation-reference portion of GAP-158-01 by replacing legacy-preferred planning, gap, and settings examples across the still-shipped docs evidence surfaces.

Purpose: CMD-03 still fails at phase scope if the architecture, planning-system, agent, expert, configuration, and troubleshooting docs keep teaching `/bgsd-plan-phase`, `/bgsd-plan-gaps`, or `/bgsd-set-profile` as the primary path after the canonical family migration shipped.
Output: Canonical command-family wording on the remaining doc evidence files plus focused regression coverage that keeps these references canonical-first.
</objective>

> **TDD Decision:** Selected - This plan changes deterministic command strings in shipped docs and should lock the exact canonical wording with focused regressions.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-VERIFICATION.md
@docs/architecture.md
@docs/planning-system.md
@docs/agents.md
@docs/expert-guide.md
@docs/configuration.md
@docs/troubleshooting.md
@commands/bgsd-plan.md
@commands/bgsd-settings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the remaining architecture and operational docs examples</name>
  <files>docs/architecture.md, docs/planning-system.md, docs/agents.md, docs/configuration.md, docs/troubleshooting.md</files>
  <action>Rewrite the remaining preferred command examples on these evidence surfaces so they use the correct canonical family forms: `/bgsd-plan phase` for planning, `/bgsd-plan research` where the researcher flow is described, and `/bgsd-settings profile` for profile switching. Keep any retained legacy names explicitly compatibility-only. Do not broaden into a repo-wide docs audit beyond the files cited by `158-VERIFICATION.md`.</action>
  <verify>npm run test:file -- tests/guidance-docs-phase-158-gap.test.cjs</verify>
  <done>- The cited architecture, planning-system, agents, configuration, and troubleshooting docs no longer recommend legacy planning or settings aliases as the primary path
- Canonical examples use the right family subcommand for the described flow instead of a generic or incorrect replacement
- Any retained legacy alias wording is clearly compatibility-only</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize the remaining expert-guide planning and gap examples</name>
  <files>docs/expert-guide.md</files>
  <action>Update every GAP-158-01 evidence example in `docs/expert-guide.md` so the guide teaches canonical `/bgsd-plan phase`, `/bgsd-plan gaps`, and related planning-family forms as the primary route. Preserve compatibility mentions only when they help explain migration fallback, and keep the guide aligned with the locked Phase 158 family map rather than starting the broader Phase 159 command-integrity pass.</action>
  <verify>npm run test:file -- tests/guidance-docs-phase-158-gap.test.cjs</verify>
  <done>- The cited expert-guide examples no longer center `/bgsd-plan-phase` or `/bgsd-plan-gaps`
- Gap-closure and research examples point to the correct canonical planning-family subcommand for each scenario
- The guide stays within Phase 158's command-family scope without expanding into unrelated reference cleanup</done>
</task>

<task type="auto">
  <name>Task 3: Add focused regressions for the remaining doc guidance evidence surfaces</name>
  <files>tests/guidance-docs-phase-158-gap.test.cjs</files>
  <action>Create a focused regression file that reads only the remaining doc evidence surfaces from GAP-158-01 and fails if those files revert to legacy-preferred planning, gap, or settings commands. Cover the exact canonical replacements expected on each surface so the test proves correct subcommand usage rather than only the absence of legacy text.</action>
  <verify>npm run test:file -- tests/guidance-docs-phase-158-gap.test.cjs</verify>
  <done>- Focused regression coverage exists for every remaining doc evidence file cited by GAP-158-01
- Tests fail if those surfaces revert to legacy-preferred `/bgsd-plan-phase`, `/bgsd-plan-gaps`, or `/bgsd-set-profile` wording
- Tests also assert the intended canonical replacement strings so the guidance stays semantically correct</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-docs-phase-158-gap.test.cjs`
2. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/158-canonical-command-families/158-14-PLAN.md`
</verification>

<success_criteria>
- The remaining Phase 158 doc evidence surfaces now teach canonical planning and settings family commands first
- Legacy aliases survive only as compatibility notes on those touched documentation surfaces
- Focused regressions give durable proof that these docs stay canonical-first without widening into Phase 159's broader integrity audit
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-14-SUMMARY.md`
</output>
