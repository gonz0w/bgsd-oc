---
phase: 158-canonical-command-families
plan: 03
type: tdd
wave: 3
depends_on: [158-02]
files_modified:
  - commands/bgsd-plan.md
  - commands/bgsd-add-phase.md
  - commands/bgsd-insert-phase.md
  - commands/bgsd-remove-phase.md
  - commands/bgsd-plan-gaps.md
  - commands/bgsd-add-todo.md
  - commands/bgsd-check-todos.md
  - workflows/plan-phase.md
autonomous: true
requirements: [CMD-02, CMD-03]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - `/bgsd-plan` absorbs roadmap mutation, gap-planning entry, and plan-scoped todo actions without reopening a standalone todo surface.
    - Legacy roadmap, gap-planning, and todo commands resolve through the same normalized behavior as the canonical planning family.
    - The expanded planning family still behaves like one contract instead of forking into command-specific implementations.
  artifacts:
    - "path: "commands/bgsd-plan.md"
    - "path: "workflows/plan-phase.md"
    - "path: "commands/bgsd-plan-gaps.md"
  key_links:
    - "from: "commands/bgsd-add-phase.md"
    - "from: "commands/bgsd-plan-gaps.md"
    - "from: "commands/bgsd-add-todo.md"
---

<objective>
Finish the `/bgsd-plan` migration by collapsing roadmap mutation, gap-planning entry, and plan-scoped todo flows behind the canonical planning family.

Purpose: This follow-on slice keeps the earlier planning-contract work executable while moving the remaining planning-oriented aliases onto the same canonical behavior without crossing the scope threshold.
Output: An expanded `/bgsd-plan` family contract and repointed roadmap/gap/todo aliases that a follow-on parity plan can verify.
</objective>

> **TDD Decision:** Selected — This plan changes command-routing behavior with clear canonical-versus-alias parity outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-RESEARCH.md
@.planning/phases/158-canonical-command-families/158-02-SUMMARY.md
@commands/bgsd-plan.md
@commands/bgsd-add-phase.md
@commands/bgsd-plan-gaps.md
@commands/bgsd-check-todos.md
@workflows/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand `/bgsd-plan` to cover roadmap, gaps, and plan-scoped todos</name>
  <files>commands/bgsd-plan.md, workflows/plan-phase.md</files>
  <action>Extend the canonical planning-family contract so `/bgsd-plan` can normalize roadmap add/insert/remove actions, gap-planning entry, and plan-scoped todo add/check operations. Keep the family intentionally narrow: todos remain tied to planning context, and this plan should not invent a standalone general task-management surface.</action>
  <verify>node bin/bgsd-tools.cjs verify:references commands/bgsd-plan.md</verify>
  <done>- `/bgsd-plan` can express the covered roadmap, gap, and todo actions
- Todos remain explicitly plan-scoped under the planning umbrella
- Gap planning is normalized without redesigning the broader milestone-gap surface</done>
</task>

<task type="auto">
  <name>Task 2: Convert roadmap, gap, and todo commands into thin compatibility shims</name>
  <files>commands/bgsd-add-phase.md, commands/bgsd-insert-phase.md, commands/bgsd-remove-phase.md, commands/bgsd-plan-gaps.md, commands/bgsd-add-todo.md, commands/bgsd-check-todos.md</files>
  <action>Repoint the covered legacy wrappers so they route into the normalized `/bgsd-plan` behavior instead of owning separate workflow text or drifting defaults. Keep compatibility intact, but avoid reintroducing any alias as a preferred surface inside the touched files.</action>
  <verify>node bin/bgsd-tools.cjs verify:references commands/bgsd-plan-gaps.md</verify>
  <done>- Covered roadmap, gap, and todo wrappers are clearly compatibility routes
- Canonical and legacy entrypoints land on equivalent underlying behavior
- No touched alias reclaims preferred-surface wording</done>
</task>

<task type="auto">
  <name>Task 3: Document the shared contract boundaries for the follow-on parity slice</name>
  <files>commands/bgsd-plan.md, workflows/plan-phase.md</files>
  <action>Make the canonical planning-family wrapper text and shared workflow contract explicit enough that a follow-on regression plan can compare canonical and legacy roadmap, gap, and todo entrypoints against one normalized behavior. Keep this task implementation-facing and avoid broad help or docs cleanup.</action>
  <verify>node bin/bgsd-tools.cjs verify:references workflows/plan-phase.md</verify>
  <done>- The normalized planning-family contract is explicit in the touched wrapper/workflow files
- A follow-on regression slice can test roadmap, gap, and todo parity without reshaping behavior again
- This slice stays on command routing rather than Phase 159 discoverability work</done>
</task>

</tasks>

<verification>
1. `node bin/bgsd-tools.cjs verify:references commands/bgsd-plan.md`
2. `node bin/bgsd-tools.cjs verify:references commands/bgsd-plan-gaps.md`
3. Representative dry-run or fixture checks show `/bgsd-plan roadmap add ...`, `/bgsd-plan gaps 158`, and `/bgsd-plan todo add ...` normalize to the expected existing workflow contracts
</verification>

<success_criteria>
- `/bgsd-plan` is the real master command for roadmap, gap, and plan-scoped todo flows
- Legacy roadmap, gap, and todo commands stay behaviorally equivalent as compatibility shims
- Plan-scoped todos remain under the planning family instead of reopening a broad todo product surface
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-03-SUMMARY.md`
</output>
