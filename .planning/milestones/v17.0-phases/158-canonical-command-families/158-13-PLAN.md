---
phase: 158-canonical-command-families
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/commands.md
  - workflows/help.md
  - workflows/verify-work.md
  - workflows/new-milestone.md
  - workflows/execute-phase.md
  - workflows/settings.md
  - src/plugin/advisory-guardrails.js
  - plugin.js
  - tests/guidance-remaining-surfaces.test.cjs
autonomous: true
requirements: [CMD-03]
gap_closure: true
must_haves:
  truths:
    - Remaining shipped help, docs, and workflow guidance prefer `/bgsd-plan phase`, `/bgsd-plan gaps`, and `/bgsd-settings profile` instead of legacy aliases as the primary path.
    - Compatibility aliases may still be mentioned on touched surfaces, but only as compatibility-only notes rather than the recommended command.
    - Plugin planning-file advisories and the shipped `plugin.js` bundle no longer recommend `/bgsd-plan-phase` for editing PLAN artifacts safely.
  artifacts:
    - path: "docs/commands.md"
      provides: "Canonical-first command reference entries for planning and settings aliases still shipped in Phase 158 scope"
    - path: "workflows/help.md"
      provides: "Canonical gap-closure help guidance"
    - path: "workflows/verify-work.md"
      provides: "Canonical verify-to-gap handoff command"
    - path: "workflows/new-milestone.md"
      provides: "Canonical skip-discussion planning next step"
    - path: "workflows/execute-phase.md"
      provides: "Canonical execute-to-gap follow-up guidance"
    - path: "workflows/settings.md"
      provides: "Canonical planning/settings quick-command guidance"
    - path: "src/plugin/advisory-guardrails.js"
      provides: "Canonical planning-file advisory command mapping for PLAN.md edits"
    - path: "plugin.js"
      provides: "Bundled plugin artifact matching the updated advisory mapping"
    - path: "tests/guidance-remaining-surfaces.test.cjs"
      provides: "Focused regression coverage for the remaining GAP-158-01 guidance surfaces"
  key_links:
    - from: "docs/commands.md"
      to: "commands/bgsd-plan.md"
      via: "command reference entries teaching canonical planning-family subcommands while demoting legacy aliases to compatibility-only mentions"
      pattern: "/bgsd-plan phase|/bgsd-plan gaps"
    - from: "workflows/settings.md"
      to: "commands/bgsd-settings.md"
      via: "settings quick commands preferring the canonical settings-family profile subcommand"
      pattern: "/bgsd-settings profile"
    - from: "workflows/verify-work.md"
      to: "commands/bgsd-plan.md"
      via: "gap-routing handoff persisting the canonical gaps subcommand"
      pattern: "/bgsd-plan gaps"
    - from: "src/plugin/advisory-guardrails.js"
      to: "plugin.js"
      via: "bundle rebuild keeping shipped planning-file advisory guidance aligned with source"
      pattern: "/bgsd-plan phase"
---

<objective>
Close the last Phase 158 guidance gap by updating the remaining shipped help, docs, workflow, and plugin advisory surfaces that still prefer legacy planning or settings aliases.

Purpose: CMD-03 remains unverified until every still-shipped Phase 158 surface touched by GAP-158-01 points users at the canonical planning and settings families while preserving alias compatibility only as a fallback.
Output: One final gap-closure slice covering the cited docs/workflow/plugin guidance files, bundled plugin parity, and focused regression coverage for those remaining surfaces.
</objective>

> **TDD Decision:** Selected — This plan changes deterministic user-facing command guidance with clear string-level outcomes that should be locked by focused regressions.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-VERIFICATION.md
@commands/bgsd-plan.md
@commands/bgsd-settings.md
@docs/commands.md
@workflows/help.md
@workflows/verify-work.md
@workflows/new-milestone.md
@workflows/execute-phase.md
@workflows/settings.md
@src/plugin/advisory-guardrails.js
@plugin.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the remaining docs and workflow guidance surfaces cited by GAP-158-01</name>
  <files>docs/commands.md, workflows/help.md, workflows/verify-work.md, workflows/new-milestone.md, workflows/execute-phase.md, workflows/settings.md</files>
  <action>Update only the Phase 158 gap evidence surfaces so they prefer `/bgsd-plan phase`, `/bgsd-plan gaps`, and `/bgsd-settings profile` as appropriate. Keep this strictly inside the locked Phase 158 family map from `158-CONTEXT.md`: `/bgsd-plan` remains the master planning umbrella, `/bgsd-settings` stays separate, `/bgsd-inspect` remains read-only, and any legacy alias mentions on touched files must be rewritten as compatibility-only notes instead of preferred examples. Do not broaden into Phase 159 help/reference integrity work or unrelated command auditing.</action>
  <verify>npm run test:file -- tests/guidance-remaining-surfaces.test.cjs</verify>
  <done>- The specific files cited in GAP-158-01 no longer prefer `/bgsd-plan-phase`, `/bgsd-plan-gaps`, or `/bgsd-set-profile`
- Canonical command text matches the correct planning or settings family subcommand for each touched surface
- Any preserved legacy mention in those files is compatibility-only rather than a primary route</done>
</task>

<task type="auto">
  <name>Task 2: Update PLAN.md plugin advisories and rebuild the shipped bundle</name>
  <files>src/plugin/advisory-guardrails.js, plugin.js</files>
  <action>Change the `PLAN.md` planning-file advisory mapping so users editing plan artifacts are told to use canonical `/bgsd-plan phase` guidance instead of `/bgsd-plan-phase`, then rebuild `plugin.js` immediately so the shipped bundle stays in parity with the source wording. Keep alias compatibility behavior unchanged; this is a guidance-only correction.</action>
  <verify>npm run build</verify>
  <done>- Source-side planning-file advisories no longer recommend `/bgsd-plan-phase`
- `plugin.js` contains the same canonical advisory guidance after rebuild
- The change remains limited to surfaced advisory text and bundle parity</done>
</task>

<task type="auto">
  <name>Task 3: Add focused regressions for the remaining guidance and advisory surfaces</name>
  <files>tests/guidance-remaining-surfaces.test.cjs</files>
  <action>Create one focused regression file that reads the touched docs, workflows, advisory source, and bundled plugin output and fails if those remaining GAP-158-01 surfaces regress to legacy-preferred planning, gap, or settings commands. Keep the assertions narrow to the files named in `158-VERIFICATION.md` so this stays a Phase 158 closure slice rather than a repo-wide command-audit harness.</action>
  <verify>npm run test:file -- tests/guidance-remaining-surfaces.test.cjs</verify>
  <done>- Focused tests directly cover every remaining evidence file from GAP-158-01
- Tests fail if those surfaces revert to legacy-preferred aliases
- The regression scope stays limited to the remaining Phase 158 guidance gap</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `npm run test:file -- tests/guidance-remaining-surfaces.test.cjs`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/158-canonical-command-families/158-13-PLAN.md`
</verification>

<success_criteria>
- Every file named in GAP-158-01 now prefers canonical planning/settings family commands on its touched user-facing guidance path
- PLAN.md plugin advisories and the shipped bundle are aligned on canonical `/bgsd-plan phase` guidance
- Focused regressions give direct proof for the remaining Phase 158 guidance gap without expanding into Phase 159's broader integrity audit
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-13-SUMMARY.md`
</output>
