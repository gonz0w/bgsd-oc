---
phase: 158-canonical-command-families
plan: 04
type: tdd
wave: 2
depends_on: [158-01]
files_modified:
  - commands/bgsd-inspect.md
  - commands/bgsd-progress.md
  - commands/bgsd-impact.md
  - commands/bgsd-trace.md
  - commands/bgsd-search-decisions.md
  - commands/bgsd-search-lessons.md
  - commands/bgsd-health.md
autonomous: true
requirements: [CMD-02, CMD-03]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - `/bgsd-inspect` becomes the canonical read-only diagnostics hub for the intended cross-cutting inspection flows.
    - Legacy inspect-oriented commands resolve through the same canonical behavior instead of drifting into separate implementations.
    - The inspect family keeps its locked read-only boundary and does not absorb mutating or domain-specific review/security/readiness/release flows.
  artifacts:
    - "path: "commands/bgsd-inspect.md"
    - "path: "commands/bgsd-health.md"
  key_links:
    - "from: "commands/bgsd-progress.md"
    - "from: "commands/bgsd-search-decisions.md"
    - "from: "commands/bgsd-inspect.md"
---

<objective>
Move the intended diagnostics commands under `/bgsd-inspect` while preserving a strict read-only boundary and keeping the migrated aliases behaviorally equivalent.

Purpose: Splitting inspect routing away from the remaining inspect aliases and parity work keeps the core diagnostics migration comfortably inside the safer execution slice.
Output: A real `/bgsd-inspect` family contract and repointed core legacy inspection wrappers that a follow-on slice can extend and verify.
</objective>

> **TDD Decision:** Selected — This plan changes command-routing behavior with precise parity and boundary outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-RESEARCH.md
@.planning/phases/158-canonical-command-families/158-01-SUMMARY.md
@commands/bgsd-inspect.md
@commands/bgsd-progress.md
@commands/bgsd-search-lessons.md
@commands/bgsd-health.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the canonical inspect family for core read-only diagnostics</name>
  <files>commands/bgsd-inspect.md, commands/bgsd-progress.md, commands/bgsd-impact.md, commands/bgsd-trace.md</files>
  <action>Create `/bgsd-inspect` as the strict read-only hub for the core diagnostics locked in phase context and route progress, impact, and trace through it. Keep the family additive and do not let inspect absorb mutating or domain-specific families.</action>
  <verify>node bin/bgsd-tools.cjs verify:references commands/bgsd-inspect.md</verify>
  <done>- `/bgsd-inspect` owns the intended core read-only diagnostics family
- Progress, impact, and trace behave as compatibility routes into the canonical hub
- No mutating or domain-specific flows leak into `/bgsd-inspect`</done>
</task>

<task type="auto">
  <name>Task 2: Repoint search and health inspect aliases into compatibility shims</name>
  <files>commands/bgsd-search-decisions.md, commands/bgsd-search-lessons.md, commands/bgsd-health.md</files>
  <action>Turn the covered search and health diagnostics wrappers into thin compatibility routes that feed the canonical `/bgsd-inspect` behavior. Keep the wording compatibility-focused and preserve the locked read-only boundary so later inspect aliases can be added without reopening scope.</action>
  <verify>node bin/bgsd-tools.cjs verify:references commands/bgsd-health.md</verify>
  <done>- The covered core inspect aliases route through `/bgsd-inspect`
- Alias wording stays compatibility-focused instead of preferred-surface focused
- The canonical inspect boundary is established before the remaining diagnostics are migrated</done>
</task>

<task type="auto">
  <name>Task 3: Keep the inspect contract narrow enough for the follow-on alias and test slice</name>
  <files>commands/bgsd-inspect.md</files>
  <action>Make the canonical inspect wrapper text explicit about the covered read-only diagnostics and the excluded mutating or domain-specific families so the remaining alias and regression slice can extend the family without blurring the locked boundary.</action>
  <verify>node bin/bgsd-tools.cjs verify:references commands/bgsd-inspect.md</verify>
  <done>- The inspect contract clearly defines the read-only diagnostics hub
- Remaining inspect aliases can be added in a follow-on slice without changing the family boundary
- This plan stays out of help/runtime updates reserved for later work</done>
</task>

</tasks>

<verification>
1. `node bin/bgsd-tools.cjs verify:references commands/bgsd-inspect.md`
2. `node bin/bgsd-tools.cjs verify:references commands/bgsd-health.md`
3. Representative dry-run or fixture checks show `/bgsd-inspect progress`, `/bgsd-inspect search decisions`, and `/bgsd-inspect health` normalize to the expected existing workflow contracts
</verification>

<success_criteria>
- `/bgsd-inspect` is a real read-only diagnostics family
- Legacy inspect commands stay behaviorally equivalent as compatibility shims
- The inspect boundary remains strict enough that broader review/security/readiness/release families stay out of scope
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-04-SUMMARY.md`
</output>
