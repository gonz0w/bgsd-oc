---
phase: 158-canonical-command-families
plan: 02
type: tdd
wave: 2
depends_on: [158-01]
files_modified:
  - commands/bgsd-plan.md
  - commands/bgsd-plan-phase.md
  - commands/bgsd-discuss-phase.md
  - commands/bgsd-research-phase.md
  - commands/bgsd-list-assumptions.md
  - workflows/plan-phase.md
  - tests/plan-phase-workflow.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements: [CMD-02, CMD-03]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - `/bgsd-plan` becomes the master command for phase planning and related planning prep flows instead of leaving those entrypoints spread across sibling top-level commands.
    - Legacy planning aliases resolve through the same normalized behavior as the new `/bgsd-plan` family rather than drifting into separate implementations.
    - The planning-family contract is established before roadmap, gap-planning, and todo aliases are migrated onto it.
  artifacts:
    - "path: "commands/bgsd-plan.md"
    - "path: "workflows/plan-phase.md"
    - "path: "tests/plan-phase-workflow.test.cjs"
  key_links:
    - "from: "commands/bgsd-plan.md"
    - "from: "commands/bgsd-plan-phase.md"
    - "from: "commands/bgsd-discuss-phase.md"
---

<objective>
Establish `/bgsd-plan` as the real master entrypoint for phase-planning and planning-prep actions before the broader roadmap, gap, and todo aliases migrate onto it.

Purpose: Splitting the planning-family migration keeps the first executable slice focused on the core contract and alias normalization that later roadmap and todo work will depend on.
Output: A real `/bgsd-plan` planning-family contract, repointed core planning aliases, and parity coverage for canonical versus alias entrypoints.
</objective>

> **TDD Decision:** Selected — This plan changes testable command-routing behavior with deterministic alias-versus-canonical parity outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-RESEARCH.md
@.planning/phases/158-canonical-command-families/158-01-SUMMARY.md
@commands/bgsd-plan.md
@commands/bgsd-plan-phase.md
@commands/bgsd-discuss-phase.md
@commands/bgsd-research-phase.md
@commands/bgsd-list-assumptions.md
@workflows/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the `/bgsd-plan` contract for core planning flows</name>
  <files>commands/bgsd-plan.md, workflows/plan-phase.md</files>
  <action>Implement `/bgsd-plan` as the master family entrypoint for the core planning actions covered by this slice: phase planning, phase discussion, phase research, and assumptions review. Normalize those sub-actions onto the existing workflow contract without yet folding in roadmap mutation, gap planning, or todo aliases that will land in the next plan.</action>
  <verify>npm run test:file -- tests/plan-phase-workflow.test.cjs tests/workflow.test.cjs</verify>
  <done>- `/bgsd-plan` can express the covered planning and planning-prep actions
- The normalized planning contract is ready for later roadmap, gap, and todo expansion
- The family stays intentionally limited to planning-oriented behavior</done>
</task>

<task type="auto">
  <name>Task 2: Convert legacy planning-prep commands into thin compatibility shims</name>
  <files>commands/bgsd-plan-phase.md, commands/bgsd-discuss-phase.md, commands/bgsd-research-phase.md, commands/bgsd-list-assumptions.md</files>
  <action>Repoint the covered legacy wrappers so they feed the same normalized `/bgsd-plan` behavior instead of owning separate workflow text or diverging defaults. Keep compatibility intact, but strip user-facing preference language from the aliases so canonical `/bgsd-plan` remains the only preferred surface in files touched by this plan.</action>
  <verify>npm run test:file -- tests/plan-phase-workflow.test.cjs tests/workflow.test.cjs</verify>
  <done>- Covered legacy planning aliases are clearly compatibility routes
- Canonical and legacy planning entrypoints land on equivalent underlying behavior
- No alias becomes the preferred path again inside touched wrappers</done>
</task>

<task type="auto">
  <name>Task 3: Lock core planning-family parity with targeted routing regressions</name>
  <files>tests/plan-phase-workflow.test.cjs, tests/workflow.test.cjs</files>
  <action>Add focused tests that compare representative canonical and legacy invocations for phase planning, discussion, research, and assumptions flows. Assert shared routing contracts and argument normalization, but keep the test scope limited to the core planning family established in this slice.</action>
  <verify>npm run test:file -- tests/plan-phase-workflow.test.cjs tests/workflow.test.cjs</verify>
  <done>- Tests fail if `/bgsd-plan` and its core planning aliases diverge on workflow targets or normalized arguments
- Tests cover each planning concern moved in this slice
- Coverage stays narrow enough that roadmap, gaps, and todo parity can land cleanly in the follow-on plan</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/plan-phase-workflow.test.cjs tests/workflow.test.cjs`
2. `node bin/bgsd-tools.cjs verify:references commands/bgsd-plan.md`
3. Representative dry-run or fixture checks show `/bgsd-plan phase 158`, `/bgsd-plan discuss 158`, and `/bgsd-plan research 158` normalize to the expected existing workflow contracts
</verification>

<success_criteria>
- `/bgsd-plan` is the real master command for the core planning surface
- Legacy planning-prep commands stay behaviorally equivalent as compatibility shims
- The planning contract is ready for roadmap, gap, and todo migration without making this slice oversized
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-02-SUMMARY.md`
</output>
