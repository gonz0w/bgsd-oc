---
phase: 158-canonical-command-families
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - commands/bgsd-quick.md
  - commands/bgsd-quick-task.md
  - commands/bgsd-plan.md
  - commands/bgsd-inspect.md
  - bin/manifest.json
  - deploy.sh
  - install.js
  - tests/workflow.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - `/bgsd-quick` becomes the clearly preferred quick-entry wrapper while `/bgsd-quick-task` remains only a thin compatibility route into the same quick behavior.
    - The repo gains canonical `/bgsd-plan` and `/bgsd-inspect` entrypoints so grouped families exist before legacy commands are repointed.
    - Deployment and installer inventory include the new canonical wrappers without dropping legacy aliases that still need to resolve during migration.
  artifacts:
    - "path: "commands/bgsd-quick.md"
    - "path: "commands/bgsd-plan.md"
    - "path: "commands/bgsd-inspect.md"
    - "path: "bin/manifest.json"
    - "path: "deploy.sh"
    - "path: "install.js"
    - "path: "tests/integration.test.cjs"
  key_links:
    - "from: "bin/manifest.json"
    - "from: "bin/manifest.json"
    - "from: "commands/bgsd-quick.md"
---

<objective>
Establish the canonical wrapper surface and deployment inventory for Phase 158 so quick entry, planning, and inspection families can shrink the visible command footprint without breaking existing command files.

Purpose: The follow-on family-routing work only stays low risk if the repo first has the canonical wrapper files, deployment manifests, and alias pattern needed to ship new names and preserve old ones together.
Output: Canonical wrapper scaffolds for `/bgsd-quick`, `/bgsd-plan`, and `/bgsd-inspect`, plus manifest/install/deploy parity tests for the migration baseline.
</objective>

> **TDD Decision:** Selected — This plan changes command-routing contracts and deployment inventory with clear wrapper-parity outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/MILESTONE-INTENT.md
@.planning/phases/158-canonical-command-families/158-CONTEXT.md
@.planning/phases/158-canonical-command-families/158-RESEARCH.md
@commands/bgsd-quick.md
@commands/bgsd-quick-task.md
@bin/manifest.json
@deploy.sh
@install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote canonical quick entry and add family wrapper scaffolds</name>
  <files>commands/bgsd-quick.md, commands/bgsd-quick-task.md, commands/bgsd-plan.md, commands/bgsd-inspect.md</files>
  <action>Make `/bgsd-quick` the clearly canonical quick-entry command, convert `/bgsd-quick-task` into an alias-only shim, and add thin canonical wrapper files for `/bgsd-plan` and `/bgsd-inspect`. Keep the new family wrappers additive and scoped to the locked boundaries: `/bgsd-plan` is the master planning umbrella, `/bgsd-inspect` is read-only diagnostics only, and legacy files should not become the preferred surface again.</action>
  <verify>npm run test:file -- tests/workflow.test.cjs tests/integration.test.cjs</verify>
  <done>- `/bgsd-quick` reads as the canonical quick path
- `/bgsd-quick-task` is explicitly compatibility-only
- `/bgsd-plan` and `/bgsd-inspect` exist as real wrappers, not docs-only placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Align manifest, deploy, and install inventory with the canonical wrappers</name>
  <files>bin/manifest.json, deploy.sh, install.js</files>
  <action>Add the new canonical command files to the shipped manifest and preserve the legacy wrapper files that must continue to resolve. Keep deploy/install symmetry intact so canonical wrappers, alias wrappers, and stale-file cleanup all use the same inventory truth.</action>
  <verify>npm run test:file -- tests/integration.test.cjs</verify>
  <done>- Manifest includes canonical wrapper files
- Deploy and install logic still mirror each other
- Legacy aliases remain shippable compatibility files instead of disappearing accidentally</done>
</task>

<task type="auto">
  <name>Task 3: Add migration-baseline regression coverage for wrapper parity</name>
  <files>tests/workflow.test.cjs, tests/integration.test.cjs</files>
  <action>Add focused tests that prove the canonical quick wrapper and compatibility alias land on the same underlying quick workflow, and that manifest-backed deploy/install expectations include the new `/bgsd-plan` and `/bgsd-inspect` wrappers. Keep coverage narrow to Phase 158 wrapper inventory and parity, not a full help/reference audit.</action>
  <verify>npm run test:file -- tests/workflow.test.cjs tests/integration.test.cjs</verify>
  <done>- Tests fail if quick canonical and alias wrappers drift apart
- Tests fail if canonical family wrappers drop out of the shipped manifest
- Coverage stays scoped to wrapper parity and inventory, not Phase 159 help cleanup</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/workflow.test.cjs tests/integration.test.cjs`
2. `node bin/bgsd-tools.cjs verify:references commands/bgsd-plan.md`
3. `node bin/bgsd-tools.cjs verify:references commands/bgsd-inspect.md`
</verification>

<success_criteria>
- Canonical wrapper files for quick, plan, and inspect exist and deploy cleanly
- Quick-entry guidance flips to `/bgsd-quick` without breaking `/bgsd-quick-task`
- Manifest/install/deploy wiring is ready for later family-routing changes
</success_criteria>

<output>
After completion, create `.planning/phases/158-canonical-command-families/158-01-SUMMARY.md`
</output>
