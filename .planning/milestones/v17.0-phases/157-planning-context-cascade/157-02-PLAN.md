---
phase: 157-planning-context-cascade
plan: 02
type: tdd
wave: 2
depends_on: [157-01]
files_modified:
  - src/commands/init.js
  - src/lib/jj.js
  - tests/init.test.cjs
  - tests/contracts.test.cjs
autonomous: true
requirements: [JJ-05, INT-04, INT-03]
intent:
  outcome_ids: [DO-118, DO-120]
must_haves:
    truths:
      - Roadmapper, planner, researcher, verifier, and verify-work init/context surfaces receive compact `effective_intent` instead of only `intent_summary` or raw intent file pointers.
      - "Planning-facing JJ context is capability-only: it describes JJ-required, workspace-backed parallel execution support and recovery availability without depending on live workspace inventory."
      - The compact JJ payload includes an explicit advisory note that planning/roadmapping may prefer low-overlap sibling work when plan/file ownership data indicates it is safe, without introducing automatic routing.
      - Compact and manifest output stay additive and backward compatible when new planning context fields are present or absent.
    artifacts:
      - path: "src/commands/init.js"
        provides: "Planning and verification init payloads that inject effective_intent"
      - path: "src/lib/jj.js"
        provides: "Capability-only JJ planning context with advisory sibling-work guidance"
      - path: "tests/contracts.test.cjs"
        provides: "Additive contract regression coverage for new planning context fields"
    key_links:
      - from: "src/commands/intent.js"
        to: "src/commands/init.js"
        via: "effective_intent is computed before planning-facing init payload emission"
        pattern: "effective_intent"
      - from: "src/lib/jj.js"
        to: "src/commands/init.js"
        via: "jj planning capability context injection for compact init outputs"
        pattern: "jj_planning_context"
      - from: "tests/init.test.cjs"
        to: "src/commands/init.js"
        via: "focused init regression coverage for compact planning payloads"
        pattern: "init:plan-phase"
---

<objective>
Thread compact intent and JJ planning capability context through the init/context contract so every planning-alignment surface in scope receives the same additive data model, including an explicit advisory signal for safe low-overlap sibling work.

Purpose: Phase 157 only delivers if the new layered intent and workspace-awareness stop living as isolated helpers and become the actual contract emitted by planning, roadmapping, research, and verify-work init entrypoints.
Output: Init payload updates, static JJ capability payloads with explicit advisory sibling-work guidance, and init/contract coverage for the new fields.
</objective>

> **TDD Decision:** Selected — This plan changes stable JSON/context contracts with deterministic assertions across init, manifest, and integration tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/157-planning-context-cascade/157-CONTEXT.md
@.planning/phases/157-planning-context-cascade/157-RESEARCH.md
@.planning/phases/157-planning-context-cascade/157-01-SUMMARY.md
@src/commands/init.js
@src/lib/jj.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact intent and JJ planning capability fields to init payloads</name>
  <files>src/commands/init.js, src/lib/jj.js, tests/init.test.cjs</files>
  <action>Replace planning-surface reliance on `intent_summary`-only injection by adding `effective_intent` to the relevant init commands (`init:new-milestone`, `init:plan-phase`, `init:verify-work`, and any verification/planning entrypoint in scope). In the same contract, add a compact JJ planning capability payload sourced from stable repo/config facts such as JJ-required execution, workspace-backed parallel waves, recovery support, supported config shape, and an explicit advisory note that planning/roadmapping may prefer low-overlap sibling work when plan/file ownership data shows it is safe. Keep that guidance advisory-only and do not depend on `workspace_active`, live workspace inventory, or an automatic sibling-plan recommendation heuristic.</action>
  <verify>npm run test:file -- tests/init.test.cjs</verify>
  <done>- Planning-facing init commands expose `effective_intent` when available and visible warnings when layers are partial
- JJ planning context is static/capability-oriented, not live execution inventory
- JJ planning context includes explicit advisory low-overlap sibling-work guidance without auto-routing
- Compact output remains small and additive when new fields are missing or omitted</done>
</task>

<task type="auto">
  <name>Task 2: Lock additive init contracts for compact intent and capability context</name>
  <files>tests/init.test.cjs, tests/contracts.test.cjs</files>
  <action>Add focused regression coverage proving the updated init commands emit `effective_intent`, preserve compact/manifest compatibility, and keep JJ planning context capability-only while still carrying the explicit advisory low-overlap sibling-work note. Cover partial-layer fallback and make sure the new fields stay additive so older callers continue to function when intent layers are absent.</action>
  <verify>npm run test:file -- tests/init.test.cjs tests/contracts.test.cjs</verify>
  <done>- Init tests cover new compact intent and JJ capability fields across relevant commands
- Contracts remain additive when the new fields are absent or future fields are added
- Tests fail if planning JJ context starts depending on live workspace inventory or turns the advisory low-overlap preference into hidden automatic routing</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/init.test.cjs tests/contracts.test.cjs`
2. `node bin/bgsd-tools.cjs init:plan-phase 157 --compact` returns `effective_intent` and a compact JJ planning capability payload with explicit advisory low-overlap sibling-work guidance but without `workspace_active`
3. `node bin/bgsd-tools.cjs init:new-milestone --compact` exposes compact intent plus JJ planning capability context suitable for roadmapping without requiring live workspace state or automatic routing
</verification>

<success_criteria>
- Init/context contracts deliver compact layered intent to planning and roadmapping entrypoints in scope
- JJ-05 lands as a capability-only planning signal with explicit advisory low-overlap sibling-work guidance rather than a live execution-state dependency
- Compact output remains additive, compact, and backward compatible
</success_criteria>

<output>
After completion, create `.planning/phases/157-planning-context-cascade/157-02-SUMMARY.md`
</output>
