---
phase: 157-planning-context-cascade
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/commands/intent.js
  - src/lib/phase-context.js
  - tests/intent.test.cjs
autonomous: true
requirements: [INT-01, INT-03]
intent:
  outcome_ids: [DO-120]
must_haves:
    truths:
      - The repo gains a lightweight `.planning/MILESTONE-INTENT.md` contract for why the active milestone matters now, target outcomes, priorities, and non-goals.
      - `effective_intent` is a compact layered object with `project`, `milestone`, `phase`, and computed `effective` views instead of a raw intent-document dump.
      - Lower intent layers refine current focus without replacing the project north star, and requirements stay the delivery contract.
      - Missing milestone or phase intent degrades visibly through warnings/metadata rather than silent file creation in hot paths.
    artifacts:
      - path: "src/commands/intent.js"
        provides: "Milestone intent parsing and layered effective_intent composition"
      - path: "src/lib/phase-context.js"
        provides: "Phase-purpose extraction from existing CONTEXT artifacts"
      - path: "tests/intent.test.cjs"
        provides: "Regression proof for milestone and phase intent layering behavior"
    key_links:
      - from: "src/lib/phase-context.js"
        to: "src/commands/intent.js"
        via: "phase context extraction feeding layered effective_intent assembly"
        pattern: "effective_intent"
      - from: "tests/intent.test.cjs"
        to: "src/commands/intent.js"
        via: "focused regression coverage for milestone parsing and visible partial fallback"
        pattern: "effective_intent"
---

<objective>
Add the core milestone/phase intent contracts and one compact `effective_intent` builder so Phase 157 has a durable layered source before init surfaces and workflows start consuming it.

Purpose: Planning surfaces cannot safely switch from raw `INTENT.md` reads to compact intent injection until the repo has a durable milestone/phase intent parser path and additive fallback behavior for older phases.
Output: Milestone and phase intent parsing foundations, compact layered `effective_intent` computation, and focused regression coverage.
</objective>

> **TDD Decision:** Selected — This plan introduces deterministic parsing and merge behavior with clear fixture-style pass/fail outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/157-planning-context-cascade/157-CONTEXT.md
@.planning/phases/157-planning-context-cascade/157-RESEARCH.md
@src/commands/intent.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add milestone-intent and phase-intent parsing foundations</name>
  <files>src/commands/intent.js, src/lib/phase-context.js</files>
  <action>Create milestone-intent and phase-intent parsing helpers that can read a lightweight milestone strategy artifact plus the local-purpose signal embedded in existing `*-CONTEXT.md` files. Keep the phase path additive by sourcing local intent from current context artifacts instead of introducing a separate top-level phase intent file.</action>
  <verify>npm run test:file -- tests/intent.test.cjs</verify>
  <done>- Milestone intent has a durable lightweight parser contract
- Phase-local intent can be extracted from existing context artifacts
- No hot path silently creates missing milestone or phase intent files</done>
</task>

<task type="auto">
  <name>Task 2: Build compact layered `effective_intent` with visible partial fallback</name>
  <files>src/commands/intent.js, src/lib/phase-context.js</files>
  <action>Add one helper that reads project, milestone, and phase intent layers and returns a compact object with source-layer summaries plus a computed `effective` view. Keep the payload advisory-only, preserve the project north star when lower layers refine focus, and attach explicit warnings/metadata when milestone or phase layers are missing so older projects degrade visibly instead of silently.</action>
  <verify>npm run test:file -- tests/intent.test.cjs</verify>
  <done>- `effective_intent` exposes `project`, `milestone`, `phase`, and `effective` views
- Lower layers refine but do not replace the project objective/outcomes
- Missing-layer fallback is explicit and compact rather than silent or blocking</done>
</task>

<task type="auto">
  <name>Task 3: Lock intent-layering contracts with focused regression tests</name>
  <files>tests/intent.test.cjs</files>
  <action>Add regression coverage for the new milestone-intent template/parse path, phase-intent extraction, effective-intent composition, and partial-layer warnings. Cover the key Phase 157 decisions directly: advisory semantics, compact output, visible degradation when layers are missing, and protection against treating milestone intent as a replacement for project intent.</action>
  <verify>npm run test:file -- tests/intent.test.cjs</verify>
  <done>- Tests cover full-layer and partial-layer `effective_intent` output
- Tests prove missing milestone/phase layers warn without mutating disk
- Tests fail if lower layers overwrite the project north star instead of refining it</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/intent.test.cjs`
2. `node bin/bgsd-tools.cjs intent:show --json` or equivalent helper-backed output can represent milestone + phase layered intent without dumping raw source files
3. A fixture with missing `.planning/MILESTONE-INTENT.md` still computes `effective_intent` and surfaces a visible partial-context warning
</verification>

<success_criteria>
- The repo has a durable lightweight milestone-intent contract and phase-purpose extraction path
- `effective_intent` is compact, layered, and explicitly advisory
- Missing intent layers degrade visibly without silent file creation or raw-document fallback
</success_criteria>

<output>
After completion, create `.planning/phases/157-planning-context-cascade/157-01-SUMMARY.md`
</output>
