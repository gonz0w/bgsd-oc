---
phase: 157-planning-context-cascade
plan: 04
type: tdd
wave: 3
depends_on: [157-02]
files_modified:
  - templates/MILESTONE-INTENT.md
  - workflows/new-milestone.md
  - workflows/plan-phase.md
  - workflows/research-phase.md
  - workflows/verify-work.md
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - INT-01
  - INT-05
  - INT-04
  - JJ-05
intent:
  outcome_ids: [DO-120, DO-118]
must_haves:
    truths:
      - `/bgsd-new-milestone` owns creating or refreshing `.planning/MILESTONE-INTENT.md` instead of overloading `.planning/INTENT.md` with temporary milestone strategy.
      - Planning, research, and verify-work workflow text uses injected compact `effective_intent` as the default contract and only points to raw source docs when full-document editing is actually required.
      - Workflow guidance treats JJ planning context as advisory capability context only, not as live workspace inventory or an automatic sibling-work recommendation heuristic.
      - Workflow guidance explicitly says planners and roadmappers may prefer safe low-overlap sibling work when plan/file ownership evidence supports it, while leaving the final choice manual and non-heuristic.
      - Workflow contract tests catch regressions back to raw `INTENT.md` hot-path reads, milestone-strategy overload, or execution-shaped JJ planning wording.
    artifacts:
      - path: "templates/MILESTONE-INTENT.md"
        provides: "Milestone strategy template for why-now, outcomes, priorities, and non-goals"
      - path: "workflows/new-milestone.md"
        provides: "Workflow ownership of .planning/MILESTONE-INTENT.md creation and refresh"
      - path: "workflows/plan-phase.md"
        provides: "Planner guidance that defaults to injected effective_intent and advisory JJ context"
      - path: "workflows/verify-work.md"
        provides: "Verification guidance that consumes compact intent and capability context"
      - path: "tests/workflow.test.cjs"
        provides: "Workflow contract regression coverage for milestone intent ownership and advisory JJ wording"
    key_links:
      - from: "templates/MILESTONE-INTENT.md"
        to: "workflows/new-milestone.md"
        via: "milestone intent template ownership and downstream creation guidance"
        pattern: "MILESTONE-INTENT"
      - from: "workflows/new-milestone.md"
        to: ".planning/INTENT.md"
        via: "project intent remains north-star context while milestone-local strategy moves elsewhere"
        pattern: "north star"
      - from: "workflows/research-phase.md"
        to: "src/commands/init.js"
        via: "workflow contract relies on injected effective_intent and jj_planning_context"
        pattern: "effective_intent"
---

<objective>
Rewrite milestone and planning-alignment workflows around the new compact intent contract so milestone strategy has one owner and planning surfaces stop depending on raw intent-document reads while clearly documenting advisory low-overlap sibling-work preference.

Purpose: Phase 157 is not done when runtime fields exist but workflow text still tells agents to read or mutate raw `INTENT.md` everywhere. This plan makes the new contract the documented path and locks the ownership boundary around milestone intent.
Output: Milestone-intent template ownership, workflow rewrites for new-milestone/plan/research/verify paths, advisory low-overlap sibling-work guidance, and workflow regression coverage.
</objective>

> **TDD Decision:** Selected — This plan changes deterministic workflow contracts and wording that the repo already protects with focused tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/157-planning-context-cascade/157-CONTEXT.md
@.planning/phases/157-planning-context-cascade/157-RESEARCH.md
@.planning/phases/157-planning-context-cascade/157-02-SUMMARY.md
@workflows/new-milestone.md
@workflows/plan-phase.md
@workflows/research-phase.md
@workflows/verify-work.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move milestone-strategy ownership into `MILESTONE-INTENT.md`</name>
  <files>templates/MILESTONE-INTENT.md, workflows/new-milestone.md</files>
  <action>Create the lightweight milestone-intent template and rewrite the new-milestone workflow so it reviews project intent for the enduring north star but creates or refreshes `.planning/MILESTONE-INTENT.md` as the single owned home for milestone-specific why-now strategy, targeted outcomes, priorities, and non-goals. Remove instructions that evolve `.planning/INTENT.md` for temporary milestone focus, and keep direct project-intent editing only for true project-level intent changes.</action>
  <verify>npm run test:file -- tests/workflow.test.cjs</verify>
  <done>- New-milestone workflow clearly owns `.planning/MILESTONE-INTENT.md`
- Project intent is reviewed as north-star context rather than rewritten for milestone-local priorities
- Roadmapper inputs distinguish project intent from milestone strategy explicitly</done>
</task>

<task type="auto">
  <name>Task 2: Rewire planning, research, and verify-work workflows to compact injected context</name>
  <files>workflows/plan-phase.md, workflows/research-phase.md, workflows/verify-work.md, tests/workflow.test.cjs</files>
  <action>Update planning/research/verification workflow text to parse and use injected `effective_intent` plus JJ planning capability context instead of telling agents to read `.planning/INTENT.md` by default. Keep references to full source files only where the workflow truly needs direct editing, and preserve the Phase 157 rule that JJ context is capability-only with no live workspace inventory dependency and no automatic sibling-plan recommendation heuristic. At the same time, make the workflow contract explicit that planners and roadmappers may prefer safe low-overlap sibling work when file-ownership or plan-overlap evidence shows parallelism is low risk.</action>
  <verify>npm run test:file -- tests/workflow.test.cjs</verify>
  <done>- Planner/researcher/verify-work prompts point at compact `effective_intent` as the normal contract
- Workflow text keeps JJ planning context advisory and capability-only
- Workflow text explicitly preserves manual low-overlap sibling-work preference guidance without auto-routing
- Tests catch regressions back to raw-intent hot-path reads or heuristic overreach</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/workflow.test.cjs`
2. `workflows/new-milestone.md` instructs agents to create or refresh `.planning/MILESTONE-INTENT.md` and no longer treats `.planning/INTENT.md` as the home for milestone-local priorities
3. `workflows/plan-phase.md`, `workflows/research-phase.md`, and `workflows/verify-work.md` use injected `effective_intent` / JJ capability context as the default planning contract, explicitly permit manual low-overlap sibling-work preference, and do not require live `workspace_active` inventory
</verification>

<success_criteria>
- Milestone workflow ownership of `.planning/MILESTONE-INTENT.md` is explicit and enforceable
- Workflow text follows the compact intent cascade contract instead of raw-intent hot-path reads
- JJ planning context remains advisory capability context throughout user-facing planning-alignment workflows while still documenting manual low-overlap sibling-work preference
</success_criteria>

<output>
After completion, create `.planning/phases/157-planning-context-cascade/157-04-SUMMARY.md`
</output>
