---
phase: 157-planning-context-cascade
plan: 03
type: tdd
wave: 3
depends_on: [157-02]
files_modified:
  - src/lib/context.js
  - src/lib/codebase-intel.js
  - tests/agent.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements: [INT-04]
intent:
  outcome_ids: [DO-120]
must_haves:
    truths:
      - Planner, phase researcher, roadmapper/project researcher, and verifier agent-scoped payloads keep compact `effective_intent` where purpose and alignment matter.
      - Verifier-scoped context gains the new intent layer without inheriting executor-only workspace inventory or unrelated planning fields.
      - Any mirrored or precomputed manifest definitions stay aligned so agent-context generation does not lag behind runtime init contracts.
      - Integration coverage proves scoped consumers receive compact intent context consistently after the init contract expands.
    artifacts:
      - path: "src/lib/context.js"
        provides: "Runtime agent-scope manifests that preserve effective_intent"
      - path: "src/lib/codebase-intel.js"
        provides: "Cached manifest mirrors aligned with runtime scoped context behavior"
      - path: "tests/agent.test.cjs"
        provides: "Scoped agent payload regression coverage for effective_intent"
      - path: "tests/integration.test.cjs"
        provides: "End-to-end verification of compact scoped intent delivery"
    key_links:
      - from: "src/lib/context.js"
        to: "src/lib/codebase-intel.js"
        via: "mirrored agent manifest updates for effective_intent parity"
        pattern: "effective_intent"
      - from: "tests/agent.test.cjs"
        to: "src/lib/context.js"
        via: "scoped planner and verifier payload coverage"
        pattern: "effective_intent"
      - from: "tests/integration.test.cjs"
        to: "src/lib/codebase-intel.js"
        via: "integration proof that cached contexts match runtime scoped intent delivery"
        pattern: "effective_intent"
---

<objective>
Update agent-scoped context delivery so planner, researcher, roadmapper, and verifier agents actually receive the compact layered intent contract added in the init surfaces.

Purpose: Phase 157 is not done when init output contains `effective_intent` but agent scoping strips it back out. This plan aligns the scoped manifests and precomputed context path so planner, researcher, roadmapper, and verifier agents all see the same compact advisory contract.
Output: Manifest updates, mirrored precomputed-context updates, and scoped integration coverage.
</objective>

> **TDD Decision:** Selected — This plan changes deterministic workflow contracts and wording that the repo already protects with focused tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/157-planning-context-cascade/157-CONTEXT.md
@.planning/phases/157-planning-context-cascade/157-RESEARCH.md
@.planning/phases/157-planning-context-cascade/157-02-SUMMARY.md
@src/lib/context.js
@src/lib/codebase-intel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update runtime and mirrored agent manifests for compact intent exposure</name>
  <files>src/lib/context.js, src/lib/codebase-intel.js, tests/agent.test.cjs</files>
  <action>Update the agent manifests and any mirrored precomputed manifest definitions so planner, phase researcher, roadmapper/project researcher, and verifier keep `effective_intent` in their scoped payloads. Preserve the repo's capability-aware filtering rules by keeping executor/debugger out of scope and by ensuring verifier context gains alignment data without pulling in executor-only workspace inventory.</action>
  <verify>npm run test:file -- tests/agent.test.cjs</verify>
  <done>- Scoped planner/researcher/roadmapper/verifier payloads keep `effective_intent`
- Verifier remains compact and does not inherit executor-only workspace fields
- Mirrored manifest definitions stay aligned with runtime context behavior</done>
</task>

<task type="auto">
  <name>Task 2: Add integration coverage for scoped intent delivery</name>
  <files>tests/integration.test.cjs, tests/agent.test.cjs</files>
  <action>Add focused tests proving the expanded init contract still reaches the intended consumers after `--agent=` scoping and any integration-style handoff paths. Cover planner/researcher/roadmapper/verifier visibility, partial-layer fallback, and the explicit absence of executor-style workspace inventory in verifier-scoped payloads.</action>
  <verify>npm run test:file -- tests/agent.test.cjs tests/integration.test.cjs</verify>
  <done>- Tests prove scoped planning/alignment agents receive `effective_intent`
- Tests cover verifier visibility plus executor-only field exclusion
- Integration coverage documents the Phase 157 scoped-context contract end to end</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/agent.test.cjs tests/integration.test.cjs`
2. `node bin/bgsd-tools.cjs init:plan-phase 157 --agent=bgsd-planner --raw` includes `effective_intent`
3. `node bin/bgsd-tools.cjs init:verify-work 157 --agent=bgsd-verifier --raw` includes intent-alignment context but not `workspace_active`
</verification>

<success_criteria>
- Scoped agent payloads preserve the compact intent cascade contract for all intended planning/alignment agents
- Verifier gains intent-alignment context without executor-state leakage
- Runtime and mirrored manifest definitions stay aligned under regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/157-planning-context-cascade/157-03-SUMMARY.md`
</output>
