---
phase: 159-help-surface-command-integrity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [workflows/help.md, tests/guidance-help-surface.test.cjs]
autonomous: true
requirements: [CMD-04]
must_haves:
  truths:
    - The main help surface presents a compact canonical-first command path instead of a broad flat menu.
    - Core and advanced command paths are visibly separated, and `/bgsd-review` appears in the primary path.
    - Examples shown in main help are runnable as written and do not surface legacy command names.
  artifacts:
    - "path: "workflows/help.md"
    - "path: "tests/guidance-help-surface.test.cjs"
  key_links:
    - "from: "workflows/help.md"
    - "from: "workflows/help.md"
    - "from: "tests/guidance-help-surface.test.cjs"
---

<objective>
Refresh the top-level help surface so it becomes a small, trustworthy canonical front door with executable examples and a clear core-versus-advanced split.

Purpose: DO-119 needs the first help screen to teach the reduced command set without alias noise, missing parameters, or an overloaded flat menu.
Output: A rewritten `workflows/help.md` plus focused regression coverage that locks the new help contract.
</objective>

> **TDD Decision:** Selected — The help surface has deterministic user-visible output that should be locked with focused string-contract tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-RESEARCH.md
@workflows/help.md
@commands/bgsd-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the main help surface around a compact canonical command path</name>
  <files>workflows/help.md</files>
  <action>Restructure `workflows/help.md` so the main help output leads with a small core path, then a visibly separate advanced/family map instead of the current broad flat menu. Keep surfaced guidance canonical-only, include `/bgsd-review` in the primary path, and prefer runnable examples with required phase numbers or flags filled in when the flow shape is known. Do not reintroduce compatibility notes, legacy aliases, or Phase 160 intent-verification content.</action>
  <verify>npm run test:file -- tests/guidance-help-surface.test.cjs</verify>
  <done>- Main help clearly separates core versus advanced paths
- `/bgsd-review` appears in the primary path
- Surfaced help examples use canonical commands only and include required arguments where the workflow knows them</done>
</task>

<task type="auto">
  <name>Task 2: Add focused regression coverage for the help-surface contract</name>
  <files>tests/guidance-help-surface.test.cjs</files>
  <action>Create a focused help-surface regression that reads `workflows/help.md` and fails if the top-level help regresses to a flat menu, omits `/bgsd-review` from the primary path, surfaces legacy aliases, or shows command examples without the required arguments or flags for the named flow. Keep this test scoped to the main help contract rather than the broader Phase 159 repo sweep.</action>
  <verify>npm run test:file -- tests/guidance-help-surface.test.cjs</verify>
  <done>- The test asserts the compact core-versus-advanced split
- The test fails if `/bgsd-review` leaves the primary path
- The test fails if help examples regress to legacy names or missing required parameters</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-help-surface.test.cjs`
2. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-01-PLAN.md`
</verification>

<success_criteria>
- Main help is visibly smaller and organized around core versus advanced guidance
- `/bgsd-review` is part of the primary path
- Canonical-only executable help examples are locked by focused regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-01-SUMMARY.md`
</output>
