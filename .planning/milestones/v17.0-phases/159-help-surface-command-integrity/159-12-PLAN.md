---
phase: 159-help-surface-command-integrity
plan: 12
type: execute
wave: 4
depends_on: [159-05, 159-06, 159-11]
files_modified:
  - docs/workflows.md
  - workflows/plan-phase.md
  - workflows/execute-phase.md
  - docs/architecture.md
  - docs/agents.md
  - agents/bgsd-verifier.md
  - src/lib/constants.js
  - src/plugin/advisory-guardrails.js
  - plugin.js
  - tests/guidance-command-integrity-remaining-surfaces.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-05, CMD-06]
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - The remaining surfaced docs, workflows, agent prompts, and runtime help examples teach canonical executable commands with the arguments users need for the intended flow.
      - Reference-only compatibility notes stay clearly non-runnable so the shared validator stops treating them as active next-step guidance.
      - Runtime command help and bundled plugin output no longer advertise nonexistent `bgsd-tools codebase ...` routes or stale `/bgsd-progress` follow-ups.
    artifacts:
      - docs/workflows.md
      - workflows/plan-phase.md
      - workflows/execute-phase.md
      - docs/architecture.md
      - docs/agents.md
      - agents/bgsd-verifier.md
      - src/lib/constants.js
      - src/plugin/advisory-guardrails.js
      - plugin.js
      - tests/guidance-command-integrity-remaining-surfaces.test.cjs
    key_links:
      - `docs/workflows.md`, `workflows/plan-phase.md`, and `workflows/execute-phase.md` agree on the canonical planning and progress routes users should actually run.
      - `docs/architecture.md`, `docs/agents.md`, and `agents/bgsd-verifier.md` present runnable examples with current CLI names and required planning arguments.
      - `src/lib/constants.js` remains the source of truth for runtime help text, and `plugin.js` is rebuilt so shipped bundle output matches those canonical examples.
      - `tests/guidance-command-integrity-remaining-surfaces.test.cjs` reads the shipped surfaces directly and proves they stay validator-clean.
---

<objective>
Close the remaining repo-wide surfaced-guidance blockers so Phase 159's command-integrity contract applies to the last high-traffic docs, workflow prompts, agent guidance, and runtime help output.

Purpose: `159-VERIFICATION.md` now shows the exact blocker docs are fixed, but CMD-06 still fails because users can still hit stale aliases, missing arguments, and nonexistent runtime commands in other shipped surfaces.
Output: Canonicalized remaining surfaced guidance plus a focused regression that proves these exact shipped files stay validator-clean.
</objective>

> **TDD Decision:** Skipped — this is guidance cleanup plus regression locking on existing surfaced command examples, not net-new runtime behavior.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-05-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-06-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-11-SUMMARY.md
@docs/workflows.md
@workflows/plan-phase.md
@workflows/execute-phase.md
@docs/architecture.md
@docs/agents.md
@agents/bgsd-verifier.md
@src/lib/constants.js
@src/plugin/advisory-guardrails.js
@plugin.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the remaining planning and execution guidance surfaces</name>
  <files>docs/workflows.md, workflows/plan-phase.md, workflows/execute-phase.md</files>
  <action>Rewrite the remaining user-followable planning and execution guidance to match the canonical command families already established earlier in Phase 159. Replace surfaced legacy aliases such as `/bgsd-list-assumptions` and `/bgsd-progress` with the canonical `/bgsd-plan ...` and `/bgsd-inspect progress` routes where users are being told what to run next. Where compatibility aliases still need mention, label them as reference-only rather than active guidance. Keep required phase arguments visible on runnable examples.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-remaining-surfaces.test.cjs</verify>
  <done>- `docs/workflows.md` no longer teaches legacy planning, execution, or progress routes as the preferred user path
- `workflows/plan-phase.md` and `workflows/execute-phase.md` surface canonical next commands with required arguments
- Compatibility notes are clearly reference-only instead of runnable guidance</done>
</task>

<task type="auto">
  <name>Task 2: Fix the remaining docs, agent, and runtime help examples called out by verification</name>
  <files>docs/architecture.md, docs/agents.md, agents/bgsd-verifier.md, src/lib/constants.js, src/plugin/advisory-guardrails.js, plugin.js</files>
  <action>Update the remaining surfaced examples that still use stale `gsd-tools`/nonexistent `bgsd-tools codebase ...` routes, missing planning arguments, or stale progress guidance. Align architecture and agent docs with current canonical CLI names, make verifier guidance match the shipped planning-family syntax, correct the runtime command help source in `src/lib/constants.js`, update any remaining runtime advisory mappings in `src/plugin/advisory-guardrails.js`, then rebuild `plugin.js` so the shipped bundle matches source text exactly.</action>
  <verify>npm run build && npm run test:file -- tests/guidance-command-integrity-remaining-surfaces.test.cjs</verify>
  <done>- The cited docs and agent prompt use current canonical command names and argument shapes
- Runtime help text no longer advertises nonexistent codebase routes or stale progress follow-ups
- `plugin.js` is rebuilt from source and matches the canonicalized runtime guidance</done>
</task>

<task type="auto">
  <name>Task 3: Lock the remaining surfaced files with direct validator-backed regression coverage</name>
  <files>tests/guidance-command-integrity-remaining-surfaces.test.cjs</files>
  <action>Create focused regression coverage that reads the shipped contents of `docs/workflows.md`, `workflows/plan-phase.md`, `workflows/execute-phase.md`, `docs/architecture.md`, `docs/agents.md`, `agents/bgsd-verifier.md`, and the relevant runtime-help sources directly. Reuse `validateCommandIntegrity()` so the test proves these exact surfaces are validator-clean, and add explicit expectations for the blocker patterns named in `159-VERIFICATION.md` such as missing phase arguments, legacy planning aliases, `/bgsd-progress`, and nonexistent `bgsd-tools codebase ...` commands.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-remaining-surfaces.test.cjs</verify>
  <done>- Regression reads the real shipped files rather than synthetic snippets
- Tests fail if the exact Phase 159 blocker patterns return on these remaining surfaces
- This repo slice has one focused proof path that the remaining surfaced guidance is canonical and executable</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `npm run test:file -- tests/guidance-command-integrity-remaining-surfaces.test.cjs`
</verification>

<success_criteria>
- The remaining surfaced files cited by `159-VERIFICATION.md` teach only current canonical commands or clearly marked reference-only compatibility notes
- Runnable planning, gap, settings, and execution guidance includes the arguments users need for the intended flow
- Runtime help source and shipped bundle stop surfacing nonexistent `bgsd-tools codebase ...` examples and stale `/bgsd-progress` follow-ups
- Focused regression coverage proves the exact shipped remaining surfaces stay validator-clean
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-12-SUMMARY.md`
</output>
