---
phase: 159-help-surface-command-integrity
plan: 18
type: execute
wave: 6
depends_on: [159-13]
files_modified:
  - src/lib/constants.js
  - plugin.js
  - tests/guidance-command-integrity-runtime-tail.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-06]
must_haves:
  truths:
    - Runtime fallback guidance no longer emits bare planning-family commands without required arguments when phase context is unavailable.
    - Shipped runtime help no longer surfaces stale diagnostic aliases or nonexistent command names as user-followable next steps.
    - A focused regression proves the runtime source and rebuilt bundle stay aligned on canonical surfaced guidance.
  artifacts:
    - path: "src/lib/constants.js"
      provides: "Runtime help source wording"
    - path: "plugin.js"
      provides: "Rebuilt shipped runtime guidance"
    - path: "tests/guidance-command-integrity-runtime-tail.test.cjs"
      provides: "Focused regression over the remaining runtime blocker slice"
  key_links:
    - from: "src/lib/constants.js"
      to: "plugin.js"
      via: "rebuilt runtime bundle"
      pattern: "/bgsd-plan|/bgsd-inspect"
    - from: "tests/guidance-command-integrity-runtime-tail.test.cjs"
      to: "plugin.js"
      via: "direct-file regression"
      pattern: "plugin\.js"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - Runtime fallback guidance no longer emits bare planning-family commands without required arguments when phase context is unavailable.
      - Shipped runtime help no longer surfaces stale diagnostic aliases or nonexistent command names as user-followable next steps.
      - A focused regression proves the runtime source and rebuilt bundle stay aligned on canonical surfaced guidance.
    artifacts:
      - src/lib/constants.js
      - plugin.js
      - tests/guidance-command-integrity-runtime-tail.test.cjs
    key_links:
      - Runtime help sources and the rebuilt bundle agree on canonical surfaced guidance.
      - Focused regression reads the shipped runtime output directly.
---

<objective>
Close the remaining runtime-help blocker slice by fixing fallback guidance sources and rebuilding the shipped plugin bundle.

Purpose: `159-VERIFICATION.md` still cites runtime text for bare `/bgsd-plan phase`, stale health-style aliases, and nonexistent surfaced commands, so this plan isolates the runtime contract cleanup from workflow and skill work.
Output: Corrected runtime guidance sources, rebuilt `plugin.js`, and focused regression proving shipped runtime next-step text is executable or explicitly non-runnable.
</objective>

> **TDD Decision:** Selected — Runtime next-step output is deterministic behavior, so a focused regression should lock the exact fallback strings users actually receive from the shipped bundle.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-12-SUMMARY.md
@src/lib/constants.js
@plugin.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct runtime fallback guidance at the source and in the shipped bundle</name>
  <files>src/lib/constants.js, plugin.js</files>
  <action>Update the remaining runtime help source so it surfaces canonical executable commands or explicit inspect-style guidance instead of bare planning-family commands with missing arguments, stale diagnostic aliases, or nonexistent command names. Rebuild `plugin.js` immediately after source edits so shipped runtime output matches the corrected source wording exactly.</action>
  <verify>npm run build && npm run test:file -- tests/guidance-command-integrity-runtime-tail.test.cjs</verify>
  <done>- Runtime fallback help no longer emits incomplete or stale user-followable commands
- Source help and shipped bundle stay aligned
- Users receive executable next steps or clearly non-runnable inspect guidance</done>
</task>

<task type="auto">
  <name>Task 2: Lock the runtime-help slice with direct regression coverage</name>
  <files>tests/guidance-command-integrity-runtime-tail.test.cjs</files>
  <action>Create focused regression coverage that reads the shipped runtime bundle and relevant source help text directly and fails if the blocker patterns named in `159-VERIFICATION.md` return. Keep the test limited to the runtime slice so this plan stays surgical.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-runtime-tail.test.cjs</verify>
  <done>- Regression fails if bare planning-family fallback text or stale runtime aliases return
- The test reads the shipped runtime output directly
- Plan 18 provides a surgical proof path for the remaining runtime blocker slice</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `npm run test:file -- tests/guidance-command-integrity-runtime-tail.test.cjs`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-18-PLAN.md`
</verification>

<success_criteria>
- Runtime fallback help no longer surfaces incomplete or stale commands as runnable next steps
- `plugin.js` is rebuilt and aligned with corrected source guidance
- Focused regression coverage protects the exact runtime blocker patterns named by verification
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-18-SUMMARY.md`
</output>
