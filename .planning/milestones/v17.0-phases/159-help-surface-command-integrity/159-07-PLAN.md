---
phase: 159-help-surface-command-integrity
plan: 07
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/commandDiscovery.js
  - tests/validate-commands.test.cjs
  - tests/guidance-command-integrity-agent-skill-surfaces.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-05, CMD-06]
must_haves:
  truths:
    - Reference-style docs and skills can use placeholder or family-form commands without failing command-integrity validation.
    - Runnable help and next-step guidance still fail validation when required phase arguments or gap flags are missing.
    - Validator extraction no longer treats non-command path fragments like /bgsd-oc as surfaced guidance failures.
  artifacts:
    - src/lib/commandDiscovery.js
    - tests/validate-commands.test.cjs
    - tests/guidance-command-integrity-agent-skill-surfaces.test.cjs
  key_links:
    - validateCommandIntegrity classifies reference-style placeholder forms before enforcing runnable guidance rules.
    - Validator contract tests cover both allowed reference forms and rejected runnable guidance mistakes.
---

<objective>
Tighten the shared command-integrity validator so it stops blocking on reference-style placeholders and path fragments while still enforcing executable command guidance on real help and next-step surfaces.

Purpose: Phase 159 can only close if the validator distinguishes genuine stale guidance from allowed reference-style forms exactly as the locked gray-area decision requires.
Output: Refined validator rules plus focused regression coverage for allowed reference forms and still-blocking executable guidance mistakes.
</objective>

> **TDD Decision:** Selected — The validator behavior is deterministic and testable, but this gap closure is best delivered as targeted edits plus focused regressions inside one execute plan.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-02-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-05-SUMMARY.md
@src/lib/commandDiscovery.js
@tests/validate-commands.test.cjs
@tests/guidance-command-integrity-agent-skill-surfaces.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine validator extraction and rule classification for reference-style command forms</name>
  <files>src/lib/commandDiscovery.js</files>
  <action>Update `validateCommandIntegrity()` and its mention-extraction helpers so the validator distinguishes runnable guidance from reference-style docs/skills. Honor the locked Phase 159 rule exactly: placeholder or family-form commands are allowed in reference-style matrices, indexes, and ownership tables, but actual help, next-step, and runnable guidance surfaces must still use concrete executable commands when the flow shape is known. Also stop extracting false positives from config/path fragments such as `/bgsd-oc`, while preserving legacy-alias, wrong-command, missing-argument, and missing-flag failures on real surfaced guidance.</action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs</verify>
  <done>- Validator permits reference-style placeholder/family forms only in allowed reference contexts
- Runnable help and next-step guidance still fails on missing phase args or gap flags
- `/bgsd-oc` path mentions and similar non-command fragments no longer create command-integrity issues</done>
</task>

<task type="auto">
  <name>Task 2: Lock the refined validator contract with focused reference-versus-runnable regression coverage</name>
  <files>tests/validate-commands.test.cjs, tests/guidance-command-integrity-agent-skill-surfaces.test.cjs</files>
  <action>Extend the validator contract tests with fixtures covering the new split between allowed reference-style command forms and disallowed runnable guidance mistakes. Add explicit proof that reference-style forms like `/bgsd-plan phase [phase]`, `/bgsd-plan gaps [phase]`, and skill-table family references now pass when intentionally used as reference, while runnable prose without a concrete phase or missing `--gaps-only` still fails. Update the touched agent/skill regression so `skills/raci/SKILL.md` remains validator-clean under the refined rules without relaxing canonical-only guidance elsewhere.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-agent-skill-surfaces.test.cjs</verify>
  <done>- Focused tests prove the allowed reference-style exception and the remaining runnable-guidance requirements together
- `skills/raci/SKILL.md` can stay reference-style without reopening legacy-command guidance
- The validator contract is explicit enough that future Phase 159 help/doc sweeps can rely on repo-wide validation again</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/validate-commands.test.cjs`
2. `npm run test:file -- tests/guidance-command-integrity-agent-skill-surfaces.test.cjs`
3. `node bin/bgsd-tools.cjs util:validate-commands --raw`
</verification>

<success_criteria>
- Repo validation no longer blocks on reference-style placeholder or family-form command examples
- Runnable guidance still fails when it omits required phase arguments or gap-only flags
- Validator output now isolates only genuine stale help and next-step guidance for the remaining content sweep
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-07-SUMMARY.md`
</output>
