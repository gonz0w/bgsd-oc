---
phase: 159-help-surface-command-integrity
plan: 14
type: execute
wave: 6
depends_on: [159-13]
files_modified:
  - workflows/add-phase.md
  - workflows/add-todo.md
  - tests/guidance-command-integrity-workflow-prep-a.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-06]
must_haves:
  truths:
    - Add-phase and add-todo guidance use only canonical runnable commands with the required phase argument when the workflow knows it.
    - These workflow prompts no longer send users to legacy todo or phase-add aliases.
    - A focused regression locks this exact workflow-prep blocker slice without reopening already-verified surfaces.
  artifacts:
    - path: "workflows/add-phase.md"
      provides: "Canonical add-phase next steps"
    - path: "workflows/add-todo.md"
      provides: "Canonical todo follow-up guidance"
    - path: "tests/guidance-command-integrity-workflow-prep-a.test.cjs"
      provides: "Focused regression over the first remaining workflow-prep slice"
  key_links:
    - from: "workflows/add-todo.md"
      to: "commands/bgsd-plan.md"
      via: "canonical todo guidance"
      pattern: "/bgsd-plan todo"
    - from: "tests/guidance-command-integrity-workflow-prep-a.test.cjs"
      to: "workflows/add-phase.md"
      via: "direct-file regression"
      pattern: "add-phase|add-todo"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - Add-phase and add-todo guidance use only canonical runnable commands with the required phase argument when the workflow knows it.
      - These workflow prompts no longer send users to legacy todo or phase-add aliases.
      - A focused regression locks this exact workflow-prep blocker slice without reopening already-verified surfaces.
    artifacts:
      - workflows/add-phase.md
      - workflows/add-todo.md
      - tests/guidance-command-integrity-workflow-prep-a.test.cjs
    key_links:
      - The first workflow-prep slice agrees on canonical planning and todo-family commands.
      - Focused regression coverage reads the shipped workflow files directly.
---

<objective>
Close the first remaining workflow-prep blocker slice by fixing the still-user-followable guidance in add-phase and add-todo.

Purpose: `159-VERIFICATION.md` cites these workflow prompts for stale todo and phase-add guidance, so this plan removes that executable-guidance drift without reopening already-clean docs or skills.
Output: Canonicalized workflow-prep prompts plus a focused regression proving these two files stay executable as written.
</objective>

> **TDD Decision:** Selected — These workflow outputs are deterministic surfaced behavior, so direct file-backed regression is the right proof for the exact commands and argument shapes users will see.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-13-SUMMARY.md
@workflows/add-phase.md
@workflows/add-todo.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the add-phase and add-todo guidance slice</name>
  <files>workflows/add-phase.md, workflows/add-todo.md</files>
  <action>Rewrite the remaining user-followable next steps in these two workflows so they use canonical planning and todo commands only. Remove stale phase-add and todo aliases, keep required phase arguments visible when the workflow knows them, and convert any unavoidable non-runnable wording into clearly reference-only language rather than executable commands.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-a.test.cjs</verify>
  <done>- These workflows no longer surface stale todo or phase-add aliases
- Planning and todo follow-ups are executable exactly as written
- Any preserved shorthand is clearly reference-only</done>
</task>

<task type="auto">
  <name>Task 2: Lock the workflow-prep slice with direct regression coverage</name>
  <files>tests/guidance-command-integrity-workflow-prep-a.test.cjs</files>
  <action>Create a focused regression that reads the shipped contents of `workflows/add-phase.md` and `workflows/add-todo.md` directly and fails if the blocker patterns named in `159-VERIFICATION.md` return. Keep the test scoped to these exact files so the plan stays surgical.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-a.test.cjs</verify>
  <done>- Regression fails if stale todo or phase-add aliases reappear
- The test reads real shipped workflow files directly
- Plan 14 has one surgical proof path for this workflow-prep slice</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-command-integrity-workflow-prep-a.test.cjs`
2. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-14-PLAN.md`
</verification>

<success_criteria>
- `workflows/add-phase.md` and `workflows/add-todo.md` surface only canonical executable guidance
- The cited legacy alias and missing-argument failures are gone from this slice
- Focused regression coverage protects the exact files named in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-14-SUMMARY.md`
</output>
