---
phase: 159-help-surface-command-integrity
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/commands.md
autonomous: true
gap_closure: true
requirements: [CMD-05, CMD-06]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - The shipped command reference no longer presents planning-family shorthand or compatibility sections as runnable next steps when the command shape would be wrong or incomplete.
    - Users reading `docs/commands.md` see canonical runnable examples for the real command routes instead of legacy or ambiguous compatibility guidance.
    - The command reference itself becomes validator-clean on its remaining Phase 159 blocker surfaces before regression proof is expanded in the follow-on plan.
  artifacts:
    - docs/commands.md
  key_links:
    - docs/commands.md labels any retained family index material as reference-only so users and validation no longer treat it as executable guidance.
    - docs/commands.md removes or demotes compatibility alias sections that currently read like surfaced next-step guidance.
    - The rewritten command reference remains aligned with the canonical command families locked in Phase 158 and Phase 159 context.
---

<objective>
Remove the remaining runnable ambiguity inside `docs/commands.md` so the shipped command reference stops teaching missing-argument planning-family forms and compatibility-alias sections as active guidance.

Purpose: Verification still cites `docs/commands.md` as a real CMD-06 blocker, so this plan isolates the command-reference cleanup from the follow-on doc regression work without reopening help-surface or validator-core behavior.
Output: A canonical-only `docs/commands.md` that clearly separates reference-only indexes from executable examples.
</objective>

> **TDD Decision:** Skipped — This plan rewrites shipped reference text in one doc file; it is not introducing a new behavior unit suited to RED-GREEN-REFACTOR.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-07-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-08-SUMMARY.md
@docs/commands.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the planning-family overview as reference-only, not runnable shorthand</name>
  <files>docs/commands.md</files>
  <action>Update the planning-family section in `docs/commands.md` so it stays useful as a family index but cannot be mistaken for executable guidance. Preserve only concrete runnable examples with required arguments in the table, keep the explicit reference-only label, and remove any prose that makes `/bgsd-plan phase|discuss|research|assumptions` read like runnable shorthand.</action>
  <verify>node -e "const fs=require('fs'); const text=fs.readFileSync('docs/commands.md','utf8'); if (!text.includes('Reference-style planning-family index')) process.exit(1); if (text.includes('/bgsd-plan phase|discuss|research|assumptions')) process.exit(1);"</verify>
  <done>- Planning-family shorthand is clearly reference-only rather than surfaced runnable guidance
- The table keeps concrete examples with phase arguments for real execution paths
- Users no longer have to infer missing arguments from the command reference</done>
</task>

<task type="auto">
  <name>Task 2: Remove compatibility-alias sections that still read like surfaced guidance</name>
  <files>docs/commands.md</files>
  <action>Audit the compatibility-heavy sections in `docs/commands.md` that verification still names as surfaced guidance (`:185,276,316,324,336,348,360,374,388,404,418,426`). For each section, either remove it from the shipped reference or rewrite it into non-primary explanatory text that does not present the alias as a command users should run. Keep the user-facing product greenfield and canonical-only per locked context.</action>
  <verify>node -e "const fs=require('fs'); const text=fs.readFileSync('docs/commands.md','utf8'); if (text.includes('#### `/bgsd-context-budget`') || text.includes('#### `/bgsd-rollback-info`')) process.exit(1); if (text.includes('Compatibility alias for `/bgsd-settings validate`')) process.exit(1);"</verify>
  <done>- Compatibility alias sections no longer appear as first-class command entries in `docs/commands.md`
- Remaining notes do not surface legacy commands as normal guidance
- The command reference reads as canonical-only to a user scanning the shipped file</done>
</task>

<task type="auto">
  <name>Task 3: Perform a structural pass that leaves the shipped command reference validator-ready</name>
  <files>docs/commands.md</files>
  <action>Read through the resulting `docs/commands.md` flow after Tasks 1-2 and tighten any remaining headings, notes, or examples that still make legacy or incomplete command forms look runnable. Keep the doc small, current, and executable exactly as written, matching the Phase 159 context and verification gap language.</action>
  <verify>node -e "const fs=require('fs'); const text=fs.readFileSync('docs/commands.md','utf8'); if (text.includes('/bgsd-context-budget') || text.includes('/bgsd-rollback-info')) process.exit(1); if (!text.includes('/bgsd-plan gaps 159')) process.exit(1);"</verify>
  <done>- `docs/commands.md` is internally consistent and canonical-only on the remaining Phase 159 blocker surfaces
- Gap-planning and planning-family examples remain concrete and runnable where the flow shape is known
- The file is ready for follow-on full-file regression proof</done>
</task>

</tasks>

<verification>
1. `node -e "const fs=require('fs'); const text=fs.readFileSync('docs/commands.md','utf8'); if (!text.includes('Reference-style planning-family index')) process.exit(1); if (text.includes('/bgsd-plan phase|discuss|research|assumptions')) process.exit(1);"`
2. `node -e "const fs=require('fs'); const text=fs.readFileSync('docs/commands.md','utf8'); if (text.includes('#### `/bgsd-context-budget`') || text.includes('#### `/bgsd-rollback-info`') || text.includes('Compatibility alias for `/bgsd-settings validate`')) process.exit(1);"`
3. `node -e "const fs=require('fs'); const text=fs.readFileSync('docs/commands.md','utf8'); if (text.includes('/bgsd-context-budget') || text.includes('/bgsd-rollback-info')) process.exit(1); if (!text.includes('/bgsd-plan gaps 159')) process.exit(1);"`
</verification>

<success_criteria>
- `docs/commands.md` no longer surfaces planning-family shorthand or compatibility alias entries as runnable command guidance
- Canonical runnable examples remain concrete where users are expected to execute a command
- The command reference is ready for full-file regression locking in the follow-on plan
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-09-SUMMARY.md`
</output>
