---
phase: 159-help-surface-command-integrity
plan: 06
type: tdd
wave: 2
depends_on: [159-02]
files_modified:
  - templates/discovery.md
  - templates/UAT.md
  - templates/research.md
  - templates/assertions.md
  - src/plugin/tools/bgsd-context.js
  - src/plugin/idle-validator.js
  - plugin.js
  - tests/guidance-command-integrity-templates-runtime.test.cjs
autonomous: true
requirements: [CMD-05, CMD-06]
must_haves:
  truths:
    - Templates and runtime notices surface canonical commands only.
    - Examples on template and runtime surfaces include the required arguments or flags for the intended flow instead of vague placeholders when the workflow knows the value shape.
    - Plugin runtime text and the shipped `plugin.js` bundle stay aligned with the same canonical executable guidance.
  artifacts:
    - "path: "templates/discovery.md"
    - "path: "src/plugin/idle-validator.js"
    - "path: "plugin.js"
    - "path: "tests/guidance-command-integrity-templates-runtime.test.cjs"
  key_links:
    - "from: "templates/UAT.md"
    - "from: "src/plugin/idle-validator.js"
    - "from: "tests/guidance-command-integrity-templates-runtime.test.cjs"
---

<objective>
Canonicalize template and runtime surfaces so generated artifacts and plugin notices keep teaching the same canonical executable command examples users see elsewhere.

Purpose: DO-119 still leaks trust if templates or runtime notices keep stale aliases, omit required arguments, or let source and bundled plugin wording drift apart after the rest of the help surface is fixed.
Output: Canonicalized templates and runtime guidance, rebuilt plugin bundle parity, and focused regression coverage for those remaining user-facing artifacts.
</objective>

> **TDD Decision:** Selected — These supporting surfaces emit deterministic command guidance and runtime text that should be locked with targeted regression tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-RESEARCH.md
@templates/discovery.md
@src/plugin/tools/bgsd-context.js
@src/plugin/idle-validator.js
@plugin.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize template guidance surfaces</name>
  <files>templates/discovery.md, templates/UAT.md, templates/research.md, templates/assertions.md</files>
  <action>Run the Phase 159 canonical-only and parameter-correct guidance sweep across the generated templates cited in research. Replace stale planning aliases and incomplete command examples with canonical commands and filled required arguments or flags when the flow knows them. Keep this limited to surfaced guidance; do not redesign template structure beyond the command examples needed for Phase 159.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-templates-runtime.test.cjs</verify>
  <done>- Templates no longer surface legacy commands as normal guidance
- Examples include required phase numbers, profiles, or flags when the intended flow is known
- Changes stay guidance-only and do not alter template structure beyond command-surface updates</done>
</task>

<task type="auto">
  <name>Task 2: Align runtime notices, rebuild the plugin bundle, and lock parity</name>
  <files>src/plugin/tools/bgsd-context.js, src/plugin/idle-validator.js, plugin.js, tests/guidance-command-integrity-templates-runtime.test.cjs</files>
  <action>Update any stale command examples in the runtime guidance surfaces covered by Phase 159 so plugin notices remain canonical-only and executable-first, then rebuild `plugin.js` immediately to keep shipped runtime text in parity with the source. Add focused regressions covering the touched templates, runtime-source files, and bundled plugin file so the suite fails on legacy surfaced guidance, missing required arguments, or source/bundle wording drift.</action>
  <verify>npm run build</verify>
  <done>- Runtime notice sources no longer surface stale aliases or missing required parameters
- `plugin.js` matches the updated source guidance after rebuild
- Focused regressions fail on template/runtime drift, legacy surfaced guidance, or missing required arguments in known flows</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `npm run test:file -- tests/guidance-command-integrity-templates-runtime.test.cjs`
3. `node bin/bgsd-tools.cjs util:validate-commands --raw`
4. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-06-PLAN.md`
</verification>

<success_criteria>
- Templates and runtime notices are canonical-only on touched surfaces
- Required arguments and flags are filled in on known flows across template and runtime examples
- Bundled plugin output stays in sync with runtime-source wording and is protected by focused tests
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-06-SUMMARY.md`
</output>
