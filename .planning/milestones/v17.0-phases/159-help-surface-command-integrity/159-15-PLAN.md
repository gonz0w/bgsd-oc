---
phase: 159-help-surface-command-integrity
plan: 15
type: execute
wave: 6
depends_on: [159-13]
files_modified:
  - workflows/discuss-phase.md
  - tests/guidance-command-integrity-workflow-prep-b.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-06]
must_haves:
  truths:
    - Discuss-phase guidance uses canonical current commands that users can run literally.
    - This workflow no longer surfaces stale progress aliases or bare planning-family commands missing the required phase.
    - A focused regression locks this second workflow-prep slice without reopening unrelated verified surfaces.
  artifacts:
    - path: "workflows/discuss-phase.md"
      provides: "Phase-aware discuss and progress guidance"
    - path: "tests/guidance-command-integrity-workflow-prep-b.test.cjs"
      provides: "Focused regression over the discuss-phase blocker slice"
  key_links:
    - from: "workflows/discuss-phase.md"
      to: "commands/bgsd-plan.md"
      via: "canonical discuss follow-up with required phase argument"
      pattern: "/bgsd-plan discuss <phase>|/bgsd-inspect progress"
    - from: "tests/guidance-command-integrity-workflow-prep-b.test.cjs"
      to: "workflows/discuss-phase.md"
      via: "direct-file regression"
      pattern: "discuss-phase"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - Discuss-phase guidance uses canonical current commands that users can run literally.
      - This workflow no longer surfaces stale progress aliases or bare planning-family commands missing the required phase.
      - A focused regression locks this second workflow-prep slice without reopening unrelated verified surfaces.
    artifacts:
      - workflows/discuss-phase.md
      - tests/guidance-command-integrity-workflow-prep-b.test.cjs
    key_links:
      - Discuss follow-up guidance agrees with canonical planning-family and inspect routes.
      - Focused regression coverage reads the shipped workflow files directly.
---

<objective>
Close the discuss-phase blocker slice by fixing the remaining stale progress and missing-phase guidance in that workflow.

Purpose: `159-VERIFICATION.md` explicitly names `workflows/discuss-phase.md` for stale `/bgsd-progress` guidance and bare `/bgsd-plan discuss`, so this plan isolates that phase-aware guidance repair.
Output: Canonicalized discuss workflow guidance plus focused regression proving this file stays executable as written.
</objective>

> **TDD Decision:** Selected — These surfaced workflow commands are deterministic text behavior, so targeted regression should lock the exact canonical next steps and reject stale aliases or nonexistent commands.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-13-SUMMARY.md
@workflows/discuss-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the discuss-phase guidance slice</name>
  <files>workflows/discuss-phase.md</files>
  <action>Rewrite the remaining user-followable next steps in `workflows/discuss-phase.md` so they use canonical progress and discuss commands only. Replace stale progress aliases, supply the required phase argument on runnable discuss follow-ups when the workflow knows it, and keep any unavoidable shorthand explicitly reference-only rather than executable.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-b.test.cjs</verify>
  <done>- `workflows/discuss-phase.md` no longer advertises stale progress aliases or bare discuss commands as next steps
- Runnable follow-ups are canonical and executable literally
- The cleanup stays limited to the still-failing discuss-phase slice</done>
</task>

<task type="auto">
  <name>Task 2: Lock the discuss-phase slice with direct regression coverage</name>
  <files>tests/guidance-command-integrity-workflow-prep-b.test.cjs</files>
  <action>Create a focused regression that reads `workflows/discuss-phase.md` directly and fails if the exact blocker patterns named in `159-VERIFICATION.md` return. Keep coverage narrow so this plan does not reopen the already-clean workflow surfaces from Plans 12 and 13.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-b.test.cjs</verify>
  <done>- Regression fails if stale progress aliases or missing-phase discuss commands return
- The test reads the shipped workflow files directly
- Plan 15 provides surgical proof for the discuss-phase slice</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-command-integrity-workflow-prep-b.test.cjs`
2. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-15-PLAN.md`
</verification>

<success_criteria>
- `workflows/discuss-phase.md` surfaces only canonical executable guidance
- The cited stale progress and missing-phase failures are gone from this slice
- Focused regression coverage protects the exact files named in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-15-SUMMARY.md`
</output>
