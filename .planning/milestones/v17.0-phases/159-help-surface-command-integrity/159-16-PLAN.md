---
phase: 159-help-surface-command-integrity
plan: 16
type: execute
wave: 6
depends_on: [159-13]
files_modified:
  - workflows/check-todos.md
  - workflows/new-project.md
  - tests/guidance-command-integrity-workflow-prep-c.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-06]
must_haves:
  truths:
    - Check-todos and new-project guidance use canonical current commands that users can run literally.
    - These workflows no longer surface stale progress or deprecated phase-add guidance as follow-up steps.
    - A focused regression locks this third workflow-prep slice without reopening unrelated verified surfaces.
  artifacts:
    - path: "workflows/check-todos.md"
      provides: "Canonical todo inspection and progress guidance"
    - path: "workflows/new-project.md"
      provides: "Canonical project follow-up guidance"
    - path: "tests/guidance-command-integrity-workflow-prep-c.test.cjs"
      provides: "Focused regression over the third remaining workflow-prep slice"
  key_links:
    - from: "workflows/check-todos.md"
      to: "commands/bgsd-inspect.md"
      via: "canonical inspect/progress follow-up"
      pattern: "/bgsd-inspect"
    - from: "tests/guidance-command-integrity-workflow-prep-c.test.cjs"
      to: "workflows/new-project.md"
      via: "direct-file regression"
      pattern: "check-todos|new-project"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - Check-todos and new-project guidance use canonical current commands that users can run literally.
      - These workflows no longer surface stale progress or deprecated phase-add guidance as follow-up steps.
      - A focused regression locks this third workflow-prep slice without reopening unrelated verified surfaces.
    artifacts:
      - workflows/check-todos.md
      - workflows/new-project.md
      - tests/guidance-command-integrity-workflow-prep-c.test.cjs
    key_links:
      - Check-todos and new-project follow-ups agree with canonical inspect and planning-family routes.
      - Focused regression reads the shipped workflow files directly.
---

<objective>
Close the third workflow-prep blocker slice by fixing check-todos and new-project guidance.

Purpose: `159-VERIFICATION.md` still cites `workflows/check-todos.md` and `workflows/new-project.md` for stale follow-up guidance, so this plan isolates that small workflow-only cleanup from runtime and skill work.
Output: Canonicalized workflow guidance plus focused regression proving these two files stay executable as written.
</objective>

> **TDD Decision:** Selected — These surfaced workflow commands are deterministic text behavior, so targeted regression should lock the exact canonical next steps and reject stale aliases.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-13-SUMMARY.md
@workflows/check-todos.md
@workflows/new-project.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the check-todos and new-project guidance slice</name>
  <files>workflows/check-todos.md, workflows/new-project.md</files>
  <action>Rewrite the remaining user-followable next steps in these two workflows so they use only canonical inspect and planning-family commands. Remove stale progress or phase-add aliases and keep any remaining non-runnable references explicitly descriptive rather than phrased as executable guidance.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-c.test.cjs</verify>
  <done>- These workflows no longer advertise stale aliases as next steps
- Runnable follow-ups are canonical and executable literally
- The cleanup stays limited to the still-failing workflow slice</done>
</task>

<task type="auto">
  <name>Task 2: Lock the third workflow-prep slice with direct regression coverage</name>
  <files>tests/guidance-command-integrity-workflow-prep-c.test.cjs</files>
  <action>Create focused regression coverage that reads `workflows/check-todos.md` and `workflows/new-project.md` directly and fails if the exact blocker patterns named in `159-VERIFICATION.md` return. Keep coverage narrow so this plan does not reopen the already-clean workflow surfaces from Plans 12 and 13.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-c.test.cjs</verify>
  <done>- Regression fails if stale progress or phase-add aliases return
- The test reads the shipped workflow files directly
- Plan 16 provides surgical proof for this third workflow-prep slice</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-command-integrity-workflow-prep-c.test.cjs`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-16-PLAN.md`
</verification>

<success_criteria>
- `workflows/check-todos.md` and `workflows/new-project.md` surface only canonical executable guidance
- The cited stale-alias failures are gone from this slice
- Focused regression coverage protects the exact files named in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-16-SUMMARY.md`
</output>
