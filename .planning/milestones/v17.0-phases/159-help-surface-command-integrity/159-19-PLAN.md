---
phase: 159-help-surface-command-integrity
plan: 19
type: execute
wave: 6
depends_on: [159-13]
files_modified:
  - skills/planner-dependency-graph/SKILL.md
  - skills/skill-index/SKILL.md
  - tests/guidance-command-integrity-skill-tail.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-06]
must_haves:
  truths:
    - The remaining skill surfaces cited by verification use current canonical command forms instead of stale or nonexistent runnable commands.
    - Any concise reference text retained in these skills is clearly non-runnable metadata rather than user-followable next-step guidance.
    - A focused regression locks this final skill-content slice without reopening already-clean skills.
  artifacts:
    - path: "skills/planner-dependency-graph/SKILL.md"
      provides: "Canonical dependency-graph skill guidance"
    - path: "skills/skill-index/SKILL.md"
      provides: "Current skill index metadata"
    - path: "tests/guidance-command-integrity-skill-tail.test.cjs"
      provides: "Focused regression over the remaining skill-content blocker slice"
  key_links:
    - from: "skills/planner-dependency-graph/SKILL.md"
      to: "commands/bgsd-inspect.md"
      via: "current guidance for plan-wave inspection"
      pattern: "verify:plan-wave|inspect"
    - from: "skills/skill-index/SKILL.md"
      to: "commands/bgsd-plan.md"
      via: "current phase lookup naming"
      pattern: "phase"
    - from: "tests/guidance-command-integrity-skill-tail.test.cjs"
      to: "skills/planner-dependency-graph/SKILL.md"
      via: "direct-file regression"
      pattern: "planner-dependency-graph|skill-index"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - The remaining skill surfaces cited by verification use current canonical command forms instead of stale or nonexistent runnable commands.
      - Any concise reference text retained in these skills is clearly non-runnable metadata rather than user-followable next-step guidance.
      - A focused regression locks this final skill-content slice without reopening already-clean skills.
    artifacts:
      - skills/planner-dependency-graph/SKILL.md
      - skills/skill-index/SKILL.md
      - tests/guidance-command-integrity-skill-tail.test.cjs
    key_links:
      - The final skill-content slice is aligned with current command-family naming.
      - Focused regression reads the shipped skill files directly.
---

<objective>
Close the remaining skill-content blocker slice by fixing the last two skill files still cited by verification.

Purpose: `159-VERIFICATION.md` isolates the last skill-content failures to `skills/planner-dependency-graph/SKILL.md` and `skills/skill-index/SKILL.md`, so this plan repairs those exact surfaces without broadening scope back across all skills.
Output: Canonicalized remaining skill content plus focused regression proving these two files stay validator-clean for the right reasons.
</objective>

> **TDD Decision:** Selected — These skill surfaces are deterministic user-facing text, so direct file-backed regression is the right proof for the exact command forms they present.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-13-SUMMARY.md
@skills/planner-dependency-graph/SKILL.md
@skills/skill-index/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the remaining skill-content guidance</name>
  <files>skills/planner-dependency-graph/SKILL.md, skills/skill-index/SKILL.md</files>
  <action>Rewrite the remaining skill text named by verification so it uses current command-family naming and supported command examples only. Remove stale or nonexistent command references when they are presented as user-followable guidance, and keep any retained shorthand explicitly framed as reference-only metadata rather than executable instructions.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-skill-tail.test.cjs</verify>
  <done>- The two cited skill files no longer surface stale or nonexistent runnable commands
- Any retained shorthand is clearly reference-only
- The cleanup stays limited to the remaining skill-content slice</done>
</task>

<task type="auto">
  <name>Task 2: Lock the skill-content slice with direct regression coverage</name>
  <files>tests/guidance-command-integrity-skill-tail.test.cjs</files>
  <action>Create focused regression coverage that reads `skills/planner-dependency-graph/SKILL.md` and `skills/skill-index/SKILL.md` directly and fails if the blocker patterns named in `159-VERIFICATION.md` return. Keep the test scoped to these two files so the plan remains surgical.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-skill-tail.test.cjs</verify>
  <done>- Regression fails if stale or nonexistent command references return in the cited skill files
- The test reads the shipped skill files directly
- Plan 19 provides a surgical proof path for the remaining skill-content slice</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-command-integrity-skill-tail.test.cjs`
2. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-19-PLAN.md`
</verification>

<success_criteria>
- `skills/planner-dependency-graph/SKILL.md` and `skills/skill-index/SKILL.md` surface only canonical current command forms or clearly reference-only metadata
- The cited remaining skill-content failures are gone
- Focused regression coverage protects the exact files named in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-19-SUMMARY.md`
</output>
