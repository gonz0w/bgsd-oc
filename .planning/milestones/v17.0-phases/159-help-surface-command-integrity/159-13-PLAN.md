---
phase: 159-help-surface-command-integrity
plan: 13
type: tdd
wave: 5
depends_on: [159-07, 159-12]
files_modified:
  - docs/getting-started.md
  - docs/milestones.md
  - docs/expert-guide.md
  - workflows/research-phase.md
  - workflows/plan-milestone-gaps.md
  - templates/state.md
  - skills/phase-argument-parsing/SKILL.md
  - skills/planner-scope-estimation/SKILL.md
  - agents/bgsd-github-ci.md
  - src/lib/commandDiscovery.js
  - tests/validate-commands.test.cjs
  - tests/guidance-command-integrity-gap-closure.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-05, CMD-06]
must_haves:
  truths:
    - Remaining directly surfaced planning, gap-closure, todo, and progress guidance uses canonical executable commands with required arguments instead of legacy aliases or missing-argument shorthand.
    - Reference-only skill and agent metadata no longer trip the command-integrity validator unless they are genuinely presenting runnable guidance.
    - Phase 159 has one focused proof path for the exact remaining gap-closure surfaces plus the narrowed validator rule that supports them.
  artifacts:
    - "path: "docs/getting-started.md"
    - "path: "docs/milestones.md"
    - "path: "docs/expert-guide.md"
    - "path: "workflows/research-phase.md"
    - "path: "workflows/plan-milestone-gaps.md"
    - "path: "templates/state.md"
    - "path: "skills/phase-argument-parsing/SKILL.md"
    - "path: "skills/planner-scope-estimation/SKILL.md"
    - "path: "agents/bgsd-github-ci.md"
    - "path: "src/lib/commandDiscovery.js"
    - "path: "tests/validate-commands.test.cjs"
    - "path: "tests/guidance-command-integrity-gap-closure.test.cjs"
  key_links:
    - "from: "docs/getting-started.md"
    - "from: "workflows/plan-milestone-gaps.md"
    - "from: "templates/state.md"
    - "from: "tests/guidance-command-integrity-gap-closure.test.cjs"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - Remaining directly surfaced planning, gap-closure, todo, and progress guidance uses canonical executable commands with required arguments instead of legacy aliases or missing-argument shorthand.
      - Reference-only skill and agent metadata no longer trip the command-integrity validator unless they are genuinely presenting runnable guidance.
      - Phase 159 has one focused proof path for the exact remaining gap-closure surfaces plus the narrowed validator rule that supports them.
    artifacts:
      - docs/getting-started.md
      - docs/milestones.md
      - docs/expert-guide.md
      - workflows/research-phase.md
      - workflows/plan-milestone-gaps.md
      - templates/state.md
      - skills/phase-argument-parsing/SKILL.md
      - skills/planner-scope-estimation/SKILL.md
      - agents/bgsd-github-ci.md
      - src/lib/commandDiscovery.js
      - tests/validate-commands.test.cjs
      - tests/guidance-command-integrity-gap-closure.test.cjs
    key_links:
      - docs and workflow next steps agree on the canonical `/bgsd-plan ...`, `/bgsd-inspect progress`, and `/bgsd-execute-phase <phase>` routes users should actually run.
      - templates and surfaced support content keep compatibility notes reference-only instead of teaching deprecated todo or progress aliases as active guidance.
      - `src/lib/commandDiscovery.js` distinguishes clearly reference-only metadata from executable guidance, and the regression tests lock that boundary.
---

<objective>
Close the remaining high-signal Phase 159 gaps by fixing the still-user-followable guidance called out in re-verification and trimming validator false positives on clearly reference-only metadata.

Purpose: `159-VERIFICATION.md` shows that the last fixed Phase 159 slice is clean, but CMD-06 still fails because users can still hit stale aliases, missing arguments, and old todo/progress guidance in onboarding docs, workflows, templates, skills, and an agent prompt.
Output: Canonicalized remaining surfaced guidance plus validator and regression updates that prove these exact blocker surfaces are now executable or explicitly reference-only.
</objective>

> **TDD Decision:** Selected — The gap is deterministic surfaced guidance plus validator classification, so focused regressions should lock both the exact wording and the allowed reference-only boundary.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-07-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-12-SUMMARY.md
@docs/getting-started.md
@docs/milestones.md
@docs/expert-guide.md
@workflows/research-phase.md
@workflows/plan-milestone-gaps.md
@templates/state.md
@skills/phase-argument-parsing/SKILL.md
@skills/planner-scope-estimation/SKILL.md
@agents/bgsd-github-ci.md
@src/lib/commandDiscovery.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the remaining user-followable docs and workflow next steps</name>
  <files>docs/getting-started.md, docs/milestones.md, docs/expert-guide.md, workflows/research-phase.md, workflows/plan-milestone-gaps.md</files>
  <action>Rewrite the remaining surfaced planning, progress, gap-closure, and CLI examples so they teach the current canonical commands exactly as users should run them. Replace legacy `gsd-tools` or deprecated slash aliases, keep required phase arguments visible on runnable forms, and ensure gap-planning flows point to `/bgsd-plan gaps <phase>` rather than generic planning or execution commands when the user is explicitly closing gaps.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-gap-closure.test.cjs</verify>
  <done>- The cited docs and workflows no longer recommend `/bgsd-progress`, `/bgsd-plan phase` without the needed argument shape, or stale `gsd-tools` runnable examples
- Gap-closure follow-ups use the canonical gap-planning route instead of generic planning/execution shortcuts
- Any preserved compatibility wording is clearly reference-only</done>
</task>

<task type="auto">
  <name>Task 2: Fix the remaining template, skill, and agent guidance blockers</name>
  <files>templates/state.md, skills/phase-argument-parsing/SKILL.md, skills/planner-scope-estimation/SKILL.md, agents/bgsd-github-ci.md</files>
  <action>Update the remaining surfaced template, skill, and agent text so users and downstream agents see current command names and next-step guidance. Replace deprecated todo aliases in `templates/state.md`, refresh stale CLI naming in skill descriptions, and keep any non-runnable descriptions or metadata framed as reference-only instead of executable examples.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-gap-closure.test.cjs</verify>
  <done>- Template follow-up guidance uses supported canonical commands
- Skill and agent descriptions no longer surface stale command names as if they are runnable guidance
- Reference-only descriptions are clearly non-executable</done>
</task>

<task type="auto">
  <name>Task 3: Refine validator classification and lock the gap-closure slice with regressions</name>
  <files>src/lib/commandDiscovery.js, tests/validate-commands.test.cjs, tests/guidance-command-integrity-gap-closure.test.cjs</files>
  <action>Narrow the command-integrity validator only where needed so obviously reference-only metadata or descriptive text is not treated as executable guidance, without weakening enforcement on real help and next-step surfaces. Add focused regression coverage that reads the exact remaining Phase 159 gap files directly, asserts the blocker patterns named in `159-VERIFICATION.md` stay gone, and proves the validator still flags genuine runnable mistakes while allowing the intended reference-only forms.</action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs && npm run test:file -- tests/guidance-command-integrity-gap-closure.test.cjs</verify>
  <done>- Validator behavior distinguishes executable guidance from clearly reference-only metadata in the touched surfaces
- Regressions read the real shipped gap-closure files and fail if stale aliases, missing phase arguments, or deprecated todo/progress routes return
- The touched repo-wide validation slice is materially reduced without masking real surfaced-guidance failures</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/validate-commands.test.cjs`
2. `npm run test:file -- tests/guidance-command-integrity-gap-closure.test.cjs`
3. `node bin/bgsd-tools.cjs util:validate-commands`
4. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-13-PLAN.md`
</verification>

<success_criteria>
- The exact remaining blocker files called out by `159-VERIFICATION.md` now surface only canonical executable commands or clearly marked reference-only wording
- Templates, skills, and the CI agent prompt stop surfacing deprecated todo, progress, or stale CLI names as active next steps
- Validator regressions preserve strict enforcement on runnable guidance while allowing the specific reference-only forms intentionally kept in the touched surfaces
- Repo-wide validation no longer reports these Phase 159 blocker patterns from the touched gap-closure slice
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-13-SUMMARY.md`
</output>
