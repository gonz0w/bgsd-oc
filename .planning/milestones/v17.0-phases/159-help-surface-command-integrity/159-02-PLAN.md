---
phase: 159-help-surface-command-integrity
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/commandDiscovery.js
  - src/commands/misc.js
  - src/router.js
  - tests/validate-commands.test.cjs
autonomous: true
requirements: [CMD-05, CMD-06]
must_haves:
  truths:
    - Maintainers can run one auditable validator that checks real command inventories against user-facing guidance surfaces.
    - Validation reports grouped actionable issues for nonexistent commands, legacy mentions, and missing required arguments or flags.
    - Flow-specific guidance mistakes such as wrong planning-family subcommands or missing `--gaps-only` are caught automatically.
  artifacts:
    - "path: "src/lib/commandDiscovery.js"
    - "path: "src/commands/misc.js"
    - "path: "src/router.js"
    - "path: "tests/validate-commands.test.cjs"
  key_links:
    - "from: "src/lib/commandDiscovery.js"
    - "from: "src/commands/misc.js"
    - "from: "tests/validate-commands.test.cjs"
---

<objective>
Build the Phase 159 command-integrity gate so the repo can audit real shipped commands, canonical-only guidance, and parameter-correct next-step examples from one validator path.

Purpose: DO-119 needs automated command-reference validation to prevent stale aliases, nonexistent commands, and missing required arguments from leaking into user-facing surfaces.
Output: Expanded validation logic in the command-discovery layer, a strengthened `util:validate-commands` command, and focused validator regression tests.
</objective>

> **TDD Decision:** Selected — This plan introduces deterministic validation behavior with clear input/output contracts that should be proven through targeted tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-RESEARCH.md
@src/lib/commandDiscovery.js
@src/commands/misc.js
@src/router.js
@commands/bgsd-plan.md
@bin/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand command discovery into an inventory-backed integrity validator</name>
  <files>src/lib/commandDiscovery.js</files>
  <action>Refactor `src/lib/commandDiscovery.js` so Phase 159 validation uses real shipped inventories plus a defined scan scope for user-facing surfaces instead of registry-only checks. Add semantic rules for canonical planning guidance, canonical-only surfaced text, required phase arguments, required flags like `--gaps-only` where the flow requires them, and grouped issue metadata by surface/file. Keep the validator tool-agnostic and avoid hand-maintained parallel command allowlists.</action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs</verify>
  <done>- Validation checks command existence and flow-specific correctness, not only registry alignment
- Validation scope covers user-facing surfaces named in Phase 159 context and research
- Issues are grouped with actionable per-surface metadata</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the `util:validate-commands` CLI contract</name>
  <files>src/commands/misc.js, src/router.js</files>
  <action>Update the `util:validate-commands` entrypoint and any required router wiring so the shipped CLI exposes the new integrity validator, returns grouped actionable output, and exits non-zero when surfaced guidance is invalid. Keep the command additive and backward compatible with current invocation shape instead of inventing a new command family.</action>
  <verify>node bin/bgsd-tools.cjs util:validate-commands --raw</verify>
  <done>- `util:validate-commands` runs the expanded integrity scan
- CLI output includes grouped surface/file issues instead of only registry mismatches
- The validator fails clearly when canonical-only or parameter-correct guidance rules are violated</done>
</task>

<task type="auto">
  <name>Task 3: Add focused contract tests for validator behavior</name>
  <files>tests/validate-commands.test.cjs</files>
  <action>Create focused tests that exercise the validator against representative stale-guidance fixtures or repo surfaces, covering nonexistent commands, legacy mentions in surfaced guidance, wrong planning-family next steps, and missing required arguments or flags. Keep the test scope on validator behavior rather than duplicating the later repo-wide wording sweep.</action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs</verify>
  <done>- Tests prove grouped reporting and non-zero failure behavior
- Tests cover at least one canonical-only violation and one missing-parameter violation
- Tests protect the validator core independently of the later content-sweep plans</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/validate-commands.test.cjs`
2. `node bin/bgsd-tools.cjs util:validate-commands --raw`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-02-PLAN.md`
</verification>

<success_criteria>
- A single shipped validator can audit user-facing command guidance against real inventories
- The validator catches stale aliases, nonexistent commands, and missing required parameters or flags
- Failures are reported in grouped actionable output suitable for bulk repair
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-02-SUMMARY.md`
</output>
