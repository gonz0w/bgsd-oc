---
phase: 159-help-surface-command-integrity
plan: 20
type: execute
wave: 7
depends_on: [159-14, 159-15, 159-16, 159-17, 159-18, 159-19]
files_modified:
  - src/lib/commandDiscovery.js
  - tests/validate-commands.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-05, CMD-06]
must_haves:
  truths:
    - Validator scope remains broad across user-facing surfaces while exempting only truly reference-only text that users are not meant to run literally.
    - The command-integrity validator still fails stale aliases, nonexistent commands, and missing required arguments on runnable guidance after the remaining workflow, runtime, and skill slices are cleaned.
    - Repo-wide validation can be used as the final proof path for the remaining Phase 159 blocker set without reopening already-clean reference-only text.
  artifacts:
    - path: "src/lib/commandDiscovery.js"
      provides: "Final validator scope alignment for reference-only versus runnable guidance"
    - path: "tests/validate-commands.test.cjs"
      provides: "Validator contract proof for the final Phase 159 gap-closure boundary"
  key_links:
    - from: "src/lib/commandDiscovery.js"
      to: "tests/validate-commands.test.cjs"
      via: "contract coverage for allowed reference-only forms and rejected runnable mistakes"
      pattern: "validateCommandIntegrity"
    - from: "src/lib/commandDiscovery.js"
      to: "workflows/ and skills/ touched in Plans 14-19"
      via: "repo-wide command-integrity scan over cleaned surfaced guidance"
      pattern: "util:validate-commands"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - Validator scope remains broad across user-facing surfaces while exempting only truly reference-only text that users are not meant to run literally.
      - The command-integrity validator still fails stale aliases, nonexistent commands, and missing required arguments on runnable guidance after the remaining workflow, runtime, and skill slices are cleaned.
      - Repo-wide validation can be used as the final proof path for the remaining Phase 159 blocker set without reopening already-clean reference-only text.
    artifacts:
      - src/lib/commandDiscovery.js
      - tests/validate-commands.test.cjs
    key_links:
      - Validator scope alignment stays narrow and auditable.
      - Repo-wide validation remains the final proof path for CMD-05 and CMD-06 closure.
---

<objective>
Finish Phase 159 gap closure by aligning validator scope only where the last cleaned workflow, runtime, and skill surfaces prove a text is truly reference-only.

Purpose: The remaining blocker is not just stale text; `159-VERIFICATION.md` also allows narrowing validation where text is genuinely reference-only. This final plan preserves broad user-facing audit coverage while making repo-wide validation a trustworthy final gate once Plans 14-19 clean the cited surfaces.
Output: Final validator-scope alignment plus contract tests proving repo-wide command validation stays strict on runnable guidance and narrow on reference-only text.
</objective>

> **TDD Decision:** Selected — Validator classification is deterministic behavior with clear pass/fail outcomes, so contract tests should prove the exact boundary between allowed reference-only text and rejected runnable guidance.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-07-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-13-SUMMARY.md
@src/lib/commandDiscovery.js
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align validator scope with the final cleaned guidance boundary</name>
  <files>src/lib/commandDiscovery.js</files>
  <action>After the remaining workflow, runtime, and skill slices are cleaned, adjust `validateCommandIntegrity()` only where needed so truly reference-only text can remain concise without weakening the locked broad audit over user-followable guidance. Preserve failures for stale aliases, nonexistent commands, and missing required arguments anywhere text is still phrased as a command users should run.</action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs</verify>
  <done>- Validator exemptions stay narrow and auditable
- Runnable guidance still fails on stale aliases, nonexistent commands, and missing required arguments
- Repo-wide validation is reduced by removing only genuine reference-only false positives</done>
</task>

<task type="auto">
  <name>Task 2: Prove the final validator boundary with contract tests and repo validation</name>
  <files>tests/validate-commands.test.cjs</files>
  <action>Extend validator contract coverage so it proves the final allowed reference-only forms and still-rejected runnable mistakes after Plans 14-19 land. Use repo validation as the final proof path for this gap-closure slice, with assertions that the remaining cited workflow, runtime, and skill blockers no longer appear while genuine runnable defects would still be caught.</action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs && node bin/bgsd-tools.cjs util:validate-commands</verify>
  <done>- Contract tests document the final reference-only versus runnable boundary
- Repo-wide validation can serve as the final Phase 159 integrity gate
- Plan 20 closes the validator-scope portion of the remaining blocker without weakening CMD-05 coverage</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/validate-commands.test.cjs`
2. `node bin/bgsd-tools.cjs util:validate-commands`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-20-PLAN.md`
</verification>

<success_criteria>
- Validator scope alignment remains narrow and auditable
- Repo-wide validation stays strict on runnable guidance and clean on truly reference-only text for the touched blocker slices
- The final Phase 159 gap-closure gate can rely on `util:validate-commands` without reopening already-verified surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-20-SUMMARY.md`
</output>
