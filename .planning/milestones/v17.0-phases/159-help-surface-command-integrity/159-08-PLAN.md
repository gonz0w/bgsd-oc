---
phase: 159-help-surface-command-integrity
plan: 08
type: execute
wave: 2
depends_on: [159-07]
files_modified:
  - workflows/help.md
  - docs/commands.md
  - docs/planning-system.md
  - docs/troubleshooting.md
  - skills/raci/SKILL.md
  - tests/guidance-help-surface.test.cjs
  - tests/guidance-command-integrity-docs.test.cjs
  - tests/guidance-command-integrity-agent-skill-surfaces.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-04, CMD-05, CMD-06]
must_haves:
  truths:
    - The main help surface keeps `/bgsd-review` in the core path and no longer routes users through legacy `/bgsd-progress` guidance.
    - Remaining Phase 159 docs and skills stop surfacing stale legacy inspect/search/health guidance as runnable next steps.
    - Reference-style command tables can stay concise while runnable examples remain concrete and validator-clean.
  artifacts:
    - workflows/help.md
    - docs/commands.md
    - docs/planning-system.md
    - docs/troubleshooting.md
    - skills/raci/SKILL.md
    - tests/guidance-help-surface.test.cjs
    - tests/guidance-command-integrity-docs.test.cjs
    - tests/guidance-command-integrity-agent-skill-surfaces.test.cjs
  key_links:
    - workflows/help.md and docs/troubleshooting.md point progress and health guidance at canonical /bgsd-inspect routes.
    - docs/planning-system.md and docs/commands.md align inspect search/trace references with the canonical inspect family while preserving allowed reference-style forms.
    - Focused help/docs/skill tests and the shared validator pass on the touched Phase 159 surfaces together.
---

<objective>
Close the remaining genuine content gaps in help, docs, and reference-style skills so users see only current executable guidance on real next-step surfaces while concise reference forms stop causing false validation failures.

Purpose: Phase 159 still fails because shipped help and high-traffic docs contain stale canonical guidance even after the validator exists; this plan removes those last trust-breaking surfaces.
Output: Canonicalized help/docs/skill content plus focused regressions that prove the touched Phase 159 surfaces now pass the shared validator.
</objective>

> **TDD Decision:** Skipped — This is a surgical content-and-regression sweep across help/docs/skill surfaces, not a single RED-GREEN-REFACTOR behavior unit.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-01-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-03-SUMMARY.md
@.planning/phases/159-help-surface-command-integrity/159-05-SUMMARY.md
@workflows/help.md
@docs/commands.md
@docs/planning-system.md
@docs/troubleshooting.md
@skills/raci/SKILL.md
@commands/bgsd-inspect.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stale progress guidance in the main help surface with canonical inspect routing</name>
  <files>workflows/help.md, tests/guidance-help-surface.test.cjs</files>
  <action>Update the top-level help surface so the core path, resume example, and final “getting help” callout stop surfacing `/bgsd-progress` and instead use the canonical inspect/progress route that exists in the shipped command family. Keep the help screen compact, keep `/bgsd-review` in the primary path, and make every runnable example concrete and executable exactly as written.</action>
  <verify>npm run test:file -- tests/guidance-help-surface.test.cjs</verify>
  <done>- `workflows/help.md` no longer recommends `/bgsd-progress`
- Core help still includes `/bgsd-review` and the compact core-vs-advanced split
- Help regression coverage fails if legacy progress guidance returns</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize the remaining touched docs and reference-style skill guidance</name>
  <files>docs/commands.md, docs/planning-system.md, docs/troubleshooting.md, skills/raci/SKILL.md</files>
  <action>Fix the concrete stale guidance named in `159-VERIFICATION.md`: replace legacy inspect/search/trace/health/progress next-step wording with canonical `/bgsd-inspect ...` routes, keep runnable examples concrete when the flow shape is known, and preserve concise placeholder or family-form commands only where the file is acting as a reference matrix or index. In `docs/commands.md`, align narrative and preferred-route callouts with the validator refinement from Plan 07 so canonical runnable examples stay concrete while explicitly reference-style planning-family summaries no longer pretend to be executable next steps.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-docs.test.cjs</verify>
  <done>- Planning-system and troubleshooting guidance no longer surface `/bgsd-trace`, `/bgsd-search-decisions`, `/bgsd-search-lessons`, `/bgsd-health`, or `/bgsd-progress` as user guidance
- `docs/commands.md` separates reference-style family summaries from runnable examples cleanly enough to stay validator-clean
- `skills/raci/SKILL.md` remains canonical-first while using allowed reference-style placeholder forms only in its ownership matrix</done>
</task>

<task type="auto">
  <name>Task 3: Re-lock the touched help/docs/skill surfaces against the shared validator</name>
  <files>tests/guidance-command-integrity-docs.test.cjs, tests/guidance-command-integrity-agent-skill-surfaces.test.cjs</files>
  <action>Update the focused Phase 159 regressions so they assert the new canonical inspect guidance on touched help/doc/skill surfaces and run the shared validator against representative snippets that reflect the post-Plan-07 reference-style exception. Keep the assertions tight to the files named in the gap report, then use the shared validator as the final proof that this content sweep resolves the remaining real guidance failures without reopening placeholder false positives.</action>
  <verify>node bin/bgsd-tools.cjs util:validate-commands --raw</verify>
  <done>- Focused tests cover the exact help/doc/skill files cited by verification
- Shared validator passes on the touched surfaces and is ready for repo-wide closure proof
- Phase 159 content gaps are reduced to zero for CMD-04 and CMD-06 on the cited files</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-help-surface.test.cjs`
2. `npm run test:file -- tests/guidance-command-integrity-docs.test.cjs`
3. `npm run test:file -- tests/guidance-command-integrity-agent-skill-surfaces.test.cjs`
4. `node bin/bgsd-tools.cjs util:validate-commands --raw`
</verification>

<success_criteria>
- Help no longer surfaces stale `/bgsd-progress` guidance and still satisfies the compact core-path contract
- The docs and RACI skill cited in verification use canonical inspect/planning guidance where users are expected to run commands
- Reference-style placeholder or family forms are limited to allowed reference contexts and no longer block validator-based phase closure
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-08-SUMMARY.md`
</output>
