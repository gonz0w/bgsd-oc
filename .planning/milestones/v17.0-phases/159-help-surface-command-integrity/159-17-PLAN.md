---
phase: 159-help-surface-command-integrity
plan: 17
type: execute
wave: 6
depends_on: [159-13]
files_modified:
  - workflows/new-milestone.md
  - tests/guidance-command-integrity-workflow-prep-d.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-06]
must_haves:
  truths:
    - New-milestone guidance uses current canonical command forms instead of stale or nonexistent runnable commands.
    - Any concise reference text retained in this workflow is clearly non-runnable metadata rather than user-followable next-step guidance.
    - A focused regression locks this final workflow-only milestone slice without reopening already-clean workflow surfaces.
  artifacts:
    - path: "workflows/new-milestone.md"
      provides: "Current milestone workflow guidance"
    - path: "tests/guidance-command-integrity-workflow-prep-d.test.cjs"
      provides: "Focused regression over the remaining milestone workflow slice"
  key_links:
    - from: "workflows/new-milestone.md"
      to: "commands/bgsd-plan.md"
      via: "current milestone planning guidance"
      pattern: "/bgsd-plan"
    - from: "tests/guidance-command-integrity-workflow-prep-d.test.cjs"
      to: "workflows/new-milestone.md"
      via: "direct-file regression"
      pattern: "new-milestone"
intent:
  outcome_ids: [DO-119]
  must_haves:
    truths:
      - New-milestone guidance uses current canonical command forms instead of stale or nonexistent runnable commands.
      - Any concise reference text retained in this workflow is clearly non-runnable metadata rather than user-followable next-step guidance.
      - A focused regression locks this final workflow-only milestone slice without reopening already-clean workflow surfaces.
    artifacts:
      - workflows/new-milestone.md
      - tests/guidance-command-integrity-workflow-prep-d.test.cjs
    key_links:
      - The final workflow-only milestone slice is aligned with current command-family naming.
      - Focused regression reads the shipped workflow file directly.
---

<objective>
Close the final workflow-only milestone blocker slice by fixing the remaining stale guidance in `workflows/new-milestone.md`.

Purpose: `159-VERIFICATION.md` cites `workflows/new-milestone.md` for a nonexistent milestone command, so this plan isolates that single-file repair instead of batching it with broader runtime or skill work.
Output: Canonicalized milestone workflow guidance plus focused regression proving this file stays executable as written.
</objective>

> **TDD Decision:** Selected — This workflow surface is deterministic user-facing text, so direct file-backed regression is the right proof for the exact command form it presents.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-13-SUMMARY.md
@workflows/new-milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the new-milestone guidance slice</name>
  <files>workflows/new-milestone.md</files>
  <action>Rewrite the remaining user-followable guidance in `workflows/new-milestone.md` so it uses current canonical command-family naming only. Replace the cited nonexistent milestone command and keep any retained shorthand explicitly framed as reference-only metadata rather than executable guidance.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-d.test.cjs</verify>
  <done>- `workflows/new-milestone.md` no longer surfaces nonexistent runnable commands
- Any retained shorthand is clearly reference-only
- The cleanup stays limited to this final workflow-only milestone slice</done>
</task>

<task type="auto">
  <name>Task 2: Lock the new-milestone slice with direct regression coverage</name>
  <files>tests/guidance-command-integrity-workflow-prep-d.test.cjs</files>
  <action>Create focused regression coverage that reads `workflows/new-milestone.md` directly and fails if the blocker patterns named in `159-VERIFICATION.md` return. Keep the test scoped to this file so the plan remains surgical.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-workflow-prep-d.test.cjs</verify>
  <done>- Regression fails if the cited nonexistent milestone command returns
- The test reads the shipped workflow file directly
- Plan 17 provides a surgical proof path for this final workflow-only milestone slice</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-command-integrity-workflow-prep-d.test.cjs`
2. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-17-PLAN.md`
</verification>

<success_criteria>
- `workflows/new-milestone.md` surfaces only canonical current command forms or clearly reference-only metadata
- The cited remaining milestone workflow failure is gone
- Focused regression coverage protects the exact files named in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-17-SUMMARY.md`
</output>
