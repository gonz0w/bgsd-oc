---
phase: 159-help-surface-command-integrity
plan: 11
type: execute
wave: 3
depends_on: [159-09, 159-10]
files_modified:
  - tests/guidance-command-integrity-docs.test.cjs
autonomous: true
gap_closure: true
requirements: [CMD-05, CMD-06]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - Full-file regression proof covers the exact shipped docs named in verification, not only focused snippets.
    - Regression fails if planning-family ambiguity or the cited legacy commands return to those docs.
    - Phase 159 has one narrow proof path for the shipped doc blockers without reopening validator-core behavior.
  artifacts:
    - tests/guidance-command-integrity-docs.test.cjs
  key_links:
    - tests/guidance-command-integrity-docs.test.cjs reads the actual shipped contents of `docs/commands.md`, `docs/planning-system.md`, and `docs/troubleshooting.md` after Plans 09-10.
    - Regression expectations encode the Phase 159 canonical-only and argument-correct guidance contract on those exact files.
    - The test remains focused on shipped doc blockers rather than unrelated repo-wide validation noise.
---

<objective>
Lock the shipped-doc fixes with full-file regression coverage on the exact Phase 159 blocker docs.

Purpose: The gap report explicitly warns against relying only on focused snippets, so this final plan must prove the real shipped files stay clean without reopening already-passed validator-core work.
Output: Expanded `tests/guidance-command-integrity-docs.test.cjs` coverage that asserts the full contents of `docs/commands.md`, `docs/planning-system.md`, and `docs/troubleshooting.md`.
</objective>

> **TDD Decision:** Skipped — This is regression hardening on existing documentation surfaces, not a new behavior feature that benefits from RED-GREEN-REFACTOR.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-09-PLAN.md
@.planning/phases/159-help-surface-command-integrity/159-10-PLAN.md
@tests/guidance-command-integrity-docs.test.cjs
@docs/commands.md
@docs/planning-system.md
@docs/troubleshooting.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read the real shipped blocker docs inside the doc guidance regression</name>
  <files>tests/guidance-command-integrity-docs.test.cjs</files>
  <action>Update the doc guidance regression so it reads the actual shipped contents of `docs/commands.md`, `docs/planning-system.md`, and `docs/troubleshooting.md` instead of proving only focused snippets or isolated strings. Keep the test narrow to the Phase 159 blocker files and use deterministic assertions that match the current shipped doc contract.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-docs.test.cjs</verify>
  <done>- The test opens and checks the real shipped blocker docs directly
- Proof no longer depends on extracted snippet fixtures alone
- The regression stays focused on the exact Phase 159 blocker files</done>
</task>

<task type="auto">
  <name>Task 2: Assert that planning-family ambiguity and cited legacy commands stay gone</name>
  <files>tests/guidance-command-integrity-docs.test.cjs</files>
  <action>Add expectations that fail if `docs/commands.md` reintroduces planning-family shorthand as runnable guidance or if `docs/planning-system.md` / `docs/troubleshooting.md` reintroduce the legacy commands named in `159-VERIFICATION.md`. Encode the canonical-only, executable-first rule from context directly in the regression expectations.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-docs.test.cjs</verify>
  <done>- Regression coverage fails on the exact ambiguity and legacy-command blockers named by verification
- The canonical-only and argument-correct doc contract is explicit in test expectations
- The proof remains scoped to shipped doc blockers, not unrelated validator noise</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-command-integrity-docs.test.cjs`
</verification>

<success_criteria>
- `tests/guidance-command-integrity-docs.test.cjs` verifies the full shipped contents of the exact blocker docs named in `159-VERIFICATION.md`
- Tests fail if planning-family ambiguity or the cited legacy commands return to those docs
- Phase 159 has a narrow, executable proof path for CMD-06 on real shipped documentation
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-11-SUMMARY.md`
</output>
