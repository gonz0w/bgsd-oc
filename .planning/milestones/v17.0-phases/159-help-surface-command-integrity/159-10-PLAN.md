---
phase: 159-help-surface-command-integrity
plan: 10
type: execute
wave: 2
depends_on: [159-09]
files_modified:
  - docs/planning-system.md
  - docs/troubleshooting.md
autonomous: true
gap_closure: true
requirements: [CMD-04, CMD-06]
intent:
  outcome_ids: [DO-119]
must_haves:
  truths:
    - Planning-system and troubleshooting docs no longer surface the legacy commands named in verification as active user guidance.
    - Canonical replacement guidance includes the required routed command shape when the flow is known.
    - The two remaining narrative docs are structurally clean and ready for follow-on full-file regression proof.
  artifacts:
    - docs/planning-system.md
    - docs/troubleshooting.md
  key_links:
    - docs/planning-system.md removes `/bgsd-context-budget compare` in favor of current command guidance that matches the shipped surface.
    - docs/troubleshooting.md replaces `/bgsd-rollback-info` and `/bgsd-context-budget` with canonical executable routes.
    - Both docs stay aligned with the canonical command-reference cleanup from Plan 09 so the follow-on regression plan can lock the whole blocker set together.
---

<objective>
Finish the remaining narrative-doc cleanup so the exact legacy commands named in verification are removed from planning and troubleshooting guidance.

Purpose: After the command reference is fixed, these two shipped docs are the remaining content blockers preventing users from trusting surfaced next steps as executable exactly as written.
Output: Canonicalized `docs/planning-system.md` and `docs/troubleshooting.md` ready for follow-on full-file regression locking.
</objective>

> **TDD Decision:** Skipped — This plan edits shipped docs only; it does not introduce a new behavior surface suited to RED-GREEN-REFACTOR.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-VERIFICATION.md
@.planning/phases/159-help-surface-command-integrity/159-09-PLAN.md
@docs/planning-system.md
@docs/troubleshooting.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy context-budget guidance from planning-system docs</name>
  <files>docs/planning-system.md</files>
  <action>Replace the `docs/planning-system.md:365` guidance that still surfaces `/bgsd-context-budget compare`. Use the current canonical command route that matches the shipped command surface and keep the example concrete if the flow shape is known from the surrounding prose.</action>
  <verify>node -e "const fs=require('fs'); const text=fs.readFileSync('docs/planning-system.md','utf8'); if (text.includes('/bgsd-context-budget compare')) process.exit(1);"</verify>
  <done>- `docs/planning-system.md` no longer teaches `/bgsd-context-budget compare`
- Replacement text matches the current canonical command surface
- The example is executable as written or explicitly non-runnable reference text</done>
</task>

<task type="auto">
  <name>Task 2: Replace legacy troubleshooting routes with canonical executable guidance</name>
  <files>docs/troubleshooting.md</files>
  <action>Update the exact troubleshooting fixes cited by verification so they no longer surface `/bgsd-rollback-info` or `/bgsd-context-budget`. Replace them with canonical commands users can run as written, including required arguments when the surrounding troubleshooting flow already knows the needed shape.</action>
  <verify>node -e "const fs=require('fs'); const text=fs.readFileSync('docs/troubleshooting.md','utf8'); if (text.includes('/bgsd-rollback-info') || text.includes('/bgsd-context-budget')) process.exit(1);"</verify>
  <done>- `docs/troubleshooting.md` stops surfacing the cited legacy commands
- Replacement guidance uses the current canonical family routes
- Users following the troubleshooting steps no longer need to guess the right command path</done>
</task>

<task type="auto">
  <name>Task 3: Align both narrative docs with the post-Plan-09 canonical command surface</name>
  <files>docs/planning-system.md, docs/troubleshooting.md</files>
  <action>Read the updated docs together and tighten any remaining phrasing that still makes the old command names or incomplete routed forms look runnable. Keep the guidance concise, canonical-only, and concrete where the flow shape is known so the next plan can lock the exact shipped files with one full-file regression pass.</action>
  <verify>node -e "const fs=require('fs'); const planning=fs.readFileSync('docs/planning-system.md','utf8'); const troubleshooting=fs.readFileSync('docs/troubleshooting.md','utf8'); if (planning.includes('/bgsd-context-budget compare')) process.exit(1); if (troubleshooting.includes('/bgsd-rollback-info') || troubleshooting.includes('/bgsd-context-budget')) process.exit(1);"</verify>
  <done>- Both narrative docs are internally consistent with the current canonical command surface
- No cited legacy command remains in runnable guidance across these files
- The docs are ready for one follow-on full-file regression proof plan</done>
</task>

</tasks>

<verification>
1. `node -e "const fs=require('fs'); const text=fs.readFileSync('docs/planning-system.md','utf8'); if (text.includes('/bgsd-context-budget compare')) process.exit(1);"`
2. `node -e "const fs=require('fs'); const text=fs.readFileSync('docs/troubleshooting.md','utf8'); if (text.includes('/bgsd-rollback-info') || text.includes('/bgsd-context-budget')) process.exit(1);"`
3. `node -e "const fs=require('fs'); const planning=fs.readFileSync('docs/planning-system.md','utf8'); const troubleshooting=fs.readFileSync('docs/troubleshooting.md','utf8'); if (planning.includes('/bgsd-context-budget compare')) process.exit(1); if (troubleshooting.includes('/bgsd-rollback-info') || troubleshooting.includes('/bgsd-context-budget')) process.exit(1);"`
</verification>

<success_criteria>
- `docs/planning-system.md` and `docs/troubleshooting.md` stop surfacing the exact legacy commands cited in verification
- Replacement guidance uses current canonical command routes with concrete runnable shape where known
- The two narrative docs are ready for a separate full-file regression lock without reopening earlier help or validator-core work
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-10-SUMMARY.md`
</output>
