---
phase: 159-help-surface-command-integrity
plan: 05
type: tdd
wave: 2
depends_on: [159-02]
files_modified:
  - agents/bgsd-phase-researcher.md
  - agents/bgsd-planner.md
  - agents/bgsd-plan-checker.md
  - agents/bgsd-roadmapper.md
  - agents/bgsd-verifier.md
  - agents/bgsd-codebase-mapper.md
  - skills/continuation-format/SKILL.md
  - skills/model-profiles/SKILL.md
  - skills/raci/SKILL.md
  - tests/guidance-command-integrity-agent-skill-surfaces.test.cjs
autonomous: true
requirements: [CMD-05, CMD-06]
must_haves:
  truths:
    - Agent prompts and shared skills surface canonical commands only.
    - Examples on supporting guidance surfaces include the required arguments or flags for the intended flow instead of vague placeholders when the workflow knows the value shape.
    - Focused regressions and the shared validator catch stale aliases across agent and skill surfaces.
  artifacts:
    - "path: "agents/bgsd-planner.md"
    - "path: "skills/continuation-format/SKILL.md"
    - "path: "tests/guidance-command-integrity-agent-skill-surfaces.test.cjs"
  key_links:
    - "from: "agents/bgsd-phase-researcher.md"
    - "from: "skills/continuation-format/SKILL.md"
    - "from: "tests/guidance-command-integrity-agent-skill-surfaces.test.cjs"
---

<objective>
Canonicalize supporting agent and skill surfaces so planners, researchers, and continuation guidance all teach the same canonical executable command examples users see elsewhere.

Purpose: DO-119 still leaks trust if agent prompts or shared skills keep stale aliases or omit required arguments after the main help, docs, and workflows are fixed.
Output: Canonicalized agent/skill guidance plus focused regression coverage for those supporting user-facing artifacts.
</objective>

> **TDD Decision:** Selected — These supporting surfaces emit deterministic command guidance that should be locked with targeted regression tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/159-help-surface-command-integrity/159-CONTEXT.md
@.planning/phases/159-help-surface-command-integrity/159-RESEARCH.md
@agents/bgsd-planner.md
@skills/continuation-format/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize supporting agent prompt surfaces</name>
  <files>agents/bgsd-phase-researcher.md, agents/bgsd-planner.md, agents/bgsd-plan-checker.md, agents/bgsd-roadmapper.md, agents/bgsd-verifier.md, agents/bgsd-codebase-mapper.md</files>
  <action>Run the Phase 159 canonical-only and parameter-correct guidance sweep across the agent prompts cited in research. Replace stale planning aliases and incomplete command examples with canonical commands and filled required arguments or flags when the flow knows them. Keep this limited to surfaced guidance; do not redesign prompt responsibilities or remove compatibility command implementations.</action>
  <verify>npm run test:file -- tests/guidance-command-integrity-agent-skill-surfaces.test.cjs</verify>
  <done>- Agent prompts no longer surface legacy commands as normal guidance
- Examples include required phase numbers or flags when the intended flow is known
- Changes stay guidance-only and do not alter command routing behavior</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize shared skill examples and lock these surfaces</name>
  <files>skills/continuation-format/SKILL.md, skills/model-profiles/SKILL.md, skills/raci/SKILL.md, tests/guidance-command-integrity-agent-skill-surfaces.test.cjs</files>
  <action>Update the shared skills cited in research so their examples stay canonical-only and parameter-correct, then add focused regressions covering the touched agent and skill files. The regression should also run the shared validator against these surfaces so Phase 159 catches both string drift and semantic next-step mistakes.</action>
  <verify>node bin/bgsd-tools.cjs util:validate-commands --raw</verify>
  <done>- Shared skills no longer surface stale aliases or missing required parameters in known flows
- Focused regressions fail if touched agent or skill surfaces revert to legacy aliases or missing parameters
- Validator-backed checks prove these surfaced next steps are executable as written</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/guidance-command-integrity-agent-skill-surfaces.test.cjs`
2. `node bin/bgsd-tools.cjs util:validate-commands --raw`
3. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/159-help-surface-command-integrity/159-05-PLAN.md`
</verification>

<success_criteria>
- Agent prompts and shared skills are canonical-only on touched surfaces
- Known flows include executable canonical commands with required parameters and flags
- Focused regressions plus the shared validator keep these supporting surfaces trustworthy
</success_criteria>

<output>
After completion, create `.planning/phases/159-help-surface-command-integrity/159-05-SUMMARY.md`
</output>
