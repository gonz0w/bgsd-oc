---
phase: 155-jj-execution-gate-workspace-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/config.js
  - src/lib/context.js
  - src/commands/init.js
  - src/lib/jj.js
  - tests/init.test.cjs
  - tests/agent.test.cjs
autonomous: true
requirements:
  - JJ-01
  - JJ-02
intent:
  outcome_ids:
    - DO-118

must_haves:
  truths:
    - "Execution starts and execution-oriented init surfaces fail in Git-only repos with a JJ-required message that points users to `jj git init`."
    - "Read-only planning, roadmap, and help flows remain usable outside JJ-backed repos."
    - "`init:execute-phase` exposes `workspace_*` execution metadata instead of `worktree_*` fields so downstream execution stops teaching the legacy backend."
    - "Legacy `.planning/config.json.worktree` config is rejected explicitly instead of being silently mapped or tolerated."
  artifacts:
    - path: "src/lib/jj.js"
      provides: "Shared JJ capability detection and setup guidance"
      contains: "JJ prerequisite classification"
    - path: "src/commands/init.js"
      provides: "JJ-gated execution init payload with workspace-first fields"
      contains: "workspace_config"
    - path: "src/lib/config.js"
      provides: "Central rejection of legacy worktree config"
      contains: "worktree"
  key_links:
    - from: "src/commands/init.js"
      to: "src/lib/jj.js"
      via: "Shared execution prerequisite gate"
      pattern: "jj.*repo|git init"
    - from: "src/commands/init.js"
      to: "src/lib/config.js"
      via: "Workspace-first config loading and legacy worktree rejection"
      pattern: "workspace_|worktree"
    - from: "src/lib/context.js"
      to: "src/commands/init.js"
      via: "Scoped context exclusions updated to the workspace-first init contract"
      pattern: "workspace_"
---

<objective>
Add one shared JJ prerequisite gate for execution and execution-oriented init entrypoints, then flip the execution init contract to workspace-first metadata while explicitly rejecting legacy worktree config.

Purpose: Phase 155 starts with the contract boundary. Users should learn about the JJ prerequisite before execution prep succeeds, and downstream execution context should stop exposing the old worktree model.
Output: Shared JJ capability helper, JJ-gated execution init payload, workspace-first init/context fields, and regression coverage for gate/config behavior.
</objective>

> **TDD Decision:** Selected - This plan changes observable execution gating and config-validation behavior with clear pass/fail outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-CONTEXT.md
@.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-RESEARCH.md
@src/lib/config.js
@src/lib/context.js
@src/commands/init.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the shared JJ prerequisite gate</name>
  <files>src/lib/jj.js, src/commands/init.js</files>
  <action>Create a single helper that classifies `jj missing`, `git-only repo`, and `jj-backed repo` using authoritative JJ CLI behavior rather than filesystem sniffing. Apply it to `init:execute-phase`, `init:quick`, and any equivalent execution bootstrap path that prepares immediate execution state, while leaving planning/help/read-only flows outside the gate. Reuse JJ's own supported setup guidance shape and make the user-facing failure clearly point to `jj git init` for Git-only repos.</action>
  <verify>npm run test:file -- tests/init.test.cjs</verify>
  <done>- Git-only temp repos fail fast on execution-oriented init with JJ-required guidance
- JJ-backed repos still receive init payloads successfully
- Read-only planning/help flows are not routed through the new gate</done>
</task>

<task type="auto">
  <name>Task 2: Replace execution init fields and reject legacy worktree config</name>
  <files>src/lib/config.js, src/lib/context.js, src/commands/init.js</files>
  <action>Replace the execution init contract's `worktree_*` fields with a workspace-first shape (`workspace_config`, `workspace_active`, and related execution metadata). Choose a JJ-first `workspace` config block as the supported replacement shape, preserve only the settings Phase 155 actually needs, and fail centrally when legacy `worktree` config is present instead of silently mapping it. Update scoped-context exclusions so downstream consumers stop seeing `worktree_*` keys.</action>
  <verify>npm run test:file -- tests/init.test.cjs tests/agent.test.cjs</verify>
  <done>- `init:execute-phase` no longer emits `worktree_enabled`, `worktree_config`, or `worktree_active`
- Workspace-first fields are present when execution init succeeds
- Loading config with a `worktree` section fails with an explicit migration error</done>
</task>

<task type="auto">
  <name>Task 3: Lock regression coverage for the new gate contract</name>
  <files>tests/init.test.cjs, tests/agent.test.cjs</files>
  <action>Add focused tests that prove the shared gate blocks Git-only execution prep, allows JJ-backed execution prep, preserves non-execution surfaces, and keeps context shaping aligned with the new `workspace_*` payload. Port or replace any init assertions that still mention `worktree_*` so later Phase 155 work can rely on the new contract.</action>
  <verify>npm run test:file -- tests/init.test.cjs tests/agent.test.cjs</verify>
  <done>- Init tests cover Git-only failure, JJ-backed success, and read-only exemptions
- Agent/context tests stop expecting `worktree_*` execution payload fields
- The new gate contract is documented by executable regression coverage</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/init.test.cjs tests/agent.test.cjs`
2. In a temp Git-only repo, `node bin/bgsd-tools.cjs init:execute-phase 155` fails with a JJ-required message that includes `jj git init`
3. In a temp JJ-backed repo, `node bin/bgsd-tools.cjs init:execute-phase 155` returns `workspace_*` execution metadata and no `worktree_*` keys
</verification>

<success_criteria>
- One shared helper owns JJ execution readiness detection and setup guidance
- Execution-oriented init surfaces fail fast outside JJ-backed repos while read-only flows remain available
- Execution init/context payloads are workspace-first and legacy worktree config is explicitly unsupported
</success_criteria>

<output>
After completion, create `.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-01-SUMMARY.md`
</output>
