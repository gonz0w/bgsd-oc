---
phase: 155-jj-execution-gate-workspace-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 155-01
files_modified:
  - src/commands/workspace.js
  - src/router.js
  - src/lib/constants.js
  - src/lib/command-help.js
  - src/lib/commandDiscovery.js
  - workflows/execute-phase.md
  - tests/workspace.test.cjs
  - tests/worktree.test.cjs
autonomous: true
requirements:
  - JJ-02
  - JJ-01
intent:
  outcome_ids:
    - DO-118

must_haves:
  truths:
    - "Users manage execution isolation through a top-level `workspace` command family instead of `execute:worktree`."
    - "`workspace add`, `workspace list`, `workspace forget`, and `workspace cleanup` wrap native JJ workspace behavior with predictable phase/plan-derived names and paths."
    - "`workspace cleanup` both forgets tracked JJ workspaces and removes their directories from disk, so cleanup does not leave orphaned folders behind."
    - "Execution help, discovery, and workflow text describe JJ-first workspace execution and no longer advertise Git worktree fallback commands."
    - "Workspace command coverage runs against real JJ temp repos rather than Git worktree fixtures."
  artifacts:
    - path: "src/commands/workspace.js"
      provides: "JJ-native workspace lifecycle wrapper"
      contains: "workspace add"
    - path: "src/router.js"
      provides: "Top-level workspace command routing"
      contains: "workspace"
    - path: "workflows/execute-phase.md"
      provides: "JJ-first workspace execution guidance"
      contains: "workspace"
    - path: "tests/workspace.test.cjs"
      provides: "Real JJ workspace lifecycle regression coverage"
      contains: "jj workspace"
  key_links:
    - from: "src/router.js"
      to: "src/commands/workspace.js"
      via: "Top-level workspace family dispatch"
      pattern: "workspace"
    - from: "workflows/execute-phase.md"
      to: "src/commands/workspace.js"
      via: "JJ-first command examples and lifecycle wording"
      pattern: "workspace add|workspace list|workspace cleanup"
    - from: "tests/workspace.test.cjs"
      to: "src/commands/workspace.js"
      via: "Real JJ workspace add/list/forget/cleanup/reconcile coverage"
      pattern: "workspace"
---

<objective>
Replace the legacy worktree command surface with a top-level JJ-native `workspace` family, then rewire help, discovery, workflow guidance, and regression coverage so Phase 155 visibly ships a JJ-first execution path.

Purpose: The gate from Plan 01 only matters if every user-facing execution surface teaches the new backend. This plan makes the command reset obvious, removes the old vocabulary, and proves the lifecycle against real JJ repos.
Output: Workspace command wrapper, top-level router/help/discovery updates, JJ-first execute workflow guidance, and workspace-focused test coverage.
</objective>

> **TDD Decision:** Selected - This plan replaces user-facing command behavior and lifecycle output with a testable JJ workspace contract.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-CONTEXT.md
@.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-RESEARCH.md
@.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-01-SUMMARY.md
@src/commands/worktree.js
@src/router.js
@src/lib/constants.js
@src/lib/command-help.js
@src/lib/commandDiscovery.js
@workflows/execute-phase.md
@tests/worktree.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the JJ-native workspace command family</name>
  <files>src/commands/workspace.js, src/router.js</files>
  <action>Replace the Git worktree command module with a JJ-native top-level `workspace` family. Implement the Phase 155 lifecycle surface explicitly as `workspace add`, `workspace list`, `workspace forget`, `workspace cleanup`, and a narrow `workspace reconcile` wrapper. Use deterministic phase/plan-derived workspace names under a JJ-focused base path, wrap native `jj workspace` commands instead of custom copy logic, and keep reconcile intentionally minimal so stale-workspace recovery and op-log UX remain deferred to Phase 156.</action>
  <verify>npm run test:file -- tests/workspace.test.cjs</verify>
  <done>- `execute:worktree` is gone from routing and replaced by top-level `workspace`
- Workspace creation/list/forget use JJ-native commands with deterministic naming
- Cleanup both forgets workspaces and removes their directories from disk</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite execution surfaces to be visibly JJ-first</name>
  <files>src/lib/constants.js, src/lib/command-help.js, src/lib/commandDiscovery.js, workflows/execute-phase.md</files>
  <action>Audit and replace remaining user-facing `execute:worktree` and worktree wording across help text, command discovery, usage strings, and execution workflow guidance. Update the workflow to teach JJ-first workspace parallelism, remove any user-facing Git worktree fallback messaging, and keep the scope narrow by explicitly deferring stale-workspace recovery, op-log diagnostics, and planner workspace-awareness to later phases.</action>
  <verify>npm run test:file -- tests/workspace.test.cjs tests/init.test.cjs</verify>
  <done>- Help and discovery surfaces advertise `workspace` as the supported execution-isolation path
- `workflows/execute-phase.md` no longer tells users to run `execute:worktree ...`
- User-facing execution docs consistently describe Phase 155 as JJ-first, not hybrid</done>
</task>

<task type="auto">
  <name>Task 3: Port worktree coverage to real JJ workspace tests</name>
  <files>tests/workspace.test.cjs, tests/worktree.test.cjs</files>
  <action>Replace the legacy Git worktree test suite with JJ workspace integration coverage built on real temp repos and real `jj workspace` commands. Preserve the useful lifecycle assertions from the old suite where they still apply, add explicit proof that `workspace cleanup` removes directories after `jj workspace forget`, and remove obsolete worktree-only expectations so the shipped test corpus reflects the new contract instead of the old backend.</action>
  <verify>npm run test:file -- tests/workspace.test.cjs</verify>
  <done>- Workspace tests cover add, list, forget, cleanup, and the first-pass reconcile path in JJ-backed temp repos
- Cleanup tests prove both JJ metadata removal and filesystem deletion
- No legacy worktree-specific test file remains as the authoritative execution backend suite</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/workspace.test.cjs tests/init.test.cjs`
2. `npm run build`
3. `node bin/bgsd-tools.cjs workspace list` succeeds in a JJ-backed temp repo and `node bin/bgsd-tools.cjs execute:worktree list` fails as unsupported/unknown
</verification>

<success_criteria>
- The supported execution-isolation surface is a top-level `workspace` command family, not `execute:worktree`
- Help, discovery, and workflow text are JJ-first and no longer teach the legacy backend
- Regression coverage validates the JJ workspace lifecycle with real JJ repos, including cleanup removing directories after forget
</success_criteria>

<output>
After completion, create `.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-02-SUMMARY.md`
</output>
