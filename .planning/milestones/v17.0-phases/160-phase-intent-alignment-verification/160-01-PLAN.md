---
phase: 160-phase-intent-alignment-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/discuss-phase.md
  - templates/CONTEXT.md
  - src/lib/phase-context.js
  - src/commands/intent.js
  - tests/intent.test.cjs
autonomous: true
requirements: [INT-02]
must_haves:
  truths:
    - "Fresh phase CONTEXT artifacts include an explicit phase-intent block with local purpose, expected user change, and non-goals."
    - "The expected user change is stored as an observable before/after-style claim rather than inferred from generic discussion prose."
    - "effective_intent reads the explicit phase-intent block when present and leaves legacy phases visibly partial instead of guessing intent details."
  artifacts:
    - path: "workflows/discuss-phase.md"
      provides: "Context-writing instructions that emit the explicit phase-intent block"
    - path: "templates/CONTEXT.md"
      provides: "Phase context template showing the explicit phase-intent contract"
    - path: "src/lib/phase-context.js"
      provides: "Parser for explicit phase-intent fields in CONTEXT artifacts"
    - path: "tests/intent.test.cjs"
      provides: "Regression coverage for explicit phase-intent parsing and fallback semantics"
  key_links:
    - from: "workflows/discuss-phase.md"
      to: "templates/CONTEXT.md"
      via: "shared phase-intent block shape"
    - from: "src/lib/phase-context.js"
      to: "src/commands/intent.js"
      via: "parsed phase intent feeding effective_intent"
---

<objective>
Lock the new phase-intent contract into phase context authoring and parsing so downstream planning/alignment flows read explicit local purpose instead of inferring it from loose prose.

Purpose: INT-02 depends on a durable, lightweight context shape that humans can write and the runtime can parse without creating a new top-level artifact.
Output: Updated context-authoring guidance, explicit phase-intent parsing, and focused regression coverage.
</objective>

> **TDD Decision:** Selected — this plan changes a deterministic parsing contract with clear expected outputs and fallback behavior, so tests should define the new intent shape before implementation settles.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-phase-intent-alignment-verification/160-CONTEXT.md
@.planning/phases/157-planning-context-cascade/157-01-SUMMARY.md
@workflows/discuss-phase.md
@templates/CONTEXT.md
@src/lib/phase-context.js
@src/commands/intent.js
@tests/intent.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the explicit phase-intent block to context-authoring surfaces</name>
  <files>workflows/discuss-phase.md, templates/CONTEXT.md</files>
  <action>Update the discuss workflow and shared CONTEXT template so newly written phase context artifacts include one lightweight intent block with exactly the three locked fields: Local Purpose, Expected User Change, and Non-Goals. Make the workflow guidance require observable before/after wording for expected user change plus 1-3 concrete examples for expected user change and non-goals, while keeping the block embedded in CONTEXT.md rather than introducing a separate file.</action>
  <verify>rg -n "Local Purpose|Expected User Change|Non-Goals" workflows/discuss-phase.md templates/CONTEXT.md</verify>
  <done>Both authoring surfaces teach the same explicit phase-intent block and the instructions match the locked wording constraints from 160-CONTEXT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Parse the explicit phase-intent contract in runtime intent helpers</name>
  <files>src/lib/phase-context.js, src/commands/intent.js</files>
  <action>Replace the current heuristic phase-intent extraction with parsing that prefers the explicit phase-intent block and surfaces its three fields directly. Keep effective_intent advisory, preserve the project north star, and make missing explicit phase-intent data on legacy phases stay visible as partial/unavailable instead of guessed from domain/specifics/deferred prose.</action>
  <verify>node --test tests/intent.test.cjs --test-name-pattern "phase-local intent"</verify>
  <done>The runtime exposes explicit phase intent when the new block exists and emits the intended partial/missing-layer behavior when it does not.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused regression coverage for explicit phase-intent parsing and fallback</name>
  <files>tests/intent.test.cjs</files>
  <action>Add or rewrite intent tests so they lock the explicit three-field contract, verify that expected user change stays observable and concrete, and prove legacy contexts without the new block do not receive guessed phase intent.</action>
  <verify>node --test tests/intent.test.cjs --test-name-pattern "effective_intent|phase-local intent|partial intent context"</verify>
  <done>The intent test suite directly covers the new explicit phase-intent block plus the no-guess fallback path for legacy contexts.</done>
</task>

</tasks>

<verification>
node --test tests/intent.test.cjs
</verification>

<success_criteria>
- New phase context guidance emits an explicit Local Purpose / Expected User Change / Non-Goals block.
- `effective_intent.phase` is sourced from that explicit block instead of inferred bullets.
- Legacy phases without the new block remain clearly partial/unavailable rather than receiving guessed intent data.
</success_criteria>

<output>
After completion, create `.planning/phases/160-phase-intent-alignment-verification/160-01-SUMMARY.md`
</output>
