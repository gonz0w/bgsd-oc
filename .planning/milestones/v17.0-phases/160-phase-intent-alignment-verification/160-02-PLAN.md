---
phase: 160-phase-intent-alignment-verification
plan: 02
type: execute
wave: 2
depends_on: ["160-01"]
files_modified:
  - agents/bgsd-verifier.md
  - workflows/execute-phase.md
  - workflows/verify-work.md
  - templates/verification-report.md
  - templates/UAT.md
  - tests/guidance-intent-alignment.test.cjs
autonomous: true
requirements: [INT-06]
must_haves:
  truths:
    - "Verification and UAT guidance treat intent alignment as a separate judgment from requirement coverage for the active phase."
    - "Intent alignment uses the locked verdict ladder: aligned, partial, misaligned, with core expected-user-change misses forced to misaligned."
    - "Legacy phases without the explicit phase-intent block are reported as not assessed/unavailable with a plain reason instead of a guessed verdict."
    - "Review outputs surface intent alignment before or alongside requirement coverage so the first scan answers whether the phase delivered its promised user-facing change."
  artifacts:
    - path: "agents/bgsd-verifier.md"
      provides: "Verifier instructions for intent-alignment judgment"
    - path: "templates/verification-report.md"
      provides: "Verification report contract with intent-alignment reporting"
    - path: "workflows/verify-work.md"
      provides: "UAT workflow instructions for intent-aware testing and reporting"
    - path: "templates/UAT.md"
      provides: "UAT artifact contract with separate intent-alignment reporting"
    - path: "tests/guidance-intent-alignment.test.cjs"
      provides: "Regression coverage locking the new guidance and ordering contract"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "agents/bgsd-verifier.md"
      via: "phase verification handoff requirements"
    - from: "agents/bgsd-verifier.md"
      to: "templates/verification-report.md"
      via: "shared intent-alignment verdict contract"
    - from: "workflows/verify-work.md"
      to: "templates/UAT.md"
      via: "UAT intent-alignment section and fallback wording"
---

<objective>
Make verification and UAT report whether a phase delivered its claimed user-facing change, not only whether requirement-linked work shipped.

Purpose: INT-06 is the product-value half of this milestone — reviewers must see intent alignment as a first-class outcome, with explicit verdicts and legacy-phase fallback rules.
Output: Updated verifier/UAT guidance, report templates, and focused regression coverage for the new intent-alignment contract.
</objective>

> **TDD Decision:** Skipped — this plan is primarily workflow, prompt, and template contract work; it should still add focused regressions, but the change is guidance-first rather than implementation logic that benefits from a red-green execution loop.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-phase-intent-alignment-verification/160-CONTEXT.md
@workflows/execute-phase.md
@workflows/verify-work.md
@agents/bgsd-verifier.md
@templates/verification-report.md
@templates/UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the intent-alignment verdict contract to verifier-facing surfaces</name>
  <files>agents/bgsd-verifier.md, workflows/execute-phase.md, templates/verification-report.md</files>
  <action>Update verifier instructions, execute-phase handoff wording, and the verification-report template so intent alignment is reported before or alongside requirement coverage using the locked verdicts `aligned`, `partial`, `misaligned`, plus an explicit unavailable/not-assessed fallback when the phase-intent block is missing. Make the template and prompt explain the core-miss rule: if the main expected user change did not land, the verdict cannot be `partial`.</action>
  <verify>rg -n "Intent Alignment|aligned \| partial \| misaligned|not assessed|Requirement Coverage" agents/bgsd-verifier.md workflows/execute-phase.md templates/verification-report.md</verify>
  <done>Verifier-facing surfaces share one explicit intent-alignment verdict contract and place it ahead of or adjacent to requirement coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Make UAT guidance and artifacts report intent alignment separately</name>
  <files>workflows/verify-work.md, templates/UAT.md</files>
  <action>Update verify-work and the UAT template so UAT can derive intent-aware checks from the active phase intent, report an intent-alignment verdict in plain language, and record the explicit unavailable/not-assessed fallback for legacy phases. Keep the output readable to humans and separate it from requirement coverage instead of burying intent in generic summary prose.</action>
  <verify>rg -n "Intent Alignment|not assessed|Requirement Coverage|expected user change" workflows/verify-work.md templates/UAT.md</verify>
  <done>UAT guidance and the persisted UAT artifact both expose a separate intent-alignment judgment with the locked fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the new intent-alignment guidance contract with focused regressions</name>
  <files>tests/guidance-intent-alignment.test.cjs</files>
  <action>Create focused direct-file regression coverage that proves the touched verifier/UAT surfaces use the correct verdict names, preserve the core-miss rule, place intent alignment before or alongside requirement coverage, and keep legacy fallback wording explicit instead of guessed.</action>
  <verify>node --test tests/guidance-intent-alignment.test.cjs</verify>
  <done>A dedicated regression test file guards the new intent-alignment wording, ordering, and legacy fallback contract across the touched surfaces.</done>
</task>

</tasks>

<verification>
node --test tests/guidance-intent-alignment.test.cjs
</verification>

<success_criteria>
- VERIFICATION.md guidance includes a separate intent-alignment verdict with aligned/partial/misaligned plus explicit unavailable fallback.
- UAT guidance and template report intent alignment separately from requirement coverage.
- Touched verifier/UAT surfaces are locked by focused regressions that enforce verdict wording and ordering.
</success_criteria>

<output>
After completion, create `.planning/phases/160-phase-intent-alignment-verification/160-02-SUMMARY.md`
</output>
