---
phase: 162-intent-runtime-parity-legacy-context-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/phase-context.js
  - src/commands/intent.js
  - bin/bgsd-tools.cjs
autonomous: true
requirements: [INT-02, INT-06]
must_haves:
  truths:
    - "Legacy phase contexts without an explicit `Phase Intent` block resolve to no phase-local intent instead of guessed intent data."
    - "Source helpers and the rebuilt shipped CLI share the same explicit legacy fallback contract, so rebuilds cannot silently change intent-alignment behavior."
    - "Runtime-facing intent consumers keep legacy verification and UAT on the existing `not assessed` / unavailable path by preserving absent phase intent for real legacy contexts such as Phase 160."
  artifacts:
    - path: "src/lib/phase-context.js"
      provides: "Authoritative legacy phase-intent fallback parsing behavior"
    - path: "src/commands/intent.js"
      provides: "Runtime effective_intent consumer that reflects the shared fallback contract"
    - path: "bin/bgsd-tools.cjs"
      provides: "Rebuilt shipped CLI bundle carrying the same fallback behavior as source"
  key_links:
    - from: "src/lib/phase-context.js"
      to: "src/commands/intent.js"
      via: "shared no-guess phase-intent fallback feeding effective_intent"
    - from: "src/lib/phase-context.js"
      to: "bin/bgsd-tools.cjs"
      via: "rebuilt bundle preserves the same legacy fallback semantics"
intent:
  outcome_ids: [DO-120]
  must_haves:
    truths:
      - Legacy phase contexts without an explicit `Phase Intent` block resolve to no phase-local intent instead of guessed intent data.
      - Source helpers and the rebuilt shipped CLI share the same explicit legacy fallback contract, so rebuilds cannot silently change intent-alignment behavior.
      - Runtime-facing intent consumers keep legacy verification and UAT on the existing `not assessed` / unavailable path by preserving absent phase intent for real legacy contexts such as Phase 160.
    artifacts:
      - src/lib/phase-context.js
      - src/commands/intent.js
      - bin/bgsd-tools.cjs
    key_links:
      - Source parsing and runtime effective_intent consumption use one fallback authority.
      - The built CLI is refreshed in the same slice so shipped behavior cannot lag behind source fixes.
---

<objective>
Make legacy phase-intent fallback behavior come from one explicit source contract and carry that exact contract into the shipped CLI bundle.

Purpose: Phase 160 intentionally chose a no-guess fallback for old CONTEXT artifacts, but the milestone audit showed that source helpers and rebuilt runtime behavior can drift unless the fallback is treated as one shared contract and the bundle is refreshed in the same slice.
Output: Unified legacy phase-intent fallback behavior in source, any required runtime-consumer alignment, and a rebuilt `bin/bgsd-tools.cjs` that matches the source contract.
</objective>

> **TDD Decision:** Selected - this plan changes deterministic parsing and runtime output behavior with clear legacy-versus-explicit expectations, so the safest implementation path is to lock the fallback contract while changing the source of truth.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/MILESTONE-INTENT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v17.0-MILESTONE-AUDIT.md
@.planning/phases/160-phase-intent-alignment-verification/160-CONTEXT.md
@.planning/phases/160-phase-intent-alignment-verification/160-VERIFICATION.md
@.planning/phases/162-intent-runtime-parity-legacy-context-stability/162-CONTEXT.md
@src/lib/phase-context.js
@src/commands/intent.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the explicit phase-intent block the only legacy fallback authority</name>
  <files>src/lib/phase-context.js, src/commands/intent.js</files>
  <action>Review the current phase-intent parsing and effective-intent consumption path, then tighten it so phase-local intent is only produced from the explicit `&lt;phase_intent&gt;` / `## Phase Intent` contract. Preserve the locked Phase 160 behavior for legacy contexts: if the explicit block is missing, return no phase-local intent and keep the consumer-visible fallback reason plain instead of inferring purpose from domain, specifics, or deferred prose.</action>
  <verify>node bin/bgsd-tools.cjs plan:intent show effective 160</verify>
  <done>The source helpers and runtime intent consumer both treat the real legacy Phase 160 context as missing phase-local intent rather than guessed intent data.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the shipped CLI immediately after the fallback change</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>Rebuild `bin/bgsd-tools.cjs` from the updated source in the same plan slice so the shipped runtime picks up the exact same legacy fallback contract without waiting for a later cleanup pass.</action>
  <verify>npm run build</verify>
  <done>The bundled CLI reflects the updated source behavior and no longer risks stale legacy fallback semantics after rebuild-sensitive edits.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-test the rebuilt runtime on the real legacy verification path</name>
  <files>src/lib/phase-context.js, src/commands/intent.js, bin/bgsd-tools.cjs</files>
  <action>Run one runtime-facing spot check against the real Phase 160 legacy context so the rebuilt CLI proves it still leaves intent alignment unavailable / not assessed when the explicit phase-intent block is missing.</action>
  <verify>node bin/bgsd-tools.cjs init:verify-work 160 --verbose</verify>
  <done>The shipped runtime shows the same absent-phase-intent fallback as the source helpers for the audit-cited legacy path.</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `node bin/bgsd-tools.cjs plan:intent show effective 160`
3. `node bin/bgsd-tools.cjs init:verify-work 160 --verbose`
4. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/162-intent-runtime-parity-legacy-context-stability/162-01-PLAN.md`
</verification>

<success_criteria>
- Legacy phase contexts without an explicit phase-intent block produce no guessed phase-local intent in source helpers.
- The rebuilt `bin/bgsd-tools.cjs` carries the same fallback contract as source.
- Runtime-facing intent consumers continue to keep legacy verification and UAT on the `not assessed` / unavailable path for missing explicit phase intent.
</success_criteria>

<output>
After completion, create `.planning/phases/162-intent-runtime-parity-legacy-context-stability/162-01-SUMMARY.md`
</output>
