---
phase: 162-intent-runtime-parity-legacy-context-stability
plan: 02
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - tests/intent.test.cjs
  - tests/init.test.cjs
  - tests/contracts.test.cjs
autonomous: true
requirements: [INT-02, INT-06]
must_haves:
  truths:
    - "Regression coverage proves the real legacy Phase 160 context stays phase-intent-free in both source parsing and shipped runtime paths."
    - "Parity proof fails if `plan:intent show effective 160` or `init:verify-work 160` changes after a rebuild and starts guessing phase-local intent from legacy prose."
    - "The Phase 162 guardrail is surgical: it protects legacy fallback parity without reopening the broader Phase 160 intent-verdict contract."
  artifacts:
    - path: "tests/intent.test.cjs"
      provides: "Source-level regression coverage for explicit-vs-legacy phase-intent parsing using the real legacy artifact"
    - path: "tests/init.test.cjs"
      provides: "Runtime init coverage for legacy effective_intent fallback on Phase 160"
    - path: "tests/contracts.test.cjs"
      provides: "Contract-level proof that additive init output keeps phase intent absent for legacy contexts"
  key_links:
    - from: "tests/intent.test.cjs"
      to: ".planning/phases/160-phase-intent-alignment-verification/160-CONTEXT.md"
      via: "real legacy context fixture for no-guess fallback proof"
    - from: "tests/init.test.cjs"
      to: "bin/bgsd-tools.cjs"
      via: "rebuilt runtime check for legacy verify-work parity"
intent:
  outcome_ids: [DO-120]
  must_haves:
    truths:
      - Regression coverage proves the real legacy Phase 160 context stays phase-intent-free in both source parsing and shipped runtime paths.
      - Parity proof fails if `plan:intent show effective 160` or `init:verify-work 160` changes after a rebuild and starts guessing phase-local intent from legacy prose.
      - The Phase 162 guardrail is surgical: it protects legacy fallback parity without reopening the broader Phase 160 intent-verdict contract.
    artifacts:
      - tests/intent.test.cjs
      - tests/init.test.cjs
      - tests/contracts.test.cjs
    key_links:
      - Source parsing and init output are checked against the same real legacy context.
      - Rebuild-sensitive runtime parity is guarded by direct tests instead of manual audit rechecks alone.
---

<objective>
Lock the legacy fallback parity path with direct regressions that compare source behavior and rebuilt runtime behavior against the real Phase 160 legacy context.

Purpose: Phase 162 is only complete when future edits cannot silently reintroduce source-vs-runtime drift on legacy phase contexts, so the repo needs focused proof over the exact audit-cited path rather than source-only parser tests.
Output: Focused source, init, and contract regressions that fail if the real legacy context ever starts producing guessed phase intent or inconsistent runtime results.
</objective>

> **TDD Decision:** Selected - this plan is pure regression-proof work around deterministic legacy fallback outputs, so the tests are the product and should drive the final guardrail shape.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v17.0-MILESTONE-AUDIT.md
@.planning/phases/160-phase-intent-alignment-verification/160-CONTEXT.md
@.planning/phases/160-phase-intent-alignment-verification/160-VERIFICATION.md
@.planning/phases/162-intent-runtime-parity-legacy-context-stability/162-CONTEXT.md
@src/lib/phase-context.js
@src/commands/intent.js
@src/commands/init.js
@tests/intent.test.cjs
@tests/init.test.cjs
@tests/contracts.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source-level proof against the real legacy Phase 160 context</name>
  <files>tests/intent.test.cjs</files>
  <action>Add focused intent tests that read the real `.planning/phases/160-phase-intent-alignment-verification/160-CONTEXT.md` artifact and prove the parser plus `effective_intent` still return no phase-local intent when the explicit block is absent. Keep the assertions tied to the locked no-guess fallback rather than broader wording churn.</action>
  <verify>node --test tests/intent.test.cjs --test-name-pattern "legacy|phase-local intent|effective_intent"</verify>
  <done>Source-side regressions fail if the real Phase 160 legacy context ever starts exposing guessed phase intent.</done>
</task>

<task type="auto">
  <name>Task 2: Add rebuilt-runtime parity coverage for verify-work on Phase 160</name>
  <files>tests/init.test.cjs, tests/contracts.test.cjs</files>
  <action>Extend init and contract coverage so the rebuilt CLI path for `init:verify-work 160` keeps `effective_intent.phase` absent, reports partial legacy context consistently, and stays aligned with the source-side expectation on the same real legacy context.</action>
  <verify>node --test tests/init.test.cjs tests/contracts.test.cjs</verify>
  <done>Init output and its contract tests fail if runtime parity drifts from the shared legacy fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Run the focused parity suite and runtime spot checks</name>
  <files>tests/intent.test.cjs, tests/init.test.cjs, tests/contracts.test.cjs</files>
  <action>Run the targeted test suite plus the two runtime commands named by the audit so Phase 162 finishes with both automated proof and readable spot-check evidence for the exact legacy fallback path.</action>
  <verify>node --test tests/intent.test.cjs tests/init.test.cjs tests/contracts.test.cjs && node bin/bgsd-tools.cjs plan:intent show effective 160 && node bin/bgsd-tools.cjs init:verify-work 160 --verbose</verify>
  <done>The focused suite and runtime spot checks all agree that legacy contexts remain `not assessed` / unavailable via absent explicit phase intent after rebuilds.</done>
</task>

</tasks>

<verification>
1. `node --test tests/intent.test.cjs tests/init.test.cjs tests/contracts.test.cjs`
2. `node bin/bgsd-tools.cjs plan:intent show effective 160`
3. `node bin/bgsd-tools.cjs init:verify-work 160 --verbose`
4. `node bin/bgsd-tools.cjs verify:verify plan-structure .planning/phases/162-intent-runtime-parity-legacy-context-stability/162-02-PLAN.md`
</verification>

<success_criteria>
- Focused tests prove the real Phase 160 legacy context still has no explicit phase-local intent in source parsing and runtime init output.
- Rebuild-sensitive parity is covered by automated regressions plus spot-check commands on the audit-cited path.
- Future edits cannot silently change legacy intent-alignment behavior without failing the focused Phase 162 proof suite.
</success_criteria>

<output>
After completion, create `.planning/phases/162-intent-runtime-parity-legacy-context-stability/162-02-SUMMARY.md`
</output>
