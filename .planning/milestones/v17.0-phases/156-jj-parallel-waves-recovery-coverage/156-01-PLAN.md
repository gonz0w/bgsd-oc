---
phase: 156-jj-parallel-waves-recovery-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/workspace.js
  - src/lib/jj.js
  - src/lib/jj-workspace.js
  - tests/helpers.cjs
  - tests/workspace.test.cjs
autonomous: true
requirements:
  - JJ-03
  - JJ-04
intent:
  outcome_ids:
    - DO-118

must_haves:
  truths:
    - "Users can inspect managed JJ workspaces and see which ones are healthy, stale, divergent, or failed without reconstructing state manually."
    - "When a workspace is stale or otherwise recoverable, bGSD shows a preview-first recovery path before any mutation."
    - "Workspace diagnostics include JJ-backed evidence, including operation-log context, so recovery guidance is auditable."
    - "Failed workspaces and recovery breadcrumbs remain available during recovery work instead of being silently cleaned up."
  artifacts:
    - path: "src/lib/jj-workspace.js"
      provides: "JJ workspace status classification, op-log diagnostics, and recovery preview helpers"
      contains: "workspace status"
    - path: "src/commands/workspace.js"
      provides: "Recovery-capable workspace inspect/reconcile/cleanup command behavior"
      contains: "recovery_preview"
    - path: "tests/workspace.test.cjs"
      provides: "Real JJ regression coverage for stale/divergent inspection and preview-first recovery"
      contains: "workspace reconcile"
  key_links:
    - from: "src/commands/workspace.js"
      to: "src/lib/jj-workspace.js"
      via: "Workspace diagnostics and recovery preview composition"
      pattern: "status|diagnostics|recovery"
    - from: "src/lib/jj-workspace.js"
      to: "src/lib/jj.js"
      via: "Shared JJ command execution and environment checks"
      pattern: "jj"
    - from: "tests/workspace.test.cjs"
      to: "src/commands/workspace.js"
      via: "Real JJ temp-repo recovery assertions"
      pattern: "workspace reconcile|workspace cleanup"
---

<objective>
Turn the top-level `workspace` family from a Phase 155 lifecycle shell into a real inspection and recovery surface with JJ-backed status classification, op-log diagnostics, and preview-first recovery behavior.

Purpose: Phase 156 starts at the command layer because execution, docs, and tests all need one authoritative workspace-state contract. If stale/divergent logic only lives in workflow prose, users still have to guess what happened.
Output: Shared JJ workspace diagnostics helper, richer `workspace` command payloads for inspect/reconcile/cleanup, and real JJ regression coverage for stale/divergent and retained-failure scenarios.
</objective>

> **TDD Decision:** Selected - This plan adds observable CLI behavior with clear status, preview, and recovery outcomes that fit focused regression tests.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-CONTEXT.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-RESEARCH.md
@.planning/phases/155-jj-execution-gate-workspace-lifecycle/155-02-SUMMARY.md
@src/commands/workspace.js
@src/lib/jj.js
@tests/workspace.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared JJ workspace diagnostics and state classification</name>
  <files>src/lib/jj-workspace.js, src/lib/jj.js, src/commands/workspace.js</files>
  <action>Create a shared helper that inspects managed JJ workspaces and classifies them as healthy, stale, divergent, missing, or otherwise actionable using authoritative JJ commands rather than filesystem guesses. Include short evidence payloads and a bounded operation-log slice that downstream surfaces can show without dumping raw JJ output. Keep the helper additive to the existing `workspace` family instead of inventing a new recovery namespace.</action>
  <verify>npm run test:file -- tests/workspace.test.cjs</verify>
  <done>- Managed workspace output includes explicit status and diagnostics fields
- Workspace classification distinguishes at least healthy vs stale/divergent/action-needed cases
- Diagnostics are JJ-backed and structured enough for execution/workflow reuse</done>
</task>

<task type="auto">
  <name>Task 2: Implement preview-first recovery and retention-aware cleanup behavior</name>
  <files>src/commands/workspace.js, src/lib/jj-workspace.js</files>
  <action>Upgrade `workspace reconcile` from validate-only output into a preview-first recovery surface that shows proposed actions, marks whether automated apply is allowed, and only mutates when the explicit recovery path is invoked through the existing command family. Refine cleanup behavior so failed or recovery-needed workspaces keep their breadcrumbs available during recovery work instead of being treated like ordinary finished workspaces.</action>
  <verify>npm run test:file -- tests/workspace.test.cjs</verify>
  <done>- Recoverable stale cases expose a preview before any mutation
- Divergent or conflict-heavy cases are reported with stronger guardrails instead of silent repair
- Cleanup behavior no longer erases recovery-needed workspace evidence prematurely</done>
</task>

<task type="auto">
  <name>Task 3: Lock real JJ regression coverage for stale, divergent, and recovery scenarios</name>
  <files>tests/helpers.cjs, tests/workspace.test.cjs</files>
  <action>Extend the real JJ temp-repo test fixtures to create stale, divergent, and failed-workspace cases, then add focused regression tests for status classification, op-log-backed diagnostics, preview-first recovery, and recovery-aware cleanup/retention. Keep tests grounded in real JJ behavior so future CLI parsing or command-contract drift shows up immediately.</action>
  <verify>npm run test:file -- tests/workspace.test.cjs</verify>
  <done>- Workspace tests cover stale/divergent inspection and recovery-preview output
- Tests prove no recovery mutation happens before the preview/apply boundary
- Tests cover retained failed-workspace breadcrumbs vs safe cleanup behavior</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/workspace.test.cjs`
2. In a JJ-backed temp repo, `node bin/bgsd-tools.cjs workspace reconcile 156-01 --json` reports status, diagnostics, and a recovery preview instead of the old validate-only placeholder
3. A simulated stale or divergent workspace case shows JJ-backed evidence and does not auto-mutate before the explicit recovery path is chosen
</verification>

<success_criteria>
- Workspace inspection and reconcile surfaces explain stale/divergent state with JJ-backed diagnostics
- Recovery is preview-first and guardrailed rather than silent auto-repair
- Failed/recovery-needed workspace state stays available long enough for users to inspect and recover it
</success_criteria>

<output>
After completion, create `.planning/phases/156-jj-parallel-waves-recovery-coverage/156-01-SUMMARY.md`
</output>
