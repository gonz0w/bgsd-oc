---
phase: 156-jj-parallel-waves-recovery-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 156-01
files_modified:
  - templates/config.json
  - templates/config-full.json
  - src/lib/constants.js
  - src/lib/config.js
  - src/lib/command-help.js
  - src/lib/commandDiscovery.js
  - tests/contracts.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - JJ-06
intent:
  outcome_ids:
    - DO-118

must_haves:
  truths:
    - "Maintainers reading shipped config templates and help surfaces see the JJ-first workspace model, not rejected worktree config."
    - "Repo-facing command/help/discovery text reflects recovery-capable workspace execution as the supported model."
    - "Regression tests catch future drift between runtime workspace behavior and published config/help/workflow guidance."
  artifacts:
    - path: "templates/config.json"
      provides: "Default JJ-first configuration example"
      contains: "workspace"
    - path: "src/lib/constants.js"
      provides: "Config schema and CLI help text aligned with workspace-first execution"
      contains: "workspace"
    - path: "tests/workflow.test.cjs"
      provides: "Coverage that docs/help/workflow wording stays aligned with runtime behavior"
      contains: "workspace"
  key_links:
    - from: "templates/config-full.json"
      to: "src/lib/config.js"
      via: "Published config shape matches runtime validation"
      pattern: "workspace"
    - from: "src/lib/constants.js"
      to: "src/lib/command-help.js"
      via: "Shared help and config wording for workspace recovery model"
      pattern: "workspace|recovery"
    - from: "tests/contracts.test.cjs"
      to: "templates/config.json"
      via: "Template/runtime alignment checks"
      pattern: "workspace"
---

<objective>
Align templates, config/help/discovery text, and regression coverage with the JJ-first workspace recovery model so repo-facing guidance matches what Phase 155 and Phase 156 actually support.

Purpose: Right now the repo ships contradictory guidance because runtime rejects `worktree` config while templates still publish it. This plan closes that trust gap and makes JJ-first recovery the visible supported path throughout the repo.
Output: Workspace-first config templates and schema/help text plus drift-catching regression coverage for config/help/workflow alignment.
</objective>

> **TDD Decision:** Selected - This plan changes published config/help contracts and locks them with deterministic template and workflow assertions.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-CONTEXT.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-RESEARCH.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-01-PLAN.md
@templates/config.json
@templates/config-full.json
@src/lib/constants.js
@src/lib/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy worktree examples with supported workspace config</name>
  <files>templates/config.json, templates/config-full.json, src/lib/constants.js, src/lib/config.js</files>
  <action>Update the shipped config templates and config/help schema so the documented execution settings use the supported `workspace` block, JJ-first defaults, and any recovery-relevant fields Phase 156 needs. Keep runtime validation and published examples aligned so users are not taught a config shape that the CLI rejects.</action>
  <verify>npm run test:file -- tests/contracts.test.cjs</verify>
  <done>- Default and full config templates publish `workspace`, not `worktree`
- Config schema/help text documents the supported workspace settings clearly
- Runtime validation and templates no longer contradict each other</done>
</task>

<task type="auto">
  <name>Task 2: Refresh help and discovery wording around recovery-capable workspaces</name>
  <files>src/lib/constants.js, src/lib/command-help.js, src/lib/commandDiscovery.js, tests/workflow.test.cjs</files>
  <action>Audit the repo-facing help and command discovery text for the workspace surface so it teaches inspection, preview-first recovery, and JJ-first execution consistently without reintroducing Git worktree fallback language. Keep the wording narrow to Phase 156 scope and leave broader command-surface simplification to later phases.</action>
  <verify>npm run test:file -- tests/workflow.test.cjs</verify>
  <done>- Help/discovery text describes workspace inspection and recovery as the supported local execution model
- No published help/config wording points users back to legacy worktree behavior
- Workflow/help regression tests lock the updated JJ-first wording</done>
</task>

<task type="auto">
  <name>Task 3: Add drift-catching coverage for repo-facing JJ-first guidance</name>
  <files>tests/contracts.test.cjs, tests/workflow.test.cjs</files>
  <action>Add or update tests that fail when templates, workflow/help text, and runtime-facing workspace guidance drift apart. Cover the specific Phase 156 promises that matter here: workspace config examples, recovery-aware help wording, and JJ-first execution guidance throughout the repo.</action>
  <verify>npm run test:file -- tests/contracts.test.cjs tests/workflow.test.cjs</verify>
  <done>- Tests fail if legacy worktree config returns to shipped templates
- Tests fail if help/workflow wording drops Phase 156 workspace-recovery guidance
- Repo-facing JJ-first guidance is validated, not just manually reviewed</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/contracts.test.cjs tests/workflow.test.cjs`
2. `templates/config.json` and `templates/config-full.json` publish a `workspace` block that matches runtime support
3. Help/discovery surfaces describe JJ workspace recovery and no longer ship contradictory `worktree` configuration examples
</verification>

<success_criteria>
- Repo-facing config and help surfaces match the supported JJ workspace model
- Recovery-capable workspace execution is visible throughout templates/help/discovery
- Regression coverage protects against future guidance drift
</success_criteria>

<output>
After completion, create `.planning/phases/156-jj-parallel-waves-recovery-coverage/156-03-SUMMARY.md`
</output>
