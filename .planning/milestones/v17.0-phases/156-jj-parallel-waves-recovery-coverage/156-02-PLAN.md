---
phase: 156-jj-parallel-waves-recovery-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 156-01
files_modified:
  - src/commands/init.js
  - src/lib/context.js
  - workflows/execute-phase.md
  - tests/init.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - JJ-03
  - JJ-04
intent:
  outcome_ids:
    - DO-118

must_haves:
  truths:
    - "Multi-plan execution creates and tracks one managed JJ workspace per runnable plan in a wave instead of treating the wave as a single shared working copy."
    - "Execution init surfaces active managed JJ workspaces and their recovery state instead of always reporting an empty workspace list."
    - "Parallel wave execution guidance clearly separates successful workspaces that can reconcile now from failed or divergent workspaces that need recovery."
    - "Users can finish unaffected plans in a wave without losing visibility into failed workspace recovery work."
  artifacts:
    - path: "src/commands/init.js"
      provides: "Execute-phase init payload with live workspace inventory and recovery-aware metadata"
      contains: "workspace_active"
    - path: "workflows/execute-phase.md"
      provides: "Supported partial-wave JJ execution and recovery guidance"
      contains: "workspace reconcile"
    - path: "tests/integration.test.cjs"
      provides: "Execution-entrypoint regression coverage for active workspaces and partial-wave resume behavior"
      contains: "workspace_active"
  key_links:
    - from: "src/commands/init.js"
      to: "src/commands/workspace.js"
      via: "Live workspace inventory and overlap/recovery metadata"
      pattern: "workspace_active|file_overlaps"
    - from: "src/lib/context.js"
      to: "src/commands/init.js"
      via: "Executor-scoped exposure of new workspace metadata"
      pattern: "workspace_"
    - from: "workflows/execute-phase.md"
      to: "src/commands/init.js"
      via: "Execution contract consumes active workspace and recovery state"
      pattern: "workspace_active|partial"
---

<objective>
Wire the richer workspace recovery model into execution init and execute-phase orchestration so each runnable plan gets its own managed JJ workspace, waves report partial completion honestly, healthy workspaces reconcile independently, and failed ones retain recovery context.

Purpose: Phase 156 is not complete if recovery lives only in `workspace` commands. Execution entrypoints and wave orchestration must create, track, and reconcile per-plan workspaces while telling executors what to do with healthy versus recovery-needed workspaces.
Output: Live `workspace_active` init metadata, updated execute-phase per-plan workspace creation/tracking/reconcile guidance, and regression coverage for execution-facing workspace context.
</objective>

> **TDD Decision:** Selected - This plan changes execution-entrypoint payloads and workflow-visible behavior with clear JSON and contract assertions.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-CONTEXT.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-RESEARCH.md
@.planning/phases/156-jj-parallel-waves-recovery-coverage/156-01-PLAN.md
@src/commands/init.js
@src/lib/context.js
@workflows/execute-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface live workspace execution context in init payloads</name>
  <files>src/commands/init.js, src/lib/context.js, tests/init.test.cjs</files>
  <action>Replace the current placeholder `workspace_active: []` behavior with live managed-workspace inventory derived from the workspace command layer, including the per-plan mapping and recovery-relevant fields execution needs to track one workspace per runnable plan. Keep context scoping aligned so execution agents receive the useful workspace metadata without reintroducing legacy worktree vocabulary or leaking unrelated planning fields.</action>
  <verify>npm run test:file -- tests/init.test.cjs</verify>
  <done>- `init:execute-phase` returns real active workspace entries when managed workspaces exist
- Active workspace entries are attributable to the runnable plan they belong to
- Scoped executor context includes the new workspace metadata and no legacy worktree keys
- Empty workspace state is still represented explicitly when nothing is active</done>
</task>

<task type="auto">
  <name>Task 2: Implement per-plan workspace orchestration for parallel waves</name>
  <files>workflows/execute-phase.md, tests/integration.test.cjs</files>
  <action>Rewrite the Phase 155 placeholder wording in `workflows/execute-phase.md` so workspace-based parallel execution now describes the supported Phase 156 contract as executable orchestration behavior: create one managed workspace per runnable plan in a wave, track success/failure by workspace, reconcile healthy workspaces independently in deterministic order, preserve failed/divergent workspaces for inspection and recovery, and clean up obsolete failed workspaces only after successful phase completion. Lock the orchestration contract with integration-style tests where the repo already guards workflow contracts.</action>
  <verify>npm run test:file -- tests/integration.test.cjs</verify>
  <done>- Execute-phase workflow explicitly creates and tracks one workspace per runnable plan in a wave
- Healthy workspaces reconcile independently while failed/divergent ones remain recoverable
- Execute-phase workflow no longer says stale/conflict handling is deferred to Phase 156
- Workflow guidance explicitly covers partial-wave outcomes and retained recovery workspaces
- Regression tests lock the execution entrypoint/workflow contract for Phase 156 behavior</done>
</task>

</tasks>

<verification>
1. `npm run test:file -- tests/init.test.cjs tests/integration.test.cjs`
2. In a JJ-backed temp repo with managed workspaces, `node bin/bgsd-tools.cjs init:execute-phase 156` returns non-empty `workspace_active` entries that include status and recovery context
3. `workflows/execute-phase.md` clearly states that each runnable plan gets its own workspace, healthy plans may reconcile independently, and failed/divergent workspaces remain available for recovery
</verification>

<success_criteria>
- Parallel-wave execution guidance creates and tracks one managed workspace per runnable plan
- Execution init exposes live workspace state instead of placeholder emptiness
- Execute-phase guidance matches the locked partial-wave and preview-first recovery decisions
- Recovery-needed workspaces stay visible to execution flows while healthy plans can still finish
</success_criteria>

<output>
After completion, create `.planning/phases/156-jj-parallel-waves-recovery-coverage/156-02-SUMMARY.md`
</output>
