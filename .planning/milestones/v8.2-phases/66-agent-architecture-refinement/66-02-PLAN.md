---
phase: 66-agent-architecture-refinement
plan: 02
type: execute
wave: 2
depends_on:
  - 66-01
files_modified:
  - agents/gsd-executor.md
  - agents/gsd-planner.md
  - agents/gsd-verifier.md
  - agents/gsd-roadmapper.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-project-researcher.md
  - agents/gsd-codebase-mapper.md
  - agents/gsd-debugger.md
  - agents/gsd-plan-checker.md
  - references/reviewer-agent.md
autonomous: true
requirements:
  - AGENT-02
  - AGENT-04

must_haves:
  truths:
    - "Agent manifests only grant tools the agent actually uses in its workflows"
    - "Unused context fields are removed from agent manifests"
    - "Agent merge evaluation produces documented recommendation for every agent pair with >50% RACI overlap"
    - "Reviewer agent disposition is decided (merge, deploy, or remove)"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "Tightened executor manifest with verified tool grants"
      contains: "tools:"
    - path: ".planning/phases/66-agent-architecture-refinement/66-02-SUMMARY.md"
      provides: "Merge evaluation results and manifest changes"
  key_links:
    - from: "references/RACI.md"
      to: "merge evaluation"
      via: "RACI overlap counting determines merge candidates"
      pattern: "Responsible"
    - from: "workflows/*.md"
      to: "agents/*.md tools:"
      via: "workflow tool usage determines which grants agents need"
      pattern: "tools:"
---

<objective>
Audit agent manifests against actual usage, tighten tool grants and context declarations, and evaluate all agent pairs for merge opportunities using RACI overlap analysis.

Purpose: Remove unused capabilities from agent manifests (security + efficiency), determine if any agents should be merged based on overlapping responsibilities, and decide the fate of the undeployed reviewer agent.
Output: Tightened agent manifests, merge evaluation report in SUMMARY.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-agent-architecture-refinement/66-CONTEXT.md
@.planning/phases/66-agent-architecture-refinement/66-01-SUMMARY.md
@references/RACI.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and tighten agent manifests</name>
  <files>agents/gsd-executor.md, agents/gsd-planner.md, agents/gsd-verifier.md, agents/gsd-roadmapper.md, agents/gsd-phase-researcher.md, agents/gsd-project-researcher.md, agents/gsd-codebase-mapper.md, agents/gsd-debugger.md, agents/gsd-plan-checker.md</files>
  <action>
For each of the 9 agents, analyze which tools they ACTUALLY use by examining:
1. The agent's own .md system prompt — what tool calls does it instruct?
2. The workflows that spawn this agent — what tools does the workflow expect the agent to use?
3. Cross-reference against the `tools:` frontmatter in each agent .md

**Analysis approach (static, per CONTEXT.md "conservative trimming" decision):**

For each agent, grep the agent's .md body (not just frontmatter) for tool usage patterns:
- `Read tool` / `mcp_read` → needs `read: true`
- `Write tool` / `mcp_write` → needs `write: true`
- `Edit tool` / `mcp_edit` → needs `edit: true`
- `Bash tool` / `mcp_bash` → needs `bash: true`
- `Grep tool` / `mcp_grep` → needs `grep: true`
- `Glob tool` / `mcp_glob` → needs `glob: true`
- `webfetch` / `mcp_webfetch` → needs `webfetch: true`
- `websearch` / `mcp_EXA_web_search` / `mcp_brave` → needs `websearch: true`
- `mcp__context7__*` → needs context7 tool grants

**For each agent, produce a table:**
| Tool | Declared | Actually Used | Action |
|------|----------|---------------|--------|
| read | true | true | keep |
| write | true | false | REMOVE |

**Then update each agent's frontmatter:**
- Remove tool grants not used by the agent
- Keep anything ambiguous (per CONTEXT.md "conservative trimming")
- Do NOT change the agent's system prompt body — only frontmatter tools

**Also audit context fields:** Check if agents declare `model:` or other context fields in frontmatter that aren't consumed. The current frontmatter has: description, color, tools. If any agent has additional unused fields, remove them.

**Token budget note:** Per CONTEXT.md, token budgets should be "verified from real runs." Since we can't run real workflows in this plan, document estimated token budgets as comments in each agent frontmatter based on the agent's .md file size (which is the system prompt size). Format: `# estimated_tokens: ~{N}k (system prompt: {lines} lines)`
  </action>
  <verify>
- For each agent: `grep "tools:" agents/gsd-*.md` shows tools section exists
- For gsd-plan-checker specifically: verify it does NOT have `write: true` (it only reads plans, never writes)
- For gsd-codebase-mapper: verify it has `write: true` (it writes codebase/*.md files)
- `npm test` passes (agent changes don't affect test suite)
  </verify>
  <done>All 9 agent manifests have only the tool grants they actually use, unused context fields removed, token budget estimates documented</done>
</task>

<task type="auto">
  <name>Task 2: Run merge evaluation using RACI overlap analysis</name>
  <files>references/reviewer-agent.md</files>
  <action>
Using the RACI matrix from references/RACI.md (created in Plan 01):

**1. Count overlap for all agent pairs:**
For each pair of agents (A, B), count how many lifecycle steps they BOTH have as Responsible. Calculate overlap percentage: `shared_R_steps / min(A_total_R_steps, B_total_R_steps) * 100`.

Per CONTEXT.md: ">50% overlap → merge candidate"

**2. Build overlap matrix:**
Document in SUMMARY (not a separate file) a table:
| Agent A | Agent B | Shared R Steps | Overlap % | Recommendation |
|---------|---------|----------------|-----------|----------------|

**3. Evaluate reviewer agent (references/reviewer-agent.md):**
Per CONTEXT.md decision: "Reviewer agent included in evaluation. Disposition to be recommended: merge into existing agent, deploy standalone, or remove."

Read references/reviewer-agent.md. It's a code review agent that reviews executor output. Assess:
- Does its responsibility overlap >50% with gsd-verifier? (both check completed work)
- Is it referenced by any active workflow? (grep workflows/ for "reviewer")
- Would merging into gsd-verifier add value without bloating the verifier?

**4. Implement recommendations:**
- If any agent pair has >50% overlap: recommend merge with rationale (but do NOT merge in this plan — that would be a significant structural change requiring its own plan)
- For reviewer agent: implement the recommended disposition:
  - If "remove": delete references/reviewer-agent.md, note in SUMMARY
  - If "merge": add key review capabilities to the target agent, delete the reference file
  - If "deploy standalone": create agents/gsd-reviewer.md from the reference file
- Per CONTEXT.md: "Merged agents get new names" — if merging, propose the new name

**5. Document all findings in SUMMARY** with rationale for each decision.

Note: If the RACI analysis reveals NO agent pairs with >50% overlap (likely, since Phase 53 already consolidated the obvious merges), document that finding. The requirement is that the evaluation was COMPLETED, not that merges were found.
  </action>
  <verify>
- Overlap matrix documented (can be in commit message or SUMMARY)
- Reviewer agent disposition implemented (file either deleted, merged, or deployed)
- `ls agents/gsd-*.md | wc -l` confirms expected agent count
- `grep -l "reviewer" workflows/*.md` shows whether any workflows reference the reviewer
  </verify>
  <done>Merge evaluation completed for all agent pairs, overlap percentages documented, reviewer agent disposition decided and implemented, no agent pair has >50% RACI overlap without documented recommendation</done>
</task>

</tasks>

<verification>
- All 9 agent manifests have been audited — only necessary tool grants remain
- Merge evaluation completed with overlap percentages for all pairs
- Reviewer agent has a clear disposition (merge/deploy/remove) with rationale
- No agent has tool grants it doesn't use in its system prompt or spawning workflows
</verification>

<success_criteria>
- Agent manifests are tightened with documented rationale for each change
- Merge evaluation completed — all pairs assessed, reviewer agent disposition decided
- If any pair has >50% overlap, it's documented with merge recommendation
</success_criteria>

<output>
After completion, create `.planning/phases/66-agent-architecture-refinement/66-02-SUMMARY.md`
</output>
