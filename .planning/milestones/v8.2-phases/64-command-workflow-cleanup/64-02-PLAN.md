---
phase: 64-command-workflow-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - workflows/execute-phase.md
  - workflows/execute-plan.md
  - workflows/plan-phase.md
  - workflows/settings.md
  - workflows/health.md
  - workflows/progress.md
  - workflows/set-profile.md
  - workflows/audit-milestone.md
  - workflows/complete-milestone.md
  - workflows/new-project.md
  - workflows/pause-work.md
  - workflows/cmd-codebase-impact.md
  - workflows/cmd-session-diff.md
  - workflows/cmd-search-decisions.md
  - workflows/cmd-search-lessons.md
  - workflows/cmd-velocity.md
  - workflows/cmd-validate-deps.md
  - workflows/cmd-validate-config.md
  - workflows/cmd-rollback-info.md
  - workflows/cmd-trace-requirement.md
  - workflows/cmd-test-run.md
  - workflows/cmd-context-budget.md
  - bin/gsd-tools.test.cjs
  - src/lib/constants.js
  - commands/bgsd-list-phase-assumptions.md
autonomous: true
requirements: [CMD-02, CMD-04]

must_haves:
  truths:
    - "Zero flat-form gsd-tools invocations remain in any .md or .js file"
    - "All 762+ tests pass using namespaced command forms"
    - "COMMAND_HELP only shows user-facing commands"
    - "No slash command wrapper exists for internal-only CLI commands"
  artifacts:
    - path: "bin/gsd-tools.test.cjs"
      provides: "Test suite with all invocations using namespace:command form"
    - path: "src/lib/constants.js"
      provides: "COMMAND_HELP with namespaced entries only, internal commands excluded"
  key_links:
    - from: "workflows/*.md"
      to: "src/router.js"
      via: "gsd-tools.cjs namespace:command invocations"
      pattern: "gsd-tools\\.cjs\\s+\\w+:"
    - from: "bin/gsd-tools.test.cjs"
      to: "src/router.js"
      via: "runGsdTools namespace:command test invocations"
      pattern: "runGsdTools\\('\\w+:"
---

<objective>
Migrate all flat-form gsd-tools references to namespaced forms across workflows, tests, and help strings. Clean up internal-only slash commands and consolidate COMMAND_HELP.

Purpose: After Plan 01 removed the backward-compat block, flat-form invocations are now broken. This plan migrates every reference and restores the full test suite to passing.
Output: All files using namespaced command forms, clean help output, 762+ tests passing
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-command-workflow-cleanup/64-CONTEXT.md
@.planning/phases/64-command-workflow-cleanup/64-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate flat-form references in workflows, references, and commands</name>
  <files>workflows/*.md, references/*.md, commands/*.md</files>
  <action>
Migrate every flat-form `gsd-tools.cjs <command>` invocation to `gsd-tools.cjs <namespace>:<command>` form across all .md files. Use the mapping below. For each file, read it, apply all applicable replacements, write it back.

**Flat → Namespace Mapping (apply to ALL .md files):**

| Flat Form | Namespaced Form |
|-----------|-----------------|
| `validate-dependencies` | `verify:validate-dependencies` |
| `validate roadmap` | `verify:validate roadmap` |
| `validate health` | `verify:validate health` |
| `validate consistency` | `verify:validate consistency` |
| `validate-config` | `verify:validate-config` |
| `summary-extract` | `util:summary-extract` |
| `search-lessons` | `verify:search-lessons` |
| `search-decisions` | `verify:search-decisions` |
| `review` (standalone, not verify:review) | `verify:review` |
| `memory` (standalone) | `util:memory` |
| `find-phase` (standalone) | `plan:find-phase` |
| `context-budget` | `verify:context-budget` |
| `config-ensure-section` | `util:config-ensure-section` |
| `resolve-model` | `util:resolve-model` |
| `velocity` (standalone) | `execute:velocity` |
| `trace-requirement` | `util:trace-requirement` |
| `test-run` (standalone) | `execute:test-run` |
| `state-snapshot` | `util:state-snapshot` |
| `session-diff` (standalone) | `execute:session-diff` |
| `rollback-info` (standalone) | `execute:rollback-info` |
| `roadmap` (standalone, as in `gsd-tools.cjs roadmap`) | `plan:roadmap` |
| `phase-plan-index` | `util:phase-plan-index` |
| `milestone` (standalone) | `plan:milestone` |
| `generate-slug` | `util:generate-slug` |
| `current-timestamp` | `util:current-timestamp` |
| `codebase-impact` | `util:codebase impact` |
| `assertions` (standalone) | `verify:assertions` |
| `commit` (standalone, not execute:commit) | `execute:commit` |
| `tdd` (standalone, not execute:tdd) | `execute:tdd` |

**IMPORTANT edge cases:**
- Do NOT double-namespace: if a reference already uses `execute:commit`, leave it alone
- The `gsd-tools.cjs execute commit` space-separated form is ALREADY routed correctly by the namespace parser (command="execute" triggers namespace routing, no colon needed since `execute` is the first arg). But for consistency and clarity, prefer converting `gsd-tools.cjs execute commit` → `gsd-tools.cjs execute:commit` where found (this is cosmetic, not functional)
- Watch for `gsd-tools phase ...` vs `gsd-tools.cjs plan:phase ...` — the former is a different pattern (bare `gsd-tools` without `.cjs`) used in documentation prose, not in bash invocations. Only migrate actual bash invocations (`node ... gsd-tools.cjs`)

**Specific files requiring changes (identified from audit):**
- `workflows/execute-phase.md`: `validate-dependencies` → `verify:validate-dependencies`, `phase-plan-index` → `util:phase-plan-index`, `validate roadmap` → `verify:validate roadmap`
- `workflows/execute-plan.md`: `context-budget` → `verify:context-budget`, `tdd` → `execute:tdd`, `review` → `verify:review`
- `workflows/plan-phase.md`: `search-lessons` → `verify:search-lessons`, `assertions` → `verify:assertions`
- `workflows/settings.md`: `config-ensure-section` → `util:config-ensure-section`
- `workflows/health.md`: `validate health` → `verify:validate health`
- `workflows/progress.md`: `state-snapshot` → `util:state-snapshot`
- `workflows/set-profile.md`: `config-ensure-section` → `util:config-ensure-section`
- `workflows/audit-milestone.md`: `resolve-model` → `util:resolve-model`, `summary-extract` → `util:summary-extract`
- `workflows/complete-milestone.md`: `summary-extract` → `util:summary-extract`, `milestone` → `plan:milestone`
- `workflows/new-project.md`: `validate roadmap` → `verify:validate roadmap`
- `workflows/pause-work.md`: `current-timestamp` → `util:current-timestamp`
- `workflows/cmd-codebase-impact.md`: `codebase-impact` → `util:codebase impact` (NOTE: also change args format to match util:codebase impact syntax — pass files as `--files` arg)
- `workflows/cmd-session-diff.md`: `session-diff` → `execute:session-diff`
- `workflows/cmd-search-decisions.md`: `search-decisions` → `verify:search-decisions`
- `workflows/cmd-search-lessons.md`: `search-lessons` → `verify:search-lessons`
- `workflows/cmd-velocity.md`: `velocity` → `execute:velocity`
- `workflows/cmd-validate-deps.md`: `validate-dependencies` → `verify:validate-dependencies`
- `workflows/cmd-validate-config.md`: If exists, update `validate-config` → `verify:validate-config`
- `workflows/cmd-rollback-info.md`: `rollback-info` → `execute:rollback-info`
- `workflows/cmd-trace-requirement.md`: `trace-requirement` → `util:trace-requirement`
- `workflows/cmd-test-run.md`: `test-run` → `execute:test-run`
- `workflows/cmd-context-budget.md`: `context-budget` → `verify:context-budget`
- Any other .md files that `rg 'gsd-tools\.cjs\s+[a-z][\w-]+' --glob '*.md' workflows/ references/ commands/ templates/` reveals

**After all replacements, verify zero flat-form references remain:**
```bash
rg 'gsd-tools\.cjs\s+(?!init:|plan:|execute:|verify:|util:|research:|cache:)[a-z][\w-]+' workflows/ references/ commands/ templates/
```
This grep should return ZERO results. If any remain, fix them.
  </action>
  <verify>
Run: `rg 'gsd-tools\.cjs\s+(?!init:|plan:|execute:|verify:|util:|research:|cache:)[a-z][\w-]+' workflows/ references/ commands/ templates/`
Must return zero matches. Every gsd-tools.cjs invocation in .md files uses namespace:command form.
  </verify>
  <done>All ~45 flat-form gsd-tools invocations across workflow, reference, command, and template .md files are migrated to namespaced forms. Zero flat-form references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate tests, clean COMMAND_HELP, remove internal slash wrappers</name>
  <files>bin/gsd-tools.test.cjs, src/lib/constants.js, commands/bgsd-list-phase-assumptions.md</files>
  <action>
**Part A: Migrate test invocations (~282 flat-form calls)**

In `bin/gsd-tools.test.cjs`, migrate all `runGsdTools('flat-command ...')` invocations to `runGsdTools('namespace:command ...')` using the same mapping from Task 1. Common patterns to find-and-replace:

| Test Pattern | Replace With |
|--------------|-------------|
| `runGsdTools('state ...)` | `runGsdTools('verify:state ...)` |
| `runGsdTools('commit ...)` | `runGsdTools('execute:commit ...)` |
| `runGsdTools('find-phase ...)` | `runGsdTools('plan:find-phase ...)` |
| `runGsdTools('verify ...)` (not already namespaced) | `runGsdTools('verify:verify ...)` |
| `runGsdTools('roadmap ...)` | `runGsdTools('plan:roadmap ...)` |
| `runGsdTools('phases ...)` | `runGsdTools('plan:phases ...)` |
| `runGsdTools('requirements ...)` | `runGsdTools('plan:requirements ...)` |
| `runGsdTools('phase ...)` (not plan:phase) | `runGsdTools('plan:phase ...)` |
| `runGsdTools('milestone ...)` | `runGsdTools('plan:milestone ...)` |
| `runGsdTools('validate ...)` (consistency/health/roadmap) | `runGsdTools('verify:validate ...)` |
| `runGsdTools('progress ...)` | `runGsdTools('util:progress ...)` |
| `runGsdTools('todo ...)` | `runGsdTools('util:todo ...)` |
| `runGsdTools('scaffold ...)` | `runGsdTools('util:scaffold ...)` |
| `runGsdTools('init ...)` (flat form) | `runGsdTools('init:...)` (colon form) |
| `runGsdTools('frontmatter ...)` | `runGsdTools('util:frontmatter ...)` |
| `runGsdTools('history-digest ...)` | `runGsdTools('util:history-digest ...)` |
| `runGsdTools('websearch ...)` | `runGsdTools('util:websearch ...)` |
| `runGsdTools('session-diff ...)` | `runGsdTools('execute:session-diff ...)` |
| `runGsdTools('context-budget ...)` | `runGsdTools('verify:context-budget ...)` |
| `runGsdTools('test-run ...)` | `runGsdTools('execute:test-run ...)` |
| `runGsdTools('search-decisions ...)` | `runGsdTools('verify:search-decisions ...)` |
| `runGsdTools('search-lessons ...)` | `runGsdTools('verify:search-lessons ...)` |
| `runGsdTools('codebase ...)` | `runGsdTools('util:codebase ...)` |
| `runGsdTools('codebase-impact ...)` | `runGsdTools('util:codebase impact ...)` |
| `runGsdTools('rollback-info ...)` | `runGsdTools('execute:rollback-info ...)` |
| `runGsdTools('velocity ...)` | `runGsdTools('execute:velocity ...)` |
| `runGsdTools('trace-requirement ...)` | `runGsdTools('util:trace-requirement ...)` |
| `runGsdTools('memory ...)` | `runGsdTools('util:memory ...)` |
| `runGsdTools('intent ...)` | `runGsdTools('plan:intent ...)` |
| `runGsdTools('env ...)` | `runGsdTools('util:env ...)` |
| `runGsdTools('mcp-profile ...)` | `runGsdTools('util:mcp profile ...)` |
| `runGsdTools('mcp ...)` | `runGsdTools('util:mcp ...)` |
| `runGsdTools('assertions ...)` | `runGsdTools('verify:assertions ...)` |
| `runGsdTools('worktree ...)` | `runGsdTools('execute:worktree ...)` |
| `runGsdTools('agent ...)` | `runGsdTools('util:agent ...)` |
| `runGsdTools('git ...)` | `runGsdTools('util:git ...)` |
| `runGsdTools('review ...)` | `runGsdTools('verify:review ...)` |
| `runGsdTools('tdd ...)` | `runGsdTools('execute:tdd ...)` |
| `runGsdTools('classify ...)` | `runGsdTools('util:classify ...)` |
| `runGsdTools('trajectory ...)` | `runGsdTools('execute:trajectory ...)` |
| `runGsdTools('cache ...)` | `runGsdTools('cache:...)` |
| `runGsdTools('research ...)` | `runGsdTools('research:...)` |
| `runGsdTools('profiler ...)` | `runGsdTools('util:profiler ...)` |
| `runGsdTools('resolve-model ...)` | `runGsdTools('util:resolve-model ...)` |
| `runGsdTools('verify-summary ...)` | `runGsdTools('verify:summary ...)` |
| `runGsdTools('template ...)` | `runGsdTools('util:template ...)` |
| `runGsdTools('generate-slug ...)` | `runGsdTools('util:generate-slug ...)` |
| `runGsdTools('current-timestamp ...)` | `runGsdTools('util:current-timestamp ...)` |
| `runGsdTools('list-todos ...)` | `runGsdTools('util:list-todos ...)` |
| `runGsdTools('config-set ...)` | `runGsdTools('util:config-set ...)` |
| `runGsdTools('config-get ...)` | `runGsdTools('util:config-get ...)` |
| `runGsdTools('config-migrate ...)` | `runGsdTools('util:config-migrate ...)` |
| `runGsdTools('config-ensure-section ...)` | `runGsdTools('util:config-ensure-section ...)` |
| `runGsdTools('phase-plan-index ...)` | `runGsdTools('util:phase-plan-index ...)` |
| `runGsdTools('state-snapshot ...)` | `runGsdTools('util:state-snapshot ...)` |
| `runGsdTools('summary-extract ...)` | `runGsdTools('util:summary-extract ...)` |
| `runGsdTools('validate-dependencies ...)` | `runGsdTools('verify:validate-dependencies ...)` |
| `runGsdTools('validate-config ...)` | `runGsdTools('verify:validate-config ...)` |
| `runGsdTools('token-budget ...)` | `runGsdTools('verify:token-budget ...)` |
| `runGsdTools('quick-summary ...)` | `runGsdTools('util:quick-summary ...)` |
| `runGsdTools('extract-sections ...)` | `runGsdTools('util:extract-sections ...)` |
| `runGsdTools('test-coverage ...)` | `runGsdTools('verify:test-coverage ...)` |
| `runGsdTools('verify-path-exists ...)` | `runGsdTools('util:verify-path-exists ...)` |

**CRITICAL:** Watch for tests specifically testing backward-compatibility behavior. Per user decision, these should be DELETED ENTIRELY (not converted to error tests). Look for test names containing "backward compat", "legacy", "flat form", or "back compat".

Also handle `runGsdTools('init execute-phase...')` → `runGsdTools('init:execute-phase...')` (space to colon).

**Part B: Clean COMMAND_HELP in constants.js**

In `src/lib/constants.js`, update the `COMMAND_HELP` object:
1. Remove all flat-form keys that have a namespaced equivalent (e.g., remove `'commit': ...` if `'execute:commit': ...` exists)
2. Add namespaced keys for any that only existed in flat form
3. Remove help entries for internal-only commands that users should never call directly:
   - `init:*` commands (these are agent-internal workflow initializers)
   - `util:config-ensure-section` (internal setup)
   - `util:verify-path-exists` (internal utility)
   - `util:state-snapshot` (internal)
   - `util:phase-plan-index` (internal)
   - `util:summary-extract` (internal)
   - `util:scaffold` (internal — used by agents)
   - `util:resolve-model` (internal — used by orchestrators)
   - `verify:summary` (internal — used by execute-plan)
4. Keep help entries for user-facing commands only

**Part C: Remove internal-only slash command wrappers**

Check each slash command .md file against this criteria:
- If it wraps a command that ONLY agents/workflows call (not users), delete the file
- Per CONTEXT: "Agent decides per-command whether an internal wrapper has value as agent documentation"

Candidates for removal (verify each):
- `commands/bgsd-list-phase-assumptions.md` — if this just calls an internal command with no user value

Do NOT remove any command that users would reasonably invoke directly via `/bgsd-*` in the editor.

**Part D: Rebuild and test**

After all changes:
1. Run `npm run build` — must succeed
2. Run `npm test` — ALL 762+ tests must pass
3. Final sweep: `rg 'gsd-tools\.cjs\s+(?!init:|plan:|execute:|verify:|util:|research:|cache:)[a-z][\w-]+' workflows/ references/ commands/ templates/ src/ bin/gsd-tools.test.cjs` — must return ZERO results
  </action>
  <verify>
- `npm run build` succeeds
- `npm test` shows all tests passing (762+)
- `rg 'gsd-tools\.cjs\s+(?!init:|plan:|execute:|verify:|util:|research:|cache:)[a-z][\w-]+' workflows/ references/ commands/ templates/ src/ bin/gsd-tools.test.cjs` returns zero matches
- `node bin/gsd-tools.cjs --help` shows only namespaced commands
  </verify>
  <done>All ~282 test invocations migrated to namespaced forms. All 762+ tests pass. COMMAND_HELP cleaned to show only user-facing namespaced commands. Internal-only slash command wrappers removed. Zero flat-form references remain across the entire project.</done>
</task>

</tasks>

<verification>
- Zero flat-form gsd-tools.cjs invocations across entire project
- All 762+ tests pass
- COMMAND_HELP contains only namespaced, user-facing entries
- Help output (`--help`) shows clean namespace:command listing
- All workflows function correctly with namespaced invocations
</verification>

<success_criteria>
Every gsd-tools.cjs invocation in every file (workflows, references, commands, templates, tests, source) uses namespace:command syntax. All 762+ tests pass. COMMAND_HELP is clean and user-facing only. Internal-only slash command wrappers are removed.
</success_criteria>

<output>
After completion, create `.planning/phases/64-command-workflow-cleanup/64-02-SUMMARY.md`
</output>
