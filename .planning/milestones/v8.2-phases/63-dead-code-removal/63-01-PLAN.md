---
phase: 63-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/review/stage-review.js
  - src/lib/output.js
  - src/lib/context.js
  - src/lib/helpers.js
  - src/lib/deps.js
  - src/lib/recovery/stuck-detector.js
  - src/lib/git.js
  - src/lib/orchestration.js
  - src/lib/profiler.js
  - src/lib/codebase-intel.js
  - src/lib/conventions.js
  - src/lib/ast.js
  - src/lib/regex-cache.js
  - src/lib/review/severity.js
  - src/commands/state.js
  - src/commands/init.js
  - src/commands/features.js
  - src/commands/misc.js
  - src/commands/env.js
  - src/commands/mcp.js
  - src/commands/worktree.js
  - src/commands/codebase.js
  - src/commands/cache.js
  - src/commands/agent.js
  - src/commands/research.js
autonomous: true
requirements: [DEAD-01, DEAD-02]

must_haves:
  truths:
    - "src/lib/review/stage-review.js no longer exists on disk"
    - "Internal-only functions are no longer exported from module.exports blocks"
    - "All 762+ tests still pass after export removal"
    - "Build succeeds and produces valid bundle"
  artifacts:
    - path: "bin/gsd-tools.cjs"
      provides: "Rebuilt bundle without dead code"
  key_links:
    - from: "module.exports blocks"
      to: "test file imports"
      via: "10 test-referenced exports preserved"
      pattern: "_tmpFiles|AGENT_MANIFESTS|compactPlanState|compactDepGraph|LANG_MANIFESTS|parsePlanId|computeRiskLevel|classifyTaskComplexity|parseTasksFromPlan|getTimings"
---

<objective>
Remove the one confirmed dead file and un-export 95 internal helper functions that are needlessly exposed in module.exports blocks across 24 source files.

Purpose: Reduce public API surface — these functions are used within their own files but no external module imports them. Removing from exports shrinks the public interface and makes future dead code analysis cleaner.
Output: Cleaned module.exports blocks, deleted dead file, passing tests.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/baselines/audit/audit-summary.json
@.planning/phases/62-audit-discovery/62-01-SUMMARY.md
@.planning/phases/62-audit-discovery/62-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead file and un-export internal helpers from lib/ modules</name>
  <files>
    src/lib/review/stage-review.js
    src/lib/output.js
    src/lib/context.js
    src/lib/helpers.js
    src/lib/deps.js
    src/lib/recovery/stuck-detector.js
    src/lib/git.js
    src/lib/orchestration.js
    src/lib/profiler.js
    src/lib/codebase-intel.js
    src/lib/conventions.js
    src/lib/ast.js
    src/lib/regex-cache.js
    src/lib/review/severity.js
  </files>
  <action>
    **Step 1: Delete the dead file.**
    - `rm src/lib/review/stage-review.js` — confirmed unused by both JS static analysis and markdown cross-reference.

    **Step 2: Un-export internal helpers from lib/ module.exports blocks.**

    For each file below, edit the `module.exports = { ... }` block to REMOVE the listed names. Keep the function/constant definitions — only remove from the exports object.

    - `src/lib/output.js`: Remove `_tmpFiles, outputMode`
    - `src/lib/context.js`: Remove `isWithinBudget` — KEEP `AGENT_MANIFESTS, compactPlanState, compactDepGraph` (test-referenced)
    - `src/lib/helpers.js`: Remove `searchPhaseInDir, invalidateMilestoneCache`
    - `src/lib/deps.js`: Remove `IMPORT_PARSERS, parseImports`
    - `src/lib/recovery/stuck-detector.js`: Remove `StuckDetector, DEFAULT_CONFIG`
    - `src/lib/git.js`: Remove `branchInfo, trajectoryBranch`
    - `src/lib/orchestration.js`: KEEP `classifyTaskComplexity, parseTasksFromPlan` (test-referenced). Remove `routeTask`
    - `src/lib/profiler.js`: KEEP `getTimings` (test-referenced)
    - `src/lib/codebase-intel.js`: Remove `INTEL_PATH, BINARY_EXTENSIONS, analyzeFile`
    - `src/lib/conventions.js`: Remove `detectNamingConventions, detectFileOrganization, detectFrameworkConventions, FRAMEWORK_DETECTORS`
    - `src/lib/ast.js`: Remove `DETECTOR_REGISTRY, stripTypeScript, parseWithAcorn, extractCjsExports, extractJsSignaturesRegex`
    - `src/lib/regex-cache.js`: Remove `FRONTMATTER_DELIMITERS, PHASE_HEADER, ACTIVE_MILESTONE, ACTIVE_TAG_MILESTONE, VERSION_PATTERN, DATE_PATTERN, COMMIT_SHA, UNCHECKED_PHASE`
    - `src/lib/review/severity.js`: Remove `classifySeverity, createFinding, filterBySeverity, getSeveritySummary, blocksCompletion, CLASSIFICATION_RULES`

    **CRITICAL:** Do NOT remove the function/const definitions themselves. Only remove from the `module.exports` object. These functions are used within their own files.

    **CRITICAL:** Keep these 4 lib/ exports that are test-referenced: `AGENT_MANIFESTS`, `compactPlanState`, `compactDepGraph`, `getTimings`, `classifyTaskComplexity`, `parseTasksFromPlan`.
  </action>
  <verify>
    Run `npm run build` — must succeed.
    Run `ls src/lib/review/stage-review.js` — must fail (file deleted).
    Spot-check 3 files to confirm exports were removed but functions remain.
  </verify>
  <done>stage-review.js deleted. All listed internal helpers removed from lib/ module.exports blocks. Functions still defined in-file. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Un-export internal helpers from commands/ modules and verify</name>
  <files>
    src/commands/state.js
    src/commands/init.js
    src/commands/features.js
    src/commands/misc.js
    src/commands/env.js
    src/commands/mcp.js
    src/commands/worktree.js
    src/commands/codebase.js
    src/commands/cache.js
    src/commands/agent.js
    src/commands/research.js
  </files>
  <action>
    **Un-export internal helpers from commands/ module.exports blocks.**

    For each file below, edit the `module.exports = { ... }` block to REMOVE the listed names. Keep function definitions.

    - `src/commands/state.js`: Remove `stateExtractField, stateReplaceField`
    - `src/commands/init.js`: Remove `getSessionDiffSummary`
    - `src/commands/features.js`: Remove `extractSectionsFromFile`
    - `src/commands/misc.js`: Remove `preCommitChecks, FRONTMATTER_SCHEMAS`
    - `src/commands/env.js`: Remove `checkEnvManifestStaleness, scanManifests, detectPackageManager, matchSimpleGlob, performEnvScan, writeManifest, ensureManifestGitignored, writeProjectProfile, getWatchedFiles, getWatchedFilesMtimes` — KEEP `LANG_MANIFESTS` (test-referenced)
    - `src/commands/mcp.js`: Remove ALL 17 internal helpers: `discoverMcpServers, estimateTokenCost, scoreServerRelevance, generateRecommendations, applyRecommendations, restoreBackup, MCP_KNOWN_SERVERS, RELEVANCE_INDICATORS, DEFAULT_CONTEXT_WINDOW, DEFAULT_TOKENS_PER_TOOL, DEFAULT_BASE_TOKENS, LOW_COST_THRESHOLD, extractFromMcpJson, extractFromOpencodeJson, safeReadJson, matchIndicatorKey, checkEnvHints`
    - `src/commands/worktree.js`: Remove `parseMergeTreeConflicts, isAutoResolvable, WORKTREE_DEFAULTS` — KEEP `parsePlanId` (test-referenced)
    - `src/commands/codebase.js`: Remove `readCodebaseIntel, checkCodebaseIntelStaleness, spawnBackgroundAnalysis, scoreRelevance, getRecentlyModifiedFiles, getPlanFiles, enforceTokenBudget, matchFileConventions` — KEEP `computeRiskLevel` (test-referenced)
    - `src/commands/cache.js`: Remove `registerCacheCommand`
    - `src/commands/agent.js`: Remove `scanAgents, parseRaciMatrix`
    - `src/commands/research.js`: Remove `detectCliTools, detectMcpServers, calculateTier, collectWebSources, collectYouTubeSources, collectNlmSynthesis, formatSourcesForAgent, parseVtt, saveSession, loadSession, deleteSession`

    **After all edits:** Run `npm run build && npm test` — all 762+ tests must pass.

    **CRITICAL:** Do NOT remove function definitions. Only remove names from module.exports objects.
    **CRITICAL:** KEEP these 4 command/ exports that are test-referenced: `LANG_MANIFESTS`, `parsePlanId`, `computeRiskLevel`.
  </action>
  <verify>
    `npm run build` succeeds.
    `npm test` — all 762+ tests pass (0 failures from export changes, pre-existing config-migrate failures acceptable).
  </verify>
  <done>All 95 internal helper exports removed from module.exports blocks across 24 files. Build and all tests pass. No runtime regressions.</done>
</task>

</tasks>

<verification>
- `ls src/lib/review/stage-review.js` returns error (file deleted)
- `npm run build` succeeds
- `npm test` passes all 762+ tests
- Bundle size in build output is equal or smaller than baseline (~1216KB)
- `node -e "const m = require('./bin/gsd-tools.cjs'); console.log('OK')"` succeeds (bundle loads cleanly)
</verification>

<success_criteria>
1. stage-review.js deleted from disk
2. 95 internal helpers removed from module.exports (10 test-referenced preserved)
3. All function definitions remain intact (only exports removed)
4. npm test passes with 0 new failures
5. Build produces working bundle
</success_criteria>

<output>
After completion, create `.planning/phases/63-dead-code-removal/63-01-SUMMARY.md`
</output>
