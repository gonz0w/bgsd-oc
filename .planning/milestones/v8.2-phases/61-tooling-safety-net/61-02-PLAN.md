---
phase: 61-tooling-safety-net
plan: 02
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - deploy.sh
  - build.js
  - baseline.js
  - package.json
autonomous: true
requirements:
  - AUDIT-05

must_haves:
  truths:
    - "Deploy script removes files from target that no longer exist in source"
    - "Deploy script prints summary of files added, updated, and removed"
    - "First deploy with no prior manifest does a full copy without deletions"
    - "Running npm run baseline captures init timing, fs I/O counts, and bundle size into performance.json"
    - "Performance baselines are reproducible — multiple runs yield consistent median timing"
  artifacts:
    - path: "deploy.sh"
      provides: "Manifest-based deploy with stale file removal"
      contains: "manifest"
    - path: "build.js"
      provides: "Manifest generation during build"
      contains: "manifest.json"
    - path: "baseline.js"
      provides: "Performance baseline capture script"
      contains: "init_timing_ms"
    - path: ".planning/baselines/performance.json"
      provides: "Captured performance baselines"
    - path: "package.json"
      provides: "npm run baseline script entry"
      contains: "baseline"
  key_links:
    - from: "build.js"
      to: "bin/manifest.json"
      via: "manifest generation after build"
      pattern: "manifest\\.json"
    - from: "deploy.sh"
      to: "bin/manifest.json"
      via: "reads manifest to determine copy/delete"
      pattern: "manifest"
    - from: "baseline.js"
      to: ".planning/baselines/performance.json"
      via: "writes captured metrics"
      pattern: "performance\\.json"
---

<objective>
Implement manifest-based deploy sync that removes stale files from the deploy target, and create a performance baseline capture script for before/after comparison.

Purpose: Deploy safety prevents stale files from accumulating at the deploy target (current copy-only approach never deletes). Performance baselines give Phase 65 concrete before/after measurement capability.
Output: Manifest-aware deploy.sh, baseline.js script, performance.json baselines.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-tooling-safety-net/61-01-SUMMARY.md
@deploy.sh
@build.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manifest generation to build and implement manifest-based deploy sync</name>
  <files>build.js, deploy.sh</files>
  <action>
  **build.js changes:**

  After the existing metafile analysis (added in Plan 01), add manifest generation:

  1. Collect all deployable file paths by scanning the source directories that deploy.sh copies:
     - `bin/` — all files (gsd-tools.cjs, manifest.json itself)
     - `workflows/` — all .md files (recursive)
     - `templates/` — all .md files (recursive)
     - `references/` — all .md files (recursive)
     - `src/` — all files (recursive, for source copy)
     - `commands/` — all bgsd-*.md files
     - `agents/` — all gsd-*.md files
     - `VERSION` file
     Use `fs.readdirSync` with `recursive: true` option (Node 18.17+) or a simple recursive helper.
     Store paths relative to project root (e.g., `workflows/plan-phase.md`, `bin/gsd-tools.cjs`).

  2. Write manifest to `bin/manifest.json`:
     ```json
     {
       "generated": "ISO timestamp",
       "files": ["bin/gsd-tools.cjs", "workflows/plan-phase.md", "commands/bgsd-help.md", ...]
     }
     ```

  3. Console output: `Manifest: {N} files`

  **deploy.sh changes:**

  Replace the current copy-everything approach (Steps 3, 3b, 3d) with manifest-based sync:

  1. After build completes, read `bin/manifest.json` from source.

  2. Read previous manifest from deploy target (`$DEST/bin/manifest.json`) if it exists.

  3. For each file in the new manifest:
     - Determine source path and destination path (map `commands/bgsd-*.md` → `$CMD_DIR/`, `agents/gsd-*.md` → `$AGENT_DIR/`, everything else → `$DEST/`)
     - Copy source to destination (creating parent dirs as needed with `mkdir -p`)

  4. If a previous manifest exists, compute stale files = files in old manifest but NOT in new manifest.
     - Delete each stale file from the deploy target
     - Print: `Removed stale: {path}` for each
     - Print summary: `Cleaned {N} stale files`

  5. If no previous manifest exists (first manifest-based deploy): full copy, no deletions, print "First manifest deploy — no cleanup needed"

  6. Copy the new manifest to `$DEST/bin/manifest.json` for next deploy's comparison.

  Keep the existing backup step, path placeholder substitution (Step 3e), and smoke test (Step 4) intact. The backup provides rollback safety for the new deletion capability.

  Use `jq` for JSON parsing in bash (it's standard on Linux). The manifest format is simple enough for jq: `jq -r '.files[]' bin/manifest.json`.
  </action>
  <verify>
  1. Run `npm run build` — confirm "Manifest: N files" appears in output and `bin/manifest.json` exists
  2. Inspect `bin/manifest.json` — confirm it lists all deployable files
  3. Manually verify the deploy would work: `jq '.files | length' bin/manifest.json` should return a reasonable count (50+)
  4. If safe to test deploy: run `./deploy.sh` and confirm it completes with file sync summary
  </verify>
  <done>Build generates manifest.json listing all deployable files, and deploy.sh uses manifest diff to sync files and remove stale entries from the deploy target</done>
</task>

<task type="auto">
  <name>Task 2: Create performance baseline capture script</name>
  <files>baseline.js, package.json, .planning/baselines/performance.json</files>
  <action>
  Create `baseline.js` in project root — a standalone Node.js script that captures performance metrics:

  1. **Init timing** (median of 5 runs):
     - Run `node bin/gsd-tools.cjs current-timestamp --raw` 5 times using `execSync`
     - Measure each run's wall-clock time via `Date.now()` before/after
     - Compute median (sort, take middle value)
     - Store all individual run times and the median

  2. **Bundle size**:
     - Read `bin/gsd-tools.cjs` file size via `fs.statSync`
     - Convert to KB

  3. **File I/O counting** (per CONTEXT.md decision):
     - Monkey-patch `fs.readFileSync` and `fs.writeFileSync` before running gsd-tools.cjs:
       ```js
       let fsReadCount = 0, fsWriteCount = 0;
       const origRead = fs.readFileSync;
       const origWrite = fs.writeFileSync;
       fs.readFileSync = (...args) => { fsReadCount++; return origRead.apply(fs, args); };
       fs.writeFileSync = (...args) => { fsWriteCount++; return origWrite.apply(fs, args); };
       ```
     - Run `require('child_process').execFileSync('node', ['bin/gsd-tools.cjs', 'init', 'plan-phase', '1'], { encoding: 'utf-8' })` — note: this runs in a subprocess so the monkey-patch won't apply directly. Instead, use a simpler approach:
     - Create a small inline wrapper that requires gsd-tools.cjs after patching fs, then captures the counts. Or, more practically: count the fs operations by running gsd-tools with `strace -e trace=read,write` if available, OR simply use `GSD_PROFILE=1` and parse profiler output if it includes I/O stats.
     - **Simplest reliable approach:** Use `strace -c -e trace=openat node bin/gsd-tools.cjs init plan-phase 1 2>&1` to count file opens, OR just record the source file count and total lines as a proxy baseline. The agent should pick the approach that works reliably — this is under Agent's Discretion per CONTEXT.md.

  4. **Source file count and total lines**:
     - Count `.js` files in `src/` recursively
     - Sum total lines across all source files (quick `fs.readFileSync` + split by newline)

  5. **Write to `.planning/baselines/performance.json`**:
     ```json
     {
       "timestamp": "ISO string",
       "init_timing_ms": 45,
       "init_timing_runs": [42, 45, 48, 44, 46],
       "bundle_size_kb": 1216,
       "bundle_size_bytes": 1244687,
       "fs_read_count": 12,
       "fs_write_count": 3,
       "source_file_count": 34,
       "source_total_lines": 8500,
       "node_version": "v22.x.x"
     }
     ```

  6. **Console output** (TTY-friendly):
     ```
     Performance Baselines:
       Init timing:    45ms (median of 5 runs)
       Bundle size:    1216KB
       Source files:   34 files, 8500 lines
       Node version:   v22.x.x
     Saved to: .planning/baselines/performance.json
     ```

  7. Add npm script to package.json: `"baseline": "node baseline.js"`

  The script must be zero-dependency (uses only Node.js built-ins: fs, path, child_process).
  File is overwritten each run — git history preserves previous snapshots per CONTEXT.md decision.
  </action>
  <verify>
  - `npm run baseline` completes without errors
  - Console output shows init timing, bundle size, source stats
  - `.planning/baselines/performance.json` exists with all required fields
  - Run it twice — median timing should be within ~20% between runs (reproducible)
  - `npm test` still passes
  </verify>
  <done>npm run baseline captures init timing (median of 5), bundle size, and source metrics into performance.json for before/after comparison in Phase 65</done>
</task>

</tasks>

<verification>
- `npm run build` generates `bin/manifest.json` with all deployable files listed
- `./deploy.sh` uses manifest to sync files and would remove stale files
- `npm run baseline` captures reproducible performance metrics to performance.json
- `npm test` passes with no regressions
- All three new npm scripts work: `build` (enhanced), `baseline` (new), deploy (enhanced)
</verification>

<success_criteria>
Deploy script safely removes stale files using manifest comparison, and performance baselines are captured for before/after measurement throughout v8.2.
</success_criteria>

<output>
After completion, create `.planning/phases/61-tooling-safety-net/61-02-SUMMARY.md`
</output>
