---
phase: 152-cached-handoffs-fresh-context-delivery
plan: 04
type: execute
wave: 4
depends_on:
  - "152-03"
files_modified:
  - workflows/research-phase.md
  - workflows/plan-phase.md
  - workflows/execute-phase.md
  - workflows/verify-work.md
  - tests/workflow.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - FLOW-07
  - FLOW-08
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "`research`, `plan`, `execute`, and `verify` fail closed with repair or restart guidance when chain state is missing, invalid, or stale instead of guessing continuation from partial disk state."
    - "If source files changed during the run, the system warns, rebuilds from source, validates the reconstructed state, and only then allows continuation."
    - "Standalone command invocations still work normally when no valid handoff exists, even after downstream chain gating is wired in."
  artifacts:
    - path: "workflows/verify-work.md"
      provides: "fail-closed verification entrypoint with resume/inspect/restart handoff behavior"
      contains: "resume"
    - path: "workflows/execute-phase.md"
      provides: "resume-aware execution gating with rebuild-or-repair handling after source drift"
      contains: "repair"
    - path: "tests/workflow.test.cjs"
      provides: "workflow-wrapper coverage for fail-closed continuation and standalone fallback behavior"
      contains: "repair guidance"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "src/lib/phase-handoff.js"
      via: "workflow gating rebuilds and validates chain state before resuming when source fingerprints changed"
      pattern: "repair"
    - from: "tests/integration.test.cjs"
      to: "workflows/verify-work.md"
      via: "integration coverage proves missing-chain fail-closed behavior, source-drift rebuild logic, and standalone fallback across downstream workflow entrypoints"
      pattern: "latest valid artifact"
---

<objective>
Move the fail-closed downstream workflow gating into its own plan so the resume-contract slice stays small and execution quality stays high.

Purpose: Deliver the workflow-gating half of FLOW-07 and FLOW-08 for DO-116 by making `research`, `plan`, `execute`, and `verify` consume validated handoff state, rebuild after source drift, and stop with repair/restart guidance instead of guessing continuation.

Output: Updated downstream workflow contracts for `research`, `plan`, `execute`, and `verify` plus focused workflow/integration coverage for repair guidance, stale-source rebuild, and standalone fallback behavior.
</objective>

> **TDD Decision:** Skipped — this plan rewires downstream workflow gating across multiple orchestration surfaces, so the deterministic floor classifies it as tooling integration rather than a behavior-first RED/GREEN/REFACTOR slice.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-CONTEXT.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-RESEARCH.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-03-PLAN.md
@workflows/research-phase.md
@workflows/plan-phase.md
@workflows/execute-phase.md
@workflows/verify-work.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make research/plan/execute/verify fail closed with rebuild-or-repair guidance</name>
  <files>workflows/research-phase.md, workflows/plan-phase.md, workflows/execute-phase.md, workflows/verify-work.md</files>
  <action>
  Update the downstream workflow contracts so active-chain invocations for `research`, `plan`, `execute`, and `verify` consume validated handoff state, stop with repair or restart guidance when that state is missing or invalid, and rebuild from source after detected source drift before continuing.

  Keep the additive constraint explicit: these commands still work normally for standalone use when no chain state is being continued. Use the Phase 152 resume-summary contract from `152-03` rather than duplicating re-entry logic inside each workflow, and avoid touching command wrappers unless execution uncovers a real contract mismatch.
  </action>
  <verify>Run `node --test tests/workflow.test.cjs tests/integration.test.cjs --test-name-pattern "repair guidance|stale source|latest valid artifact|standalone"`.</verify>
  <done>`research`, `plan`, `execute`, and `verify` fail closed for invalid chain continuation, rebuild on source drift before continuing, and still preserve standalone behavior when no chain state is active.</done>
</task>

<task type="auto">
  <name>Task 2: Prove downstream chain gating and standalone fallback behavior</name>
  <files>tests/workflow.test.cjs, tests/integration.test.cjs</files>
  <action>
  Add workflow and integration coverage that exercises missing-chain fail-closed behavior, stale-source rebuild logic, latest-valid-artifact selection after corruption, and ordinary standalone execution when no handoff artifact is being continued.

  Keep the tests focused on downstream entrypoints only; do not yet pull discuss or transition auto-advance into this slice. This keeps the fail-closed gating work independent from the final additive discuss/yolo wiring.
  </action>
  <verify>Run `node --test tests/workflow.test.cjs tests/integration.test.cjs --test-name-pattern "repair guidance|stale source|standalone"`.</verify>
  <done>Automated coverage fails if downstream workflows guess continuation from partial disk state, skip rebuild validation after source drift, or regress standalone fallback behavior.</done>
</task>

</tasks>

<verification>
Run `node --test tests/workflow.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- `research`, `plan`, `execute`, and `verify` fail closed with repair or restart guidance when chain state is missing, invalid, or stale.
- Source drift triggers warn-and-rebuild validation before any resumed continuation is allowed.
- Standalone command behavior remains intact when no valid handoff artifact exists.
</success_criteria>

<output>
After completion, create `.planning/phases/152-cached-handoffs-fresh-context-delivery/152-04-SUMMARY.md`
</output>
