---
phase: 152-cached-handoffs-fresh-context-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/misc.js
  - src/lib/planning-cache.js
  - tests/plan.test.cjs
autonomous: true
requirements:
  - FLOW-06
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "`util:phase-plan-index` uses cached parsed plan data on the hot path instead of reparsing every phase plan markdown file on each invocation."
    - "Cache-backed phase plan reads stay additive-safe by rebuilding from markdown when the cache is cold, stale, or unavailable, rather than returning partial or guessed plan state."
    - "Later Phase 152 handoff work can trust plan-index freshness because source edits invalidate or rebuild the cached read path before chain logic consumes it."
  artifacts:
    - path: "src/commands/misc.js"
      provides: "cache-backed `util:phase-plan-index` command"
      contains: "cmdPhasePlanIndex"
    - path: "src/lib/planning-cache.js"
      provides: "plan-row accessors rich enough for plan-index reuse without markdown reparsing"
      contains: "getPlansForPhase"
    - path: "tests/plan.test.cjs"
      provides: "hot-path, cold-path, and stale-cache regression coverage for plan indexing"
      contains: "util:phase-plan-index"
  key_links:
    - from: "src/commands/misc.js"
      to: "src/lib/planning-cache.js"
      via: "phase-plan-index reads cached phase rows first, then falls back only when cache validation fails"
      pattern: "getPlansForPhase"
    - from: "tests/plan.test.cjs"
      to: "src/commands/misc.js"
      via: "tests prove cache hits preserve the existing JSON contract and source edits trigger rebuild behavior"
      pattern: "util:phase-plan-index"
---

<objective>
Land the lowest-coupling read-path slice first so the later handoff and workflow plans can stay within context budget instead of mixing cache migration with resume-state behavior.

Purpose: Deliver FLOW-06 for DO-116 by moving `util:phase-plan-index` onto `PlanningCache`-backed reads, while preserving the existing JSON contract and graceful markdown fallback behavior that standalone workflows already rely on.

Output: A cache-first plan-index implementation plus focused phase-command regression coverage for cache-hit, cache-miss, and stale-source scenarios.
</objective>

> **TDD Decision:** Skipped — this plan upgrades shared CLI orchestration and cache plumbing, so the deterministic floor classifies it as tooling work rather than a behavior slice that benefits from a RED/GREEN/REFACTOR loop.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-CONTEXT.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-RESEARCH.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-01-PLAN.md
@src/commands/misc.js
@src/lib/planning-cache.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor `util:phase-plan-index` to prefer cache-backed plan rows with validated fallback</name>
  <files>src/commands/misc.js, src/lib/planning-cache.js</files>
  <action>
  Rework `cmdPhasePlanIndex()` so it resolves phase metadata through the existing shared phase-tree helpers, then attempts `PlanningCache.getPlansForPhase()` and any needed single-plan reads before touching markdown directly.

  Preserve the current additive JSON contract (`plans`, `waves`, `incomplete`, `has_checkpoints`) and keep Map-backend, cold-cache, and stale-cache paths safe by rebuilding from source when cached rows are missing, invalid, or older than the plan files they represent. Avoid inventing a second plan-index format or relying on guessed task counts.
  </action>
  <verify>Run `node --test tests/plan.test.cjs --test-name-pattern "phase-plan-index|phase snapshot"`.</verify>
  <done>`util:phase-plan-index` uses cached parsed plan data on the hot path, yet still returns the same contract and degrades cleanly when cached state cannot be trusted.</done>
</task>

<task type="auto">
  <name>Task 2: Lock cache-hit and rebuild behavior with targeted CLI regression coverage</name>
  <files>tests/plan.test.cjs</files>
  <action>
  Add focused tests that prove cache-backed plan indexing works for normal phases, cold-cache fallback, stale plan edits, and environments where `PlanningCache` cannot serve SQLite-backed rows.

  Keep the coverage narrow: assert the plan-index contract, freshness behavior, and any init/integration call sites that depend on plan inventory, without snapshotting unrelated payload fields or turning this slice into broader handoff testing.
  </action>
  <verify>Run `node --test tests/plan.test.cjs --test-name-pattern "phase-plan-index|plan index|snapshot-backed"`.</verify>
  <done>Regression coverage fails if plan indexing falls back to unconditional markdown reparsing, returns stale cache data after source edits, or breaks callers that expect the pre-Phase-152 JSON shape.</done>
</task>

</tasks>

<verification>
Run `node --test tests/plan.test.cjs && npm run build`.
</verification>

<success_criteria>
- `util:phase-plan-index` uses `PlanningCache`-backed reads on the hot path instead of reparsing every plan markdown file.
- Cache misses, stale rows, and Map fallback rebuild from source before returning data, preserving the existing additive contract.
- Automated coverage catches stale-cache regressions before handoff/resume work builds on this read path.
</success_criteria>

<output>
After completion, create `.planning/phases/152-cached-handoffs-fresh-context-delivery/152-01-SUMMARY.md`
</output>
