---
phase: 152-cached-handoffs-fresh-context-delivery
plan: 05
type: execute
wave: 5
depends_on:
  - "152-04"
files_modified:
  - workflows/discuss-phase.md
  - workflows/transition.md
  - tests/discuss-phase-workflow.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - FLOW-08
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "Users can move through the standard `discuss -> research -> plan -> execute -> verify` loop across fresh context windows instead of relying on one long-lived chat transcript."
    - "`discuss` remains the only step that may restart cleanly without prior chain state, while yolo and `--auto` use the chain as an additive fast path rather than a replacement mode."
    - "No silent resume path bypasses the explicit summary, even after auto-advance wiring is added."
  artifacts:
    - path: "workflows/discuss-phase.md"
      provides: "chain-aware discuss entrypoint that can start fresh or hand off deterministically into the rest of the loop"
      contains: "--auto"
    - path: "workflows/transition.md"
      provides: "phase-transition wiring that preserves the existing yolo fast path while using handoff-aware resume logic"
      contains: "auto_advance"
    - path: "tests/discuss-phase-workflow.test.cjs"
      provides: "contract coverage for discuss clean-start and additive chaining behavior"
      contains: "discuss-phase"
  key_links:
    - from: "workflows/discuss-phase.md"
      to: "workflows/transition.md"
      via: "discuss can create a fresh run, then hand off into the normal loop without inventing a second orchestration model"
      pattern: "--auto"
    - from: "tests/integration.test.cjs"
      to: "workflows/discuss-phase.md"
      via: "integration coverage proves chain-aware discuss entry still preserves standalone and auto-advance behavior"
      pattern: "fresh-context"
---

<objective>
Finish the chain by wiring only the remaining discuss and transition edges, which keeps the power-user yolo path additive instead of turning Phase 152 into a broad workflow rewrite.

Purpose: Deliver the final orchestration slice of FLOW-08 for DO-116 by making `discuss` the clean-start exception, connecting it to the validated handoff pipeline, and preserving existing yolo/`--auto` continuation behavior as the fast path for chained delivery.

Output: Updated discuss and transition workflow contracts plus focused regression coverage for clean-start discuss, additive auto-advance chaining, and explicit-summary preservation.
</objective>

> **TDD Decision:** Skipped — this plan finishes workflow prompt wiring and auto-advance orchestration, so the deterministic floor classifies it as integration work rather than a behavior-sized feature slice suited to RED/GREEN/REFACTOR execution.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-CONTEXT.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-RESEARCH.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-04-PLAN.md
@workflows/discuss-phase.md
@workflows/transition.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `discuss` the clean-start exception and handoff producer for chained delivery</name>
  <files>workflows/discuss-phase.md, tests/discuss-phase-workflow.test.cjs</files>
  <action>
  Update `workflows/discuss-phase.md` so it can either start a fresh same-phase run when no chain state exists or route through the validated resume summary when a prior handoff artifact set is present.

  Preserve the additive constraint: only `discuss` may restart cleanly without prior chain state, and that clean-start path must replace the previous same-phase run only after the new handoff artifacts are durable. Keep the existing low-risk discuss acceleration behavior from Phase 151 intact while adding the new chain-aware branching.
  </action>
  <verify>Run `node --test tests/discuss-phase-workflow.test.cjs --test-name-pattern "discuss-phase|clean start|resume"`.</verify>
  <done>`discuss` becomes the only clean-start entrypoint for chained delivery, and automated coverage fails if it stops preserving the additive standalone path or the explicit resume-summary branch.</done>
</task>

<task type="auto">
  <name>Task 2: Fold chain continuation into existing transition auto-advance behavior</name>
  <files>workflows/transition.md, tests/integration.test.cjs</files>
  <action>
  Wire `workflows/transition.md` so the existing yolo/`--auto` phase-delivery fast path follows the validated handoff pipeline across fresh contexts instead of assuming one long-lived transcript.

  Keep the product surface intentionally familiar: no new persistence mode, no replacement for standalone commands, and no silent resume. Integration coverage should prove additive chaining through discuss into the rest of the loop while preserving the explicit summary gate introduced earlier in the phase.
  </action>
  <verify>Run `node --test tests/integration.test.cjs --test-name-pattern "fresh-context|auto-advance|summary"`.</verify>
  <done>Yolo/`--auto` uses the new handoff layer as the power-user fast path, while no silent resume path bypasses the explicit summary before continuation.</done>
</task>

</tasks>

<verification>
Run `node --test tests/discuss-phase-workflow.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- `discuss` is the only clean-start exception when no prior chain state exists.
- The existing yolo/`--auto` fast path continues through the validated handoff pipeline without introducing a separate persistence mode.
- No silent resume path bypasses the explicit summary before continuation.
</success_criteria>

<output>
After completion, create `.planning/phases/152-cached-handoffs-fresh-context-delivery/152-05-SUMMARY.md`
</output>
