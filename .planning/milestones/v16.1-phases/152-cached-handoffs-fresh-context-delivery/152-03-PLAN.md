---
phase: 152-cached-handoffs-fresh-context-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - "152-02"
files_modified:
  - src/commands/init.js
  - src/lib/questions.js
  - tests/init.test.cjs
autonomous: true
requirements:
  - FLOW-07
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "Fresh-context return surfaces a resume summary before chain continuation and offers exactly `resume`, `inspect`, or `restart` based on the latest valid handoff artifact."
    - "Resume targeting stays artifact-derived, so downstream workflow entrypoints can consume one deterministic contract instead of guessing from `STATE.md` or partial markdown artifacts."
    - "Inspection and restart guidance are available before continuation, keeping the handoff layer explicit and user-visible rather than silently resuming."
  artifacts:
    - path: "src/commands/init.js"
      provides: "resume summary payload and inspection data derived from handoff validation"
      contains: "resume"
    - path: "tests/init.test.cjs"
      provides: "resume summary coverage for exact option shape and latest-valid targeting"
      contains: "resume summary"
  key_links:
    - from: "src/commands/init.js"
      to: "src/lib/phase-handoff.js"
      via: "resume entrypoints expose exact options and latest-valid state by reading the validated handoff artifact set"
      pattern: "resume"
    - from: "tests/init.test.cjs"
      to: "src/commands/init.js"
      via: "tests lock the exact `resume` / `inspect` / `restart` option contract before downstream workflow wiring consumes it"
      pattern: "resume summary"
---

<objective>
Add the resume summary contract first so later workflow gating can consume one deterministic handoff-entry surface instead of re-solving re-entry UX inside multiple workflow files.

Purpose: Deliver the resume-summary half of FLOW-07 for DO-116 by exposing exact resume choices, inspection data, and artifact-derived targeting before any downstream workflow resumes or restarts.

Output: Resume-summary init data, exact question/option contract updates, and targeted init coverage for latest-valid targeting and explicit inspect/restart guidance.
</objective>

> **TDD Decision:** Skipped — this plan defines resume-entry orchestration and inspection contract behavior, so the deterministic floor classifies it as tooling integration rather than a behavior-first RED/GREEN/REFACTOR slice.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-CONTEXT.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-RESEARCH.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-02-PLAN.md
@src/commands/init.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose exact resume choices and chain inspection data before continuation</name>
  <files>src/commands/init.js, src/lib/questions.js, tests/init.test.cjs</files>
  <action>
  Extend the relevant init/resume entrypoints so a fresh-context return presents a summary of the active phase handoff, the latest valid step, produced artifacts, and the next safe command before any continuation happens.

  Keep the interaction contract exact: `resume`, `inspect`, `restart` only. Derive the target from the latest valid artifact, never from `STATE.md`, and expose repair guidance when validation or freshness checks fail. This plan should not yet auto-chain `discuss`; it exists to make later workflow wiring deterministic and inspectable.
  </action>
  <verify>Run `node --test tests/init.test.cjs --test-name-pattern "resume summary|inspect|restart|latest valid"`.</verify>
  <done>Fresh-context entrypoints present an explicit summary first, expose exactly `resume` / `inspect` / `restart`, and derive the resume target from validated handoff data.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the resume-summary contract with focused init coverage</name>
  <files>tests/init.test.cjs</files>
  <action>
  Add focused init tests for newest-valid selection, corrupt-latest fallback to the latest valid artifact, and exact `resume` / `inspect` / `restart` option shape so downstream workflow plans can depend on a stable re-entry contract.

  Keep the coverage narrow to init/resume behavior. Do not fold downstream workflow gating or auto-advance into this slice; this plan exists specifically to reduce scope and establish one stable contract for later plans to consume.
  </action>
  <verify>Run `node --test tests/init.test.cjs --test-name-pattern "resume summary|inspect|restart|latest valid"`.</verify>
  <done>Resume-summary coverage fails if the option contract drifts, if latest-valid targeting stops being artifact-derived, or if inspect/restart guidance disappears from fresh-context re-entry.</done>
</task>

</tasks>

<verification>
Run `node --test tests/init.test.cjs && npm run build`.
</verification>

<success_criteria>
- Fresh-context return shows a validated resume summary first and offers exactly `resume`, `inspect`, and `restart`.
- Resume targeting is derived from the latest valid handoff artifact instead of `STATE.md` or partial markdown heuristics.
- Inspect and restart guidance are available before downstream workflow continuation begins.
</success_criteria>

<output>
After completion, create `.planning/phases/152-cached-handoffs-fresh-context-delivery/152-03-SUMMARY.md`
</output>
