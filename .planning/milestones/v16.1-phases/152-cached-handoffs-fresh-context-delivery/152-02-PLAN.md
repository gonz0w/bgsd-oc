---
phase: 152-cached-handoffs-fresh-context-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - "152-01"
files_modified:
  - src/lib/phase-handoff.js
  - src/commands/state.js
  - src/lib/constants.js
  - src/router.js
  - tests/state.test.cjs
autonomous: true
requirements:
  - FLOW-07
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "Each discuss/research/plan/execute/verify step can persist one compact same-run handoff artifact that later fresh-context invocations can validate and inspect deterministically."
    - "Starting a new run for the same phase replaces the previous same-phase artifact set without introducing a separate pointer file or duplicating the source of truth in `STATE.md`."
    - "Resume targeting metadata is derived from the latest valid artifact so later init/workflow entrypoints do not need to guess continuation from `STATE.md` or stray markdown files."
  artifacts:
    - path: "src/lib/phase-handoff.js"
      provides: "deterministic per-step handoff schema, validation, selection, and cleanup helpers"
      contains: "latest_valid_step"
    - path: "src/commands/state.js"
      provides: "command surface for writing and updating phase handoff artifacts"
      contains: "handoff"
    - path: "tests/state.test.cjs"
      provides: "artifact write, replacement, and validation coverage"
      contains: "phase handoff"
  key_links:
    - from: "src/commands/state.js"
      to: "src/lib/phase-handoff.js"
      via: "state-level commands persist and validate per-step artifact files through one shared handoff helper"
      pattern: "write"
    - from: "src/lib/phase-handoff.js"
      to: "src/commands/state.js"
      via: "shared validation and replacement rules stay centralized before later resume and workflow entrypoints consume them"
      pattern: "latest_valid_step"
---

<objective>
Create the deterministic handoff state layer before any resume UX or workflow rewiring so later chain orchestration can stay thin, fail closed, and fit the safer 4-plan split.

Purpose: Deliver the durable-state half of FLOW-07 for DO-116 by adding compact per-step handoff artifacts, same-phase replacement rules, and latest-valid-artifact selection without treating markdown continuity as the machine source of truth.

Output: A shared handoff-state helper, command surfaces for writing and validating artifacts, and regression coverage for schema validation, replacement behavior, and deterministic latest-valid selection.
</objective>

> **TDD Decision:** Skipped — this plan establishes resumable workflow state, command contracts, and validation rules, so the deterministic floor classifies it as orchestration infrastructure rather than feature logic suited to a RED/GREEN/REFACTOR cycle.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-CONTEXT.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-RESEARCH.md
@.planning/phases/152-cached-handoffs-fresh-context-delivery/152-01-PLAN.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-03-PLAN.md
@src/lib/release/state.js
@src/commands/state.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add compact per-step phase handoff storage, validation, and same-phase replacement</name>
  <files>src/lib/phase-handoff.js, src/commands/state.js, src/lib/constants.js, src/router.js</files>
  <action>
  Introduce a dedicated phase handoff helper modeled on the release-state discipline: compact JSON artifacts, explicit versioning, per-step status, source freshness fingerprints, and deterministic `latest_valid_step` selection across `discuss`, `research`, `plan`, `execute`, and `verify`.

  Add command support to write and update these artifacts at durable checkpoints, replace the prior same-phase run only after the new artifact set is valid, and avoid any separate current-pointer artifact. Keep markdown canonical, keep `STATE.md` continuity human-oriented only, and return repair/restart guidance when validation fails.
  </action>
  <verify>Run `node --test tests/state.test.cjs --test-name-pattern "phase handoff|latest valid|same-phase replacement"`.</verify>
  <done>Phase handoff artifacts exist as the machine source of truth, they replace old same-phase runs deterministically, and validation can choose the latest valid step without consulting a pointer file.</done>
</task>

<task type="auto">
  <name>Task 2: Lock durable handoff behavior with focused state-level regression coverage</name>
  <files>tests/state.test.cjs</files>
  <action>
  Add focused state tests for newest-artifact-valid, newest-artifact-corrupt, same-phase rerun replacement, and source-fingerprint validation so later init/workflow plans can consume this layer without re-proving the core state rules.

  Keep the tests state-oriented: prove the artifact schema, cleanup ordering, and latest-valid selection logic directly rather than coupling them to resume UX wording or workflow auto-advance, which land in the next two plans.
  </action>
  <verify>Run `node --test tests/state.test.cjs --test-name-pattern "phase handoff|latest valid|fingerprint|same-phase replacement"`.</verify>
  <done>Automated coverage fails if handoff artifacts stop replacing old same-phase runs safely, if validation cannot identify the latest valid artifact, or if stale-source checks stop surfacing deterministic repair state.</done>
</task>

</tasks>

<verification>
Run `node --test tests/state.test.cjs && npm run build`.
</verification>

<success_criteria>
- Compact per-step handoff artifacts persist durable same-run state for discuss/research/plan/execute/verify.
- New same-phase runs replace the older artifact set without a separate pointer artifact or duplicate machine state in `STATE.md`.
- Latest-valid-artifact metadata exists for later resume surfaces to consume without guessing continuation from `STATE.md` or stray markdown artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/152-cached-handoffs-fresh-context-delivery/152-02-SUMMARY.md`
</output>
