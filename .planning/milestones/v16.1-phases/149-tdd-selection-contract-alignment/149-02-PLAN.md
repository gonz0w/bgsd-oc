---
phase: 149-tdd-selection-contract-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - "149-01"
files_modified:
  - agents/bgsd-planner.md
  - workflows/plan-phase.md
  - src/lib/helpers.js
  - src/commands/roadmap.js
  - src/plugin/parsers/roadmap.js
  - src/commands/init.js
  - tests/plan.test.cjs
  - tests/init.test.cjs
  - tests/plugin.test.cjs
autonomous: true
requirements:
  - TDD-01
  - TDD-02
intent:
  outcome_ids:
    - DO-115
must_haves:
  truths:
    - "Planning an implementation phase always records a deterministic TDD selected/skipped decision even when the roadmap omits a `**TDD:**` hint."
    - "Each implementation plan shows a short human-readable TDD rationale in the body, not hidden in frontmatter."
    - "Legacy roadmap or plan artifacts with older TDD metadata shapes are still accepted, then normalized and rewritten back to disk on read so the canonical contract becomes persistent."
  artifacts:
    - path: "agents/bgsd-planner.md"
      provides: "planner contract requiring deterministic TDD choice and visible rationale callout"
      contains: "TDD"
    - path: "workflows/plan-phase.md"
      provides: "workflow prompt that always asks for a TDD decision, even when roadmap hint is null"
      contains: "TDD hint"
    - path: "src/commands/roadmap.js"
      provides: "roadmap phase reader that preserves backward compatibility while normalizing TDD metadata"
      contains: "tdd"
    - path: "src/plugin/parsers/roadmap.js"
      provides: "plugin-side roadmap parser aligned to the same normalization rules"
      contains: "tdd"
  key_links:
    - from: "workflows/plan-phase.md"
      to: "agents/bgsd-planner.md"
      via: "workflow prompt passes the deterministic TDD-selection contract into the planner agent"
      pattern: "TDD hint"
    - from: "src/commands/roadmap.js"
      to: "src/plugin/parsers/roadmap.js"
      via: "CLI and plugin roadmap readers share the same backward-compatible TDD normalization rules"
      pattern: "tdd"
    - from: "src/commands/init.js"
      to: "workflows/plan-phase.md"
      via: "plan-phase bootstrap surfaces normalized TDD context to downstream planning"
      pattern: "init:plan-phase"
---

<objective>
Make TDD selection deterministic and visible during planning.

Purpose: Deliver the Phase 149 planning half of DO-115 so omitted roadmap hints no longer lead to silent or discretionary TDD choices, and legacy planning artifacts converge to one persisted contract instead of drifting forever.

Output: Planner/workflow guidance for deterministic TDD selection, visible plan-body rationale rules, normalization-on-read helpers, and targeted parser/init tests.
</objective>

> **TDD Decision:** Skipped — this plan changes planner/parser behavior and migration rules, so the deterministic floor correctly classifies it as tooling work rather than a feature that benefits from a TDD execution cycle.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/149-tdd-selection-contract-alignment/149-CONTEXT.md
@.planning/phases/149-tdd-selection-contract-alignment/149-01-PLAN.md
@agents/bgsd-planner.md
@workflows/plan-phase.md
@src/commands/roadmap.js
@src/plugin/parsers/roadmap.js
@src/commands/init.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the deterministic TDD floor and visible rationale rule into planning guidance</name>
  <files>agents/bgsd-planner.md, workflows/plan-phase.md</files>
  <action>
  Update the planner agent and plan-phase workflow so every implementation plan must make an explicit TDD selected/skipped decision, even when ROADMAP.md has no `**TDD:**` field.

  Implement the locked rule exactly: default to TDD when work introduces or changes testable behavior with clear expected outcomes; skip clearly docs-only, config-only, or layout-only work. Require a short visible plan-body rationale callout that explains how the rule applied for that plan, and keep that rationale out of frontmatter.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs workflow:verify-structure workflows/plan-phase.md` and confirm the planner prompt explicitly covers omitted hints plus visible rationale.</verify>
  <done>Planner instructions now force deterministic TDD decisions and a visible body rationale instead of treating omitted hints as silent discretion.</done>
</task>

<task type="auto">
  <name>Task 2: Implement persisted normalization-on-read for legacy TDD metadata</name>
  <files>src/lib/helpers.js, src/commands/roadmap.js, src/plugin/parsers/roadmap.js, src/commands/init.js</files>
  <action>
  Add a shared normalization path that accepts older roadmap or plan TDD metadata shapes, converts them to the canonical Phase 149 contract, and writes the normalized result back to disk when those files are read.

  Keep the behavior additive and backward-compatible: older artifacts must still load, but normalization cannot stay in memory only. Reuse existing helper/frontmatter patterns, avoid new dependencies, and keep the implementation compatible with the single-file CLI build.
  </action>
  <verify>Run `node --test tests/plan.test.cjs tests/init.test.cjs tests/plugin.test.cjs`.</verify>
  <done>Legacy artifacts continue to work, and normalized TDD metadata is persisted back to disk so future reads see the canonical shape immediately.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused parser/init coverage for omitted hints, malformed hints, and rewrite-on-read behavior</name>
  <files>tests/plan.test.cjs, tests/init.test.cjs, tests/plugin.test.cjs</files>
  <action>
  Extend the roadmap and planning tests with fixtures that cover three cases: omitted `**TDD:**` hints still yielding explicit decisions, malformed or legacy hints normalizing cleanly, and normalization being persisted back to disk after read.

  Keep these tests scoped to selection/rationale/normalization behavior only. Do not cross into Phase 150 semantic gate testing or end-to-end TDD execution proof.
  </action>
  <verify>Run `node --test tests/plan.test.cjs tests/init.test.cjs tests/plugin.test.cjs` again after adding the new fixtures.</verify>
  <done>Automated coverage proves deterministic selection works with null hints and that backward-compatible normalization rewrites artifacts instead of silently tolerating drift.</done>
</task>

</tasks>

<verification>
Run `node --test tests/plan.test.cjs tests/init.test.cjs tests/plugin.test.cjs && npm run build`.
</verification>

<success_criteria>
- Omitted roadmap TDD hints still produce explicit selected/skipped planning decisions.
- Each implementation plan is instructed to include a short visible body rationale.
- Legacy TDD metadata is normalized on read and persisted back to disk without breaking older artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/149-tdd-selection-contract-alignment/149-02-SUMMARY.md`
</output>
