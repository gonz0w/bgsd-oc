---
phase: 149-tdd-selection-contract-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/tdd-execution/SKILL.md
  - skills/tdd-execution/tdd-reference.md
  - workflows/tdd.md
  - workflows/execute-plan.md
  - templates/plans/tdd.md
  - src/lib/constants.js
  - src/lib/command-help.js
  - tests/agent.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - TDD-04
intent:
  outcome_ids:
    - DO-115
must_haves:
  truths:
    - "One canonical skill under `skills/tdd-execution/` defines the TDD structure, terms, command names, and expected artifacts used everywhere else."
    - "Executor-facing workflow text, plan templates, and CLI help all use the same TDD language instead of drifting into competing contracts."
    - "Phase 149 stops at contract alignment and does not add new RED/GREEN/REFACTOR semantic gates or end-to-end execution proof reserved for Phase 150."
  artifacts:
    - path: "skills/tdd-execution/SKILL.md"
      provides: "canonical TDD contract and planner/executor rules"
      contains: "TDD"
    - path: "skills/tdd-execution/tdd-reference.md"
      provides: "detailed contract reference aligned to the canonical skill"
      contains: "validate-red"
    - path: "workflows/tdd.md"
      provides: "executor workflow text aligned to the canonical contract"
      contains: "validate-green"
    - path: "src/lib/constants.js"
      provides: "CLI help text using the same TDD command names and artifact language"
      contains: "execute:tdd"
  key_links:
    - from: "skills/tdd-execution/SKILL.md"
      to: "workflows/tdd.md"
      via: "workflow instructions explicitly defer to the canonical skill for TDD contract terms"
      pattern: "tdd-execution"
    - from: "skills/tdd-execution/SKILL.md"
      to: "templates/plans/tdd.md"
      via: "template wording mirrors the canonical TDD structure and artifacts"
      pattern: "type: tdd"
    - from: "skills/tdd-execution/SKILL.md"
      to: "src/lib/constants.js"
      via: "CLI help text reuses the same command names and expected TDD phases"
      pattern: "execute:tdd"
---

<objective>
Establish the single canonical TDD contract for Phase 149.

Purpose: Deliver DO-115's alignment layer first so planner, executor, docs, and CLI help all reference one source of truth before any later execution-hardening work begins.

Output: Canonical `skills/tdd-execution` contract updates, aligned workflow/template/help copy, and focused contract tests.
</objective>

> **TDD Decision:** Skipped — this plan aligns workflow/docs/help contract text, so the deterministic floor correctly treats it as non-behavioral contract work rather than a RED/GREEN/REFACTOR feature.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/149-tdd-selection-contract-alignment/149-CONTEXT.md
@skills/tdd-execution/SKILL.md
@skills/tdd-execution/tdd-reference.md
@workflows/tdd.md
@workflows/execute-plan.md
@templates/plans/tdd.md
@src/lib/constants.js
@src/lib/command-help.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize the TDD contract inside the shared skill and reference doc</name>
  <files>skills/tdd-execution/SKILL.md, skills/tdd-execution/tdd-reference.md</files>
  <action>
  Rewrite the TDD contract so `skills/tdd-execution/SKILL.md` is explicitly the single authoritative source for structure, terminology, command names, and expected artifacts.

  Bring `tdd-reference.md` into exact agreement with that skill instead of letting it evolve as a parallel contract. Keep Phase 149 scoped to contract alignment only: no new execute:tdd semantics, no Phase 150 proof requirements, and no new dependencies that would threaten the single-file CLI build.
  </action>
  <verify>Read the updated skill and reference files and confirm they use one shared vocabulary for RED/GREEN/REFACTOR steps, `execute:tdd` subcommands, and expected artifacts.</verify>
  <done>The canonical contract lives in one skill under `skills/`, and the detailed reference is subordinate to that skill instead of competing with it.</done>
</task>

<task type="auto">
  <name>Task 2: Align workflow, template, and CLI help text to the canonical skill</name>
  <files>workflows/tdd.md, workflows/execute-plan.md, templates/plans/tdd.md, src/lib/constants.js, src/lib/command-help.js</files>
  <action>
  Update the executor workflow, TDD plan template, and CLI help surfaces so they all point back to the canonical skill and use the same command names and artifact expectations.

  Remove or rewrite any wording that invents a second contract, especially if it implies extra semantic gates or end-to-end proof that belong to Phase 150. Keep all help text additive and backward-compatible so older commands still read cleanly after the bundle rebuild.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs execute:tdd --help` and `node bin/bgsd-tools.cjs workflow:verify-structure workflows/tdd.md`.</verify>
  <done>The workflow/template/help surfaces all describe the same TDD contract and visibly defer to `skills/tdd-execution/` as the source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests that lock contract alignment in place</name>
  <files>tests/agent.test.cjs, tests/workflow.test.cjs</files>
  <action>
  Add or update targeted tests that fail when the canonical skill, workflow text, or CLI help drift apart on TDD terminology, command names, or expected artifacts.

  Keep the coverage narrow and fixture-driven: the goal is to catch contract drift early without turning this phase into broad execution validation. The tests should prove alignment, not Phase 150 semantics.
  </action>
  <verify>Run `node --test tests/agent.test.cjs tests/workflow.test.cjs`.</verify>
  <done>There is automated coverage proving the canonical TDD contract stays synchronized across the skill, workflow, and CLI help copy.</done>
</task>

</tasks>

<verification>
Run `node --test tests/agent.test.cjs tests/workflow.test.cjs && npm run build`.
</verification>

<success_criteria>
- The canonical TDD contract is authored once under `skills/tdd-execution/`.
- Workflow, template, and CLI help text use the same TDD terms and commands.
- No Phase 150 semantic-gate or end-to-end proof scope leaks into this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/149-tdd-selection-contract-alignment/149-01-SUMMARY.md`
</output>
