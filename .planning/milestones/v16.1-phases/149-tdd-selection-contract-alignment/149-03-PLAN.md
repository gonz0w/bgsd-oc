---
phase: 149-tdd-selection-contract-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - "149-01"
  - "149-02"
files_modified:
  - agents/bgsd-plan-checker.md
  - workflows/plan-phase.md
  - templates/roadmap.md
  - tests/verify.test.cjs
  - tests/workflow.test.cjs
  - tests/contracts.test.cjs
autonomous: true
requirements:
  - TDD-03
  - TDD-04
intent:
  outcome_ids:
    - DO-115
must_haves:
  truths:
    - "Plan checking treats `required` TDD hints as blocker-level violations, `recommended` hints as warnings, and omitted hints as informational output instead of silence."
    - "Checker/orchestrator guidance uses the same TDD terms and selection contract already established in the canonical skill and planner guidance."
    - "Phase 149 checker behavior stops at selection/rationale severity and does not harden `execute:tdd` semantics or claim end-to-end proof."
  artifacts:
    - path: "agents/bgsd-plan-checker.md"
      provides: "strict checker severity ladder for TDD compliance"
      contains: "blocker"
    - path: "workflows/plan-phase.md"
      provides: "orchestrator/checker prompt flow aligned to the same severity ladder"
      contains: "warning"
    - path: "templates/roadmap.md"
      provides: "author-facing roadmap guidance that matches the checker meanings of required, recommended, and omitted hints"
      contains: "recommended"
    - path: "tests/verify.test.cjs"
      provides: "targeted coverage for TDD severity classification behavior"
      contains: "TDD"
  key_links:
    - from: "agents/bgsd-plan-checker.md"
      to: "workflows/plan-phase.md"
      via: "checker invocation and revision loop preserve the same severity ladder end to end"
      pattern: "severity"
    - from: "templates/roadmap.md"
      to: "agents/bgsd-plan-checker.md"
      via: "roadmap author guidance matches checker enforcement semantics for TDD hints"
      pattern: "required"
    - from: ".planning/phases/149-tdd-selection-contract-alignment/149-01-PLAN.md"
      to: ".planning/phases/149-tdd-selection-contract-alignment/149-02-PLAN.md"
      via: "checker severity decisions consume the canonical contract and deterministic planner rules from earlier plans"
      pattern: "TDD"
---

<objective>
Finish Phase 149 by making TDD checking severity deterministic and aligned.

Purpose: Complete DO-115's pre-execution contract so planner review consistently reports blocker, warning, or informational TDD outcomes with no silent branch, while staying intentionally short of Phase 150 execution semantics.

Output: Tightened checker/orchestrator guidance, aligned roadmap-author documentation, and focused severity-contract tests.
</objective>

> **TDD Decision:** Skipped — this plan tightens checker and authoring contracts, which the deterministic floor correctly classifies as guidance/validation work rather than behavior-first product logic.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/149-tdd-selection-contract-alignment/149-CONTEXT.md
@.planning/phases/149-tdd-selection-contract-alignment/149-01-PLAN.md
@.planning/phases/149-tdd-selection-contract-alignment/149-02-PLAN.md
@agents/bgsd-plan-checker.md
@workflows/plan-phase.md
@templates/roadmap.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the strict TDD severity ladder in the checker and orchestration prompts</name>
  <files>agents/bgsd-plan-checker.md, workflows/plan-phase.md</files>
  <action>
  Update the checker contract so the TDD dimension no longer skips omitted hints silently. Required hints must yield blocker-level issues, recommended hints warnings, and omitted hints informational output that still reports the deterministic TDD decision path.

  Keep the language aligned with the Phase 149 canonical skill and deterministic planner floor. Do not add execution-time semantic validation or end-to-end proof obligations from Phase 150.
  </action>
  <verify>Read the checker and plan-phase workflow updates and confirm they explicitly map `required → blocker`, `recommended → warning`, and `omitted → info`.</verify>
  <done>The checker/orchestrator path now has one explicit TDD severity ladder with no silent omitted-hint branch.</done>
</task>

<task type="auto">
  <name>Task 2: Align roadmap-author guidance to the same checker meanings</name>
  <files>templates/roadmap.md</files>
  <action>
  Update roadmap template guidance so future roadmap authors see the same TDD meanings the checker now enforces: `required`, `recommended`, and omitted each have distinct downstream behavior.

  Make the wording explicitly compatible with the Phase 149 contract and Phase 150 boundary: omitted means informational selection reporting, not silence; it does not imply omitted phases should gain semantic `execute:tdd` enforcement.
  </action>
  <verify>Read `templates/roadmap.md` and confirm the TDD field guidance matches the checker ladder and still scopes semantic hardening to later work.</verify>
  <done>Author-facing roadmap instructions and checker behavior now describe the same TDD-hint meanings.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for severity ladder and omitted-hint reporting</name>
  <files>tests/verify.test.cjs, tests/workflow.test.cjs, tests/contracts.test.cjs</files>
  <action>
  Add or update targeted tests that fail when the TDD severity ladder drifts from the locked Phase 149 contract, especially the omitted-hint informational path.

  Cover prompt/contract surfaces only: severity wording, no-silence behavior, and alignment with roadmap guidance. Do not turn these tests into Phase 150 execution-proof coverage.
  </action>
  <verify>Run `node --test tests/verify.test.cjs tests/workflow.test.cjs tests/contracts.test.cjs`.</verify>
  <done>Automated coverage protects the blocker/warning/info ladder and prevents future regressions back to silent omitted-hint behavior.</done>
</task>

</tasks>

<verification>
Run `node --test tests/verify.test.cjs tests/workflow.test.cjs tests/contracts.test.cjs && npm run build`.
</verification>

<success_criteria>
- The checker severity ladder is explicit for required, recommended, and omitted TDD hints.
- Omitted hints still surface informational TDD behavior instead of silence.
- Roadmap-author, planner, checker, and canonical skill wording all stay aligned without importing Phase 150 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/149-tdd-selection-contract-alignment/149-03-SUMMARY.md`
</output>
