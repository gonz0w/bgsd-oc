---
phase: 154-end-to-end-fresh-context-proof-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - "154-01"
files_modified:
  - src/commands/init.js
  - workflows/tdd.md
  - workflows/execute-phase.md
  - workflows/verify-work.md
  - tests/init.test.cjs
  - tests/summary-generate.test.cjs
  - tests/workflow.test.cjs
  - tests/integration.test.cjs
autonomous: true
gap_closure: true
requirements:
  - TDD-06
  - FLOW-08
intent:
  outcome_ids:
    - DO-115
    - DO-116
must_haves:
  truths:
    - "One integrated regression proves discuss -> research -> plan -> execute -> verify preserves durable handoffs, TDD proof continuity, and downstream proof rendering together in one real flow."
    - "Corrupt-newest fallback, stale-source blocking, and refreshed resume behavior still compose after proof persistence lands."
    - "Any resume inspection or workflow wording changes describe the final proof-delivery behavior only, without reopening Phase 150 semantics or Phase 153 freshness rules."
  artifacts:
    - path: "tests/integration.test.cjs"
      provides: "milestone-closing end-to-end regression for fresh-context proof delivery"
      contains: "fresh-context"
    - path: "tests/init.test.cjs"
      provides: "resume-summary coverage for any additive proof metadata or inspection surface"
      contains: "resume summary"
    - path: "tests/summary-generate.test.cjs"
      provides: "proof-rendering safety net after resumable execution creates the audit artifact"
      contains: "TDD Audit Trail"
  key_links:
    - from: "tests/integration.test.cjs"
      to: "src/commands/init.js"
      via: "the composed regression exercises real resume entrypoints after production-created proof and durable handoffs exist"
      pattern: "resume_summary"
    - from: "tests/summary-generate.test.cjs"
      to: "workflows/verify-work.md"
      via: "downstream proof rendering stays intact after fresh-context continuation reaches summary generation"
      pattern: "TDD Audit Trail"
    - from: "tests/workflow.test.cjs"
      to: "workflows/tdd.md"
      via: "wording only changes where runtime behavior is now more explicit about durable proof preservation"
      pattern: "proof"
---

<objective>
Finish the gap-closure phase with one milestone-closing proof that shows the resumable production chain now carries and re-renders TDD evidence end to end for both DO-115 and DO-116.

Purpose: combine the existing fresh-context chain and isolated TDD proof groundwork into one realistic regression, then tighten only the additive inspection or wording surfaces that changed because proof delivery is now real in production.

Output: composed end-to-end regression coverage plus any narrow resume-summary, rendering, or workflow contract updates required to match the landed runtime behavior.
</objective>

> **TDD Decision:** Skipped — this slice is end-to-end regression locking and contract reconciliation around an existing workflow chain, so the deterministic floor classifies it as proof integration rather than a standalone RED/GREEN/REFACTOR implementation plan.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/v16.1-MILESTONE-AUDIT.md
@.planning/phases/154-end-to-end-fresh-context-proof-delivery/154-CONTEXT.md
@.planning/phases/154-end-to-end-fresh-context-proof-delivery/154-RESEARCH.md
@.planning/phases/154-end-to-end-fresh-context-proof-delivery/154-01-PLAN.md
@.planning/phases/150-tdd-execution-semantics-proof/150-02-PLAN.md
@.planning/phases/153-production-handoff-persistence-resume-freshness/153-03-PLAN.md
@src/commands/init.js
@workflows/tdd.md
@workflows/execute-phase.md
@workflows/verify-work.md
@tests/init.test.cjs
@tests/summary-generate.test.cjs
@tests/workflow.test.cjs
@tests/integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one composed production regression for fresh-context proof delivery</name>
  <files>tests/integration.test.cjs, tests/init.test.cjs</files>
  <action>
  Extend the existing production-chain integration so it uses a real `type: tdd` fixture and a production-created proof artifact rather than manually seeded handoffs or sidecars, then drive the chain far enough to prove discuss -> research -> plan -> execute -> verify preserves resumable continuity and leaves proof available to downstream consumers.

  Assert the combined Phase 154 boundary in one place: durable handoffs persist, resume entrypoints still honor latest-valid fallback and stale-source blocking, and the refreshed chain becomes resumable again after current-source artifacts are rewritten. Do not reopen Phase 150 validator semantics or broaden into unrelated milestone debt.
  </action>
  <verify>Run `node --test tests/integration.test.cjs tests/init.test.cjs --test-name-pattern "fresh-context|TDD|resume summary|stale source|latest valid"`.</verify>
  <done>One integrated regression now fails if the production chain stops preserving TDD proof, if resume entrypoints lose freshness protections, or if the repaired chain can no longer continue end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Lock additive proof rendering and wording contracts to the final runtime behavior</name>
  <files>src/commands/init.js, workflows/tdd.md, workflows/execute-phase.md, workflows/verify-work.md, tests/summary-generate.test.cjs, tests/workflow.test.cjs</files>
  <action>
  Add or update only the focused rendering, inspection, and workflow contract checks required by the landed runtime behavior: summary generation should still render preserved TDD proof, resume inspection should expose any new proof metadata only if runtime now surfaces it, and wording should describe durable proof continuity precisely where the chain became more specific.

  Keep the cleanup narrow and additive. Do not invent a second proof format, change summary structure beyond what the existing audit contract needs, or require proof state for standalone non-TDD use.
  </action>
  <verify>Run `node --test tests/summary-generate.test.cjs tests/workflow.test.cjs tests/integration.test.cjs --test-name-pattern "proof|TDD Audit Trail|resume|verify"`.</verify>
  <done>Rendering, inspection, and workflow tests all match the final proof-delivery behavior, with no scope creep into new orchestration models or reopened validator semantics.</done>
</task>

</tasks>

<verification>
Run `node --test tests/init.test.cjs tests/summary-generate.test.cjs tests/workflow.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- A single realistic regression proves fresh-context resume, durable handoff continuity, and TDD proof rendering together in one end-to-end flow.
- Corrupt-newest fallback, stale-source blocking, and refreshed continuation remain compatible with that proof-delivery chain.
- Any inspection or wording updates stay narrowly aligned to runtime changes and keep non-TDD standalone behavior additive.
</success_criteria>

<output>
After completion, create `.planning/phases/154-end-to-end-fresh-context-proof-delivery/154-02-SUMMARY.md`
</output>
