---
phase: 154-end-to-end-fresh-context-proof-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/misc.js
  - src/lib/phase-handoff.js
  - src/commands/state.js
  - workflows/tdd.md
  - workflows/execute-phase.md
  - workflows/verify-work.md
  - tests/state.test.cjs
  - tests/integration.test.cjs
autonomous: true
gap_closure: true
requirements:
  - TDD-06
  - FLOW-08
intent:
  outcome_ids:
    - DO-115
    - DO-116
must_haves:
  truths:
    - "A real production `type: tdd` execution path writes the canonical `{phase}-{plan}-TDD-AUDIT.json` sidecar without fixture-only manual setup."
    - "Execute -> verify fresh-context handoff refreshes preserve the proof artifact or its deterministic metadata so downstream steps do not silently drop TDD evidence."
    - "Summary and verification steps can still read and render preserved TDD proof after resume while non-TDD and standalone flows remain additive."
  artifacts:
    - path: "src/commands/misc.js"
      provides: "canonical TDD audit artifact write/read plumbing for production summary generation"
      contains: "TDD-AUDIT.json"
    - path: "src/lib/phase-handoff.js"
      provides: "deterministic handoff context preservation across later step writes"
      contains: "buildPhaseHandoffPayload"
    - path: "workflows/execute-phase.md"
      provides: "execute-step handoff instructions that preserve proof continuity before verify"
      contains: "handoff write"
    - path: "tests/integration.test.cjs"
      provides: "production-path proof persistence coverage across fresh-context handoff transitions"
      contains: "TDD-AUDIT"
  key_links:
    - from: "workflows/tdd.md"
      to: "src/commands/misc.js"
      via: "the real RED/GREEN/REFACTOR flow persists the existing proof contract to the durable sidecar instead of leaving proof only in transient command output"
      pattern: "TDD-AUDIT.json"
    - from: "workflows/execute-phase.md"
      to: "src/lib/phase-handoff.js"
      via: "execute handoff refresh keeps proof metadata available to later verify and summary steps"
      pattern: "context"
    - from: "workflows/verify-work.md"
      to: "src/commands/misc.js"
      via: "verify-side continuation can still reach proof-aware summary rendering after the handoff chain advances"
      pattern: "summary"
---

<objective>
Close the missing production proof-delivery seam first so DO-115 and DO-116 are satisfied by the real resumable chain instead of by isolated fixtures plus a separate handoff runtime.

Purpose: make a production `type: tdd` execution path self-write the existing TDD proof sidecar, preserve that proof across execute -> verify handoff transitions, and keep the downstream proof reader additive for non-TDD flows.

Output: production TDD proof-write plumbing, deterministic handoff proof preservation, and focused regression coverage that fails if the fresh-context chain still drops or depends on manually seeded proof.
</objective>

> **TDD Decision:** Skipped — this slice is shared orchestration and proof-persistence plumbing across existing workflow surfaces, so the deterministic floor classifies it as integration work rather than a feature-sized RED/GREEN/REFACTOR plan.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/v16.1-MILESTONE-AUDIT.md
@.planning/phases/154-end-to-end-fresh-context-proof-delivery/154-CONTEXT.md
@.planning/phases/154-end-to-end-fresh-context-proof-delivery/154-RESEARCH.md
@.planning/phases/150-tdd-execution-semantics-proof/150-02-PLAN.md
@.planning/phases/153-production-handoff-persistence-resume-freshness/153-03-PLAN.md
@src/commands/misc.js
@src/lib/phase-handoff.js
@src/commands/state.js
@workflows/tdd.md
@workflows/execute-phase.md
@workflows/verify-work.md
@tests/state.test.cjs
@tests/integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the real `type: tdd` execution path self-write the canonical proof sidecar</name>
  <files>src/commands/misc.js, workflows/tdd.md, tests/integration.test.cjs</files>
  <action>
  Add one additive production write path for `{phase}-{plan}-TDD-AUDIT.json` that reuses the existing RED/GREEN/REFACTOR proof schema already consumed by summary generation instead of relying on fixture-only `fs.writeFileSync(...)` setup.

  Keep the Phase 150 validator semantics, commit trailers, and summary rendering contract intact. The new behavior should activate only when a real `type: tdd` plan produces structured proof, materialize the sidecar before downstream summary work, and remain a no-op for execute plans and standalone non-TDD flows.
  </action>
  <verify>Run `node --test tests/integration.test.cjs --test-name-pattern "TDD|proof|type:tdd"`.</verify>
  <done>A production `type: tdd` flow now leaves the same durable audit artifact that summary generation already knows how to read, without changing the established proof schema.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve proof continuity across execute and verify handoff refreshes</name>
  <files>src/lib/phase-handoff.js, src/commands/state.js, workflows/execute-phase.md, workflows/verify-work.md, tests/state.test.cjs, tests/integration.test.cjs</files>
  <action>
  Choose and implement one deterministic carry-forward contract so later handoff writes do not silently discard proof state: either merge prior proof metadata into subsequent `context` payloads or preserve one canonical proof-path reference that downstream steps can reuse.

  Keep the artifact schema additive, preserve Phase 153 latest-valid and stale-source behavior, and do not make proof mandatory for non-TDD chains. The result must survive later `execute` and `verify` handoff writes without inventing a new continuation model.
  </action>
  <verify>Run `node --test tests/state.test.cjs tests/integration.test.cjs --test-name-pattern "handoff|proof|fresh-context|verify"`.</verify>
  <done>The execute -> verify fresh-context boundary keeps TDD proof available to downstream resume and summary steps, while non-TDD handoffs stay backward-compatible.</done>
</task>

</tasks>

<verification>
Run `node --test tests/state.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- The real production `type: tdd` path writes the canonical `TDD-AUDIT.json` sidecar before downstream proof consumers run.
- Execute and verify handoff refreshes preserve proof continuity instead of dropping TDD state on later writes.
- Summary behavior remains backward-compatible for non-TDD plans while resumed TDD chains keep durable proof available.
</success_criteria>

<output>
After completion, create `.planning/phases/154-end-to-end-fresh-context-proof-delivery/154-01-SUMMARY.md`
</output>
