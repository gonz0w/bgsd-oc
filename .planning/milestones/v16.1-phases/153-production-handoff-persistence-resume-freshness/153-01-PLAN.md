---
phase: 153-production-handoff-persistence-resume-freshness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/helpers.js
  - src/lib/phase-handoff.js
  - src/commands/state.js
  - workflows/discuss-phase.md
  - workflows/research-phase.md
  - workflows/plan-phase.md
  - workflows/execute-phase.md
  - workflows/verify-work.md
  - workflows/transition.md
  - tests/state.test.cjs
  - tests/integration.test.cjs
autonomous: true
gap_closure: true
requirements:
  - FLOW-07
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "Running the standard discuss -> research -> plan -> execute -> verify path in production leaves valid handoff artifacts behind each durable checkpoint without manual `verify:state handoff write` setup."
    - "Refreshing a handoff within the same production run preserves one deterministic resumable chain, so later steps read consistent run identity and continuation targets."
    - "Starting discuss cleanly for the same phase does not remove the previous resumable state until a new valid discuss handoff has been written."
  artifacts:
    - path: "src/lib/phase-handoff.js"
      provides: "shared production-safe handoff write path preserving current validation and replacement semantics"
      contains: "writePhaseHandoff"
    - path: "workflows/discuss-phase.md"
      provides: "explicit durable write step for clean-start and resumed discuss flow"
      contains: "durable"
    - path: "tests/integration.test.cjs"
      provides: "production-path regression proving handoffs are self-written instead of directly seeded"
      contains: "handoff"
  key_links:
    - from: "workflows/research-phase.md"
      to: "src/lib/phase-handoff.js"
      via: "research continuation refreshes the current run using the shared write path rather than hand-built artifact JSON"
      pattern: "handoff"
    - from: "tests/integration.test.cjs"
      to: "workflows/transition.md"
      via: "integration coverage proves the fresh-context chain only advances after durable artifacts exist"
      pattern: "fresh-context"
---

<objective>
Close the missing FLOW-07 producer wiring first so DO-116 is satisfied by the real production chain, not just by the handoff schema and test-only write command.

Purpose: make discuss, research, plan, execute, verify, and auto-advance transitions create or refresh deterministic handoff artifacts through one shared path while preserving standalone behavior and current replacement safety.

Output: shared handoff payload/write helper updates, production workflow write-path wiring, and regression coverage that proves the happy path no longer depends on direct handoff seeding.
</objective>

> **TDD Decision:** Skipped — this slice is cross-workflow wiring and shared runtime plumbing, so the deterministic floor does not justify a dedicated RED/GREEN/REFACTOR plan.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/v16.1-MILESTONE-AUDIT.md
@.planning/phases/153-production-handoff-persistence-resume-freshness/153-RESEARCH.md
@src/lib/phase-handoff.js
@src/commands/state.js
@workflows/discuss-phase.md
@workflows/research-phase.md
@workflows/plan-phase.md
@workflows/execute-phase.md
@workflows/verify-work.md
@workflows/transition.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one shared production handoff payload and write path</name>
  <files>src/lib/helpers.js, src/lib/phase-handoff.js, src/commands/state.js, tests/state.test.cjs</files>
  <action>
  Add a reusable runtime helper that constructs the canonical handoff payload for a phase step so production callers do not hand-build `run_id`, `source_fingerprint`, `summary`, or `resume_target` inconsistently.

  Keep `verify:state handoff write` backward compatible, preserve the current write-then-select-then-replace semantics in `src/lib/phase-handoff.js`, and make the shared path safe for both clean-start discuss and later same-run refreshes. Do not redesign the artifact schema.
  </action>
  <verify>Run `node --test tests/state.test.cjs --test-name-pattern "phase handoff|same-phase run|source fingerprint"`.</verify>
  <done>Production callers have one deterministic way to build and write valid handoff artifacts without changing the existing validation contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire production workflow checkpoints to write durable handoffs</name>
  <files>workflows/discuss-phase.md, workflows/research-phase.md, workflows/plan-phase.md, workflows/execute-phase.md, workflows/verify-work.md, workflows/transition.md, tests/integration.test.cjs</files>
  <action>
  Update only the production workflow steps whose runtime instructions must change so durable checkpoints explicitly write or refresh the current phase handoff for `discuss`, `research`, `plan`, `execute`, and `verify`, and so transition/`--auto` chaining only advances after those artifacts exist.

  Keep this slice focused on durable-write wiring and the additive constraints from Phase 152: discuss stays the only clean-start exception, standalone commands still work when no handoff exists, and no wording-only reconciliation or any Phase 154 work is pulled in for `TDD-AUDIT.json` or `src/commands/misc.js`.
  </action>
  <verify>Run `node --test tests/integration.test.cjs --test-name-pattern "handoff|fresh-context|auto-advance"`.</verify>
  <done>The normal workflow chain now self-produces durable handoff artifacts, and regression coverage fails if the happy path still depends on direct `verify:state handoff write` setup.</done>
</task>

</tasks>

<verification>
Run `node --test tests/state.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- The production chain writes or refreshes handoff artifacts at durable checkpoints without test-only seeding.
- Shared payload construction keeps run IDs, fingerprints, summaries, and resume targets consistent across steps.
- Discuss clean-start replacement remains fail-closed and only replaces older same-phase artifacts after a new valid handoff exists.
</success_criteria>

<output>
After completion, create `.planning/phases/153-production-handoff-persistence-resume-freshness/153-01-SUMMARY.md`
</output>
