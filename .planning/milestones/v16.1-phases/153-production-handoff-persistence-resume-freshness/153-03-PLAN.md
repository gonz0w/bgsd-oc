---
phase: 153-production-handoff-persistence-resume-freshness
plan: 03
type: execute
wave: 3
depends_on:
  - "153-02"
files_modified:
  - tests/init.test.cjs
  - tests/integration.test.cjs
  - tests/workflow.test.cjs
  - tests/discuss-phase-workflow.test.cjs
  - workflows/discuss-phase.md
  - workflows/research-phase.md
  - workflows/plan-phase.md
  - workflows/execute-phase.md
  - workflows/verify-work.md
  - workflows/transition.md
autonomous: true
gap_closure: true
requirements:
  - FLOW-07
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "One integrated regression proves the production chain is now self-producing and freshness-aware across discuss, research, plan, execute, and verify."
    - "Corrupt-newest fallback, stale-source blocking, repaired continuation, and standalone fallback still work together after the production wiring lands."
    - "Any workflow wording cleanup reflects the final runtime behavior only; Phase 154 proof-persistence work remains out of scope."
  artifacts:
    - path: "tests/integration.test.cjs"
      provides: "end-to-end production regression for handoff persistence and resume freshness"
      contains: "fresh-context"
    - path: "tests/workflow.test.cjs"
      provides: "wording contract coverage aligned to the final runtime semantics"
      contains: "repair guidance"
    - path: "workflows/transition.md"
      provides: "final auto-advance wording that matches the runtime handoff and freshness behavior"
      contains: "resume"
  key_links:
    - from: "tests/integration.test.cjs"
      to: "src/commands/init.js"
      via: "the integrated regression exercises production-produced handoffs through real resume entrypoints instead of isolated validate-only checks"
      pattern: "resume summary"
    - from: "tests/workflow.test.cjs"
      to: "workflows/verify-work.md"
      via: "doc contract assertions stay aligned only where runtime behavior became more specific after the freshness fix"
      pattern: "stale"
---

<objective>
Finish the gap-closure phase with one regression lock that proves the composed production behavior, then clean up only the workflow wording that changed because the runtime is now self-writing and freshness-aware.

Purpose: protect DO-116 and FLOW-07 from backsliding by exercising the final discuss -> research -> plan -> execute -> verify chain as a real production path for handoff persistence and stale-source enforcement only, while explicitly leaving `TDD-AUDIT.json` persistence, proof rendering, and other Phase 154 work untouched.

Output: integrated regression coverage for final composed behavior plus any minimal workflow wording updates required to match the landed runtime semantics.
</objective>

> **TDD Decision:** Skipped — this final slice is regression locking and wording reconciliation around existing runtime behavior, not a dedicated test-first feature boundary.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/v16.1-MILESTONE-AUDIT.md
@.planning/phases/153-production-handoff-persistence-resume-freshness/153-RESEARCH.md
@.planning/phases/153-production-handoff-persistence-resume-freshness/153-02-PLAN.md
@tests/init.test.cjs
@tests/integration.test.cjs
@tests/workflow.test.cjs
@tests/discuss-phase-workflow.test.cjs
@workflows/discuss-phase.md
@workflows/research-phase.md
@workflows/plan-phase.md
@workflows/execute-phase.md
@workflows/verify-work.md
@workflows/transition.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a composed production-path regression for handoff persistence and freshness</name>
  <files>tests/init.test.cjs, tests/integration.test.cjs</files>
  <action>
  Add one realistic regression that drives the production handoff chain through discuss, research, plan, execute, and verify without relying on direct happy-path seeding, and assert only the Phase 153 contract: production-written handoffs persist across the chain and resume entrypoints enforce freshness against those artifacts.

  Make the regression prove the combined handoff edge cases still compose inside that boundary: corrupt newest plus older valid fallback, stale-source blocking until rebuild or refresh, repaired continuation after the fingerprint matches again, and no proof-persistence or rendering assertions that belong to Phase 154.
  </action>
  <verify>Run `node --test tests/init.test.cjs tests/integration.test.cjs --test-name-pattern "fresh-context|stale source|latest valid|standalone"`.</verify>
  <done>The final integration coverage fails if the production chain stops self-writing handoffs, if stale resume is no longer blocked, or if latest-valid and repair behavior break under the composed flow.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile workflow wording only where final runtime behavior changed</name>
  <files>workflows/discuss-phase.md, workflows/research-phase.md, workflows/plan-phase.md, workflows/execute-phase.md, workflows/verify-work.md, workflows/transition.md, tests/workflow.test.cjs, tests/discuss-phase-workflow.test.cjs</files>
  <action>
  Tighten workflow wording only if the final production implementation changed the exact behavior described in Phase 152, such as self-written artifacts, expected-fingerprint terminology, or repair-vs-rebuild guidance.

  Keep the cleanup narrow: preserve additive standalone behavior, do not introduce new orchestration modes, and do not add any `TDD-AUDIT.json` persistence or `src/commands/misc.js` changes that belong to Phase 154.
  </action>
  <verify>Run `node --test tests/workflow.test.cjs tests/discuss-phase-workflow.test.cjs tests/integration.test.cjs --test-name-pattern "repair guidance|fresh-context|resume|standalone"`.</verify>
  <done>Workflow docs and wording tests match the final runtime semantics exactly, with no scope creep into proof persistence or broader milestone cleanup.</done>
</task>

</tasks>

<verification>
Run `npm test && npm run build`.
</verification>

<success_criteria>
- An integrated regression proves production-written handoffs persist and stale-source enforcement holds across the discuss -> research -> plan -> execute -> verify chain.
- Latest-valid fallback, stale blocking, and repaired continuation remain compatible with that production handoff chain.
- Any workflow wording edits stay narrowly aligned to runtime changes and leave Phase 154 proof-persistence scope untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/153-production-handoff-persistence-resume-freshness/153-03-SUMMARY.md`
</output>
