---
phase: 153-production-handoff-persistence-resume-freshness
plan: 02
type: execute
wave: 2
depends_on:
  - "153-01"
files_modified:
  - src/lib/helpers.js
  - src/lib/phase-handoff.js
  - src/commands/init.js
  - tests/init.test.cjs
  - tests/integration.test.cjs
autonomous: true
gap_closure: true
requirements:
  - FLOW-07
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "Real resume entrypoints block stale handoff sources by comparing the latest valid artifact to a deterministic expected fingerprint derived from current canonical phase inputs."
    - "Freshness enforcement is fail-closed but still preserves `resume`, `inspect`, `restart`, latest-valid fallback, and repair guidance for corrupt or partial handoff sets."
    - "Unchanged source inputs continue to resume normally, so freshness checks protect real drift instead of introducing false stale failures."
  artifacts:
    - path: "src/commands/init.js"
      provides: "resume summary generation with expected-fingerprint enforcement at real entrypoints"
      contains: "buildPhaseHandoffResumeSummary"
    - path: "src/lib/helpers.js"
      provides: "stable source fingerprint derivation from canonical planning artifacts"
      contains: "fingerprint"
    - path: "tests/init.test.cjs"
      provides: "resume-entry coverage for stale, unchanged, corrupt, and repaired handoff states"
      contains: "resume summary"
  key_links:
    - from: "src/commands/init.js"
      to: "src/lib/phase-handoff.js"
      via: "init resume summary passes the current expected fingerprint into handoff validation before exposing continuation options"
      pattern: "expected_fingerprint"
    - from: "tests/integration.test.cjs"
      to: "src/lib/helpers.js"
      via: "integration coverage locks the stable-fingerprint boundary so only meaningful source changes block resume"
      pattern: "stale source"
---

<objective>
Turn Phase 152's documented stale-source protection into real runtime enforcement so DO-116 and FLOW-07 are satisfied where users actually re-enter the chain.

Purpose: derive one stable expected fingerprint from canonical phase inputs, thread it through init resume-summary creation, and fail closed on stale artifacts while preserving latest-valid inspection and repair behavior.

Output: expected-fingerprint helper/runtime wiring plus targeted init and integration tests for stale-source rejection, unchanged-source success, and repaired continuation.
</objective>

> **TDD Decision:** Skipped — this work hardens resume orchestration and freshness plumbing across existing entrypoints, so a dedicated TDD plan would add ceremony without a clean behavior-first boundary.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/v16.1-MILESTONE-AUDIT.md
@.planning/phases/153-production-handoff-persistence-resume-freshness/153-RESEARCH.md
@.planning/phases/153-production-handoff-persistence-resume-freshness/153-01-PLAN.md
@src/commands/init.js
@src/lib/helpers.js
@src/lib/phase-handoff.js
@tests/init.test.cjs
@tests/integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive and thread the expected source fingerprint into resume summary validation</name>
  <files>src/lib/helpers.js, src/lib/phase-handoff.js, src/commands/init.js</files>
  <action>
  Add a deterministic helper that computes the current handoff source fingerprint from stable canonical planning inputs for the target phase, then pass that fingerprint through `buildPhaseHandoffResumeSummary()` and the relevant init entrypoints before any resumable state is exposed.

  Keep the fingerprint source intentionally narrow and stable so harmless metadata churn does not create false stale blocks. Preserve existing latest-valid selection, inspection payloads, and the standalone `null` path when no handoff artifacts exist.
  </action>
  <verify>Run `node --test tests/init.test.cjs --test-name-pattern "resume summary|stale source|latest valid|fingerprint"`.</verify>
  <done>Init resume entrypoints validate against a current expected fingerprint before offering resume options, and unchanged source inputs still resume cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Lock stale-source and repair behavior at real resume entrypoints</name>
  <files>tests/init.test.cjs, tests/integration.test.cjs</files>
  <action>
  Extend tests to cover unchanged-source success, meaningful source-change failure, corrupt-newest plus older-valid fallback, and a repaired or refreshed handoff set becoming resumable again after the expected fingerprint matches.

  Keep the assertions focused on real init entrypoints and resume-summary output. Do not broaden scope into Phase 154 proof persistence or summary-sidecar handling.
  </action>
  <verify>Run `node --test tests/init.test.cjs tests/integration.test.cjs --test-name-pattern "resume summary|stale source|latest valid|repair"`.</verify>
  <done>Automated coverage fails if stale handoffs resume anyway, if false stale blocks appear for unchanged sources, or if inspect/restart/latest-valid repair behavior regresses.</done>
</task>

</tasks>

<verification>
Run `node --test tests/init.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- Resume entrypoints compare handoff artifacts against a stable expected fingerprint derived from current phase inputs.
- Stale sources block resume while still surfacing inspect, restart, latest-valid fallback, and repair guidance.
- Unchanged sources remain resumable, preventing noisy false-positive freshness failures.
</success_criteria>

<output>
After completion, create `.planning/phases/153-production-handoff-persistence-resume-freshness/153-02-SUMMARY.md`
</output>
