---
phase: 151-snapshot-fast-path-workflow-acceleration
plan: 03
type: execute
wave: 3
depends_on:
  - "151-02"
files_modified:
  - src/commands/state.js
  - src/lib/planning-cache.js
  - src/lib/helpers.js
  - workflows/execute-plan.md
  - tests/state.test.cjs
  - tests/session-state.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - FLOW-02
  - FLOW-03
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "Completing a plan can use one batched state-finalization command for the durable core transition instead of five fragmented state updates."
    - "The core transition keeps progress, position, and decision writes together, while metrics and continuity remain optional tail writes with explicit warnings and recovery guidance."
    - "After bGSD-owned writes, later snapshot/init reads observe fresh planning state instead of stale cached discovery data."
  artifacts:
    - path: "src/commands/state.js"
      provides: "batched plan-finalization command with atomic core plus optional tail behavior"
      contains: "complete-plan"
    - path: "workflows/execute-plan.md"
      provides: "executor workflow adoption of the batched state finalization command"
      contains: "update_current_position"
    - path: "tests/state.test.cjs"
      provides: "happy-path and tail-warning coverage for batched completion"
      contains: "complete-plan"
    - path: "tests/session-state.test.cjs"
      provides: "SQLite dual-write coverage for batched core/tail session state updates"
      contains: "session_continuity"
  key_links:
    - from: "workflows/execute-plan.md"
      to: "src/commands/state.js"
      via: "plan completion calls one batched state finalization command instead of a five-command sequence"
      pattern: "complete-plan"
    - from: "src/commands/state.js"
      to: "src/lib/helpers.js"
      via: "successful writes invalidate cached discovery state before later snapshot/init reads"
      pattern: "invalidateFileCache"
---

<objective>
Batch the write path for plan completion while keeping markdown canonical and post-write reads fresh.

Purpose: Deliver FLOW-02 and the write-side of FLOW-03 for DO-116 by replacing fragmented completion updates with an atomic core plus optional tail model and explicit cache invalidation after bGSD-owned writes.

Output: A batched `verify:state` completion command, execute-plan workflow adoption, and focused state/session/integration regression coverage.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-CONTEXT.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-RESEARCH.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-02-PLAN.md
@src/commands/state.js
@src/lib/planning-cache.js
@src/lib/helpers.js
@workflows/execute-plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a batched `verify:state` completion command with atomic core and optional tail writes</name>
  <files>src/commands/state.js, src/lib/planning-cache.js, src/lib/helpers.js</files>
  <action>
  Add a `verify:state complete-plan`-style command that performs the durable core transition for progress, position, and decisions together, then handles metrics and continuity as optional tail writes with structured warnings if they fail.

  Reuse existing STATE.md mutation helpers and session-state tables instead of inventing a second storage model. Keep markdown canonical, preserve Map fallback semantics, and do not expand this command into a giant all-or-nothing transaction across roadmap or requirements artifacts.
  </action>
  <verify>Run `node --test tests/state.test.cjs tests/session-state.test.cjs --test-name-pattern "complete-plan|record-session|record-metric|add-decision|advance-plan"`.</verify>
  <done>One state command performs the durable plan-completion transition, exposes tail-write warnings explicitly, and preserves existing markdown + SQLite dual-write guarantees.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt the batched completion flow and test write-then-read freshness</name>
  <files>workflows/execute-plan.md, tests/state.test.cjs, tests/session-state.test.cjs, tests/integration.test.cjs</files>
  <action>
  Replace the five-command completion sequence in `workflows/execute-plan.md` with the new batched command while keeping roadmap and requirements updates as separate follow-on steps.

  Add focused tests for full success, core-success-with-tail-warning behavior, and write-then-snapshot or write-then-init sequences that would catch stale-cache regressions after bGSD-owned writes.
  </action>
  <verify>Run `node --test tests/state.test.cjs tests/session-state.test.cjs tests/integration.test.cjs`.</verify>
  <done>The executor workflow uses one batched state finalization command, and automated coverage fails if tail warnings are hidden or later snapshot/init reads see stale data.</done>
</task>

</tasks>

<verification>
Run `node --test tests/state.test.cjs tests/session-state.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- Plan completion state updates are batched behind one command with atomic core behavior for progress, position, and decisions.
- Metrics and continuity are optional tail writes with explicit warning output and recovery guidance when they fail.
- Post-write cache invalidation keeps later snapshot/init reads fresh.
</success_criteria>

<output>
After completion, create `.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-03-SUMMARY.md`
</output>
