---
phase: 151-snapshot-fast-path-workflow-acceleration
plan: 04
type: execute
wave: 4
depends_on:
  - "151-03"
files_modified:
  - workflows/discuss-phase.md
  - workflows/verify-work.md
  - commands/bgsd-discuss-phase.md
  - commands/bgsd-verify-work.md
  - src/lib/questions.js
  - tests/discuss-phase-workflow.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - FLOW-04
  - FLOW-05
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "The default `discuss-phase` flow gets faster on low-risk clarification turns without splitting the product into separate normal and fast modes."
    - "Any compatibility `--fast` surface stays thin and still preserves locked decisions, agent discretion, deferred ideas, and one-challenge-at-a-time stress testing."
    - "`verify-work --batch N` keeps one-at-a-time verification as the default, uses a cheap grouped clean path when batching is requested, and drills down exactly only for failing groups."
  artifacts:
    - path: "workflows/discuss-phase.md"
      provides: "faster default discuss flow that preserves decision fidelity and stress-test rules"
      contains: "customer_stress_test"
    - path: "workflows/verify-work.md"
      provides: "optional grouped verification flow with clean-path summary and failing-group drill-down"
      contains: "present_test"
    - path: "tests/discuss-phase-workflow.test.cjs"
      provides: "contract coverage for discuss stress-test discipline after acceleration work"
      contains: "one challenge at a time"
    - path: "tests/workflow.test.cjs"
      provides: "structural coverage for verify-work batching and wrapper/workflow contract changes"
      contains: "verify-work"
  key_links:
    - from: "commands/bgsd-discuss-phase.md"
      to: "workflows/discuss-phase.md"
      via: "wrapper keeps the default discuss workflow as the product surface, with any fast flag treated only as compatibility"
      pattern: "discuss-phase"
    - from: "commands/bgsd-verify-work.md"
      to: "workflows/verify-work.md"
      via: "wrapper passes optional batch parameters into grouped verification behavior without changing the default path"
      pattern: "--batch"
---

<objective>
Accelerate the human-facing discuss and verify workflows without sacrificing quality or exactness.

Purpose: Deliver FLOW-04 and FLOW-05 for DO-116 by making default `discuss-phase` faster on low-risk clarification work and adding optional grouped verification that stays cheap on clean runs but exact on failures.

Output: Updated discuss/verify workflow contracts, any necessary wrapper/question-template adjustments, and workflow-focused regression coverage.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-CONTEXT.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-RESEARCH.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-03-PLAN.md
@workflows/discuss-phase.md
@workflows/verify-work.md
@commands/bgsd-discuss-phase.md
@commands/bgsd-verify-work.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accelerate the default discuss workflow without creating a split UX</name>
  <files>workflows/discuss-phase.md, commands/bgsd-discuss-phase.md, src/lib/questions.js, tests/discuss-phase-workflow.test.cjs</files>
  <action>
  Rework the low-risk clarification path in `workflows/discuss-phase.md` so the default flow can move through non-contentious decisions faster, while still preserving locked decisions, deferred ideas, agent discretion capture, and the existing one-challenge-per-turn stress-test discipline.

  If a `--fast` compatibility surface is helpful, keep it thin and wording-only where possible. Do not create a separate long-term workflow branch with different semantics from the default discuss experience.
  </action>
  <verify>Run `node --test tests/discuss-phase-workflow.test.cjs`.</verify>
  <done>The default discuss workflow reaches low-risk decisions faster, while the stress-test section still enforces single-challenge interactive turns and decision-fidelity rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add optional grouped verify mode with cheap clean-path behavior and exact failing-group drill-down</name>
  <files>workflows/verify-work.md, commands/bgsd-verify-work.md, tests/workflow.test.cjs</files>
  <action>
  Extend verify-work so one-at-a-time verification remains the default, but an optional `--batch N` mode can present routine checks in groups using a cheap summary-first clean path.

  When a group fails, require exact item-level drill-down only for that failing group. Keep UAT tracking, severity inference, and downstream gap-planning behavior intact, and cover the new behavior with structural workflow tests rather than brittle full-text snapshots.
  </action>
  <verify>Run `node --test tests/workflow.test.cjs --test-name-pattern "verify-work|workflow"`.</verify>
  <done>`verify-work --batch N` exists as an optional grouped mode, the clean path stays cheap, failing groups drill down exactly, and one-at-a-time verification remains the default workflow.</done>
</task>

</tasks>

<verification>
Run `node --test tests/discuss-phase-workflow.test.cjs tests/workflow.test.cjs && npm run build`.
</verification>

<success_criteria>
- Default `discuss-phase` is faster for low-risk clarification without becoming a separate fast-mode product.
- Any compatibility `--fast` surface does not bypass locked decisions, deferred ideas, agent discretion capture, or one-challenge stress testing.
- `verify-work --batch N` adds optional grouped verification with cheap clean-path summaries and exact drill-down only for failing groups, while default one-at-a-time verification remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-04-SUMMARY.md`
</output>
