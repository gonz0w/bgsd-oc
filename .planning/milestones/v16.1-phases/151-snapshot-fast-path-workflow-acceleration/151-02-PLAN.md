---
phase: 151-snapshot-fast-path-workflow-acceleration
plan: 02
type: execute
wave: 2
depends_on:
  - "151-01"
files_modified:
  - src/commands/init.js
  - src/lib/helpers.js
  - tests/init.test.cjs
  - tests/contracts.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - FLOW-01
  - FLOW-03
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "Hot init flows reuse the shared snapshot/read path instead of rescanning the same phase directory independently."
    - "Init outputs keep their current additive contracts while pulling artifact paths and plan inventory from one shared snapshot source."
    - "Discovery cache reuse is trusted inside the workflow fast path, but later bGSD-owned writes can invalidate the reused data cleanly instead of leaving stale state behind."
  artifacts:
    - path: "src/commands/init.js"
      provides: "snapshot-backed init outputs for plan-phase, execute-phase, verify-work, and phase-op"
      contains: "cmdInitPlanPhase"
    - path: "src/lib/helpers.js"
      provides: "shared artifact discovery helpers reused by snapshot and init commands"
      contains: "findPhaseInternal"
    - path: "tests/init.test.cjs"
      provides: "regression coverage proving init commands preserve their contracts after snapshot adoption"
      contains: "init:plan-phase"
  key_links:
    - from: "src/commands/init.js"
      to: "src/commands/phase.js"
      via: "init commands derive phase metadata and artifact paths from the snapshot contract, then layer init-specific enrichment on top"
      pattern: "phase:snapshot"
    - from: "tests/init.test.cjs"
      to: "src/commands/init.js"
      via: "tests protect existing init outputs while proving duplicate artifact scans are no longer required"
      pattern: "init:execute-phase"
---

<objective>
Adopt the shared snapshot across hot init flows so Phase 151's read-path speedup shows up in real workflow entrypoints.

Purpose: Deliver the init-consumer side of FLOW-01 and FLOW-03 for DO-116 by making high-traffic init commands reuse the snapshot/artifact discovery path while preserving their current contracts.

Output: Snapshot-backed init commands and targeted init/contract/integration coverage.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-CONTEXT.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-RESEARCH.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-01-PLAN.md
@src/commands/init.js
@src/lib/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor hot init commands to consume the shared snapshot path</name>
  <files>src/commands/init.js, src/lib/helpers.js</files>
  <action>
  Rework `init:plan-phase`, `init:execute-phase`, `init:verify-work`, and `init:phase-op` so they start from the shared snapshot/artifact discovery path, then add only the init-specific enrichment they uniquely need.

  Remove repeated phase-directory scans where snapshot data already covers context/research/verification/UAT files, plan inventory, and phase metadata. Keep compact and manifest behavior additive-safe, and do not move workflow-specific enrichment into the generic snapshot helper.
  </action>
  <verify>Run `node --test tests/init.test.cjs --test-name-pattern "init plan-phase|init execute-phase|init verify-work|init phase-op"`.</verify>
  <done>The high-traffic init commands reuse one shared read path for phase-tree and artifact discovery while preserving their current JSON contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add init and integration coverage for snapshot-backed discovery reuse</name>
  <files>tests/init.test.cjs, tests/contracts.test.cjs, tests/integration.test.cjs</files>
  <action>
  Add focused tests that prove init outputs still expose the expected fields after snapshot adoption and that snapshot-backed reads work in realistic workflow sequences.

  Keep the tests additive-safe: verify the required fields and artifact paths survive the refactor without snapshotting entire payloads unless a narrow contract fixture is already the established pattern.
  </action>
  <verify>Run `node --test tests/init.test.cjs tests/contracts.test.cjs tests/integration.test.cjs`.</verify>
  <done>Coverage fails if init commands regress their existing fields or if snapshot adoption fails to replace the duplicated discovery work Phase 151 is removing.</done>
</task>

</tasks>

<verification>
Run `node --test tests/init.test.cjs tests/contracts.test.cjs tests/integration.test.cjs && npm run build`.
</verification>

<success_criteria>
- `init:plan-phase`, `init:execute-phase`, `init:verify-work`, and `init:phase-op` reuse the shared snapshot/read path instead of independently rescanning the same phase directory.
- Existing init JSON contracts remain additive-safe after the refactor.
- Snapshot-backed discovery reuse is proven in targeted init and integration coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-02-SUMMARY.md`
</output>
