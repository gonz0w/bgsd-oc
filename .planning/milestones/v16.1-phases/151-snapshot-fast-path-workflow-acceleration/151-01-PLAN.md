---
phase: 151-snapshot-fast-path-workflow-acceleration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/helpers.js
  - src/commands/phase.js
  - src/router.js
  - src/lib/constants.js
  - tests/plan.test.cjs
autonomous: true
requirements:
  - FLOW-01
intent:
  outcome_ids:
    - DO-116
must_haves:
  truths:
    - "`phase:snapshot <phase>` returns one compact payload with phase metadata, requirement IDs, artifact paths, and plan-index context instead of forcing callers through repeated discovery commands."
    - "The snapshot contract is richer by default but still structured and bounded, so common callers do not need immediate follow-up reads for basic phase understanding."
    - "Roadmap-only phases and Map-fallback cache paths degrade gracefully instead of failing or requiring a second discovery path."
  artifacts:
    - path: "src/lib/helpers.js"
      provides: "shared snapshot builder on top of existing phase-tree and roadmap discovery"
      contains: "getPhaseTree"
    - path: "src/commands/phase.js"
      provides: "`phase:snapshot` command contract"
      contains: "snapshot"
    - path: "tests/plan.test.cjs"
      provides: "snapshot contract coverage for normal, empty, and roadmap-only phases"
      contains: "phase:snapshot"
  key_links:
    - from: "src/commands/phase.js"
      to: "src/lib/helpers.js"
      via: "phase command delegates snapshot assembly to one shared helper instead of duplicating discovery logic"
      pattern: "getPhaseTree"
    - from: "tests/plan.test.cjs"
      to: "src/commands/phase.js"
      via: "tests lock the richer default payload and graceful fallback behavior"
      pattern: "phase:snapshot"
---

<objective>
Introduce the shared snapshot primitive that makes Phase 151's read-path acceleration possible.

Purpose: Deliver FLOW-01 for DO-116 by creating a richer default `phase:snapshot` contract on top of existing discovery and cache primitives, without breaking backward compatibility or markdown-first behavior.

Output: A reusable snapshot builder, CLI routing for `phase:snapshot`, and focused snapshot contract coverage.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-CONTEXT.md
@.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-RESEARCH.md
@src/lib/helpers.js
@src/commands/phase.js
@src/router.js
@src/lib/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared snapshot builder and `phase:snapshot` command surface</name>
  <files>src/lib/helpers.js, src/commands/phase.js, src/router.js, src/lib/constants.js</files>
  <action>
  Add `phase:snapshot &lt;phase&gt;` on the phase command path and implement it by composing existing `getPhaseTree()`, `findPhaseInternal()`, roadmap requirement lookup, and plan-index metadata rather than inventing a second discovery stack.

  Make the payload richer by default but still compact and structured: include phase metadata, requirement IDs, artifact paths, plan inventory, and execution context fields common callers repeatedly need. Preserve additive JSON behavior, markdown canon, and Map fallback semantics. If the roadmap knows the phase before the directory exists, return roadmap-backed metadata instead of a hard failure.
  </action>
  <verify>Run `node --test tests/plan.test.cjs --test-name-pattern "phase snapshot|phase-plan-index"`.</verify>
  <done>`phase:snapshot` exists as the reusable read primitive for common phase workflows and returns rich default data without a duplicate discovery path.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the snapshot contract with focused phase command tests</name>
  <files>tests/plan.test.cjs</files>
  <action>
  Add focused tests for normal phases, empty phase directories, and roadmap-only phases so the new snapshot command's shape and fallbacks stay stable while remaining additive-safe.

  Reuse existing phase-plan-index patterns where helpful, but make the new tests assert the richer snapshot payload directly rather than relying on unrelated commands to imply it.
  </action>
  <verify>Run `node --test tests/plan.test.cjs`.</verify>
  <done>Regression coverage fails if `phase:snapshot` loses requirement/artifact data, breaks roadmap-only fallback behavior, or drifts from the shared contract Phase 151 needs.</done>
</task>

</tasks>

<verification>
Run `node --test tests/plan.test.cjs && npm run build`.
</verification>

<success_criteria>
- `phase:snapshot <phase>` returns compact rich phase metadata, requirement IDs, artifact paths, and plan index data in one additive payload.
- The snapshot builder reuses existing discovery/cache helpers instead of creating a second scanning path.
- Roadmap-only and cache-miss scenarios degrade gracefully without breaking callers.
</success_criteria>

<output>
After completion, create `.planning/phases/151-snapshot-fast-path-workflow-acceleration/151-01-SUMMARY.md`
</output>
