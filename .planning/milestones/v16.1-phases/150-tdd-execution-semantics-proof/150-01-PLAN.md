---
phase: 150-tdd-execution-semantics-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/misc.js
  - workflows/tdd.md
  - workflows/execute-plan.md
  - skills/tdd-execution/SKILL.md
  - skills/tdd-execution/tdd-reference.md
  - templates/plans/tdd.md
  - src/lib/constants.js
  - src/lib/command-help.js
  - tests/agent.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - TDD-05
intent:
  outcome_ids:
    - DO-115
must_haves:
  truths:
    - "Running `execute:tdd validate-red` only succeeds when the exact planned target command fails, so RED cannot pass on a green or missing target."
    - "Running `execute:tdd validate-green` or `validate-refactor` only succeeds when the same exact planned target command passes, so GREEN and REFACTOR prove the targeted behavior still works."
    - "Each TDD validation result returns structured proof for the exact target command, exit status, and matched pass/fail evidence without reopening Phase 149's selection and severity contracts."
  artifacts:
    - path: "src/commands/misc.js"
      provides: "semantic TDD validators and structured per-stage proof payload"
      contains: "validate-red"
    - path: "workflows/tdd.md"
      provides: "executor instructions that require exact-command targeting for RED/GREEN/REFACTOR"
      contains: "validate-refactor"
    - path: "skills/tdd-execution/SKILL.md"
      provides: "canonical TDD contract extended from selection alignment into execution semantics"
      contains: "execute:tdd"
    - path: "tests/agent.test.cjs"
      provides: "focused coverage for semantic validator pass/fail behavior and proof fields"
      contains: "execute:tdd"
  key_links:
    - from: "workflows/tdd.md"
      to: "src/commands/misc.js"
      via: "workflow validation steps call the semantic gates with the exact declared target command"
      pattern: "validate-red"
    - from: "skills/tdd-execution/SKILL.md"
      to: "templates/plans/tdd.md"
      via: "canonical contract requires explicit target commands and targeted-only RED/GREEN/REFACTOR proof"
      pattern: "type: tdd"
    - from: "src/lib/constants.js"
      to: "src/commands/misc.js"
      via: "CLI help text advertises the same semantic validator contract the command returns"
      pattern: "execute:tdd"
---

<objective>
Harden `execute:tdd` so RED, GREEN, and REFACTOR mean real targeted state transitions instead of raw exit-code checks.

Purpose: Deliver TDD-05 for DO-115 by making the executor prove the exact planned test command failed or passed at the right stage, while preserving the canonical Phase 149 contract and avoiding any reopen of selection, rationale, or severity rules.

Output: Semantic validator behavior in `src/commands/misc.js`, aligned TDD contract/help/workflow wording, and focused tests that lock the richer proof payload in place.
</objective>

> **TDD Decision:** Skipped — this plan hardens orchestration, command semantics, and contract surfaces across the plugin, so it does not fit one feature-sized RED/GREEN/REFACTOR delivery slice even though it enables stricter TDD execution for downstream plans.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/150-tdd-execution-semantics-proof/150-CONTEXT.md
@.planning/phases/150-tdd-execution-semantics-proof/150-RESEARCH.md
@.planning/phases/149-tdd-selection-contract-alignment/149-01-PLAN.md
@.planning/phases/149-tdd-selection-contract-alignment/149-03-PLAN.md
@skills/tdd-execution/SKILL.md
@skills/tdd-execution/tdd-reference.md
@workflows/tdd.md
@workflows/execute-plan.md
@templates/plans/tdd.md
@src/commands/misc.js
@src/lib/constants.js
@src/lib/command-help.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement semantic RED/GREEN/REFACTOR gates with structured proof payloads</name>
  <files>src/commands/misc.js</files>
  <action>
  Replace the current exit-code-only `execute:tdd` validation behavior with stage-aware checks that treat the exact declared `--test-cmd` as the canonical target.

  RED must only validate when that target command fails, GREEN must only validate when the same target passes after implementation, and REFACTOR must only validate when the same target still passes after cleanup. Extend the returned JSON so each stage reports the exact target command, exit status, and matched pass/fail evidence snippet in a stable proof shape. Keep the contract tolerant of runner wording differences: do not require brittle root-cause matching or reopen Phase 149's command names.
  </action>
  <verify>Run `node --test tests/agent.test.cjs --test-name-pattern "execute:tdd|GSD-Phase"` after the semantic validator work lands.</verify>
  <done>`execute:tdd` gates now prove the exact targeted state transition and emit structured evidence instead of only a boolean derived from raw process success or failure.</done>
</task>

<task type="auto">
  <name>Task 2: Align the canonical TDD contract, workflow text, template guidance, and CLI help to exact-command semantics</name>
  <files>workflows/tdd.md, workflows/execute-plan.md, skills/tdd-execution/SKILL.md, skills/tdd-execution/tdd-reference.md, templates/plans/tdd.md, src/lib/constants.js, src/lib/command-help.js</files>
  <action>
  Update all TDD-facing guidance so the canonical contract now says every RED/GREEN/REFACTOR step declares the exact target command, GREEN and REFACTOR are targeted-only by default, and validators preserve structured proof per stage.

  Keep Phase 149 boundaries intact: do not rename `execute:tdd` subcommands, do not revisit TDD selection or severity semantics, and do not claim end-to-end proof is complete in this plan. Make the plan template and workflow wording precise enough that a later fixture can execute the contract exactly as written.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs execute:tdd --help` and `node bin/bgsd-tools.cjs workflow:verify-structure workflows/tdd.md`.</verify>
  <done>The shared skill, workflow, plan template, and help surfaces all describe the same exact-command semantic gate contract with no Phase 149 regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused command and workflow tests that lock semantic gate behavior</name>
  <files>tests/agent.test.cjs, tests/workflow.test.cjs</files>
  <action>
  Add or update targeted tests for RED failure acceptance, GREEN success acceptance, REFACTOR regression rejection, and the richer validator proof payload fields.

  Cover contract wording in `workflows/tdd.md` and command behavior in `tests/agent.test.cjs`, but keep the scope intentionally short of the full fixture-backed end-to-end proof reserved for TDD-06. The goal is to protect the machine-readable semantic contract before Plan 02 uses it in a real temp-repo execution trace.
  </action>
  <verify>Run `node --test tests/agent.test.cjs tests/workflow.test.cjs`.</verify>
  <done>Automated coverage fails if semantic gates drift back to exit-code-only behavior or if workflow/help text diverges from the canonical exact-command contract.</done>
</task>

</tasks>

<verification>
Run `node --test tests/agent.test.cjs tests/workflow.test.cjs && npm run build`.
</verification>

<success_criteria>
- `execute:tdd` semantic gates enforce exact-command RED/GREEN/REFACTOR behavior instead of raw success/failure alone.
- Validator output includes the exact target command, exit status, and matched pass/fail evidence for each stage.
- Canonical TDD docs, workflow text, templates, and CLI help all describe the same execution semantics without reopening Phase 149 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/150-tdd-execution-semantics-proof/150-01-SUMMARY.md`
</output>
