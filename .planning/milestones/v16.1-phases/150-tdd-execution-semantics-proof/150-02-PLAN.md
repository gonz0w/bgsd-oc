---
phase: 150-tdd-execution-semantics-proof
plan: 02
type: execute
wave: 2
depends_on:
  - "150-01"
files_modified:
  - src/lib/git.js
  - src/commands/misc.js
  - workflows/execute-plan.md
  - templates/summary.md
  - tests/summary-generate.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - TDD-06
intent:
  outcome_ids:
    - DO-115
must_haves:
  truths:
    - "A real fixture-backed `type: tdd` plan executes RED, GREEN, and REFACTOR end to end in a temp repo using the Phase 150 semantic gates."
    - "The resulting audit trail proves each stage, including REFACTOR, through stable structured validator evidence, commit history, and summary output rather than relying on ad hoc log inspection."
    - "Git and summary tooling expose `GSD-Phase` trailer data and stage proof details clearly enough that future reviewers can confirm the full RED/GREEN/REFACTOR cycle actually happened."
  artifacts:
    - path: "src/lib/git.js"
      provides: "structured git history with trailer visibility needed for TDD audit proof"
      contains: "structuredLog"
    - path: "src/commands/misc.js"
      provides: "summary/audit generation that can surface TDD stage proof from commit and validator evidence"
      contains: "summary-generate"
    - path: "tests/integration.test.cjs"
      provides: "fixture-backed end-to-end proof of a real `type: tdd` plan execution"
      contains: "type: tdd"
    - path: "tests/summary-generate.test.cjs"
      provides: "summary assertions for trailer-derived TDD audit fields"
      contains: "GSD-Phase"
  key_links:
    - from: "src/lib/git.js"
      to: "src/commands/misc.js"
      via: "summary generation reads trailer-aware commit metadata to build the TDD audit trail"
      pattern: "GSD-Phase"
    - from: "workflows/execute-plan.md"
      to: "templates/summary.md"
      via: "plan completion guidance tells executors how TDD proof appears in summary output"
      pattern: "SUMMARY"
    - from: "tests/integration.test.cjs"
      to: "tests/summary-generate.test.cjs"
      via: "temp-repo execution proof and summary assertions validate the same end-to-end audit contract"
      pattern: "type: tdd"
---

<objective>
Prove the Phase 150 TDD contract end to end with fixture-backed execution and an auditable summary trail.

Purpose: Deliver TDD-06 for DO-115 by showing a real `type: tdd` plan produces the expected RED/GREEN/REFACTOR proof package, including a real REFACTOR validation pass, commit trailers, and summary-visible audit evidence instead of only passing narrow validator unit tests.

Output: Trailer-aware git/summary plumbing, execute-plan summary guidance for TDD proof, and fixture-backed integration coverage that exercises a real TDD cycle in a temporary repository.
</objective>

> **TDD Decision:** Skipped — this plan builds proof infrastructure and fixture coverage around the TDD engine itself, so `type: execute` is the correct shape even though the tested fixture must be a real `type: tdd` plan.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/150-tdd-execution-semantics-proof/150-CONTEXT.md
@.planning/phases/150-tdd-execution-semantics-proof/150-RESEARCH.md
@.planning/phases/150-tdd-execution-semantics-proof/150-01-PLAN.md
@workflows/execute-plan.md
@templates/summary.md
@src/lib/git.js
@src/commands/misc.js
@tests/summary-generate.test.cjs
@tests/integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose TDD trailer and proof data through git and summary-generation plumbing</name>
  <files>src/lib/git.js, src/commands/misc.js, workflows/execute-plan.md, templates/summary.md</files>
  <action>
  Extend the git and summary layers so TDD audit generation can read commit trailers, preserve per-stage proof details, and present a stable review surface for RED/GREEN/REFACTOR history.

  Make `structuredLog()` and any summary-generation helpers surface `GSD-Phase` metadata instead of only commit subjects. Update summary guidance so TDD summaries explicitly capture exact target commands, exit status, matched evidence, and the resulting `test(...)`, `feat(...)`, and `refactor(...)` commits, with REFACTOR proof called out as required phase evidence rather than an optional extra. Keep the output additive and backward-compatible for non-TDD plans.
  </action>
  <verify>Run `node --test tests/summary-generate.test.cjs --test-name-pattern "summary|GSD-Phase|tdd"`.</verify>
  <done>The audit trail has one stable machine-readable and human-readable path from commit trailers plus validator proof into summary output.</done>
</task>

<task type="auto">
  <name>Task 2: Add a fixture-backed temp-repo proof that executes a real `type: tdd` plan end to end</name>
  <files>tests/summary-generate.test.cjs, tests/integration.test.cjs</files>
  <action>
  Build a temp-repo fixture that contains a real `type: tdd` plan with explicit target commands for RED, GREEN, and REFACTOR, then drive the execution path far enough to prove the semantic gates, commits, trailers, and summary output all line up.

  Reuse the repository's existing temp-repo testing patterns instead of inventing a browser or external-service harness. Assert the validator JSON proof for all three stages, git history with `test(...)`, `feat(...)`, and `refactor(...)` commits, `GSD-Phase: red|green|refactor` trailers, and summary-visible audit fields together so the phase proves the full cycle end to end rather than through isolated snapshots.
  </action>
  <verify>Run `node --test tests/integration.test.cjs tests/summary-generate.test.cjs`.</verify>
  <done>There is automated fixture-backed coverage showing a real `type: tdd` plan leaves the expected RED/GREEN/REFACTOR proof package, commit trail, and summary audit evidence.</done>
</task>

</tasks>

<verification>
Run `node --test tests/integration.test.cjs tests/summary-generate.test.cjs && npm run build`.
</verification>

<success_criteria>
- A temp-repo fixture proves a real `type: tdd` plan executes RED/GREEN/REFACTOR with the Phase 150 semantic contract, including a validator-proven REFACTOR stage.
- Git history and summary output both expose `GSD-Phase` and per-stage proof details clearly enough for review, including explicit `refactor(...)` commit and trailer evidence.
- Non-TDD summary behavior stays backward-compatible while TDD plans gain an auditable proof trail.
</success_criteria>

<output>
After completion, create `.planning/phases/150-tdd-execution-semantics-proof/150-02-SUMMARY.md`
</output>
