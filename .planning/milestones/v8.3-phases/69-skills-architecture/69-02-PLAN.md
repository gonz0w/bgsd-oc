---
phase: 69-skills-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/project-context/SKILL.md
  - skills/commit-protocol/SKILL.md
  - skills/checkpoint-protocol/SKILL.md
  - skills/checkpoint-protocol/checkpoints-reference.md
  - skills/state-update-protocol/SKILL.md
  - skills/deviation-rules/SKILL.md
  - skills/goal-backward/SKILL.md
  - skills/structured-returns/SKILL.md
  - skills/research-patterns/SKILL.md
  - skills/tdd-execution/SKILL.md
  - skills/tdd-execution/tdd-reference.md
autonomous: true
requirements: [SKIL-01, SKIL-04]
outcome_ids: [DO-37, DO-28]

must_haves:
  truths:
    - "Each shared skill directory contains a SKILL.md with valid YAML frontmatter (name, description, type, agents, sections)"
    - "Each SKILL.md has required sections: Purpose, Placeholders, Content, Cross-references, Examples"
    - "Content is rewritten and improved from the source — not copy-pasted verbatim"
    - "Skill descriptions are specific enough that agents can identify the right skill from description alone"
    - "Cross-references between skills use <skill:name /> tag syntax and reference existing skills"
    - "Placeholders ({{agent_name}}, {{action_verb}}, {{phase}}, {{gsd_home}}) are documented and used consistently"
  artifacts:
    - path: "skills/project-context/SKILL.md"
      provides: "Project discovery block for all agents"
      contains: "project-context"
    - path: "skills/commit-protocol/SKILL.md"
      provides: "Task commit protocol for executor and CI agents"
      contains: "commit-protocol"
    - path: "skills/checkpoint-protocol/SKILL.md"
      provides: "Checkpoint handling and return formats"
      contains: "checkpoint-protocol"
    - path: "skills/state-update-protocol/SKILL.md"
      provides: "STATE.md update procedures"
      contains: "state-update-protocol"
    - path: "skills/deviation-rules/SKILL.md"
      provides: "Auto-fix decision framework for deviations"
      contains: "deviation-rules"
    - path: "skills/goal-backward/SKILL.md"
      provides: "Goal-backward verification methodology"
      contains: "goal-backward"
    - path: "skills/structured-returns/SKILL.md"
      provides: "Per-agent structured return formats"
      contains: "structured-returns"
    - path: "skills/research-patterns/SKILL.md"
      provides: "Research methodology for phase/project researchers"
      contains: "research-patterns"
    - path: "skills/tdd-execution/SKILL.md"
      provides: "TDD red-green-refactor execution flow"
      contains: "tdd-execution"
  key_links:
    - from: "skills/commit-protocol/SKILL.md"
      to: "skills/state-update-protocol/SKILL.md"
      via: "Cross-reference in commit-protocol"
      pattern: "<skill:state-update-protocol"
    - from: "skills/checkpoint-protocol/SKILL.md"
      to: "skills/structured-returns/SKILL.md"
      via: "Cross-reference for return formats"
      pattern: "<skill:structured-returns"
---

<objective>
Create all 9 shared protocol skills — the reusable instruction modules that multiple agents share. These are the concept-named skills (project-context, commit-protocol, checkpoint-protocol, state-update-protocol, goal-backward, structured-returns, deviation-rules, research-patterns, tdd-execution).

Purpose: These shared skills eliminate the most duplication across agents. project-context is duplicated 10 times, structured-returns 10 times, and several others appear in 2-4 agents each. Extracting them into skills creates a single source of truth for each protocol.
Output: 9 skill directories under skills/ with SKILL.md files (some with supporting files).
</objective>

<execution_context>
@__OPENCODE_CONFIG__/get-shit-done/workflows/execute-plan.md
@__OPENCODE_CONFIG__/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-skills-architecture/69-CONTEXT.md
@.planning/phases/69-skills-architecture/69-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core protocol skills (project-context, commit-protocol, checkpoint-protocol, state-update-protocol, deviation-rules)</name>
  <files>
    skills/project-context/SKILL.md
    skills/commit-protocol/SKILL.md
    skills/checkpoint-protocol/SKILL.md
    skills/checkpoint-protocol/checkpoints-reference.md
    skills/state-update-protocol/SKILL.md
    skills/deviation-rules/SKILL.md
  </files>
  <action>
Create 5 skill directories with SKILL.md files. For each, READ the source content from agent files, REWRITE and IMPROVE it (don't copy-paste), and structure with the required sections.

**SKILL.md structure** (required for every skill):
```markdown
---
name: {kebab-case-name}
description: {Specific description for OpenCode discovery — must be unique and descriptive enough that agents find the right skill}
type: shared
agents: [{comma-separated agent list}]
sections: [{comma-separated section list if multi-section}]
---

## Purpose
{Why this skill exists, who uses it, when}

## Placeholders
| Placeholder | Description | Example |
|---|---|---|
{Only list placeholders actually used in Content}

## Content
{The actual protocol/instructions — rewritten and improved from source}

## Cross-references
{List <skill:name /> references to related skills}

## Examples
{1-2 concrete usage examples}
```

**1. project-context** — Source: `<project_context>` blocks in all 10 agents
- Read agents/gsd-executor.md for the canonical version
- Agents: all 10 (executor, planner, verifier, debugger, github-ci, roadmapper, codebase-mapper, project-researcher, phase-researcher, plan-checker)
- Placeholders: `{{action_verb}}` (e.g., "executing", "planning", "verifying", "investigating")
- Content: The 5-step project context discovery protocol (read AGENTS.md, check skills, read SKILL.md files, load rules as needed, apply patterns)
- This skill should be marked as a candidate for `eager` loading since every agent needs it
- Description must convey: "Project-specific discovery of AGENTS.md and skills before performing any work"

**2. commit-protocol** — Source: `<task_commit_protocol>` in gsd-executor.md
- Read agents/gsd-executor.md for the source content
- Agents: executor, github-ci
- Placeholders: `{{phase}}`, `{{plan}}`
- Sections: staging, commit-message, hash-tracking
- Use `<!-- section: staging -->`, `<!-- section: commit-message -->`, `<!-- section: hash-tracking -->` markers
- Content: File staging rules (never `git add .`), commit message format table (feat/fix/test/refactor/chore), hash tracking for SUMMARY.md
- Cross-reference: `<skill:state-update-protocol />`

**3. checkpoint-protocol** — Source: `<checkpoint_protocol>` + `<checkpoint_return_format>` from gsd-executor.md and gsd-github-ci.md
- Read both agent files for the source content
- Agents: executor, github-ci, planner (planning context), debugger
- Sections: detection, handling, return-format
- Content: How to detect checkpoints, how to handle each type (human-verify, decision, human-action), return format for pausing
- Supporting file: Create `checkpoints-reference.md` by migrating content from `references/checkpoints.md` (the detailed reference material — rewrite/improve, don't copy)
- Cross-reference: `<skill:structured-returns />`

**4. state-update-protocol** — Source: `<state_updates>` from gsd-executor.md
- Read agents/gsd-executor.md for the source content
- Agents: executor, github-ci
- Placeholders: `{{phase}}`, `{{gsd_home}}`
- Content: How to update STATE.md after plan completion — recording decisions, updating position, performance metrics
- Include the gsd-tools commands used for state updates
- Cross-reference: `<skill:commit-protocol />`

**5. deviation-rules** — Source: `<deviation_rules>` from gsd-executor.md and gsd-github-ci.md
- Read both agent files for the source content
- Agents: executor, github-ci
- Sections: executor, github-ci (the deviation handling differs by agent context)
- Content: Classification system (cosmetic/enhancement/structural/requirement), decision framework (auto-fix vs escalate), logging requirements
- Cross-reference: `<skill:checkpoint-protocol />` (escalation may trigger checkpoint)
  </action>
  <verify>
For each created skill directory:
1. `ls skills/{name}/SKILL.md` exists
2. `head -10 skills/{name}/SKILL.md` shows valid YAML frontmatter with name and description
3. `grep "## Purpose" skills/{name}/SKILL.md` confirms required section exists
4. `grep "## Content" skills/{name}/SKILL.md` confirms required section exists

Run: `ls -la skills/*/SKILL.md | wc -l` should show 5 SKILL.md files.
Run: `ls skills/checkpoint-protocol/checkpoints-reference.md` confirms supporting file exists.
  </verify>
  <done>5 shared protocol skill directories created with SKILL.md files following the standard structure, content rewritten from agent sources, cross-references using valid skill names, placeholders documented</done>
</task>

<task type="auto">
  <name>Task 2: Create methodology skills (goal-backward, structured-returns, research-patterns, tdd-execution)</name>
  <files>
    skills/goal-backward/SKILL.md
    skills/structured-returns/SKILL.md
    skills/research-patterns/SKILL.md
    skills/tdd-execution/SKILL.md
    skills/tdd-execution/tdd-reference.md
  </files>
  <action>
Create 4 more shared skill directories with SKILL.md files, following the same structure as Task 1.

**6. goal-backward** — Source: `<goal_backward>` from gsd-planner.md, verification methodology from gsd-verifier.md
- Read agents/gsd-planner.md and agents/gsd-verifier.md for the source content
- Agents: planner, verifier, plan-checker, roadmapper
- Content: The 5-step goal-backward methodology (state goal → derive truths → derive artifacts → derive wiring → identify key links)
- Include the must-haves output format (YAML structure for truths, artifacts, key_links)
- Include common failures section (truths too vague, artifacts too abstract, missing wiring)
- This is one of the larger skills (~100 lines) — that's fine, it's a deep methodology
- Cross-reference: `<skill:structured-returns />` (must-haves feed into return formats)

**7. structured-returns** — Source: `<structured_returns>` from ALL 10 agents
- Read all 10 agent files to collect their structured return formats
- Agents: all 10
- Sections: executor, planner, verifier, debugger, github-ci, roadmapper, codebase-mapper, project-researcher, phase-researcher, plan-checker
- Use `<!-- section: executor -->`, `<!-- section: planner -->`, etc. markers
- Content: Per-agent return format templates. For each agent, extract the markdown templates they use to communicate results back to orchestrators
- This is the largest skill (~200+ lines) because it's a collection of per-agent formats
- Description: "Agent-specific structured return formats for orchestrator communication. Load specific sections via section attribute."
- Cross-reference: `<skill:commit-protocol />` (executor returns include commit hashes)

**8. research-patterns** — Source: Tool strategy and verification protocol from gsd-phase-researcher.md and gsd-project-researcher.md
- Read both researcher agent files for the source content
- Agents: phase-researcher, project-researcher
- Content: RAG tier strategy (which tools to use when), source verification protocol, citation requirements, confidence assessment
- Include the tool selection strategy (Context7 for library docs, Exa for code examples, Brave for broad search)
- Cross-reference: `<skill:structured-returns section="phase-researcher" />`, `<skill:structured-returns section="project-researcher" />`

**9. tdd-execution** — Source: `<tdd_execution>` from gsd-executor.md, `<tdd_integration>` from gsd-planner.md
- Read both agent files for the source content
- Agents: executor, planner
- Sections: executor, planner (execution cycle vs planning integration)
- Content: RED→GREEN→REFACTOR cycle, commit protocol for each phase, context budget (40% target)
- Supporting file: Create `tdd-reference.md` by migrating content from `references/tdd.md` (rewrite/improve)
- Cross-reference: `<skill:commit-protocol />` (TDD commits follow commit protocol)
  </action>
  <verify>
For each created skill directory:
1. `ls skills/{name}/SKILL.md` exists
2. `head -10 skills/{name}/SKILL.md` shows valid YAML frontmatter
3. `grep "## Purpose" skills/{name}/SKILL.md` confirms section exists

Run: `ls -la skills/*/SKILL.md | wc -l` should now show 9 total SKILL.md files.
Run: `ls skills/tdd-execution/tdd-reference.md` confirms supporting file exists.
Run: `grep "<!-- section:" skills/structured-returns/SKILL.md | wc -l` should show 10 section markers.
  </verify>
  <done>4 methodology skill directories created — goal-backward with full 5-step methodology, structured-returns with all 10 agent sections, research-patterns with RAG strategy, tdd-execution with RED→GREEN→REFACTOR cycle and reference material</done>
</task>

</tasks>

<verification>
- `ls skills/*/SKILL.md | wc -l` shows 9 files
- `npm run build` passes (if Plan 01 completed — validates skills)
- Each SKILL.md has YAML frontmatter with name and description fields
- Each SKILL.md has all 5 required sections (Purpose, Placeholders, Content, Cross-references, Examples)
- No two skills have the same description
- Cross-references use `<skill:name />` syntax with valid skill names
</verification>

<success_criteria>
All 9 shared protocol skills exist as properly structured skill directories with rewritten content (not copy-pasted), valid frontmatter, documented placeholders, and working cross-references.
</success_criteria>

<output>
After completion, create `.planning/phases/69-skills-architecture/69-02-SUMMARY.md`
</output>
