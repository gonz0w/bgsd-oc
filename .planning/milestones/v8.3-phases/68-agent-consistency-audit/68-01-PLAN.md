---
phase: 68-agent-consistency-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-codebase-mapper.md
  - agents/gsd-verifier.md
  - agents/gsd-executor.md
autonomous: true
requirements: [ACON-02, ACON-03]
outcome_ids: [DO-35]

must_haves:
  truths:
    - "codebase-mapper has PATH SETUP block identical to the other 9 agents"
    - "codebase-mapper has <project_context> block with domain-adapted discovery steps"
    - "codebase-mapper has <structured_returns> section with mapping-appropriate return formats"
    - "codebase-mapper uses <execution_flow> tag instead of <process>"
    - "verifier has <project_context> block with verification-adapted discovery steps"
    - "verifier has <structured_returns> section extracted from its <output> block"
    - "executor uses <structured_returns> tag instead of <completion_format>"
  artifacts:
    - path: "agents/gsd-codebase-mapper.md"
      provides: "PATH SETUP, project_context, structured_returns, execution_flow rename"
      contains: "PATH SETUP|project_context|structured_returns|execution_flow"
    - path: "agents/gsd-verifier.md"
      provides: "project_context, structured_returns"
      contains: "project_context|structured_returns"
    - path: "agents/gsd-executor.md"
      provides: "structured_returns tag rename"
      contains: "structured_returns"
  key_links:
    - from: "agents/gsd-codebase-mapper.md"
      to: "GSD_HOME resolution"
      via: "PATH SETUP block"
      pattern: "GSD_HOME.*ls -d"
    - from: "agents/gsd-executor.md"
      to: "execute-phase workflow"
      via: "structured_returns output format"
      pattern: "structured_returns"
---

<objective>
Bring codebase-mapper, verifier, and executor agents to structural consistency — adding missing blocks and normalizing tag names.

Purpose: These 3 agents have the most structural gaps (codebase-mapper missing all 3 named blocks, verifier missing 2, executor has misnamed tag). Fixing them first addresses ACON-02 and ACON-03 plus the highest-gap agents from ACON-01.
Output: 3 agent files updated with consistent structural patterns matching the reference agents (gsd-github-ci, gsd-planner).

Traces to: DO-35 (agent boundaries validated — precise manifest, minimal context load, structured handoff contracts)
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-agent-consistency-audit/68-RESEARCH.md

@agents/gsd-codebase-mapper.md
@agents/gsd-verifier.md
@agents/gsd-executor.md
@agents/gsd-github-ci.md (reference agent — has all 3 blocks)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PATH SETUP, project_context, structured_returns, and rename process tag in codebase-mapper</name>
  <files>agents/gsd-codebase-mapper.md</files>
  <action>
  Make 4 changes to gsd-codebase-mapper.md:

  1. **Add PATH SETUP block** — Insert immediately after the frontmatter closing `---` and before `<role>`. Use the identical 3-line block from all other agents:
     ```
     **PATH SETUP:** Before running any gsd-tools commands, first resolve:
     ```bash
     GSD_HOME=$(ls -d $HOME/.config/*/get-shit-done 2>/dev/null | head -1)
     ```
     Then use `$GSD_HOME` in all subsequent commands. Never hardcode the config path.
     ```

  2. **Add `<project_context>` block** — Insert after closing `</role>` and before `<why_this_matters>`. Adapt for codebase mapping domain per RESEARCH.md guidance:
     - Intro: "Before mapping, discover project context:"
     - Step 3: "Load specific `rules/*.md` files as needed during mapping"
     - Step 5: "Account for project skill patterns when documenting conventions"
     - Closing: "This ensures codebase analysis reflects project-specific conventions and patterns."

  3. **Add `<structured_returns>` section** — Insert before `<success_criteria>` (after `</critical_rules>`). The mapper currently has no structured return — just "return confirmation". Create a section with two return formats:
     - **Mapping Complete**: documents written (list with line counts), focus area, confirmation message
     - **Mapping Blocked**: reason (e.g., empty codebase, missing focus area), what was attempted
     Keep it concise — the mapper's return is intentionally minimal (~10 lines) per its own critical_rules.

  4. **Rename `<process>` to `<execution_flow>`** — Change the opening `<process>` tag (line 76) to `<execution_flow>` and the closing `</process>` tag (line 176) to `</execution_flow>`. Content inside remains unchanged.

  After all changes, update the `# estimated_tokens` comment in frontmatter — add ~50 lines, so increase appropriately (794 + ~50 = ~844 lines, tokens ~14k).
  </action>
  <verify>
  Run these grep checks:
  ```bash
  grep 'PATH SETUP' agents/gsd-codebase-mapper.md
  grep '<project_context>' agents/gsd-codebase-mapper.md
  grep '<structured_returns>' agents/gsd-codebase-mapper.md
  grep '<execution_flow>' agents/gsd-codebase-mapper.md
  grep '<process>' agents/gsd-codebase-mapper.md  # Should return NO matches
  grep '</process>' agents/gsd-codebase-mapper.md  # Should return NO matches
  ```
  </verify>
  <done>codebase-mapper has PATH SETUP, project_context, structured_returns, and execution_flow tag — no `<process>` tags remain</done>
</task>

<task type="auto">
  <name>Task 2: Add project_context and structured_returns to verifier, rename completion_format in executor</name>
  <files>agents/gsd-verifier.md, agents/gsd-executor.md</files>
  <action>
  **gsd-verifier.md — 2 changes:**

  1. **Add `<project_context>` block** — Insert after closing `</role>` (line 30) and before `<core_principle>`. Adapt for verification domain per RESEARCH.md:
     - Intro: "Before verifying, discover project context:"
     - Step 3: "Load specific `rules/*.md` files as needed during verification"
     - Step 5: "Verify implementations follow project skill patterns"
     - Closing: "This ensures verification checks implementations against project-specific conventions."

  2. **Add `<structured_returns>` section** — The verifier's `<output>` section (lines 362-484) contains both VERIFICATION.md creation instructions AND a "Return to Orchestrator" block. Extract the return-to-orchestrator portion (lines 451-482) into a new `<structured_returns>` section placed after `</output>` and before `<critical_rules>`. Keep the VERIFICATION.md creation instructions in `<output>`. In the `<output>` section where the "Return to Orchestrator" block was, add a brief note: "See `<structured_returns>` below for return format to orchestrator." The structured_returns content is the existing "Verification Complete" return format (already well-formed with passed/gaps_found/human_needed variants).

  After changes, update `# estimated_tokens` comment (580 + ~25 = ~605 lines, tokens ~10k stays reasonable).

  **gsd-executor.md — 1 change:**

  3. **Rename `<completion_format>` to `<structured_returns>`** — Change the opening tag (line 453) from `<completion_format>` to `<structured_returns>` and the closing tag (line 469) from `</completion_format>` to `</structured_returns>`. Content inside remains unchanged. Also update the success_criteria reference from "Completion format returned" to "Structured return format returned" (line 482).

  After change, update `# estimated_tokens` comment if line count changed (it shouldn't — just a rename).
  </action>
  <verify>
  Run these grep checks:
  ```bash
  # Verifier checks
  grep '<project_context>' agents/gsd-verifier.md
  grep '<structured_returns>' agents/gsd-verifier.md
  # Executor checks
  grep '<structured_returns>' agents/gsd-executor.md
  grep '<completion_format>' agents/gsd-executor.md  # Should return NO matches
  grep '</completion_format>' agents/gsd-executor.md  # Should return NO matches
  ```
  </verify>
  <done>Verifier has project_context and structured_returns sections. Executor uses structured_returns tag (no completion_format remains).</done>
</task>

</tasks>

<verification>
Run combined verification for all 3 agents:

```bash
# ACON-02: codebase-mapper has PATH SETUP
grep -c 'PATH SETUP' agents/gsd-codebase-mapper.md  # Expected: 1

# ACON-03: codebase-mapper has structured_returns
grep -c '<structured_returns>' agents/gsd-codebase-mapper.md  # Expected: 1

# All 3 agents touched in this plan have structured_returns
for agent in gsd-codebase-mapper gsd-verifier gsd-executor; do
  echo "$agent: $(grep -c '<structured_returns>' agents/$agent.md) structured_returns"
done
# Expected: all show 1

# No old tags remain
grep -l '<completion_format>' agents/gsd-*.md  # Expected: no output
grep -l '<process>' agents/gsd-codebase-mapper.md  # Expected: no output

# All 3 agents have project_context
for agent in gsd-codebase-mapper gsd-verifier gsd-executor; do
  echo "$agent: $(grep -c '<project_context>' agents/$agent.md) project_context"
done
# Expected: all show 1
```
</verification>

<success_criteria>
- codebase-mapper has PATH SETUP block (ACON-02 satisfied)
- codebase-mapper has `<structured_returns>` section (ACON-03 satisfied)
- codebase-mapper has `<project_context>` block (partial ACON-01)
- codebase-mapper uses `<execution_flow>` not `<process>`
- verifier has `<project_context>` and `<structured_returns>` (partial ACON-01)
- executor uses `<structured_returns>` not `<completion_format>`
- No old/inconsistent tags remain in any of the 3 files
</success_criteria>

<output>
After completion, create `.planning/phases/68-agent-consistency-audit/68-01-SUMMARY.md`
</output>
