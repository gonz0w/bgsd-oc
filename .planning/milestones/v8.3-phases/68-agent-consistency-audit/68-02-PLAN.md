---
phase: 68-agent-consistency-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-debugger.md
  - agents/gsd-project-researcher.md
  - agents/gsd-roadmapper.md
autonomous: true
requirements: [ACON-01]
outcome_ids: [DO-35]

must_haves:
  truths:
    - "debugger has <project_context> block with investigation-adapted discovery steps"
    - "project-researcher has <project_context> block with research-adapted discovery steps"
    - "roadmapper has <project_context> block with roadmap-adapted discovery steps"
    - "All 10 agents have <project_context> when combined with Plan 01 results"
    - "All 10 agents have PATH SETUP when combined with Plan 01 results"
    - "All 10 agents have <structured_returns> when combined with Plan 01 results"
  artifacts:
    - path: "agents/gsd-debugger.md"
      provides: "project_context block"
      contains: "project_context"
    - path: "agents/gsd-project-researcher.md"
      provides: "project_context block"
      contains: "project_context"
    - path: "agents/gsd-roadmapper.md"
      provides: "project_context block"
      contains: "project_context"
  key_links:
    - from: "agents/gsd-debugger.md"
      to: "AGENTS.md + .agents/skills/"
      via: "project_context discovery"
      pattern: "project_context"
    - from: "agents/gsd-project-researcher.md"
      to: "AGENTS.md + .agents/skills/"
      via: "project_context discovery"
      pattern: "project_context"
    - from: "agents/gsd-roadmapper.md"
      to: "AGENTS.md + .agents/skills/"
      via: "project_context discovery"
      pattern: "project_context"
---

<objective>
Add `<project_context>` discovery blocks to the 3 remaining agents that are missing them — debugger, project-researcher, and roadmapper.

Purpose: After Plan 01 fixes codebase-mapper, verifier, and executor, these 3 agents are the remaining gaps for ACON-01 (all 10 agents have project_context). Each addition is adapted to the agent's domain — not cookie-cutter.
Output: 3 agent files updated. Combined with Plan 01, all 10 agents now have all 3 structural blocks.

Traces to: DO-35 (agent boundaries validated — precise manifest, minimal context load, structured handoff contracts)
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-agent-consistency-audit/68-RESEARCH.md

@agents/gsd-debugger.md
@agents/gsd-project-researcher.md
@agents/gsd-roadmapper.md
@agents/gsd-github-ci.md (reference agent — project_context template)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add project_context to debugger and project-researcher</name>
  <files>agents/gsd-debugger.md, agents/gsd-project-researcher.md</files>
  <action>
  **gsd-debugger.md:**

  Insert `<project_context>` block after closing `</role>` (line 41) and before `<philosophy>` (line 43). Adapt for debugging domain per RESEARCH.md:
  - Intro: "Before investigating, discover project context:"
  - Step 3: "Load specific `rules/*.md` files as needed during investigation"
  - Step 5: "Use skill patterns to understand expected behavior vs actual behavior"
  - Closing: "This ensures debugging investigates against project-specific conventions and patterns."

  After change, update `# estimated_tokens` comment in frontmatter (1222 + ~15 = ~1237 lines, ~21k tokens).

  **gsd-project-researcher.md:**

  Insert `<project_context>` block after closing `</role>` (line 43) and before `<philosophy>` (line 45). Adapt for project-level research domain per RESEARCH.md:
  - Intro: "Before researching, discover project context:"
  - Step 3: "Load specific `rules/*.md` files as needed during research"
  - Step 5: "Research should account for project skill patterns"
  - Closing: "This ensures research aligns with project-specific conventions and libraries."

  After change, update `# estimated_tokens` comment in frontmatter (655 + ~15 = ~670 lines, ~11k stays reasonable).
  </action>
  <verify>
  ```bash
  grep '<project_context>' agents/gsd-debugger.md
  grep '<project_context>' agents/gsd-project-researcher.md
  ```
  Both should return 1 match each.
  </verify>
  <done>debugger and project-researcher both have domain-adapted project_context blocks</done>
</task>

<task type="auto">
  <name>Task 2: Add project_context to roadmapper and validate all 10 agents</name>
  <files>agents/gsd-roadmapper.md</files>
  <action>
  **gsd-roadmapper.md:**

  Insert `<project_context>` block after closing `</role>` (line 40) and before `<downstream_consumer>` (line 42). Adapt for roadmapping domain per RESEARCH.md:
  - Intro: "Before creating the roadmap, discover project context:"
  - Step 3: "Load specific `rules/*.md` files as needed during roadmap creation"
  - Step 5: "Account for project skill patterns when structuring phases"
  - Closing: "This ensures roadmap phases account for project-specific conventions and existing patterns."

  After change, update `# estimated_tokens` comment in frontmatter (667 + ~15 = ~682 lines, ~11k stays reasonable).

  **Final validation** — Run the full audit across all 10 agents to confirm consistency:
  ```bash
  # All 10 have project_context
  grep -l '<project_context>' agents/gsd-*.md | wc -l  # Expected: 10

  # All 10 have PATH SETUP
  grep -l 'PATH SETUP' agents/gsd-*.md | wc -l  # Expected: 10

  # All 10 have structured_returns
  grep -l '<structured_returns>' agents/gsd-*.md | wc -l  # Expected: 10

  # No stale tags
  grep -l '<completion_format>' agents/gsd-*.md | wc -l  # Expected: 0
  grep -l '<process>' agents/gsd-codebase-mapper.md | wc -l  # Expected: 0
  ```
  </action>
  <verify>
  ```bash
  grep '<project_context>' agents/gsd-roadmapper.md  # 1 match

  # Full audit — the phase-level success criteria
  echo "=== project_context ===" && grep -l '<project_context>' agents/gsd-*.md | wc -l
  echo "=== PATH SETUP ===" && grep -l 'PATH SETUP' agents/gsd-*.md | wc -l
  echo "=== structured_returns ===" && grep -l '<structured_returns>' agents/gsd-*.md | wc -l
  ```
  All 3 counts must be 10.
  </verify>
  <done>roadmapper has project_context. All 10 agents pass the full structural consistency audit — project_context (10/10), PATH SETUP (10/10), structured_returns (10/10).</done>
</task>

</tasks>

<verification>
Run the definitive Phase 68 verification — all 3 requirements:

```bash
# ACON-01: All 10 agents have <project_context>
echo "ACON-01: $(grep -rl '<project_context>' agents/gsd-*.md | wc -l)/10 agents have project_context"

# ACON-02: All 10 agents have PATH SETUP (only codebase-mapper was missing)
echo "ACON-02: $(grep -rl 'PATH SETUP' agents/gsd-*.md | wc -l)/10 agents have PATH SETUP"

# ACON-03: codebase-mapper has structured_returns
echo "ACON-03: $(grep -c '<structured_returns>' agents/gsd-codebase-mapper.md) structured_returns in codebase-mapper"

# Bonus: all 10 have structured_returns
echo "Bonus: $(grep -rl '<structured_returns>' agents/gsd-*.md | wc -l)/10 agents have structured_returns"

# No stale tags
echo "Stale completion_format: $(grep -rl '<completion_format>' agents/gsd-*.md 2>/dev/null | wc -l)"
echo "Stale process tag: $(grep -c '<process>' agents/gsd-codebase-mapper.md 2>/dev/null)"
```

Expected: ACON-01=10/10, ACON-02=10/10, ACON-03=1, Bonus=10/10, stale=0, process=0
</verification>

<success_criteria>
- All 10 agents have `<project_context>` discovery blocks (ACON-01 fully satisfied)
- Each project_context is domain-adapted (not cookie-cutter)
- Combined with Plan 01: PATH SETUP 10/10, structured_returns 10/10
- No stale/inconsistent tags remain across any agent files
</success_criteria>

<output>
After completion, create `.planning/phases/68-agent-consistency-audit/68-02-SUMMARY.md`
</output>
