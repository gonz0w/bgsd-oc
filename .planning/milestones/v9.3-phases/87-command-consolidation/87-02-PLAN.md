---
phase: 87-command-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - commands/
  - workflows/help.md
autonomous: true
requirements:
  - CMND-02
  - CMND-03
  - CMND-04

must_haves:
  truths:
    - "Stale commands removed from slash command surface"
    - "Overlapping commands consolidated"
    - "Internal-only functions not exposed"
    - "Help documentation updated to reflect new structure"
  artifacts:
    - path: "commands/"
      provides: "Clean command directory with wrappers and essential originals"
    - path: "workflows/help.md"
      provides: "Updated help showing new subcommand structure"
  key_links:
    - from: "help.md"
      to: "commands/bgsd-*.md"
      via: "command reference update"
---

<objective>
Clean up stale and overlapping commands, remove internal-only functions from slash command surface, update help documentation.

Purpose: Complete the command consolidation by removing superseded commands while maintaining backward compatibility during transition.

Output: Updated commands/ directory, updated help.md
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-command-consolidation/COMMAND_CONSOLIDATION.md
@/mnt/raid/DEV/bgsd-oc/commands/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove or deprecate stale commands</name>
  <files>commands/bgsd-*.md</files>
  <action>
Based on COMMAND_CONSOLIDATION.md from plan 87-01, identify and remove stale commands:
- Check for commands that are no longer functional or useful
- Check for duplicate functionality across commands
- For commands to remove: delete the .md file from commands/

Common staleness indicators:
- Command that does nothing but delegate to another
- Command that has been superseded by a wrapper
- Command with no active workflow reference
  </action>
  <verify>ls commands/bgsd-*.md | wc -l (should be less than 50)</verify>
  <done>Stale commands identified and removed from commands/</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate overlapping commands</name>
  <files>commands/bgsd-*.md, workflows/</files>
  <action>
Identify and consolidate overlapping commands:
- Commands with similar functionality -> merge into single entry point
- Example: if /bgsd-velocity and /bgsd-context-budget both do performance analysis, consolidate

Look for:
- Similar workflow references
- Duplicate feature sets
- Commands that chain to each other
  </action>
  <verify>grep -l "similar.*functionality" commands/ (no results expected)</verify>
  <done>Overlapping commands consolidated into single entry points</done>
</task>

<task type="auto">
  <name>Task 3: Mark internal-only commands</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Identify commands that should be internal-only (not exposed as slash commands):
- Commands only called by other agents/workflows
- Implementation details not meant for direct user invocation
- Commands for debugging/development

For internal commands:
- Remove from commands/ directory
- Keep implementation in bin/bgsd-tools.cjs
- Document as internal in COMMAND_CONSOLIDATION.md

Examples might include:
- verify:agents (used by other agents)
- Some util:* commands called programmatically
  </action>
  <verify>grep -r "internal" COMMAND_CONSOLIDATION.md</verify>
  <done>Internal-only commands removed from slash command surface</done>
</task>

<task type="auto">
  <name>Task 4: Update help documentation</name>
  <files>workflows/help.md</files>
  <action>
Update workflows/help.md to reflect new command structure:
- Show new subcommand groups (/bgsd plan, /bgsd exec, etc.)
- Document backward compatibility (old commands still work during transition)
- List all available subcommands under each group
- Update quick start section

Use format:
## bGSD Commands (New Structure)

### /bgsd plan
- project - Initialize new project
- phase <n> - Plan a phase
- discuss <n> - Discuss a phase
...

### /bgsd exec
- phase <n> - Execute a phase
- quick - Quick task execution
...
  </action>
  <verify>grep -c "bgsd plan" workflows/help.md</verify>
  <done>Help documentation updated with new subcommand structure</done>
</task>

</tasks>

<verification>
- Verify stale commands removed (check commands/ count)
- Verify help.md shows new structure
- Verify internal commands not in commands/ directory
- Run /bgsd-help to verify command display works
</verification>

<success_criteria>
- Stale commands removed from slash command surface (CMND-02)
- Overlapping commands consolidated (CMND-03)
- Internal-only functions not exposed (CMND-04)
- Help documentation reflects new structure
</success_criteria>

<output>
After completion, create `.planning/phases/87-command-consolidation/87-02-SUMMARY.md`
</output>
