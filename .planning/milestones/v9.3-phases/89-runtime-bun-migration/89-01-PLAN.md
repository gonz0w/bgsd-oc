---
phase: 89-runtime-bun-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cli-tools/bun-runtime.js
  - src/router.js
  - src/lib/config.js
autonomous: true
requirements:
  - RUNT-01
user_setup: []

must_haves:
  truths:
    - "CLI detects Bun runtime at startup"
    - "Detection result is persisted in config for faster subsequent runs"
    - "Startup banner shows which runtime is being used (Bun vX.X.X or Node.js)"
  artifacts:
    - path: "src/lib/cli-tools/bun-runtime.js"
      provides: "Bun detection with config persistence"
      contains: "config.get|config.set"
    - path: "src/router.js"
      provides: "CLI startup integration"
      contains: "detectBun|showRuntimeBanner"
    - path: "src/lib/config.js"
      provides: "Runtime config option"
      contains: "runtime"
  key_links:
    - from: "src/router.js"
      to: "src/lib/cli-tools/bun-runtime.js"
      via: "require('./lib/cli-tools/bun-runtime')"
      pattern: "detectBun"
---

<objective>
Integrate Bun runtime detection at CLI startup with config persistence and user-visible runtime banner.

Purpose: Enable 3-5x startup improvement by detecting and using Bun when available, while informing users which runtime is active.
Output: Modified bun-runtime.js with config persistence, router.js with startup detection, runtime banner output.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-runtime-bun-migration/89-CONTEXT.md
@.planning/phases/89-runtime-bun-migration/89-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bun-runtime.js with config persistence</name>
  <files>src/lib/cli-tools/bun-runtime.js</files>
  <action>
1. Read existing src/lib/cli-tools/bun-runtime.js to understand current detection logic
2. Add import for config module: `const config = require('../config');`
3. Modify detectBun() to:
   - First check config for cached runtime preference (config.get('runtime'))
   - If config.runtime is 'node', skip detection and return { available: false, fromConfig: true }
   - If config.runtime is 'bun', assume available (skip detection for speed)
   - Otherwise run detection as normal
4. After successful detection, cache result in config: config.set('bun.detected', version)
5. Add function getCachedBunVersion() that returns cached version from config
6. Ensure all detection is wrapped in try-catch for silent failure
  </action>
  <verify>node -e "const br = require('./src/lib/cli-tools/bun-runtime'); console.log(JSON.stringify(br.detectBun()))"</verify>
  <done>detectBun() returns {available, version?, fromConfig?} and config.set is called after detection</done>
</task>

<task type="auto">
  <name>Task 2: Add startup detection in router.js</name>
  <files>src/router.js</files>
  <action>
1. Read src/router.js to understand startup flow
2. Add require for bun-runtime: `const { detectBun, getCachedBunVersion } = require('./lib/cli-tools/bun-runtime');`
3. Add require for config: `const config = require('./lib/config');`
4. At the top of router initialization (before parsing args):
   - Check config for forced runtime: config.get('runtime')
   - Call detectBun() to get runtime status
   - If Bun available and not already running under Bun, determine if should re-execute
   - Set environment flag to prevent re-execution loop: process.env.BGSD_RUNTIME_DETECTED='true'
5. Add showRuntimeBanner(runtime, version, isFallback) function that:
   - If runtime === 'bun' and isFallback === false: console.log('[bGSD] Running with Bun v' + version)
   - If runtime === 'node' and isFallback === true: console.log('[bGSD] Falling back to Node.js')
   - Check verbose flag to show extra details
6. Call showRuntimeBanner at appropriate point in startup
7. Handle re-execution with Bun if applicable (using execFileSync pattern from RESEARCH)
  </action>
  <verify>node src/router.js --help 2>&1 | head -5</verify>
  <done>router.js integrates Bun detection at startup, banner appears showing runtime</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime config option to config schema</name>
  <files>src/lib/config.js</files>
  <action>
1. Read src/lib/config.js to find CONFIG_SCHEMA
2. Add runtime option to CONFIG_SCHEMA:
   ```javascript
   runtime: {
     type: 'string',
     default: 'auto',
     description: 'Runtime to use (auto/bun/node)',
     aliases: ['runtime'],
     nested: null
   }
   ```
3. Ensure config.get('runtime') returns 'auto', 'bun', or 'node'
4. Add bun.detected key to store cached detection result
  </action>
  <verify>node -e "const c = require('./src/lib/config'); console.log(c.get('runtime'))"</verify>
  <done>config.get('runtime') returns 'auto' by default, can be set to 'bun' or 'node'</done>
</task>

</tasks>

<verification>
- Run `node src/router.js --help` and verify banner appears
- Run with Bun installed (if available) and verify "Running with Bun vX.X.X" appears
- Run without Bun and verify "Falling back to Node.js" appears or silent fallback
- Test config: set runtime to 'node' and verify fallback behavior
</verification>

<success_criteria>
- CLI shows runtime banner at startup
- Bun detection cached in config after first run
- Config option 'runtime' available (auto/bun/node)
- Re-execution with Bun works without infinite loop
</success_criteria>

<output>
After completion, create `.planning/phases/89-runtime-bun-migration/89-01-SUMMARY.md`
</output>
