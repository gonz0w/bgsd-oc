---
phase: 89-runtime-bun-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 89-01
files_modified:
  - src/lib/cli-tools/bun-runtime.js
  - bin/bgsd-tools.cjs
  - src/commands/runtime.js
autonomous: true
requirements:
  - RUNT-02
  - RUNT-03
user_setup: []

must_haves:
  truths:
    - "Projects without Bun work exactly as before (backward compatible)"
    - "Bundle size not significantly increased by Bun support"
    - "3-5x startup improvement demonstrated when Bun available"
  artifacts:
    - path: "src/lib/cli-tools/bun-runtime.js"
      provides: "Fallback logic to Node.js when Bun unavailable"
      contains: "fallback|node"
    - path: "bin/bgsd-tools.cjs"
      provides: "Bundle size baseline"
      contains: "bun"
  key_links:
    - from: "bin/bgsd-tools.cjs"
      to: "src/lib/cli-tools/bun-runtime.js"
      via: "require"
      pattern: "bun-runtime"
---

<objective>
Validate backward compatibility and bundle size, demonstrate 3-5x startup improvement.

Purpose: Ensure projects without Bun work identically, bundle size not increased, and Bun provides measurable speedup.
Output: Bundle size report, benchmark comparison showing 3-5x improvement.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-runtime-bun-migration/89-CONTEXT.md
@.planning/phases/89-runtime-bun-migration/89-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add force-node config option and verify fallback logic</name>
  <files>src/lib/cli-tools/bun-runtime.js</files>
  <action>
1. Extend the existing detectBun() function to handle forced Node.js:
   - If config.get('runtime') === 'node', return { available: false, forced: true }
   - If config.get('runtime') === 'bun', return { available: true, version: 'forced', forced: true }
2. Add environment variable override: check process.env.BGSD_RUNTIME for 'node', 'bun', or 'auto'
   - Env var takes precedence over config
3. Add test scenarios:
   - config.runtime='node' -> should always fallback to Node.js
   - config.runtime='auto' -> should detect normally
   - config.runtime='bun' -> should use Bun even if not installed (for error messaging)
4. Verify fallback is silent (no errors, just uses Node.js)
  </action>
  <verify>node -e "const c = require('./src/lib/config'); c.set('runtime', 'node'); const br = require('./src/lib/cli-tools/bun-runtime'); console.log(JSON.stringify(br.detectBun()))"</verify>
  <done>Setting runtime to 'node' causes detectBun() to return {available: false, forced: true}</done>
</task>

<task type="auto">
  <name>Task 2: Validate bundle size not significantly increased</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
1. Check current bundle size: ls -la bin/bgsd-tools.cjs
2. Compare to v8.1 baseline (~1216KB from INTENT.md SC-26)
3. If bundle increased >10%, analyze what added the size:
   - Check if Bun detection code is minimal
   - Look for any unnecessary Bun-specific code
   - Ensure lazy loading where possible
4. If bundle is acceptable (<1500KB), document in verification
5. Report bundle size in output
  </action>
<verify>ls -la bin/bgsd-tools.cjs | awk '{print $5}'</verify>
  <done>Bundle size reported and compared to baseline</done>
</task>

<task type="auto">
<name>Task 3: Demonstrate 3-5x startup improvement with benchmark</name>
<files>src/commands/runtime.js</files>
  <action>
1. Run the existing benchmark command to measure startup:
   - First with Node.js: time node bin/bgsd-tools.cjs --help
   - Then with Bun if available: time bun run bin/bgsd-tools.cjs --help
2. Use the existing runtime benchmark if implemented in src/commands/runtime.js
3. Run multiple iterations (5+) and take average to account for variance
4. Calculate speedup ratio: Node.js time / Bun time
5. Verify speedup is 3-5x or document actual results
6. If Bun not available, document expected improvement based on RESEARCH
  </action>
<verify>node bin/bgsd-tools.cjs runtime benchmark 2>&1 | head -20</verify>
  <done>Startup benchmark shows X.XXx improvement (target: 3-5x)</done>
</task>

</tasks>

<verification>
- Set runtime to 'node' in config, verify CLI still works normally
- Check bundle size is within acceptable range
- Run benchmark and document startup improvement
- Test with both Node.js and Bun (if available)
</verification>

<success_criteria>
- Config runtime='node' works without errors
- Bundle size <1500KB or within 10% of baseline
- Startup improvement demonstrated (actual or expected 3-5x)
- All fallback scenarios tested and working
</success_criteria>

<output>
After completion, create `.planning/phases/89-runtime-bun-migration/89-02-SUMMARY.md`
</output>
