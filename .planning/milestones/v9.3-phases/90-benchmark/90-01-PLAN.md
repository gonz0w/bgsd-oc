---
phase: 90-benchmark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BENCH-01, BENCH-02]
intent:
  outcome_ids: [DO-48]
user_setup: []

must_haves:
  truths:
    - /bgsd-measure command runs and outputs table-formatted metrics
    - Benchmark measures startup time (cold and warm)
    - Benchmark measures command execution time
    - Benchmark measures memory usage
    - Benchmark measures context load time
    - --verbose flag shows full metrics in table format
    - Build-time feature flag controls benchmark code inclusion
    - Baseline metrics stored in .planning/config.json or .planning/benchmarks/
  artifacts:
    - src/lib/cli-tools/plugin-benchmark.js (new benchmark module)
    - src/commands/measure.js (new /bgsd-measure command)
    - build.cjs updated with INCLUDE_BENCHMARKS feature flag
  key_links:
    - Extends existing bun-runtime.js benchmark patterns
    - Uses process.hrtime.bigint() for timing
    - Uses process.memoryUsage() for memory measurement
    - Uses execFileSync for subprocess timing
---

<objective>

Purpose: Build plugin benchmark adapter for cross-plugin comparison and capture baseline metrics for v9.3

Output: Plugin benchmark adapter with /bgsd-measure command that measures startup time, command execution, memory usage, and context loading

</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-benchmark/90-CONTEXT.md
@.planning/phases/90-benchmark/90-RESEARCH.md

src/lib/cli-tools/bun-runtime.js (existing benchmark patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin-benchmark.js module</name>
  <files>src/lib/cli-tools/plugin-benchmark.js</files>
  <action>
Create new plugin-benchmark.js module in src/lib/cli-tools/ with:
- measureStartup(): Measure cold start (fresh subprocess) and warm cache (cached modules) times
- measureCommandExecution(): Time execution of various bgsd commands
- measureContextLoad(): Time loading of STATE.md, ROADMAP.md, REQUIREMENTS.md, PROJECT.md
- measureMemory(): Capture process.memoryUsage() heap and RSS
- formatTable(): Format results as human-readable table with columns
- Benchmark includes both cold start and warm cache results
- Uses process.hrtime.bigint() for nanosecond precision timing
- Uses process.memoryUsage() for memory stats
- Single run execution (not averaged)
- Follows existing bun-runtime.js patterns (lines 319-601)
  </action>
  <verify>node -e "const {measureStartup, measureCommandExecution, measureContextLoad, measureMemory} = require('./src/lib/cli-tools/plugin-benchmark'); console.log('Module loads:', typeof measureStartup)"</verify>
  <done>plugin-benchmark.js created with all measurement functions exported</done>
</task>

<task type="auto">
  <name>Task 2: Create /bgsd-measure command</name>
  <files>src/commands/measure.js</files>
  <action>
Create new measure.js command with:
- Implements /bgsd-measure subcommand (undocumented in production)
- Default output: table format with columns (Metric | Cold | Warm)
- --verbose flag shows full metrics: startup, command execution, memory, context load
- No --json flag (per user constraint)
- Single run execution (report that result, no averaging)
- Calls plugin-benchmark.js functions
- Output follows table format from 90-CONTEXT.md
- Feature flag controls whether command is included in production build
  </action>
  <verify>node bin/bgsd-tools.cjs measure --help 2>&1 | head -5</verify>
  <done>/bgsd-measure command registered and responds to --help</done>
</task>

<task type="auto">
  <name>Task 3: Add build-time feature flag</name>
  <files>build.cjs</files>
  <action>
Update build.cjs to:
- Add INCLUDE_BENCHMARKS define flag for esbuild
- When INCLUDE_BENCHMARKS=false: exclude measure.js command and plugin-benchmark.js from bundle
- When INCLUDE_BENCHMARKS=true: include benchmark code
- Default: INCLUDE_BENCHMARKS=true for development
- Documentation (help text) behind feature flag (not visible when disabled)
  </action>
  <verify>npm run build 2>&1 | grep -E "(benchmark|measure)" || echo "Build completed"</verify>
  <done>Build completes with feature flag support</done>
</task>

<task type="auto">
  <name>Task 4: Capture baseline metrics</name>
  <files>.planning/config.json or .planning/benchmarks/</files>
  <action>
Run /bgsd-measure and capture baseline metrics:
- Execute benchmark and capture results
- Store baseline in .planning/benchmarks/v9.3-baseline.json or config.json
- Record: startup time (cold/warm), command execution time, memory usage, context load time
- These become the v9.3 baseline for future comparison
  </action>
  <verify>ls -la .planning/benchmarks/ 2>/dev/null || ls -la .planning/config.json</verify>
  <done>Baseline metrics stored for v9.3</done>
</task>

</tasks>

<verification>
- /bgsd-measure runs without errors
- Table output displays correctly (default mode)
- --verbose shows full metrics
- Both cold start and warm cache results shown
- No JSON output (per constraint)
- Feature flag successfully excludes benchmarks when disabled
</verification>

<success_criteria>
Plugin benchmark adapter built for cross-plugin comparison (BENCH-01)
Baseline metrics captured for v9.3 (BENCH-02)
Benchmark measures: startup time, command execution, context loading (BENCH-03 implicit)
</success_criteria>

<output>
After completion, create `.planning/phases/90-benchmark/90-01-SUMMARY.md`
</output>
