---
phase: 88-quality-and-context
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/deps.js
  - src/lib/ast.js
  - src/lib/codebase-intel.js
autonomous: true
requirements:
  - CTXT-02
  - CTXT-03
user_setup: []

must_haves:
  truths:
    - "Every exported function is imported by at least one other file"
    - "Every workflow/template is referenced by a command or agent"
    - "Dead code audit produces zero orphaned items"
  artifacts:
    - path: "src/lib/deps.js"
      provides: "Reachability audit function"
      contains: "findOrphanedExports|findOrphanedWorkflows"
    - path: "src/lib/codebase-intel.js"
      provides: "CLI command to run orphan audit"
      contains: "audit-orphans"
  key_links:
    - from: "src/lib/deps.js"
      to: "src/lib/ast.js"
      via: "extractExports"
      pattern: "extractExports"
---

<objective>
Build a reachability audit system that reports orphaned exports, files, workflows, templates, and config entries.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-quality-and-context/88-RESEARCH.md
@src/lib/deps.js
@src/lib/ast.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reachability audit for exports</name>
  <files>src/lib/deps.js</files>
  <action>
1. Add function `findOrphanedExports(intel, graph)` that:
   - Uses existing extractExports() from ast.js to get all exports per file
   - Uses existing buildDependencyGraph() to get import relationships  
   - For each named export, checks if any other file imports it
   - Returns array of { file, export_name, reason: "not imported" }

2. Add function `findOrphanedFiles(intel, graph)` that:
   - Lists all files in intel
   - Checks if file appears in any import graph edge
   - Excludes entry points (bin files, main entry files, command files)

3. Handle known runtime patterns: allow marking items as "known runtime" in config (exclude from audit)
  </action>
  <verify>
grep -n "findOrphanedExports\|findOrphanedFiles" src/lib/deps.js</verify>
  <done>
Reachability audit functions added to deps.js</done>
</task>

<task type="auto">
  <name>Task 2: Add reachability check for workflows/templates/configs</name>
  <files>src/lib/deps.js</files>
  <action>
1. Add function `findOrphanedWorkflows(intel)` that:
   - Scans workflows/*.md files
   - Checks if any command file or agent references them (via @path references)
   - Reports orphaned workflows

2. Add function `findOrphanedTemplates(intel)` that:
   - Scans templates/*.md files
   - Checks if referenced by workflow or command files
   - Reports orphaned templates

3. Add function `findOrphanedConfigs(intel)` that:
   - Checks config.json entries are referenced by some code
   - Checks skills referenced by agents exist and are valid
  </action>
  <verify>
grep -n "findOrphanedWorkflows\|findOrphanedTemplates" src/lib/deps.js</verify>
  <done>
Workflow/template/config orphan detection added</done>
</task>

<task type="auto">
  <name>Task 3: Integrate audit into codebase-intel CLI</name>
  <files>src/lib/codebase-intel.js</files>
  <action>
1. Add command/function to run full orphan audit:
   - Load intel
   - Build dependency graph  
   - Run all findOrphaned* functions
   - Output summary: { orphaned_exports: [], orphaned_files: [], orphaned_workflows: [], orphaned_templates: [] }

2. Export a CLI-friendly function `auditOrphans(cwd)` that can be called via:
   - node bin/bgsd-tools.cjs verify:orphans
   - or similar

3. Ensure audit can be run independently and produces clean output
  </action>
  <verify>
node bin/bgsd-tools.cjs verify:orphans 2>&1 | head -20</verify>
  <done>
Orphan audit command functional and produces report</done>
</task>

</tasks>

<verification>
- Orphan audit runs without errors
- Report shows all categories of orphaned items
- Zero false positives for valid code
</verification>

<success_criteria>
CTXT-02: Zero orphaned code — every function, export, workflow, template, and config entry is reachable
CTXT-03: Dead code audit produces zero orphaned items
</success_criteria>

<output>
After completion, create `.planning/phases/88-quality-and-context/88-02-SUMMARY.md`
</output>
