---
phase: 88-quality-and-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/codebase-intel.js
  - src/lib/context.js
  - .planning/codebase/codebase-intel.json
autonomous: true
requirements:
  - CTXT-01
user_setup: []

must_haves:
  truths:
    - "Agents receive pre-computed context, not search-and-discover"
    - "Context cache is invalidated when git commit changes"
    - "Pre-scoped contexts match what scopeContextForAgent() would produce"
  artifacts:
    - path: "src/lib/codebase-intel.js"
      provides: "Agent context caching with git hash invalidation"
      contains: "agent_contexts"
    - path: "src/lib/context.js"
      provides: "Retrieve cached agent context"
      contains: "getCachedAgentContext"
    - path: ".planning/codebase/codebase-intel.json"
      provides: "Pre-computed agent contexts"
      contains: "agent_contexts"
  key_links:
    - from: "src/lib/codebase-intel.js"
      to: "src/lib/context.js"
      via: "AGENT_MANIFESTS"
      pattern: "scopeContextForAgent"
---

<objective>
Implement deterministic context loading where agents receive pre-computed context keyed by git commit hash, eliminating on-demand search-and-discover.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-quality-and-context/88-RESEARCH.md
@src/lib/context.js
@src/lib/codebase-intel.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend codebase-intel.json to include agent_contexts</name>
  <files>src/lib/codebase-intel.js</files>
  <action>
1. In codebase-intel.js, add a function `generateAgentContexts(cwd, intel)` that:
   - Loads current init data using existing init:output logic
   - For each agent type in AGENT_MANIFESTS (bgsd-executor, bgsd-verifier, bgsd-planner, bgsd-phase-researcher, bgsd-plan-checker, bgsd-reviewer), computes scoped context using scopeContextForAgent()
   - Returns object: { [agentType]: { generated_at, fields: {...}, _savings } }

2. Modify the main intel generation to call this function and include result under `agent_contexts` key

3. Use git commit hash for cache invalidation - compare current HEAD with stored hash in intel file
  </action>
  <verify>
grep -n "agent_contexts" src/lib/codebase-intel.js && echo "Function added"</verify>
  <done>
codebase-intel.json includes agent_contexts keyed by agent type</done>
</task>

<task type="auto">
  <name>Task 2: Add git hash-based cache invalidation</name>
  <files>src/lib/codebase-intel.js</files>
  <action>
1. Add function `isIntelStale(cwd)` that:
   - Reads existing codebase-intel.json 
   - Gets current git commit hash via getGitInfo()
   - Returns true if hashes differ (stale) or if agent_contexts is missing

2. Modify readIntel() or create new readIntelWithCache() to check staleness and return cached contexts only when valid

3. Ensure stale cache triggers regeneration on next init
  </action>
  <verify>
grep -n "isIntelStale\|git_commit_hash" src/lib/codebase-intel.js</verify>
  <done>
Cache invalidation triggers when git commit changes</done>
</task>

<task type="auto">
  <name>Task 3: Add getCachedAgentContext() to context.js</name>
  <files>src/lib/context.js</files>
  <action>
1. Add function `getCachedAgentContext(cwd, agentType)` that:
   - Calls readIntelWithCache() to get current intel
   - Returns intel.agent_contexts[agentType] if present
   - Falls back to computing context on-demand via scopeContextForAgent() if cache miss

2. Export this function in module.exports

3. Verify output matches what scopeContextForAgent() would produce for same agentType
  </action>
  <verify>
node -e "const {getCachedAgentContext}=require('./src/lib/context'); console.log(typeof getCachedAgentContext)"</verify>
  <done>
getCachedAgentContext returns pre-computed context or computes on-demand</done>
</task>

</tasks>

<verification>
- codebase-intel.json contains agent_contexts after regeneration
- Cache invalidates when HEAD changes
- getCachedAgentContext returns same fields as scopeContextForAgent
</verification>

<success_criteria>
CTXT-01: Context loading is deterministic — agents receive pre-computed context, not search-and-discover
</success_criteria>

<output>
After completion, create `.planning/phases/88-quality-and-context/88-01-SUMMARY.md`
</output>
