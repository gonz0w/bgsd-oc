---
phase: 86-agent-sharpening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02

must_haves:
  truths:
    - "Each agent has documented single responsibility in its manifest"
    - "No two agents claim overlapping capabilities"
    - "Agent boundaries validated via automated script"
    - "Agent manifests contain essential fields (name, description, capabilities)"
  artifacts:
    - path: "agents/*.md"
      provides: "Agent manifests with documented responsibilities"
      contains: "single responsibility, capabilities, input/output types"
    - path: "bin/bgsd-tools.cjs"
      provides: "Boundary validation script"
      contains: "overlap detection, capability comparison"
  key_links:
    - from: "agents/*.md"
      to: "bin/bgsd-tools.cjs"
      via: "validation script parses manifests"
      pattern: "readFile.*agents.*parse.*capabilities"
---

<objective>
Audit all agent manifests and validate boundaries to ensure zero overlap between agent responsibilities.

Purpose: Establish clear agent responsibilities with no capability overlap. Each agent must have a single, documented responsibility that doesn't conflict with other agents.

Output: Agent manifest documentation, boundary validation script, overlap report
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-agent-sharpening/86-CONTEXT.md

# Relevant source files
@/home/cam/.config/opencode/agents/bgsd-planner.md
@/home/cam/.config/opencode/agents/bgsd-executor.md
@/home/cam/.config/opencode/agents/bgsd-verifier.md
@/home/cam/.config/opencode/agents/bgsd-debugger.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing agent manifests</name>
  <files>/home/cam/.config/opencode/agents/*.md</files>
  <action>
1. Read all agent definition files in /home/cam/.config/opencode/agents/
2. For each agent, extract: name, description, capabilities, primary role
3. Document each agent's single responsibility in a consistent format
4. Verify each manifest has essential fields: name, description, capabilities, input/output types
</action>
  <verify>grep -l "description\|capabilities\|tools" /home/cam/.config/opencode/agents/*.md | wc -l</verify>
  <done>
- All 10 agent files have documented responsibilities
- Each agent has: name, description, capabilities, primary role
- No agents have undefined or vague responsibilities
</done>
</task>

<task type="auto">
  <name>Task 2: Create boundary validation script</name>
  <files>/mnt/raid/DEV/bgsd-oc/bin/bgsd-tools.cjs</files>
  <action>
1. Add validation function to bgsd-tools.cjs that:
   - Parses all agent .md files
   - Extracts capabilities section from each
   - Compares capabilities for overlap
   - Detects dependency cycles
2. Output format: JSON report with overlap findings
3. On overlap detected: Report for manual review (per CONTEXT.md decision)
</action>
  <verify>node /mnt/raid/DEV/bgsd-oc/bin/bgsd-tools.cjs verify:agents --check-overlap 2>&1 | head -20</verify>
  <done>
- Command 'bgsd-tools verify:agents --check-overlap' works
- Produces JSON report of agent capabilities
- Detects and reports any capability overlap
</done>
</task>

<task type="auto">
  <name>Task 3: Run validation and generate overlap report</name>
  <files>/mnt/raid/DEV/bgsd-oc/.planning/phases/86-agent-sharpening/86-01-OVERLAP-REPORT.md</files>
  <action>
1. Run the boundary validation script against all agents
2. Review output for any overlaps or conflicts
3. Generate overlap report documenting findings
4. If overlaps found, document for manual review per CONTEXT.md decision
</action>
  <verify>cat .planning/phases/86-agent-sharpening/86-01-OVERLAP-REPORT.md</verify>
  <done>
- Overlap report exists
- All agent boundaries validated
- Any overlaps documented for review
</done>
</task>

</tasks>

<verification>
- All 10 agents have documented single responsibilities
- No two agents claim the same capability
- Boundary validation script runs successfully
- Overlap report generated
</verification>

<success_criteria>
- [ ] Each agent has single, documented responsibility in manifest
- [ ] Agent boundaries validated via automated script
- [ ] Zero capability overlap between agents
- [ ] Overlap report generated
</success_criteria>

<output>
After completion, create `.planning/phases/86-agent-sharpening/86-01-SUMMARY.md`
</output>
