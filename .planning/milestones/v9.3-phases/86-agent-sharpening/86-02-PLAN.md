---
phase: 86-agent-sharpening
plan: "02"
type: execute
wave: 2
depends_on:
  - "86-01"
files_modified: []
autonomous: true
requirements:
  - AGNT-03

must_haves:
  truths:
    - "Handoff contracts documented for all agent pairs"
    - "Each contract includes inputs, outputs, preconditions"
    - "Handoff contracts stored in RACI skill document"
    - "Templates guide handoff documentation"
  artifacts:
    - path: "/home/cam/.config/opencode/skills/raci/SKILL.md"
      provides: "RACI skill document with handoff contracts"
      contains: "inputs, outputs, preconditions for each agent pair"
    - path: "/home/cam/.config/opencode/skills/raci/references/RACI.md"
      provides: "Detailed handoff contract templates"
      contains: "required sections for each contract"
  key_links:
    - from: "RACI skill document"
      to: "All agent definitions"
      via: "handoff contracts reference agent responsibilities"
      pattern: "handoff.*agent.*input.*output"
---

<objective>
Document and enforce agent handoff contracts, storing them in the RACI skill document.

Purpose: Ensure smooth handoffs between agents by documenting what each agent needs to receive (inputs), what it produces (outputs), and what must be true before handoff (preconditions).

Output: Updated RACI skill document with full handoff contracts
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/86-agent-sharpening/86-CONTEXT.md

# RACI skill (handles handoff contracts per CONTEXT.md decision)
@/home/cam/.config/opencode/skills/raci/SKILL.md

# Prior plan summary (86-01 establishes agent boundaries)
@.planning/phases/86-agent-sharpening/86-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document handoff contracts in RACI skill</name>
  <files>/home/cam/.config/opencode/skills/raci/SKILL.md</files>
  <action>
1. Read existing RACI skill document
2. For each handoff contract (from RACI matrix), add:
   - Inputs: What the receiving agent needs to receive
   - Outputs: What the responsible agent produces
   - Preconditions: What must be true before handoff
3. Per CONTEXT.md: Store in RACI skill document, include all three (inputs, outputs, preconditions)
</action>
  <verify>grep -c "inputs\|outputs\|preconditions" /home/cam/.config/opencode/skills/raci/SKILL.md</verify>
  <done>
- RACI skill updated with full handoff contracts
- Each contract has inputs, outputs, preconditions documented
- All agent pairs covered
</done>
</task>

<task type="auto">
  <name>Task 2: Add handoff contract templates</name>
  <files>/home/cam/.config/opencode/skills/raci/references/RACI.md</files>
  <action>
1. Create or update references/RACI.md with handoff contract templates
2. Template structure for each contract:
   - Responsible agent -> Receiving agent
   - Required inputs (data, context, artifacts)
   - Produced outputs (files, state, decisions)
   - Preconditions (what must be true before handoff)
3. Per CONTEXT.md: Templates guide but don't enforce
</action>
<verify>cat /home/cam/.config/opencode/skills/raci/references/RACI.md | head -50</verify>
  <done>
- Handoff contract templates exist
- Templates cover all required sections
- Templates available for agent authors
</done>
</task>

<task type="auto">
  <name>Task 3: Verify contracts reference agent manifests</name>
  <files>/home/cam/.config/opencode/skills/raci/SKILL.md</files>
  <action>
1. Verify handoff contracts reference the agent responsibilities documented in 86-01
2. Ensure inputs/outputs align with agent capabilities
3. Confirm preconditions match what's actually needed
</action>
<verify>grep -E "bgsd-planner|bgsd-executor|bgsd-verifier" /home/cam/.config/opencode/skills/raci/SKILL.md | wc -l</verify>
  <done>
- Handoff contracts reference agent responsibilities
- Inputs/outputs aligned with agent capabilities
- Preconditions are realistic
</done>
</task>

</tasks>

<verification>
- RACI skill has all handoff contracts documented
- Each contract has inputs, outputs, preconditions
- Templates exist in references/RACI.md
- Contracts reference validated agent responsibilities
</verification>

<success_criteria>
- [ ] Handoff contracts documented for all agent pairs
- [ ] Each contract includes inputs, outputs, preconditions
- [ ] Stored in RACI skill document per CONTEXT.md decision
- [ ] Templates guide handoff documentation
</success_criteria>

<output>
After completion, create `.planning/phases/86-agent-sharpening/86-02-SUMMARY.md`
</output>
