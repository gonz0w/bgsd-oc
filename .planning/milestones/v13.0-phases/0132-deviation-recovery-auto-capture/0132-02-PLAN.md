---
phase: 0132-deviation-recovery-auto-capture
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - workflows/execute-plan.md
  - src/lib/constants.js
  - src/lib/commandDiscovery.js
  - src/lib/command-help.js
autonomous: true
requirements:
  - DEVCAP-02
  - DEVCAP-03
  - DEVCAP-04
intent:
  outcome_ids: [DO-94]

must_haves:
  truths:
    - "execute-plan.md deviation_rules section includes auto-capture hook instructions after successful Rule-1 recovery"
    - "Hook calls `lessons:deviation-capture --rule 1 --failure-count N --behavioral-change 'description' --agent AGENT 2>/dev/null || true`"
    - "Hook only fires after a SUCCESSFUL Rule-1 recovery (not on failure or escalation)"
    - "Hook never fires for Rule-3 environmental failures — the command itself filters, but workflow text also explicitly excludes Rule-3"
    - "Non-blocking: `2>/dev/null || true` suffix ensures capture failure never blocks execution"
    - "constants.js help text documents lessons:deviation-capture usage"
  artifacts:
    - path: "workflows/execute-plan.md"
      provides: "Deviation auto-capture hook in execute_waves step"
    - path: "src/lib/constants.js"
      provides: "Help text for lessons:deviation-capture"
    - path: "src/lib/commandDiscovery.js"
      provides: "Updated lessons command list including deviation-capture"
    - path: "src/lib/command-help.js"
      provides: "Help metadata for lessons:deviation-capture"
  key_links:
    - from: "workflows/execute-plan.md"
      to: "src/commands/lessons.js"
      via: "CLI invocation: bgsd-tools lessons:deviation-capture"
      pattern: "lessons:deviation-capture"
---

<objective>
Add the deviation auto-capture hook to the execute-plan workflow and complete the help/discovery metadata for `lessons:deviation-capture`.

Purpose: Wire the capture command (from Plan 01) into the execution workflow so agents automatically capture winning Rule-1 recovery patterns after successful deviation fixes.
Output: Updated execute-plan.md with capture hook + complete help/discovery entries.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/INTENT.md
@.planning/phases/0132-deviation-recovery-auto-capture/0132-CONTEXT.md
@.planning/phases/0132-deviation-recovery-auto-capture/0132-01-PLAN.md
@workflows/execute-plan.md
@src/lib/constants.js
@src/lib/commandDiscovery.js
@src/lib/command-help.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deviation auto-capture hook to execute-plan.md</name>
  <files>workflows/execute-plan.md</files>
  <action>
**Add a `<deviation_auto_capture>` section** immediately after the closing `</deviation_rules>` tag (after line 122) in `workflows/execute-plan.md`:

```markdown
<deviation_auto_capture>
After a SUCCESSFUL Rule 1 (Bug) deviation recovery — meaning the fix was applied, verified working, and execution continued — auto-capture the recovery pattern as a structured lesson:

```bash
node __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs lessons:deviation-capture \
  --rule 1 \
  --failure-count "${FAILURE_COUNT}" \
  --behavioral-change "${WHAT_FIXED_IT}" \
  --agent "${AGENT_NAME}" \
  2>/dev/null || true
```

Where:
- `FAILURE_COUNT`: Number of failed attempts before the successful recovery (1-3)
- `WHAT_FIXED_IT`: Brief description of the behavioral change that resolved the issue (e.g., "Added null check before property access", "Used path.resolve instead of relative path")
- `AGENT_NAME`: The agent that performed the recovery (e.g., bgsd-executor)

**Rules:**
- ONLY capture after Rule 1 (Bug) recoveries — the command filters internally, but do NOT invoke for Rule 3 (Blocking/environmental) failures like missing deps, npm errors, or permission issues
- ONLY capture after SUCCESSFUL recovery (fix verified working) — never on failure or escalation
- The `2>/dev/null || true` suffix is MANDATORY — capture must never block execution
- The command silently stops after 3 captures per milestone — no action needed from the executor
- Do NOT capture Rule 2 (Missing Critical) or Rule 4 (Architectural) deviations — only Rule 1 code bugs carry reusable behavioral patterns
</deviation_auto_capture>
```

This is a workflow instruction section — it tells executor agents when and how to invoke the capture command. The `__OPENCODE_CONFIG__` placeholder is standard in workflow files and gets resolved at runtime.
  </action>
  <verify>
1. `grep "deviation_auto_capture" workflows/execute-plan.md` finds the new section
2. `grep "lessons:deviation-capture" workflows/execute-plan.md` finds the CLI invocation
3. `grep "2>/dev/null || true" workflows/execute-plan.md` confirms non-blocking suffix
4. The section explicitly mentions "ONLY capture after Rule 1" and "do NOT invoke for Rule 3"
  </verify>
  <done>
execute-plan.md has a `deviation_auto_capture` section that instructs executor agents to call `lessons:deviation-capture` after successful Rule-1 recoveries, with non-blocking error handling and clear rules about when NOT to capture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add help/discovery metadata + rebuild + test</name>
  <files>src/lib/constants.js, src/lib/commandDiscovery.js, src/lib/command-help.js</files>
  <action>
**1. Add help text in `src/lib/constants.js`:**

Find the `'lessons:capture'` help entry and add a new entry after it for `'lessons:deviation-capture'`:

```javascript
'lessons:deviation-capture': `Usage: bgsd-tools lessons:deviation-capture --rule <number> --failure-count <number> --behavioral-change <text> --agent <name>

Captures a deviation recovery pattern as a structured lesson entry.
Only Rule 1 (code bug) recoveries are captured — all other rules are silently filtered.
Capped at 3 entries per milestone to prevent noise.

Options:
  --rule               Deviation rule number (1=bug, 2=missing, 3=blocking, 4=architectural)
  --failure-count      Number of failed attempts before successful recovery
  --behavioral-change  Description of what behavioral change fixed the issue
  --agent              Name of the agent that performed the recovery

Examples:
  bgsd-tools lessons:deviation-capture --rule 1 --failure-count 2 --behavioral-change "Added null check before property access" --agent bgsd-executor
  bgsd-tools lessons:deviation-capture --rule 3 --failure-count 1 --behavioral-change "Reinstalled deps" --agent bgsd-executor  # → silently filtered (Rule 3)`,
```

**2. Update `src/lib/commandDiscovery.js`:**

Find the lessons command list (line ~63) and add `'lessons:deviation-capture'` to the commands array.

**3. Update `src/lib/command-help.js`:**

- Add `'lessons:deviation-capture'` to the lessons commands list (around line 100)
- Add description: `'lessons:deviation-capture': 'Auto-capture deviation recovery pattern as structured lesson'`
- Add related commands: `'lessons:deviation-capture': ['lessons:capture', 'lessons:list']`

**4. Rebuild:** Run `npm run build` to bundle all changes into `bin/bgsd-tools.cjs`.

**5. Test:** Run `npm test` to verify no regressions.
  </action>
  <verify>
1. `node bin/bgsd-tools.cjs --help lessons:deviation-capture` shows usage text
2. `grep "deviation-capture" src/lib/commandDiscovery.js` confirms discovery entry
3. `grep "deviation-capture" src/lib/command-help.js` confirms help metadata
4. `npm run build` succeeds
5. `npm test` passes
  </verify>
  <done>
`lessons:deviation-capture` has complete help text, command discovery, and help metadata. Build passes. Tests pass.
  </done>
</task>

</tasks>

<verification>
1. `grep "deviation_auto_capture" workflows/execute-plan.md` finds the hook section
2. `grep "lessons:deviation-capture" workflows/execute-plan.md` confirms CLI command in workflow
3. `grep "2>/dev/null || true" workflows/execute-plan.md` confirms non-blocking
4. `node bin/bgsd-tools.cjs --help lessons:deviation-capture` shows complete usage
5. `npm test` passes with no regressions
</verification>

<success_criteria>
1. execute-plan.md has explicit auto-capture instructions that fire only after successful Rule-1 recovery
2. Rule-3 environmental failures are explicitly excluded in workflow text
3. Non-blocking pattern (`2>/dev/null || true`) is present
4. Help and discovery metadata is complete for the new subcommand
5. Build and tests pass
</success_criteria>
