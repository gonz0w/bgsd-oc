---
phase: 0133-enhanced-research-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - "0133-01"
files_modified:
  - src/router.js
  - src/lib/constants.js
  - src/lib/command-help.js
  - src/lib/commandDiscovery.js
  - workflows/new-milestone.md
autonomous: true
requirements:
  - RESEARCH-02
intent:
  outcome_ids:
    - DO-95

must_haves:
  truths:
    - "Running `bgsd-tools research:score <path>` routes correctly through the CLI and returns the structured profile JSON"
    - "Running `bgsd-tools research:gaps <path>` routes correctly through the CLI and returns flagged_gaps JSON"
    - "Both commands appear in --help output, command discovery, and category listings"
    - "new-milestone.md Step 8 surfaces the quality profile after RESEARCH COMPLETE and flags LOW-confidence files for optional re-research (non-blocking)"
    - "new-milestone.md filters to HIGH + MEDIUM severity gaps only — LOW gaps are not shown"
  artifacts:
    - src/router.js (research:score and research:gaps routes added)
    - src/lib/constants.js (COMMAND_HELP entries for research:score and research:gaps)
    - src/lib/command-help.js (category, description, related, keyword entries)
    - src/lib/commandDiscovery.js (COMMAND_TREE, COMMAND_ALIASES, discovery entries)
    - workflows/new-milestone.md (quality profile step after RESEARCH COMPLETE banner)
  key_links:
    - "router.js research case: 'score' → lazyResearch().cmdResearchScore(cwd, restArgs, raw)"
    - "router.js research case: 'gaps' → lazyResearch().cmdResearchGaps(cwd, restArgs, raw)"
    - "router.js error message string updated to include 'score, gaps'"
    - "new-milestone.md calls research:score on each .planning/research/*.md file after Step 8 RESEARCH COMPLETE banner"
---

<objective>
Wire `research:score` and `research:gaps` into the CLI router, help system, and command discovery — then integrate the quality profile into the `new-milestone.md` workflow.

Purpose: Make the scoring engine accessible through the standard CLI interface and surface research quality during milestone creation so users can catch weak research before committing to a roadmap (DO-95, RESEARCH-02).

Output: Router routes, help entries, discovery metadata for both commands, and workflow integration in new-milestone.md Step 8.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0133-enhanced-research-workflow/0133-01-SUMMARY.md
@src/router.js
@src/lib/constants.js
@src/lib/command-help.js
@src/lib/commandDiscovery.js
@workflows/new-milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire router + help + discovery metadata for research:score and research:gaps</name>
  <files>src/router.js, src/lib/constants.js, src/lib/command-help.js, src/lib/commandDiscovery.js</files>
  <action>
**1. src/router.js** — In the `case 'research':` block (line 1285), add two new `else if` branches before the `else` default error:
```
} else if (subCmd === 'score') {
  lazyResearch().cmdResearchScore(cwd, restArgs, raw);
} else if (subCmd === 'gaps') {
  lazyResearch().cmdResearchGaps(cwd, restArgs, raw);
}
```
Update the error message string in the `else` branch to include `score, gaps`:
`'Unknown research subcommand. Available: capabilities, yt-search, yt-transcript, collect, nlm-create, nlm-add-source, nlm-ask, nlm-report, score, gaps'`

**2. src/lib/constants.js** — Add COMMAND_HELP entries after the existing `research:nlm-report` entry (around line 965):

`'research:score'` — Usage, description ("Score a RESEARCH.md file and return a structured quality profile"), arguments (`<path>`), output JSON shape (`{ source_count, high_confidence_pct, oldest_source_days, has_official_docs, confidence_level, flagged_gaps[], conflicts[] }`), example command.

`'research:gaps'` — Usage, description ("Extract flagged gaps from a cached research quality profile"), arguments (`<path>`), notes (reads from research-score.json cache, run research:score first), example command.

**3. src/lib/command-help.js** — Update 4 sections:

a. **COMMAND_CATEGORIES** 'Research' category `commands` array (line 51-61): add `'research:score'` and `'research:gaps'`

b. **COMMAND_DESCRIPTIONS** (around line 165-172): add:
   - `'research:score': 'Score research quality'`
   - `'research:gaps': 'List research gaps'`

c. **RELATED_COMMANDS** (around line 247-252): add:
   - `'research:score': ['research:gaps', 'research:capabilities']`
   - `'research:gaps': ['research:score', 'research:capabilities']`

d. **KEYWORD_ALIASES** (around line 347-357): add:
   - `'score': 'research:score'`
   - `'gaps': 'research:gaps'`
   - `'quality': 'research:score'`

**4. src/lib/commandDiscovery.js** — Update 5 locations:

a. **NAMESPACE_CATEGORIES** `research.commands` array (line 57-59): add `'research:score'` and `'research:gaps'`

b. **COMMAND_ALIASES** `research` object (line 120-129): add `'score': null` and `'gaps': null`

c. **knownCommands()** static array (line 353-354): add `'research:score'` and `'research:gaps'`

d. **COMMAND_TREE** `research` object (line 557-568): add `'score': null` and `'gaps': null`

e. **spaceFormatCommands** array (line 599-602): add `'research score'` and `'research gaps'`
  </action>
  <verify>
Run: `npm run build && node bin/bgsd-tools.cjs research:score --help` — shows help text. Run: `node bin/bgsd-tools.cjs research:gaps --help` — shows help text. Run: `npm test` — full suite passes (no regressions from wiring changes).
  </verify>
  <done>Both commands routed correctly. Help text, discovery metadata, categories, related commands, and keyword aliases all registered. Router error message updated. Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate quality profile into new-milestone.md Step 8</name>
  <files>workflows/new-milestone.md</files>
  <action>
In `workflows/new-milestone.md`, after the "RESEARCH COMPLETE" banner block (lines 226-232) and before the "If Skip research" line (line 234), insert a new quality profile section:

```markdown

**Research Quality Profile:**

For each research file in `.planning/research/`:

```bash
SCORE=$(node __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs research:score "$RESEARCH_FILE")
```

Display profile summary for each file:
```
  {filename}: confidence={confidence_level}, sources={source_count}, high_pct={high_confidence_pct}%, age={oldest_source_days}d, official_docs={has_official_docs}
```

If any file has `confidence_level: "LOW"`:

```
  LOW confidence: {filename} — {flagged_gaps count matching HIGH or MEDIUM severity} gaps detected
  Gaps (HIGH/MEDIUM only):
  - {gap.gap} ({gap.severity}) — {gap.suggestion}
  
  Re-research this file? (y/N)
```

Filter: only show gaps where `severity` is `HIGH` or `MEDIUM` — suppress `LOW` severity gaps per CONTEXT.md.

If conflicts detected (`conflicts.length > 0`):
```
  Conflicts detected in {filename}:
  - "{claim}" — {source_a} vs {source_b}
```

If user chooses "yes" for re-research: re-spawn researcher for that dimension with gap context.
If "no" (default): continue — non-blocking per RESEARCH-02.
```

Ensure the integration is purely advisory — research quality issues never block milestone creation.
  </action>
  <verify>Read `workflows/new-milestone.md` and confirm the quality profile section exists between the RESEARCH COMPLETE banner and the "If Skip research" line. Verify it includes: research:score call, profile display, LOW-confidence flagging with HIGH+MEDIUM gap filtering, conflict display, and non-blocking re-research prompt.</verify>
  <done>new-milestone.md Step 8 surfaces research quality profiles after RESEARCH COMPLETE. LOW-confidence files are flagged with HIGH+MEDIUM severity gaps. Conflicts are displayed. Re-research is offered but non-blocking. LOW severity gaps are filtered out.</done>
</task>

</tasks>

<verification>
1. `npm run build && node bin/bgsd-tools.cjs research:score .planning/phases/0133-enhanced-research-workflow/0133-RESEARCH.md` — returns valid JSON through full CLI path
2. `node bin/bgsd-tools.cjs research:gaps .planning/phases/0133-enhanced-research-workflow/0133-RESEARCH.md` — returns flagged_gaps (after score caches it)
3. `node bin/bgsd-tools.cjs research:unknown` — error message includes 'score, gaps'
4. `npm test` — full test suite passes with zero regressions
5. `grep -c 'research:score' workflows/new-milestone.md` — at least 1 match (workflow integration present)
6. `grep -c 'HIGH.*MEDIUM' workflows/new-milestone.md` — gap filtering language present
</verification>

<success_criteria>
- research:score and research:gaps are routable through the CLI (router wired)
- Both commands appear in help, categories, discovery, related commands, and keyword aliases
- new-milestone.md Step 8 dumps quality profile after RESEARCH COMPLETE banner
- LOW-confidence files are flagged with gaps (HIGH+MEDIUM severity only)
- Multi-source conflicts are displayed when present
- Re-research is offered as non-blocking (default: no)
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/0133-enhanced-research-workflow/0133-02-SUMMARY.md`
</output>
