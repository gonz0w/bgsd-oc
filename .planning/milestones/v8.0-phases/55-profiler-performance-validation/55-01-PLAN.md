---
phase: 55-profiler-performance-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/profiler.js, src/lib/helpers.js, src/lib/git.js, src/lib/context.js, src/lib/ast.js]
autonomous: true
requirements: [PERF-01]
user_setup: []

must_haves:
  truths:
    - "GSD_PROFILE=1 records timing for file read operations"
    - "GSD_PROFILE=1 records timing for git operations"
    - "GSD_PROFILE=1 records timing for markdown parsing"
    - "GSD_PROFILE=1 records timing for AST analysis"
  artifacts:
    - path: "src/lib/profiler.js"
      provides: "Timing instrumentation API"
      exports: ["startTimer", "endTimer", "mark", "measure", "isProfilingEnabled", "getTimings"]
    - path: "src/lib/helpers.js"
      provides: "Instrumented file read with profiling"
      contains: "profiler.startTimer"
    - path: "src/lib/git.js"
      provides: "Instrumented git operations with profiling"
      contains: "profiler.startTimer"
    - path: "src/lib/context.js"
      provides: "Instrumented markdown parsing with profiling"
      contains: "profiler.startTimer"
    - path: "src/lib/ast.js"
      provides: "Instrumented AST analysis with profiling"
      contains: "profiler.startTimer"
  key_links:
    - from: "src/lib/profiler.js"
      to: "src/lib/helpers.js"
      via: "profiler.startTimer/endTimer calls in cachedReadFile"
      pattern: "profiler\\.(startTimer|endTimer)"
    - from: "src/lib/profiler.js"
      to: "src/lib/git.js"
      via: "profiler calls in execGit"
      pattern: "profiler\\.(startTimer|endTimer)"
    - from: "src/lib/profiler.js"
      to: "src/lib/context.js"
      via: "profiler calls in markdown parsing"
      pattern: "profiler\\.(startTimer|endTimer)"
    - from: "src/lib/profiler.js"
      to: "src/lib/ast.js"
      via: "profiler calls in AST parsing"
      pattern: "profiler\\.(startTimer|endTimer)"
---

<objective>
Add sub-operation timing instrumentation to hot paths: file reads, git operations, markdown parsing, and AST analysis.

Purpose: Enable GSD_PROFILE=1 to collect granular timing data for performance analysis
Output: Instrumented functions that record timing when profiling is enabled
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-profiler-performance-validation/55-CONTEXT.md

# Current profiler state (src/lib/profiler.js):
- isProfilingEnabled() - checks GSD_PROFILE=1
- startTimer(label) / endTimer(timer) - manual timing
- getTimings() - returns collected timings array
- writeBaseline(cwd, commandName) - writes to .planning/baselines/

# Key modules to instrument:
- src/lib/helpers.js: cachedReadFile, safeReadFile
- src/lib/git.js: execGit, structuredLog
- src/lib/context.js: markdown parsing functions
- src/lib/ast.js: AST analysis functions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument file read operations</name>
  <files>src/lib/helpers.js</files>
  <action>
    Add profiler timing to cachedReadFile and safeReadFile in src/lib/helpers.js.
    
    Import profiler at top: const { startTimer, endTimer, isProfilingEnabled } = require('./profiler');
    
    Wrap cachedReadFile body with timer:
    - Start timer: `const timer = startTimer('file:read:' + filePath.split('/').pop())`
    - End timer at return: `return endTimer(timer)` (for both cache hit and miss paths)
    
    Wrap safeReadFile similarly with timer 'file:read-uncached'
    
    Keep profiling zero-cost when disabled (startTimer returns null when not enabled).
  </action>
  <verify>
    Run: GSD_PROFILE=1 node bin/gsd-tools.js state validate 2>&1 | grep -E "file:read|timings"
    Verify timings array in output includes file read entries
  </verify>
  <done>
    cachedReadFile and safeReadFile emit timing entries when GSD_PROFILE=1
  </done>
</task>

<task type="auto">
  <name>Task 2: Instrument git operations</name>
  <files>src/lib/git.js</files>
  <action>
    Add profiler timing to execGit and structuredLog in src/lib/git.js.
    
    Import profiler: const { startTimer, endTimer } = require('./profiler');
    
    Wrap execGit with timer:
    - Start: `const timer = startTimer('git:' + args[0])` (use first git subcommand as label)
    - End before return: `endTimer(timer)`
    
    Wrap structuredLog with nested timer for 'git:log:structured'
    
    Include timing for diff-tree calls within structuredLog using 'git:diff-tree' label.
  </action>
  <verify>
    Run: GSD_PROFILE=1 node bin/gsd-tools.js state validate 2>&1 | grep -E "git:|timings"
    Verify git timing entries appear in output
  </verify>
  <done>
    execGit and structuredLog emit timing entries when GSD_PROFILE=1
  </done>
</task>

<task type="auto">
  <name>Task 3: Instrument markdown parsing and AST analysis</name>
  <files>src/lib/context.js, src/lib/ast.js</files>
  <action>
    Add profiler timing to markdown parsing in src/lib/context.js and AST analysis in src/lib/ast.js.
    
    For src/lib/context.js:
    - Import profiler: const { startTimer, endTimer } = require('./profiler')
    - Find markdown processing functions (look for frontmatter parsing, content extraction)
    - Wrap with timer labels like 'markdown:parse', 'markdown:frontmatter'
    
    For src/lib/ast.js:
    - Import profiler at top
    - Find AST parsing/generation functions
    - Wrap with timer labels like 'ast:parse', 'ast:generate', 'ast:traverse'
    
    Use short labels for readability in profiler output.
  </action>
  <verify>
    Run: GSD_PROFILE=1 node bin/gsd-tools.js verify plan-structure .planning/phases/01-foundation/01-01-PLAN.md 2>&1 | grep -E "markdown:|ast:|timings"
    Verify markdown and AST timing entries appear
  </verify>
  <done>
    Markdown parsing and AST analysis emit timing entries when GSD_PROFILE=1
  </done>
</task>

</tasks>

<verification>
- Run with GSD_PROFILE=1 and verify all 4 operation types emit timing entries
- Verify baseline file is written to .planning/baselines/
- Verify no overhead when GSD_PROFILE is not set (timers return null)
</verification>

<success_criteria>
GSD_PROFILE=1 produces timing entries for:
- file reads (at least one entry with "file:read" prefix)
- git operations (at least one entry with "git:" prefix)  
- markdown parsing (at least one entry with "markdown:" prefix)
- AST analysis (at least one entry with "ast:" prefix)
</success_criteria>

<output>
After completion, create `.planning/phases/55-profiler-performance-validation/{phase}-01-SUMMARY.md`
</output>
