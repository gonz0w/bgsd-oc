---
phase: 55-profiler-performance-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/commands/profiler.js, src/index.js, bin/gsd-tools.cjs]
autonomous: true
requirements: [PERF-02, CACHE-06]
user_setup: []

must_haves:
  truths:
    - "gsd-tools profiler compare shows before/after timing deltas"
    - "Regression highlighting uses red for slower, green for faster"
    - "Cache-enabled is measurably faster than cache-disabled"
  artifacts:
    - path: "src/commands/profiler.js"
      provides: "profiler compare command and cache-speedup test"
      exports: ["cmdProfilerCompare", "cmdProfilerCacheSpeedup"]
    - path: "bin/gsd-tools.cjs"
      provides: "profiler command entry point"
      contains: "profiler compare"
  key_links:
    - from: "src/commands/profiler.js"
      to: ".planning/baselines/"
      via: "reads baseline JSON files for comparison"
      pattern: "fs\\.readFileSync.*baselines"
    - from: "src/commands/profiler.js"
      to: "src/lib/cache.js"
      via: "compares GSD_CACHE_ENABLED=1 vs GSD_CACHE=off"
      pattern: "GSD_CACHE"
---

<objective>
Create `profiler compare` command to show before/after timing deltas and verify cache speedup.

Purpose: Enable performance regression detection and cache effectiveness validation
Output: Working profiler compare command and cache speedup test
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-profiler-performance-validation/55-CONTEXT.md
@.planning/phases/55-profiler-performance-validation/55-01-PLAN.md
@src/lib/profiler.js
@src/lib/cache.js

# Baseline file format (from .planning/baselines/):
{
  "command": "state-validate",
  "timestamp": "2026-03-02T21:53:24.089Z",
  "node_version": "v22.10.0",
  "timings": [
    { "label": "file:read:STATE.md", "duration_ms": 2.34 },
    { "label": "git:log", "duration_ms": 15.67 }
  ],
  "total_ms": 45.67
}

# Agent's Discretion for:
- Output format: Use table format with columns (Operation, Before, After, Delta, Change%)
- Baseline storage: .planning/baselines/ directory
- Regression threshold: 10% (configurable)
- Color coding: red for slower, green for faster (use chalk or ANSI codes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profiler compare command</name>
  <files>src/commands/profiler.js</files>
  <action>
    Create src/commands/profiler.js with cmdProfilerCompare function.
    
    Implementation:
    1. Parse arguments: --before <file> --after <file> [--threshold 10]
    2. Load both baseline JSON files
    3. Match timings by label (fuzzy match for similar operations)
    4. Calculate delta: after_ms - before_ms
    5. Calculate percentage: (delta / before_ms) * 100
    6. Output table with columns: Operation | Before (ms) | After (ms) | Delta (ms) | Change%
    7. Use ANSI colors: green for negative delta (faster), red for positive (slower)
    8. Sort by absolute delta descending (biggest changes first)
    9. Print summary: "X operations faster, Y slower, Z unchanged"
    
    Use require('fs') and require('path') - no external dependencies.
    Use ANSI codes directly: \x1b[32m for green, \x1b[31m for red, \x1b[0m for reset.
  </action>
  <verify>
    Create two test baseline files, then run:
    node bin/gsd-tools.cjs profiler compare --before baseline-before.json --after baseline-after.json
    Verify table output with color codes appears
  </verify>
  <done>
    profiler compare command displays before/after deltas with color-coded regression highlighting
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate profiler command into CLI</name>
  <files>src/index.js</files>
  <action>
    Add profiler namespace to src/index.js router.
    
    In the command routing switch, add:
    case 'profiler': {
      const { cmdProfilerCompare, cmdProfilerCacheSpeedup } = require('./commands/profiler');
      const subcmd = args[0] || '';
      switch (subcmd) {
        case 'compare':
          cmdProfilerCompare(args.slice(1));
          break;
        case 'cache-speedup':
          cmdProfilerCacheSpeedup(args.slice(1));
          break;
        default:
          // Print profiler help
      }
    }
    
    Add COMMAND_HELP entry for profiler commands.
  </action>
  <verify>
    Run: node bin/gsd-tools.cjs profiler --help
    Verify help text appears with compare and cache-speedup subcommands
  </verify>
  <done>
    gsd-tools profiler command is accessible from CLI
  </done>
</task>

<task type="auto">
<name>Task 3: Create cache speedup verification test</name>
<files>src/commands/profiler.js</files>
<action>
    Add cmdProfilerCacheSpeedup function to src/commands/profiler.js.
    
    Implementation:
    1. Accept --runs N (default 5) --command "args" parameters
    2. Run command N times with GSD_CACHE_ENABLED=1, record average time
    3. Run command N times with GSD_CACHE=off, record average time
    4. Calculate speedup: (uncached_time - cached_time) / uncached_time * 100
    5. Output results:
       - "Cache enabled: X ms (avg of N runs)"
       - "Cache disabled: Y ms (avg of N runs)"
       - "Speedup: Z% (W ms saved)"
    6. If speedup > 0: show in green, if < 0 show in red
    
    Use process.hrtime.bigint() for precise timing.
    Use GSD_CACHE=off to disable cache (this bypasses cache.get() calls).
  </action>
  <verify>
    Run: node bin/gsd-tools.cjs profiler cache-speedup --runs 3 --command "state validate"
    Verify output shows timing comparison between cache-enabled and disabled
  </verify>
  <done>
    cache-speedup test demonstrates measurable speedup from cache
  </done>
</task>

</tasks>

<verification>
- profiler compare shows colored output (red=regression, green=improvement)
- cache-speedup shows measurable difference between enabled/disabled
- Both commands accessible via CLI
</verification>

<success_criteria>
1. `gsd-tools profiler compare --before <file> --after <file>` displays timing delta table
2. Positive deltas shown in red, negative in green
3. `gsd-tools profiler cache-speedup --runs N --command "X"` shows speedup percentage
4. All requirements from ROADMAP (PERF-01, PERF-02, CACHE-06) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/55-profiler-performance-validation/{phase}-02-SUMMARY.md`
</output>
