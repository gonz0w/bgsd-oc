---
phase: 51-cache-foundation
plan: '03'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/phase.js
  - src/commands/roadmap.js
autonomous: true
gap_closure: true
requirements: [CACHE-03]

must_haves:
  truths:
    - "File writes via gsd-tools invalidate cache entry"
  artifacts:
    - path: "src/commands/phase.js"
      provides: "Phase commands with cache invalidation on write"
      contains: "invalidateFileCache"
    - path: "src/commands/roadmap.js"
      provides: "Roadmap commands with cache invalidation on write"
      contains: "invalidateFileCache"
  key_links:
    - from: "src/commands/phase.js"
      to: "src/lib/cache.js"
      via: "invalidateFileCache from helpers"
      pattern: "invalidateFileCache\\(roadmapPath\\)"
    - from: "src/commands/roadmap.js"
      to: "src/lib/cache.js"
      via: "invalidateFileCache from helpers"
      pattern: "invalidateFileCache\\(roadmapPath\\)"
---

<objective>
Fix CACHE-03 partial implementation: Add explicit cache invalidation to phase.js and roadmap.js file writes.

Purpose: Ensure file writes through gsd-tools immediately invalidate cache entries, making the cache consistent faster (mtime-based staleness detection is already in place as a safety net).

Output: Both phase.js and roadmap.js call invalidateFileCache after writing ROADMAP.md
</objective>

<context>
@.planning/phases/51-cache-foundation/51-VERIFICATION.md
@.planning/phases/51-cache-foundation/51-02-SUMMARY.md

# Reference: How state.js implements invalidation
- Line 5: const { ..., invalidateFileCache, ... } = require('../lib/helpers');
- Lines 194, 216, 256, 264, 297, 329, 356, 379, 411, 446: invalidateFileCache(statePath) after each fs.writeFileSync

# Current state
- CacheEngine already has mtime-based staleness detection (works as fallback)
- state.js properly calls invalidateFileCache after writes
- phase.js and roadmap.js do NOT call invalidateFileCache (the gap)
</context>

<tasks>

<task type="auto">
  <name>Add invalidateFileCache to phase.js</name>
  <files>src/commands/phase.js</files>
  <action>
    1. Add `invalidateFileCache` to the import from '../lib/helpers' at line 4
    
    2. Add `invalidateFileCache(roadmapPath)` call after each fs.writeFileSync to roadmapPath:
       - Line 216 (cmdPhaseAdd)
       - Line 299 (cmdPhaseInsert)
       - Line 513 (cmdPhaseRemove)
       - Line 657 (cmdPhaseComplete)
    
    3. Also add invalidateFileCache(statePath) after line 531 (cmdPhaseRemove writes STATE.md)
    
    Reference: state.js pattern shows import at line 5, calls after each write
  </action>
  <verify>
    Run: node bin/gsd-tools.cjs phase list
    Verify: Command runs without error (cache invalidation happens transparently)
    Run: npm test
    Verify: All tests pass
  </verify>
  <done>
    phase.js imports and calls invalidateFileCache after all .planning file writes
  </done>
</task>

<task type="auto">
  <name>Add invalidateFileCache to roadmap.js</name>
  <files>src/commands/roadmap.js</files>
  <action>
    1. Add `invalidateFileCache` to the import from '../lib/helpers' at line 4
    
    2. Add `invalidateFileCache(roadmapPath)` call after fs.writeFileSync at line 283 (cmdRoadmapUpdatePlanProgress)
    
    Reference: state.js pattern shows import at line 5, calls after each write
  </action>
  <verify>
    Run: node bin/gsd-tools.cjs roadmap update-plan-progress 51
    Verify: Command runs without error
    Run: npm test
    Verify: All tests pass
  </verify>
  <done>
    roadmap.js imports and calls invalidateFileCache after ROADMAP.md writes
  </done>
</task>

<task type="auto">
  <name>Verify CACHE-03 complete</name>
  <files>bin/gsd-tools.cjs (rebuild)</files>
  <action>
    Run npm run build to rebuild the bundle
    Run npm test to verify all tests pass with the cache invalidation changes
  </action>
  <verify>
    Run: npm test
    Verify: All tests pass (762+ tests)
  </verify>
  <done>
    CACHE-03 fully satisfied: all gsd-tools file writes invalidate cache entries
  </done>
</task>

</tasks>

<verification>
- [ ] phase.js imports invalidateFileCache from helpers
- [ ] phase.js calls invalidateFileCache(roadmapPath) after each write to ROADMAP.md
- [ ] phase.js calls invalidateFileCache(statePath) after STATE.md write
- [ ] roadmap.js imports invalidateFileCache from helpers
- [ ] roadmap.js calls invalidateFileCache(roadmapPath) after write
- [ ] npm test passes (no regressions)
- [ ] Rebuild completes successfully
</verification>

<success_criteria>
- CACHE-03: "Any file write through gsd-tools invalidates the corresponding cache entry" - FULLY SATISFIED
- All existing tests continue to pass
- No functional changes to command behavior (only cache invalidation added)
</success_criteria>

<output>
After completion, create `.planning/phases/51-cache-foundation/{phase}-{plan}-SUMMARY.md`
</output>
