---
phase: 51-cache-foundation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - CACHE-01
  - CACHE-04

must_haves:
  truths:
    - Cache module exports CacheEngine class with SQLite/Map backend selection
    - Cache location follows XDG_CONFIG_HOME convention
    - Map fallback activates transparently on Node <22.5
    - Cache backend selection happens at runtime, not compile time
  artifacts:
    - path: src/lib/cache.js
      provides: CacheEngine class with get/set/invalidate/clear/status/warm methods
      min_lines: 150
  key_links:
    - from: src/lib/cache.js
      to: node:sqlite
      via: require('node:sqlite')
      pattern: require.*node:sqlite
---
<objective>
Create persistent cache module with SQLite backend and Map fallback.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-cache-foundation/51-CONTEXT.md
@.planning/phases/51-cache-foundation/51-RESEARCH.md

# Reference existing helpers.js for patterns
@src/lib/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Create src/lib/cache.js with CacheEngine class</name>
  <files>src/lib/cache.js</files>
  <action>
Create src/lib/cache.js module exporting CacheEngine class:

1. CacheEngine constructor accepts {maxSize: 1000, ttl: 3600000}
2. _selectBackend() checks:
   - process.env.GSD_CACHE_FORCE_MAP === '1' → use Map
   - Node version >= 22.5 → try SQLite
   - Otherwise → Map fallback
3. SQLite backend:
   - Database path: XDG_CONFIG_HOME/oc/get-shit-done/cache.db (use process.env.XDG_CONFIG_HOME || ~/.config)
   - Schema: CREATE TABLE file_cache (key TEXT PRIMARY KEY, value TEXT, mtime REAL, created REAL, accessed REAL)
   - Implements: get(key), set(key, value), invalidate(key), clear(), status(), warm(files)
4. Map backend:
   - Same interface as SQLite
   - LRU eviction via Map order
   - In-memory only (no persistence)
5. All methods should work identically regardless of backend
  </action>
  <verify>
node -e "const CacheEngine = require('./src/lib/cache'); const c = new CacheEngine(); console.log(c.status())"
</verify>
  <done>CacheEngine class exists, reports backend type and entry count</done>
</task>

</tasks>

<verification>
1. Cache module loads without errors on Node >= 22.5
2. Cache module loads without errors on Node < 22.5 (Map fallback)
3. Cache status shows correct backend type
</verification>

<success_criteria>
gsd-tools cache status reports backend type and entry count
</success_criteria>

<output>
After completion, create .planning/phases/51-cache-foundation/51-01-SUMMARY.md
</output>
