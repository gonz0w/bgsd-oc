---
phase: 52-cache-integration-warm-up
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/gsd-tools.cjs
  - src/commands/phase.js
  - src/commands/init.js
  - src/commands/verify.js
  - src/commands/misc.js
  - src/lib/helpers.js
autonomous: true
requirements:
  - CACHE-05

must_haves:
  truths:
    - "All hot-path file readers use cachedReadFile instead of direct fs.readFileSync"
    - "All 751+ tests pass with cache enabled"
    - "All 751+ tests pass with GSD_CACHE_FORCE_MAP=1 (cache disabled)"
    - "Second invocation of init execute-phase is measurably faster than first"
  artifacts:
    - path: "src/commands/phase.js"
      provides: "Phase commands with cached reads"
      contains: "cachedReadFile"
    - path: "src/commands/init.js"
      provides: "Init commands with cached reads"
      contains: "cachedReadFile"
  key_links:
    - from: "src/commands/phase.js"
      to: "src/lib/helpers.js"
      via: "cachedReadFile import and call"
      pattern: "require.*cachedReadFile"
---

<objective>
Wire cache into all hot-path file readers and verify test parity between cache-enabled and cache-disabled modes.

Purpose: Ensure cache provides measurable performance benefit and tests pass identically in both modes.
Output: Hot-path commands use cachedReadFile, test parity verified.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cache-integration-warm-up/52-CONTEXT.md
@.planning/phases/52-cache-integration-warm-up/52-RESEARCH.md

Context from Plan 01 (52-01):
- --no-cache flag implemented
- cache warm auto-discovery implemented
- Auto-warm message on first use implemented
</context>

<tasks>

<task type="auto">
  <name>Wire phase.js to use cachedReadFile</name>
  <files>src/commands/phase.js</files>
  <action>
1. Add import: const { cachedReadFile } = require('../lib/helpers');
2. Find all fs.readFileSync calls (20+ expected)
3. Replace each with cachedReadFile(path) or cachedReadFile(path, 'utf-8')
4. Keep fs.readFileSync only for:
   - Binary file reads (images, etc.)
   - Cache database operations
   - Any file outside .planning/ directory
5. Preserve all existing error handling and return value usage

Priority files to migrate first (most frequently read):
- STATE.md reads
- ROADMAP.md reads
- PLAN.md reads
- Phase config reads
  </action>
  <verify>
Test: Run `npm test` - all tests should pass
Test: Run `node bin/gsd-tools.cjs phase list` - should work correctly with cache
  </verify>
  <done>All phase.js file reads use cachedReadFile</done>
</task>

<task type="auto">
  <name>Wire init.js to use cachedReadFile</name>
  <files>src/commands/init.js</files>
  <action>
1. Add import: const { cachedReadFile } = require('../lib/helpers');
2. Find all fs.readFileSync calls (8+ expected)
3. Replace each with cachedReadFile(path) or cachedReadFile(path, 'utf-8')
4. Keep fs.readFileSync only for:
   - Binary file reads
   - Cache database operations
   - Files outside .planning/
5. Focus on execute-phase hot paths first (most benefit)
  </action>
  <verify>
Test: Run `npm test` - all tests should pass
Test: Run `node bin/gsd-tools.cjs init execute-phase --help` - should work correctly
  </verify>
  <done>All init.js file reads use cachedReadFile</done>
</task>

<task type="auto">
  <name>Wire verify.js and misc.js to use cachedReadFile</name>
  <files>src/commands/verify.js, src/commands/misc.js</files>
  <action>
1. Add import to each file: const { cachedReadFile } = require('../lib/helpers');
2. Find all fs.readFileSync calls in both files
3. Replace with cachedReadFile where appropriate
4. Keep fs.readFileSync for binary files and cache DB operations
  </action>
  <verify>
Test: Run `npm test` - all tests should pass
  </verify>
  <done>verify.js and misc.js use cachedReadFile for .planning/ reads</done>
</task>

<task type="auto">
  <name>Verify test parity with and without cache</name>
  <files>bin/gsd-tools.cjs</files>
  <action>
Run full test suite twice:
1. First run: `npm test` (cache enabled, default)
2. Second run: `GSD_CACHE_FORCE_MAP=1 npm test` (cache disabled, Map fallback)

Verify both runs have identical pass/fail results.
If any test fails in either mode, investigate and fix.
  </action>
  <verify>
Test: `npm test` passes
Test: `GSD_CACHE_FORCE_MAP=1 npm test` passes
Test: Both runs show identical pass/fail counts
  </verify>
  <done>All 751+ tests pass identically in both cache modes</done>
</task>

</tasks>

<verification>
1. Run `npm test` - all tests pass (cache enabled)
2. Run `GSD_CACHE_FORCE_MAP=1 npm test` - all tests pass (cache disabled)
3. Compare pass/fail counts - should be identical
4. Run `node bin/gsd-tools.cjs phase list` twice - second invocation should be faster
</verification>

<success_criteria>
- All hot-path commands (phase.js, init.js, verify.js, misc.js) use cachedReadFile
- All 751+ tests pass with cache enabled
- All 751+ tests pass with cache disabled (GSD_CACHE_FORCE_MAP=1)
- Second invocation of init execute-phase measurably faster than first
</success_criteria>

<output>
After completion, create `.planning/phases/52-cache-integration-warm-up/52-02-SUMMARY.md`
</output>
