---
phase: 52-cache-integration-warm-up
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/gsd-tools.cjs
  - src/commands/cache.js
  - src/lib/cache.js
  - src/lib/helpers.js
autonomous: true
requirements:
  - CACHE-05
intent:
  outcome_ids:
    - DO-29

must_haves:
  truths:
    - "gsd-tools cache warm discovers all .planning/ files when run without arguments"
    - "gsd-tools cache warm reports count of files warmed and elapsed time"
    - "--no-cache flag forces Map fallback, bypassing SQLite"
    - "GSD_CACHE_FORCE_MAP=1 env var forces Map fallback (existing)"
  artifacts:
    - path: "bin/gsd-tools.cjs"
      provides: "CLI entry point with --no-cache flag parsing"
      exports: ["--no-cache flag"]
    - path: "src/commands/cache.js"
      provides: "cache warm command with auto-discovery"
      exports: ["cmdCacheWarm"]
    - path: "src/lib/cache.js"
      provides: "CacheEngine.warm() method"
      exports: ["warm"]
  key_links:
    - from: "src/commands/cache.js"
      to: "src/lib/cache.js"
      via: "CacheEngine.warm() call"
      pattern: "cacheEngine\\.warm"
---

<objective>
Implement cache warm command with auto-discovery and add --no-cache flag for test parity verification.

Purpose: Enable users to pre-populate cache after checkout/pull (CACHE-05), and allow forced fallback for testing.
Output: Working cache warm command with auto-discovery, --no-cache flag in CLI.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-cache-integration-warm-up/52-CONTEXT.md
@.planning/phases/52-cache-integration-warm-up/52-RESEARCH.md

Relevant prior work:
- Phase 51: CacheEngine with SQLite/Map backend, cachedReadFile wrapper, cache invalidation on writes
</context>

<tasks>

<task type="auto">
  <name>Add --no-cache flag to CLI router</name>
  <files>bin/gsd-tools.cjs</files>
  <action>
Add --no-cache flag parsing near the top of the CLI entry point, before command routing. When --no-cache is present:
1. Set process.env.GSD_CACHE_FORCE_MAP = '1'
2. Remove --no-cache from args array
3. Continue with normal command routing

Reference existing GSD_CACHE_FORCE_MAP handling in cache.js for consistency.
  </action>
  <verify>
Test: Run `node bin/gsd-tools.cjs --no-cache cache status` and verify it uses Map fallback (not SQLite)
  </verify>
  <done>--no-cache flag parsed correctly, forces Map backend, removed from args before routing</done>
</task>

<task type="auto">
  <name>Add auto-discovery to cache warm command</name>
  <files>src/commands/cache.js</files>
  <action>
In cmdCacheWarm function:
1. Check if filePaths array is empty (no args provided)
2. If empty, call discoverPlanningFiles(cwd) to find all .planning/*.md files recursively
3. Use existing discoverPlanningFiles pattern from RESEARCH.md (recursive walk of .planning/ directory)
4. If paths provided, use them as-is (existing behavior)
5. Call cacheEngine.warm() with resolved paths
6. Report: "Warmed X files in Yms" format per CONTEXT.md locked decision

Output format must include: file count, elapsed time in ms.
  </verify>
  <verify>
Test: Run `node bin/gsd-tools.cjs cache warm` (no args) and verify it discovers .planning/ files
Test: Run `node bin/gsd-tools.cjs cache warm file.md` (with arg) and verify it uses provided path
  </verify>
  <done>cache warm auto-discovers .planning/ files when run without arguments</done>
</task>

<task type="auto">
  <name>Add auto-warm message on first cache use</name>
  <files>src/lib/helpers.js (cachedReadFile)</files>
  <action>
In cachedReadFile function (or where cache is first accessed):
1. On first cache population (cache miss that gets populated), output brief message like "Warming cache... X files"
2. Use existing output() pattern for consistent formatting
3. Only show on first warm, not on every read
4. Keep message brief per CONTEXT.md locked decision: "Warming cache... X files"
  </verify>
  <verify>
Test: Clear cache, run any gsd-tools command that reads .planning/ files, verify warm message appears once
  </verify>
  <done>Auto-warm message appears once on first cache use</done>
</task>

</tasks>

<verification>
1. Run `node bin/gsd-tools.cjs cache warm` - should auto-discover .planning/ files
2. Run `node bin/gsd-tools.cjs cache warm` - should report count and timing
3. Run `node bin/gsd-tools.cjs --no-cache cache status` - should show Map backend
4. Run `node bin/gsd-tools.cjs cache status` - should show SQLite backend (or Map fallback)
</verification>

<success_criteria>
- `gsd-tools cache warm` (no args) discovers and warms all .planning/ files
- `gsd-tools cache warm` reports count and elapsed time
- `--no-cache` flag forces Map fallback
- First cache use shows brief "Warming cache..." message
</success_criteria>

<output>
After completion, create `.planning/phases/52-cache-integration-warm-up/52-01-SUMMARY.md`
</output>
