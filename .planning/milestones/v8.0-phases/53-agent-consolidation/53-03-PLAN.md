---
phase: 53-agent-consolidation
plan: 03
type: execute
wave: 2
depends_on: [53-agent-consolidation-01, 53-agent-consolidation-02]
files_modified: []
autonomous: true
requirements: [AGENT-04, AGENT-05]

must_haves:
  truths:
    - "All 9 agent manifests declare max token budgets"
    - "Context builder refuses to exceed budgets with logged warning"
    - "All workflows reference correct agent names"
  artifacts:
    - path: /home/cam/.config/oc/agents/*.md
      provides: Agent manifests with token budgets
    - path: workflows/*.md
      provides: Updated agent name references
  key_links:
    - from: context builder
      to: agent manifests
      via: Reads max_tokens from manifest, enforces limit
    - from: workflows
      to: agent manifests
      via: Task() spawn calls reference correct subagent_type
---

<objective>
Add token budgets to agent manifests and update all workflow references.
Purpose: Manifest-driven context loading with deterministic limits; no broken spawn chains.
Output: Updated agent manifests with max_tokens + fixed workflow references
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/53-agent-consolidation/53-01-PLAN.md
@.planning/phases/53-agent-consolidation/53-02-PLAN.md

# Workflows referencing old agents (need updates):
# - workflows/new-project.md: gsd-research-synthesizer → gsd-roadmapper
# - workflows/new-milestone.md: gsd-research-synthesizer → gsd-roadmapper  
# - workflows/audit-milestone.md: gsd-integration-checker → gsd-verifier
</context>

<tasks>

<task type="auto">
  <name>Add token budgets to agent manifests</name>
  <files>/home/cam/.config/oc/agents/*.md</files>
  <action>
For each of the 9 agent files in /home/cam/.config/oc/agents/:
1. Read the agent manifest
2. Add max_tokens field to frontmatter:
   - gsd-executor: 80000
   - gsd-planner: 80000
   - gsd-verifier: 80000 (merged integration-checker)
   - gsd-roadmapper: 80000 (merged synthesizer)
   - gsd-phase-researcher: 60000
   - gsd-project-researcher: 60000
   - gsd-codebase-mapper: 60000
   - gsd-debugger: 60000
   - gsd-plan-checker: 60000
3. Update context builder in gsd-tools to read max_tokens and warn on truncation

Reference: src/lib/context.js or similar for context building logic
</action>
  <verify>All 9 agent .md files have max_tokens in frontmatter</verify>
  <done>All 9 agent manifests declare max token budgets</done>
</task>

<task type="auto">
  <name>Update workflow references to merged agents</name>
  <files>workflows/new-project.md, workflows/new-milestone.md, workflows/audit-milestone.md</files>
  <action>
1. Update workflows/new-project.md line 186:
   - Change subagent_type="gsd-research-synthesizer" to subagent_type="gsd-roadmapper"
   - Change model variable from {synthesizer_model} to {roadmapper_model}
   
2. Update workflows/new-milestone.md line 220:
   - Same change as above
   
3. Update workflows/audit-milestone.md:
   - Line 21: Change resolve-model gsd-integration-checker to gsd-verifier
   - Line 76: Change subagent_type="gsd-integration-checker" to subagent_type="gsd-verifier"

4. Search for any other references to old agent names in entire workflows/ directory
   and update them
</action>
  <verify>grep -r "gsd-research-synthesizer\|gsd-integration-checker" workflows/ returns no matches</verify>
  <done>All workflows and command wrappers reference correct agent names — no broken spawn chains</done>
</task>

</tasks>

<verification>
Run: node bin/gsd-tools.cjs agent audit
Expected: Zero gaps, zero overlaps, 9 agents

Run: npm test
Expected: All tests pass with updated workflow references
</verification>

<success_criteria>
- [ ] All 9 agent manifests have max_tokens in frontmatter
- [ ] Context builder logs warning on token budget exceedance
- [ ] No references to gsd-research-synthesizer in workflows/
- [ ] No references to gsd-integration-checker in workflows/
- [ ] Agent audit shows 9 agents with zero gaps and zero overlaps
</success_criteria>

<output>
After completion, create .planning/phases/53-agent-consolidation/53-03-SUMMARY.md
</output>
