---
phase: 53-agent-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [AGENT-01, AGENT-02]

must_haves:
  truths:
    - "RACI matrix document exists mapping lifecycle steps to agents"
    - "gsd-tools agent audit command reports gaps and overlaps"
  artifacts:
    - path: /home/cam/.config/oc/agents/RACI.md
      provides: RACI matrix mapping lifecycle steps to responsible agents
    - path: src/commands/agent.js
      provides: agent audit command implementation
  key_links:
    - from: agent audit command
      to: RACI matrix
      via: reads agent manifests and RACI.md to detect gaps/overlaps
---

<objective>
Create RACI matrix document and implement agent audit command.
Purpose: Establish clear ownership mapping and verification tool for lifecycle coverage.
Output: RACI.md + gsd-tools agent audit command
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-agent-consolidation/53-RESEARCH.md

# Current 11 agents:
# gsd-executor, gsd-planner, gsd-verifier, gsd-roadmapper, gsd-phase-researcher,
# gsd-project-researcher, gsd-codebase-mapper, gsd-debugger, gsd-plan-checker,
# gsd-integration-checker (to merge), gsd-research-synthesizer (to merge)
</context>

<tasks>

<task type="auto">
  <name>Create RACI matrix document</name>
  <files>/home/cam/.config/oc/agents/RACI.md</files>
  <action>
Create RACI.md in agents directory with:
- Lifecycle steps: Init → Discuss → Research → Plan → Execute → Verify → Complete
- Each step mapped to exactly one responsible agent
- Columns: Step, Responsible, Accountable, Consulted, Informed
- All 11 current agents included (will be 9 after merge)

Reference current agent list from /home/cam/.config/oc/agents/*.md
  </action>
  <verify>RACI.md exists at /home/cam/.config/oc/agents/RACI.md with lifecycle step mapping</verify>
  <done>RACI matrix document exists mapping every product lifecycle step to exactly one responsible agent</done>
</task>

<task type="auto">
  <name>Implement agent audit command</name>
  <files>src/commands/agent.js, src/lib/commands.js</files>
  <action>
Add 'agent' subcommand to gsd-tools with 'audit' sub-subcommand:
- Scan all agent .md files in /home/cam/.config/oc/agents/
- Parse frontmatter to extract agent names and roles
- Read RACI.md to get lifecycle step mappings
- Report: which steps have no owner (gaps), which have multiple owners (overlaps)
- Exit code: 0 if zero gaps and zero overlaps, non-zero otherwise

Use existing command pattern from src/commands/*.js
  </action>
  <verify>node bin/gsd-tools.cjs agent audit reports zero gaps and zero overlaps</verify>
  <done>Running gsd-tools agent audit reports zero gaps and zero overlaps across all lifecycle steps</done>
</task>

</tasks>

<verification>
Run: node bin/gsd-tools.cjs agent audit
Expected: Reports gaps and overlaps, exits with appropriate code
</verification>

<success_criteria>
- [ ] RACI.md exists with lifecycle step to agent mapping
- [ ] gsd-tools agent audit command exists and runs
- [ ] Audit reports gaps/overlaps correctly
</success_criteria>

<output>
After completion, create .planning/phases/53-agent-consolidation/53-01-SUMMARY.md
</output>
