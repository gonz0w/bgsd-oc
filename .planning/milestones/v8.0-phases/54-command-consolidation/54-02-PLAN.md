---
phase: 54-command-consolidation
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified: [bin/gsd-tools.test.cjs]
autonomous: true
requirements: [CMD-02]

must_haves:
  truths:
    - "All test invocations use namespace:command format"
    - "npm test passes with new command names"
    - "No old flat command names remain in test file"
  artifacts:
    - path: "bin/gsd-tools.test.cjs"
      contains: "plan:intent"
      min_occurrences: 50
  key_links: []
---

<objective>
Update all test references in bin/gsd-tools.test.cjs from flat command names to namespaced command names.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-command-consolidation/54-CONTEXT.md
@.planning/phases/54-command-consolidation/54-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Update test command references</name>
  <files>bin/gsd-tools.test.cjs</files>
  <action>
1. Read bin/gsd-tools.test.cjs to understand test patterns
2. Identify all runGsdTools('command-name') calls
3. Update command names using namespace mapping:
   - intent* → plan:intent*
   - requirements* → plan:requirements*
   - roadmap* → plan:roadmap*
   - phase*, phases*, find-phase, milestone → plan:phase*, plan:phases*, plan:find-phase, plan:milestone
   - commit, rollback-info, session-diff, session-summary, velocity, worktree, tdd, test-run, profile → execute:*
   - state, verify, assertions, search-decisions, search-lessons, review, context-budget, token-budget → verify:*
   - config-get, config-set, env, current-timestamp, list-todos, todo, memory, mcp, classify, frontmatter, progress, websearch, history-digest, trace-requirement, codebase* → util:*
   - init, scaffold → init:*
4. Run: npm test to verify all tests pass with new command names
  </action>
  <verify>npm test passes with all updated command names</verify>
  <done>All test references use namespace:command format</done>
</task>

</tasks>

<verification>
- [ ] All runGsdTools() calls use namespace:command format
- [ ] npm test passes (669+ tests)
- [ ] No references to old flat command names remain in test file
</verification>

<success_criteria>
All test references updated to new namespaced commands, all tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/54-command-consolidation/54-02-SUMMARY.md`
</output>
