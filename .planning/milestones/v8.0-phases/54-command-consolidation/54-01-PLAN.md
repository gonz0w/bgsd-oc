---
phase: 54-command-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/router.js, src/lib/constants.js]
autonomous: true
requirements: [CMD-01]

must_haves:
  truths:
    - "Router accepts namespace:command syntax (e.g., plan:intent show)"
    - "All 5 namespaces (init, plan, execute, verify, util) function correctly"
    - "Help output shows grouped commands by namespace"
  artifacts:
    - path: "src/router.js"
      provides: "Namespace parsing and command routing"
      contains: "namespace switch"
    - path: "src/lib/constants.js"
      provides: "COMMAND_HELP with namespace keys"
      contains: "plan:intent"
  key_links:
    - from: "src/router.js"
      to: "src/lib/constants.js"
      via: "COMMAND_HELP lookup"
      pattern: "COMMAND_HELP\\[command\\]"
---

<objective>
Implement namespace routing in router.js to support command grouping under logical namespaces (init, plan, execute, verify, util).
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/54-command-consolidation/54-CONTEXT.md
@.planning/phases/54-command-consolidation/54-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Add namespace parsing to router.js</name>
  <files>src/router.js</files>
  <action>
1. Read src/router.js to understand current flat command structure
2. Add namespace parsing: split command on first colon only (const [namespace, ...cmdParts] = command.split(':'); const cmd = cmdParts.join(' '))
3. Create namespace switch statement with nested command handlers for: init, plan, execute, verify, util
4. Move existing command cases into appropriate namespace sections
5. Update error message at line ~112 to show namespaced commands
6. Test with: node bin/gsd-tools.cjs plan:intent --help (should work)
7. Test with: node bin/gsd-tools.cjs --help (should show grouped help)
  </action>
  <verify>node bin/gsd-tools.cjs plan:intent show returns valid output</verify>
  <done>Router accepts namespace:command syntax for all 5 namespaces</done>
</task>

<task type="auto">
  <name>Update COMMAND_HELP constants</name>
  <files>src/lib/constants.js</files>
  <action>
1. Read src/lib/constants.js to find COMMAND_HELP object
2. Update all keys from 'intent' to 'plan:intent', 'state' to 'verify:state', etc.
3. Add namespace grouping metadata to help constants
4. Ensure help output shows grouped commands by namespace
   <verify>node bin/gsd-tools.cjs --help shows organized namespaces</verify>
  <done>All help text accessible via namespace:command keys</done>
</task>

</tasks>

<verification>
- [ ] `node bin/gsd-tools.cjs plan:intent show` returns valid output
- [ ] `node bin/gsd-tools.cjs verify:state get phase` works
- [ ] `node bin/gsd-tools.cjs execute:commit --dry-run` works
- [ ] `node bin/gsd-tools.cjs --help` shows grouped namespaces
- [ ] Old flat commands like `intent show` no longer work (backward compat removed per CONTEXT.md)
</verification>

<success_criteria>
Router.js accepts namespace:command syntax. All 5 namespaces (init, plan, execute, verify, util) function correctly with their commands.
</success_criteria>

<output>
After completion, create `.planning/phases/54-command-consolidation/54-01-SUMMARY.md`
</output>
