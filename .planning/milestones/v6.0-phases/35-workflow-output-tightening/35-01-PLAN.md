---
phase: 35-workflow-output-tightening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [references/ui-brand.md]
autonomous: true
requirements: [WKFL-06]

must_haves:
  truths:
    - "ui-brand.md uses bGSD branding consistently (not GSD)"
    - "ui-brand.md has concrete examples and tighter patterns"
  artifacts:
    - path: "references/ui-brand.md"
      provides: "Updated brand reference with bGSD and tighter patterns"
      contains: "bGSD"
  key_links: []
---

<objective>
Update ui-brand.md to use bGSD branding and tighter output patterns with concrete examples.

Purpose: The brand reference still says "GSD ►" — needs to say "bGSD ►" per the Phase 30 rename. Also tighten pattern descriptions with concrete examples for information-dense output.
Output: Updated ui-brand.md with correct branding and improved pattern documentation.
</objective>

<execution_context>
@__OPENCODE_CONFIG__/get-shit-done/workflows/execute-plan.md
@__OPENCODE_CONFIG__/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@references/ui-brand.md
@src/lib/format.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ui-brand.md branding and patterns</name>
  <files>references/ui-brand.md</files>
  <action>
Update references/ui-brand.md with these changes:

1. **Brand rename**: Replace all instances of `GSD` with `bGSD` in the file:
   - `GSD ► {STAGE NAME}` → `bGSD ► {STAGE NAME}`
   - Any other `GSD` references → `bGSD`

2. **Tighter stage banner spec**: Update the banner section to reference `format.js` `banner()` function:
   ```
   Use `banner(title)` from format.js. Renders as:
   bGSD ▶ {TITLE}
   ────────────────────────────────────
   ```

3. **Add concrete output examples**: Add a new section "## Command Output Examples" showing what formatted command output looks like for key commands:
   - `init progress` example showing banner + progress bar + phase table
   - `verify quality` example showing dimensions table with grades
   - Show the pattern: banner → content → summary line → action hint

4. **Tighter table spec**: Update the Tables section to reference `formatTable()` from format.js:
   - PSql-style (header + separator line, no borders by default)
   - Column alignment: left for text, right for numbers
   - Truncation with `…` for long values
   - Max 10 rows by default, `--all` for full list

5. **Add information density guidelines**:
   - "One line per data point — no blank lines between data"
   - "Status symbols inline, not on separate lines"
   - "Action hints at bottom only, one per command"
   - "No 'Starting...', 'Processing...', 'Done.' noise"

6. **Update anti-patterns**: Add:
   - "Using inline ANSI codes instead of format.js color utility"
   - "Multiple action hints per command"
   - "Verbose status messages ('Starting analysis...', 'Processing files...', 'Analysis complete.')"
  </action>
  <verify>Read references/ui-brand.md — confirm all GSD → bGSD, concrete examples present, tighter specs documented.</verify>
  <done>ui-brand.md uses bGSD consistently, has concrete command output examples, tighter table/information density specs, updated anti-patterns</done>
</task>

</tasks>

<verification>
1. `grep -c 'GSD' references/ui-brand.md` — should be 0 (all replaced with bGSD)
2. `grep 'bGSD' references/ui-brand.md` — should appear in banner spec and examples
3. File contains "Command Output Examples" section
4. File contains "information density" guidelines
</verification>

<success_criteria>
ui-brand.md uses bGSD branding, has concrete examples, and documents tighter information-dense output patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/35-workflow-output-tightening/35-01-SUMMARY.md`
</output>
