---
phase: 146-code-review-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/review.js
  - src/router.js
  - src/lib/constants.js
  - src/lib/command-help.js
  - src/lib/review/config.js
  - src/lib/review/target.js
  - src/lib/review/diff.js
  - src/lib/review/exclusions.js
  - tests/review.test.cjs
autonomous: true
requirements:
  - REV-01
  - REV-05
intent:
  outcome_ids:
    - DO-109

must_haves:
  truths:
    - "`review:scan` defaults to the staged diff and returns a machine-readable review target instead of silently guessing a broader scope"
    - "When nothing is staged, the review flow returns a promptable fallback state that prefers commit-range review over file-path guessing"
    - "Users are warned when nearby unstaged or untracked changes suggest the review scope may be incomplete"
    - "Rule-plus-path exclusions suppress known false positives while preserving the reason text as the audit trail"
  artifacts:
    - path: "src/commands/review.js"
      provides: "review:scan command entrypoint and JSON/formatted output contract"
      contains: "review:scan"
    - path: "src/lib/review/target.js"
      provides: "staged-first target resolution with empty-stage fallback and incomplete-scope warnings"
      contains: "git diff --cached"
    - path: "src/lib/review/exclusions.js"
      provides: "review exclusion loader and exact rule-plus-path matcher for .planning/review-exclusions.json"
      contains: "review-exclusions.json"
    - path: "tests/review.test.cjs"
      provides: "CLI-contract coverage for target resolution, exclusions, and warning behavior"
      contains: "review:scan"
  key_links:
    - from: "src/router.js"
      to: "src/commands/review.js"
      via: "review namespace routing and command discovery/help wiring"
      pattern: "review:scan"
    - from: "src/commands/review.js"
      to: "src/lib/review/target.js"
      via: "target resolution before any rule execution"
      pattern: "resolveReviewTarget|target"
    - from: "src/commands/review.js"
      to: "src/lib/review/exclusions.js"
      via: "suppression pass applied before final output emission"
      pattern: "review-exclusions|exclude"
---

<objective>
Establish the CLI review surface and diff-target plumbing for Phase 146.

Purpose: Create the deterministic staged-first foundation for DO-109 so later review rules and workflow orchestration consume one stable target/exclusion contract instead of rediscovering scope in prompts.

Output: `review:scan` command wiring, review target resolution helpers, exclusion matching for `.planning/review-exclusions.json`, and command-level tests for scope selection and suppression behavior.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/146-code-review-workflow/146-CONTEXT.md
@.planning/phases/146-code-review-workflow/146-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@src/router.js
@src/commands/workflow.js
@src/commands/scaffold.js
@src/lib/review/severity.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the review command surface and router/help wiring</name>
  <files>src/commands/review.js, src/router.js, src/lib/constants.js, src/lib/command-help.js</files>
  <action>
Create `src/commands/review.js` as the Phase 146 entrypoint for `review:scan`, then wire it through `src/router.js`, `src/lib/constants.js`, and `src/lib/command-help.js` using the repo's existing namespace/lazy-loader/help patterns.

Expose `review:scan` as a CLI-first command that returns structured output by default when piped and human-readable output when attached to a TTY. Keep the surface narrow: Phase 146 needs the scan command and any minimal review-specific helper entrypoints required by the workflow, but should not introduce a generalized lint framework or broad repo-wide review mode.

Make the command contract explicitly diff-oriented so later workflow steps can consume one JSON payload containing target metadata, warnings, findings, ask groups, and fix summaries without re-deriving scope from git state.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs review:scan --help` and confirm the command is routed and documented.</verify>
  <done>
  `review:scan` is discoverable through the router/help system, follows existing command-module conventions, and exposes a stable CLI contract for diff-scoped review results.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement staged-first target resolution, incomplete-scope warnings, and exclusion matching</name>
  <files>src/lib/review/config.js, src/lib/review/target.js, src/lib/review/diff.js, src/lib/review/exclusions.js, src/commands/review.js</files>
  <action>
Implement the deterministic pre-scan pipeline described in `146-CONTEXT.md` and `146-RESEARCH.md`.

Build helpers that:
- default the review target to the staged diff;
- return a promptable fallback state when nothing is staged, preferring commit-range review over file-path review;
- optionally surface untracked-file inclusion decisions without silently broadening scope;
- emit a separate incomplete-scope warning when adjacent unstaged or untracked work suggests the chosen review target may miss nearby changes;
- parse Git diff hunks into repo-relative changed-line ranges for downstream rule filtering; and
- load `.planning/review-exclusions.json` as exact `rule_id + path + reason` entries with repo-relative path normalization and no broader path-only ignores.

Do not silently fall back to full-repo scanning, and keep all paths repo-relative and path-agnostic.
  </action>
  <verify>Run `npm test -- tests/review.test.cjs` and confirm the target-resolution and exclusion-contract cases pass.</verify>
  <done>
  `review:scan` can resolve staged and commit-range review targets, warn about incomplete scope, parse changed hunks for downstream filtering, and suppress exact rule-plus-path false positives while preserving the user-provided reason.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CLI-contract tests for review target selection and exclusions</name>
  <files>tests/review.test.cjs</files>
  <action>
Create `tests/review.test.cjs` using the repo's subprocess-first test style (`runGsdTools`, temp project fixtures, on-disk git setup as needed).

Cover at least these cases:
- staged diff is chosen by default when staged changes exist;
- empty staged state returns a promptable fallback that recommends commit-range review;
- untracked or unstaged neighboring changes produce an incomplete-scope warning rather than silently changing the target;
- exclusion entries match only exact `rule_id + normalized path` combinations;
- exclusion entries require a reason and stay hidden from the live findings list once matched; and
- JSON output includes enough target metadata for later rule and workflow stages.
  </action>
  <verify>Run `npm test -- tests/review.test.cjs` and verify the new review target/exclusion cases pass with zero failures.</verify>
  <done>
  The test suite proves the review scope is staged-first, empty-stage fallback is explicit, incomplete-scope warnings are preserved, and exclusions suppress only the intended false positives.
  </done>
</task>

</tasks>

<verification>
1. `node bin/bgsd-tools.cjs review:scan --help`
2. `npm test -- tests/review.test.cjs`
3. In a temp git project, verify `review:scan` chooses staged changes by default, returns a promptable fallback when nothing is staged, and hides matched exclusions from emitted findings
</verification>

<success_criteria>
- `review:scan` exists as a routed CLI command with stable JSON/formatted output paths
- Review target selection honors staged-first behavior, explicit empty-stage fallback, and incomplete-scope warnings
- `.planning/review-exclusions.json` is supported as exact rule-plus-path suppression with required reasons and no broader path-only ignores
- Command-level tests prove the target/exclusion contract before rule execution begins
</success_criteria>

<output>
After completion, create `.planning/phases/146-code-review-workflow/146-01-SUMMARY.md`
</output>
