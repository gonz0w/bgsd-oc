---
phase: 145-structured-agent-memory
plan: 02
type: execute
wave: 2
depends_on:
  - "145-01"
files_modified:
  - src/plugin/index.js
  - src/plugin/context-builder.js
  - src/plugin/file-watcher.js
  - tests/plugin.test.cjs
autonomous: true
requirements:
  - MEM-02
  - MEM-03
intent:
  outcome_ids:
    - DO-113

must_haves:
  truths:
    - "Agent prompts receive a MEMORY.md snapshot once at session start and keep using that frozen text for the rest of the session"
    - "Editing MEMORY.md mid-session updates disk state but does not silently change the active prompt"
    - "When MEMORY.md changes after the snapshot is captured, the user sees a concise refresh/restart notice explaining the prompt is stale"
    - "Unsafe memory entries are blocked before injection using normalized threat-pattern matching, while safe entries still load"
    - "Blocked-entry notices show blocker category plus a redacted snippet without teaching rewrites or bypasses"
  artifacts:
    - path: "src/plugin/context-builder.js"
      provides: "memory snapshot assembly and prompt-ready rendering for safe MEMORY.md entries"
      contains: "MEMORY.md"
    - path: "src/plugin/index.js"
      provides: "session-scoped frozen snapshot cache, stale tracking, and notification routing"
      contains: "experimental.chat.system.transform"
    - path: "tests/plugin.test.cjs"
      provides: "plugin integration coverage for snapshot freeze, stale notices, and unsafe-entry blocking"
      contains: "MEMORY.md"
  key_links:
    - from: "src/plugin/index.js"
      to: "src/plugin/context-builder.js"
      via: "session-scoped get-or-build memory snapshot path used by system transform"
      pattern: "buildSystemPrompt|MEMORY\.md|snapshot"
    - from: "src/plugin/file-watcher.js"
      to: "src/plugin/index.js"
      via: "external MEMORY.md change callback marks snapshot stale without mutating the active prompt"
      pattern: "MEMORY\.md|stale"
---

<objective>
Inject structured memory into plugin prompts as a frozen, screened session snapshot.

Purpose: Complete DO-113 by making MEMORY.md useful across sessions without crossing the phase boundary into live prompt mutation or retrieval systems, while blocking prompt-injection content before it reaches the system prompt.

Output: Plugin snapshot caching, stale-memory notifications, entry-level sanitization/blocking, and integration tests proving frozen-session behavior.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/INTENT.md
@.planning/phases/145-structured-agent-memory/145-CONTEXT.md
@.planning/phases/145-structured-agent-memory/145-RESEARCH.md
@.planning/phases/145-structured-agent-memory/145-01-SUMMARY.md
@src/plugin/index.js
@src/plugin/context-builder.js
@src/plugin/file-watcher.js
@src/plugin/advisory-guardrails.js
@src/plugin/token-budget.js
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session-frozen MEMORY.md snapshot caching and stale-change tracking</name>
  <files>src/plugin/index.js, src/plugin/context-builder.js, src/plugin/file-watcher.js</files>
  <action>
Extend the plugin so structured memory is captured once per plugin session and reused for every later system-transform call.

Implement this exact behavior:
- first prompt injection in a session reads `.planning/MEMORY.md`, builds the rendered memory snapshot, and caches the final injected text;
- later turns reuse that exact string instead of re-reading the file;
- if `.planning/MEMORY.md` changes on disk after the snapshot exists, mark the cached snapshot stale but keep using the frozen string until the session restarts or explicitly refreshes;
- emit one concise info notification telling the user memory changed on disk and a restart/refresh is needed for the new entries to become active.

Use existing plugin architecture rather than inventing a new subsystem:
- keep state scoped to the plugin/session instance in `src/plugin/index.js`;
- add the minimal callback or event plumbing needed in `src/plugin/file-watcher.js` so `MEMORY.md` changes can be identified without polling every turn;
- keep `buildSystemPrompt`/context-builder responsibilities focused on rendering, not global mutable state.

Do not expand into live prompt mutation, retrieval ranking, or auto-touching last-used timestamps.
  </action>
  <verify>Run `npm test -- tests/plugin.test.cjs` and verify the new snapshot-freeze and stale-notice cases pass.</verify>
  <done>
The plugin caches one MEMORY.md snapshot per session, reuses it on later turns, marks it stale when the file changes externally, and surfaces a concise refresh/restart notice without silently changing the active prompt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Screen memory entries before injection with normalized blocker categories and redacted warnings</name>
  <files>src/plugin/context-builder.js, src/plugin/index.js</files>
  <action>
Add entry-level memory screening before prompt injection, following the locked decisions from CONTEXT.md and the deterministic pattern guidance from RESEARCH.md.

Implement screening in the memory snapshot build path with these exact rules:
- scan each parsed entry independently so one bad entry does not disable the whole file;
- check both raw text and normalized text;
- reuse the same normalization pipeline shape already established by `src/plugin/advisory-guardrails.js` (NFKD, strip zero-width chars, strip combining marks) without changing GARD-04 behavior;
- block on strong matches in these categories from v1: instruction override, exfiltration/prompt leak, tool-coercion or guardrail bypass;
- exclude blocked entries from the injected snapshot;
- emit a concise warning containing count/category data and a redacted snippet for each blocked entry class;
- do not provide rewrite suggestions.

While assembling the prompt text, keep the injected memory compact and budget-aware. If the rendered block would materially bloat prompt space, use the existing token-budget warning pattern to surface an advisory notice rather than silently mutating content beyond the frozen-snapshot rules.
  </action>
  <verify>Run `npm test -- tests/plugin.test.cjs --test-name-pattern "memory|snapshot|stale|blocked"` and confirm blocked-entry and safe-subset injection cases pass.</verify>
  <done>
Unsafe entries are filtered out before prompt injection using normalized deterministic blocker patterns, safe entries still inject, warnings show category plus redacted snippet, and no live prompt mutation or rewrite hints are introduced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add plugin integration tests for frozen snapshot behavior, stale notices, and safe-subset injection</name>
  <files>tests/plugin.test.cjs</files>
  <action>
Extend `tests/plugin.test.cjs` with Phase 145 integration coverage.

Add tests for at least these scenarios:
- a project with MEMORY.md injects a structured memory block into system prompt output;
- the first read captures a snapshot and subsequent calls reuse it without re-reading updated disk content;
- changing MEMORY.md after initial injection marks the snapshot stale and queues a concise info notification;
- a blocked entry containing direct instruction override language is excluded while neighboring safe entries remain in the injected snapshot;
- normalized Unicode/zero-width variants of blocker phrases are also excluded;
- blocked warnings include category plus redacted snippet rather than full unsafe text or rewrite hints;
- projects without MEMORY.md still behave normally with no crash;
- existing `<bgsd>` project-state injection remains intact alongside the new memory block.

Keep assertions at the plugin API level where possible (`BgsdPlugin`, `experimental.chat.system.transform`, notifier queue behavior) so the tests verify real prompt integration, not only helper internals.
  </action>
  <verify>Run `npm test -- tests/plugin.test.cjs` and then `npm run build` to confirm plugin integration still bundles cleanly.</verify>
  <done>
`tests/plugin.test.cjs` proves MEMORY.md is injected once per session, stale changes produce notices instead of live prompt mutation, unsafe entries are filtered at entry level, and existing plugin context injection remains compatible.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- tests/plugin.test.cjs`
2. `npm run build`
3. Confirm plugin system-transform output includes both the existing `<bgsd>` state block and the new frozen memory block when MEMORY.md exists
4. Confirm mid-session MEMORY.md edits trigger a stale notice but do not alter the already-cached memory text
</verification>

<success_criteria>
- MEMORY.md is injected into prompts as a frozen session snapshot
- Mid-session file changes do not mutate the active prompt and instead produce a concise restart/refresh notice
- Entry-level screening blocks unsafe memory content using the repo's normalization style and injects only the safe subset
- Blocked-entry warnings include category plus redacted snippet and never offer rewrite guidance
- Plugin integration tests cover frozen snapshot, stale notices, Unicode-obfuscated blockers, and safe-subset injection
</success_criteria>

<output>
After completion, create `.planning/phases/145-structured-agent-memory/145-02-SUMMARY.md`
</output>
