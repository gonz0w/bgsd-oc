---
phase: 148-review-readiness-release-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - "148-03"
files_modified:
  - src/commands/init.js
  - src/router.js
  - commands/bgsd-release.md
  - workflows/release.md
  - tests/release-workflow.test.cjs
autonomous: true
requirements:
  - REL-04
intent:
  outcome_ids:
    - DO-112

must_haves:
  truths:
    - "Users can run `/bgsd-release` as one command and see a dry-run preview before any git mutation happens"
    - "The workflow uses a single explicit confirmation gate for the release essentials only: proposed bump, changelog summary, tag name, and target PR details"
    - "The workflow consumes deterministic `release:*` command output and persisted resume-state instead of improvising release logic in prompt space"
    - "Release PR creation interoperates with the existing github-ci workflow instead of creating a separate post-PR automation path"
    - "If a release stops partway through, the workflow offers resume guidance from the next safe step rather than forcing a full restart"
  artifacts:
    - path: "commands/bgsd-release.md"
      provides: "thin slash-command wrapper for the release workflow"
      contains: "workflows/release.md"
    - path: "workflows/release.md"
      provides: "dry-run-first release orchestration with one confirmation gate and resume guidance"
      contains: "release:bump"
    - path: "workflows/github-ci.md"
      provides: "existing PR automation workflow that `/bgsd-release` should hand off to or reference when continuing release CI"
      contains: "/bgsd-github-ci"
    - path: "src/commands/init.js"
      provides: "init payload for release workflow model/context selection and release-state path injection"
      contains: "init release"
    - path: "tests/release-workflow.test.cjs"
      provides: "workflow contract coverage for bootstrap, dry-run preview, single confirmation, and resume handling"
      contains: "/bgsd-release"
  key_links:
    - from: "commands/bgsd-release.md"
      to: "workflows/release.md"
      via: "slash-command wrapper launches the release workflow"
      pattern: "release.md"
    - from: "workflows/release.md"
      to: "src/commands/init.js"
      via: "workflow bootstrap resolves models, active phase metadata, and release-state paths through `init:release`"
      pattern: "init:release"
    - from: "workflows/release.md"
      to: "src/commands/release.js"
      via: "dry-run analysis runs before the single confirmation gate and mutation subcommands"
      pattern: "release:bump|release:changelog|release:tag|release:pr"
    - from: "workflows/release.md"
      to: "workflows/github-ci.md"
      via: "post-PR automation guidance reuses the existing github-ci workflow instead of inventing a separate CI handoff"
      pattern: "github-ci|/bgsd-github-ci"
---

<objective>
Deliver `/bgsd-release` as the single-command workflow entrypoint for Phase 148.

Purpose: Finish DO-112 by turning the release analysis and mutation helpers from Plans 01-03 into one dry-run-first workflow with a single meaningful confirmation gate and explicit resume guidance.

Output: `init:release` bootstrap support, `/bgsd-release` wrapper, release workflow markdown, and workflow contract tests.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/148-review-readiness-release-pipeline/148-CONTEXT.md
@.planning/phases/148-review-readiness-release-pipeline/148-RESEARCH.md
@.planning/phases/148-review-readiness-release-pipeline/148-03-PLAN.md
@commands/bgsd-review.md
@commands/bgsd-security.md
@workflows/review.md
@workflows/security.md
@workflows/github-ci.md
@src/commands/init.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add release workflow bootstrap support and slash-command wrapper</name>
  <files>src/commands/init.js, src/router.js, commands/bgsd-release.md</files>
  <action>
  Add an `init:release` path so `/bgsd-release` follows the same CLI-first bootstrap pattern as `/bgsd-review` and `/bgsd-security`.

  The init payload should include the workflow model(s), active phase metadata, release command names, commit-docs flag, and the `.planning/release-state.json` path so the workflow can resume intelligently without rediscovering state from raw files. Create `commands/bgsd-release.md` as the standard thin wrapper pointing at the workflow.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs init:release --pretty` and confirm the wrapper plus bootstrap path resolve without errors.</verify>
  <done>
  `/bgsd-release` has a proper wrapper and release-specific bootstrap payload, and the workflow no longer needs to guess model or resume-state context.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the dry-run-first release workflow with one confirmation gate and resume guidance</name>
  <files>workflows/release.md, src/commands/init.js</files>
  <action>
  Create `workflows/release.md` as the orchestration layer for the full Phase 148 release experience.

  The workflow must:
  - bootstrap through `init:release`;
  - run deterministic release analysis first (`release:bump`, `release:changelog`, and any readiness preview the flow chooses to show);
  - present one explicit confirmation gate before any git mutations;
  - keep the preview focused on the release essentials only: proposed version bump, changelog summary, tag name, and target PR details;
  - execute mutation steps only after confirmation; and
  - if something fails after mutations begin, use `.planning/release-state.json` to offer resume guidance from the next safe step.

  When a release PR is created successfully, make the workflow's final guidance explicitly point to the existing `/bgsd-github-ci` path (or the repo's equivalent github-ci automation contract) rather than inventing a separate post-PR release flow.

  Do not add extra confirmation ceremony, hidden readiness gates, or rollback behavior beyond the safe-cleanup boundaries already defined in Plan 03.
  </action>
  <verify>Review the generated workflow text and confirm it references dry-run preview, the single confirmation gate, release essentials, github-ci handoff, and resume guidance in sequence.</verify>
  <done>
  `/bgsd-release` runs as one dry-run-first flow with a single meaningful confirmation, explicit github-ci handoff, and resume handling instead of ad hoc shell choreography.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add workflow contract tests for bootstrap, confirmation order, and resume behavior</name>
  <files>tests/release-workflow.test.cjs</files>
  <action>
  Create `tests/release-workflow.test.cjs` to validate the workflow/document contract.

  Cover at least these checks:
  - `commands/bgsd-release.md` points to `workflows/release.md` using the repo's wrapper style;
  - the workflow bootstraps via `init:release` rather than hard-coded model assumptions;
  - release analysis commands run before the confirmation gate;
  - the preview lists only the locked essentials (bump, changelog summary, tag, PR details);
  - exactly one explicit confirmation gate appears before mutation steps; and
  - successful PR creation points to the existing github-ci workflow instead of a separate post-PR release path; and
  - resume guidance references persisted release state instead of retrying blindly from the beginning.
  </action>
  <verify>Run `node --test --test-force-exit tests/release-workflow.test.cjs` and verify the wrapper/workflow contract cases pass.</verify>
  <done>
  The workflow contract is locked down: `/bgsd-release` remains single-command, dry-run-first, single-confirmation, and resumable.
  </done>
</task>

</tasks>

<verification>
1. `node --test --test-force-exit tests/release-workflow.test.cjs`
2. Confirm `commands/bgsd-release.md` points to `workflows/release.md`
3. Confirm the workflow runs release analysis before the single confirmation gate and references `.planning/release-state.json` for resume guidance
</verification>

<success_criteria>
- `/bgsd-release` exists as a single-command wrapper over a dedicated release workflow
- The workflow is dry-run by default and shows only the release essentials before one explicit confirmation gate
- Release mutations are driven by deterministic CLI output rather than prompt-space rediscovery, and successful PR handoff stays aligned with the existing github-ci workflow
- Resume guidance is explicit and based on persisted `.planning/release-state.json` state
</success_criteria>

<output>
After completion, create `.planning/phases/148-review-readiness-release-pipeline/148-04-SUMMARY.md`
</output>
