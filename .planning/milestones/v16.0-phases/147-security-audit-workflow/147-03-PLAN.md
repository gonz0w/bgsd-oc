---
phase: 147-security-audit-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - "147-02"
files_modified:
  - src/commands/security.js
  - src/lib/security/scan.js
  - src/lib/security/engines/secrets.js
  - src/lib/security/engines/dependencies.js
  - tests/security.test.cjs
autonomous: true
requirements:
  - SEC-02
  - SEC-03
  - SEC-04
intent:
  outcome_ids:
    - DO-110
    - DO-111

must_haves:
  truths:
    - "Secret findings are redacted, explicitly allowlisted when intentional, and still scan the repo by default rather than silently skipping test-like paths"
    - "Dependency findings include severity, remediation guidance, and confidence that reflects evidence quality such as resolved lockfile versus range-only manifest data"
    - "The shared scan pipeline combines OWASP, secrets, and dependency findings into one severity-led, action-first report with explicit confidence labels"
  artifacts:
    - path: "src/lib/security/engines/secrets.js"
      provides: "provider-specific plus generic secret detection with redaction, allowlist handling, and stable finding fingerprints"
      contains: "redact"
    - path: "src/lib/security/engines/dependencies.js"
      provides: "manifest and lockfile parsing plus advisory-backed dependency findings"
      contains: "osv"
    - path: "tests/security.test.cjs"
      provides: "coverage for secret allowlisting, dependency confidence/remediation, and multi-engine output shaping"
      contains: "dependency"
  key_links:
    - from: "src/lib/security/engines/secrets.js"
      to: "src/lib/security/scan.js"
      via: "secret findings normalize into the shared scan pipeline with redacted evidence"
      pattern: "redacted|secret"
    - from: "src/lib/security/engines/dependencies.js"
      to: "src/lib/security/scan.js"
      via: "dependency advisory matches contribute severity, remediation, and confidence-scored findings"
      pattern: "advisory|remediation"
---

<objective>
Build the secrets and dependency engines plus final multi-engine reporting for `security:scan`.

Purpose: Deliver the remaining core DO-110 behavior so the scanner can detect hardcoded secrets and vulnerable dependencies, then merge those results with the OWASP engine into one confidence-explicit security audit.

Output: Secrets engine with allowlist/redaction, dependency advisory engine with evidence-quality scoring, integrated severity-led reporting, and tests covering the combined multi-engine contract.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/147-security-audit-workflow/147-CONTEXT.md
@.planning/phases/147-security-audit-workflow/147-RESEARCH.md
@.planning/phases/147-security-audit-workflow/147-02-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/commands/security.js
@src/lib/security/scan.js
@src/lib/review/severity.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the secrets engine with redaction, allowlisting, and finding fingerprints</name>
  <files>src/lib/security/engines/secrets.js, src/lib/security/scan.js, src/commands/security.js</files>
  <action>
Implement the secrets detector while honoring the locked user decisions from `147-CONTEXT.md`.

For secrets:
- scan the repo by default;
- prefer provider-specific and context-aware patterns before generic heuristics;
- redact all matched values in findings;
- use explicit allowlisting for fixtures/examples rather than path-based auto-relaxation; and
- emit stable fingerprints so intentional suppressions stay finding-specific.
  </action>
  <verify>Run `npm test -- tests/security.test.cjs` and confirm secret redaction, allowlist, and finding-fingerprint cases pass.</verify>
  <done>
  Secret findings are safely redacted, scan repo content by default, and remain suppressible only through explicit finding-specific allowlist/exclusion behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the dependency engine with advisory lookups and evidence-quality confidence</name>
  <files>src/lib/security/engines/dependencies.js, src/lib/security/scan.js, src/commands/security.js</files>
  <action>
Implement the dependency detector and its evidence model.

Apply these exact behaviors:
- parse the required manifests (`package.json`, `requirements.txt`, `go.mod`) and opportunistically use lockfiles when present to raise confidence;
- query a shared advisory backend rather than shelling out to ecosystem-specific audit commands as the main path;
- distinguish resolved, pinned, and range-only evidence quality before assigning confidence; and
- attach severity plus concrete remediation guidance to emitted findings.
  </action>
  <verify>Run `npm test -- tests/security.test.cjs` and confirm dependency confidence/remediation cases pass.</verify>
  <done>
  Dependency findings reflect advisory data and version-evidence quality instead of overclaiming certainty from loose version ranges.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate multi-engine confidence routing and severity-led action-first output</name>
  <files>src/lib/security/scan.js, src/commands/security.js, tests/security.test.cjs</files>
  <action>
Finish the shared post-detection pipeline so `security:scan` presents one coherent security audit instead of disconnected reports.

Apply these exact behaviors:
- normalize all engines into one schema;
- suppress low-confidence findings while preserving explicitly labeled `high` and `medium` results per locked context;
- keep confidence visible in security output instead of hiding it like Phase 146 review output;
- sort and group findings by severity first, then engine/category details;
- include short rationale and concrete next-step guidance for every surfaced finding; and
- keep machine-readable output rich enough for `/bgsd-security` and later readiness/reporting reuse, which is why this plan also traces to DO-111.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs security:scan --pretty` in a fixture repo and confirm a dependency advisory and other detector findings appear in one severity-led report with explicit confidence labels; then run `npm test -- tests/security.test.cjs`.</verify>
  <done>
  `security:scan` emits one action-first, severity-led report combining OWASP, secrets, and dependency findings with explicit confidence labels, advisory-backed dependency remediation, and workflow-ready structured JSON.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- tests/security.test.cjs`
2. `node bin/bgsd-tools.cjs security:scan --help`
3. Manual smoke check in a temp repo: confirm an OWASP finding, a redacted secret finding, and a dependency advisory all appear in one severity-led report with explicit confidence labels
</verification>

<success_criteria>
- Secret scanning detects likely credentials repo-wide, redacts evidence, and relies on explicit allowlisting for intentional fixtures/examples
- Dependency scanning reports advisory-backed findings with remediation info and confidence shaped by version evidence quality
- Final `security:scan` output is severity-led, action-first, and reusable by downstream workflow/readiness consumers
</success_criteria>

<output>
After completion, create `.planning/phases/147-security-audit-workflow/147-03-SUMMARY.md`
</output>
