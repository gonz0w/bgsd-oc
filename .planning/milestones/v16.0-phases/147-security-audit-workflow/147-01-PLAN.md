---
phase: 147-security-audit-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/security.js
  - src/router.js
  - src/lib/constants.js
  - src/lib/command-help.js
  - src/lib/security/config.js
  - src/lib/security/schema.js
  - src/lib/security/scan.js
  - src/lib/security/exclusions.js
  - tests/security.test.cjs
autonomous: true
requirements:
  - SEC-04
  - SEC-05
intent:
  outcome_ids:
    - DO-110

must_haves:
  truths:
    - "`security:scan` exists as a routed CLI command with one deterministic JSON contract for all security engines"
    - "Security findings carry severity, explicit confidence band, location, rationale, next step, and independent-verification metadata before workflow handling"
    - "Security suppressions stay narrow and auditable at the finding level, with required reason and expiry instead of broad ignore paths"
    - "Expired exclusions are surfaced instead of silently hiding findings"
  artifacts:
    - path: "src/commands/security.js"
      provides: "security:scan command entrypoint and output contract"
      contains: "security:scan"
    - path: "src/lib/security/schema.js"
      provides: "normalized security finding shape shared by OWASP, secrets, and dependency engines"
      contains: "confidence_band"
    - path: "src/lib/security/exclusions.js"
      provides: "security exclusion loader and exact finding-level matcher for .planning/security-exclusions.json"
      contains: "security-exclusions.json"
    - path: "tests/security.test.cjs"
      provides: "CLI-contract coverage for security scan setup, exclusion behavior, and schema stability"
      contains: "security:scan"
  key_links:
    - from: "src/router.js"
      to: "src/commands/security.js"
      via: "security namespace routing and command discovery/help wiring"
      pattern: "security:scan"
    - from: "src/commands/security.js"
      to: "src/lib/security/scan.js"
      via: "command entrypoint delegates to the shared multi-engine scan coordinator"
      pattern: "scanSecurity"
    - from: "src/lib/security/scan.js"
      to: "src/lib/security/exclusions.js"
      via: "suppression pass runs after finding normalization and before final emission"
      pattern: "exclude|security-exclusions"
---

<objective>
Establish the CLI surface, shared finding contract, and auditable exclusion plumbing for Phase 147.

Purpose: Create the deterministic scan-first foundation for DO-110 so OWASP, secrets, dependency, and workflow logic all consume one stable security schema instead of inventing engine-specific behavior.

Output: `security:scan` command wiring, shared config/schema helpers, scan coordinator scaffolding, `.planning/security-exclusions.json` matching, and command-level tests for exclusion and output-contract behavior.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/147-security-audit-workflow/147-CONTEXT.md
@.planning/phases/147-security-audit-workflow/147-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@src/commands/review.js
@src/lib/review/config.js
@src/lib/review/exclusions.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the security command surface and shared config/schema helpers</name>
  <files>src/commands/security.js, src/router.js, src/lib/constants.js, src/lib/command-help.js, src/lib/security/config.js, src/lib/security/schema.js</files>
  <action>
Create `src/commands/security.js` as the Phase 147 entrypoint for `security:scan`, then wire it through `src/router.js`, `src/lib/constants.js`, and `src/lib/command-help.js` using the repo's existing namespace, lazy-loader, and help-text patterns.

Implement `src/lib/security/config.js` and `src/lib/security/schema.js` so all detector engines share one normalized contract. Honor the locked context decisions directly: severity-led action-first output, explicit confidence in results, and support for both high- and medium-confidence findings while suppressing low-confidence noise. Because the roadmap requirement still mentions an 8/10 gate, model the contract as confidence bands (`high`, `medium`, `low`) with a default high threshold at 0.8 and a separate medium band for explicitly labeled lower-confidence findings.

Keep the command CLI-first and deterministic: JSON when piped, human-readable severity-led output when attached to a TTY, and no prompt-space rediscovery of findings.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs security:scan --help` and confirm the command is routed and documented.</verify>
  <done>
  `security:scan` is discoverable through the router/help system, its config/schema surface reflects the phase's confidence and reporting rules, and later detector engines have one stable finding contract to emit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the scan coordinator scaffold and exact security exclusion matching</name>
  <files>src/lib/security/scan.js, src/lib/security/exclusions.js, src/lib/security/schema.js, src/commands/security.js</files>
  <action>
Implement the pre-engine scan pipeline described in `147-CONTEXT.md` and `147-RESEARCH.md`.

Build helpers that:
- resolve repo-root scanning by default while preserving an explicit target contract for future narrowing;
- collect raw findings from separate detector engines without forcing one monolithic rule format;
- normalize all findings to the shared schema before any suppression or formatting;
- load `.planning/security-exclusions.json` as finding-level exclusions with exact matching fields, required `reason`, and required `expires_at`; and
- surface expired exclusions as warnings instead of silently honoring them.

Use a finding identity that stays narrower than Phase 146's rule-plus-path suppression — for example exact `rule_id + normalized path + fingerprint` where the engine can provide one — so the implementation honors the user's locked "finding-level exclusions" decision rather than broad rule suppression.
  </action>
  <verify>Run `npm test -- tests/security.test.cjs` and confirm the exclusion-contract and schema-coordinator cases pass.</verify>
  <done>
  The security scan pipeline can normalize multi-engine findings, apply auditable finding-level suppressions with expiry checks, and emit warnings when exclusion entries are stale or invalid.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CLI-contract tests for security schema, confidence bands, and exclusions</name>
  <files>tests/security.test.cjs</files>
  <action>
Create `tests/security.test.cjs` using the repo's subprocess-first test style.

Cover at least these cases:
- `security:scan` is routed and returns the expected top-level JSON shape;
- findings can carry explicit `high` and `medium` confidence labels while low-confidence findings remain suppressible;
- exclusion entries require `reason` and `expires_at`;
- exclusion matching stays exact to the intended finding identity rather than suppressing unrelated findings from the same rule family; and
- expired exclusions remain visible as warnings for auditability.
  </action>
  <verify>Run `npm test -- tests/security.test.cjs` and verify the new contract/exclusion cases pass with zero failures.</verify>
  <done>
  The test suite proves the security workflow foundation is deterministic: stable schema, explicit confidence bands, and narrow auditable suppressions.
  </done>
</task>

</tasks>

<verification>
1. `node bin/bgsd-tools.cjs security:scan --help`
2. `npm test -- tests/security.test.cjs`
3. In a temp project, verify invalid or expired security exclusions produce warnings and do not silently hide findings
</verification>

<success_criteria>
- `security:scan` exists as a routed CLI command with a stable JSON/formatted contract
- Shared schema/config helpers define severity-led, explicit-confidence security findings for all engines
- `.planning/security-exclusions.json` supports narrow finding-level suppressions with required reason and expiry metadata
- Command-level tests lock down the exclusion and output contract before detector engines are added
</success_criteria>

<output>
After completion, create `.planning/phases/147-security-audit-workflow/147-01-SUMMARY.md`
</output>
