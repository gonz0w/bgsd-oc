---
phase: 147-security-audit-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - "147-01"
files_modified:
  - src/commands/security.js
  - src/lib/security/scan.js
  - src/lib/security/engines/owasp.js
  - src/lib/security/rules/index.js
  - src/lib/security/rules/js-injection.js
  - src/lib/security/rules/unsafe-exec.js
  - src/lib/security/rules/auth-misconfig.js
  - src/lib/security/rules/config-misuse.js
  - src/lib/security/rules/xss.js
  - src/lib/security/rules/xxe.js
  - src/lib/security/rules/access-control.js
  - src/lib/security/rules/insecure-deserialization.js
  - src/lib/security/rules/logging-gaps.js
  - tests/security.test.cjs
autonomous: true
requirements:
  - SEC-01
  - SEC-04
intent:
  outcome_ids:
    - DO-110
    - DO-111

must_haves:
  truths:
    - "OWASP coverage includes an explicit mapped implementation path for each SEC-01 category promised in roadmap/requirements: injection, broken auth, sensitive data exposure, XXE, broken access control, security misconfiguration, XSS, insecure deserialization, known vulnerable components, and insufficient logging"
    - "OWASP registry metadata stays high-signal and honest by making narrow v1 coverage explicit per category instead of claiming unsupported depth"
    - "The shared scan pipeline can accept OWASP engine findings and preserve category mapping, rationale, and next-step guidance for later multi-engine integration"
  artifacts:
    - path: "src/lib/security/engines/owasp.js"
      provides: "curated OWASP-style code-risk detector over supported source patterns"
      contains: "owasp"
    - path: "tests/security.test.cjs"
      provides: "coverage for OWASP category mapping, code-pattern detection, and registry honesty"
      contains: "owasp"
  key_links:
    - from: "src/lib/security/rules/index.js"
      to: "src/lib/security/engines/owasp.js"
      via: "high-signal rule registry mapped to OWASP categories"
      pattern: "A0"
    - from: "src/lib/security/engines/owasp.js"
      to: "src/lib/security/scan.js"
      via: "OWASP findings normalize into the shared scan pipeline before later multi-engine composition"
      pattern: "owasp|normalize"
---

<objective>
Build the OWASP detector inventory and code-pattern engine for `security:scan`.

Purpose: Deliver the SEC-01 foundation first so Phase 147 can truthfully claim OWASP category coverage with explicit rule-to-category mapping before adding secrets, dependency, and workflow layers.

Output: OWASP rule registry and engine, category-mapped rule files for each promised SEC-01 area, scan-pipeline integration for OWASP findings, and tests covering category mapping plus code-pattern detection.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/147-security-audit-workflow/147-CONTEXT.md
@.planning/phases/147-security-audit-workflow/147-RESEARCH.md
@.planning/phases/147-security-audit-workflow/147-01-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/lib/ast.js
@src/commands/review.js
@src/lib/review/severity.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the OWASP rule registry with explicit coverage for every SEC-01 category</name>
  <files>src/lib/security/engines/owasp.js, src/lib/security/rules/index.js, src/lib/security/rules/js-injection.js, src/lib/security/rules/unsafe-exec.js, src/lib/security/rules/auth-misconfig.js, src/lib/security/rules/config-misuse.js, src/lib/security/rules/xss.js, src/lib/security/rules/xxe.js, src/lib/security/rules/access-control.js, src/lib/security/rules/insecure-deserialization.js, src/lib/security/rules/logging-gaps.js</files>
  <action>
Implement the v1 OWASP-style code-risk detector as a curated, high-signal ruleset rather than a broad heuristic sweep.

Create a documented rule inventory and registry entries that give each promised SEC-01 category a concrete implementation path in v1. That can be a direct detector rule, or a clearly scoped detector family in the registry, but the plan must result in shipped coverage for:
- injection;
- broken authentication;
- sensitive data exposure / cryptographic misuse;
- XXE;
- broken access control;
- security misconfiguration;
- XSS;
- insecure deserialization;
- known vulnerable components (fed by the dependency engine integration); and
- insufficient logging / monitoring where only explicit high-signal cases should surface.

Keep the implementation honest and high-signal: when a category only supports narrow explicit patterns in v1, encode that narrowness in rule metadata and tests instead of omitting the category entirely or inflating noisy regexes just to claim breadth.
  </action>
  <verify>Run `npm test -- tests/security.test.cjs` and confirm OWASP rule-mapping and code-pattern detection cases pass.</verify>
  <done>
  The scanner ships a documented high-signal OWASP rule inventory whose registry and tests explicitly account for every SEC-01 category promised by the phase requirements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate OWASP findings into the shared scan pipeline with stable output metadata</name>
  <files>src/lib/security/scan.js, src/commands/security.js, src/lib/security/engines/owasp.js, tests/security.test.cjs</files>
  <action>
Wire the OWASP engine into the shared scan pipeline so the Phase 147 command can emit truthful SEC-01 results before the other engines land.

Ensure the integration preserves:
- explicit OWASP category metadata on every emitted finding;
- action-first rationale and next-step guidance in the shared schema;
- severity-first ordering consistent with later engines; and
- a narrow, high-signal result set rather than noisy category-padding heuristics.
  </action>
  <verify>Run `npm test -- tests/security.test.cjs` and confirm OWASP findings flow through the shared scan contract with the expected category/rationale metadata.</verify>
  <done>
  `security:scan` can already emit real OWASP-mapped findings through the shared contract, giving later plans a stable base for secrets and dependency integration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lock down SEC-01 coverage with focused rule and mapping tests</name>
  <files>tests/security.test.cjs</files>
  <action>
Extend `tests/security.test.cjs` so SEC-01 coverage is provable instead of implied.

Cover at least these checks:
- every SEC-01 category has at least one registry entry or detector-family mapping in shipped metadata;
- high-signal fixtures trigger representative findings for injection, auth/access, config misuse, XSS/XXE/deserialization-style patterns, and logging gaps where supported;
- findings retain explicit category mapping plus rationale/next-step text through the command output; and
- narrow v1 categories stay narrow in tests rather than silently broadening into low-precision heuristics.
  </action>
  <verify>Run `npm test -- tests/security.test.cjs` and confirm SEC-01 mapping/detection cases pass with zero failures.</verify>
  <done>
  The test suite proves SEC-01 is implemented as real shipped category coverage rather than aspirational roadmap text.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- tests/security.test.cjs`
2. `node bin/bgsd-tools.cjs security:scan --help`
3. Manual smoke check in a temp repo: confirm representative OWASP findings surface with explicit category mapping, rationale, and next-step guidance
</verification>

<success_criteria>
- OWASP-style detection ships as a curated high-signal rule inventory mapped to explicit OWASP categories, with a concrete v1 implementation path for every SEC-01 category named in the phase requirements
- `security:scan` can already emit OWASP-mapped findings through the shared schema instead of keeping the detector isolated
- Tests prove category coverage and keep narrow v1 coverage honest instead of overclaiming unsupported depth
</success_criteria>

<output>
After completion, create `.planning/phases/147-security-audit-workflow/147-02-SUMMARY.md`
</output>
