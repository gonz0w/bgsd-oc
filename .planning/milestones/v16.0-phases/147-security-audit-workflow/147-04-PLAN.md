---
phase: 147-security-audit-workflow
plan: 04
type: execute
wave: 4
depends_on:
  - "147-03"
files_modified:
  - src/commands/init.js
  - commands/bgsd-security.md
  - workflows/security.md
  - tests/security-workflow.test.cjs
autonomous: true
requirements:
  - SEC-04
intent:
  outcome_ids:
    - DO-110
    - DO-111

must_haves:
  truths:
    - "Users can run `/bgsd-security` as a single command and get the full scan-plus-assessment flow without manually chaining CLI steps"
    - "The workflow treats `security:scan` JSON as the deterministic source of findings instead of re-discovering vulnerabilities in prompt space"
    - "Surviving findings are independently verified or clearly labeled when confidence remains medium, rather than flattening all findings into one certainty level"
    - "The final workflow summary remains severity-led and reusable by later readiness/reporting work"
  artifacts:
    - path: "commands/bgsd-security.md"
      provides: "slash-command wrapper for the security workflow"
      contains: "workflows/security.md"
    - path: "workflows/security.md"
      provides: "scan-first security workflow orchestrating CLI findings, verifier assessment, and final report"
      contains: "security:scan"
    - path: "src/commands/init.js"
      provides: "init payload for security workflow model/context selection"
      contains: "init security"
    - path: "tests/security-workflow.test.cjs"
      provides: "workflow contract coverage for single-command security orchestration"
      contains: "/bgsd-security"
  key_links:
    - from: "commands/bgsd-security.md"
      to: "workflows/security.md"
      via: "slash-command execution wrapper"
      pattern: "security.md"
    - from: "workflows/security.md"
      to: "src/commands/init.js"
      via: "workflow bootstrap for model/context payload"
      pattern: "init:security"
    - from: "workflows/security.md"
      to: "security:scan"
      via: "deterministic CLI scan before any verifier assessment or user-facing summary"
      pattern: "security:scan"
---

<objective>
Deliver `/bgsd-security` as the single-command workflow entrypoint for Phase 147.

Purpose: Complete the CLI-first / workflow-second pattern by turning the scan contract from Plans 01-03 into an orchestrated security audit flow that verifies findings, preserves confidence clarity, and produces a readiness-friendly structured summary.

Output: `init:security` bootstrap support, `/bgsd-security` slash-command wrapper, workflow orchestration markdown, and workflow contract tests.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/STATE.md
@.planning/phases/147-security-audit-workflow/147-CONTEXT.md
@.planning/phases/147-security-audit-workflow/147-RESEARCH.md
@.planning/phases/147-security-audit-workflow/147-03-PLAN.md
@commands/bgsd-review.md
@workflows/review.md
@src/commands/init.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security workflow bootstrap support and slash-command wrapper</name>
  <files>src/commands/init.js, commands/bgsd-security.md</files>
  <action>
Add an `init:security` path so `/bgsd-security` follows the repo's workflow bootstrap pattern instead of hard-coding models or guessing project context from raw markdown.

The init payload should provide the minimal workflow inputs needed for a deterministic security flow: workflow model/verifier model selection, current phase/project metadata, commit-docs flag, and any scan/report settings the workflow needs to store results in the right planning area without rediscovery.

Create `commands/bgsd-security.md` as the thin wrapper that points at the new security workflow using the same wrapper style used throughout `commands/`.
  </action>
  <verify>Run `node bin/bgsd-tools.cjs init:security --pretty` if implemented as a direct init entrypoint or the equivalent routed form, and confirm the wrapper plus bootstrap path resolve without errors.</verify>
  <done>
  `/bgsd-security` has a proper command wrapper and workflow bootstrap path, and the security workflow no longer needs to improvise model/context selection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement the scan-first security workflow with verifier assessment and severity-led reporting</name>
  <files>workflows/security.md, src/commands/init.js</files>
  <action>
Create `workflows/security.md` as the orchestration layer for Phase 147, keeping the architecture CLI-first and workflow-second.

The workflow must:
- run `security:scan` first and treat its JSON as the deterministic findings source of truth;
- keep clean runs quiet and concise;
- independently verify each surfaced finding before final reporting, especially medium-confidence results;
- preserve explicit confidence labeling in user-facing output instead of collapsing findings into a binary pass/fail;
- surface narrow exclusion suggestions only for specific findings rather than broad suppressions; and
- end with an action-first severity-led summary that is reusable by later readiness/reporting work.

If a finding cannot be independently strengthened, the workflow should report it as medium-confidence with rationale rather than silently upgrading certainty.
  </action>
  <verify>Review the generated workflow text and confirm it references `security:scan`, independent verifier assessment, explicit confidence labeling, finding-level exclusion suggestions, and severity-led reporting in sequence.</verify>
  <done>
  `/bgsd-security` executes the whole security-audit experience as one workflow: scan, verification pass, narrow suppression handling, and final severity-led report.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add workflow contract tests for single-command security orchestration</name>
  <files>tests/security-workflow.test.cjs</files>
  <action>
Create `tests/security-workflow.test.cjs` to validate the orchestration contract at the workflow/document level.

Cover at least these checks:
- `commands/bgsd-security.md` points to `workflows/security.md` using the repo's wrapper format;
- the workflow bootstraps through the security init path rather than hard-coded model assumptions;
- `security:scan` is the first deterministic step before verifier assessment;
- medium-confidence findings receive explicit verification/labeling logic rather than silent suppression or inflation;
- exclusion suggestions remain finding-specific; and
- the workflow concludes with severity-led structured output suitable for later readiness consumption, which is why this plan also traces to DO-111.
  </action>
  <verify>Run `npm test -- tests/security-workflow.test.cjs` and verify the wrapper/workflow contract cases pass.</verify>
  <done>
  The workflow contract is locked down: `/bgsd-security` remains single-command, scan-first, confidence-explicit, independently verified, and reusable by downstream readiness/reporting features.
  </done>
</task>

</tasks>

<verification>
1. `npm test -- tests/security-workflow.test.cjs`
2. Confirm `commands/bgsd-security.md` points to `workflows/security.md`
3. Confirm the workflow runs `security:scan` before verifier assessment and keeps medium-confidence findings explicitly labeled in the final output
</verification>

<success_criteria>
- `/bgsd-security` exists as a single-command wrapper over a dedicated security workflow
- The workflow consumes scan JSON first, then performs verifier assessment rather than re-discovering vulnerabilities
- Findings remain severity-led and explicitly confidence-labeled through the final summary
- Final workflow output is reusable by later readiness/reporting features
</success_criteria>

<output>
After completion, create `.planning/phases/147-security-audit-workflow/147-04-SUMMARY.md`
</output>
