---
phase: 141-taxonomy-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/questions.js
autonomous: true
requirements:
  - TAX-01
  - TAX-03
user_setup: []

must_haves:
  truths:
    - TAXONOMY enum has exactly 7 types: BINARY, SINGLE_CHOICE, MULTI_CHOICE, RANKING, FILTERING, EXPLORATION, CLARIFICATION
    - OPTION_RULES enforces MIN 3 / MAX 5 options, diversity dimensions, formatting parity, escape hatch
    - Escape hatch option is "Something else" at position last
    - Option templates are data structures only (no function logic)
  artifacts:
    - src/lib/questions.js exists with TAXONOMY, OPTION_RULES, and OPTION_TEMPLATES
  key_links:
    - questions.js exports TAXONOMY, OPTION_RULES, getQuestionTemplate()
---

<objective>
Create the foundational questions.js module containing the TAXONOMY enum, OPTION_RULES, and template data structures. This is the foundation that prompts.js and decision-rules.js depend on.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
</execution_context>

<context>
@.planning/phases/141-taxonomy-infrastructure/141-CONTEXT.md
@.planning/phases/141-taxonomy-infrastructure/141-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Create questions.js with TAXONOMY and OPTION_RULES</name>
  <files>src/lib/questions.js</files>
  <action>
Create src/lib/questions.js with:
1. TAXONOMY enum object with 7 types: BINARY, SINGLE_CHOICE, MULTI_CHOICE, RANKING, FILTERING, EXPLORATION, CLARIFICATION
2. OPTION_RULES constant object with: MIN_OPTIONS: 3, MAX_OPTIONS: 5, DIVERSITY_DIMENSIONS: ['certainty', 'scope', 'approach', 'priority'], FORMATTING_PARITY: true, ESCAPE_HATCH: 'Something else', ESCAPE_HATCH_POSITION: 'last'
3. Template registry structure: OPTION_TEMPLATES object keyed by template ID
4. getQuestionTemplate(id, context) function that looks up template by ID, applies tone parameter (formal/casual), returns { question, options, escapeHatch, typeHint } or null if not found
5. Graceful fallback: if template not found, return null (not an error)
6. generateRuntimeOptions(type, context) function for structured runtime generation with diversity constraints

Use ES module-compatible exports (module.exports).
  </action>
  <verify>
node -e "const q = require('./src/lib/questions.js'); console.log('TAXONOMY keys:', Object.keys(q.TAXONOMY).join(', ')); console.log('MIN_OPTIONS:', q.OPTION_RULES.MIN_OPTIONS); console.log('ESCAPE_HATCH:', q.OPTION_RULES.ESCAPE_HATCH); console.log('getQuestionTemplate exists:', typeof q.getQuestionTemplate === 'function'); console.log('generateRuntimeOptions exists:', typeof q.generateRuntimeOptions === 'function');"
  </verify>
  <done>
- TAXONOMY enum has exactly 7 values
- OPTION_RULES has all required fields (MIN_OPTIONS, MAX_OPTIONS, DIVERSITY_DIMENSIONS, FORMATTING_PARITY, ESCAPE_HATCH, ESCAPE_HATCH_POSITION)
- getQuestionTemplate(id, context) returns template data or null
- generateRuntimeOptions(type, context) returns array of 3-5 options
</done>
</task>

</tasks>

<verification>
Questions.js module loads without errors, TAXONOMY enum has correct 7 types, OPTION_RULES has all required fields, getQuestionTemplate returns expected structure.
</verification>

<success_criteria>
- src/lib/questions.js created with TAXONOMY (7 types), OPTION_RULES, getQuestionTemplate(), generateRuntimeOptions()
- Module can be required without errors
- TAXONOMY enum values match requirements
</success_criteria>

<output>
After completion, create `.planning/phases/141-taxonomy-infrastructure/141-01-SUMMARY.md`
</output>
