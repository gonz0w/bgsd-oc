---
phase: 141-taxonomy-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/prompts.js
autonomous: true
requirements:
  - TAX-02
  - TAX-06
  - TAX-07
user_setup: []

must_haves:
  truths:
    - questionTemplate(id, type, context) is a function exported from prompts.js
    - Templates contain only options array (no question text)
    - Tone parameter (formal/casual) adjusts option wording
    - Mutual exclusivity signaled via type-specific hint ("Pick one" for SINGLE_CHOICE/BINARY, "Select all that apply" for MULTI_CHOICE)
    - Consequence-framed options include trade-off hints where applicable
    - Graceful fallback returns null if template missing (no error thrown)
  artifacts:
    - questionTemplate() function in prompts.js
  key_links:
    - questionTemplate() calls questions.js getQuestionTemplate() internally
---

<objective>
Add questionTemplate(id, type, context) function to prompts.js that provides workflow integration for question templates. This function is the interface workflows use to get options.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/141-taxonomy-infrastructure/141-CONTEXT.md
@.planning/phases/141-taxonomy-infrastructure/141-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/lib/prompts.js
@src/lib/questions.js
</context>

<tasks>

<task type="auto">
  <name>Add questionTemplate() function to prompts.js</name>
  <files>src/lib/prompts.js</files>
  <action>
Add to prompts.js:
1. Require questions.js at top: const { getQuestionTemplate, generateRuntimeOptions, OPTION_RULES } = require('./questions');
2. questionTemplate(id, type, context = {}) function that:
   - Calls getQuestionTemplate(id, context) to get pre-authored options
   - If template found: returns { options, typeHint, escapeHatch } with typeHint based on type (TAXONOMY.SINGLE_CHOICE/BINARY → "Pick one", TAXONOMY.MULTI_CHOICE → "Select all that apply")
   - If template not found: calls generateRuntimeOptions(type, context) for structured runtime generation
   - Wraps result to include: { templateId: id, typeHint, escapeHatch: OPTION_RULES.ESCAPE_HATCH, options }
   - Tone parameter support: context.tone === 'formal' || context.tone === 'casual' adjusts wording
3. Export questionTemplate in module.exports

Note: Templates provide OPTIONS ONLY — question text stays in workflow. The typeHint signals mutual exclusivity.
  </action>
  <verify>
node -e "const p = require('./src/lib/prompts.js'); console.log('questionTemplate exists:', typeof p.questionTemplate === 'function'); const result = p.questionTemplate('test-template', 'SINGLE_CHOICE', {}); console.log('Returns object:', typeof result === 'object'); if (result) { console.log('Has options:', Array.isArray(result.options)); console.log('Has typeHint:', typeof result.typeHint === 'string'); console.log('Has escapeHatch:', typeof result.escapeHatch === 'string'); }"
  </verify>
  <done>
- questionTemplate(id, type, context) exported from prompts.js
- Function returns { templateId, options, typeHint, escapeHatch }
- typeHint is "Pick one" for SINGLE_CHOICE/BINARY, "Select all that apply" for MULTI_CHOICE
- Graceful fallback when template not found (calls generateRuntimeOptions)
- Tone parameter support
</done>
</task>

</tasks>

<verification>
questionTemplate function exists, returns correct structure, typeHint signals mutual exclusivity correctly.
</verification>

<success_criteria>
- questionTemplate() function callable with (id, type, context)
- Returns object with options array, typeHint, escapeHatch
- Graceful fallback when template missing (no error thrown)
</success_criteria>

<output>
After completion, create `.planning/phases/141-taxonomy-infrastructure/141-02-SUMMARY.md`
</output>
