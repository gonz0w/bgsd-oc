---
phase: 142-primary-workflow-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/verify-work.md
  - src/lib/questions.js
autonomous: true
requirements:
  - MIGRATE-05
must_haves:
  truths:
    - verify-work.md uses questionTemplate() for inline questions
    - Verification flow preserved (test presentation, response processing, gap handling)
  artifacts:
    - Question templates added to src/lib/questions.js
    - verify-work.md updated with questionTemplate() calls
  key_links:
    - questionTemplate() in prompts.js calls getQuestionTemplate() in questions.js
    - Graceful fallback generates options at runtime if template missing
---

<objective>
Migrate verify-work.md to use questionTemplate() for inline questions while preserving verification flow.
</objective>

<execution_context>
@/Users/cam/DEV/bgsd-oc/workflows/verify-work.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
@/Users/cam/DEV/bgsd-oc/src/lib/prompts.js
</execution_context>

<tasks>

<task type="auto">
  <name>Identify inline questions in verify-work.md</name>
  <files>workflows/verify-work.md</files>
  <action>
Search verify-work.md for inline question patterns:

Step check_active_session: Resume / Start new / Which session to resume options

Step present_test: Pass/Fail response handling (yes/y/pass/next vs issue)

Step complete_session: Issues found → diagnose_issues vs suggest next phase

Step diagnose_issues: Spawn debug agents vs manual handling

For each question, document: template ID, question text, options, multiSelect flag.
  </action>
  <verify>All inline questions in verify-work.md identified</verify>
  <done>List of {templateId, questionText, options} for each question</done>
</task>

<task type="auto">
  <name>Add question templates to questions.js</name>
  <files>src/lib/questions.js</files>
  <action>
Add templates to OPTION_TEMPLATES:

1. 'verify-session-resume': Resume existing / Start new session (SINGLE_CHOICE)
2. 'verify-test-response': Pass / Fail / Skip (SINGLE_CHOICE) - for test result processing
3. 'verify-complete-issues': Diagnose issues / Suggest next phase (SINGLE_CHOICE)
4. 'verify-diagnose': Spawn debug / Manual handling (SINGLE_CHOICE)

Follow OPTION_RULES: MIN 3, MAX 5 options, diversity, escape hatch at last position.
  </action>
  <verify>grep -c "verify-" src/lib/questions.js returns 4+</verify>
  <done>4 question templates added to OPTION_TEMPLATES</done>
</task>

<task type="auto">
  <name>Update verify-work.md to use questionTemplate() calls</name>
  <files>workflows/verify-work.md</files>
  <action>
Replace inline question structures with questionTemplate() calls:

1. check_active_session: Replace session options with questionTemplate('verify-session-resume', 'SINGLE_CHOICE')

2. present_test: Response processing uses questionTemplate('verify-test-response', 'SINGLE_CHOICE')

3. complete_session: Issues handling uses questionTemplate('verify-complete-issues', 'SINGLE_CHOICE')

4. diagnose_issues: Debug spawn options use questionTemplate('verify-diagnose', 'SINGLE_CHOICE')

Preserve question text, headers, and multiSelect flags. Only options come from templates.
  </action>
  <verify>grep "questionTemplate" workflows/verify-work.md | wc -l returns 4+</verify>
  <done>verify-work.md uses questionTemplate() for all questions</done>
</task>

</tasks>

<verification>
- Verify: grep -c "questionTemplate" workflows/verify-work.md returns 4+
- Verify: Verification flow logic intact (UAT creation, test presentation, gap handling)
</verification>

<success_criteria>
- [ ] All question templates added to questions.js OPTION_TEMPLATES
- [ ] verify-work.md has questionTemplate() calls for inline questions
- [ ] Verification flow preserved
- [ ] Test result processing (pass/fail/skip) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/142-primary-workflow-migration/142-05-SUMMARY.md`
</output>
