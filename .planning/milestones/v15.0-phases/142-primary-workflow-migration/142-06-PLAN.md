---
phase: 142-primary-workflow-migration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/execute-phase.md
  - src/lib/questions.js
autonomous: true
requirements:
  - MIGRATE-06
must_haves:
  truths:
    - execute-phase.md checkpoint human-verify step uses questionTemplate()
    - Phase execution and wave-based parallel execution unchanged
  artifacts:
    - Question templates added to src/lib/questions.js
    - execute-phase.md updated with questionTemplate() calls for checkpoints
  key_links:
    - questionTemplate() in prompts.js calls getQuestionTemplate() in questions.js
    - Graceful fallback generates options at runtime if template missing
---

<objective>
Migrate execute-phase.md checkpoint human-verify step to use questionTemplate() while preserving wave-based execution.
</objective>

<execution_context>
@/Users/cam/DEV/bgsd-oc/workflows/execute-phase.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
@/Users/cam/DEV/bgsd-oc/src/lib/prompts.js
</execution_context>

<tasks>

<task type="auto">
  <name>Identify checkpoint human-verify questions in execute-phase.md</name>
  <files>workflows/execute-phase.md</files>
  <action>
Search execute-phase.md for checkpoint human-verify step:

checkpoint_handling section (line ~194-212):
- human-verify checkpoint type: presents verification items to user
- Auto-mode: auto-approves with log
- Standard flow: spawn agent → checkpoint → user responds → continuation

The checkpoint handling doesn't use inline question() calls - it presents verification items and awaits user response. Check if any question() calls exist in this section or if the workflow references questions that need templates.

Also check for any other inline questions in execute-phase.md that affect checkpoint flow.
  </action>
  <verify>All checkpoint-related questions in execute-phase.md identified</verify>
  <done>List of {templateId, questionText, options} for checkpoint questions</done>
</task>

<task type="auto">
  <name>Add question templates for checkpoint handling</name>
  <files>src/lib/questions.js</files>
  <action>
Add templates to OPTION_TEMPLATES for checkpoint human-verify:

1. 'execute-checkpoint-verify': Pass / Fail / Needs adjustment (SINGLE_CHOICE)
2. 'execute-checkpoint-retry': Retry / Continue / Skip (SINGLE_CHOICE)
3. 'execute-wave-continue': Proceed to next wave / Review current / Pause (SINGLE_CHOICE)

Follow OPTION_RULES: MIN 3, MAX 5 options, diversity, escape hatch at last position.
  </action>
  <verify>grep -c "execute-checkpoint-" src/lib/questions.js returns 3+</verify>
  <done>3 question templates added to OPTION_TEMPLATES</done>
</task>

<task type="auto">
  <name>Update execute-phase.md checkpoint handling</name>
  <files>workflows/execute-phase.md</files>
  <action>
Replace inline question structures in checkpoint_handling section with questionTemplate() calls:

1. human-verify checkpoint: Replace result options with questionTemplate('execute-checkpoint-verify', 'SINGLE_CHOICE')

2. Spot-check failures: Replace retry options with questionTemplate('execute-checkpoint-retry', 'SINGLE_CHOICE')

3. Wave completion: Replace wave continuation options with questionTemplate('execute-wave-continue', 'SINGLE_CHOICE')

Preserve checkpoint presentation logic. Only options come from templates.
  </action>
  <verify>grep "questionTemplate" workflows/execute-phase.md | wc -l returns 3+</verify>
  <done>execute-phase.md uses questionTemplate() for checkpoint questions</done>
</task>

</tasks>

<verification>
- Verify: grep -c "questionTemplate" workflows/execute-phase.md returns 3+
- Verify: Wave-based execution logic intact
- Verify: Checkpoint handling preserves autonomous: false plans
</verification>

<success_criteria>
- [ ] All checkpoint question templates added to questions.js OPTION_TEMPLATES
- [ ] execute-phase.md has questionTemplate() calls for checkpoint human-verify
- [ ] Wave-based parallel execution unchanged
- [ ] Checkpoint flow (auto-approve, standard, continuation) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/142-primary-workflow-migration/142-06-SUMMARY.md`
</output>
