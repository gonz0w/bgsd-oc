---
phase: 0112-workflow-integration-measurement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/command-enricher.js
  - tests/enricher-decisions.test.cjs
  - bin/bgsd-tools.cjs
autonomous: true
requirements: [FLOW-01]

must_haves:
  truths:
    - "Enrichment object includes plan_count, summary_count, and uat_gap_count derived from existing file data"
    - "Enrichment object includes has_research and has_context booleans for the active phase"
    - "Enrichment object includes task_types array extracted from first incomplete plan"
    - "Decision rules fire with correct results when all enrichment inputs are supplied"
    - "Existing enrichment behavior is unchanged — new fields are purely additive"
  artifacts:
    - path: "src/plugin/command-enricher.js"
      provides: "Extended enrichment inputs for decision rules"
    - path: "tests/enricher-decisions.test.cjs"
      provides: "Contract tests for enrichment expansion"
    - path: "bin/bgsd-tools.cjs"
      provides: "Rebuilt bundle with enrichment changes"
  key_links:
    - from: "src/plugin/command-enricher.js"
      to: "src/lib/decision-rules.js"
      via: "evaluateDecisions() receives enrichment with new fields"
      pattern: "evaluateDecisions"
---

<objective>
Expand the command enricher to supply all missing inputs that decision rules need to fire accurately. Currently the enricher provides paths and config flags, but decision rules like `progress-route`, `execution-pattern`, and `plan-existence-route` need counts, booleans, and task types that the enricher already has access to but doesn't yet expose.

Purpose: Decision rules exist (Phase 111) but fire on incomplete data — this plan completes the data pipeline so rules produce correct results.
Output: Extended `command-enricher.js` with new enrichment fields, contract tests, rebuilt bundle.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0112-workflow-integration-measurement/0112-RESEARCH.md
@src/plugin/command-enricher.js
@src/lib/decision-rules.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend command-enricher.js with missing decision rule inputs</name>
  <files>src/plugin/command-enricher.js</files>
  <action>
Add enrichment fields that decision rules need but the enricher doesn't yet supply. All data is already available or trivially derivable from existing enrichment logic — no new I/O patterns.

**After plan enumeration (where `enrichment.plans` is set):**
1. Add `enrichment.plan_count = enrichment.plans ? enrichment.plans.length : 0`
2. Count summary files (already computed as `summaryFiles`): `enrichment.summary_count = summaryFiles.length`
3. Count UAT gap files: scan phaseDir for `*-UAT.md` files, count those whose content contains `status: diagnosed`. If none or scan fails, set `enrichment.uat_gap_count = 0`

**After phase dir resolution:**
4. Add `enrichment.has_research = existsSync(join(resolvedCwd, phaseDir, paddedPhase + '-RESEARCH.md'))` where paddedPhase is the 4-digit padded phase number
5. Add `enrichment.has_context = existsSync(join(resolvedCwd, phaseDir, paddedPhase + '-CONTEXT.md'))`

**For execution-pattern rule:**
6. Extract task types from the first incomplete plan. If `enrichment.incomplete_plans` has entries, re-parse the first plan with `parsePlans()` and extract `task_types` from its tasks: `plan.tasks.map(t => t.type).filter(Boolean)`. Set `enrichment.task_types = [...]`

**For state-assessment rules:**
7. Add `enrichment.state_exists = existsSync(join(resolvedCwd, '.planning/STATE.md'))`
8. Add `enrichment.project_exists = existsSync(join(resolvedCwd, '.planning/PROJECT.md'))`
9. Add `enrichment.roadmap_exists = existsSync(join(resolvedCwd, '.planning/ROADMAP.md'))`
10. Add `enrichment.current_phase = phaseNum` (already available as the detected phase number)
11. Derive `enrichment.highest_phase` from the roadmap object if available (scan roadmap phases for max number), else null

**For previous-check-gate rule:**
12. If summary files exist for the current phase, check the last one for `unresolved` or `blockers` text — set `enrichment.has_previous_summary`, `enrichment.has_unresolved_issues`, `enrichment.has_blockers` as booleans. Use simple `readFileSync` + string `.includes()` check — no heavy parsing.

**For CI gate rule:**
13. Add `enrichment.ci_enabled = config ? Boolean(config.ci) : false`
14. Add `enrichment.has_test_command = Boolean(config && config.test_command)`

All new fields are purely additive — they don't modify any existing enrichment values. Wrap each derivation in try/catch so a failure in one field doesn't break enrichment.

Use `readFileSync` from the existing `fs` import for any file content checks. Use `String(phaseNum).padStart(4, '0')` for padded phase numbers.
  </action>
  <verify>
`npm run build` succeeds. Run `node bin/bgsd-tools.cjs decisions:evaluate --state '{"plan_count":3,"summary_count":1,"state_exists":true,"roadmap_exists":true,"project_exists":true}'` and verify `progress-route` returns a non-null value.
  </verify>
  <done>
Enrichment object includes plan_count, summary_count, uat_gap_count, has_research, has_context, task_types, state_exists, project_exists, roadmap_exists, current_phase, highest_phase, has_previous_summary, ci_enabled, has_test_command. All fields are additive. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add contract tests for enrichment expansion and rebuild</name>
  <files>tests/enricher-decisions.test.cjs, bin/bgsd-tools.cjs</files>
  <action>
Create `tests/enricher-decisions.test.cjs` with tests covering the enrichment expansion:

1. **Import test:** Import `enrichCommand` from the built bundle or source module. If ESM import is complex, test via CLI integration — run `decisions:evaluate` with mock state and verify output.

2. **Enrichment field presence tests:** Create a mock project structure (use `mkdtempSync`), populate with `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, a phase dir with PLAN.md and SUMMARY.md files, then call enrichment and verify:
   - `plan_count` equals number of PLAN files
   - `summary_count` equals number of SUMMARY files
   - `has_research` is true/false based on RESEARCH.md existence
   - `has_context` is true/false based on CONTEXT.md existence
   - `state_exists`, `project_exists`, `roadmap_exists` are all true
   - `task_types` is an array when plans have typed tasks

3. **Decision rule integration tests:** Verify that with the new enrichment fields, key decision rules produce non-null values:
   - `progress-route` returns a route letter with plan_count/summary_count/state_exists/roadmap_exists
   - `execution-pattern` returns A/B/C with task_types
   - `auto-advance` returns boolean with auto_advance_config
   - `ci-gate` returns run/skip/warn with ci_enabled

4. **Backward compatibility test:** Verify enrichment with zero new fields still works — old behavior unchanged.

Use the existing test patterns from `tests/decisions.test.cjs` for consistency (node:test, describe/it blocks, assert).

After tests pass, rebuild: `npm run build`.

Verify full test suite: `npm test`.
  </action>
  <verify>
`npm test` passes with zero regressions. New test file has at least 10 assertions covering enrichment fields and decision rule integration.
  </verify>
  <done>
Contract tests validate all new enrichment fields. Decision rules fire correctly with expanded inputs. Full test suite passes. Bundle rebuilt.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `npm test` passes — zero regressions from baseline
3. `node bin/bgsd-tools.cjs decisions:list` still works (no regression)
4. `node bin/bgsd-tools.cjs decisions:evaluate --state '{"plan_count":2,"summary_count":0,"state_exists":true,"roadmap_exists":true}'` returns `progress-route` with value `A` (more plans than summaries)
</verification>

<success_criteria>
- Enrichment object includes all fields needed by the 12 registered decision rules
- Decision rules produce correct results with the expanded enrichment data
- No regressions in existing tests or enrichment behavior
- Contract tests cover enrichment expansion and decision rule integration
</success_criteria>

<output>
After completion, create `.planning/phases/0112-workflow-integration-measurement/0112-01-SUMMARY.md`
</output>
