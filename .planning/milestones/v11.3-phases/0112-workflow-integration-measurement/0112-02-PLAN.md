---
phase: 0112-workflow-integration-measurement
plan: 02
type: execute
wave: 2
depends_on: [0112-01]
files_modified:
  - workflows/progress.md
  - workflows/execute-plan.md
  - workflows/execute-phase.md
  - workflows/resume-project.md
  - workflows/discuss-phase.md
  - workflows/plan-phase.md
  - workflows/transition.md
  - workflows/debug.md
  - workflows/audit-milestone.md
  - src/commands/decisions.js
  - bin/bgsd-tools.cjs
autonomous: true
requirements: [FLOW-02, FLOW-03]

must_haves:
  truths:
    - "13 workflows consume pre-computed decisions from bgsd-context instead of re-deriving via LLM reasoning"
    - "Every modified workflow retains its original derivation logic as a fallback when decisions field is absent"
    - "CLI decisions:savings command reports before/after LLM call counts per workflow"
    - "2 manual model resolution subprocess calls replaced with enricher-provided values"
  artifacts:
    - path: "workflows/progress.md"
      provides: "Consumes decisions.progress-route"
    - path: "workflows/execute-plan.md"
      provides: "Consumes decisions.execution-pattern, context-budget-gate, previous-check-gate"
    - path: "workflows/execute-phase.md"
      provides: "Consumes decisions.branch-handling, ci-gate"
    - path: "workflows/resume-project.md"
      provides: "Consumes decisions.resume-route"
    - path: "workflows/debug.md"
      provides: "Consumes decisions.debug-handler-route, uses enricher model value"
    - path: "workflows/audit-milestone.md"
      provides: "Uses enricher model value instead of subprocess"
    - path: "src/commands/decisions.js"
      provides: "decisions:savings subcommand with before/after LLM call counts"
  key_links:
    - from: "workflows/*.md"
      to: "src/plugin/command-enricher.js"
      via: "bgsd-context JSON injection with decisions field"
      pattern: "decisions\\."
    - from: "src/commands/decisions.js"
      to: "workflows/*.md"
      via: "Static analysis of workflow files for LLM step counting"
      pattern: "decisions:savings"
---

<objective>
Modify 13 workflows to consume pre-computed decisions from `<bgsd-context>` instead of re-deriving them via LLM reasoning. Replace 2 manual model resolution subprocess calls. Add a `decisions:savings` CLI subcommand that reports before/after LLM call counts.

Purpose: This is the payoff of Phases 110-111 — workflows actually use the decision engine, saving ~27 LLM reasoning steps per session.
Output: 13 simplified workflow files, measurement report CLI command, rebuilt bundle.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0112-workflow-integration-measurement/0112-RESEARCH.md
@.planning/phases/0112-workflow-integration-measurement/0112-01-SUMMARY.md
@src/lib/decision-rules.js
@src/commands/decisions.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add decision consumption to 13 workflows</name>
  <files>
    workflows/progress.md
    workflows/execute-plan.md
    workflows/execute-phase.md
    workflows/resume-project.md
    workflows/discuss-phase.md
    workflows/plan-phase.md
    workflows/transition.md
    workflows/debug.md
    workflows/audit-milestone.md
  </files>
  <action>
For each workflow below, add a decision consumption block at the TOP of the relevant step, BEFORE the existing derivation logic. Keep all existing logic as fallback. Follow this exact pattern:

```markdown
**Pre-computed decision:** If `decisions.{rule-id}` exists in `<bgsd-context>`, use its `.value` directly. Skip to [next step / corresponding route section].

**Fallback** (if decisions not available):
[...existing logic unchanged...]
```

**Specific modifications per workflow:**

### 1. progress.md (RESEARCH item #1)
At the top of the `<step name="route">` section (around line 104), add:
"If `decisions.progress-route` exists in `<bgsd-context>`, use its `.value` as the route letter (A/B/C/D/E/F or no-project/no-state). Skip file counting and routing table evaluation."
Keep the entire routing table below as fallback.

### 2. execute-plan.md — execution pattern (RESEARCH item #2)
At the step that selects Pattern A/B/C (around line 44-57), add:
"If `decisions.execution-pattern` exists in `<bgsd-context>`, use its `.value` as the pattern letter (A/B/C). Skip checkpoint type scanning."
Keep existing grep + classification as fallback.

### 3. execute-plan.md — context budget gate (RESEARCH item #3)
At the context budget check (around line 36-41), add:
"If `decisions.context-budget-gate` exists in `<bgsd-context>`, use its `.value` (proceed/warn/stop) to determine budget action. Skip CLI budget command interpretation."
Keep existing `verify:context-budget` call as fallback.

### 4. execute-plan.md — previous summary check (RESEARCH item #4)
At the previous summary check (around line 81), add:
"If `decisions.previous-check-gate` exists in `<bgsd-context>`, use its `.value` (proceed/warn/block). Skip previous SUMMARY analysis."
Keep existing logic as fallback.

### 5. execute-phase.md — branch handling (RESEARCH item #5)
At the branch handling step (around line 57-63), add:
"If `decisions.branch-handling` exists in `<bgsd-context>`, use its `.value` (skip/create/update/use-existing). Skip branch state evaluation."
Keep existing branching logic as fallback.

### 6. execute-phase.md — CI gate (RESEARCH item #6)
At the CI gate step (around line 389-438), add:
"If `decisions.ci-gate` exists in `<bgsd-context>`, use its `.value` (run/skip/warn). Skip CI flag evaluation."
Keep existing CI configuration check as fallback.

### 7. resume-project.md — next action (RESEARCH item #7)
At the routing step (around line 116-148), add:
"If `decisions.resume-route` exists in `<bgsd-context>`, use its `.value` as the primary next action. Skip condition evaluation."
Keep existing 6-condition evaluation as fallback.

### 8. discuss-phase.md — auto-advance (RESEARCH item #8)
At the auto-advance check (around line 404-450), add:
"If `decisions.auto-advance` exists in `<bgsd-context>`, use its `.value` (boolean). Skip config/flag check."
Keep existing `--auto` flag + config check as fallback.

### 9. plan-phase.md — auto-advance (RESEARCH item #9)
At the auto-advance check (around line 147-153), add same pattern as discuss-phase.md #8.

### 10. transition.md — auto-advance + branch (RESEARCH item #10)
At the auto-advance section (around line 306-467), add consumption of `decisions.auto-advance`.
At the branch handling section (around line 424-426), add consumption of `decisions.branch-handling`.
Keep existing logic as fallback for both.

### 11. debug.md — return handler route (RESEARCH item #11)
At the return handler section (around line 79-99), add:
"If `decisions.debug-handler-route` exists in `<bgsd-context>`, use its `.value` as a suggestion (MEDIUM confidence — LLM may override). Skip agent return type interpretation."
Note the MEDIUM confidence — this is a suggestion, not authoritative.

### 12. debug.md — model resolution (RESEARCH item #15)
Replace the `DEBUGGER_MODEL=$(node .../bgsd-tools.cjs util:resolve-model bgsd-debugger --raw)` subprocess call (around line 17) with:
"Use `executor_model` from `<bgsd-context>` if available. Fallback: run `util:resolve-model` as before."

### 13. audit-milestone.md — model resolution (RESEARCH item #16)
Replace the `CHECKER_MODEL=$(node .../bgsd-tools.cjs util:resolve-model bgsd-verifier)` subprocess call (around line 21) with:
"Use `verifier_model` or `checker_model` from `<bgsd-context>` if available. Fallback: run `util:resolve-model` as before."

**Critical rules:**
- NEVER remove existing derivation logic — keep it as labeled "Fallback"
- Add decision consumption BEFORE existing logic, not after
- Use "If decisions.X exists" conditional — never assume it's present
- For MEDIUM confidence rules (debug-handler-route), note that LLM may override
  </action>
  <verify>
Read each modified workflow and confirm: (1) decision consumption added at correct location, (2) original logic preserved as fallback, (3) no structural changes to workflow beyond the additions.
  </verify>
  <done>
All 13 workflows modified with decision consumption blocks. Every workflow retains its original derivation logic as fallback. 2 model resolution subprocess calls replaced with enricher values. No workflows rewritten — moderate additions only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add decisions:savings CLI subcommand and rebuild</name>
  <files>src/commands/decisions.js, src/router.js, src/lib/constants.js, bin/bgsd-tools.cjs</files>
  <action>
Add a `decisions:savings` subcommand to the existing `decisions` CLI namespace that reports before/after LLM call counts per workflow.

**In `src/commands/decisions.js`:**

1. Add a `handleSavings(args, flags)` function that produces a static report:
   - Define a hardcoded table of before/after counts per workflow (from RESEARCH.md estimates):
     ```javascript
     const SAVINGS_DATA = [
       { workflow: 'progress.md', before: 7, after: 1, decisions: ['progress-route'] },
       { workflow: 'execute-plan.md', before: 5, after: 1, decisions: ['execution-pattern', 'context-budget-gate', 'previous-check-gate'] },
       { workflow: 'execute-phase.md', before: 4, after: 1, decisions: ['branch-handling', 'ci-gate'] },
       { workflow: 'resume-project.md', before: 5, after: 1, decisions: ['resume-route'] },
       { workflow: 'discuss-phase.md', before: 2, after: 0, decisions: ['auto-advance'] },
       { workflow: 'plan-phase.md', before: 2, after: 0, decisions: ['auto-advance'] },
       { workflow: 'transition.md', before: 3, after: 1, decisions: ['auto-advance', 'branch-handling'] },
       { workflow: 'debug.md', before: 3, after: 1, decisions: ['debug-handler-route', 'model-resolution'] },
       { workflow: 'audit-milestone.md', before: 2, after: 0, decisions: ['model-resolution'] },
     ];
     ```
   - Compute totals: sum of before, sum of after, difference, percentage reduction
   - Output via `output()` with `formatter` for TTY:
     - Table with columns: Workflow | Before | After | Saved | Decisions Used
     - Summary line: "Total: {before} → {after} LLM reasoning steps ({saved} saved, {pct}% reduction)"
     - Note: "Counts are per-invocation. High-traffic workflows multiply savings."

2. Export `handleSavings` alongside existing `handleList`, `handleInspect`, `handleEvaluate`.

**In `src/router.js`:**
3. Add `case 'savings':` to the decisions namespace case block, calling `handleSavings(args, flags)`.

**In `src/lib/constants.js`:**
4. Add COMMAND_HELP entry for `decisions:savings`:
   - usage: `decisions:savings`
   - description: `Show before/after LLM reasoning step counts per workflow`
   - example: `decisions:savings`
   - output: `Table of per-workflow LLM step savings from decision offloading`

**In `src/lib/commandDiscovery.js`:**
5. Add `decisions:savings` to the analysis category and aliases (`d:s`).

After all changes: `npm run build` to rebuild the bundle.
  </action>
  <verify>
`npm run build` succeeds. `node bin/bgsd-tools.cjs decisions:savings` outputs a table with before/after counts for 9 workflows and a summary total. `npm test` passes with zero regressions.
  </verify>
  <done>
`decisions:savings` CLI command produces a per-workflow report showing ~33 before vs ~6 after LLM reasoning steps (~27 saved, ~82% reduction). Full test suite passes. Bundle rebuilt.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `npm test` passes — zero regressions
3. Read 3 high-traffic workflows (progress.md, execute-plan.md, execute-phase.md) and confirm decision consumption blocks present with fallback logic preserved
4. `node bin/bgsd-tools.cjs decisions:savings` outputs a table with per-workflow before/after counts and total savings
5. `node bin/bgsd-tools.cjs decisions:list` still works (no regression)
</verification>

<success_criteria>
- 13 workflows modified to consume decisions from bgsd-context
- Every workflow retains fallback derivation logic
- 2 model resolution subprocess calls replaced
- decisions:savings CLI command reports ~27 LLM steps saved per session
- Zero regressions in build and test suite
</success_criteria>

<output>
After completion, create `.planning/phases/0112-workflow-integration-measurement/0112-02-SUMMARY.md`
</output>
