---
phase: 0113-programmatic-summary-generation
plan: 02
type: execute
wave: 2
depends_on: [0113-01]
files_modified:
  - workflows/execute-plan.md
  - .planning/REQUIREMENTS.md
  - bin/bgsd-tools.cjs
autonomous: true
requirements: [SUM-02]

must_haves:
  truths:
    - "The execute-plan workflow calls summary:generate after recording completion time and before the LLM writes summary content"
    - "The workflow explicitly lists remaining TODO sections for the LLM to fill — the LLM does not need to discover them"
    - "On summary:generate failure, the workflow falls back to the current full-authorship approach with a warning"
    - "The LLM fills only judgment sections (one-liner, accomplishments, decisions, deviations, lessons/issues/next-phase) — at least 50% less writing than full authorship"
    - "SUM-01, SUM-02, SUM-03 requirements exist in REQUIREMENTS.md with traceability to Phase 113"
  artifacts:
    - path: "workflows/execute-plan.md"
      provides: "Modified create_summary step calling summary:generate before LLM fills judgment sections"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SUM-01, SUM-02, SUM-03 requirement definitions with traceability"
  key_links:
    - from: "workflows/execute-plan.md"
      to: "bin/bgsd-tools.cjs"
      via: "CLI call to util:summary-generate in create_summary step"
      pattern: "summary-generate"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "SUM-01/02/03 mapped to Phase 113"
      pattern: "SUM-0[123]"
---

<objective>
Integrate the `summary:generate` command into the execute-plan workflow so the LLM receives a pre-built scaffold and only fills judgment sections, and add the SUM-01/02/03 requirements to REQUIREMENTS.md for traceability.

Purpose: This completes the LLM offloading loop — the command from Plan 01 generates the scaffold, and this plan wires it into the actual workflow the LLM follows during execution. Also ensures requirement traceability is complete.
Output: Modified `execute-plan.md` workflow, updated `REQUIREMENTS.md`.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0113-programmatic-summary-generation/0113-01-SUMMARY.md
@.planning/phases/0113-programmatic-summary-generation/0113-CONTEXT.md
@workflows/execute-plan.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate summary:generate into execute-plan workflow</name>
  <files>workflows/execute-plan.md</files>
  <action>
Modify the `<step name="create_summary">` in `workflows/execute-plan.md` (currently at line 234) to call `summary:generate` before the LLM fills in judgment sections. The modified step should:

1. **Call summary:generate first:**
```bash
SCAFFOLD=$(node __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs util:summary-generate ${PHASE_NUM} ${PLAN_NUM} --raw 2>/dev/null)
```

2. **Check for success:** Parse the JSON output. If `error` field exists or the command failed (non-zero exit), log a warning and fall back to the current full-authorship approach (the existing step content).

3. **If scaffold succeeded:** The SUMMARY.md file is already written to disk with all data sections populated. The LLM's job is now limited to filling the remaining TODO sections. **Explicitly list the TODO sections to fill** (per CONTEXT.md constraint — do NOT ask the LLM to "find the TODOs"):

   Remaining sections to fill in the generated SUMMARY.md:
   - **one-liner** in frontmatter `one-liner:` field AND the bold line under the heading
   - **Accomplishments** section (2-3 bullets describing what changed and why)
   - **Decisions Made** section (key decisions with rationale, or "None - followed plan as specified")
   - **Deviations from Plan** section (describe deviations, or "None - plan executed exactly as written")
   - **Lessons Learned** section (what would you do differently, or "None")
   - **Issues Encountered** section (problems and how resolved, or "None")
   - **Next Phase Readiness** section (what's ready for next phase)
   - **key-decisions** in frontmatter (list of decision strings)
   - **patterns-established** in frontmatter (if any new patterns)

4. **Read the generated file**, fill each TODO section with substantive content based on execution experience, then write back.

5. **Preserve the existing instructions** about frontmatter population, one-liner rules, and review findings inclusion as fallback guidance (in case scaffold generation fails).

The key restructuring: the old step said "Create SUMMARY.md using templates/summary.md" — now it says "Generate scaffold via CLI, then fill remaining judgment sections." The template reference remains for fallback.

Also, include a brief note in the step about the `todos_remaining` count from the scaffold output — the LLM can use this to verify it filled all sections.
  </action>
  <verify>
Read the modified `workflows/execute-plan.md` and verify:
1. The `create_summary` step calls `summary:generate` before LLM writing
2. There is a fallback path for when scaffold generation fails
3. The TODO sections are explicitly listed (not "find the TODOs")
4. The step preserves review findings inclusion guidance
5. The workflow file is valid markdown with no syntax errors
  </verify>
  <done>
The execute-plan workflow's `create_summary` step calls `summary:generate` first, explicitly lists remaining TODO sections for the LLM to fill, and falls back to full authorship on failure. The LLM writes ~5-7 judgment sections instead of the entire ~100-line SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SUM-01/02/03 requirements to REQUIREMENTS.md and rebuild</name>
  <files>.planning/REQUIREMENTS.md, bin/bgsd-tools.cjs</files>
  <action>
1. **Add requirements to REQUIREMENTS.md:** Add a new section `### Summary Generation` under the `## v11.3 Requirements` heading (after the Workflow Integration section), with three requirements:

```markdown
### Summary Generation

- [ ] **SUM-01**: `summary:generate` CLI command produces a pre-filled SUMMARY.md with frontmatter, performance, task commits, and files created/modified — all without LLM involvement
- [ ] **SUM-02**: Execute-plan workflow calls `summary:generate` and LLM only fills judgment sections — at least 50% less LLM writing per summary
- [ ] **SUM-03**: Generated summaries pass `verify:summary` validation and `summary-extract` parsing without regressions
```

2. **Update traceability table:** Add three rows to the traceability table:

```markdown
| SUM-01 | Phase 113 | Not Started | npm test |
| SUM-02 | Phase 113 | Not Started | Manual verification |
| SUM-03 | Phase 113 | Not Started | npm test |
```

3. **Update coverage count:** Change the total from "10 total" to "13 total" and "Mapped to phases: 10" to "Mapped to phases: 13".

4. **Update ROADMAP.md coverage map:** Add a row for Phase 113:
```markdown
| 113 | SUM-01, SUM-02, SUM-03 | 3 | In Progress | - |
```

5. **Rebuild bundle:** Run `npm run build` to ensure the bundle reflects any changes.
  </action>
  <verify>
1. Verify REQUIREMENTS.md has SUM-01, SUM-02, SUM-03 definitions
2. Verify traceability table maps all three to Phase 113
3. Verify coverage counts are updated to 13
4. Verify ROADMAP.md coverage map includes Phase 113
5. Run `npm run build` — clean build
6. Run `npm test` — zero regressions
  </verify>
  <done>
REQUIREMENTS.md contains SUM-01/02/03 definitions with traceability to Phase 113. ROADMAP.md coverage map updated. Bundle rebuilt. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. Read `workflows/execute-plan.md` — the `create_summary` step calls `summary:generate` before LLM writing
2. Read `.planning/REQUIREMENTS.md` — SUM-01, SUM-02, SUM-03 are defined with traceability
3. Read `.planning/ROADMAP.md` — coverage map includes Phase 113
4. Run `npm test` — all tests pass
5. Manually verify that the workflow's TODO-section listing covers all 5+ judgment sections from CONTEXT.md
</verification>

<success_criteria>
- Execute-plan workflow calls `summary:generate` and LLM only fills judgment sections (SUM-02)
- Workflow explicitly lists TODO sections — LLM doesn't have to discover them
- Graceful fallback to full authorship on generation failure
- SUM-01/02/03 requirements exist in REQUIREMENTS.md with traceability to Phase 113
- Zero regressions in test suite
</success_criteria>

<output>
After completion, create `.planning/phases/0113-programmatic-summary-generation/0113-02-SUMMARY.md`
</output>
