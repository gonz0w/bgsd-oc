---
phase: 177-runtime-guidance-integrity-cleanup
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/plugin/advisory-guardrails.js
  - plugin.js
  - tests/plugin-advisory-guardrails.test.cjs
  - tests/validate-commands.test.cjs
autonomous: true
requirements:
  - SAFE-03
intent:
  outcome_ids:
    - DO-122
    - DO-123
  rationale: "This slice keeps the canonical greenfield command surface runnable in shipped runtime guidance while reusing the narrower command-contract cleanup already established in the milestone."
must_haves:
  truths:
    - "Runtime roadmap follow-up guidance surfaces operand-complete canonical `/bgsd-plan roadmap ...` syntax instead of bare commands that fail immediately."
    - "The generated `plugin.js` bundle carries the same corrected roadmap guidance as `src/plugin/advisory-guardrails.js`, so shipped runtime output cannot drift behind source edits."
    - "Focused regressions fail if plugin roadmap guidance drops required operands or if validator coverage stops protecting the runtime roadmap gap."
  artifacts:
    - path: "src/plugin/advisory-guardrails.js"
      provides: "Plugin roadmap advisory source strings"
      contains: "/bgsd-plan roadmap add \"<description>\""
    - path: "plugin.js"
      provides: "Rebuilt shipped plugin bundle with roadmap guidance parity"
      contains: "/bgsd-plan roadmap insert <after> \"<description>\""
    - path: "tests/plugin-advisory-guardrails.test.cjs"
      provides: "Runtime guidance regression coverage for full roadmap command shapes"
      contains: "roadmap add|roadmap remove|roadmap insert"
    - path: "tests/validate-commands.test.cjs"
      provides: "Validator regression coverage for the runtime roadmap guidance gap"
      contains: "missing-argument|plugin.js|roadmap"
  key_links:
    - from: "src/plugin/advisory-guardrails.js"
      to: "plugin.js"
      via: "npm run build refreshes the shipped plugin bundle from the updated advisory source"
      pattern: "roadmap add \"<description>\"|roadmap remove <phase-number>|roadmap insert <after> \"<description>\""
    - from: "tests/plugin-advisory-guardrails.test.cjs"
      to: "src/plugin/advisory-guardrails.js"
      via: "runtime guidance assertions lock operand-complete roadmap syntax at the source of truth"
      pattern: "ROADMAP\.md|<phase-number>|<description>"
    - from: "tests/validate-commands.test.cjs"
      to: "plugin.js"
      via: "validator coverage proves the shipped runtime surface no longer reports roadmap missing-argument failures"
      pattern: "plugin\.js|missing-argument|roadmap"
---

<objective>
Close the narrow SAFE-03 runtime guidance gap by making plugin roadmap follow-up commands operand-complete in source, rebuilt in the shipped bundle, and protected by focused regression proof.

Purpose: Phase 175 already established the canonical roadmap command shapes, but the runtime advisory map still emits bare roadmap commands that fail validation and user follow-through. This slice reuses the existing command contract rather than inventing a new guidance system.
Output: corrected roadmap advisory strings in `src/plugin/advisory-guardrails.js`, rebuilt `plugin.js`, and focused regressions that lock runtime and validator parity.
</objective>

> **TDD Decision:** Selected — the runtime roadmap strings are deterministic surfaced behavior with clear expected syntax, so RED tests should fail on the current missing-argument guidance before the source map and rebuilt bundle are updated.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/MILESTONE-INTENT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/177-runtime-guidance-integrity-cleanup/177-RESEARCH.md
@.planning/phases/175-canonical-command-surface-alignment/175-01-PLAN.md
@.planning/phases/175-canonical-command-surface-alignment/175-VERIFICATION.md
@src/plugin/advisory-guardrails.js
@build.cjs
@workflows/add-phase.md
@workflows/insert-phase.md
@workflows/remove-phase.md
@tests/plugin-advisory-guardrails.test.cjs
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for full roadmap operand syntax on the runtime surface</name>
  <files>
    tests/plugin-advisory-guardrails.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add focused failing tests that make the open SAFE-03 gap explicit before changing the plugin source map.

  Cover the runtime source and shipped-surface contract that Phase 177 owns: roadmap add requires a quoted description, roadmap remove requires a phase number, and roadmap insert requires both a position and a quoted description. Keep the validator proof narrow to the runtime/plugin roadmap gap instead of broadening into unrelated docs, agent, or workflow findings.
  </action>
  <verify>node --test tests/plugin-advisory-guardrails.test.cjs tests/validate-commands.test.cjs</verify>
  <done>The focused regressions fail on the current operand-incomplete roadmap guidance in the source map and shipped plugin surface.</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize the roadmap advisory map and rebuild the shipped plugin bundle</name>
  <files>
    src/plugin/advisory-guardrails.js
    plugin.js
    tests/plugin-advisory-guardrails.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <action>
  Update only the `ROADMAP.md` advisory entries in `src/plugin/advisory-guardrails.js` so they use the operand-complete canonical roadmap shapes already established in Phase 175, then rebuild `plugin.js` immediately with `npm run build`.

  Make the tests pass without inventing a second roadmap guidance registry, validator exception, or compatibility alias. Keep scope tight: source-of-truth guidance, rebuilt bundle parity, and the targeted runtime-validator proof for the three missing-argument cases only.
  </action>
  <verify>npm run build</verify>
  <done>The source advisory map, rebuilt `plugin.js`, and focused regressions all agree on operand-complete canonical roadmap guidance.</done>
</task>

</tasks>

<feature>
  <name>Operand-complete runtime roadmap guidance parity</name>
  <files>
    src/plugin/advisory-guardrails.js
    plugin.js
    tests/plugin-advisory-guardrails.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/plugin-advisory-guardrails.test.cjs tests/validate-commands.test.cjs</red>
    <green>node --test tests/plugin-advisory-guardrails.test.cjs tests/validate-commands.test.cjs</green>
    <refactor>node --test tests/plugin-advisory-guardrails.test.cjs tests/validate-commands.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Plugin roadmap follow-up guidance uses the canonical command family with the required operands on both the source and shipped runtime surfaces.

    Cases:
      - `ROADMAP.md` advisory -> `/bgsd-plan roadmap add "<description>"`
      - `ROADMAP.md` advisory -> `/bgsd-plan roadmap remove <phase-number>`
      - `ROADMAP.md` advisory -> `/bgsd-plan roadmap insert <after> "<description>"`
      - rebuilt `plugin.js` -> preserves the same three operand-complete roadmap strings after `npm run build`
      - validator regression -> no longer treats the runtime/plugin roadmap guidance as `missing-argument`
  </behavior>
  <implementation>
    Change the static roadmap command map in `src/plugin/advisory-guardrails.js`, rebuild the shipped bundle, and keep regression coverage focused on source guidance, bundle parity, and the known runtime validator gap instead of unrelated command-integrity backlog.
  </implementation>
</feature>

<verification>
node --test tests/plugin-advisory-guardrails.test.cjs tests/validate-commands.test.cjs
</verification>

<success_criteria>
- [ ] Runtime roadmap follow-up guidance surfaces operand-complete canonical `/bgsd-plan roadmap add|remove|insert ...` syntax
- [ ] `plugin.js` is rebuilt from the updated source and preserves the same roadmap guidance strings
- [ ] Focused runtime and validator regressions fail if the plugin roadmap surface reintroduces missing-argument guidance
</success_criteria>

<output>
After completion, create `.planning/phases/177-runtime-guidance-integrity-cleanup/177-01-SUMMARY.md`
</output>
