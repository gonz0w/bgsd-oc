---
phase: 179-shipped-guidance-surface-integrity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/plugin/idle-validator.js
  - tests/plugin.test.cjs
  - tests/integration.test.cjs
  - tests/validate-commands.test.cjs
  - plugin.js
autonomous: true
gap_closure: true
requirements:
  - SAFE-03
intent:
  outcome_ids: []
  rationale: "Phase-local intent is unavailable, so this gap-closure slice follows the milestone focus: keep shipped runtime guidance runnable exactly as surfaced without widening into the broader Phase 180 validator backlog."
must_haves:
  truths:
    - "Plugin idle-validation guidance surfaces a runnable canonical phase-next action instead of a malformed prose-prefixed string."
    - "The rebuilt `plugin.js` bundle preserves the corrected phase-next action from `src/plugin/idle-validator.js`, so shipped runtime guidance matches source edits."
    - "Focused regressions fail if the idle-validator action reintroduces the malformed prose-prefixed runtime surface or if shipped-runtime validation stops protecting that contract."
  artifacts:
    - path: "src/plugin/idle-validator.js"
      provides: "Idle-validation notification source of truth"
      contains: "action: `"
    - path: "plugin.js"
      provides: "Rebuilt shipped plugin bundle with canonical phase-next guidance"
      contains: "phase-complete"
    - path: "tests/plugin.test.cjs"
      provides: "Plugin runtime regression coverage for phase-complete guidance"
      contains: "doesNotMatch(validatorSource"
    - path: "tests/validate-commands.test.cjs"
      provides: "Validator-backed regression coverage for the shipped plugin guidance surface"
      contains: "plugin.js"
  key_links:
    - from: "src/plugin/idle-validator.js"
      to: "plugin.js"
      via: "npm run build regenerates the shipped plugin bundle from the updated idle-validator source"
      pattern: "action:\\s*`|phase-complete|nextPhase\\.number"
    - from: "tests/plugin.test.cjs"
      to: "src/plugin/idle-validator.js"
      via: "source-backed plugin tests lock the phase-complete action payload to the canonical runnable command"
      pattern: "phase-complete|doesNotMatch\\(validatorSource|nextPhase\\.number"
    - from: "tests/validate-commands.test.cjs"
      to: "plugin.js"
      via: "validator regression proves the shipped runtime surface stays free of the malformed idle-validator command"
      pattern: "plugin\\.js|phase-complete|malformed"
---

<objective>
Close the remaining shipped SAFE-03 runtime guidance gap by making the plugin idle-validator action literal, runnable, and preserved in the rebuilt bundle with focused regression proof.

Purpose: Phase 177 already closed the runtime roadmap operand gap, so Phase 179 should only repair the malformed `Next: /bgsd-plan phase ...` action that still reopens shipped guidance integrity. This keeps the slice narrow and leaves broader surfaced-validator drift to Phase 180.
Output: corrected `src/plugin/idle-validator.js` action text, rebuilt `plugin.js`, and focused tests that lock source, bundle, and validator parity for the phase-next runtime surface.
</objective>

> **TDD Decision:** Selected — the idle-validator action string is deterministic shipped behavior with a clear expected command shape, so RED coverage should fail on the current malformed runtime guidance before the source and rebuilt bundle are corrected.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v18.1-MILESTONE-AUDIT.md
@.planning/phases/179-shipped-guidance-surface-integrity/179-RESEARCH.md
@.planning/phases/177-runtime-guidance-integrity-cleanup/177-01-SUMMARY.md
@src/plugin/idle-validator.js
@build.cjs
@tests/plugin.test.cjs
@tests/integration.test.cjs
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for the malformed phase-next runtime action</name>
  <files>
    tests/plugin.test.cjs
    tests/integration.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add focused failing tests that make the current shipped gap explicit before changing the plugin source.

  Cover only the Phase 179 defect boundary: the idle-validator action payload must be the runnable canonical `/bgsd-plan phase ${nextPhase.number}` command, not prose-prefixed guidance. Update or add the narrowest assertions needed in the existing plugin, integration, and validator suites so they fail on the current malformed `Next:` action without broadening into docs, workflows, agent prompts, or the unrelated validator backlog reserved for Phase 180.
  </action>
  <verify>node --test tests/plugin.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs --test-name-pattern "phase|plugin.js|runtime"</verify>
  <done>The targeted regressions fail on the current `Next: /bgsd-plan phase ...` source or shipped runtime surface.</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize the idle-validator action and rebuild the shipped plugin bundle</name>
  <files>
    src/plugin/idle-validator.js
    plugin.js
    tests/plugin.test.cjs
    tests/integration.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <action>
  Update `src/plugin/idle-validator.js` so the phase-complete notification `action` field contains only the runnable canonical `/bgsd-plan phase ${nextPhase.number}` command, leaving explanatory prose in `message` only.

  Rebuild `plugin.js` with `npm run build`, then make the RED slice pass by aligning the touched tests and validator-backed assertions to the corrected shipped-runtime contract. Do not edit docs, workflows, agents, or validator parser internals unless a direct runtime proof blocker appears.
  </action>
  <verify>npm run build</verify>
  <done>The idle-validator source, rebuilt `plugin.js`, and the touched runtime regressions all agree on the canonical command-only phase-next action.</done>
</task>

</tasks>

<feature>
  <name>Canonical phase-next plugin runtime guidance parity</name>
  <files>
    src/plugin/idle-validator.js
    plugin.js
    tests/plugin.test.cjs
    tests/integration.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/plugin.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs --test-name-pattern "phase|plugin.js|runtime"</red>
    <green>npm run build && node --test tests/plugin.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs --test-name-pattern "phase|plugin.js|runtime"</green>
    <refactor>node --test tests/plugin.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs --test-name-pattern "phase|plugin.js|runtime"</refactor>
  </tdd-targets>
  <behavior>
    Plugin idle-validation guidance surfaces a directly runnable canonical phase-next command on both the source and shipped runtime surfaces.

    Cases:
      - phase-complete notification source keeps the `action` payload command-only instead of prose-prefixed
      - source regression rejects the malformed `Next:`-prefixed action payload
      - rebuilt `plugin.js` preserves the same canonical phase-next action after `npm run build`
      - shipped-runtime validator proof -> no longer reports the malformed idle-validator command surface in `plugin.js`
  </behavior>
  <implementation>
    Change only the phase-complete notification action in `src/plugin/idle-validator.js`, rebuild the shipped plugin bundle, and keep proof focused on the touched plugin runtime and validator-backed assertions that guard this exact shipped guidance surface.
  </implementation>
</feature>

<verification>
npm run build && node --test tests/plugin.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs --test-name-pattern "phase|plugin.js|runtime"
</verification>

<success_criteria>
- [ ] Plugin idle-validation guidance surfaces only the canonical runnable `/bgsd-plan phase <number>` command in its `action` payload
- [ ] `plugin.js` is rebuilt from `src/plugin/idle-validator.js` and preserves the corrected phase-next guidance string
- [ ] Focused plugin, integration, and validator regressions fail if the malformed `Next:` runtime action returns
</success_criteria>

<output>
After completion, create `.planning/phases/179-shipped-guidance-surface-integrity/179-01-SUMMARY.md`
</output>
