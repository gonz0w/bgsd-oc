---
phase: 180-command-validator-drift-resolution
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/commandDiscovery.js
  - tests/validate-commands.test.cjs
autonomous: true
requirements:
  - CLEAN-03
intent:
  outcome_ids: []
  rationale: "No intent outcome IDs are defined for this phase, so this slice traces directly to the milestone-close goal: make util:validate-commands --raw trustworthy for the supported surfaced command family without widening scope beyond Phase 180."
must_haves:
  truths:
    - "`util:validate-commands --raw` no longer reports supported node-invoked commands like `init:execute-phase`, `init:phase-op`, `verify:validate roadmap`, or `detect:gh-preflight` as nonexistent surfaced guidance."
    - "Quoted examples, redirect-bearing shell snippets, internal-bootstrap reconstruction commands, and transition-style output headings are classified intentionally instead of failing as generic unknown commands."
    - "Validator output itself reports the proof inventory and named exclusions it used, so a green result has stable, inspectable meaning."
  artifacts:
    - path: "src/lib/commandDiscovery.js"
      provides: "Validator contract derivation, explicit special-case classifiers, and proof inventory reporting"
      contains: "validateCommandIntegrity"
    - path: "tests/validate-commands.test.cjs"
      provides: "Regression coverage for authoritative CLI inventory, named exclusions, and proof output"
      contains: "validateCommandIntegrity"
  key_links:
    - from: "src/lib/commandDiscovery.js"
      to: "src/router.js"
      via: "CLI inventory derivation recognizes routed command forms the validator must accept"
      pattern: "init:execute-phase|init:phase-op|verify:validate roadmap|detect:gh-preflight"
    - from: "tests/validate-commands.test.cjs"
      to: "src/lib/commandDiscovery.js"
      via: "Focused validator regressions lock the new command contract and named exclusion behavior"
      pattern: "proof inventory|named exclusions|node-invoked"
---

<objective>
Settle the Phase 180 validator contract first by deriving the supported surfaced-command inventory from authoritative routed behavior, classifying named exception cases explicitly, and making the validator report its own proof inventory.

Purpose: The current milestone-close backlog is validator drift, not a docs cleanup project. Phase 180 should first make the validator truthful about supported commands and intentional exceptions before any surfaced files are considered for edits.
Output: updated validator logic in `src/lib/commandDiscovery.js`, focused regressions in `tests/validate-commands.test.cjs`, and a raw validator payload that names the covered proof surface and exclusions it used.
</objective>

> **TDD Decision:** Selected — the validator contract is deterministic behavior with clear pass/fail outcomes, so Phase 180 should lock the false-positive classes in RED tests before changing the inventory, classifier, and proof-output logic.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/180-command-validator-drift-resolution/180-CONTEXT.md
@.planning/phases/180-command-validator-drift-resolution/180-RESEARCH.md
@.planning/phases/175-canonical-command-surface-alignment/175-04-SUMMARY.md
@.planning/phases/177-runtime-guidance-integrity-cleanup/177-01-SUMMARY.md
@.planning/phases/179-shipped-guidance-surface-integrity/179-01-SUMMARY.md
@commands/bgsd-plan.md
@src/lib/commandDiscovery.js
@src/router.js
@tests/validate-commands.test.cjs
@agents/bgsd-executor.md
@agents/bgsd-phase-researcher.md
@agents/bgsd-plan-checker.md
@agents/bgsd-planner.md
@docs/architecture.md
@workflows/execute-phase.md
@workflows/github-ci.md
@workflows/new-milestone.md
@workflows/transition.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for the known validator-drift backlog</name>
  <files>
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add focused failing tests that make the current false-positive classes explicit before changing the validator.

  Cover the exact Phase 180 drift now visible in the raw baseline: node-invoked `bgsd-tools.cjs` examples for `init:execute-phase`, `init:phase-op`, `verify:validate roadmap`, and `detect:gh-preflight`; redirect-bearing shell examples like `2>/dev/null`; transition-style `## ▶ Next Up:` output headings; and the requirement that raw validator output expose validator-owned proof inventory plus named exclusions. Keep the RED slice narrow to the current milestone-close proof surface instead of broadening into unrelated command-linting work.
  </action>
  <verify>node --test tests/validate-commands.test.cjs</verify>
  <done>The focused validator regressions fail on the current inventory drift, generic special-case misclassification, or missing proof-inventory output.</done>
</task>

<task type="auto">
  <name>Task 2: Implement authoritative contract derivation and named classifications</name>
  <files>
    src/lib/commandDiscovery.js
    tests/validate-commands.test.cjs
  </files>
  <action>
  Update `src/lib/commandDiscovery.js` so CLI inventory is derived from authoritative routed behavior instead of `COMMAND_HELP` alone, and so the validator recognizes the known legitimate special cases by explicit named classification.

  Keep the implementation inside the existing validator path: reconcile router-backed command forms, preserve the canonical planning-family grammar already loaded from `commands/bgsd-plan.md`, classify output/reference/bootstrap contexts intentionally, and extend the raw validator payload so it reports its own proof inventory and named exclusions. Do not add a second proof manifest, a general-purpose linter, or broad regex skips.
  </action>
  <verify>npm run build</verify>
  <done>The validator accepts the supported routed command surface, classifies the known non-runnable contexts intentionally, and emits proof-inventory metadata from the same code path used for milestone-close validation.</done>
</task>

</tasks>

<feature>
  <name>Trustworthy milestone-close command validator contract</name>
  <files>
    src/lib/commandDiscovery.js
    tests/validate-commands.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/validate-commands.test.cjs</red>
    <green>npm run build && node --test tests/validate-commands.test.cjs</green>
    <refactor>node --test tests/validate-commands.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    The validator uses the real supported command contract for milestone-close surfaced guidance and explains the proof scope it applied.

    Cases:
      - node-invoked `bgsd-tools.cjs` examples for `init:execute-phase`, `init:phase-op`, `verify:validate roadmap`, and `detect:gh-preflight` validate as supported routed commands
      - redirect-bearing shell examples keep the command match instead of treating `2>/dev/null` as part of the command name
      - quoted examples, bootstrap reconstruction snippets, and transition/output headings fall into named intentional classifications instead of generic `nonexistent-command` failures
      - `util:validate-commands --raw` reports proof inventory and named exclusions from validator-owned data
  </behavior>
  <implementation>
    Strengthen the existing validator module in place: derive CLI command inventory from authoritative routed sources, formalize the narrow named exception classes already implied by the research and backlog, and surface proof inventory from the raw validator payload without creating a second source of truth.
  </implementation>
</feature>

<verification>
npm run build && node --test tests/validate-commands.test.cjs
</verification>

<success_criteria>
- [ ] Supported routed commands now validate cleanly in surfaced node-invoked examples instead of failing as nonexistent
- [ ] Named exception classes cover the known quoted-example, redirect, internal-bootstrap, and transition-output cases without broad proof erosion
- [ ] Raw validator output includes validator-owned proof inventory and named exclusions that explain what a green result means
</success_criteria>

<output>
After completion, create `.planning/phases/180-command-validator-drift-resolution/180-01-SUMMARY.md`
</output>
