---
phase: 180-command-validator-drift-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 180-01
files_modified:
  - agents/bgsd-executor.md
  - agents/bgsd-phase-researcher.md
  - agents/bgsd-plan-checker.md
  - agents/bgsd-planner.md
  - docs/architecture.md
  - workflows/execute-phase.md
  - workflows/github-ci.md
  - workflows/new-milestone.md
  - workflows/transition.md
  - tests/validate-commands.test.cjs
autonomous: true
requirements:
  - CLEAN-03
intent:
  outcome_ids: []
  rationale: "No intent outcome IDs are available, so this follow-on slice traces directly to the remaining Phase 180 outcome: if any surfaced guidance still drifts after the validator contract is corrected, repair only those files and lock the final proof surface with closure-level regression coverage."
must_haves:
  truths:
    - "The milestone-close surfaced proof files across agents, workflows, docs, and built/runtime guidance either validate cleanly against the settled contract or are intentionally excluded by named validator-owned classifications."
    - "No surfaced file remains green only because the proof scope was narrowed or a second manifest was introduced."
    - "A final repo-close validator run is trustworthy because the current surfaced files and the validator's proof inventory agree on what was checked and what was intentionally excluded."
  artifacts:
    - path: "tests/validate-commands.test.cjs"
      provides: "Closure regression coverage for the exact milestone-close surfaced backlog"
      contains: "util:validate-commands --raw"
    - path: "workflows/transition.md"
      provides: "Transition next-step output surface that must either validate cleanly or remain intentionally classified"
      contains: "## ▶ Next Up"
    - path: "workflows/github-ci.md"
      provides: "Redirect-bearing gh preflight surfaced guidance covered by the final proof slice"
      contains: "detect:gh-preflight"
  key_links:
    - from: "tests/validate-commands.test.cjs"
      to: "agents/bgsd-planner.md"
      via: "closure regression keeps the previously failing surfaced agent/workflow/docs files aligned with the settled validator contract"
      pattern: "init:plan-phase|init:phase-op|init:execute-phase|verify:validate roadmap|detect:gh-preflight"
    - from: "tests/validate-commands.test.cjs"
      to: "workflows/transition.md"
      via: "closure proof ensures transition-style output headings stay intentionally classified instead of failing as generic slash commands"
      pattern: "## ▶ Next Up|/bgsd-complete-milestone"
---

<objective>
Close any remaining true surfaced drift after the validator contract lands, then lock the final milestone-close proof surface with a regression that matches the validator-owned inventory and exclusions.

Purpose: Phase 180 should edit surfaced files only if they still legitimately drift after Plan 01 settles the contract. This keeps the phase contract-first, avoids validator-loophole cleanup, and makes the final green result trustworthy instead of accidental.
Output: only the remaining truly drifting surfaced files among the current backlog (if any), plus a closure regression in `tests/validate-commands.test.cjs` and a final green `util:validate-commands --raw` run whose inventory meaning is stable.
</objective>

> **TDD Decision:** Skipped — this slice is primarily surface reconciliation and closure proof around an already-settled validator contract, not a new standalone behavior feature.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/180-command-validator-drift-resolution/180-CONTEXT.md
@.planning/phases/180-command-validator-drift-resolution/180-RESEARCH.md
@.planning/phases/180-command-validator-drift-resolution/180-01-SUMMARY.md
@tests/validate-commands.test.cjs
@agents/bgsd-executor.md
@agents/bgsd-phase-researcher.md
@agents/bgsd-plan-checker.md
@agents/bgsd-planner.md
@docs/architecture.md
@workflows/execute-phase.md
@workflows/github-ci.md
@workflows/new-milestone.md
@workflows/transition.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile only the surfaced files that still truly drift after Plan 01</name>
  <files>
    agents/bgsd-executor.md
    agents/bgsd-phase-researcher.md
    agents/bgsd-plan-checker.md
    agents/bgsd-planner.md
    docs/architecture.md
    workflows/execute-phase.md
    workflows/github-ci.md
    workflows/new-milestone.md
    workflows/transition.md
  </files>
  <action>
  Re-run `node bin/bgsd-tools.cjs util:validate-commands --raw` after Plan 01 and treat its grouped output as the authority for whether any surfaced-file edits are still needed.

  If any of the known backlog files above still fail because their surfaced guidance truly disagrees with the settled contract, edit only those files so each command is either runnable under the supported contract or clearly rewritten into an intentional reference/output form. If the validator is already green for these files, leave them unchanged and record in the summary that no surfaced-file reconciliation was required. Do not add new exclusions, reduce proof scope, or perform unrelated docs cleanup.
  </action>
  <verify>node bin/bgsd-tools.cjs util:validate-commands --raw</verify>
  <done>The known milestone-close surfaced backlog is either fully green or reduced only by legitimate file fixes that match the settled validator contract.</done>
</task>

<task type="auto">
  <name>Task 2: Lock final closure proof for the exact milestone-close backlog</name>
  <files>
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add or update a focused closure regression in `tests/validate-commands.test.cjs` that covers the exact previously failing milestone-close surfaced slice and the final raw payload meaning.

  The regression should load the affected surfaced files directly, assert they validate cleanly under the Plan 01 contract, and assert the raw validator payload still exposes proof inventory plus named exclusions. Keep the test narrow to this phase's closure surface; do not turn it into a second manifest or a broad full-repo snapshot test.
  </action>
  <verify>node --test tests/validate-commands.test.cjs</verify>
  <done>The closure regression fails if the known Phase 180 surfaced backlog returns or if raw validator output stops explaining its proof inventory and named exclusions.</done>
</task>

</tasks>

<verification>
node bin/bgsd-tools.cjs util:validate-commands --raw
</verification>

<success_criteria>
- [ ] The milestone-close surfaced files in scope validate green against the settled contract, with no remaining true drift in the known Phase 180 backlog
- [ ] Final closure proof still comes from validator-owned proof inventory and named exclusions rather than a second manifest or narrower scope
- [ ] A focused closure regression guards the exact surfaced backlog that made Phase 180 necessary
</success_criteria>

<output>
After completion, create `.planning/phases/180-command-validator-drift-resolution/180-02-SUMMARY.md`
</output>
