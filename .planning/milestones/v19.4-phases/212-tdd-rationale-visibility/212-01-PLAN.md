---
phase: 212-tdd-rationale-visibility
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/misc/templates.js
autonomous: true
requirements:
  - TDD-06
  - REGR-01
  - REGR-02
  - REGR-03
  - REGR-04
  - REGR-05
  - REGR-06
  - REGR-07
  - REGR-08
user_setup: []
verification_route: skip
verification_route_reason: "CLI tooling work (frontmatter extraction, summary rendering) using existing test infrastructure — no new behavioral contracts"

must_haves:
  truths:
    - "cmdSummaryGenerate extracts tdd_rationale from PLAN.md frontmatter via extractFrontmatter"
    - "SUMMARY.md contains '## TDD Decision' section when plan has tdd_rationale field"
    - "TDD Decision section renders before TDD Audit Trail section in summary output"
    - "tdd_rationale rendered in narrative format — no backtick-wrapped tokens"
    - "cmdSummaryGenerate continues to render TDD audit trail from TDD-AUDIT.json unchanged"
  artifacts:
    - path: src/commands/misc/templates.js
      contains: "extractFrontmatter(planContent)"
      provides: "tdd_rationale extraction from plan frontmatter"
    - path: src/commands/misc/templates.js
      contains: "## TDD Decision"
      provides: "TDD Decision section rendering in SUMMARY.md"
    - path: src/commands/misc/templates.js
      contains: "TDD Audit Trail"
      provides: "TDD Audit Trail section unchanged (after TDD Decision)"
  key_links:
    - from: "cmdSummaryGenerate"
      to: "extractFrontmatter"
      via: "function call"
      pattern: "tdd_rationale extracted from plan frontmatter"
    - from: "PLAN.md frontmatter"
      to: "SUMMARY.md"
      via: "cmdSummaryGenerate"
      pattern: "tdd_rationale surfaced in summary output"
---

<objective>
Extract tdd_rationale from PLAN.md frontmatter and render it as "TDD Decision" section in SUMMARY.md output via cmdSummaryGenerate in templates.js. Close GAP-R4: "Only audit trail rendering; decision rationale NOT surfaced".
</objective>

> **TDD Decision:** Skipped — Implementation is CLI tooling work (frontmatter extraction, summary rendering). No testable behavior with clear expected outcomes. This is non-behavioral tooling work.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/212-tdd-rationale-visibility/212-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract tdd_rationale from plan frontmatter in cmdSummaryGenerate</name>
  <files>src/commands/misc/templates.js</files>
  <action>
Extend cmdSummaryGenerate to extract tdd_rationale from PLAN.md frontmatter.

1. **Read current cmdSummaryGenerate implementation:**
   - Review cmdSummaryGenerate (lines 365-502) in templates.js
   - Note that it already imports extractFrontmatter at line 7

2. **Add tdd_rationale extraction after reading planContent:**
   After line 404 (where planContent is read), add:

   ```javascript
   // Extract tdd_rationale from plan frontmatter
   const planFrontmatter = extractFrontmatter(planContent);
   const tddRationale = planFrontmatter.tdd_rationale || null;
   ```

3. **Pass tddRationale to summary rendering logic:**
   The tddRationale variable will be used in the next task to render the TDD Decision section.
   No changes to the TDD audit trail rendering logic — that remains unchanged.
  </action>
  <verify>
node -e "
const t = require('./src/commands/misc/templates.js');
const src = t.cmdSummaryGenerate.toString();
// Verify extractFrontmatter is imported
console.log('extractFrontmatter imported:', src.includes('extractFrontmatter'));
// Verify tdd_rationale extraction pattern exists
console.log('tdd_rationale extraction:', src.includes('tdd_rationale') || src.includes('tddRationale'));
"
  </verify>
  <done>
tdd_rationale extracted from plan frontmatter via extractFrontmatter
</done>
</task>

<task type="auto">
  <name>Task 2: Render TDD Decision section before TDD Audit Trail in SUMMARY.md</name>
  <files>src/commands/misc/templates.js</files>
  <action>
Add "## TDD Decision" section rendering before the TDD Audit Trail section.

1. **Locate the summary content building logic:**
   Review lines 469-486 where summaryContent is built

2. **Insert TDD Decision section before TDD Audit Trail:**
   Modify the logic that builds summaryContent to include TDD Decision when tddRationale exists.

   The rendering should happen BEFORE the "## TDD Audit Trail" section (which starts at line 474 check).

   Format:
   ```
   ## TDD Decision

   **Rationale:** {tddRationale}

   ## TDD Audit Trail
   [existing audit trail content]
   ```

3. **Ensure narrative format:**
   - tddRationale value is rendered as-is (no backticks)
   - Section comes before TDD Audit Trail

4. **Handle missing tdd_rationale gracefully:**
   If tddRationale is null, don't render the TDD Decision section (preserve existing behavior).
  </action>
  <verify>
node -e "
const t = require('./src/commands/misc/templates.js');
const src = t.cmdSummaryGenerate.toString();
// Verify TDD Decision section rendering
console.log('TDD Decision section:', src.includes('## TDD Decision'));
// Verify it comes before TDD Audit Trail (check order)
const decisionIdx = src.indexOf('## TDD Decision');
const auditIdx = src.indexOf('## TDD Audit Trail');
console.log('Order correct:', decisionIdx < auditIdx && decisionIdx > 0);
"
  </verify>
  <done>
TDD Decision section rendered before TDD Audit Trail; narrative format maintained
</done>
</task>

<task type="auto">
  <name>Task 3: Verify GAP-R4 closure — tdd_rationale now surfaces in summary</name>
  <files>src/commands/misc/templates.js</files>
  <action>
Verify that the implementation correctly surfaces tdd_rationale in summary output, closing GAP-R4.

1. **Review the changes made in Tasks 1 and 2:**
   - extractFrontmatter used to get tdd_rationale from plan frontmatter
   - TDD Decision section rendered before TDD Audit Trail
   - Narrative format maintained (no backticks)

2. **Verify the rendering order:**
   - TDD Decision section appears BEFORE TDD Audit Trail section
   - This ensures rationale is visible before the proof (audit trail)

3. **Ensure no duplication:**
   - TDD Audit Trail rendering logic unchanged
   - Only tdd_rationale extraction and TDD Decision rendering added

4. **Test with sample data:**
   The implementation should work for both type:tdd and type:execute plans that have tdd_rationale field.
  </action>
  <verify>
node -e "
const t = require('./src/commands/misc/templates.js');
const src = t.cmdSummaryGenerate.toString();

// Check key implementation markers
const hasExtract = src.includes('extractFrontmatter(planContent)');
const hasDecision = src.includes('## TDD Decision');
const hasRationale = src.includes('tddRationale');
const hasAuditTrail = src.includes('## TDD Audit Trail');

console.log('Implementation checks:');
console.log('  extractFrontmatter(planContent):', hasExtract);
console.log('  ## TDD Decision:', hasDecision);
console.log('  tddRationale variable:', hasRationale);
console.log('  ## TDD Audit Trail unchanged:', hasAuditTrail);

// Verify decision comes before audit trail
const decisionMatch = src.match(/## TDD Decision[^#]*## TDD Audit Trail/);
console.log('  Decision before Audit Trail:', !!decisionMatch);
"
  </verify>
  <done>
GAP-R4 closed: tdd_rationale now surfaces in summary output before TDD Audit Trail
</done>
</task>

</tasks>

<verification>
# Verify templates.js loads without error
node -e "
require('./src/commands/misc/templates.js');
console.log('templates.js loads OK');
"

# Verify cmdSummaryGenerate is a function
node -e "
const t = require('./src/commands/misc/templates.js');
console.log('cmdSummaryGenerate type:', typeof t.cmdSummaryGenerate);
"

# Verify extractFrontmatter usage
node -e "
const t = require('./src/commands/misc/templates.js');
const src = t.cmdSummaryGenerate.toString();
const usesExtract = src.includes('extractFrontmatter(planContent)');
const hasDecision = src.includes('## TDD Decision');
console.log('extractFrontmatter used:', usesExtract);
console.log('TDD Decision rendered:', hasDecision);
"
</verification>

<success_criteria>
- cmdSummaryGenerate extracts tdd_rationale from PLAN.md frontmatter via extractFrontmatter
- SUMMARY.md contains '## TDD Decision' section before '## TDD Audit Trail' when plan has tdd_rationale
- tdd_rationale rendered in narrative format (no backticks)
- TDD Audit Trail rendering unchanged (no duplication)
- GAP-R4 closed: decision rationale now surfaced in summary output
- No regression: existing phase:snapshot, summary:generate continue working
</success_criteria>

<output>
After completion, create `.planning/phases/212-tdd-rationale-visibility/212-01-SUMMARY.md`
</output>
