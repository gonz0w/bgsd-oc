---
phase: 84-extended-tools
plan: 01
type: execute
wave: 1
depends_on: [83-01]
files_modified: [src/lib/cli-tools/yq.js, src/lib/cli-tools/bat.js, src/lib/cli-tools/gh.js, src/lib/cli-tools/index.js]
autonomous: true
requirements: [CLI-07, CLI-08, CLI-09]
user_setup: []

must_haves:
  truths:
    - "User can parse YAML files and extract data using yq"
    - "User can transform YAML with filters using yq"
    - "User can view files with syntax highlighting using bat"
    - "User can list open PRs using gh CLI"
    - "User can view GitHub issues using gh CLI"
    - "All tools gracefully fallback when CLI tools unavailable"
  artifacts:
    - path: "src/lib/cli-tools/yq.js"
      provides: "YAML processing wrapper with extraction and transformation"
      contains: "parseYAML, transformYAML, YAMLtoJSON"
    - path: "src/lib/cli-tools/bat.js"
      provides: "Syntax highlighting wrapper for file viewing"
      contains: "catWithHighlight, getFileTheme, listThemes"
    - path: "src/lib/cli-tools/gh.js"
      provides: "GitHub CLI wrapper for PRs and issues"
      contains: "listPRs, listIssues, getIssue, getRepoInfo"
    - path: "src/lib/cli-tools/index.js"
      provides: "Unified exports for all extended tools"
      exports: ["yq", "bat", "gh"]
  key_links:
    - from: "yq.js, bat.js, gh.js"
      to: "fallback.js"
      via: "withToolFallback wrapper"
      pattern: "withToolFallback\\(['\"](yq|bat|gh)"
    - from: "yq.js, bat.js, gh.js"
      to: "detector.js"
      via: "isToolAvailable check"
      pattern: "isToolAvailable\\(['\"](yq|bat|gh)"
---

<objective>
Implement yq, bat, and gh CLI tool wrappers for YAML processing, syntax highlighting, and GitHub operations.

Purpose: Complete the extended CLI tools integration (DO-46) enabling users to process YAML, view syntax-highlighted files, and perform GitHub operations via the CLI.

Output: Four new CLI tool modules (yq.js, bat.js, gh.js) with unified exports in index.js
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 83 created patterns to follow
@.planning/phases/83-search-and-discovery/83-01-SUMMARY.md

# Existing implementation pattern
@src/lib/cli-tools/jq.js

# Phase context decisions
@.planning/phases/84-extended-tools/84-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create yq.js YAML processing wrapper</name>
  <files>src/lib/cli-tools/yq.js</files>
  <action>
Create src/lib/cli-tools/yq.js with the following functions:
- parseYAML(yamlString, options) - Parse YAML string to JSON, use yq -o json when available
- transformYAML(yamlString, expression, options) - Apply jq-like filter expressions to YAML
- YAMLtoJSON(yamlString, options) - Convert YAML to JSON using yq
- isYqAvailable() - Check if yq CLI is installed

Use execFileSync with array args (per v9.2 security decision for shell injection prevention).
Include graceful fallback using withToolFallback() wrapper (like jq.js pattern).
For fallback: use js-yaml library if available, or basic YAML parsing for simple cases.
Use JSON output format (yq -o json) for programmatic parsing.

YAML-specific considerations:
- yq supports expressions like '.key', '.[]', 'select(.value > 5)'
- Default to compact output for machine consumption
- Handle both YAML 1.1 and 1.2 if yq version supports it
  </action>
  <verify>
node -e "const yq = require('./src/lib/cli-tools/yq'); console.log(JSON.stringify(yq.parseYAML('key: value')))"
node -e "const yq = require('./src/lib/cli-tools/yq'); console.log(JSON.stringify(yq.isYqAvailable()))"
  </verify>
  <done>
yq.js exports: parseYAML, transformYAML, YAMLtoJSON, isYqAvailable
Graceful fallback works when yq unavailable
JSON output format used for programmatic parsing
  </done>
</task>

<task type="auto">
  <name>Task 2: Create bat.js syntax highlighting wrapper</name>
  <files>src/lib/cli-tools/bat.js</files>
  <action>
Create src/lib/cli-tools/bat.js with the following functions:
- catWithHighlight(filePath, options) - Output file with syntax highlighting
- getFileTheme(filePath) - Detect appropriate theme for file type
- listThemes() - List available bat themes
- getLanguage(filePath) - Detect language from file extension

Use execFileSync with array args. Include graceful fallback using withToolFallback() wrapper.
For fallback: use basic cat output without colors when bat unavailable.

bat-specific options to support:
- --style (full, header, numbers, grid, none)
- --theme (Monokai, GitHub, OneHalfDark, etc.)
- --language (auto-detect or explicit)
- --line-range (for partial file viewing)
- --color (never, always, auto)

Default to: --style=header,grid --color=auto for balanced output.
  </action>
  <verify>
node -e "const bat = require('./src/lib/cli-tools/bat'); console.log(JSON.stringify(bat.isBatAvailable()))"
node -e "const bat = require('./src/lib/cli-tools/bat'); console.log(bat.listThemes().slice(0,3))"
  </verify>
  <done>
bat.js exports: catWithHighlight, getFileTheme, listThemes, getLanguage, isBatAvailable
Graceful fallback works when bat unavailable
Theme and language detection functional
  </done>
</task>

<task type="auto">
  <name>Task 3: Create gh.js GitHub operations wrapper</name>
  <files>src/lib/cli-tools/gh.js</files>
  <action>
Create src/lib/cli-tools/gh.js with the following functions:
- listPRs(options) - List pull requests (open, closed, all) with filters
- getPR(prNumber) - Get details of a specific PR
- listIssues(options) - List issues with state filters (open, closed, all)
- getIssue(issueNumber) - Get details of a specific issue
- getRepoInfo() - Get current repository information from gh
- isGhAvailable() - Check if gh CLI is installed
- checkAuth() - Check if user is authenticated to gh

Use execFileSync with array args. Use --json flag for structured output parsing.
For listPRs: support --state (open, closed, all), --limit, --head (branch), --base (branch)
For listIssues: support --state, --limit, --labels, --assignee
Include graceful fallback: return error message when gh unavailable (no Node.js fallback for gh)

GitHub CLI considerations:
- Requires authentication (gh auth status)
- Use 'gh pr list --json' and 'gh issue list --json' for structured output
- Parse JSON response for programmatic use
- Handle rate limiting gracefully
- Support --jq flag for additional filtering if needed
  </action>
  <verify>
node -e "const gh = require('./src/lib/cli-tools/gh'); console.log(JSON.stringify(gh.isGhAvailable()))"
node -e "const gh = require('./src/lib/cli-tools/gh'); console.log(JSON.stringify(gh.checkAuth()))"
  </verify>
  <done>
gh.js exports: listPRs, getPR, listIssues, getIssue, getRepoInfo, isGhAvailable, checkAuth
Uses --json flag for structured output
Graceful handling when unauthenticated or unavailable
  </done>
</task>

<task type="auto">
  <name>Task 4: Update index.js with unified exports</name>
  <files>src/lib/cli-tools/index.js</files>
  <action>
Update src/lib/cli-tools/index.js to export all extended tools:
- Add: const yq = require('./yq');
- Add: const bat = require('./bat');
- Add: const gh = require('./gh');
- Add exports: yq, bat, gh
- Add pipeline convenience functions if useful (e.g., readYAML, highlightAndShow)

Update module.exports to include all tools.
  </action>
  <verify>
node -e "const cli = require('./src/lib/cli-tools/index'); console.log(Object.keys(cli))"
  </verify>
  <done>
index.js exports: yq, bat, gh alongside existing ripgrep, fd, jq
All modules accessible via unified import
  </done>
</task>

<task type="auto">
  <name>Task 5: Bundle and verify integration</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Run npm run build to bundle the CLI with new modules.
Verify all new modules are included in the bundle.
Test that requiring the CLI tools works:
- node -e "require('./bin/bgsd-tools.cjs')" (should not error)
  </action>
  <verify>
npm run build 2>&1 | tail -20
ls -la bin/bgsd-tools.cjs | head -2
  </verify>
  <done>
Build completes without errors
Bundle includes all new CLI tool modules
  </done>
</task>

</tasks>

<verification>
- All three tools (yq, bat, gh) have wrapper modules
- Each wrapper has graceful fallback using withToolFallback
- Each wrapper uses execFileSync with array args (shell injection prevention)
- JSON output format used where possible for programmatic parsing
- index.js exports all extended tools
- Bundle builds successfully
</verification>

<success_criteria>
Phase 84 complete when:
1. yq.js provides YAML parsing and transformation with fallback
2. bat.js provides syntax highlighting with fallback
3. gh.js provides GitHub PR/issue operations (no fallback, but clear error)
4. All tools integrate with Phase 82 detection infrastructure
5. index.js exports unified interface
6. CLI bundles successfully

Requirements addressed:
- CLI-07 (yq): Task 1
- CLI-08 (bat): Task 2  
- CLI-09 (gh): Task 3
</success_criteria>

<output>
After completion, create `.planning/phases/84-extended-tools/84-01-SUMMARY.md`
</output>
