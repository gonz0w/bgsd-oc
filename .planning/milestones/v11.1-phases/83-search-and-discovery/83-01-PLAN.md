---
phase: 83-search-and-discovery
plan: 01
type: execute
wave: 1
depends_on: ["82-01"]
files_modified: []
autonomous: true
requirements: [CLI-04, CLI-05, CLI-06]

must_haves:
  truths:
    - User can run ripgrep searches and receive JSON-formatted output suitable for parsing
    - User can run fd file discovery commands that respect .gitignore patterns
    - User can pipe JSON data through jq for transformation and extraction in CLI pipelines
  artifacts:
    - src/lib/cli-tools/ripgrep.js
    - src/lib/cli-tools/fd.js
    - src/lib/cli-tools/jq.js
    - src/lib/cli-tools/index.js
  key_links:
    - Uses detector.js from Phase 82 for tool availability checking
    - Uses fallback.js from Phase 82 for graceful degradation
    - Provides CLI tool wrappers that integrate with existing Phase 82 infrastructure
---

<objective>

Implement ripgrep, fd, and jq CLI tool wrappers for faster content search and file discovery.

Purpose: Enable users to use native CLI tools (ripgrep, fd, jq) with JSON output for programmatic parsing, while maintaining graceful fallback to Node.js implementations when tools are unavailable.

Output: New CLI tool wrapper modules in src/lib/cli-tools/

</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-search-and-discovery/83-RESEARCH.md
@.planning/phases/83-search-and-discovery/83-CONTEXT.md
@.planning/phases/82-tool-detection-infrastructure/82-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ripgrep.js wrapper module</name>
  <files>src/lib/cli-tools/ripgrep.js</files>
  <action>
1. Create src/lib/cli-tools/ripgrep.js with:
   - Import detectTool from './detector.js'
   - Import withToolFallback from './fallback.js'
   - Function searchRipgrep(pattern, options) that:
     - Uses detectTool('ripgrep') to check availability
     - Uses execFileSync with array args ['--json', pattern, ...paths]
     - Parses JSON Lines output (split by newline, filter empty, parse each line)
     - Filters for obj.type === 'match' to get actual matches
     - Returns structured results with path, lineNumber, line, offset
   - Function parseRipgrepJson(output) for JSON Lines parsing
   - Handle exit code 1 (no matches) as success with empty results
   - Set default timeout of 30 seconds
2. Export searchRipgrep, parseRipgrepJson
  </action>
  <verify>node -e "
const { searchRipgrep } = require('./src/lib/cli-tools/ripgrep.js');
const result = searchRipgrep('TODO', { paths: ['.'] });
console.log('Success:', result.success);
console.log('Has results:', result.result && result.result.length > 0);
console.log('JSON output:', JSON.stringify(result.result?.[0], null, 2));
"</verify>
  <done>ripgrep.js exports searchRipgrep function that:
- Checks tool availability via detector.js
- Runs ripgrep --json and parses JSON Lines output
- Returns structured results with file, line, text
- Gracefully handles unavailable tool or no matches</done>
</task>

<task type="auto">
  <name>Task 2: Create fd.js wrapper module</name>
  <files>src/lib/cli-tools/fd.js</files>
  <action>
1. Create src/lib/cli-tools/fd.js with:
   - Import detectTool from './detector.js'
   - Import withToolFallback from './fallback.js'
   - Function findFiles(pattern, options) that:
     - Uses detectTool('fd') to check availability
     - Builds args: [pattern, '-e', extension, '-t', type] etc.
     - Uses execFileSync with array args
     - Respects .gitignore by default (fd default behavior)
     - Returns array of file paths (split by newline, filter empty)
   - Support options: extension (-e), type (-t f/d), exclude, hidden (-H)
   - Set default timeout of 30 seconds
2. Export findFiles
  </action>
  <verify>node -e "
const { findFiles } = require('./src/lib/cli-tools/fd.js');
const result = findFiles('*.js', { type: 'f' });
console.log('Success:', result.success);
console.log('Has results:', result.result && result.result.length > 0);
console.log('Sample:', result.result?.[0]);
"</verify>
  <done>fd.js exports findFiles function that:
- Checks tool availability via detector.js
- Runs fd with file pattern, respects .gitignore by default
- Returns array of file paths
- Gracefully handles unavailable tool or no matches</done>
</task>

<task type="auto">
  <name>Task 3: Create jq.js wrapper module</name>
  <files>src/lib/cli-tools/jq.js</files>
  <action>
1. Create src/lib/cli-tools/jq.js with:
   - Import detectTool from './detector.js'
   - Import withToolFallback from './fallback.js'
   - Function transformJson(inputJson, filter, options) that:
     - Uses detectTool('jq') to check availability
     - Uses execFileSync with array args ['-c', filter]
     - Passes input via stdin (stdio: ['pipe', 'pipe', 'pipe'])
     - Returns transformed output
   - Support options: compact (-c), raw output (-r), streaming (--stream)
   - Common filter presets for convenience (e.g., '.[]', '.key', 'map(...)')
   - Set default timeout of 30 seconds
2. Export transformJson and common filter presets
  </action>
  <verify>node -e "
const { transformJson } = require('./src/lib/cli-tools/jq.js');
const input = JSON.stringify({ items: [1, 2, 3] });
const result = transformJson(input, '.items[]');
console.log('Success:', result.success);
console.log('Output:', result.result);
"</verify>
  <done>jq.js exports transformJson function that:
- Checks tool availability via detector.js
- Runs jq with filter expression on input JSON
- Returns transformed output
- Gracefully handles unavailable tool or invalid JSON</done>
</task>

<task type="auto">
  <name>Task 4: Create index.js unified exports</name>
  <files>src/lib/cli-tools/index.js</files>
  <action>
1. Create src/lib/cli-tools/index.js with:
   - Export all functions from ripgrep.js, fd.js, jq.js
   - Add convenience function searchFiles(pattern, filePattern, options) for fd → ripgrep pipeline
   - Add convenience function searchAndTransform(pattern, filter, options) for full pipeline
2. Export detector, installGuidance, fallback from Phase 82 modules for convenience
  </action>
  <verify>node -e "
const cli = require('./src/lib/cli-tools/index.js');
console.log('Has searchRipgrep:', typeof cli.searchRipgrep === 'function');
console.log('Has findFiles:', typeof cli.findFiles === 'function');
console.log('Has transformJson:', typeof cli.transformJson === 'function');
console.log('Has searchFiles:', typeof cli.searchFiles === 'function');
"</verify>
  <done>index.js exports all CLI tool functions and pipeline convenience functions</done>
</task>

<task type="auto">
  <name>Task 5: Bundle and verify</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
1. Run build to bundle new modules into bin/bgsd-tools.cjs
2. Verify all modules load correctly from bundled CLI
3. Run basic smoke test of each function
  </action>
  <verify>npm run build && node bin/bgsd-tools.cjs util:tools | grep -E "ripgrep|fd.jq"</verify>
  <done>Build succeeds, CLI tools command shows ripgrep, fd, jq availability</done>
</task>

</tasks>

<verification>

- All three tools (ripgrep, fd, jq) can be detected and used
- ripgrep returns JSON-parsed results with file, line, text
- fd returns file paths respecting .gitignore
- jq transforms JSON input with filter expressions
- Graceful fallback when tools unavailable (via Phase 82 fallback.js)
- Build completes without errors

</verification>

<success_criteria>

1. User can run ripgrep searches and receive JSON-formatted output suitable for parsing
2. User can run fd file discovery commands that respect .gitignore patterns
3. User can pipe JSON data through jq for transformation and extraction in CLI pipelines
4. All tools integrate with Phase 82 detection infrastructure
5. Graceful fallback to Node.js when tools unavailable

</success_criteria>

<output>
After completion, create `.planning/phases/83-search-and-discovery/83-01-SUMMARY.md`
</output>
