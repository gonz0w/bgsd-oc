---
phase: 164-shared-planning-indexes-metadata-truthfulness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/plan-metadata.js
  - src/lib/helpers.js
  - src/commands/scaffold.js
  - src/commands/features.js
  - tests/plan-metadata-contract.test.cjs
autonomous: true
requirements:
  - FOUND-03
  - VERIFY-01
must_haves:
  truths:
    - "Planning and verification consumers reuse one shared must_haves normalization contract instead of hand-rolling YAML scraping per command."
    - "Nested, inline-array, and plain-string must_haves shapes normalize into one verifier-facing representation before downstream analysis runs."
    - "Repeated plan analysis can reuse shared planning metadata or workspace evidence indexes instead of rescanning the same files per consumer."
  artifacts:
    - "src/lib/plan-metadata.js"
    - "src/lib/helpers.js"
    - "src/commands/scaffold.js"
    - "src/commands/features.js"
    - "tests/plan-metadata-contract.test.cjs"
  key_links:
    - "src/commands/scaffold.js -> src/lib/plan-metadata.js via shared must_haves extraction instead of local parsing"
    - "src/commands/features.js -> src/lib/plan-metadata.js via normalized truths lookup for requirement tracing"
    - "src/lib/helpers.js -> cached phase or workspace metadata reuse consumed by the new helper"
---

<objective>
Establish the shared metadata and index substrate that Phase 164 consumers will trust.

Purpose: Create one reusable verifier-facing contract for plan metadata so later verifier and planner/checker work can stop depending on indentation-sensitive raw YAML parsing or repeated ad hoc rescans.
Output: Shared metadata/index helper plus focused regression coverage proving real must_haves shapes normalize consistently.
</objective>

> **TDD Decision:** Selected - metadata normalization and cache/index reuse are deterministic contracts with clear input/output expectations and should be locked before verifier and planner flows depend on them.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-RESEARCH.md
@src/lib/frontmatter.js
@src/lib/helpers.js
@src/commands/scaffold.js
@src/commands/features.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage for real must_haves shapes and reuseable indexes</name>
  <files>tests/plan-metadata-contract.test.cjs</files>
  <action>Add focused tests that describe the shared verifier-facing contract for plan metadata: nested list blocks, inline arrays, and plain-string must_haves entries from real PLAN frontmatter should normalize into stable truths, artifacts, and key_links collections. Cover repeated reads in one analysis pass so the helper contract proves it can reuse cached planning or workspace evidence instead of rescanning the same source per consumer.</action>
  <verify>node --test tests/plan-metadata-contract.test.cjs</verify>
  <done>The tests would fail if consumers still depend on indentation-specific YAML scraping or if repeated metadata lookups bypass the shared index contract.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the shared plan metadata and index helper</name>
  <files>src/lib/plan-metadata.js, src/lib/helpers.js, src/commands/scaffold.js, src/commands/features.js, tests/plan-metadata-contract.test.cjs</files>
  <action>Create `src/lib/plan-metadata.js` as the single verifier-facing helper for extracting and normalizing must_haves data from frontmatter-backed plan metadata, including status details that later commands can distinguish as present, missing, or inconclusive. Reuse cached phase data from `src/lib/helpers.js` and expose any needed workspace evidence lookup helpers so `src/commands/scaffold.js` and `src/commands/features.js` stop maintaining their own must_haves parsing rules.</action>
  <verify>node --test tests/plan-metadata-contract.test.cjs</verify>
  <done>One shared helper owns must_haves normalization and index reuse, and scaffold/features consume that contract instead of bespoke plan parsing.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/plan-metadata-contract.test.cjs
</verification>

<success_criteria>
- Shared verifier-facing metadata normalization exists under `src/lib/` and accepts the real must_haves shapes used by planning artifacts.
- Repeated metadata analysis reuses shared planning or workspace indexes instead of rescanning per consumer.
- The repo still builds from `src/`, and the focused metadata-contract tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-01-SUMMARY.md`
</output>
