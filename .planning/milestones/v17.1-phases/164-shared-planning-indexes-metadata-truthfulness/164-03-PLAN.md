---
phase: 164-shared-planning-indexes-metadata-truthfulness
plan: 03
type: execute
wave: 3
depends_on:
  - "164-02"
files_modified:
  - src/commands/verify.js
  - workflows/plan-phase.md
  - agents/bgsd-planner.md
  - agents/bgsd-plan-checker.md
  - tests/verify.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - PLAN-01
  - FOUND-03
  - VERIFY-02
must_haves:
  truths:
    - "Planner and checker approval flows reject malformed verifier-facing must_haves metadata before a phase plan is approved for execution."
    - "Plan-structure validation checks verifier-consumable metadata semantics, not just field presence, so malformed artifacts or key_links are caught early."
    - "Planning approval guidance reuses the same shared metadata/index contract as verification instead of describing a separate interpretation of must_haves."
  artifacts:
    - "src/commands/verify.js"
    - "workflows/plan-phase.md"
    - "agents/bgsd-planner.md"
    - "agents/bgsd-plan-checker.md"
    - "tests/verify.test.cjs"
    - "tests/workflow.test.cjs"
  key_links:
    - "src/commands/verify.js -> plan-structure metadata checks consumed by planner and checker approval steps"
    - "workflows/plan-phase.md -> planner and checker orchestration that now requires verifier-consumable metadata before approval"
    - "agents/bgsd-planner.md and agents/bgsd-plan-checker.md -> shared metadata validation contract for plan approval"
---

<objective>
Close the loop by making planning approval enforce the same metadata truthfulness contract as verification.

Purpose: Prevent malformed must_haves metadata from surviving planning and reaching execution by wiring the shared contract into plan-structure validation and planning workflow guidance.
Output: Approval-time metadata guardrails in code plus aligned planner/checker workflow regressions.
</objective>

> **TDD Decision:** Selected - approval gating and workflow wording are deterministic enough to capture with focused command and workflow regressions before the guardrails are wired into the planning path.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-RESEARCH.md
@.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-02-PLAN.md
@src/commands/verify.js
@workflows/plan-phase.md
@agents/bgsd-planner.md
@agents/bgsd-plan-checker.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add approval-path regressions for malformed verifier-facing metadata</name>
  <files>tests/verify.test.cjs, tests/workflow.test.cjs</files>
  <action>Add focused tests that prove `verify:verify plan-structure` flags malformed or inconclusive verifier-facing must_haves metadata as an approval blocker instead of treating the field as merely present. Add workflow/prompt regressions that require planner and checker flows to run and honor that semantic validation so malformed metadata cannot be silently approved for execution.</action>
  <verify>node --test tests/verify.test.cjs tests/workflow.test.cjs</verify>
  <done>The tests would fail if plan approval still checks only frontmatter presence or if workflow guidance drifts away from the installed metadata-validation contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire planner and checker approval flows to the shared metadata gate</name>
  <files>src/commands/verify.js, workflows/plan-phase.md, agents/bgsd-planner.md, agents/bgsd-plan-checker.md, tests/verify.test.cjs, tests/workflow.test.cjs</files>
  <action>Extend `verify:verify plan-structure` to validate whether must_haves metadata is verifier-consumable under the shared contract from 164-01/02, and surface malformed or inconclusive artifacts/key_links as approval-time issues. Update `workflows/plan-phase.md`, `agents/bgsd-planner.md`, and `agents/bgsd-plan-checker.md` so planning explicitly runs and respects that gate before plans are considered approved, keeping the approval flow on the same shared planning/index contract used by verification.</action>
  <verify>node --test tests/verify.test.cjs tests/workflow.test.cjs</verify>
  <done>Planner/checker approval uses the same semantic metadata gate as verification, and malformed verifier-facing must_haves blocks are rejected before execution.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/verify.test.cjs tests/workflow.test.cjs
</verification>

<success_criteria>
- `verify:verify plan-structure` blocks malformed verifier-facing must_haves metadata rather than checking only field presence.
- Planner and checker workflow guidance explicitly requires that semantic metadata gate before approval.
- The repo still builds from `src/`, and the focused approval-path regressions pass.
</success_criteria>

<output>
After completion, create `.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-03-SUMMARY.md`
</output>
