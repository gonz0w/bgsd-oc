---
phase: 164-shared-planning-indexes-metadata-truthfulness
plan: 02
type: execute
wave: 2
depends_on:
  - "164-01"
files_modified:
  - src/commands/verify.js
  - agents/bgsd-verifier.md
  - tests/verify-metadata-truthfulness.test.cjs
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - FOUND-03
must_haves:
  truths:
    - "Verifier artifact and key-link checks accept normalized nested, inline-array, and plain-string must_haves metadata without false missing-metadata failures."
    - "Verifier output fails loudly and actionably when artifact or key-link extraction is empty or inconclusive instead of implying success or neutrality."
    - "Verifier passes reuse the shared metadata or workspace indexes introduced in 164-01 instead of rereading the same planning evidence per item."
  artifacts:
    - "src/commands/verify.js"
    - "agents/bgsd-verifier.md"
    - "tests/verify-metadata-truthfulness.test.cjs"
  key_links:
    - "src/commands/verify.js -> the shared metadata helper from 164-01 for artifact, key-link, and quality must_haves evaluation"
    - "agents/bgsd-verifier.md -> verifier CLI contract wording that now distinguishes missing from inconclusive metadata"
    - "tests/verify-metadata-truthfulness.test.cjs -> loud-failure regression proof for empty or inconclusive extraction paths"
---

<objective>
Make verifier-facing must_haves analysis truthful and reusable.

Purpose: Remove the false-negative and empty-success behavior in artifact, key-link, and quality verification so reviewers get accurate metadata verdicts from one shared contract.
Output: Hardened verifier commands, aligned verifier guidance, and focused regression coverage for missing versus inconclusive metadata.
</objective>

> **TDD Decision:** Selected - verifier result shaping is deterministic and directly tied to user-visible pass/fail semantics, so the expected payloads should be fixed in tests before the command logic changes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-RESEARCH.md
@.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-01-PLAN.md
@src/commands/verify.js
@agents/bgsd-verifier.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for truthful verifier metadata outcomes</name>
  <files>tests/verify-metadata-truthfulness.test.cjs</files>
  <action>Add targeted tests for `verify:verify artifacts`, `verify:verify key-links`, and `verify:verify quality` using plans whose must_haves metadata appears as nested lists, inline arrays, and plain-string entries. Cover missing metadata separately from malformed or inconclusive metadata, and assert that empty actionable extraction now returns a loud failure/result detail instead of the old quiet neutral wording.</action>
  <verify>node --test tests/verify-metadata-truthfulness.test.cjs</verify>
  <done>The tests would fail if verifier commands still reject valid shapes, collapse inconclusive extraction into success, or rescan evidence through bespoke parsing paths.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate verifier commands onto the shared truthfulness contract</name>
  <files>src/commands/verify.js, agents/bgsd-verifier.md, tests/verify-metadata-truthfulness.test.cjs</files>
  <action>Replace direct `parseMustHavesBlock()` usage in `src/commands/verify.js` with the shared metadata/index helper from 164-01 so artifact, key-link, and quality checks consume one normalized must_haves contract. Return explicit missing-versus-inconclusive failure payloads whenever metadata exists but yields no trustworthy actionable entries, and update `agents/bgsd-verifier.md` so the agent instructions describe the louder contract accurately.</action>
  <verify>node --test tests/verify-metadata-truthfulness.test.cjs</verify>
  <done>Verifier commands accept real must_haves shapes, loud-fail on empty or inconclusive extraction, and agent guidance matches the installed CLI behavior.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/verify-metadata-truthfulness.test.cjs
</verification>

<success_criteria>
- Artifact, key-link, and quality verification use one normalized metadata contract.
- Missing metadata and inconclusive extraction produce distinct, actionable verifier outcomes.
- The repo still builds from `src/`, and the targeted verifier truthfulness tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/164-shared-planning-indexes-metadata-truthfulness/164-02-SUMMARY.md`
</output>
