---
phase: 163-shared-contracts-safe-mutation
plan: 04
type: execute
wave: 2
depends_on:
  - "163-01"
files_modified:
  - src/lib/json-store-mutator.js
  - src/commands/memory.js
  - src/commands/lessons.js
  - src/commands/trajectory.js
  - src/lib/phase-handoff.js
  - tests/memory-store-mutator.test.cjs
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
must_haves:
  truths:
    - "Touched memory and handoff writers update JSON artifacts through a locked read-transform-publish path instead of stale whole-file overwrites."
    - "Best-effort SQLite side effects are replaced with one shared JSON-store mutation contract for the touched flows."
    - "Concurrent writes to touched memory and handoff stores no longer silently drop one writer's changes."
  artifacts:
    - "src/lib/json-store-mutator.js"
    - "src/commands/memory.js"
    - "src/commands/lessons.js"
    - "src/commands/trajectory.js"
    - "src/lib/phase-handoff.js"
    - "tests/memory-store-mutator.test.cjs"
  key_links:
    - "src/commands/memory.js, src/commands/lessons.js, and src/commands/trajectory.js -> src/lib/json-store-mutator.js via one locked transform/publish contract"
    - "src/lib/phase-handoff.js -> shared lock and atomic writer from 163-01"
    - "src/lib/json-store-mutator.js -> SQLite mirror writes inside the same critical section as JSON publish"
---

<objective>
Move touched JSON-backed memory and handoff flows onto one locked mutation contract.

Purpose: Eliminate the remaining lock-free whole-file rewrites that can lose concurrent updates in memory and handoff stores.
Output: Shared JSON-store mutator consumed by the touched memory and handoff paths with targeted regression coverage.
</objective>

> **TDD Decision:** Selected — locked append/update behavior and JSON-plus-SQLite sequencing are deterministic enough to define with focused tests before migrating the write paths.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/163-shared-contracts-safe-mutation/163-RESEARCH.md
@.planning/phases/163-shared-contracts-safe-mutation/163-01-PLAN.md
@src/commands/memory.js
@src/commands/lessons.js
@src/commands/trajectory.js
@src/lib/phase-handoff.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression coverage for locked JSON-store mutations</name>
  <files>tests/memory-store-mutator.test.cjs</files>
  <action>Add targeted tests for locked append/update behavior across the touched JSON-backed stores, including repeated appends, ordered writes, and failure handling around publish-plus-SQLite mirror work. Keep the suite focused on the shared helper contract rather than duplicating command-level assertions across many existing tests.</action>
  <verify>node --test tests/memory-store-mutator.test.cjs</verify>
  <done>The tests would catch stale-read whole-file overwrites or JSON/SQLite ordering regressions in the touched flows.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate touched memory and handoff writers to the shared helper</name>
  <files>src/lib/json-store-mutator.js, src/commands/memory.js, src/commands/lessons.js, src/commands/trajectory.js, src/lib/phase-handoff.js, tests/memory-store-mutator.test.cjs</files>
  <action>Create a shared JSON-store mutator that runs lock, fresh read, pure transform, temp-file publish, and SQLite mirror/update work inside one critical section. Migrate `src/commands/memory.js`, `src/commands/lessons.js`, `src/commands/trajectory.js`, and touched `src/lib/phase-handoff.js` writes onto that helper so they stop performing independent read/modify/write rewrites and best-effort SQLite appends.</action>
  <verify>node --test tests/memory-store-mutator.test.cjs</verify>
  <done>Touched JSON-backed stores and handoff writes share one locked mutation helper, and the targeted tests pass through the migrated path.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/memory-store-mutator.test.cjs
</verification>

<success_criteria>
- Touched memory, lessons, trajectory, and handoff writes use one locked transform-and-publish helper.
- JSON and SQLite mirror work for the touched stores no longer rely on best-effort dual-write ordering.
- The repo still builds from `src/`, and the targeted JSON-store mutation tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/163-shared-contracts-safe-mutation/163-04-SUMMARY.md`
</output>
