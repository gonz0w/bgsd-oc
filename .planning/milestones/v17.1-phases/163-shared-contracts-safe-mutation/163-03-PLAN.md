---
phase: 163-shared-contracts-safe-mutation
plan: 03
type: execute
wave: 3
depends_on:
  - "163-02"
files_modified:
  - src/plugin/tools/bgsd-progress.js
  - src/plugin/parsers/state.js
  - tests/plugin-progress-contract.test.cjs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-04
must_haves:
  truths:
    - "Plugin progress mutations use the same canonical state/session contract as CLI state commands."
    - "Plugin-side state invalidation/readback no longer leaves related session tables stale after a mutation."
    - "Concurrent progress updates no longer depend on plugin-only mutation rules that can drift from the CLI contract."
  artifacts:
    - "src/plugin/tools/bgsd-progress.js"
    - "src/plugin/parsers/state.js"
    - "tests/plugin-progress-contract.test.cjs"
  key_links:
    - "src/plugin/tools/bgsd-progress.js -> the shared state/session mutator introduced in 163-02"
    - "src/plugin/tools/bgsd-progress.js -> src/plugin/parsers/state.js via full affected-table invalidation/readback"
    - "src/plugin/parsers/state.js -> session tables beyond session_state so cached derived views stay honest"
---

<objective>
Move the plugin progress surface onto the same canonical state/session contract established for the CLI path.

Purpose: Remove the last duplicated state mutation path that would otherwise let plugin progress drift from the CLI contract introduced earlier in the phase.
Output: Plugin progress and state invalidation wired to the canonical state/session mutator with focused regression coverage.
</objective>

> **TDD Decision:** Selected — plugin progress actions and parser invalidation have deterministic state-transition behavior, so targeted tests should define the contract before the migration lands.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/163-shared-contracts-safe-mutation/163-RESEARCH.md
@.planning/phases/163-shared-contracts-safe-mutation/163-01-PLAN.md
@.planning/phases/163-shared-contracts-safe-mutation/163-02-PLAN.md
@src/plugin/tools/bgsd-progress.js
@src/plugin/parsers/state.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for plugin progress contract reuse</name>
  <files>tests/plugin-progress-contract.test.cjs</files>
  <action>Add targeted tests that prove plugin progress actions call the same canonical mutation semantics as CLI state updates, including blocker/decision changes, progress advancement, and readback after invalidation. Keep the tests focused on observable plugin results so the migration does not change the tool surface while still removing duplicate mutation logic.</action>
  <verify>node --test tests/plugin-progress-contract.test.cjs</verify>
  <done>The tests would fail if plugin progress keeps its own write choreography instead of delegating to the shared contract.</done>
</task>

<task type="auto">
  <name>Task 2: Replace plugin-local mutation logic with the shared state/session contract</name>
  <files>src/plugin/tools/bgsd-progress.js, src/plugin/parsers/state.js, tests/plugin-progress-contract.test.cjs</files>
  <action>Remove the plugin-local lock and direct regex write choreography from `src/plugin/tools/bgsd-progress.js` by delegating its touched actions to the canonical state/session mutator introduced in 163-02. Update `src/plugin/parsers/state.js` invalidation so every affected session table is refreshed after mutation instead of only deleting `session_state`.</action>
  <verify>node --test tests/plugin-progress-contract.test.cjs</verify>
  <done>The plugin progress surface now shares the CLI mutation contract, and plugin cache invalidation/readback stays truthful across affected session tables.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/plugin-progress-contract.test.cjs
</verification>

<success_criteria>
- Plugin progress actions use the same canonical mutation contract as CLI state commands.
- Plugin invalidation/readback refreshes all affected session tables after touched mutations.
- The repo still builds from `src/`, and the targeted plugin regression tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/163-shared-contracts-safe-mutation/163-03-SUMMARY.md`
</output>
