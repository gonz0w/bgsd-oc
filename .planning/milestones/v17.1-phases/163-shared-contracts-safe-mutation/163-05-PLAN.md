---
phase: 163-shared-contracts-safe-mutation
plan: 05
type: execute
wave: 2
depends_on:
  - "163-01"
files_modified:
  - src/lib/config-contract.js
  - src/lib/config.js
  - src/commands/misc.js
  - src/plugin/parsers/config.js
  - src/plugin/idle-validator.js
  - tests/config-contract.test.cjs
autonomous: true
requirements:
  - FOUND-04
must_haves:
  truths:
    - "Touched config reads, writes, and repairs use one schema-driven normalization contract across CLI and plugin surfaces."
    - "Plugin config defaults no longer drift away from the CLI's `CONFIG_SCHEMA` behavior for touched settings."
    - "Maintainers can change touched config defaults and coercion in one shared implementation."
  artifacts:
    - "src/lib/config-contract.js"
    - "src/lib/config.js"
    - "src/commands/misc.js"
    - "src/plugin/parsers/config.js"
    - "src/plugin/idle-validator.js"
    - "tests/config-contract.test.cjs"
  key_links:
    - "src/lib/config.js, src/commands/misc.js, src/plugin/parsers/config.js, and src/plugin/idle-validator.js -> src/lib/config-contract.js via one normalization/default source"
    - "src/plugin/idle-validator.js -> the same normalized repair payload produced by CLI config writes"
    - "CONFIG_SCHEMA -> shared config contract so touched coercion and defaults stop diverging"
---

<objective>
Unify touched config normalization and write behavior behind one schema-driven contract.

Purpose: Remove duplicated config default and coercion logic so maintainers only change touched config behavior in one place.
Output: Shared config contract consumed by touched CLI and plugin config flows with focused regression coverage.
</objective>

> **TDD Decision:** Selected — schema normalization, coercion, and corrupt-config repair each have deterministic inputs and outputs that should be captured in targeted tests first.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/163-shared-contracts-safe-mutation/163-RESEARCH.md
@.planning/phases/163-shared-contracts-safe-mutation/163-01-PLAN.md
@src/lib/config.js
@src/commands/misc.js
@src/plugin/parsers/config.js
@src/plugin/idle-validator.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for the shared config contract</name>
  <files>tests/config-contract.test.cjs</files>
  <action>Add targeted tests for default derivation from `CONFIG_SCHEMA`, nested key coercion, workflow alias handling, and corrupt-config repair output so the shared contract is defined before CLI and plugin consumers are migrated.</action>
  <verify>node --test tests/config-contract.test.cjs</verify>
  <done>The tests would fail if touched CLI and plugin config flows still normalize or repair config differently.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate touched config consumers onto one contract</name>
  <files>src/lib/config-contract.js, src/lib/config.js, src/commands/misc.js, src/plugin/parsers/config.js, src/plugin/idle-validator.js, tests/config-contract.test.cjs</files>
  <action>Extract one schema-driven config normalizer/writer based on `CONFIG_SCHEMA`, then migrate CLI config reads and writes (`loadConfig`, `cmdConfigSet`, `cmdConfigMigrate`) and plugin config consumers (`parseConfig`, `idle-validator`) to that shared contract. Remove plugin-local default drift and make corrupt-config repair write the same normalized shape that CLI commands understand.</action>
  <verify>node --test tests/config-contract.test.cjs</verify>
  <done>There is one default source and one normalization path for touched config flows, and CLI/plugin behavior stays aligned on the same schema-driven output.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/config-contract.test.cjs
</verification>

<success_criteria>
- Touched config reads, writes, and repairs derive defaults from one schema-driven contract.
- Plugin config parsing and repair no longer maintain a separate default shape from CLI config behavior.
- The repo still builds from `src/`, and the targeted config-contract tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/163-shared-contracts-safe-mutation/163-05-SUMMARY.md`
</output>
