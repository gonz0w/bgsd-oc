---
phase: 163-shared-contracts-safe-mutation
plan: 02
type: execute
wave: 2
depends_on:
  - "163-01"
files_modified:
  - src/lib/planning-cache.js
  - src/lib/state-session-mutator.js
  - src/commands/state.js
  - tests/state-session-mutator.test.cjs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-04
must_haves:
  truths:
    - "Touched state and session mutations compute one canonical next model before writing Markdown and SQLite."
    - "Touched CLI state/session updates no longer race through independent SQLite-first plus regex-rewrite sequences."
    - "State/session compatibility stays intact for existing STATE.md layouts while touched flows stop duplicating regex mutation logic."
  artifacts:
    - "src/lib/planning-cache.js"
    - "src/lib/state-session-mutator.js"
    - "src/commands/state.js"
    - "tests/state-session-mutator.test.cjs"
  key_links:
    - "src/commands/state.js -> src/lib/state-session-mutator.js via semantic operations such as patch, decision, blocker, session, and completion updates"
    - "src/lib/state-session-mutator.js -> src/lib/planning-cache.js via transaction-scoped session bundle helpers"
    - "src/commands/state.js -> compatibility STATE.md rendering via one canonical structured payload"
---

<objective>
Move touched CLI state and session mutations onto one canonical contract so Markdown and SQLite stay in sync under normal and concurrent workflow updates.

Purpose: Deliver the core Phase 163 reliability win in the CLI path first by replacing duplicated state mutation logic with one semantic mutator.
Output: Canonical CLI state/session mutator, cache helpers, and regression coverage.
</objective>

> **TDD Decision:** Selected — semantic state operations, rollback behavior, and compatibility rendering are all deterministic enough to lock down with tests before migrating the live mutation surfaces.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/163-shared-contracts-safe-mutation/163-RESEARCH.md
@.planning/phases/163-shared-contracts-safe-mutation/163-01-PLAN.md
@src/commands/state.js
@src/lib/planning-cache.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for canonical state/session mutations</name>
  <files>tests/state-session-mutator.test.cjs</files>
  <action>Add targeted tests that describe the semantic mutation contract for touched CLI operations: state field patch/update, blocker and decision mutations, session continuity updates, and complete-plan core behavior. Cover failure/rollback expectations and compatibility with existing STATE.md layouts so the later migration can prove Markdown and SQLite stay aligned.</action>
  <verify>node --test tests/state-session-mutator.test.cjs</verify>
  <done>The new tests fail against the pre-migration behavior or would catch drift/regression if the canonical mutator is bypassed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the CLI canonical state/session mutator</name>
  <files>src/lib/planning-cache.js, src/lib/state-session-mutator.js, src/commands/state.js, tests/state-session-mutator.test.cjs</files>
  <action>Add transaction-scoped session bundle helpers to `src/lib/planning-cache.js`, implement `src/lib/state-session-mutator.js` with semantic operations such as patch, appendDecision, appendBlocker, resolveBlocker, recordContinuity, and completePlanCore, and have `src/commands/state.js` call that mutator instead of independently sequencing SQLite writes and regex-based file rewrites. Preserve regex patching only as a compatibility fallback for sections the canonical renderer cannot safely regenerate yet.</action>
  <verify>node --test tests/state-session-mutator.test.cjs</verify>
  <done>Touched CLI state commands route through the mutator, Markdown and SQLite are derived from one payload, and rollback/compatibility tests pass.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/state-session-mutator.test.cjs
</verification>

<success_criteria>
- Touched CLI state/session commands call one shared semantic mutator.
- Canonical writes keep STATE.md and session tables aligned for the covered operations.
</success_criteria>

<output>
After completion, create `.planning/phases/163-shared-contracts-safe-mutation/163-02-SUMMARY.md`
</output>
