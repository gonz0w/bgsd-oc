---
phase: 163-shared-contracts-safe-mutation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/project-lock.js
  - src/lib/atomic-write.js
  - tests/mutation-primitives.test.cjs
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
must_haves:
  truths:
    - "Touched workflow writers use one shared project lock contract instead of per-surface ad hoc coordination."
    - "Whole-file replacement writes can publish through one temp-file-plus-rename helper instead of raw writeFileSync rewrites."
  artifacts:
    - "src/lib/project-lock.js"
    - "src/lib/atomic-write.js"
    - "tests/mutation-primitives.test.cjs"
  key_links:
    - "src/lib/project-lock.js -> state, memory, handoff, and config mutators via one acquire/release API"
    - "src/lib/atomic-write.js -> JSON/Markdown publishers via temp-file rename semantics"
---

<objective>
Establish the shared locking and atomic publish primitives that every touched Phase 163 writer will reuse.

Purpose: Create the one reusable substrate required to stop lock-free rewrites and raw whole-file publishes before migrating individual workflows.
Output: Shared source helpers ready for state/session and storage/config migrations.
</objective>

> **TDD Decision:** Selected — lock acquisition, atomic publish, and mutation-bundle behavior are deterministic contracts that should be proven with targeted tests before later plans depend on them.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/163-shared-contracts-safe-mutation/163-RESEARCH.md
@src/plugin/tools/bgsd-progress.js
@src/lib/phase-handoff.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared lock and atomic publish helpers</name>
  <files>src/lib/project-lock.js, src/lib/atomic-write.js, tests/mutation-primitives.test.cjs</files>
  <action>Create a project-scoped lock helper that extracts the `.planning/.lock` mkdir/rmdir discipline out of `src/plugin/tools/bgsd-progress.js`, includes stale-lock recovery, and is callable from both CLI and plugin code. Add a shared atomic file publish helper that standardizes temp-file + rename writes for JSON/Markdown replacements so later plans stop calling raw `writeFileSync(target, content)` for whole-file mutations.</action>
  <verify>node --test tests/mutation-primitives.test.cjs</verify>
  <done>Shared helpers exist, cover stale-lock and atomic-publish behavior in tests, and no touched caller needs to invent its own lock or temp-file pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the primitives are reusable from touched mutation surfaces</name>
  <files>src/lib/project-lock.js, src/lib/atomic-write.js, tests/mutation-primitives.test.cjs</files>
  <action>Cover the new helper APIs with focused tests that model the touched Phase 163 write patterns: stale-lock recovery, guaranteed lock release, and temp-file replacement without partial publish exposure. Keep the helper surface small and source-first so later plans can import it without dragging in unrelated command code.</action>
  <verify>node --test tests/mutation-primitives.test.cjs</verify>
  <done>The primitives are tested as the single shared lock/publish contract for later state, memory, handoff, and config migrations.</done>
</task>

</tasks>

<verification>
npm run build && node --test tests/mutation-primitives.test.cjs
</verification>

<success_criteria>
- Shared lock and atomic publish helpers exist in source and are ready for reuse by both CLI and plugin flows.
- The repo builds from `src/` after the new primitives are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/163-shared-contracts-safe-mutation/163-01-SUMMARY.md`
</output>
