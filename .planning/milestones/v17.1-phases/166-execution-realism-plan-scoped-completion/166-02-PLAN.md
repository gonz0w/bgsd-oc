---
phase: 166-execution-realism-plan-scoped-completion
plan: 02
type: execute
wave: 2
depends_on:
  - "166-01"
files_modified:
  - src/commands/misc.js
  - src/commands/state.js
  - src/lib/state-session-mutator.js
  - src/commands/roadmap.js
  - workflows/execute-plan.md
  - tests/summary-generate.test.cjs
  - tests/state.test.cjs
  - tests/state-session-mutator.test.cjs
  - tests/workflow.test.cjs
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
must_haves:
  truths:
    - "Generated summary file lists and file-count metrics come from plan-owned commits or declared plan files instead of unrelated dirty workspace changes."
    - "Plan completion recalculates current-plan and total-plan values from on-disk phase truth before writing STATE.md or ROADMAP.md metadata."
    - "Automated completion runs a focused readback that repairs stale focus text, plan totals, and roadmap summary wording before finalization."
    - "Execution workflow guidance uses the hardened completion path so final metadata reflects the active plan rather than ambient workspace noise."
  artifacts:
    - path: "src/commands/misc.js"
      provides: "Summary scaffolding that derives files and metrics from plan-scoped truth"
      contains: "summary"
    - path: "src/commands/state.js"
      provides: "Complete-plan command that refreshes totals and validates metadata readback"
      contains: "completePlan"
    - path: "src/commands/roadmap.js"
      provides: "Roadmap progress updater aligned to refreshed on-disk plan or summary counts"
      contains: "cmdRoadmapUpdatePlanProgress"
    - path: "workflows/execute-plan.md"
      provides: "Executor metadata-finalization flow that uses the repaired summary and completion contract"
      contains: "complete-plan"
  key_links:
    - from: "workflows/execute-plan.md"
      to: "src/commands/state.js"
      via: "Execution metadata finalization runs the refreshed completion and readback path"
      pattern: "complete-plan"
    - from: "src/commands/misc.js"
      to: "tests/summary-generate.test.cjs"
      via: "Summary generation regressions prove plan-scoped file and metric derivation"
      pattern: "summary-generate"
    - from: "src/lib/state-session-mutator.js"
      to: "src/commands/roadmap.js"
      via: "Shared completion truth feeds both STATE and ROADMAP updates from the same refreshed plan inventory"
      pattern: "current_plan|total_plans"
---

<objective>
Make summary scaffolding and completion metadata reflect the active plan by deriving files and totals from plan-scoped evidence, then repairing stale STATE or ROADMAP summaries before metadata finalization.

Purpose: Recent dirty-workspace and plan-count-drift lessons show the current completion path can document unrelated files or leave high-level summaries stale even when the underlying plan finished correctly. This plan makes summary and completion outputs truthful to the active plan and adds readback repair before the final metadata step.
Output: Hardened summary generation, refreshed completion or roadmap update behavior, updated execute-plan guidance, and regressions for dirty-workspace and plan-total drift scenarios.
</objective>

> **TDD Decision:** Selected — summary derivation and metadata repair are deterministic file-state transformations with concrete expected outputs, so focused regressions should lock the behavior first.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/165-jj-execution-repo-local-verification/165-03-SUMMARY.md
@workflows/execute-plan.md
@src/commands/misc.js
@src/commands/state.js
@src/lib/state-session-mutator.js
@src/commands/roadmap.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regressions for plan-scoped summary truth and completion readback repair</name>
  <files>tests/summary-generate.test.cjs, tests/state.test.cjs, tests/state-session-mutator.test.cjs, tests/workflow.test.cjs</files>
  <action>Add focused regressions that reproduce dirty-workspace summary drift, stale current-plan or total-plan metadata after plan completion, and stale focus or roadmap summary text after automated completion updates. Cover both commit-backed and no-scoped-commit fallback paths so the new contract remains truthful even when a plan has incomplete commit metadata.</action>
  <verify>node --test tests/summary-generate.test.cjs tests/state.test.cjs tests/state-session-mutator.test.cjs tests/workflow.test.cjs</verify>
  <done>- Tests fail if summary scaffolds include unrelated dirty-workspace files
- Tests fail if complete-plan leaves stale totals, current-plan fields, focus text, or roadmap summary lines behind
- Workflow coverage proves execute-plan calls the repaired metadata path instead of the drift-prone one</done>
</task>

<task type="auto">
  <name>Task 2: Scope summary generation to active-plan commits or declared plan files</name>
  <files>src/commands/misc.js, bin/bgsd-tools.cjs</files>
  <action>Update `util:summary-generate` so file lists, file-count metrics, and related summary metadata come from plan-owned commits when they exist, and fall back to declared plan task or `files_modified` paths when commit data is absent or incomplete. Preserve existing summary-merge behavior, but stop using ambient workspace noise as the source of truth. Rebuild the bundled CLI after the source change.</action>
  <verify>npm run build && node --test tests/summary-generate.test.cjs</verify>
  <done>- Summary scaffolds prefer plan-scoped commits and use declared plan files as the fallback truth source
- File-count and files-modified sections no longer depend on unrelated dirty working-copy state
- The shipped CLI bundle matches the new summary-generation behavior</done>
</task>

<task type="auto">
  <name>Task 3: Refresh completion metadata from disk and repair stale state or roadmap summaries</name>
  <files>src/commands/state.js, src/lib/state-session-mutator.js, src/commands/roadmap.js, workflows/execute-plan.md</files>
  <action>Rework `verify:state complete-plan` and related roadmap update logic so they recompute active-plan counts from the current phase inventory, refresh current focus and plan totals from disk truth, then run a focused readback that patches stale STATE or ROADMAP summary fields before metadata finalization returns success. Update execute-plan guidance to use the refreshed completion path and make the readback expectation explicit.</action>
  <verify>node --test tests/state.test.cjs tests/state-session-mutator.test.cjs tests/workflow.test.cjs</verify>
  <done>- Completion updates derive current-plan, total-plan, and focus text from on-disk phase truth
- STATE and ROADMAP summary fields are read back and repaired before finalization completes
- Execute-plan guidance reflects the new metadata-repair contract</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `node --test tests/summary-generate.test.cjs tests/state.test.cjs tests/state-session-mutator.test.cjs tests/workflow.test.cjs`
3. In a dirty JJ-style workspace fixture, confirm the generated summary file list and completion totals stay scoped to the active plan and that stale STATE or ROADMAP summary lines are repaired before completion returns success
</verification>

<success_criteria>
- Summary scaffolds and file metrics are derived from plan-scoped commits or declared plan files rather than ambient dirty workspace changes
- Complete-plan refreshes plan totals, current-plan fields, and focus text from on-disk truth before writing metadata
- Final metadata flow performs focused STATE and ROADMAP readback repair so high-level summaries match the completed plan on disk
</success_criteria>

<output>
After completion, create `.planning/phases/166-execution-realism-plan-scoped-completion/166-02-SUMMARY.md`
</output>
