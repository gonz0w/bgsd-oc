---
phase: 166-execution-realism-plan-scoped-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/verify.js
  - src/lib/commandDiscovery.js
  - workflows/plan-phase.md
  - workflows/verify-work.md
  - agents/bgsd-planner.md
  - agents/bgsd-plan-checker.md
  - tests/validate-commands.test.cjs
  - tests/plan-phase-workflow.test.cjs
  - tests/plan.test.cjs
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - VERIFY-04
  - PLAN-02
  - PLAN-03
must_haves:
  truths:
    - "Approval-time planning checks fail when a plan references stale runnable commands, stale file paths, or unavailable validation steps."
    - "Plan analysis flags task-order verification dependencies before execution when a task verifies against artifacts created later in the same plan."
    - "Overscoped plan batches surface an actionable warning or blocker before handoff instead of waiting for executor repair."
    - "Phase verification guidance expands to broader command-family or discoverability surfaces when the phase goal depends on surfaced guidance beyond the directly touched regression file."
  artifacts:
    - path: "src/commands/verify.js"
      provides: "Planning realism checks for task ordering, scope pressure, and approval-time gating"
      contains: "analyzePlanRealism"
    - path: "src/lib/commandDiscovery.js"
      provides: "Runnable command validation against the shipped slash and CLI inventories"
      contains: "validateCommandIntegrity"
    - path: "workflows/plan-phase.md"
      provides: "Planner workflow steps that require the new approval gates before execution handoff"
      contains: "plan-structure"
    - path: "workflows/verify-work.md"
      provides: "Verifier workflow guidance for broader surfaced-guidance checks when phase success depends on discoverability"
      contains: "guidance"
  key_links:
    - from: "workflows/plan-phase.md"
      to: "src/commands/verify.js"
      via: "Approval flow runs the new plan realism checks before treating plans as executable"
      pattern: "plan-structure|analyze"
    - from: "src/lib/commandDiscovery.js"
      to: "tests/validate-commands.test.cjs"
      via: "Command integrity regressions prove stale runnable guidance is caught"
      pattern: "validateCommandIntegrity"
    - from: "workflows/verify-work.md"
      to: "src/commands/verify.js"
      via: "Phase verification guidance names when to expand beyond touched-file regressions"
      pattern: "guidance|surface|phase"
---

<objective>
Make planning and verification flows catch non-executable plans early by validating runnable guidance, real paths, task order, and scope before execution starts.

Purpose: Recent lessons show plans can look structurally valid while still failing at execution time because commands drifted, paths moved, or verification depended on later tasks. This plan closes that realism gap and makes phase-level guidance verification honest when success depends on discoverability surfaces.
Output: Hardened planning or verification validation logic, updated planner or verifier workflow guidance, and focused regressions that block stale or overscoped plans before handoff.
</objective>

> **TDD Decision:** Selected — these planner and verifier behaviors are deterministic approval gates with clear pass/fail outcomes, so targeted regressions should define the contract first.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/165-jj-execution-repo-local-verification/165-03-SUMMARY.md
@workflows/plan-phase.md
@workflows/verify-work.md
@agents/bgsd-planner.md
@agents/bgsd-plan-checker.md
@src/commands/verify.js
@src/lib/commandDiscovery.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regressions for stale plan guidance and execution-risky plan structure</name>
  <files>tests/validate-commands.test.cjs, tests/plan-phase-workflow.test.cjs, tests/plan.test.cjs</files>
  <action>Add focused regressions that fail when plan validation or workflow approval accepts stale runnable commands, stale repo paths, unavailable verify steps, verification commands that depend on later-created artifacts, or obviously overscoped task batches. Include at least one phase-verification scenario where command-family or discoverability success requires checking broader surfaced guidance instead of only the directly touched regression file.</action>
  <verify>node --test tests/validate-commands.test.cjs tests/plan-phase-workflow.test.cjs tests/plan.test.cjs</verify>
  <done>- Tests fail on stale command or path references that would make a plan non-executable
- Tests fail on task-order verification dependencies and overscoped plan batches
- A regression proves broader guidance-surface verification is required when the phase goal depends on surfaced command-family outcomes</done>
</task>

<task type="auto">
  <name>Task 2: Wire approval-time plan realism checks into the shipped validator path</name>
  <files>src/commands/verify.js, src/lib/commandDiscovery.js, bin/bgsd-tools.cjs</files>
  <action>Extend the existing plan analysis and approval path so it validates runnable command examples against the shipped inventories, globs plan or task paths against the current repo, flags verify commands that depend on artifacts created later in the same plan, and surfaces overscope risk using the existing plan-analysis contract instead of a one-off checker-only rule. Rebuild the bundled CLI so planner, checker, and verifier consumers all see the same behavior.</action>
  <verify>npm run build && node --test tests/validate-commands.test.cjs tests/plan.test.cjs</verify>
  <done>- Approval-time validation catches stale commands, stale paths, unavailable validation steps, and task-order verification hazards
- Scope pressure is exposed through the shipped analyzer rather than left to execution-time repair
- `bin/bgsd-tools.cjs` matches the new source-side validation behavior</done>
</task>

<task type="auto">
  <name>Task 3: Update planner and verifier guidance to require the new realism gates</name>
  <files>workflows/plan-phase.md, workflows/verify-work.md, agents/bgsd-planner.md, agents/bgsd-plan-checker.md</files>
  <action>Revise planner, checker, and phase-verification guidance so approval explicitly runs the new realism checks before handoff, and verifier guidance says to expand beyond touched-file regressions when a phase success criterion depends on command-family discoverability or other surfaced guidance. Keep the wording specific to executable-plan realism; do not broaden into unrelated logging or execution-commit concerns.</action>
  <verify>node --test tests/plan-phase-workflow.test.cjs</verify>
  <done>- Planner and checker prompts require the new approval gates instead of relying on structure alone
- Verifier guidance names when broader surfaced-guidance scans are mandatory for phase truth
- Workflow wording stays aligned with the shipped command inventory and analyzer behavior</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `node --test tests/validate-commands.test.cjs tests/plan-phase-workflow.test.cjs tests/plan.test.cjs`
3. Confirm a representative stale-path or stale-command plan now fails before approval, and a discoverability-driven phase check explicitly requires broader surfaced-guidance coverage
</verification>

<success_criteria>
- Planner and checker approval catches stale command examples, stale paths, unavailable validation steps, task-order hazards, and overscoped batches before execution
- Phase verification guidance is explicit about when touched-file regressions are insufficient for command-family or discoverability goals
- The shipped CLI bundle, planner workflow, and checker guidance all rely on the same realism contract
</success_criteria>

<output>
After completion, create `.planning/phases/166-execution-realism-plan-scoped-completion/166-01-SUMMARY.md`
</output>
