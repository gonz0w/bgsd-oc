---
phase: 165-jj-execution-repo-local-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/misc.js
  - src/lib/jj.js
  - tests/misc.test.cjs
  - tests/integration.test.cjs
  - tests/helpers.cjs
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
intent:
  outcome_ids: []
must_haves:
  truths:
    - "`execute:commit` supports a path-scoped commit flow for valid JJ colocated workspace states even when generic Git commit behavior would block detached or dirty cases."
    - "Users get an automatic or explicitly structured JJ-native fallback when the existing Git-first helper rejects a valid execution state instead of a misleading generic failure."
    - "Structured commit results preserve actionable reasons, path scoping, and existing trailer behavior across both primary and fallback commit paths."
  artifacts:
    - path: "src/commands/misc.js"
      provides: "JJ-aware path-scoped execute:commit orchestration"
      contains: "runJjPathScopedCommit"
    - path: "src/lib/jj.js"
      provides: "Shared JJ execution-state and fallback helper logic"
      contains: "jj"
    - path: "tests/misc.test.cjs"
      provides: "Commit-contract regression coverage for blocked, fallback, and structured-result cases"
      contains: "execute:commit"
    - path: "tests/integration.test.cjs"
      provides: "Real JJ temp-repo proof for detached or dirty path-scoped execution commits"
      contains: "execute:commit"
  key_links:
    - from: "src/commands/misc.js"
      to: "src/lib/jj.js"
      via: "JJ execution-state classification and fallback selection"
      pattern: "jj|commit"
    - from: "tests/misc.test.cjs"
      to: "src/commands/misc.js"
      via: "Structured execute:commit reason and fallback regression coverage"
      pattern: "pre_commit_blocked|nothing_to_commit|committed"
    - from: "tests/integration.test.cjs"
      to: "src/commands/misc.js"
      via: "Real JJ temp-repo path-scoped commit proof"
      pattern: "execute:commit"
---

<objective>
Make `execute:commit` trustworthy in real JJ execution repos by adding a supported path-scoped fallback contract for detached or dirty-but-valid colocated states.

Purpose: Phase 165 starts at the execution seam users hit immediately. JJ is already the supported execution backend, so the commit helper must stop treating every Git-only rejection as an unsupportable state when JJ-native execution can still complete the intended task or metadata commit safely.
Output: JJ-aware commit-state handling, a structured fallback contract, and real JJ regression proof for path-scoped execution commits.
</objective>

> **TDD Decision:** Selected - commit fallback behavior and structured reasons are deterministic user-visible contracts with clear failing and passing states.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/165-jj-execution-repo-local-verification/165-RESEARCH.md
@src/commands/misc.js
@src/lib/jj.js
@tests/misc.test.cjs
@tests/integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JJ temp-repo regressions for supported path-scoped commit states</name>
  <files>tests/misc.test.cjs, tests/integration.test.cjs, tests/helpers.cjs</files>
  <action>Add focused tests that distinguish truly blocked commit states from valid JJ colocated states where Git-first commit behavior is too strict. Cover detached and dirty JJ repos with path-scoped `--files` commits, assert the returned structured `reason` values, and keep the current blocked cases for genuinely unsupported repo states so the contract stays explicit instead of becoming a silent force-commit escape hatch.</action>
  <verify>node --test tests/misc.test.cjs tests/integration.test.cjs</verify>
  <done>- Tests fail if `execute:commit` still blocks valid JJ path-scoped commits just because Git commit rejects the state
- Tests fail if fallback loses structured `reason` output or collapses unsupported states into success
- JJ temp-repo coverage proves the phase contract against real execution state instead of fixture-only mocks</done>
</task>

<task type="auto">
  <name>Task 2: Implement JJ-aware commit-state classification and fallback</name>
  <files>src/commands/misc.js, src/lib/jj.js</files>
  <action>Refactor `execute:commit` so path-scoped staging and commit orchestration can classify valid JJ execution states separately from generic Git-only detached or dirty failures. Add a shared helper in `src/lib/jj.js` for the minimal Phase 165 decision boundary, keep `.planning/` as the default commit scope when no files are passed, and perform a JJ-native fallback or explicit supported-path guidance whenever the Git-first path rejects a still-valid JJ execution state.</action>
  <verify>node --test tests/misc.test.cjs tests/integration.test.cjs</verify>
  <done>- `execute:commit` can complete supported path-scoped commits in valid JJ colocated states
- Git-first rejection no longer automatically means unsupported execution state
- Fallback logic is shared and explicit instead of inline ad hoc branching</done>
</task>

<task type="auto">
  <name>Task 3: Preserve trailers and result semantics across both commit paths</name>
  <files>src/commands/misc.js, tests/misc.test.cjs, tests/integration.test.cjs</files>
  <action>Lock the rest of the commit contract while adding the JJ fallback: keep `Agent-Type` and `GSD-Phase` trailers, keep path-scoped file selection, and ensure `committed`, `hash`, and structured `reason` fields remain consistent enough for downstream summary and execution flows. Add targeted tests so later phases can tighten completion logic without re-breaking the commit helper contract.</action>
  <verify>node --test tests/misc.test.cjs tests/integration.test.cjs</verify>
  <done>- Trailer-bearing commits still work after the fallback refactor
- Structured output stays compatible with downstream execution consumers
- The path-scoped commit flow remains explicit and test-locked</done>
</task>

</tasks>

<verification>
1. `node --test tests/misc.test.cjs tests/integration.test.cjs`
2. In a JJ-backed temp repo, a detached or dirty path-scoped `execute:commit` run now completes through the supported contract or returns explicit JJ-native fallback guidance instead of a generic blocked result
3. Existing blocked non-JJ or genuinely unsafe states still return structured failure reasons
</verification>

<success_criteria>
- `execute:commit` supports the Phase 165 JJ path-scoped commit contract in real local execution repos
- Valid JJ fallback states are distinguished from truly blocked repo states
- Structured commit output remains stable for downstream execution and summary flows
</success_criteria>

<output>
After completion, create `.planning/phases/165-jj-execution-repo-local-verification/165-01-SUMMARY.md`
</output>
