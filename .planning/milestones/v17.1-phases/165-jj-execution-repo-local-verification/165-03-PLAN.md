---
phase: 165-jj-execution-repo-local-verification
plan: 03
type: execute
wave: 3
depends_on:
  - "165-02"
files_modified:
  - workflows/execute-phase.md
  - workflows/verify-work.md
  - src/plugin/idle-validator.js
  - plugin.js
  - tests/workflow.test.cjs
  - tests/integration.test.cjs
  - tests/guidance-command-integrity-templates-runtime.test.cjs
autonomous: true
requirements:
  - EXEC-03
  - VERIFY-03
intent:
  outcome_ids: []
must_haves:
  truths:
    - "Workflow and runtime guidance tell users to validate against the repo-local rebuilt runtime when source changes affect generated runtime artifacts."
    - "Shipped runtime surfaces stay aligned with source changes by rebuilding `plugin.js` after runtime-sensitive guidance updates."
    - "End-to-end JJ temp-repo proof shows the local execute and verify flow uses current checkout evidence plus rebuilt runtime guidance instead of stale bundle assumptions."
  artifacts:
    - path: "workflows/verify-work.md"
      provides: "Repo-local verification and rebuild-aware workflow guidance"
      contains: "verify"
    - path: "workflows/execute-phase.md"
      provides: "Execution-side runtime truth guidance that matches the new verifier contract"
      contains: "build"
    - path: "src/plugin/idle-validator.js"
      provides: "Runtime notification source for rebuild-aware local guidance"
      contains: "bgsd-plan phase"
    - path: "plugin.js"
      provides: "Rebuilt shipped runtime bundle aligned with source guidance"
      contains: "Next: /bgsd-plan phase"
  key_links:
    - from: "workflows/verify-work.md"
      to: "workflows/execute-phase.md"
      via: "Shared repo-local runtime truth and rebuild guidance"
      pattern: "build|runtime|verify"
    - from: "src/plugin/idle-validator.js"
      to: "plugin.js"
      via: "Rebuilt shipped runtime parity after source guidance changes"
      pattern: "bgsd-plan phase"
    - from: "tests/workflow.test.cjs"
      to: "workflows/verify-work.md"
      via: "Workflow-level repo-local runtime guidance regression coverage"
      pattern: "verify"
---

<objective>
Close Phase 165 with workflow-level and shipped-runtime proof that repo-local verification guidance matches the rebuilt local runtime.

Purpose: The core verifier changes are not enough if workflow prompts or shipped plugin runtime text still let users trust stale generated artifacts. This plan makes the new local-runtime truth visible on the execution and verification surfaces, then proves the source and built runtime stay aligned in realistic JJ repo flows.
Output: Updated workflow/runtime guidance, rebuilt `plugin.js`, and end-to-end proof that local verification follows current-checkout plus rebuilt-runtime truth.
</objective>

> **TDD Decision:** Selected - workflow/runtime guidance and bundle-parity behavior are deterministic enough to lock with targeted workflow, runtime, and integration regressions.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/165-jj-execution-repo-local-verification/165-RESEARCH.md
@.planning/phases/165-jj-execution-repo-local-verification/165-02-PLAN.md
@workflows/execute-phase.md
@workflows/verify-work.md
@src/plugin/idle-validator.js
@plugin.js
@tests/workflow.test.cjs
@tests/integration.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workflow and runtime regressions for rebuild-aware local guidance</name>
  <files>tests/workflow.test.cjs, tests/guidance-command-integrity-templates-runtime.test.cjs, tests/integration.test.cjs</files>
  <action>Add focused regressions that prove execution and verification workflows now tell users when local source changes require rebuilt runtime validation, and prove the shipped runtime bundle stays aligned with the touched source guidance after rebuild. Include at least one integration path that exercises current-checkout verification semantics in a JJ temp repo so the final phase proof is not source-text-only.</action>
  <verify>node --test tests/workflow.test.cjs tests/guidance-command-integrity-templates-runtime.test.cjs tests/integration.test.cjs</verify>
  <done>- Tests fail if workflow text still allows stale generated runtime assumptions
- Tests fail if source guidance changes are not preserved in rebuilt runtime output
- End-to-end integration proof exists for the repo-local execute or verify contract</done>
</task>

<task type="auto">
  <name>Task 2: Update execution and verify workflows to teach repo-local rebuilt-runtime truth</name>
  <files>workflows/execute-phase.md, workflows/verify-work.md</files>
  <action>Revise the execution and verification workflows so any runtime-sensitive validation step explicitly references repo-local current-state evidence and the rebuilt local runtime when generated artifacts are involved. Keep the guidance practical and phase-scoped: do not reopen broader logging or completion concerns, but do make the repair path obvious when source and local built runtime drift.</action>
  <verify>node --test tests/workflow.test.cjs</verify>
  <done>- Workflow next steps and verification guidance match the new repo-local runtime contract
- Users are told when rebuild is required instead of being left to trust stale local artifacts
- Scope stays inside Phase 165 execution and verification realism</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild plugin runtime output and lock bundle parity</name>
  <files>src/plugin/idle-validator.js, plugin.js, tests/guidance-command-integrity-templates-runtime.test.cjs, tests/integration.test.cjs</files>
  <action>Update the touched plugin runtime source so local notifications and runtime-adjacent guidance stay aligned with the new repo-local verification contract, then rebuild `plugin.js` immediately so shipped runtime text matches source. Add or extend tests that compare the rebuilt bundle against the touched source contract, and use an integration scenario to prove the final local-runtime truth survives the source-to-bundle path users actually invoke.</action>
  <verify>npm run build && node --test tests/guidance-command-integrity-templates-runtime.test.cjs tests/integration.test.cjs</verify>
  <done>- `plugin.js` is rebuilt from the updated source guidance in the same plan
- Source and bundle stay aligned on the repo-local runtime truth contract
- Final phase proof covers both workflow wording and shipped runtime behavior</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `node --test tests/workflow.test.cjs tests/guidance-command-integrity-templates-runtime.test.cjs tests/integration.test.cjs`
3. In a JJ-backed temp repo, the final execute or verify path now points to repo-local current-state evidence and rebuilt local runtime validation when generated artifacts are part of the touched surface
</verification>

<success_criteria>
- Workflow guidance teaches rebuild-aware repo-local verification for runtime-sensitive source changes
- Shipped plugin runtime output is rebuilt and aligned with the touched source contract
- End-to-end proof covers source, workflow, and bundle behavior together instead of in isolation
</success_criteria>

<output>
After completion, create `.planning/phases/165-jj-execution-repo-local-verification/165-03-SUMMARY.md`
</output>
