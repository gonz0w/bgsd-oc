---
phase: 165-jj-execution-repo-local-verification
plan: 02
type: execute
wave: 2
depends_on:
  - "165-01"
files_modified:
  - src/commands/verify.js
  - src/lib/helpers.js
  - src/commands/init.js
  - tests/verify.test.cjs
  - tests/init.test.cjs
  - tests/integration.test.cjs
autonomous: true
requirements:
  - VERIFY-03
  - EXEC-03
intent:
  outcome_ids: []
must_haves:
  truths:
    - "Verification results in the development workspace are based on repo-local current-state evidence from the active checkout instead of stale or ambient runtime assumptions."
    - "Runtime-sensitive source changes fail loudly or require rebuild guidance when the local built runtime no longer matches the checked-out source."
    - "Verifier and init contracts expose enough freshness evidence that downstream workflows can tell the difference between clean repo-local truth and stale runtime state."
  artifacts:
    - path: "src/commands/verify.js"
      provides: "Repo-local deliverables verification and runtime-sensitive freshness enforcement"
      contains: "runtime_freshness"
    - path: "src/lib/helpers.js"
      provides: "Shared current-checkout evidence lookup reused by verifier paths"
      contains: "RUNTIME_FRESHNESS_RULES"
    - path: "src/commands/init.js"
      provides: "Runtime freshness and stale-source metadata exposed to verification entrypoints"
      contains: "stale_sources"
    - path: "tests/verify.test.cjs"
      provides: "Deliverables and repo-local evidence regression coverage"
      contains: "verify deliverables"
  key_links:
    - from: "src/commands/verify.js"
      to: "src/lib/helpers.js"
      via: "Current-checkout artifact and key-link evidence reads"
      pattern: "artifact|key_links|workspace"
    - from: "src/commands/init.js"
      to: "src/commands/verify.js"
      via: "Stale-source and runtime freshness data consumed by verification entrypoints"
      pattern: "stale_sources|resume_summary"
    - from: "tests/verify.test.cjs"
      to: "src/commands/verify.js"
      via: "Repo-local runtime and evidence truth regression coverage"
      pattern: "verdict"
---

<objective>
Make verification tell the truth about the local repo and the runtime users actually invoke.

Purpose: After the commit path is trustworthy, Phase 165 needs verifier results to stop drifting away from the active checkout. Source-sensitive changes should be validated against repo-local evidence and the correct rebuilt local runtime, not a stale bundle or an implicitly installed surface from somewhere else on the machine.
Output: Repo-local evidence enforcement in verifier and init contracts, plus regression proof for stale-runtime and active-checkout scenarios.
</objective>

> **TDD Decision:** Selected - deliverables verdicts, freshness checks, and stale-runtime guidance are deterministic pass or fail behaviors with clear structured outcomes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/165-jj-execution-repo-local-verification/165-RESEARCH.md
@.planning/phases/165-jj-execution-repo-local-verification/165-01-PLAN.md
@src/commands/verify.js
@src/lib/helpers.js
@src/commands/init.js
@tests/verify.test.cjs
@tests/init.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regressions for repo-local evidence and stale-runtime outcomes</name>
  <files>tests/verify.test.cjs, tests/init.test.cjs, tests/integration.test.cjs</files>
  <action>Add focused tests that prove `verify:verify deliverables` and any verification-init path use repo-local current-state evidence from the active checkout, not stale bundle assumptions. Cover source-sensitive cases where the checked-out source has moved ahead of the local built runtime, and assert the command now fails loudly or returns explicit rebuild guidance rather than quietly reporting a pass.</action>
  <verify>node --test tests/verify.test.cjs tests/init.test.cjs tests/integration.test.cjs</verify>
  <done>- Tests fail if verifier output still treats stale built runtime as trustworthy
- Tests fail if repo-local artifact or key-link checks read the wrong checkout state
- Temp-repo coverage proves the freshness contract in realistic local workflow conditions</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen verifier evidence to the active checkout and rebuilt local runtime</name>
  <files>src/commands/verify.js, src/lib/helpers.js</files>
  <action>Update deliverables verification to anchor artifact, key-link, and runtime-sensitive checks to the active repo state under `cwd`. Reuse shared helper logic for workspace evidence lookups, and add the minimal Phase 165 runtime-freshness contract so source changes that feed built artifacts must be checked against the rebuilt local runtime or produce an explicit non-pass result with actionable repair guidance.</action>
  <verify>node --test tests/verify.test.cjs tests/integration.test.cjs</verify>
  <done>- Deliverables verification uses active-checkout evidence rather than ambient machine state
- Runtime-sensitive source changes cannot quietly pass against stale local artifacts
- Evidence and verdict shaping stay structured and actionable</done>
</task>

<task type="auto">
  <name>Task 3: Surface runtime freshness metadata through init for downstream verify flows</name>
  <files>src/commands/init.js, tests/init.test.cjs, tests/integration.test.cjs</files>
  <action>Extend the relevant init or resume payloads with the freshness data verification flows need so workflow-level consumers can distinguish current repo-local truth from stale sources or stale runtime artifacts. Keep the contract additive and compact, align it with the existing `resume_summary` and `stale_sources` semantics, and lock it with tests so later workflow and plugin surfaces can consume it without re-deriving local runtime state themselves.</action>
  <verify>node --test tests/init.test.cjs tests/integration.test.cjs</verify>
  <done>- Verification entrypoints receive explicit freshness metadata instead of guessing from disk state
- The contract stays additive and compatible with existing init consumers
- Later workflow/runtime surfaces can reuse this metadata without duplicating logic</done>
</task>

</tasks>

<verification>
1. `node --test tests/verify.test.cjs tests/init.test.cjs tests/integration.test.cjs`
2. `verify:verify deliverables` fails or returns explicit rebuild guidance when a touched runtime-sensitive source file is ahead of the local built runtime
3. Repo-local verification in a temp repo reflects the active checkout rather than stale generated artifacts or unrelated machine state
</verification>

<success_criteria>
- Deliverables verification is grounded in repo-local current-state evidence
- Runtime-sensitive source edits cannot hide behind stale built artifacts
- Verification and init payloads share an explicit freshness contract for downstream workflow consumers
</success_criteria>

<output>
After completion, create `.planning/phases/165-jj-execution-repo-local-verification/165-02-SUMMARY.md`
</output>
