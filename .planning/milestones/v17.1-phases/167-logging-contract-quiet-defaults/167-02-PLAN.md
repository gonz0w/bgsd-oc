---
phase: 167-logging-contract-quiet-defaults
plan: 02
type: execute
wave: 2
depends_on:
  - "167-01"
files_modified:
  - src/plugin/logger.js
  - src/plugin/safe-hook.js
  - src/lib/runtime-capabilities.js
  - src/plugin/context-builder.js
  - src/plugin/tool-registry.js
  - src/lib/helpers.js
  - tests/plugin.test.cjs
  - tests/infra.test.cjs
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - LOG-02
must_haves:
  truths:
    - "Successful default runs keep routine diagnostics quiet instead of emitting warning or debug chatter on stderr for normal-path conditions."
    - "Failure paths expose one clear operator-facing message by default, while deeper duplicate or stack-like diagnostics stay behind the explicit verbosity contract or durable log files."
    - "Touched plugin and CLI reliability flows preserve actionable investigation detail when verbosity is enabled without printing contradictory duplicate messages."
  artifacts:
    - path: "src/plugin/logger.js"
      provides: "Shared plugin logger behavior that separates operator-visible failures from verbose diagnostics"
      contains: "write"
    - path: "src/plugin/safe-hook.js"
      provides: "Hook failure reporting that avoids duplicate console and logger noise on the default path"
      contains: "Hook failed"
    - path: "src/lib/runtime-capabilities.js"
      provides: "Compile-cache diagnostics that stay quiet on normal runs and move optional detail behind verbosity"
      contains: "BGSD_COMPILE_CACHE"
    - path: "src/plugin/context-builder.js"
      provides: "Prompt-budget warnings or diagnostics routed through the quieter shared contract"
      contains: "TOKEN_BUDGET"
  key_links:
    - from: "src/plugin/logger.js"
      to: "src/plugin/safe-hook.js"
      via: "Hook failures emit one default-facing notification while detailed context remains in logs or verbose diagnostics"
      pattern: "Hook failed|ERROR|correlation"
    - from: "src/lib/runtime-capabilities.js"
      to: "tests/infra.test.cjs"
      via: "Regression coverage proves invalid or optional runtime diagnostics stay quiet unless the shared contract enables them"
      pattern: "compile-cache|stderr|quiet"
    - from: "src/plugin/context-builder.js"
      to: "src/plugin/logger.js"
      via: "Plugin warning surfaces reuse the quieter logging path instead of direct console noise"
      pattern: "budget|warn|diagnostic"
---

<objective>
Reduce default diagnostic noise in touched reliability flows while preserving one clear failure message and an explicit verbose path for deeper investigation.

Purpose: After the shared contract exists, the remaining problem is noisy default output: duplicate hook failure lines, unconditional warnings, and other routine chatter make successful runs and recoverable failures harder to scan. This plan applies the shared contract to the touched noisy paths so default output is quieter without removing the diagnostics needed for reliability work.
Output: Quiet-default behavior for the touched noisy call sites, non-duplicative failure reporting, rebuilt runtime bundle, and regressions that prove verbose mode still exposes the deeper diagnostics when requested.
</objective>

> **TDD Decision:** Selected — quiet-versus-verbose output behavior is observable and should be locked with stderr-focused regressions before implementation.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/CODEBASE-EFFICIENCY-RELIABILITY-AUDIT-PRD.md
@src/plugin/logger.js
@src/plugin/safe-hook.js
@src/lib/runtime-capabilities.js
@src/plugin/context-builder.js
@src/plugin/tool-registry.js
@src/lib/helpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regressions for quiet default output and non-duplicative failure reporting</name>
  <files>
tests/plugin.test.cjs
tests/infra.test.cjs
  </files>
  <action>Add focused regressions that fail when touched reliability flows print routine warning chatter on successful default runs, emit duplicate hook failure lines, or hide verbose-only investigation detail behind the default path. Cover both a quiet success scenario and a controlled failure scenario with explicit debug enabled.</action>
  <verify>node --test tests/plugin.test.cjs tests/infra.test.cjs</verify>
  <done>- Tests fail when touched successful flows emit avoidable default stderr chatter
- Tests fail when one failure produces duplicate or contradictory operator-facing messages
- Tests prove verbose mode still reveals deeper diagnostics needed for investigation</done>
</task>

<task type="auto">
  <name>Task 2: Route noisy plugin failure and warning paths through the quieter contract</name>
  <files>
src/plugin/logger.js
src/plugin/safe-hook.js
src/plugin/context-builder.js
src/plugin/tool-registry.js
src/lib/helpers.js
  </files>
  <action>Refactor the touched plugin logger and warning call sites so routine success stays quiet, default failure output is concise and non-duplicative, and extra context is left to durable logs or the explicit verbose mode introduced in 167-01. Keep correlation IDs and operator actionability intact, but stop printing the same incident through multiple noisy channels by default.</action>
  <verify>node --test tests/plugin.test.cjs tests/infra.test.cjs</verify>
  <done>- Hook or plugin failures emit one concise default-facing message per incident
- Durable logs still capture the richer context needed for debugging
- Other touched plugin warnings no longer produce avoidable default chatter</done>
</task>

<task type="auto">
  <name>Task 3: Quiet compile-cache and runtime diagnostics while preserving explicit verbose troubleshooting</name>
  <files>
src/lib/runtime-capabilities.js
bin/bgsd-tools.cjs
  </files>
  <action>Apply the shared verbosity contract to the touched runtime-capability diagnostics so invalid or optional compile-cache information does not warn on routine runs, but explicit verbose or debug mode still surfaces the details needed to troubleshoot runtime configuration. Rebuild the bundled CLI after the source update.</action>
  <verify>npm run build && node --test tests/infra.test.cjs</verify>
  <done>- Routine runtime-capability checks stay quiet by default
- Explicit verbose or debug mode still exposes compile-cache diagnostics when requested
- The bundled CLI matches the quieter runtime-diagnostics behavior</done>
</task>

</tasks>

<verification>
1. `npm run build`
2. `node --test tests/plugin.test.cjs tests/infra.test.cjs`
3. Confirm a default success path produces no routine warning chatter, a representative hook failure emits one concise default-facing message, and explicit debug mode reveals the deeper diagnostics without duplicate contradictions
</verification>

<success_criteria>
- Successful default runs across the touched reliability flows stay quiet on stdout and routine stderr
- Failure paths expose one clear default-facing diagnostic while richer detail stays in logs or explicit verbose mode
- Verbose mode still provides the diagnostics needed for reliability investigations without contradictory duplicates
</success_criteria>

<output>
After completion, create `.planning/phases/167-logging-contract-quiet-defaults/167-02-SUMMARY.md`
</output>
