---
phase: 0121-memory-store-migration
plan: 03
type: execute
wave: 3
depends_on: ["0121-02"]
files_modified:
  - tests/memory.test.cjs
  - tests/trajectory.test.cjs
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03

must_haves:
  truths:
    - "Test suite verifies JSON-to-SQLite migration imports all entries with zero data loss"
    - "Test suite verifies dual-write produces entries in both JSON file and SQLite table"
    - "Test suite verifies SQL LIKE search returns correct matches for decisions, lessons, trajectories"
    - "Test suite verifies Map fallback returns null/empty and does not crash"
    - "Test suite verifies bookmarks dual-write and SQLite-first read"
    - "Test suite verifies migration idempotency — second migration does not duplicate entries"
  artifacts:
    - "tests/memory.test.cjs with new describe groups for SQLite migration, dual-write, and SQL search"
    - "tests/trajectory.test.cjs with new describe group for trajectory dual-write"
  key_links:
    - "Tests import PlanningCache and getDb to verify SQLite operations directly"
    - "Tests create temp projects with JSON files, call migration, then verify SQL queries"
    - "Tests verify existing JSON-based behavior is unchanged (regression)"
---

<objective>
Comprehensive test coverage for memory store migration, dual-write, and SQL search.

Purpose: Verify the Phase 121 contract — migration correctness, dual-write reliability, SQL search accuracy, Map fallback safety, and idempotency. These tests validate all three requirements (MEM-01, MEM-02, MEM-03) through observable behavior.

Output: Extended test files with 15-20 new test cases covering migration, dual-write, search, and fallback.
</objective>

<execution_context>
@/home/cam/.config/oc/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/oc/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/memory.test.cjs
@tests/trajectory.test.cjs
@src/lib/db.js
@src/lib/planning-cache.js
@src/commands/memory.js
@.planning/phases/0121-memory-store-migration/0121-01-SUMMARY.md
@.planning/phases/0121-memory-store-migration/0121-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory migration and SQL search tests</name>
  <files>tests/memory.test.cjs</files>
  <action>
Add new describe groups to the existing memory.test.cjs file. Import `getDb`, `closeAll` from db.js and `PlanningCache` from planning-cache.js at the top of the file.

**Group 1: `describe('memory SQLite migration', ...)`**
Tests that verify JSON → SQLite migration (MEM-02):

1. **`migrateMemoryStores imports decisions from JSON`**
   - Create .planning/memory/decisions.json with 3 entries [{summary, phase, timestamp}, ...]
   - Call `cache.migrateMemoryStores(cwd)`
   - Verify return shows `migrated.decisions: 3`
   - Query SQLite directly: SELECT COUNT(*) FROM memory_decisions WHERE cwd = ?
   - Assert count === 3
   - Verify original JSON file is untouched

2. **`migrateMemoryStores imports lessons from JSON`**
   - Same pattern for lessons.json

3. **`migrateMemoryStores imports trajectories from trajectory.json`**
   - Create .planning/memory/trajectory.json (note: singular filename)
   - Include entries with various categories, tags, scope
   - Verify all entries migrated, entry_id column populated

4. **`migrateMemoryStores imports bookmarks from bookmarks.json`**
   - Create bookmarks.json with stack-style entries
   - Verify all migrated, phase/plan/task columns populated

5. **`migration is idempotent — second call does not duplicate`**
   - Call migrateMemoryStores twice
   - Assert counts are same after both calls

6. **`migration handles missing JSON files gracefully`**
   - Call migrateMemoryStores with no .planning/memory/ directory
   - Assert no errors, migrated counts all 0

7. **`migration handles corrupt JSON gracefully`**
   - Write invalid JSON to decisions.json
   - Call migrateMemoryStores
   - Assert decisions skipped, other stores still work

**Group 2: `describe('memory SQL search', ...)`**
Tests that verify SQL-powered search (MEM-01):

8. **`searchMemory finds decisions by keyword`**
   - Insert 5 decisions via writeMemoryEntry, 2 mentioning "auth"
   - Call searchMemory(cwd, 'decisions', 'auth')
   - Assert exactly 2 results returned

9. **`searchMemory filters by phase`**
   - Insert decisions for phases 118, 119, 120
   - Search with phase filter '119'
   - Assert only phase 119 entries returned

10. **`searchMemory returns results ordered by timestamp DESC`**
    - Insert 3 decisions with known timestamps
    - Search all, verify ordering

11. **`searchMemory with limit and offset`**
    - Insert 10 decisions
    - Search with limit=3, verify 3 returned, total reflects actual count

12. **`searchMemory returns null on Map backend`**
    - Create MapDatabase, wrap in PlanningCache
    - Call searchMemory, assert returns null

**Group 3: `describe('memory dual-write via CLI', ...)`**
End-to-end tests using runGsdTools (MEM-03):

13. **`memory write creates entry in both JSON and SQLite`**
    - Call `util:memory write --store decisions --entry '{"summary":"dual-write test"}'`
    - Read JSON file, verify entry exists
    - Query SQLite directly, verify entry exists

14. **`memory write bookmark dual-writes to SQLite`**
    - Write a bookmark entry via CLI
    - Verify in SQLite memory_bookmarks table

15. **`memory read --query uses SQL search`**
    - Write 5 decisions, search for a keyword
    - Verify output includes source field

Use the existing test patterns: `createTempProject()` for temp dirs, `cleanup()` in afterEach, `closeAll()` after each test group. Use string cwds for PlanningCache isolation per Phase 119 pattern.
  </action>
  <verify>npm test 2>&1 | grep -E "(memory|# tests|# pass|# fail)"</verify>
  <done>15+ new memory tests pass covering migration (7), SQL search (5), and dual-write (3). All existing memory tests still pass. Zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Trajectory dual-write tests</name>
  <files>tests/trajectory.test.cjs</files>
  <action>
Add a new describe group to the existing trajectory.test.cjs file.

**`describe('trajectory SQLite dual-write', ...)`**

1. **`trajectory checkpoint dual-writes to SQLite`**
   - Use initGitForCheckpoint helper (already exists in this test file)
   - Run `execute:trajectory checkpoint my-dual-test`
   - Verify trajectory.json has the entry
   - Query SQLite: SELECT * FROM memory_trajectories WHERE entry_id LIKE 'tj-%' AND cwd = ?
   - Assert at least 1 row exists with category='checkpoint'

2. **`trajectory dual-write survives SQLite failure gracefully`**
   - Create a scenario where SQLite is unavailable (e.g., BGSD_CACHE_FORCE_MAP=1)
   - Run trajectory checkpoint
   - Verify JSON still written, no crash
   - Verify trajectory.json has the entry

3. **`searchMemory finds trajectory entries by text`**
   - Write 3 trajectory entries directly via writeMemoryEntry with different text
   - Search for a keyword in text
   - Assert correct entries returned

4. **`searchMemory filters trajectories by category`**
   - Write entries with categories: checkpoint, decision, observation
   - Search with category filter
   - Assert only matching category returned

Import `getDb`, `closeAll`, `PlanningCache` at top. Use existing test patterns. Call `closeAll()` in afterEach to clean up SQLite connections.

Note: Trajectory checkpoint tests are slow (they run npm test internally for metrics). Limit to tests that are fast — the dual-write test can skip the metric-collecting checkpoint and instead use writeMemoryEntry directly for most tests.
  </action>
  <verify>npm test 2>&1 | grep -E "(trajectory|# tests|# pass|# fail)"</verify>
  <done>4+ new trajectory dual-write tests pass. Existing trajectory tests unchanged. Zero failures across full test suite.</done>
</task>

</tasks>

<verification>
- `npm test` — full suite passes with zero failures
- New test count: 15-20 additional tests across memory.test.cjs and trajectory.test.cjs
- Tests cover all 3 requirements:
  - MEM-01: SQL search tests (searchMemory with query, phase filter, limit)
  - MEM-02: Migration tests (JSON → SQLite import, idempotency, data integrity)
  - MEM-03: Dual-write tests (bookmark, decision, trajectory entries in both stores)
</verification>

<success_criteria>
- Full test suite passes with zero failures
- Migration correctness verified (entry counts, data integrity, idempotency)
- SQL search accuracy verified (keyword match, phase filter, ordering)
- Dual-write reliability verified (both stores populated)
- Map fallback safety verified (null returns, no crashes)
</success_criteria>

<output>
After completion, create `.planning/phases/0121-memory-store-migration/0121-03-SUMMARY.md`
</output>
