---
phase: 118-foundation-schema
plan: 03
type: execute
wave: 2
depends_on:
  - 0118-01
files_modified:
  - tests/db.test.cjs
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
outcome_ids:
  - DO-79

must_haves:
  truths:
    - "Test suite verifies SQLiteDatabase creates .cache.db with correct schema version"
    - "Test suite verifies MapDatabase provides identical interface to SQLiteDatabase"
    - "Test suite verifies migration runner upgrades schema version correctly"
    - "Test suite verifies migration failure triggers delete-and-rebuild"
    - "Test suite verifies WAL mode and busy_timeout are set on SQLite connections"
    - "Test suite verifies getDb() does not create .planning/ directory"
    - "All existing tests continue to pass — zero regressions"
  artifacts:
    - path: "tests/db.test.cjs"
      provides: "Comprehensive test suite for src/lib/db.js"
      contains: "describe.*db|getDb|SQLiteDatabase|MapDatabase|migration"
  key_links:
    - from: "tests/db.test.cjs"
      to: "src/lib/db.js"
      via: "require('../src/lib/db')"
      pattern: "require.*lib/db"
---

<objective>
Create a comprehensive test suite for the database abstraction layer, verifying all four phase requirements (FND-01 through FND-04) through automated tests.

Purpose: The database module is foundational infrastructure — all subsequent phases depend on it. Thorough testing prevents regressions and validates the dual-backend contract, migration system, and concurrency safety.

Output: `tests/db.test.cjs` — comprehensive test file following the project's existing test patterns.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0118-foundation-schema/0118-01-SUMMARY.md
@src/lib/db.js
@tests/infra.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests for getDb() factory and SQLiteDatabase backend</name>
  <files>tests/db.test.cjs</files>
  <action>
Create `tests/db.test.cjs` following the project's existing test patterns (see `tests/infra.test.cjs` for conventions — Node built-in `node:test` with `describe`/`it`/`assert`).

**Test group 1: Feature detection (`hasSQLiteSupport`)**
- Test that `hasSQLiteSupport()` returns a boolean
- Test that on the current runtime (presumably Node 22.5+), it returns `true`

**Test group 2: `getDb()` factory**
- Test that `getDb(cwd)` returns an object with `.backend` property
- Test that `getDb(cwd)` with a valid `.planning/` directory returns `'sqlite'` backend (on Node 22.5+)
- Test that `getDb(cwd)` without `.planning/` directory returns `'map'` backend (NOT creating `.planning/`)
- Test that calling `getDb(cwd)` twice with same cwd returns the same instance (singleton caching)
- Test that calling `getDb()` with different cwds returns different instances
- Use `fs.mkdtempSync` to create isolated temp directories for each test

**Test group 3: SQLiteDatabase backend (FND-01)**
- Create temp dir with `.planning/` subdirectory
- Call `getDb(tempDir)` — verify backend is `'sqlite'`
- Verify `.planning/.cache.db` file exists on disk
- Verify `getSchemaVersion()` returns 1 (after initial migration)
- Verify `_meta` table exists: `db.prepare("SELECT * FROM _meta WHERE key = 'created_at'").get()` returns a row
- Verify `dbPath` property returns the correct path

**Test group 4: SQLiteDatabase WAL mode and busy_timeout (FND-04)**
- Verify WAL mode: `db.prepare('PRAGMA journal_mode').get().journal_mode === 'wal'`
- Verify busy_timeout: `db.prepare('PRAGMA busy_timeout').get().busy_timeout === 5000`

**Test group 5: Interface parity**
- Both backends expose: `backend`, `dbPath`, `notices`, `getSchemaVersion()`, `exec()`, `prepare()`, `close()`
- Verify MapDatabase has all the same methods/properties as SQLiteDatabase
- Verify calling `exec()` on MapDatabase does not throw
- Verify `prepare()` on MapDatabase returns an object with `.get()`, `.all()`, `.run()` methods

**Cleanup:** After each test group, call `closeAll()` and remove temp directories.
  </action>
  <verify>
Run `npm test` — all tests pass including the new db tests. Run `node --test tests/db.test.cjs` specifically to verify the new file in isolation.
  </verify>
  <done>
Test groups 1-5 all pass. `getDb()` factory, SQLiteDatabase creation, WAL mode, busy_timeout, schema versioning, and interface parity are all verified by automated tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests for migrations, failure recovery, and Map fallback</name>
  <files>tests/db.test.cjs</files>
  <action>
Add additional test groups to `tests/db.test.cjs`:

**Test group 6: Schema migration (FND-02)**
- Verify initial migration: fresh DB starts at version 0, after `getDb()` it's at version 1
- Verify `_meta` table has `created_at` entry from V1 migration
- Verify idempotency: calling `getDb()` again on same DB doesn't re-run migrations (version stays at 1)

**Test group 7: Migration failure → delete-and-rebuild**
- Create a `.cache.db` file with corrupt content (e.g., write random bytes)
- Call `getDb()` — it should detect corruption, delete the file, rebuild from scratch
- Verify the rebuilt database has schema version 1 and `_meta` table
- Verify no error was thrown to the caller

**Test group 8: MapDatabase fallback (FND-03)**
- Test MapDatabase directly: `new MapDatabase()` 
- Verify `backend` is `'map'`, `dbPath` is `null`
- Verify `getSchemaVersion()` returns `0`
- Verify `exec('CREATE TABLE ...')` is a no-op (doesn't throw)
- Verify `prepare('SELECT ...').get()` returns `undefined`
- Verify `prepare('SELECT ...').all()` returns `[]`
- Verify `prepare('INSERT ...').run()` returns `{ changes: 0 }`
- Verify `close()` doesn't throw
- Verify `notices` includes the fallback notice message

**Test group 9: No `.planning/` side effect**
- Create a temp directory WITHOUT `.planning/`
- Call `getDb(tempDir)`
- Verify `.planning/` was NOT created
- Verify the returned backend is `'map'`

**Test group 10: closeAll() cleanup**
- Create multiple db instances with different temp dirs
- Call `closeAll()`
- Verify all instances are cleaned up (calling `getDb()` again creates new instances)

**Important:** Use `afterEach` or `after` hooks to clean up temp directories. All temp dirs should be created under `os.tmpdir()` with unique prefixes to avoid test pollution.
  </action>
  <verify>
Run `npm test` — all tests pass (including existing 1008+ tests and new db tests). Specifically run `node --test tests/db.test.cjs` to see the new test results. Verify zero failures.
  </verify>
  <done>
Test groups 6-10 all pass. Migration system, failure recovery (delete-and-rebuild), MapDatabase fallback, `.planning/` side-effect guard, and closeAll() cleanup are all verified. Full test suite passes with zero regressions.
  </done>
</task>

</tasks>

<verification>
1. `node --test tests/db.test.cjs` — all test groups pass
2. `npm test` — full suite passes with zero failures (existing tests unaffected)
3. Test coverage includes: feature detection, factory, SQLite backend, Map backend, WAL mode, busy_timeout, migrations, failure recovery, interface parity, side-effect guard, cleanup
4. Tests use isolated temp directories — no test pollution
</verification>

<success_criteria>
- All 10 test groups pass on the current Node runtime
- Zero regression in existing test suite (1008+ tests)
- Every phase requirement (FND-01 through FND-04) has at least one dedicated test group
- Tests are self-contained with proper cleanup
</success_criteria>

<output>
After completion, create `.planning/phases/0118-foundation-schema/0118-03-SUMMARY.md`
</output>
