---
phase: 0122-decision-rules
plan: 02
type: execute
wave: 2
depends_on:
  - "0122-01"
files_modified:
  - src/plugin/command-enricher.js
  - src/lib/helpers.js
  - src/lib/orchestration.js
  - src/commands/decisions.js
  - tests/decisions.test.cjs
  - tests/enricher-decisions.test.cjs
autonomous: true
requirements:
  - DEC-01
  - DEC-02
  - DEC-03
  - DEC-04
  - DEC-05
  - DEC-06

must_haves:
  truths:
    - Enricher populates inputs needed by all 6 new/expanded rules before calling evaluateDecisions
    - resolveModelInternal in helpers.js delegates to the model-selection decision rule when db handle is available
    - orchestration.js routeTask uses model-selection rule instead of direct MODEL_PROFILES import
    - decisions:list CLI shows all 17+ rules grouped by category
    - New decision functions have full test coverage (contract checks, edge cases, compound return shapes)
    - Enricher-decision integration tests verify new rules appear in enrichment output
  artifacts:
    - src/plugin/command-enricher.js populates agent_type, verifier_enabled, research_enabled, deps_complete, phases_total, phases_complete, plan_type, is_tdd inputs
    - tests/decisions.test.cjs has describe blocks for all 5 new rules + expanded plan-existence-route
    - tests/enricher-decisions.test.cjs validates new rule results appear in evaluateDecisions output
  key_links:
    - command-enricher.js calls evaluateDecisions() which now exercises all 17+ rules
    - helpers.js resolveModelInternal reads decision result or falls back to static lookup
    - orchestration.js routeTask resolves model via decision rule path
---

<objective>
Wire the 6 new/expanded decision rules into the enricher pipeline, migrate model resolution consumers, and add comprehensive test coverage proving all rules work end-to-end.

Purpose: Complete the integration layer so workflows consume deterministic decisions instead of ad-hoc logic.
Output: Updated enricher with new inputs, migrated model consumers, full test suite for all new rules.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0122-decision-rules/0122-CONTEXT.md
@.planning/phases/0122-decision-rules/0122-01-SUMMARY.md
@src/lib/decision-rules.js
@src/plugin/command-enricher.js
@src/lib/helpers.js
@src/lib/orchestration.js
@src/commands/decisions.js
@tests/decisions.test.cjs
@tests/enricher-decisions.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire enricher inputs for new decision rules</name>
  <files>src/plugin/command-enricher.js, src/lib/helpers.js, src/lib/orchestration.js</files>
  <action>
1. In `src/plugin/command-enricher.js`, add enrichment inputs before the `evaluateDecisions` call (around line 336):

   **For model-selection (DEC-01):**
   - `enrichment.agent_type` — derive from command name mapping (e.g., 'bgsd-execute-phase' → 'bgsd-executor', 'bgsd-plan-phase' → 'bgsd-planner')
   - `enrichment.model_profile` — from config.model_profile (already available as `config`)
   - `enrichment.db` — pass the db handle if available (import getDb from db-cache or use the enricher's existing db reference)

   **For verification-routing (DEC-02):**
   - `enrichment.files_modified_count` — from first incomplete plan's frontmatter files_modified array length
   - `enrichment.verifier_enabled` — from config.verifier (already available via CONFIG_SCHEMA lookup)

   **For research-gate (DEC-03):**
   - `enrichment.research_enabled` — from config.research (already available)
   - `enrichment.phase_has_external_deps` — check if phase description/requirements mention external APIs or new libraries (simple heuristic: false by default, true if phase goal contains 'API', 'external', 'integration')

   **For plan-existence-route expansion (DEC-04):**
   - `enrichment.deps_complete` — check if all depends_on phases are marked complete in roadmap
   - Already has: `plan_count`, `has_research`, `has_context`, `has_blockers`

   **For milestone-completion (DEC-05):**
   - `enrichment.phases_total` — from roadmap phases count
   - `enrichment.phases_complete` — count of complete phases
   - `enrichment.milestone_name` — from roadmap title or first milestone name

   **For commit-strategy (DEC-06):**
   - `enrichment.plan_type` — from first incomplete plan frontmatter.type ('execute' or 'tdd')
   - `enrichment.is_tdd` — boolean, plan_type === 'tdd'
   - Already has: `task_count` (not yet — add if missing: count tasks in first incomplete plan)

2. In `src/lib/helpers.js` `resolveModelInternal`:
   - Try to use the model-selection decision rule first: require decision-rules lazily, call resolveModelSelection with { agent_type, model_profile, db: getDb(cwd) }
   - If rule returns a valid result, return result.value.model (mapping 'opus' to 'inherit' as before)
   - Fall back to existing static lookup on any error (preserves backward compat)

3. In `src/lib/orchestration.js` `routeTask`:
   - Where it currently imports MODEL_PROFILES and does manual lookup (lines 361-376), try the model-selection decision rule first
   - Fall back to existing logic on any error
  </action>
  <verify>
Run `npm run build` to confirm no syntax errors. Run `npm test` to confirm existing tests still pass. Run `node bin/bgsd-tools.cjs decisions:list --raw | node -e "process.stdin.on('data',d=>{const r=JSON.parse(d);console.log('Total rules:',r.summary.total_rules)})"` to confirm rule count.
  </verify>
  <done>
Enricher populates all inputs needed for 6 new/expanded rules. resolveModelInternal delegates to model-selection rule. routeTask uses model-selection rule. evaluateDecisions now evaluates all 17+ rules in-process. No regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive test coverage for new decision rules</name>
  <files>tests/decisions.test.cjs, tests/enricher-decisions.test.cjs</files>
  <action>
1. In `tests/decisions.test.cjs`, add test groups for each new/expanded rule:

   **resolveModelSelection:**
   - Returns { tier, model } with valid tier and model strings
   - Falls back to static defaults when db is null
   - Returns override_model when set in state
   - Contract check: { value, confidence, rule_id }

   **resolveVerificationRouting:**
   - Returns 'full' for high task/file counts
   - Returns 'light' for small plans (<=2 tasks, <=4 files)
   - Returns 'skip' when verifier disabled
   - Contract check

   **resolveResearchGate:**
   - Returns { run: false, depth: null } when research disabled
   - Returns { run: false, depth: null } when research already exists
   - Returns { run: true, depth: 'deep' } for external deps
   - Returns { run: true, depth: 'quick' } for no context
   - Contract check

   **resolvePlanExistenceRoute (expanded):**
   - Old behavior preserved: plan_count > 0 → 'has-plans'
   - New: plan_count > 0 + has_blockers → 'blocked-deps'
   - New: plan_count > 0 + has_context → 'ready'
   - New: no plans, no context, no research → 'missing-context'
   - Old callers (without new inputs) get identical behavior

   **resolveMilestoneCompletion:**
   - Returns { ready: true, action: 'complete' } when all phases done
   - Returns { ready: false, action: 'finish-last-phase' } when one remaining
   - Returns { ready: false, action: 'continue' } otherwise
   - Contract check

   **resolveCommitStrategy:**
   - Returns per-task granularity for multi-task plans
   - Returns per-plan granularity for single-task plans
   - Returns per-phase for TDD plans
   - Returns correct prefix based on plan_type and file patterns
   - Contract check

    Update registry completeness test to expect >= 17 rules.

2. In `tests/enricher-decisions.test.cjs`, add tests:
   - New rules appear in evaluateDecisions output when their inputs are provided
   - model-selection appears when agent_type + model_profile present
   - verification-routing appears when task_count + verifier_enabled present
   - research-gate appears when research_enabled present
   - milestone-completion appears when phases_total present
   - commit-strategy appears when plan_type present
  </action>
  <verify>
Run `npm test` — all tests pass including new ones. Check total test count has increased (should be 1179+ existing + ~30-40 new = 1210+).
  </verify>
  <done>
All 5 new + 1 expanded decision rules have comprehensive test coverage. Contract checks validate { value, confidence, rule_id } shape. Edge cases covered (null db, disabled features, backward compat). Registry test updated to >= 16 rules. Enricher-decision integration tests verify new rules appear in evaluation output. Total test count increased. Zero failures.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm test` passes all tests (1210+)
3. `node bin/bgsd-tools.cjs decisions:list` shows 17+ rules across 5 categories
4. `node bin/bgsd-tools.cjs decisions:evaluate model-selection --state '{"agent_type":"bgsd-planner","model_profile":"balanced"}'` returns `{ value: { tier: "balanced", model: "opus" }, confidence: "HIGH" }`
5. `node bin/bgsd-tools.cjs decisions:evaluate verification-routing --state '{"task_count":1,"files_modified_count":2,"verifier_enabled":true}'` returns `{ value: "light" }`
6. `node bin/bgsd-tools.cjs decisions:evaluate research-gate --state '{"research_enabled":true,"has_research":false,"has_context":false}'` returns compound object
7. `node bin/bgsd-tools.cjs decisions:evaluate milestone-completion --state '{"phases_total":6,"phases_complete":6,"has_incomplete_plans":false}'` returns `{ value: { ready: true, action: "complete" } }`
8. `node bin/bgsd-tools.cjs decisions:savings` shows updated integration point counts
</verification>

<success_criteria>
- Enricher populates all inputs for new rules before evaluateDecisions call
- resolveModelInternal uses model-selection rule with SQLite fallback to static
- routeTask uses model-selection rule with fallback
- decisions:list shows 17+ rules
- All new rules have test coverage (contract + edge cases + integration)
- Total test count >= 1210 with zero failures
- No regressions in existing decision rule behavior
</success_criteria>

<output>
After completion, create `.planning/phases/0122-decision-rules/0122-02-SUMMARY.md`
</output>
