---
phase: 119-parser-integration-planning-tables
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified: [tests/planning-cache.test.cjs]
autonomous: true
requirements: [TBL-01, TBL-02, TBL-03, TBL-04]

must_haves:
  truths:
    - "Tests verify MIGRATIONS[1] creates all 7 tables with correct columns"
    - "Tests verify PlanningCache mtime invalidation: fresh/stale/missing states"
    - "Tests verify storeRoadmap + getPhases round-trip produces correct data (TBL-01)"
    - "Tests verify storePlan + getPlan round-trip with tasks and frontmatter (TBL-02)"
    - "Tests verify requirements stored and queryable by REQ-ID (TBL-03)"
    - "Tests verify mtime change triggers stale detection and re-parse (TBL-04)"
    - "Tests verify MapDatabase backend returns null/empty from all PlanningCache methods"
    - "Tests verify schema version is 2 after migrations"
    - "Full test suite passes with zero regressions"
  artifacts:
    - "tests/planning-cache.test.cjs — comprehensive test suite for schema, cache, invalidation"
  key_links:
    - "Test file imports src/lib/db.js and src/lib/planning-cache.js directly (same pattern as db.test.cjs)"
    - "Test groups use isolated temp directories with .planning/ structure (established pattern from Phase 118)"
    - "Round-trip tests verify frozen object shape matches expected parser output format"
---

<objective>
Create a comprehensive test suite for the planning cache layer — schema migration, mtime invalidation, store/query round-trips for phases, plans, tasks, and requirements, plus MapDatabase fallback.

Purpose: Lock in the PlanningCache contract so future phases can extend without breaking the cache layer. Verify all TBL-01 through TBL-04 requirements are met.
Output: tests/planning-cache.test.cjs with 40+ tests covering all cache operations
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0119-parser-integration-planning-tables/0119-01-PLAN.md
@.planning/phases/0119-parser-integration-planning-tables/0119-02-PLAN.md
@.planning/phases/0118-foundation-schema/0118-03-SUMMARY.md
@src/lib/db.js
@src/lib/planning-cache.js
@tests/db.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests for schema migration and PlanningCache construction</name>
  <files>tests/planning-cache.test.cjs</files>
  <action>
Create `tests/planning-cache.test.cjs` following the patterns established in `tests/db.test.cjs` (isolated temp dirs, closeAll cleanup, direct source imports).

**Test Group 1: Schema Migration (MIGRATIONS[1])**
- Schema version is 2 after getDb() on fresh database
- All 7 tables exist: file_cache, milestones, phases, progress, plans, tasks, requirements
- phases table has correct columns (number, cwd, name, status, plan_count, goal, depends_on, requirements, section)
- tasks table has foreign key to plans(path)
- Indexes exist for common queries (idx_phases_cwd, idx_plans_cwd, etc.)

**Test Group 2: PlanningCache Construction and Backend Detection**
- PlanningCache(sqliteDb) creates instance with working methods
- PlanningCache(mapDb) creates instance where all reads return null/empty
- Constructor does not throw on either backend

**Test Group 3: Mtime Invalidation**
- checkFreshness returns 'missing' for uncached file
- After updateMtime, checkFreshness returns 'fresh'
- After file modification (touch), checkFreshness returns 'stale'
- checkFreshness returns 'missing' for non-existent file path
- invalidateFile removes the file_cache entry
- checkAllFreshness categorizes multiple files correctly
  </action>
  <verify>Run `node --test tests/planning-cache.test.cjs` — all tests in groups 1-3 pass.</verify>
  <done>Test groups 1-3 implemented: schema migration verification, PlanningCache construction for both backends, and mtime invalidation lifecycle (missing → fresh → stale → invalidated).</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for store/query round-trips and MapDatabase fallback</name>
  <files>tests/planning-cache.test.cjs</files>
  <action>
Add test groups 4-8 to the existing test file:

**Test Group 4: Roadmap Store + Query Round-Trip (TBL-01)**
- storeRoadmap with mock parsed roadmap data (phases, milestones, progress)
- getPhases returns correct phase data with frozen objects
- getPhase(number, cwd) returns single phase with correct fields
- getMilestones returns correct milestone data
- getProgress returns correct progress rows
- Re-storing roadmap replaces old data (DELETE + INSERT idempotency)

**Test Group 5: Plan Store + Query Round-Trip (TBL-02)**
- storePlan with mock parsed plan data (frontmatter, tasks, objective)
- getPlan returns plan with correct frontmatter (JSON round-trip)
- getPlan includes tasks array with correct fields
- getPlansForPhase returns plans filtered by phase number
- Task files_json round-trips correctly (array → JSON → array)
- Re-storing plan replaces old data

**Test Group 6: Requirements Store + Query (TBL-03)**
- Requirements extracted from roadmap phase sections during storeRoadmap
- getRequirements returns all requirements for cwd
- getRequirement('TBL-01', cwd) returns correct single requirement
- getRequirement returns null for non-existent REQ-ID
- Requirements include phase_number mapping

**Test Group 7: Invalidation End-to-End (TBL-04)**
- Store roadmap → verify fresh → modify file → verify stale
- After stale detection, invalidateFile clears cached data
- getPhases returns null after invalidation (cache miss)
- Re-store after invalidation works correctly
- clearForCwd removes all data for a cwd

**Test Group 8: MapDatabase Fallback**
- PlanningCache with MapDatabase: checkFreshness returns 'missing'
- PlanningCache with MapDatabase: storeRoadmap is no-op (no throw)
- PlanningCache with MapDatabase: getPhases returns null
- PlanningCache with MapDatabase: getRequirement returns null
- PlanningCache with MapDatabase: storePlan is no-op (no throw)

Run `npm test` at the end to verify zero regressions across the full suite.
  </action>
  <verify>Run `node --test tests/planning-cache.test.cjs` — all tests pass. Run `npm test` — full suite passes with zero failures.</verify>
  <done>Test groups 4-8 implemented: roadmap round-trip, plan round-trip, requirements query, end-to-end invalidation, and MapDatabase fallback. Full test suite passes with zero regressions.</done>
</task>

</tasks>

<verification>
1. `node --test tests/planning-cache.test.cjs` — all tests pass
2. `npm test` — full suite passes with zero failures (no regressions)
3. Tests cover all 4 requirements: TBL-01 (phases query), TBL-02 (plans query), TBL-03 (requirements query), TBL-04 (mtime invalidation)
4. Tests verify both SQLite and MapDatabase backends
5. Test isolation: each group uses its own temp directory, cleaned up in after() hooks
</verification>

<success_criteria>
- 40+ tests covering schema, mtime, store/query, invalidation, and fallback
- All tests pass in isolation and in the full suite
- Every TBL-XX requirement has at least one dedicated test group
- Test patterns consistent with established db.test.cjs conventions
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/0119-parser-integration-planning-tables/0119-03-SUMMARY.md`
</output>
