---
phase: 107-unused-exports
plan: 02
type: execute
wave: 2
depends_on:
  - 107-01
files_modified: []
autonomous: false
requirements:
  - UNUSED-02
  - UNUSED-03

must_haves:
  truths:
    - "Unused exports identified by comparing exports vs imports"
    - "Protected exports are filtered out (not marked as unused)"
    - "Each unused export has file path and line number"
    - "Unverified exports are marked before removal"
    - "Removal verified - no runtime errors"
  artifacts:
    - path: ".planning/phases/107-unused-exports/unused-exports.json"
      provides: "List of exports not imported anywhere"
      contains: "Array of {file, exportName, exportType, line}"
    - path: ".planning/phases/107-unused-exports/verified-removals.json"
      provides: "Exports confirmed safe to remove"
      contains: "Array of exports approved for removal"
  key_links:
    - from: "exports-inventory.json"
      to: "unused-exports.json"
      via: "Cross-reference with imports-inventory.json"
      pattern: "export.*not.*import"
---

<objective>
Identify unused exports and safely remove them after verification.

Purpose: Complete the cleanup by finding and removing dead exports
Output: Cleaned up src/ with unused exports removed
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-unused-exports/107-01-PLAN.md
@.planning/phases/107-unused-exports/exports-inventory.json
@.planning/phases/107-unused-exports/imports-inventory.json
@.planning/phases/107-unused-exports/protected-exports.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identify unused exports</name>
  <files>.planning/phases/107-unused-exports/unused-exports.json</files>
  <action>
# Cross-reference exports vs imports to find unused
1. Load exports-inventory.json
2. Load imports-inventory.json
3. Load protected-exports.json
4. For each export in exports:
   - Check if it's imported anywhere in imports-inventory
   - Check if it's in protected-exports (skip if protected)
   - Mark as unused if neither imported nor protected
5. Output to unused-exports.json with {file, exportName, exportType, line}

Algorithm:
- Build Set of all imported names from imports-inventory
- Iterate exports, filter out protected and imported
- Remaining = unused
  </action>
  <verify>
# Check unused exports identified
node -e "
const unused = require('./.planning/phases/107-unused-exports/unused-exports.json');
console.log('Unused exports found:', unused.length);
console.log('Sample:', JSON.stringify(unused.slice(0,3), null, 2));
"
</verify>
  <done>
- unused-exports.json created
- Contains list of truly unused exports
- Protected exports correctly filtered out
</done>
</task>

<task type="auto">
  <name>Task 2: Validate and mark for removal</name>
  <files>.planning/phases/107-unused-exports/verified-removals.json</files>
  <action>
# Human-verify checkpoint - review unused exports before removal
1. Display unused exports list for review
2. For each unused export:
   - Verify it's truly not used (check with grep as double-check)
   - Confirm not part of dynamic usage pattern
3. Create verified-removals.json with confirmed safe removals

This is a checkpoint task - executor should pause for human review
of the unused exports list before proceeding to removal.
  </action>
  <verify>
# Grep check - verify export is not used anywhere
# For each unused export, run:
grep -r "exportName" src/ --include="*.js" | grep -v "export.*exportName"
</verify>
  <done>
- Each unused export double-checked with grep
- Verified-removals.json contains confirmed removals
</done>
</task>

<task type="auto">
  <name>Task 3: Remove unused exports</name>
  <files>src/**/*.js</files>
  <action>
# Remove verified unused exports from source files
1. Load verified-removals.json
2. For each entry:
   - Read the source file
   - Remove the export declaration
   - Write back the file
3. Handle both ESM (export const X) and CJS (exports.X = )

Track all changes for SUMMARY
  </action>
  <verify>
# Test that CLI still works after removals
node bin/bgsd-tools.cjs help > /dev/null && echo "CLI works"
node bin/bgsd-tools.cjs util help > /dev/null && echo "Utils work"
</verify>
  <done>
- All verified unused exports removed
- CLI functions correctly (help command works)
- No runtime errors
</done>
</task>

</tasks>

<verification>
- All removals verified with grep before deletion
- CLI works after removals (help commands succeed)
- No errors when running basic commands
</verification>

<success_criteria>
- unused-exports.json accurately identifies truly unused exports
- verified-removals.json contains only confirmed safe removals
- All removals completed without breaking CLI functionality
</success_criteria>

<output>
After completion, create `.planning/phases/107-unused-exports/107-02-SUMMARY.md`
</output>
