---
phase: 79-startup-compile-cache-acceleration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [RUNT-01, RUNT-03]
user_setup: []

must_haves:
  truths:
    - "Repeated CLI invocations start faster with warm-cache module compilation"
    - "Compile-cache can be explicitly enabled/disabled via config or environment guard"
    - "Unsupported runtimes automatically fall back to current behavior without breakage"
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "CLI entry point with compile-cache support"
      contains: "compile.*cache|v8.*cache|cache.*flag"
    - path: "src/lib/runtime-capabilities.js"
      provides: "Runtime capability detection for compile-cache support"
      exports: ["detectCompileCacheSupport"]
  key_links:
    - from: "bin/bgsd-tools.cjs"
      to: "src/lib/runtime-capabilities.js"
      via: "require and check at startup"
      pattern: "detectCompileCacheSupport"
---

<objective>
Enable V8 compile-cache to speed up repeated CLI invocations with graceful fallback for unsupported runtimes.

Purpose: Repeated CLI commands (e.g., running `bgsd-tools` multiple times) should start faster by caching module compilation. This builds on Phase 78's discovery optimization — where Phase 78 optimized file I/O, Phase 79 optimizes module parse/compile time.

Output: Compile-cache wrapper with environment/config guard, runtime capability detection, and benchmark evidence.
</objective>

<execution_context>
@/home/cam/.config/oc/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/oc/bgsd-oc/templates/summary.md

This phase follows the established pattern from Phase 77-78: implement optimization behind a flag with proven fallback. The CLI already uses lazy-loading (router.js lines 6-31), so compile-cache complements that by caching the compilation of modules that do get loaded.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-file-discovery-and-ignore-optimization/78-03-SUMMARY.md

# Key context from prior phases
- Phase 78 established: BGSD_DISCOVERY_MODE=legacy for rollback, diagnoseParity() for diagnostics
- Pattern to follow: optimization behind flag with fallback, benchmark evidence, maintainer diagnostics
- Router uses lazy-loading - this phase complements by caching module compilation

# CLI startup current state
- bin/bgsd-tools.cjs: Entry point, ~47KB bundled CLI
- src/router.js: Uses lazy-loading for all command modules
- No current compile-cache wrapper or V8 code cache integration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement compile-cache wrapper with runtime detection</name>
  <files>src/lib/runtime-capabilities.js, bin/bgsd-tools.cjs (or wrapper)</files>
  <action>
1. Create src/lib/runtime-capabilities.js with detectCompileCacheSupport() function that:
   - Detects Node.js version (v8 code cache requires Node 10+)
   - Checks for --experimental-code-cache flag support (Node 10.4+)
   - Returns { supported: boolean, reason: string }

2. Add compile-cache guard to CLI startup:
   - Check BGSD_COMPILE_CACHE env var or config setting
   - If enabled and supported: use --experimental-code-cache
   - If enabled but unsupported: warn and continue without
   - If disabled (default): current behavior preserved

3. Document the guard:
   - BGSD_COMPILE_CACHE=1 to enable
   - BGSD_COMPILE_CACHE=0 to explicitly disable
   - Default: disabled for safety (matches RUNT-03 fallback requirement)

Rationale: Follows Phase 78's pattern of default-off with explicit enable for safety.
  </action>
  <verify>
node -e "const {detectCompileCacheSupport} = require('./src/lib/runtime-capabilities.js'); console.log(JSON.stringify(detectCompileCacheSupport(), null, 2))"</verify>
  <done>
- detectCompileCacheSupport() returns { supported: boolean, reason: string }
- BGSD_COMPILE_CACHE env var is read at startup
- Unsupported runtimes continue without compile-cache (no crash)
</done>
</task>

<task type="auto">
  <name>Task 2: Capture benchmark evidence for startup improvement</name>
  <files>baseline.cjs (or new benchmark file)</files>
  <action>
1. Create baseline.cjs or extend existing to measure CLI startup time:
   - Run bgsd-tools multiple times (e.g., 5 runs with compile-cache, 5 without)
   - Use process.hrtime.bigint() to measure parse+require time
   - Report average warm-start time and speedup percentage

2. Test both enabled and disabled paths:
   - BGSD_COMPILE_CACHE=1 with supported runtime
   - BGSD_COMPILE_CACHE=0 or unset (baseline)
   - Unsupported runtime fallback

3. Save benchmark results to performance.json or similar for RUNT-01 evidence

Rationale: Must provide evidence that repeated invocations are faster (success criteria #1).
  </action>
  <verify>
node baseline.cjs (or benchmark script)</verify>
  <done>
- Benchmark shows measurable warm-start speedup (if supported runtime)
- Benchmark confirms fallback works on unsupported runtimes
- Results documented in performance output
</done>
</task>

<task type="auto">
  <name>Task 3: Document fallback controls and complete phase validation</name>
  <files>src/lib/runtime-capabilities.js (update JSDoc), README.md or docs</files>
  <action>
1. Add comprehensive JSDoc to runtime-capabilities.js documenting:
   - What compile-cache does
   - How to enable/disable (BGSD_COMPILE_CACHE env var)
   - Fallback behavior on unsupported runtimes

2. Ensure error handling is graceful:
   - If compile-cache flag fails: warn and continue
   - If runtime unsupported: silently skip (no user action needed per RUNT-03)

3. Final verification: Run CLI with and without BGSD_COMPILE_CACHE=1, confirm both work

Rationale: Maintainer must be able to control (success criteria #2), unsupported must fallback (success criteria #3).
  </action>
  <verify>
# Both commands should work without error
BGSD_COMPILE_CACHE=0 node bin/bgsd-tools --version
BGSD_COMPILE_CACHE=1 node bin/bgsd-tools --version</verify>
  <done>
- JSDoc documents all guards and fallback behavior
- Both BGSD_COMPILE_CACHE=0 and =1 work correctly
- Unsupported runtime automatically uses fallback (tested in Task 2)
</done>
</task>

</tasks>

<verification>
1. Check that compile-cache support detection works on current Node version
2. Verify BGSD_COMPILE_CACHE=1 uses cache when supported
3. Verify BGSD_COMPILE_CACHE=0 or unset uses current behavior
4. Verify unsupported runtimes get automatic fallback (mock or older Node)
5. Benchmark evidence shows measurable warm-start improvement
</verification>

<success_criteria>
- [ ] RUNT-01: User can get faster repeated CLI invocation startup through compile-cache enablement
- [ ] RUNT-03: User can keep current behavior on unsupported runtimes via automatic fallback paths
- [ ] Success criteria #1: Repeated CLI commands show measurable warm-start speedup (benchmark evidence)
- [ ] Success criteria #2: Maintainer can enable/disable via BGSD_COMPILE_CACHE env var
- [ ] Success criteria #3: Unsupported runtimes see current behavior preserved (automatic fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/79-startup-compile-cache-acceleration/79-01-SUMMARY.md`
</output>
