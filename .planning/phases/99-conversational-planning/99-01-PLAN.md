---
phase: 99-conversational-planning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [NL-05, NL-06, NL-07]
user_setup: []

must_haves:
  truths:
    - "User can describe 'I want to add user authentication' and system extracts requirements through clarifying questions"
    - "User can say 'plan phase 5 and verify it' and system detects two intents (plan, verify) and sequences them"
    - "After completing a command, system suggests next logical actions based on command type"
  artifacts:
    - path: "src/lib/nl/requirement-extractor.js"
      provides: "Convers goal descriptions to structured requirements with clarifying questions"
      exports: ["extractRequirements", "generateClarifyingQuestions", "parseGoalDescription"]
    - path: "src/lib/nl/multi-intent-detector.js"
      provides: "Detects multiple intents in compound commands"
      exports: ["detectIntents", "parseCompoundCommand", "sequenceIntents"]
    - path: "src/lib/nl/suggestion-engine.js"
      provides: "Provides contextual next-action suggestions based on command type"
      exports: ["getSuggestions", "buildSuggestionChain", "detectCommandType"]
    - path: "src/lib/nl/conversational-planner.js"
      provides: "Main orchestrator combining requirement extraction, multi-intent detection, and suggestions"
      exports: ["parseGoal", "handleCompoundCommand", "getNextSuggestions"]
  key_links:
    - from: "src/lib/nl/conversational-planner.js"
      to: "src/lib/nl/nl-parser.js"
      via: "requires nl-parser for base NL parsing"
      pattern: "require.*nl-parser"
    - from: "src/lib/nl/conversational-planner.js"
      to: "src/lib/nl/requirement-extractor.js"
      via: "orchestrates requirement extraction"
      pattern: "require.*requirement-extractor"
    - from: "src/lib/nl/conversational-planner.js"
      to: "src/lib/nl/multi-intent-detector.js"
      via: "orchestrates multi-intent detection"
      pattern: "require.*multi-intent-detector"
    - from: "src/lib/nl/conversational-planner.js"
      to: "src/lib/nl/suggestion-engine.js"
      via: "orchestrates suggestion generation"
      pattern: "require.*suggestion-engine"
---

<objective>
Build conversational planning layer that converts natural language goals into structured plans with contextual awareness. Implements NL-05 (requirement extraction with clarifying questions), NL-06 (multi-intent detection), and NL-07 (contextual suggestions).

Purpose: Enable users to describe goals naturally and receive intelligent assistance with planning, sequencing, and next-action recommendations.

Output: Four new NL modules in src/lib/nl/ forming the conversational planning capability.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-nl-foundation/98-01-SUMMARY.md
@.planning/phases/98-nl-foundation/98-02-SUMMARY.md
@.planning/phases/99-conversational-planning/99-CONTEXT.md
@src/lib/nl/nl-parser.js
@src/lib/nl/intent-classifier.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requirement-extractor.js module</name>
  <files>src/lib/nl/requirement-extractor.js</files>
  <action>
Create src/lib/nl/requirement-extractor.js that:
- Exports `parseGoalDescription(text)` - Extracts key entities and intent from natural language goal descriptions
- Exports `generateClarifyingQuestions(partialRequirements)` - Generates 1-3 clarifying questions based on ambiguous areas
- Exports `extractRequirements(goalText, answers)` - Converts goal + answers to structured requirements object
- Uses keyword analysis to detect: feature types (auth, API, UI, database), scope indicators (simple, complex, full), missing info
- Question strategy: Ask about scope, priority, dependencies when unclear
- Example: "I want to add user authentication" → questions: "What type of auth (social, email/password, SSO)?", "What's the scope (login only, full user management)?"
  </action>
  <verify>
File exists with all three exports. Module can be required without errors. Basic function tests pass.
  </verify>
  <done>
- parseGoalDescription returns {entities: [], missing: [], confidence: 0.X}
- generateClarifyingQuestions returns array of question objects with {text, options?, type}
- extractRequirements returns {requirements: [], scope, priority} from goal + answers
  </done>
</task>

<task type="auto">
  <name>Task 2: Create multi-intent-detector.js module</name>
  <files>src/lib/nl/multi-intent-detector.js</files>
  <action>
Create src/lib/nl/multi-intent-detector.js that:
- Exports `detectIntents(text)` - Detects if input contains multiple intents (AND, THEN, "and then", comma-separated commands)
- Exports `parseCompoundCommand(text)` - Parses compound command into ordered intent objects
- Exports `sequenceIntents(intents)` - Orders intents respecting dependencies (plan before exec, exec before verify)
- Intent patterns: "plan X and verify it", "plan, execute, and verify phase 5", "run tests then check coverage"
- Uses nl-parser.js from Phase 98 for single-intent parsing, wraps result in multi-intent structure
- Sequencing rules: plan → exec → verify is the standard chain; detect missing links and suggest
  </action>
  <verify>
File exists with all three exports. Module can be required without errors. Test cases for compound commands pass.
  </verify>
  <done>
- detectIntents returns {isCompound: boolean, intents: []}
- parseCompoundCommand returns [{intent: "plan", target: "phase 5"}, {intent: "verify", target: "phase 5"}]
- sequenceIntents returns ordered array respecting plan→exec→verify chain
  </done>
</task>

<task type="auto">
  <name>Task 3: Create suggestion-engine.js module</name>
  <files>src/lib/nl/suggestion-engine.js</files>
  <action>
Create src/lib/nl/suggestion-engine.js that:
- Exports `detectCommandType(lastCommand)` - Determines command category from command name or output
- Exports `getSuggestions(commandType, currentState)` - Returns contextual next-action suggestions
- Exports `buildSuggestionChain(sequence)` - Builds suggestion chain for multi-step sequences
- Suggestion rules:
  - After "plan" → suggest "exec phase X"
  - After "exec" → suggest "verify phase X"
  - After "verify" → suggest next phase or "milestone complete"
  - After "session pause" → suggest "session resume"
  - After failed command → suggest "debug" or help
- State options: currentPhase, lastPhase, milestoneStatus to tailor suggestions
  </action>
  <verify>
File exists with all three exports. Module can be required without errors. Suggestion logic tests pass.
  </verify>
  <done>
- detectCommandType returns {category: "planning"|"execution"|"verification"|"session"|"utility", command: string}
- getSuggestions returns array of {command, reason, priority} sorted by priority
- buildSuggestionChain returns complete suggestion chain for compound operations
  </done>
</task>

<task type="auto">
  <name>Task 4: Create conversational-planner.js orchestrator</name>
  <files>src/lib/nl/conversational-planner.js</files>
  <action>
Create src/lib/nl/conversational-planner.js that:
- Exports `parseGoal(text)` - Main entry: parses goal description, generates clarifying questions or returns structured plan
- Exports `handleCompoundCommand(text)` - Handles compound commands using multi-intent-detector
- Exports `getNextSuggestions(lastCommand, state)` - Returns suggestions using suggestion-engine
- Orchestration:
  1. Check if input is compound command → use multi-intent-detector
  2. Check if input is goal description → use requirement-extractor
  3. Otherwise → use standard nl-parser from Phase 98
  4. After any command → include suggestions via suggestion-engine
- Integrates with nl-parser.js for base parsing capabilities
- Handles edge cases: "plan and execute" (both intents without phase), chain of 3+ intents
  </action>
  <verify>
File exists with all exports. Integration with nl-parser works. End-to-end test cases pass.
  </verify>
  <done>
- parseGoal("I want to add auth") returns {clarifyingQuestions: [...]} or {requirements: {...}}
- parseGoal("plan phase 5 and verify it") returns {intents: [{intent: "plan", target: "phase 5"}, {intent: "verify", target: "phase 5"}]}
- getNextSuggestions returns contextual suggestions based on lastCommand and state
  </done>
</task>

<task type="auto">
  <name>Task 5: Test end-to-end conversational planning</name>
  <files>src/lib/nl/conversational-planner.js</files>
  <action>
Test the complete conversational planning flow:
1. Goal extraction: "I want to add user authentication" → clarifying questions generated
2. Multi-intent: "plan phase 5 and verify it" → two intents detected and sequenced
3. Suggestions: After "exec phase 5" → suggest "verify phase 5"
4. Edge cases:
   - "plan and execute" without phase number → prompt for phase
   - "plan, execute, and verify phase 5" → three intents detected
   - No suggestion if no logical next command
  </action>
  <verify>
All test cases pass. Each scenario produces expected output.
  </verify>
  <done>
All 4 scenarios and edge cases produce correct output with proper suggestions
  </done>
</task>

</tasks>

<verification>
- All 4 NL modules exist in src/lib/nl/
- Each module exports documented functions
- Integration tests pass for goal extraction, multi-intent detection, and suggestions
- Edge cases handled: missing phase numbers, 3+ intents, no logical next action
</verification>

<success_criteria>
- User can describe "I want to add user authentication" and system extracts requirements through clarifying questions
- User can say "plan phase 5 and verify it" and system detects two intents and sequences them
- After completing a command, system suggests next logical actions based on command type
</success_criteria>

<output>
After completion, create `.planning/phases/99-conversational-planning/99-01-SUMMARY.md`
</output>
