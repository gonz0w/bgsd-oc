---
phase: 178-phase-176-hardening-truth-reconciliation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/router.js
  - src/lib/output.js
  - src/lib/output-context.js
  - src/plugin/debug-contract.js
  - tests/phase-176-truth-reconciliation.test.cjs
  - tests/validate-commands.test.cjs
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - CLI-03
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - "Focused proof makes the live Phase 176 hardening state explicit instead of relying on stale summary prose."
    - "Supported planning and settings command paths still have runnable regression proof after any in-scope Phase 178 repair."
    - "Any source fix landed in this slice is limited to a directly implicated hotspot blocker rather than restarting the broader hotspot refactor."
  artifacts:
    - path: "tests/phase-176-truth-reconciliation.test.cjs"
      provides: "Focused regression proof for the live Phase 176 cleanup and hardening story"
      contains: "Phase 176 truth reconciliation"
    - path: "tests/validate-commands.test.cjs"
      provides: "Command-integrity regression coverage for the touched planning and settings routes"
      contains: "Phase 176 canonical routes"
    - path: "bin/bgsd-tools.cjs"
      provides: "Rebuilt CLI bundle matching any in-scope source repair"
      contains: "plan:phase"
  key_links:
    - from: "tests/phase-176-truth-reconciliation.test.cjs"
      to: "src/router.js"
      via: "focused assertions prove the shipped planning/settings proof is tied to the live hotspot implementation instead of summary claims"
      pattern: "plan:phase|plan:milestone|util:config-get|util:config-set"
    - from: "tests/phase-176-truth-reconciliation.test.cjs"
      to: "src/lib/output.js"
      via: "targeted source-backed checks lock the touched output-state behavior that Phase 176 overstated"
      pattern: "global\\._gsd|output-context|compactMode|outputMode"
    - from: "tests/validate-commands.test.cjs"
      to: "bin/bgsd-tools.cjs"
      via: "command-integrity assertions keep the runnable planning/settings command surface aligned after rebuild"
      pattern: "plan|settings|config"
---

<objective>
Establish the authoritative live proof for Phase 176 and land only the smallest directly implicated hardening repair needed to keep the repo trustworthy and mostly functional before the next refactor milestone.

Purpose: Phase 176 currently overclaims both hotspot hardening and regression proof. This slice makes the live state executable and reviewable first, then allows only narrow source changes where a directly implicated blocker is the difference between an honestly documented gap and a trustworthy cleanup baseline.
Output: focused failing-then-passing regressions for the live Phase 176 truth gap, plus any minimal source repair and rebuilt CLI bundle required by that proof.
</objective>

> **TDD Decision:** Selected — this slice owns deterministic behavior and proof for supported planning/settings routes plus any directly implicated hotspot hardening fix, so RED coverage should lock the current contradiction before the narrow repair is made green.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v18.1-MILESTONE-AUDIT.md
@.planning/phases/178-phase-176-hardening-truth-reconciliation/178-CONTEXT.md
@.planning/phases/178-phase-176-hardening-truth-reconciliation/178-RESEARCH.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-01-SUMMARY.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-03-SUMMARY.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-04-SUMMARY.md
@src/router.js
@src/lib/output.js
@src/lib/output-context.js
@src/plugin/debug-contract.js
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED proof for the live Phase 176 contradiction and supported route floor</name>
  <files>
    tests/phase-176-truth-reconciliation.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add a focused failing regression slice that encodes the actual Phase 176 truth gap before any repair lands.

  Cover two things only: the supported planning and settings command floor that SAFE-01 still needs, and the directly implicated Phase 176 hotspot hardening claim that current source still contradicts. Use current source plus runnable proof as authority. Do not broaden into unrelated repo-wide validator backlog, whole-suite stabilization, or adjacent command-family cleanup.
  </action>
  <verify>node --test tests/phase-176-truth-reconciliation.test.cjs tests/validate-commands.test.cjs --test-name-pattern "Phase 176 truth reconciliation|Phase 176 canonical routes"</verify>
  <done>The targeted regressions fail on the current live contradiction or missing proof instead of relying on stale Phase 176 summary language.</done>
</task>

<task type="auto">
  <name>Task 2: Make the focused proof green with only the smallest directly implicated repair</name>
  <files>
    src/router.js
    src/lib/output.js
    src/lib/output-context.js
    src/plugin/debug-contract.js
    tests/phase-176-truth-reconciliation.test.cjs
    tests/validate-commands.test.cjs
    bin/bgsd-tools.cjs
  </files>
  <action>
  Use the RED results to choose the smallest truthful repair path: land a narrow source fix only if it directly removes a blocker on the touched planning/settings or hotspot-hardening path, otherwise keep source edits minimal and tighten the proof boundary to the verified shipped behavior.

  Any code change must stay inside the centrally implicated output/router surfaces and their immediate proof hooks. Do not restart the wider ambient-global eradication effort across unrelated callers. Rebuild `bin/bgsd-tools.cjs` if source modules change so the proof covers the shipped CLI artifact rather than source only.
  </action>
  <verify>npm run build && node --test tests/phase-176-truth-reconciliation.test.cjs tests/validate-commands.test.cjs --test-name-pattern "Phase 176 truth reconciliation|Phase 176 canonical routes"</verify>
  <done>The focused regressions pass, the shipped CLI artifact matches any in-scope repair, and the resulting live Phase 176 proof boundary is truthful and narrowly justified.</done>
</task>

</tasks>

<feature>
  <name>Truthful Phase 176 live proof with narrow hotspot repair</name>
  <files>
    src/router.js
    src/lib/output.js
    src/lib/output-context.js
    src/plugin/debug-contract.js
    tests/phase-176-truth-reconciliation.test.cjs
    tests/validate-commands.test.cjs
    bin/bgsd-tools.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/phase-176-truth-reconciliation.test.cjs tests/validate-commands.test.cjs --test-name-pattern "Phase 176 truth reconciliation|Phase 176 canonical routes"</red>
    <green>npm run build && node --test tests/phase-176-truth-reconciliation.test.cjs tests/validate-commands.test.cjs --test-name-pattern "Phase 176 truth reconciliation|Phase 176 canonical routes"</green>
    <refactor>node --test tests/phase-176-truth-reconciliation.test.cjs tests/validate-commands.test.cjs --test-name-pattern "Phase 176 truth reconciliation|Phase 176 canonical routes"</refactor>
  </tdd-targets>
  <behavior>
    Phase 178 stops depending on stale Phase 176 prose by proving the current planning/settings floor and the directly implicated hotspot-hardening story from the live repo.

    Cases:
      - supported planning command routes still execute their help or focused regression paths after any Phase 178 repair
      - supported settings/config command paths still execute their focused regression paths after any Phase 178 repair
      - the directly implicated hotspot hardening contradiction is either made true in a narrow central path or explicitly bounded by the passing proof without pretending the broader refactor already shipped
      - rebuilt `bin/bgsd-tools.cjs` carries the same proof-backed state as the touched source modules when source code changes
  </behavior>
  <implementation>
    Add a new focused Phase 176 truth-reconciliation regression file, extend the existing command-integrity coverage only where the touched planning/settings floor needs it, and keep any source edits tightly constrained to the central output/router surfaces named by the audit evidence. Treat unrelated ambient-global callers, full-suite failures, and broader hotspot cleanup as deferred.
  </implementation>
</feature>

<verification>
- Run the focused RED slice first so the current Phase 176 contradiction is explicit before any repair.
- Re-run the focused truth-reconciliation and touched command-integrity tests after the narrow repair.
- If source files changed, rebuild the CLI and confirm the same focused proof passes against the shipped bundle.
- Smoke the supported command floor cited by the new regression coverage so planning and settings paths are proven from current runtime behavior, not summary prose.
</verification>

<success_criteria>
- [ ] Phase 178 has focused failing-then-passing proof for the real Phase 176 truth gap instead of relying on old summary claims.
- [ ] Supported planning and settings routes covered by this slice are proven runnable with current source and current shipped CLI output.
- [ ] Any landed source repair stays limited to the directly implicated hotspot blocker and does not reopen the broader command-hotspot refactor campaign.
</success_criteria>

<output>
After completion, create `.planning/phases/178-phase-176-hardening-truth-reconciliation/178-01-SUMMARY.md`
</output>
