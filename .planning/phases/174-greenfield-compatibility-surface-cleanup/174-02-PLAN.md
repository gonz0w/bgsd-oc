---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/planning-cache.js
  - src/commands/init.js
  - src/commands/memory.js
  - tests/memory.test.cjs
  - tests/init.test.cjs
autonomous: true
requirements:
  - CLEAN-01
must_haves:
  truths:
    - "Current memory and init flows do not auto-import legacy JSON memory stores into the canonical cache backend."
    - "Supported memory reads and searches still work against the current store without a migration bridge."
    - "Regression coverage fails if JSON-to-SQLite auto-upgrade behavior is reintroduced on active paths."
  artifacts:
    - path: "src/lib/planning-cache.js"
      provides: "Canonical planning-cache behavior without the one-time memory-store migration bridge"
      contains: "searchMemory"
    - path: "src/commands/memory.js"
      provides: "Memory command flow that reads current stores without auto-importing legacy files"
      contains: "searchMemory"
    - path: "tests/memory.test.cjs"
      provides: "Regression coverage for removing legacy memory-store auto-import"
      contains: "does not auto-import legacy JSON memory stores"
  key_links:
    - from: "tests/memory.test.cjs"
      to: "src/lib/planning-cache.js"
      via: "targeted regressions exercise current cache-backed memory behavior after the migration bridge is removed"
      pattern: "legacy JSON memory stores|searchMemory"
    - from: "tests/init.test.cjs"
      to: "src/commands/init.js"
      via: "init coverage proves the first-access path no longer triggers hidden memory migration"
      pattern: "memory stores"
---

<objective>
Remove the one-time JSON-to-SQLite memory migration bridge from active init and memory flows so obsolete local-state upgrades stop shipping as hidden behavior.

Purpose: Phase 174 should remove migration-only helpers by default, and `migrateMemoryStores()` currently keeps historical local-state imports alive behind current init and memory commands.
Output: canonical memory paths in `planning-cache`, `init`, and `memory` that no longer auto-import legacy JSON stores, plus focused regressions that lock that contract.
</objective>

> **TDD Decision:** Selected — deleting an active migration bridge changes deterministic init and memory behavior, so RED regressions should pin the supported contract before the helper disappears.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@src/lib/planning-cache.js
@src/commands/init.js
@src/commands/memory.js
@tests/memory.test.cjs
@tests/init.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for removing legacy memory-store auto-import</name>
  <files>
    tests/memory.test.cjs
    tests/init.test.cjs
  </files>
  <action>
  Add failing regressions that make the hidden migration bridge visible before deleting it.

  Cover at least these cases: memory search/read paths do not import legacy `.planning/memory/*.json` files into the canonical backend on demand; first-access init flows do not trigger a one-time migration; and current memory commands continue reading supported stores after the bridge is gone. Prefer explicit test names that describe removal of the auto-import behavior, not generic migration wording.
  </action>
  <verify>node --test tests/memory.test.cjs tests/init.test.cjs --test-name-pattern "legacy JSON memory stores|memory migration"</verify>
  <done>The targeted regressions fail because the active init or memory paths still auto-import legacy JSON stores.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the migration bridge and keep only canonical memory flows</name>
  <files>
    src/lib/planning-cache.js
    src/commands/init.js
    src/commands/memory.js
  </files>
  <action>
  Remove `migrateMemoryStores()` and its callers in `init` and `memory` so active paths stop upgrading legacy JSON stores behind the scenes. Keep resilience behavior for supported backends intact, but do not replace the deleted bridge with a hidden import, shadow copy, or inspect-only code path on current mutating/runtime commands.

  Update the targeted tests from Task 1 to prove the supported memory flows still work through the canonical store only.
  </action>
  <verify>npm run test:file -- tests/memory.test.cjs tests/init.test.cjs</verify>
  <done>No active init or memory command auto-imports legacy JSON memory stores, and the remaining memory commands still pass through the supported current backend.</done>
</task>

</tasks>

<feature>
  <name>Remove legacy memory-store auto-import from active paths</name>
  <files>
    src/lib/planning-cache.js
    src/commands/init.js
    src/commands/memory.js
    tests/memory.test.cjs
    tests/init.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/memory.test.cjs tests/init.test.cjs --test-name-pattern "legacy JSON memory stores|memory migration"</red>
    <green>npm run test:file -- tests/memory.test.cjs tests/init.test.cjs</green>
    <refactor>npm run test:file -- tests/memory.test.cjs tests/init.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Supported init and memory commands use only the current canonical stores and no longer auto-import legacy JSON files.

    Cases:
      - first access through init -> no hidden memory migration runs
      - `util:memory` search/read -> current store works without legacy JSON import
      - leftover legacy JSON memory files -> ignored by active paths instead of silently upgraded
  </behavior>
  <implementation>
    Delete the planning-cache migration helper and both call sites together, then rewrite or replace the targeted tests so they lock canonical-store behavior rather than the retired upgrade path.
  </implementation>
</feature>

<verification>
Run `npm run build` after the targeted tests pass so the bundled CLI no longer includes the retired memory migration bridge.
</verification>

<success_criteria>
- [ ] `migrateMemoryStores()` and its active callers are removed together
- [ ] Init and memory commands keep working through current supported stores only
- [ ] Focused regressions fail if legacy JSON auto-import behavior comes back
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-02-SUMMARY.md`
</output>
