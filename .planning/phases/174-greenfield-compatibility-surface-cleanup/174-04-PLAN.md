---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 04
type: tdd
wave: 3
depends_on:
  - "01"
  - "03"
files_modified:
  - templates/config-full.json
  - docs/configuration.md
  - docs/expert-guide.md
  - docs/troubleshooting.md
  - tests/guidance-docs.test.cjs
autonomous: true
requirements:
  - CLEAN-03
must_haves:
  truths:
    - "Published config examples and docs teach the supported workspace-first model instead of `worktree` settings or migration-era guidance."
    - "Troubleshooting content no longer tells users to run retired compatibility helpers."
    - "Guidance regressions fail if worktree-era or migration-era config teaching reappears in shipped docs or templates."
  artifacts:
    - path: "templates/config-full.json"
      provides: "Full shipped config template aligned to the supported workspace-first schema"
      contains: "workspace"
    - path: "docs/configuration.md"
      provides: "Canonical configuration reference with workspace-first guidance"
      contains: "workspace.base_path"
    - path: "tests/guidance-docs.test.cjs"
      provides: "Regression coverage for removing worktree-era and migration-era guidance from shipped docs"
      contains: "workspace-first configuration docs"
  key_links:
    - from: "tests/guidance-docs.test.cjs"
      to: "docs/configuration.md"
      via: "guidance assertions prove the published config reference no longer teaches worktree-era settings"
      pattern: "workspace|worktree"
    - from: "tests/guidance-docs.test.cjs"
      to: "templates/config-full.json"
      via: "template assertions prove the shipped full config example matches the supported workspace schema"
      pattern: "workspace"
---

<objective>
Rewrite shipped config guidance so docs and templates teach only the supported JJ/workspace-first model after compatibility surfaces are removed.

Purpose: Phase 174 should leave users reading one canonical story, but `config-full.json`, configuration docs, expert docs, and troubleshooting still teach worktree-era settings or migration helpers that the runtime no longer supports.
Output: shipped docs and templates aligned to the workspace-first schema, plus focused guidance regressions that block stale compatibility teaching from returning.
</objective>

> **TDD Decision:** Selected — these doc and template surfaces define a deterministic published contract, and focused regressions can lock the exact supported guidance before rewriting it.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@templates/config-full.json
@docs/configuration.md
@docs/expert-guide.md
@docs/troubleshooting.md
@tests/guidance-docs.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED guidance regressions for workspace-first config teaching</name>
  <files>
    tests/guidance-docs.test.cjs
  </files>
  <action>
  Add focused failing guidance tests for the shipped docs and templates touched by this plan.

  Cover at least these cases: `templates/config-full.json` uses `workspace`, not `worktree`; `docs/configuration.md` and `docs/expert-guide.md` no longer publish `worktree.*` settings as supported config; and `docs/troubleshooting.md` no longer recommends `util:config-migrate`. Keep the assertions narrow to Phase 174's supported guidance contract.
  </action>
  <verify>node --test tests/guidance-docs.test.cjs --test-name-pattern "workspace-first configuration docs|config migration guidance"</verify>
  <done>The targeted guidance regressions fail because one or more shipped docs or templates still teach worktree-era or migration-era config behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs and templates to the canonical workspace-first model</name>
  <files>
    templates/config-full.json
    docs/configuration.md
    docs/expert-guide.md
    docs/troubleshooting.md
  </files>
  <action>
  Update the touched templates and docs so they teach only the supported JJ/workspace-first config model. Remove published `worktree` examples, descriptions, and migration-helper instructions instead of keeping maintainer-only compatibility notes.

  Keep the docs aligned to the actual supported runtime and schema after Plan 01 and Plan 03 land. Do not reopen the deferred inspect/health diagnostic lane or invent historical caveat text for obsolete local setups.
  </action>
  <verify>node --test tests/guidance-docs.test.cjs</verify>
  <done>The touched docs and templates consistently teach the supported workspace-first model and no longer advertise worktree-era config or migration helpers.</done>
</task>

</tasks>

<feature>
  <name>Canonical workspace-first config guidance</name>
  <files>
    templates/config-full.json
    docs/configuration.md
    docs/expert-guide.md
    docs/troubleshooting.md
    tests/guidance-docs.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/guidance-docs.test.cjs --test-name-pattern "workspace-first configuration docs|config migration guidance"</red>
    <green>node --test tests/guidance-docs.test.cjs</green>
    <refactor>node --test tests/guidance-docs.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Published docs and templates show the supported workspace-first config contract and no longer teach worktree-era settings or config migration helpers.

    Cases:
      - shipped full config template -> `workspace` settings only
      - configuration and expert docs -> workspace-first wording, no `worktree.*` settings table
      - troubleshooting -> canonical validation/edit flow, no `util:config-migrate`
  </behavior>
  <implementation>
    Update the shipped docs and full config template together, then lock the exact guidance contract with one focused docs test file rather than scattering broad repo-wide prose checks.
  </implementation>
</feature>

<verification>
Run `rg -n "worktree|util:config-migrate" docs/configuration.md docs/expert-guide.md docs/troubleshooting.md templates/config-full.json` and expect no matches after the targeted guidance tests pass.
</verification>

<success_criteria>
- [ ] Full shipped config examples use the supported workspace schema only
- [ ] Published docs no longer describe worktree-era config as supported behavior
- [ ] Troubleshooting no longer points users at retired migration helpers
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-04-SUMMARY.md`
</output>
