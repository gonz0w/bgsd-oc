---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 06
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - src/plugin/parsers/roadmap.js
  - tests/plugin.test.cjs
autonomous: true
gap_closure: true
requirements:
  - CLEAN-02
must_haves:
  truths:
    - "The plugin roadmap parser no longer ships dead legacy TDD normalization helpers behind the canonical CLI behavior."
    - "Plugin-side roadmap parsing stays non-normalizing and aligned with the canonical planning read contract from Plan 03."
    - "Regression coverage fails if plugin roadmap parsing reintroduces hidden normalization helpers or rewrite-on-read behavior."
  artifacts:
    - path: "src/plugin/parsers/roadmap.js"
      provides: "Plugin roadmap parser with no leftover legacy normalization helper surface"
      contains: "parseRoadmap"
    - path: "tests/plugin.test.cjs"
      provides: "Targeted regression coverage for canonical plugin roadmap parsing without hidden normalization"
      contains: "parseRoadmap"
  key_links:
    - from: "tests/plugin.test.cjs"
      to: "src/plugin/parsers/roadmap.js"
      via: "plugin parser assertions prove canonical roadmap reads stay non-normalizing and no dead helper path survives"
      pattern: "parseRoadmap|normalization"
---

<objective>
Close GAP-174-01 by deleting the leftover plugin roadmap normalization helper surface so Phase 174's canonical planning-read contract is fully true in shipped parser code.

Purpose: Verification proved the active parse path works, but `src/plugin/parsers/roadmap.js` still ships dead compatibility helpers from the old rewrite-on-read behavior. Phase 174 is not done until that touched parser file matches the canonical non-normalizing contract in substance as well as behavior.
Output: a smaller plugin roadmap parser with the dead normalization helper surface removed and targeted regressions that fail if hidden rewrite-on-read behavior returns.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-VERIFICATION.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-03-SUMMARY.md
@src/plugin/parsers/roadmap.js
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add focused RED coverage for the leftover plugin normalization helper surface</name>
  <files>
    tests/plugin.test.cjs
  </files>
  <action>
  Add or tighten targeted parser regressions so this gap is explicit before deleting code.

  Cover at least these cases: the plugin roadmap parser keeps canonical roadmap reads non-normalizing; legacy helper names or helper-driven rewrite paths in `src/plugin/parsers/roadmap.js` are treated as forbidden compatibility residue; and the targeted parser assertions clearly map to GAP-174-01 rather than only proving happy-path parsing still works.
  </action>
  <verify>node --test tests/plugin.test.cjs --test-name-pattern "parseRoadmap|normalization|canonical roadmap"</verify>
  <done>The targeted parser regressions fail because the touched plugin parser still ships the leftover normalization helper surface.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the dead helper surface and keep only canonical plugin roadmap parsing</name>
  <files>
    src/plugin/parsers/roadmap.js
    tests/plugin.test.cjs
  </files>
  <action>
  Delete the leftover normalization helpers and any dead support code from `src/plugin/parsers/roadmap.js` so the file contains only the canonical parse path actually supported after Plan 03. Keep the parser non-normalizing: do not replace the deleted helpers with a hidden compatibility alias, rewrite-on-read branch, or new shadow abstraction.

  Update the focused plugin tests from Task 1 so they prove the shipped parser stays aligned with the canonical CLI planning contract.
  </action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>The plugin roadmap parser no longer ships dead normalization helpers, and targeted parser regressions pass with canonical non-normalizing behavior only.</done>
</task>

</tasks>

<verification>
- Run `node --test tests/plugin.test.cjs --test-name-pattern "parseRoadmap|normalization|canonical roadmap"` in RED to make the leftover helper surface explicit.
- Run `npm run test:file -- tests/plugin.test.cjs` after cleanup to prove the parser still behaves correctly.
- Confirm `src/plugin/parsers/roadmap.js` no longer contains legacy helper names or helper-driven rewrite-on-read code.
- Confirm GAP-174-01 is fully closed without reopening canonical roadmap parsing behavior.
</verification>

<success_criteria>
- GAP-174-01 is closed: `src/plugin/parsers/roadmap.js` no longer ships dead legacy normalization helpers.
- CLEAN-02 is fully satisfied on the touched plugin roadmap parsing surface.
- Targeted plugin parser regression coverage fails if hidden normalization returns.
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-06-SUMMARY.md`.
</output>
