---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 05
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/nl/command-registry.js
  - src/lib/nl/help-fallback.js
  - src/lib/nl/suggestion-engine.js
  - src/lib/nl/conversational-planner.js
  - src/lib/nl/nl-parser.js
  - tests/validate-commands.test.cjs
autonomous: true
requirements:
  - CLEAN-03
must_haves:
  truths:
    - "Hidden NL and help-fallback registries stop mapping users onto non-routed compatibility-era commands."
    - "Fallback command suggestions teach only supported canonical planning, verification, and progress routes."
    - "Regression coverage fails if removed compatibility mappings or stale internal command names reappear."
  artifacts:
    - path: "src/lib/nl/command-registry.js"
      provides: "Natural-language registry aligned to supported routed commands only"
      contains: "plan:phase"
    - path: "src/lib/nl/help-fallback.js"
      provides: "Fallback suggestion surface that stays within the supported command set"
      contains: "verify:state"
    - path: "tests/validate-commands.test.cjs"
      provides: "Command-integrity regressions for stale internal registry and fallback mappings"
      contains: "stale registry commands are rejected from NL fallback surfaces"
  key_links:
    - from: "tests/validate-commands.test.cjs"
      to: "src/lib/nl/command-registry.js"
      via: "command-integrity checks fail when NL phrases still point at unsupported internal commands"
      pattern: "verify:phase|session:progress|roadmap:show|milestone:new"
    - from: "tests/validate-commands.test.cjs"
      to: "src/lib/nl/help-fallback.js"
      via: "fallback-surface regressions prove help suggestions stay within supported canonical routes"
      pattern: "verify:state|session:progress|roadmap:show"
---

<objective>
Prune stale NL and fallback registries so hidden discovery behavior matches the supported canonical command surface instead of preserving compatibility-era mappings.

Purpose: Phase 174 explicitly forbids hidden compatibility behavior on active paths, and the NL registry plus fallback suggestion helpers still map users toward non-routed commands like `verify:phase`, `session:progress`, `roadmap:show`, and `milestone:new`.
Output: NL/discovery helpers aligned to supported canonical commands only, with command-integrity regressions covering the removed mappings.
</objective>

> **TDD Decision:** Selected — registry and fallback mapping cleanup changes deterministic command resolution behavior, so RED tests should pin the exact unsupported mappings before they are removed.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@src/lib/nl/command-registry.js
@src/lib/nl/help-fallback.js
@src/lib/nl/suggestion-engine.js
@src/lib/nl/conversational-planner.js
@src/lib/nl/nl-parser.js
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for stale NL and fallback mappings</name>
  <files>
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add focused failing command-integrity regressions that call out the compatibility-era internal commands Phase 174 should remove.

  Cover at least these cases: NL phrase registries or fallback help surfaces must not point at `verify:phase`, `session:progress`, `roadmap:show`, or `milestone:new`; and supported replacements stay within canonical routed families such as `verify:state`, `/bgsd-inspect progress`, and `/bgsd-plan milestone`. Keep the assertions tied to concrete touched files so regressions stay actionable.
  </action>
  <verify>node --test tests/validate-commands.test.cjs --test-name-pattern "stale registry commands|NL fallback surfaces"</verify>
  <done>The targeted regressions fail because one or more NL or fallback registries still reference unsupported internal command names.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hidden compatibility mappings from NL registries and fallback helpers</name>
  <files>
    src/lib/nl/command-registry.js
    src/lib/nl/help-fallback.js
    src/lib/nl/suggestion-engine.js
    src/lib/nl/conversational-planner.js
    src/lib/nl/nl-parser.js
  </files>
  <action>
  Delete or replace the stale internal mappings so every touched NL or fallback helper points only at supported canonical commands. Remove the obsolete command names instead of keeping hidden aliases, but preserve resilience behavior that still protects supported current flows.

  Make the touched helpers agree on the same canonical replacements so the repo does not keep split-brain discovery behavior behind a cleaner public surface.
  </action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs</verify>
  <done>The touched NL and fallback helpers no longer advertise unsupported internal command names, and the focused command-integrity regressions pass.</done>
</task>

</tasks>

<feature>
  <name>Canonical NL and fallback command mapping</name>
  <files>
    src/lib/nl/command-registry.js
    src/lib/nl/help-fallback.js
    src/lib/nl/suggestion-engine.js
    src/lib/nl/conversational-planner.js
    src/lib/nl/nl-parser.js
    tests/validate-commands.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/validate-commands.test.cjs --test-name-pattern "stale registry commands|NL fallback surfaces"</red>
    <green>npm run test:file -- tests/validate-commands.test.cjs</green>
    <refactor>npm run test:file -- tests/validate-commands.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Hidden NL and fallback registries resolve only to supported canonical commands and stop preserving compatibility-era internal names.

    Cases:
      - stale NL phrase -> no mapping to `verify:phase`, `session:progress`, `roadmap:show`, or `milestone:new`
      - fallback help suggestion -> canonical routed replacement only
      - touched helper maps -> one consistent command vocabulary across registry, parser, and suggestion layers
  </behavior>
  <implementation>
    Remove the stale internal mappings from all touched NL helper layers together and express the supported replacements directly in the same files so no mirrored fallback copy survives.
  </implementation>
</feature>

<verification>
Run `npm run build` after the focused command-integrity tests pass so the bundled runtime picks up the cleaned NL/fallback mappings.
</verification>

<success_criteria>
- [ ] Touched NL and fallback helpers stop mapping to non-routed compatibility-era commands
- [ ] Canonical replacements are consistent across registry, parser, and suggestion helpers
- [ ] Targeted regressions fail if stale hidden command mappings reappear
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-05-SUMMARY.md`
</output>
