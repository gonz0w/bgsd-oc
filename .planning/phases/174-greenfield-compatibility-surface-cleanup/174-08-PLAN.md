---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 08
type: execute
wave: 5
depends_on:
  - "07"
files_modified:
  - src/lib/commandDiscovery.js
  - tests/validate-commands.test.cjs
autonomous: true
gap_closure: true
requirements:
  - CLEAN-03
must_haves:
  truths:
    - "Phase 174 surfaced command guidance passes command-integrity validation without treating internal workflow fallback reconstruction as runnable public guidance."
    - "Removed planning aliases such as `/bgsd-plan-phase` are classified as legacy surfaced guidance rather than generic unknown commands."
    - "Focused command-integrity regressions cover both legacy planning alias classification and the workflow-fallback-context boundary."
  artifacts:
    - path: "src/lib/commandDiscovery.js"
      provides: "Command-integrity validation that distinguishes internal workflow fallback context from surfaced runnable guidance"
      contains: "validateCommandIntegrity"
    - path: "tests/validate-commands.test.cjs"
      provides: "Regression coverage for legacy alias classification and Phase 174 surfaced workflow guidance"
      contains: "Phase 174 canonical command guidance"
  key_links:
    - from: "tests/validate-commands.test.cjs"
      to: "src/lib/commandDiscovery.js"
      via: "focused integrity assertions prove legacy planning aliases and workflow-internal bootstrap examples are classified correctly"
      pattern: "/bgsd-plan-phase|init:plan-phase|init:phase-op"
    - from: "tests/validate-commands.test.cjs"
      to: "workflows/plan-phase.md"
      via: "Phase 174 surfaced guidance stays green while real runnable command mistakes remain enforceable"
      pattern: "/bgsd-plan phase|init:plan-phase"
    - from: "tests/validate-commands.test.cjs"
      to: "workflows/discuss-phase.md"
      via: "Phase 174 surfaced guidance stays green while real runnable command mistakes remain enforceable"
      pattern: "/bgsd-plan discuss|init:phase-op"
---

<objective>
Close GAP-174-RV-01 and GAP-174-RV-02 by making command-integrity validation distinguish internal workflow fallback reconstruction from surfaced runnable guidance, while restoring legacy alias classification for removed planning commands.

Purpose: Phase 174 verification is still red in two tightly related ways. The validator currently treats internal fallback reconstruction lines in `workflows/plan-phase.md` and `workflows/discuss-phase.md` as surfaced runnable commands, and it classifies `/bgsd-plan-phase 159` as a generic unknown command instead of a legacy planning alias. This follow-on slice should make the validation boundary precise without weakening canonical guidance enforcement.

Output: tightened command-integrity rules and focused regression proof for the two remaining verification gaps.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-VERIFICATION.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-07-SUMMARY.md
@src/lib/commandDiscovery.js
@tests/validate-commands.test.cjs
@workflows/plan-phase.md
@workflows/discuss-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock RED coverage for the remaining Phase 174 verification gaps</name>
  <files>
    tests/validate-commands.test.cjs
  </files>
  <action>
  Tighten the focused command-integrity regression coverage so the current failures are explicit before changing validator behavior.

  Cover at least these cases: `/bgsd-plan-phase 159` must be treated as legacy surfaced guidance rather than a generic nonexistent slash command; and the Phase 174 surfaced-guidance slice must only fail for true public drift, not because internal fallback reconstruction blocks in the two planning workflows were misread as runnable user guidance. Keep the assertions traceable to GAP-174-RV-01 and GAP-174-RV-02.
  </action>
  <verify>node --test tests/validate-commands.test.cjs --test-name-pattern "groups actionable issues|Phase 174 canonical command guidance"</verify>
  <done>The focused tests fail for the intended legacy-alias and workflow-fallback-boundary reasons before the validator fix lands.</done>
</task>

<task type="auto">
  <name>Task 2: Fix command-integrity classification for legacy planning aliases and internal workflow fallback context</name>
  <files>
    src/lib/commandDiscovery.js
    tests/validate-commands.test.cjs
  </files>
  <action>
  Narrow the validator so it distinguishes compatibility-era planning aliases from truly unknown commands, and so it recognizes the internal fallback-reconstruction context used in the two planning workflows.

  Preserve current enforcement for real surfaced runnable guidance: do not broadly exempt `/bgsd-plan phase`, `/bgsd-plan discuss`, or `node ... bgsd-tools.cjs ...` examples everywhere. The rule should stay specific to removed planning aliases and to workflow-internal fallback/bootstrap context that exists only to reconstruct hook-bypassed execution.
  </action>
  <verify>node --test tests/validate-commands.test.cjs --test-name-pattern "groups actionable issues|Phase 174 canonical command guidance|stale registry commands|fallback surfaces"</verify>
  <done>The validator reports removed planning aliases as legacy guidance, and the Phase 174 surfaced workflow slice no longer flags the internal fallback/bootstrap lines.</done>
</task>

</tasks>

<verification>
- Run the focused red slice first so the remaining legacy-alias and workflow-boundary failures are explicit.
- Re-run the focused command-integrity slice after the validator change.
- Re-run the touched-file `tests/validate-commands.test.cjs` gate after the validator fix.
- Confirm `validateCommandIntegrity()` reports `/bgsd-plan-phase 159` as `legacy-command`.
- Confirm the surfaced Phase 174 workflow slice is green without weakening validation for real runnable guidance mistakes.
</verification>

<success_criteria>
- GAP-174-RV-01 is closed: `workflows/plan-phase.md` and `workflows/discuss-phase.md` no longer fail surfaced command-integrity validation for internal fallback context.
- GAP-174-RV-02 is closed: removed planning aliases are classified and tested as legacy surfaced guidance.
- CLEAN-03 is fully satisfied across the touched workflow guidance and command-integrity regression surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-08-SUMMARY.md`.
</output>
