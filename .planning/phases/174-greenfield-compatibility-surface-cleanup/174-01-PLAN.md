---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/commands/misc.js
  - src/router.js
  - src/lib/constants.js
  - src/lib/commandDiscovery.js
  - docs/troubleshooting.md
  - docs/expert-guide.md
  - tests/infra.test.cjs
  - tests/integration.test.cjs
  - tests/validate-commands.test.cjs
autonomous: true
requirements:
  - CLEAN-01
must_haves:
  truths:
    - "`util:config-migrate` is no longer routed or advertised as a supported CLI/discovery command."
    - "Canonical config troubleshooting tells users to validate or edit the current config instead of running a migration helper."
    - "Regression coverage fails if the retired config-migrate route, help text, docs, or discovery inventory are reintroduced."
  artifacts:
    - path: "src/router.js"
      provides: "Canonical util command routing without the retired migration route"
      contains: "util:config-set"
    - path: "src/lib/constants.js"
      provides: "Supported util help text after the migration helper is removed"
      contains: "util:config-get"
    - path: "src/lib/commandDiscovery.js"
      provides: "Discovery inventory aligned to routed util commands after config-migrate removal"
      contains: "validate-commands"
    - path: "tests/validate-commands.test.cjs"
      provides: "Regression coverage that discovery/help inventory no longer surfaces config-migrate"
      contains: "config-migrate is absent from discovery inventory"
  key_links:
    - from: "tests/infra.test.cjs"
      to: "src/router.js"
      via: "CLI contract assertions prove the removed route now fails as unknown instead of dispatching"
      pattern: "config-migrate"
    - from: "tests/validate-commands.test.cjs"
      to: "src/lib/commandDiscovery.js"
      via: "command-integrity assertions prove discovery/help inventory no longer lists the retired command"
      pattern: "config-migrate"
---

<objective>
Delete the `util:config-migrate` command surface and its surrounding contract so obsolete config-upgrade behavior stops shipping as a supported path.

Purpose: Phase 174 needs migration-only commands gone by default, and this one currently survives in router, help, docs, and tests even though the greenfield product now expects canonical config files instead of in-place upgrades.
Output: routed CLI, help, discovery inventory, docs, and regression coverage that no longer include `util:config-migrate`.
</objective>

> **TDD Decision:** Selected — removing a user-facing command changes deterministic CLI behavior, help output, and docs contract in ways that should be locked with failing regressions first.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@.planning/research/GREENFIELD-COMPAT-CLEANUP-PRD.md
@src/commands/misc.js
@src/router.js
@src/lib/constants.js
@src/lib/commandDiscovery.js
@docs/troubleshooting.md
@docs/expert-guide.md
@tests/infra.test.cjs
@tests/integration.test.cjs
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for the retired config migration surface</name>
  <files>
    tests/infra.test.cjs
    tests/integration.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <action>
  Add focused failing regressions that capture the Phase 174 contract before deleting code.

  Cover at least these cases: the available-command/help surface no longer lists `util:config-migrate`; command-discovery inventory and command-integrity checks no longer surface it as a valid util command; invoking `util:config-migrate` now returns the normal unknown-command failure path; and the remaining supported config workflow still uses canonical validation/editing commands rather than a migration helper. Name the new tests so the removal intent is explicit.
  </action>
  <verify>node --test tests/infra.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs --test-name-pattern "config-migrate|canonical config|discovery inventory"</verify>
  <done>The targeted regressions fail because the repo still routes or advertises `util:config-migrate` in help, discovery, or docs, so GREEN has a precise contract to satisfy.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the command, help text, and troubleshooting guidance together</name>
  <files>
    src/commands/misc.js
    src/router.js
    src/lib/constants.js
    src/lib/commandDiscovery.js
    docs/troubleshooting.md
    docs/expert-guide.md
  </files>
  <action>
  Delete `cmdConfigMigrate()` and the `util:config-migrate` router/help/discovery entries instead of preserving them as stubs. Update troubleshooting and expert guidance in the same slice so users are pointed at canonical config validation or manual edits, not an obsolete migration command.

  Keep the active config path strict: do not replace the deleted command with rewrite-on-read behavior or any hidden fallback. If a still-supported parser sees an unsupported legacy key, it should continue using explicit rejection patterns rather than reviving migration helpers.
  </action>
  <verify>npm run test:file -- tests/infra.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs</verify>
  <done>The repo has no routed `util:config-migrate` surface, no matching help/discovery entry, and no user-facing docs telling maintainers to run it.</done>
</task>

</tasks>

<feature>
  <name>Retire the config migration command surface</name>
  <files>
    src/commands/misc.js
    src/router.js
    src/lib/constants.js
    src/lib/commandDiscovery.js
    docs/troubleshooting.md
    docs/expert-guide.md
    tests/infra.test.cjs
    tests/integration.test.cjs
    tests/validate-commands.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/infra.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs --test-name-pattern "config-migrate|canonical config|discovery inventory"</red>
    <green>npm run test:file -- tests/infra.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs</green>
    <refactor>npm run test:file -- tests/infra.test.cjs tests/integration.test.cjs tests/validate-commands.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    The CLI no longer exposes `util:config-migrate` as a supported route, help entry, discovery entry, or troubleshooting recommendation.

    Cases:
      - `util:config-migrate --help` or direct invocation -> unknown-command failure rather than dispatching a migration helper
      - util/help/discovery inventory -> no `util:config-migrate` entry remains
      - troubleshooting and expert guidance -> canonical validation/editing flow only, no migration helper recommendation
  </behavior>
  <implementation>
    Remove the migration command implementation and router/constants/discovery references together, then update the touched docs and targeted tests in the same slice so no routed dead surface or stale discovery contract survives.
  </implementation>
</feature>

<verification>
Run `npm run build` so the generated CLI bundle reflects the retired command surface after the targeted tests pass.
</verification>

<success_criteria>
- [ ] `util:config-migrate` no longer exists in routed CLI behavior, help/discovery inventory, or user-facing docs
- [ ] The removed command is not preserved as a stub or fallback alias
- [ ] Targeted regressions fail if `config-migrate` is reintroduced anywhere in the supported contract or discovery surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-01-SUMMARY.md`
</output>
