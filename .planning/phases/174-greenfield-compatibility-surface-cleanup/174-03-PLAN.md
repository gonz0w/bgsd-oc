---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 03
type: tdd
wave: 2
depends_on:
  - "02"
files_modified:
  - src/lib/helpers.js
  - src/commands/init.js
  - src/commands/roadmap.js
  - src/plugin/parsers/roadmap.js
  - tests/init.test.cjs
  - tests/plugin.test.cjs
autonomous: true
requirements:
  - CLEAN-02
must_haves:
  truths:
    - "Active planning readers stop rewriting legacy TDD metadata on read and instead keep canonical files as the only supported shape."
    - "Current canonical ROADMAP and PLAN artifacts still parse and validate without any legacy normalization helper in the read path."
    - "The plugin roadmap parser matches CLI behavior instead of preserving hidden normalization behind the scenes."
    - "Regression coverage fails if roadmap or plan readers start mutating legacy metadata on read again."
  artifacts:
    - path: "src/commands/roadmap.js"
      provides: "Canonical roadmap reads without rewrite-on-read normalization"
      contains: "cmdRoadmapGetPhase"
    - path: "src/plugin/parsers/roadmap.js"
      provides: "Plugin roadmap parser aligned with the canonical CLI roadmap contract"
      contains: "parseRoadmap"
    - path: "tests/init.test.cjs"
      provides: "Regression coverage for canonical plan/roadmap success and legacy TDD metadata rejection without rewriting plan files"
      contains: "canonical roadmap and plan metadata still parse and validate"
  key_links:
    - from: "tests/init.test.cjs"
      to: "src/lib/helpers.js"
      via: "init plan-phase regressions prove helper-layer readers no longer normalize legacy roadmap or plan metadata while canonical files still validate cleanly"
      pattern: "legacy TDD metadata|canonical roadmap and plan metadata still parse and validate"
    - from: "tests/plugin.test.cjs"
      to: "src/plugin/parsers/roadmap.js"
      via: "plugin roadmap regressions prove no mirrored normalization survives behind the CLI"
      pattern: "parseRoadmap"
---

<objective>
Remove rewrite-on-read planning normalization from both CLI and plugin roadmap readers so canonical plan and roadmap shapes are enforced instead of silently rewritten.

Purpose: Phase 174 explicitly locks active planning paths to canonical shapes, and the mirrored TDD metadata normalization in helpers, init, roadmap commands, and plugin parsers is the clearest hidden compatibility behavior still mutating files on read.
Output: canonical-only roadmap and plan readers, plus targeted regressions proving canonical artifacts still parse/validate while the CLI and plugin no longer rewrite legacy TDD metadata behind the scenes.
</objective>

> **TDD Decision:** Selected — changing parser behavior around canonical versus unsupported planning shapes is deterministic I/O logic and should be guarded by RED/GREEN regressions.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/tdd-reference.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@src/lib/helpers.js
@src/commands/init.js
@src/commands/roadmap.js
@src/plugin/parsers/roadmap.js
@tests/init.test.cjs
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED regressions for canonical-only roadmap and plan reads</name>
  <files>
    tests/init.test.cjs
    tests/plugin.test.cjs
  </files>
  <action>
  Replace the old rewrite-on-read expectations with failing regressions that capture the locked Phase 174 behavior.

  Cover at least these cases: canonical ROADMAP and PLAN fixtures still succeed through `init:plan-phase` and plan validation without any rewrite helper; `init:plan-phase` does not rewrite legacy TDD frontmatter or callouts into canonical form on read; roadmap readers stop mutating `ROADMAP.md` to normalize legacy `**TDD:**` hints; and the plugin roadmap parser no longer performs its own mirrored normalization pass behind the CLI. If a supported reader still encounters an explicitly retired shape, assert an unsupported-shape failure or non-mutating rejection rather than a normalized rewrite.
  </action>
  <verify>node --test tests/init.test.cjs tests/plugin.test.cjs --test-name-pattern "legacy TDD metadata|parseRoadmap|canonical roadmap and plan metadata"</verify>
  <done>The targeted regressions fail because the current readers still rewrite roadmap or plan metadata on read, or because canonical fixtures no longer parse and validate cleanly without those helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Remove mirrored normalization from helpers, init, roadmap, and plugin readers</name>
  <files>
    src/lib/helpers.js
    src/commands/init.js
    src/commands/roadmap.js
    src/plugin/parsers/roadmap.js
  </files>
  <action>
  Delete `readRoadmapWithTddNormalization()` and `normalizePhasePlanFilesTddMetadata()` along with the init and roadmap call sites that rely on them. Remove the mirrored plugin-side roadmap normalization in the same slice so no hidden compatibility copy survives.

  Keep the inspect/health-style diagnostic lane narrow and separate: active planning/runtime readers should not rewrite legacy metadata, silently coerce it, or preserve a shadow normalization helper. Use explicit unsupported-shape behavior only where a still-supported reader directly encounters a retired shape, and keep canonical ROADMAP/PLAN inputs on the normal successful parse-and-validate path.
  </action>
  <verify>npm run test:file -- tests/init.test.cjs tests/plugin.test.cjs</verify>
  <done>The CLI and plugin both read canonical roadmap/plan metadata without mutating files on read, canonical fixtures still validate, and no mirrored normalization helper remains.</done>
</task>

</tasks>

<feature>
  <name>Enforce canonical planning metadata without rewrite-on-read</name>
  <files>
    src/lib/helpers.js
    src/commands/init.js
    src/commands/roadmap.js
    src/plugin/parsers/roadmap.js
    tests/init.test.cjs
    tests/plugin.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/init.test.cjs tests/plugin.test.cjs --test-name-pattern "legacy TDD metadata|parseRoadmap|canonical roadmap and plan metadata"</red>
    <green>npm run test:file -- tests/init.test.cjs tests/plugin.test.cjs</green>
    <refactor>npm run test:file -- tests/init.test.cjs tests/plugin.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Planning readers keep canonical roadmap and plan files as the only supported active-path shape, still succeed on canonical parse/validate flows, and never rewrite legacy TDD metadata while reading.

    Cases:
      - canonical roadmap + canonical plan -> `init:plan-phase` and plan validation still succeed without helper-driven normalization
      - legacy plan TDD frontmatter or callout -> explicit unsupported-shape handling or non-mutating rejection, not silent rewrite
      - legacy roadmap `**TDD:**` hint syntax -> no write-back normalization on read
      - plugin roadmap parse -> same canonical-only behavior as the CLI, with no mirrored fallback copy
  </behavior>
  <implementation>
    Remove the helper-layer and plugin-layer normalization helpers, update the init and roadmap readers to use direct canonical reads, and convert the tests from rewrite assertions into canonical-only contract assertions that also lock the positive canonical parse-and-validate path.
  </implementation>
</feature>

<verification>
Run `npm run build` after the targeted tests pass so the generated CLI and plugin bundle both reflect the canonical-only reader behavior.
</verification>

<success_criteria>
- [ ] No active roadmap or plan reader rewrites legacy TDD metadata on read
- [ ] Canonical ROADMAP and PLAN fixtures still parse and validate after the normalization helpers are removed
- [ ] CLI and plugin roadmap readers share the same canonical-only behavior
- [ ] Focused regressions fail if mirrored normalization helpers or write-on-read behavior return
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-03-SUMMARY.md`
</output>
