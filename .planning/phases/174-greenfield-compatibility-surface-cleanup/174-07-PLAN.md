---
phase: 174-greenfield-compatibility-surface-cleanup
plan: 07
type: execute
wave: 4
depends_on:
  - "04"
  - "05"
files_modified:
  - src/lib/nl/command-registry.js
  - src/lib/nl/help-fallback.js
  - src/lib/nl/suggestion-engine.js
  - src/lib/nl/conversational-planner.js
  - src/lib/nl/nl-parser.js
  - docs/commands.md
  - docs/workflows.md
  - workflows/plan-phase.md
  - workflows/discuss-phase.md
  - tests/validate-commands.test.cjs
autonomous: true
gap_closure: true
requirements:
  - CLEAN-03
must_haves:
  truths:
    - "Touched NL and fallback helpers point only at supported canonical command routes instead of unsupported internal command names."
    - "Surfaced docs and workflow guidance used by command-integrity validation match the canonical CLI and planning-family surface."
    - "Expanded command-integrity regressions fail if removed hidden mappings or stale surfaced guidance reappear."
  artifacts:
    - path: "src/lib/nl/command-registry.js"
      provides: "Hidden NL phrase routing aligned to supported canonical commands only"
      contains: "plan:phase"
    - path: "docs/commands.md"
      provides: "Surfaced command reference aligned to the canonical command surface"
      contains: "/bgsd-plan"
    - path: "tests/validate-commands.test.cjs"
      provides: "Regression coverage for hidden and surfaced command-integrity drift"
      contains: "stale registry commands"
  key_links:
    - from: "tests/validate-commands.test.cjs"
      to: "src/lib/nl/help-fallback.js"
      via: "integrity assertions prove fallback suggestions stay within supported canonical routes"
      pattern: "verify:work|session:resume|session:pause|execute:phase|execute:quick"
    - from: "tests/validate-commands.test.cjs"
      to: "docs/commands.md"
      via: "integrity assertions prove surfaced docs and workflows teach the canonical supported commands"
      pattern: "/bgsd-plan|/bgsd-execute-phase|/bgsd-inspect"
---

<objective>
Close GAP-174-02 and GAP-174-03 by finishing the remaining canonical command-integrity cleanup across hidden NL/fallback helpers and the surfaced docs/workflow guidance they drifted from.

Purpose: Verification showed Phase 174 still leaks compatibility-era command drift in two places: hidden NL/fallback helpers still reference unsupported internal command names, and surfaced docs/workflows still fail command-integrity validation against the canonical CLI. This follow-on plan removes both split-brain surfaces in one focused cleanup slice.
Output: touched NL helpers and surfaced guidance that agree on the same canonical command surface, plus expanded command-integrity regressions covering both hidden and surfaced drift.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-CONTEXT.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-RESEARCH.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-VERIFICATION.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-04-SUMMARY.md
@.planning/phases/174-greenfield-compatibility-surface-cleanup/174-05-SUMMARY.md
@src/lib/nl/command-registry.js
@src/lib/nl/help-fallback.js
@src/lib/nl/suggestion-engine.js
@src/lib/nl/conversational-planner.js
@src/lib/nl/nl-parser.js
@docs/commands.md
@docs/workflows.md
@workflows/plan-phase.md
@workflows/discuss-phase.md
@tests/validate-commands.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED command-integrity coverage for the remaining hidden and surfaced drift</name>
  <files>
    tests/validate-commands.test.cjs
  </files>
  <action>
  Expand the focused command-integrity regressions so the remaining verification gaps are explicit before cleanup.

  Cover at least these cases: hidden NL and fallback helpers must not reference unsupported command names still called out in `174-VERIFICATION.md` (`execute:phase`, `execute:quick`, `verify:work`, `session:resume`, `session:pause`, and any other touched stale names you confirm in the same files); and the surfaced command/docs workflow examples cited in the verification gap must use canonical supported commands only. Keep test names tied to GAP-174-02 and GAP-174-03 so the follow-on proof is traceable.
  </action>
  <verify>node --test tests/validate-commands.test.cjs --test-name-pattern "stale registry commands|fallback surfaces|canonical command guidance|Phase 174"</verify>
  <done>The focused command-integrity regressions fail because one or more hidden helpers or surfaced docs/workflows still teach unsupported command names.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unsupported hidden mappings from the touched NL and fallback helpers</name>
  <files>
    src/lib/nl/command-registry.js
    src/lib/nl/help-fallback.js
    src/lib/nl/suggestion-engine.js
    src/lib/nl/conversational-planner.js
    src/lib/nl/nl-parser.js
    tests/validate-commands.test.cjs
  </files>
  <action>
  Delete or replace the unsupported internal command mappings on the touched hidden surfaces so they point only at supported canonical routes. Preserve resilience for supported current flows, but do not keep compatibility-only aliases behind the scenes.

  Make the touched helper files agree on the same replacements rather than fixing one surface while another still advertises stale commands.
  </action>
  <verify>node --test tests/validate-commands.test.cjs --test-name-pattern "stale registry commands|fallback surfaces|Phase 174"</verify>
  <done>The touched NL and fallback helpers no longer advertise unsupported internal command names, and the hidden-surface regressions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite surfaced docs and workflows to the canonical command surface</name>
  <files>
    docs/commands.md
    docs/workflows.md
    workflows/plan-phase.md
    workflows/discuss-phase.md
    tests/validate-commands.test.cjs
  </files>
  <action>
  Update the surfaced guidance files cited in GAP-174-03 so they teach the real canonical command surface first. Remove stale or contradictory command examples instead of preserving them as preferred guidance.

  Keep the touched docs and workflows aligned with the same supported routes used by the hidden helpers after Task 2 so Phase 174 no longer presents a split-brain command story.
  </action>
  <verify>npm run test:file -- tests/validate-commands.test.cjs</verify>
  <done>The touched docs and workflows pass focused command-integrity validation and now match the canonical supported command surface.</done>
</task>

</tasks>

<verification>
- Run the focused RED command-integrity pattern first so the remaining hidden and surfaced drift is explicit before cleanup.
- Re-run the focused `tests/validate-commands.test.cjs` slice after Task 2 and the full touched-file command-integrity suite after Task 3.
- Confirm the touched NL helpers no longer reference unsupported internal command names from GAP-174-02.
- Confirm the surfaced docs/workflows cited in GAP-174-03 now teach only canonical supported commands.
</verification>

<success_criteria>
- GAP-174-02 is closed: touched NL and fallback helpers no longer point at unsupported commands.
- GAP-174-03 is closed: surfaced docs and workflows pass focused canonical command-integrity validation.
- CLEAN-03 is fully satisfied across the touched hidden and surfaced guidance surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/174-greenfield-compatibility-surface-cleanup/174-07-SUMMARY.md`.
</output>
