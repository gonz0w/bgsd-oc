---
phase: 115-cli-command-routing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CMD-04]
user_setup: []

must_haves:
  truths:
    - "User can run util:validate-commands and see accurate results"
    - "audit namespace is recognized by validator"
    - "Subcommand lists in validator match actual router"
  artifacts:
    - path: "src/lib/commandDiscovery.js"
      provides: "Command discovery registry"
      contains: "'audit':"
      contains: "plan:roadmap"
      contains: "plan:milestone"
      contains: "execute:tdd"
      contains: "verify:state"
      contains: "util:git"
  key_links:
    - from: "src/lib/commandDiscovery.js"
      to: "src/router.js"
      via: "command list synchronization"
      pattern: "routerImplementations"
---

<objective>
Fix the command validator (commandDiscovery.js) to accurately reflect the router state.

Purpose: The validator is missing the audit namespace and has 5 stale subcommand lists that don't match the actual router. This causes false positives/negatives in validation.

Output: Validator accurately reflects router state
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FEATURES.md

# Reference files
@src/lib/commandDiscovery.js
@src/router.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit namespace to commandDiscovery.js</name>
  <files>src/lib/commandDiscovery.js</files>
  <action>
1. In src/lib/commandDiscovery.js, find the routerImplementations object (around L341-463)
2. Add 'audit' namespace entry: 'audit': { 'scan': null }
3. Add 'audit' to the analysis category if not present
4. Rebuild: node build.cjs
  </action>
  <verify>node bin/bgsd-tools.cjs util:validate-commands 2>&1 | grep -i audit</verify>
  <done>audit namespace recognized by validate-commands</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale subcommand lists in routerImplementations</name>
  <files>src/lib/commandDiscovery.js</files>
  <action>
Fix these 5 stale entries in routerImplementations:

1. plan:roadmap - Change from ['add', 'insert', 'remove', 'list'] to ['get-phase', 'analyze', 'update-plan-progress']

2. plan:milestone - Change from ['new', 'complete', 'audit', 'gaps'] to ['complete', 'summary', 'info']

3. execute:tdd - Change from ['init', 'red', 'green', 'refactor', 'cycle', 'auto'] to match actual router (dynamic pass-through)

4. verify:state - Remove 'add-todo' from the list (doesn't exist in router)

5. util:git - Change from ['status', 'log', 'diff', 'branch', 'checkout'] to ['log', 'diff-summary', 'blame', 'branch-info', 'rewind', 'trajectory-branch']

Rebuild: node build.cjs
  </action>
  <verify>node bin/bgsd-tools.cjs util:validate-commands 2>&1</verify>
  <done>validate-commands shows no false positives for these namespaces</done>
</task>

</tasks>

<verification>
- Run: node bin/bgsd-tools.cjs util:validate-commands
- Check output shows no "Unknown namespace: audit" error
- Verify subcommand lists match actual router state
</verification>

<success_criteria>
Validator accurately reports command state with no false positives
</success_criteria>

<output>
After completion, create `.planning/phases/115-CLI-command-routing/115-03-SUMMARY.md`
</output>
