---
phase: 115-cli-command-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CMD-03, CMD-06]
user_setup: []

must_haves:
  truths:
    - "User can run builds without bundling orphaned ci.js module"
    - "User sees no duplicate command routes when listing available commands"
    - "execute:profile dead route is removed"
  artifacts:
    - path: "src/commands/ci.js"
      provides: "orphaned module - should be deleted"
      status: "deleted"
    - path: "src/router.js"
      provides: "CLI routing"
      does_not_contain: "execute:profile"
    - path: "src/lib/commandDiscovery.js"
      provides: "Command registry"
      does_not_contain: "standalone runtime"
      does_not_contain: "standalone measure"
  key_links:
    - from: "bin/bgsd-tools.cjs"
      to: "src/commands/ci.js"
      via: "require"
      status: "broken - module removed"
---

<objective>
Remove orphaned ci.js module and deduplicate command routes.

Purpose: The orphaned ci.js (327 lines) is never imported but gets bundled. The standalone runtime/measure commands duplicate util:runtime/util:measure. The execute:profile route is dead code.

Output: Dead code removed, duplicates eliminated
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FEATURES.md

# Reference files
@src/commands/ci.js
@src/router.js
@src/lib/commandDiscovery.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned src/commands/ci.js module</name>
  <files>src/commands/ci.js</files>
  <action>
1. Delete src/commands/ci.js (327 lines, never imported or routed)
2. Verify no references: grep -r "commands/ci\|cmdExecuteCi\|lazyCi" src/
3. Rebuild: node build.cjs

This module is completely orphaned - no lazy loader in router, no route, never imported anywhere.
  </action>
  <verify>ls src/commands/ci.js 2>&1 || echo "File deleted successfully"</verify>
  <done>src/commands/ci.js no longer exists</done>
</task>

<task type="auto">
  <name>Task 2: Remove execute:profile dead route</name>
  <files>src/router.js, src/lib/constants.js, src/lib/commandDiscovery.js</files>
  <action>
1. Find and remove execute:profile case in src/router.js (around L524-526)
2. Remove from COMMAND_HELP in src/lib/constants.js if present
3. Remove from routerImplementations in src/lib/commandDiscovery.js if present
4. Rebuild: node build.cjs
  </action>
  <verify>node bin/bgsd-tools.cjs execute:profile 2>&1 | head -5</verify>
  <done>execute:profile no longer routes (should show unknown command)</done>
</task>

<task type="auto">
  <name>Task 3: Deduplicate runtime and measure routes</name>
  <files>src/router.js, src/lib/constants.js, src/lib/commandDiscovery.js</files>
  <action>
1. In src/router.js: Remove standalone 'runtime' and 'measure' case handlers (keep only util:runtime, util:measure)
2. Remove standalone entries from COMMAND_HELP in src/lib/constants.js
3. Remove standalone entries from routerImplementations in src/lib/commandDiscovery.js
4. Rebuild: node build.cjs

Keep util:runtime and util:measure as the canonical forms. Remove the duplicate standalone commands.
  </action>
  <verify>node bin/bgsd-tools.cjs util:runtime --help 2>&1 | head -10</verify>
  <done>Only util:runtime and util:measure exist (not standalone duplicates)</done>
</task>

</tasks>

<verification>
- Verify ci.js deleted: ls src/commands/ci.js should fail
- Verify execute:profile removed: node bin/bgsd-tools.cjs execute:profile should show unknown command
- Verify no duplicates: List commands, ensure only util:runtime and util:measure exist
</verification>

<success_criteria>
Builds don't bundle ci.js, no duplicate routes visible
</success_criteria>

<output>
After completion, create `.planning/phases/115-CLI-command-routing/115-02-SUMMARY.md`
</output>
