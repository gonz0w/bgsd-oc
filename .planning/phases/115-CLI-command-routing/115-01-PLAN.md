---
phase: 115-cli-command-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CMD-01, CMD-02]
user_setup: []

must_haves:
  truths:
    - "User can call verify:handoff without silent failure"
    - "User can call verify:agents without silent failure"
    - "execute-phase.md workflow can complete without CLI errors"
  artifacts:
    - path: "src/router.js"
      provides: "CLI command routing"
      contains: "verify:handoff"
      contains: "verify:agents"
    - path: "src/lib/constants.js"
      provides: "COMMAND_HELP entries"
      contains: "verify:handoff"
      contains: "verify:agents"
    - path: "src/lib/commandDiscovery.js"
      provides: "Command discovery registry"
      contains: "handoff"
      contains: "agents"
  key_links:
    - from: "workflows/execute-phase.md"
      to: "verify:handoff"
      via: "CLI call"
      pattern: "verify:handoff"
    - from: "workflows/execute-phase.md"
      to: "verify:agents"
      via: "CLI call"
      pattern: "verify:agents"
---

<objective>
Implement the missing verify:handoff and verify:agents CLI commands that are called by execute-phase.md workflow.

Purpose: The execute-phase.md workflow references these commands but they don't exist, causing runtime failures. This plan implements them so the workflow can complete successfully.

Output: verify:handoff and verify:agents commands implemented and registered
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FEATURES.md

# Reference for what execute-phase.md expects
@workflows/execute-phase.md
@src/router.js
@src/lib/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement verify:handoff CLI command</name>
  <files>src/router.js, src/lib/constants.js, src/commands/verify.js, src/lib/commandDiscovery.js</files>
  <action>
1. Add verify:handoff handler to src/commands/verify.js - implement handoff validation logic
2. Add route handler in src/router.js case 'verify' - handle 'handoff' subcommand
3. Add COMMAND_HELP entry in src/lib/constants.js for verify:handoff
4. Register in src/lib/commandDiscovery.js routerImplementations.verify
5. Rebuild: node build.cjs

The command should support:
- --preview: Show what context would transfer between agents
- --from <agent>: Source agent name
- --to <agent>: Target agent name
- --validate <context>: Validate handoff completeness
  </action>
  <verify>node bin/bgsd-tools.cjs verify:handoff --help 2>&1 | head -20</verify>
  <done>verify:handoff command exists and responds to --help</done>
</task>

<task type="auto">
  <name>Task 2: Implement verify:agents CLI command</name>
  <files>src/router.js, src/lib/constants.js, src/commands/verify.js, src/lib/commandDiscovery.js</files>
  <action>
1. Add verify:agents handler to src/commands/verify.js - implement agent boundary validation
2. Add route handler in src/router.js case 'verify' - handle 'agents' subcommand
3. Add COMMAND_HELP entry in src/lib/constants.js for verify:agents
4. Register in src/lib/commandDiscovery.js routerImplementations.verify
5. Rebuild: node build.cjs

The command should support:
- --verify: Verify specific agent contract
- --contracts: Show all handoff contracts
- --check-overlap: Check for capability overlap
- --from <agent>: Source agent name  
- --to <agent>: Target agent name
  </action>
  <verify>node bin/bgsd-tools.cjs verify:agents --help 2>&1 | head -20</verify>
  <done>verify:agents command exists and responds to --help</done>
</task>

</tasks>

<verification>
- Run: node bin/bgsd-tools.cjs verify:handoff --preview --from planner --to executor
- Run: node bin/bgsd-tools.cjs verify:agents --contracts
- Verify both commands execute without error
</verification>

<success_criteria>
User can call verify:handoff and verify:agents without silent failure
</success_criteria>

<output>
After completion, create `.planning/phases/115-CLI-command-routing/115-01-SUMMARY.md`
</output>
