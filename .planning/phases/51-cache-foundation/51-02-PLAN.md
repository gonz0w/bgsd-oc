---
phase: 51-cache-foundation
plan: '02'
type: execute
wave: 2
depends_on: ["51-01"]
files_modified: []
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-03
  - CACHE-04

must_haves:
  truths:
    - gsd-tools cache status returns backend type and entry count
    - gsd-tools cache clear removes all entries
    - gsd-tools cache warm populates cache for specified files
    - cachedReadFile uses persistent cache with mtime staleness check
    - File writes via gsd-tools invalidate cache entry
  artifacts:
    - path: src/commands/cache.js
      provides: CLI commands for cache status/clear/warm
      min_lines: 100
  key_links:
    - from: src/router.js
      to: src/commands/cache.js
      via: require('./commands/cache')
      pattern: require.*cache
---
<objective>
Add cache CLI commands and integrate persistent cache into existing cachedReadFile.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-cache-foundation/51-CONTEXT.md
@.planning/phases/51-cache-foundation/51-RESEARCH.md
@.planning/phases/51-cache-foundation/51-01-SUMMARY.md

@src/lib/helpers.js
@src/router.js
</context>

<tasks>

<task type="auto">
  <name>Add cache CLI commands to src/commands/cache.js</name>
  <files>src/commands/cache.js</files>
  <action>
Create src/commands/cache.js with commands:

1. cache status: Reports backend type, entry count, hit/miss stats
2. cache clear: Clears all cache entries  
3. cache warm: Accepts file paths as args, populates cache with file contents

Use existing command pattern from src/commands/*.js as reference.
Export registerCacheCommand(router) function.
  </action>
  <verify>
node bin/gsd-tools.cjs cache status
node bin/gsd-tools.cjs cache clear
</verify>
  <done>All three cache commands execute without errors</done>
</task>

<task type="auto">
  <name>Wire CacheEngine into cachedReadFile in src/lib/helpers.js</name>
  <files>src/lib/helpers.js</files>
  <action>
Modify src/lib/helpers.js:

1. Replace module-level fileCache Map with: const cacheEngine = new (require('./cache').CacheEngine)()
2. Update cachedReadFile to:
   - Check cache.get(filePath) first
   - If cache miss OR stale (mtime changed), read fresh and cache.set(filePath, content)
   - Return cached content
3. Update invalidateFileCache to call cacheEngine.invalidate(filePath)
4. Keep dirCache as-is (separate from file cache)
5. Ensure graceful fallback if cache module fails to load

CRITICAL: cachedReadFile must validate mtime on every read for staleness detection (CACHE-02).
  </action>
  <verify>
node bin/gsd-tools.cjs cache status
node bin/gsd-tools.cjs cache warm .planning/STATE.md .planning/ROADMAP.md
node bin/gsd-tools.cjs cache status
</verify>
  <done>cachedReadFile uses persistent cache with staleness detection</done>
</task>

<task type="auto">
  <name>Register cache commands in router</name>
  <files>src/router.js</files>
  <action>
Add to src/router.js:

1. Import: const { registerCacheCommand } = require('./commands/cache')
2. Call registerCacheCommand(router) in the router setup

Search router.js for existing command registration pattern.
  </action>
  <verify>
node bin/gsd-tools.cjs cache --help
</verify>
  <done>gsd-tools cache command available in CLI</done>
</task>

</tasks>

<verification>
1. gsd-tools cache status reports backend type and entry count
2. Editing .planning/STATE.md and reading via gsd-tools returns fresh content
3. Running npm test passes all tests
</verification>

<success_criteria>
- gsd-tools cache status reports cache backend and entry count
- Cached reads return identical content to direct filesystem reads
- Editing a .planning/ file and reading returns updated content (never stale)
- Node <22.5 produces identical behavior to Node >=22.5 (Map fallback transparent)
</success_criteria>

<output>
After completion, create .planning/phases/51-cache-foundation/51-02-SUMMARY.md
</output>
