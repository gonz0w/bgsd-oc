---
phase: 60-testing-caching-polish
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/commands/research.js
  - src/router.js
  - src/lib/constants.js
  - bin/gsd-tools.cjs
autonomous: true
requirements: [INFRA-05]

must_haves:
  truths:
    - "An interrupted research:collect run leaves .planning/research-session.json on disk with stage progress"
    - "Running research:collect 'same query' --resume skips already-completed stages and continues from last checkpoint"
    - "Running research:collect without --resume ignores any existing session file and runs fresh"
    - "Session file is deleted on successful completion of all stages"
    - "All 762+ tests continue to pass with zero new regressions"
  artifacts:
    - path: "src/commands/research.js"
      provides: "Session persistence: saveSession/loadSession helpers + --resume flag in cmdResearchCollect"
      contains: "research-session.json"
    - path: ".planning/research-session.json"
      provides: "Runtime artifact: stage checkpoint file written during collection"
      contains: "stage"
  key_links:
    - from: "src/commands/research.js cmdResearchCollect"
      to: ".planning/research-session.json"
      via: "fs.writeFileSync after each stage completion"
      pattern: "research-session"
    - from: "src/commands/research.js cmdResearchCollect --resume"
      to: ".planning/research-session.json"
      via: "fs.readFileSync + JSON.parse at start if --resume flag present"
      pattern: "resume"
---

<objective>
Add session persistence to research:collect so interrupted runs can be resumed from the last completed stage instead of starting over.

Purpose: A full Tier 1 collection takes 3-8 minutes across 4 stages. Network failures, timeouts, or user interruptions mid-run waste all completed work. Session persistence lets the agent restart exactly where it left off.

Output: --resume flag on research:collect, session checkpoint file after each stage, auto-delete on success
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/commands/research.js
@.planning/phases/60-testing-caching-polish/60-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session persistence to cmdResearchCollect</name>
  <files>src/commands/research.js, src/router.js, src/lib/constants.js</files>
  <action>
Add session persistence to cmdResearchCollect() in src/commands/research.js.

**Session file location:** path.join(cwd, '.planning', 'research-session.json')

**Session file schema:**
  {
    "query": string,
    "tier": number,
    "started": ISO timestamp,
    "last_saved": ISO timestamp,
    "completed_stages": ["web", "youtube", "context7", "nlm"],  // stages done so far
    "sources": [],         // accumulated sources
    "timing": {},          // accumulated timing
    "nlm_synthesis": null  // null until nlm stage completes
  }

**Add two private helpers before cmdResearchCollect:**

saveSession(cwd, data):
  const sessionPath = path.join(cwd, '.planning', 'research-session.json');
  try { fs.writeFileSync(sessionPath, JSON.stringify({...data, last_saved: new Date().toISOString()}, null, 2)); }
  catch(e) { debugLog('research.session', 'failed to save session: '+e.message); }

loadSession(cwd, query):
  const sessionPath = path.join(cwd, '.planning', 'research-session.json');
  try {
    if (!fs.existsSync(sessionPath)) return null;
    const data = JSON.parse(fs.readFileSync(sessionPath, 'utf-8'));
    // Only resume if query matches exactly
    if (data.query !== query) return null;
    return data;
  } catch(e) { return null; }

deleteSession(cwd):
  const sessionPath = path.join(cwd, '.planning', 'research-session.json');
  try { if (fs.existsSync(sessionPath)) fs.unlinkSync(sessionPath); }
  catch(e) { debugLog('research.session', 'failed to delete session: '+e.message); }

**Modify cmdResearchCollect() — add --resume support:**

Parse flags (add alongside existing --quick and --no-cache):
  const resume = args.includes('--resume');

After query validation and before stage execution, add resume load:
  let session = null;
  if (resume) {
    session = loadSession(cwd, query);
    if (session) {
      status('Resuming session from stage: ' + (session.completed_stages.slice(-1)[0] || 'start'));
    } else {
      status('No matching session found — starting fresh');
    }
  }

Initialize accumulators from session if available:
  const allSources = session ? [...(session.sources || [])] : [];
  const timing = session ? {...(session.timing || {})} : {};
  const completedStages = session ? new Set(session.completed_stages || []) : new Set();

Wrap each stage with a skip check:

Stage 1 (web sources) — BEFORE the stage body:
  if (completedStages.has('web')) {
    status('[1/' + totalStages + '] Web sources: restored from session (' + allSources.filter(s=>s.source==='brave_search').length + ' sources)');
    timing.web_ms = timing.web_ms || 0;
  } else {
    // ... existing stage 1 code ...
    completedStages.add('web');
    saveSession(cwd, { query, tier: tier.number, started: session?.started || new Date().toISOString(), completed_stages: [...completedStages], sources: allSources, timing, nlm_synthesis: null });
  }

Stage 2 (youtube) — same pattern with 'youtube' key.
Stage 3 (context7 note) — same pattern with 'context7' key.
Stage 4 (nlm) — same pattern with 'nlm' key; save nlm_synthesis in session too:
  saveSession(cwd, { ..., nlm_synthesis: nlmSynthesis });

After output() call at the end — delete session on success:
  deleteSession(cwd);

**src/lib/constants.js** — add COMMAND_HELP entries:
  'research:collect --resume': 'Resume interrupted research session from last completed stage'
  'research collect --resume': same

**src/router.js** — no routing changes needed (--resume is a flag on existing research:collect route).
But verify the existing research:collect route passes all args through to cmdResearchCollect correctly.
  </action>
  <verify>
1. node bin/gsd-tools.cjs research:collect "test resume" --quick 2>/dev/null (should work, no --resume flag, Tier 4 quick path)
2. node -e "const fs=require('fs');const path=require('path');const sess={query:'test',tier:3,started:new Date().toISOString(),completed_stages:['web'],sources:[{type:'web',title:'test',url:'http://test.com',snippet:'test',source:'brave_search'}],timing:{web_ms:100},nlm_synthesis:null};fs.mkdirSync('.planning',{recursive:true});fs.writeFileSync('.planning/research-session.json',JSON.stringify(sess));" && echo "Session written" && ls -la .planning/research-session.json
3. node bin/gsd-tools.cjs research:collect "test resume" --quick --resume 2>/dev/null | head -3 (quick bypasses resume but should not crash)
4. npm test 2>&1 | tail -5 (760/762 — 2 pre-existing failures only)
  </verify>
  <done>--resume flag accepted by research:collect without error. saveSession/loadSession/deleteSession helpers exist. After each non-skipped stage, .planning/research-session.json is updated. Session deleted on successful full completion. npm test: 760/762.</done>
</task>

<task type="auto">
  <name>Task 2: Build bundle and verify full test suite</name>
  <files>bin/gsd-tools.cjs</files>
  <action>
Rebuild the production bundle and verify zero regressions across the complete v8.1 pipeline.

1. Run: npm run build
   Verify: bundle size stays under 1500KB (currently ~1204KB, Phase 60 adds minimal code)
   Run: ls -lh bin/gsd-tools.cjs

2. Run full test suite: npm test
   Expected: 760/762 passing (the 2 pre-existing failures from Phase 56 config-migrate are known and acceptable)
   FAIL condition: any NEW test failure beyond the 2 pre-existing ones

3. Smoke test the full --resume flag end-to-end with a mock session:
   Write a session file manually, then run research:collect with --resume and verify it picks up the session.
   node -e "
     const fs=require('fs');
     fs.mkdirSync('.planning',{recursive:true});
     fs.writeFileSync('.planning/research-session.json',JSON.stringify({
       query:'nodejs caching best practices',
       tier:3,
       started:new Date().toISOString(),
       completed_stages:['web','youtube','context7'],
       sources:[{type:'web',title:'Caching in Node.js',url:'https://example.com',snippet:'Best practices',source:'brave_search'}],
       timing:{web_ms:1200,youtube_ms:0,context7_available:false},
       nlm_synthesis:null
     }));
   " && node bin/gsd-tools.cjs research:collect "nodejs caching best practices" --resume 2>&1 | head -10

4. Verify cache commands from Plan 01 still work after rebuild:
   node bin/gsd-tools.cjs cache:research-stats 2>/dev/null
   node bin/gsd-tools.cjs cache:research-clear 2>/dev/null

5. Clean up any test session files:
   rm -f .planning/research-session.json
  </action>
  <verify>
npm test output shows 760 passing (or more), 2 failing (pre-existing only).
ls -lh bin/gsd-tools.cjs shows file under 1500KB.
cache:research-stats and cache:research-clear return valid JSON.
research:collect --resume picks up the written session file (status line shows "Resuming session").
  </verify>
  <done>Bundle rebuilt under 1500KB. npm test: 760/762 (only the 2 pre-existing config-migrate failures). Session resume end-to-end verified. Phase 60 complete — v8.1 milestone ready for wrapup.</done>
</task>

</tasks>

<verification>
1. npm test: 760/762 (2 pre-existing failures only — no new failures)
2. ls -lh bin/gsd-tools.cjs (under 1500KB)
3. node bin/gsd-tools.cjs cache:research-stats 2>/dev/null (returns JSON)
4. node bin/gsd-tools.cjs cache:research-clear 2>/dev/null (returns cleared:true)
5. Write session file + node bin/gsd-tools.cjs research:collect "..." --resume (shows "Resuming session from stage: ...")
6. node bin/gsd-tools.cjs research:collect "test" --no-cache --quick 2>/dev/null (Tier 4 unaffected)
</verification>

<success_criteria>
- --resume flag: research:collect reads .planning/research-session.json, skips completed stages, continues from next
- Session file written after each stage with completed_stages array
- Session file deleted on successful full completion
- --resume with no matching session: runs fresh (no crash)
- All 762+ tests pass (760 currently; 2 pre-existing config-migrate failures are acceptable)
- Bundle under 1500KB
- No regression in Tier 4 --quick path or existing cache commands
</success_criteria>

<output>
After completion, create .planning/phases/60-testing-caching-polish/60-02-SUMMARY.md
</output>
