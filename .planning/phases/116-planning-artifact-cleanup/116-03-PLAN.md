---
phase: 116-planning-artifact-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [.planning/PROJECT.md, bin/bgsd-tools.cjs]
autonomous: true
requirements: [ART-07]

must_haves:
  truths:
    - "User can see current constraints that are still relevant"
    - "User can see current Key Decisions that are still valid"
    - "CLI validates planning artifacts on build to prevent regressions"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Current constraints section"
      contains: "only active constraints, resolved ones archived"
    - path: ".planning/PROJECT.md"
      provides: "Current Key Decisions table"
      contains: "only current decisions, archived ones noted"
    - path: "bin/bgsd-tools.cjs"
      provides: "Artifact validation command"
      contains: "validates MILESTONES.md and PROJECT.md structure"
  key_links:
    - Validation runs in build pipeline
    - Validation fails build if artifacts have issues
---

<objective>
Update constraints and Key Decisions in PROJECT.md, and add CLI validation to prevent future regressions.

Purpose: Users reading PROJECT.md should see only current, relevant constraints and decisions. Automated validation prevents artifact drift.
Output: Cleaned PROJECT.md with current constraints/decisions + CLI validation command
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive resolved constraints and update active ones</name>
  <files>.planning/PROJECT.md</files>
  <action>
Review and update the Constraints section in PROJECT.md (starting around line 230):
1. Identify constraints that have been resolved or are no longer relevant
2. Move resolved constraints to an "Archived Constraints" section at bottom of file (or note as resolved)
3. Keep only active constraints that still apply:
   - Backward compatibility (still relevant)
   - No breaking changes (still relevant)
   - Single-file deploy (still relevant)
   - Node.js 18+ (update to 22.5+ for sqlite)
   - Test against current project (still relevant)
   - Agent cap at 9 (still relevant)
4. Add any new constraints from recent decisions

Archived constraints example format:
```
### Archived Constraints
- ~~Node.js 18+ minimum~~ — Raised to 22.5+ in v11.x for node:sqlite support
```
  </action>
  <verify>
Read Constraints section and verify only active constraints remain; archived constraints moved to bottom</verify>
  <done>Constraints section contains only relevant, active constraints; resolved constraints archived</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update Key Decisions table</name>
  <files>.planning/PROJECT.md</files>
  <action>
Review and update the Key Decisions table in PROJECT.md (starting around line 239):
1. Check each decision for accuracy and currency
2. Add any new decisions from v11.x milestones that aren't yet listed:
   - Progressive confidence model (v11.3)
   - In-process decision engine via enricher (v11.3)
   - Scaffold-then-fill for SUMMARY.md (v11.3)
3. Mark any decisions that were reversed or superseded
4. Ensure format is consistent (Decision | Rationale | Outcome columns)

New decisions to add from recent work:
- "Progressive confidence model for decisions" - HIGH=authoritative, MEDIUM=LLM confirms, LOW=LLM decides
- "In-process decision engine via enricher" - Zero subprocess overhead
- "Scaffold-then-fill for SUMMARY.md" - CLI generates data, LLM fills judgment
  </action>
  <verify>
Read Key Decisions table and verify all recent decisions are included with proper format</verify>
  <done>Key Decisions table contains all current decisions with accurate outcomes</done>
</task>

<task type="auto">
  <name>Task 3: Add artifact validation to CLI (build gate)</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
Add validation to bgsd-tools.cjs that checks planning artifacts for common issues:

1. Add new command: `util:validate-artifacts` or extend existing validation
2. Checks to implement:
   - MILESTONES.md: Check for balanced headers, valid date formats, chronological order
   - PROJECT.md: Check for balanced HTML tags (</details> count matches <details>), no strikethrough in out-of-scope
   - General: Check files exist and are readable
3. Integrate into build pipeline: Run validation in build.cjs before bundling
4. Exit with error code if validation fails (blocks deploy)

Example validation logic:
```
function validateArtifacts() {
  const ms = readFileSync('.planning/MILESTONES.md', 'utf8');
  const proj = readFileSync('.planning/PROJECT.md', 'utf8');
  
  // Check <details> balance
  const openDetails = (proj.match(/<details>/g) || []).length;
  const closeDetails = (proj.match(/<\/details>/g) || []).length;
  if (openDetails !== closeDetails) {
    throw new Error('PROJECT.md: unbalanced <details> tags');
  }
  
  // Check no strikethrough in out-of-scope
  if (proj.includes('~~')) {
    throw new Error('PROJECT.md: found strikethrough (~~) in out-of-scope section');
  }
}
```
  </action>
  <verify>
Run: `node bin/bgsd-tools.cjs util:validate-artifacts` — should pass or show clear errors; Run: `npm run build` — should run validation as part of build</verify>
  <done>CLI has artifact validation that runs as build gate, preventing broken artifacts from being deployed</done>
</task>

</tasks>

<verification>
- [ ] Constraints section has only active constraints
- [ ] Resolved constraints archived
- [ ] Key Decisions table includes recent v11.x decisions
- [ ] Key Decisions table has consistent format
- [ ] CLI validates artifacts on build
- [ ] Validation checks for balanced HTML tags
- [ ] Validation checks for strikethrough in out-of-scope
</verification>

<success_criteria>
User can see constraints that are still relevant — resolved constraints archived. User can see a Key Decisions table where all entries are current and properly formatted.
</success_criteria>

<output>
After completion, create `.planning/phases/116-planning-artifact-cleanup/116-03-SUMMARY.md`
</output>
