---
phase: 116-planning-artifact-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/PROJECT.md]
autonomous: true
requirements: [ART-04, ART-05, ART-06]

must_haves:
  truths:
    - "User can read PROJECT.md without broken HTML"
    - "User can read PROJECT.md with accurate counts (modules, tests, bundle)"
    - "User can see current out-of-scope list without stale items"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Valid HTML structure"
      contains: "no orphaned </details> tags"
    - path: ".planning/PROJECT.md"
      provides: "Accurate project statistics"
      contains: "current module count, test count, bundle size"
    - path: ".planning/PROJECT.md"
      provides: "Current out-of-scope list"
      contains: "only items relevant to current project state"
  key_links:
    - HTML structure validates (no broken tags)
    - Counts match current bin/bgsd-tools.cjs module count
    - Out-of-scope items are from last ~12 milestones, not older
---

<objective>
Fix PROJECT.md to have valid HTML, accurate counts, and current out-of-scope list.

Purpose: Users reading PROJECT.md should find accurate information without broken HTML or stale data.
Output: Cleaned PROJECT.md with valid HTML, updated counts, and current out-of-scope list
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken HTML in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
Fix the following HTML issues in PROJECT.md:
1. Remove orphaned \`</details>\` tag on line 68 - it's not closing any open \`<details>\`
2. Fix broken table rows in the version history section (lines 52-65) - ensure each row has proper markdown table format
3. Verify all \`<details>\` sections have matching \`</details>\` closing tags
4. Check for any other malformed HTML tags
  </action>
  <verify>
Run: `node -e "const fs=require('fs');const c=fs.readFileSync('.planning/PROJECT.md','utf8');console.log((c.match(/<details>/g)||[]).length + ' open tags, ' + (c.match(/<\/details>/g)||[]).length + ' close tags')"` — counts should match</verify>
  <done>PROJECT.md has valid HTML structure with no orphaned tags or broken table rows</done>
</task>

<task type="auto">
  <name>Task 2: Update stale counts in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
Update the following stale counts in PROJECT.md:
1. Module count: Update from "53 src/ modules" to actual count (check bin/bgsd-tools.cjs or src/ directory)
2. Test count: Update from "1014 tests (414 passing)" to current count (run `npm test` and count passing)
3. Bundle size: Update to current size (check bin/bgsd-tools.cjs file size)
4. Node.js version: Verify minimum version is current (should be >= 22.5 for node:sqlite)
5. Update all "18 versions shipped" references if milestone count changed
6. Update test suite description in Context section

Run actual commands to get accurate numbers:
- `wc -l bin/bgsd-tools.cjs` for bundle info
- `ls src/*.js src/**/*.js 2>/dev/null | wc -l` for module count
- `npm test 2>&1 | grep -E "passing|failing"` for test count
  </action>
  <verify>
Run: `npm test 2>&1 | grep -E "passing"` — should show current test count; Compare module count from `ls src/` with PROJECT.md</verify>
  <done>PROJECT.md contains accurate counts matching current project state</done>
</task>

<task type="auto">
  <name>Task 3: Clean out-of-scope list in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <action>
Review and clean the Out of Scope section (starting around line 205):
1. Remove any items marked with ~~strikethrough~~ (e.g., "~~SQLite codebase index~~ — Reconsidered for v8.0")
2. Remove items that are no longer relevant (from 12+ milestones ago)
3. Add any new out-of-scope items that reflect current decisions
4. Ensure format is consistent (no strikethrough, just active exclusions)

Common stale items to remove:
- Items referencing v1-v7 era decisions that are now resolved
- Items that were done/addressed in recent milestones

Keep items that are genuinely still out of scope:
- Async I/O rewrite
- npm package publishing  
- ESM output format
- TypeScript migration
- etc.
  </action>
  <verify>
Run: `grep -c "~~" .planning/PROJECT.md` — should return 0 (no strikethrough)</verify>
  <done>Out-of-scope list contains only current, relevant exclusions without strikethrough formatting</done>
</task>

</tasks>

<verification>
- [ ] All HTML tags properly balanced (no orphaned </details>)
- [ ] No broken markdown table rows
- [ ] Module count accurate
- [ ] Test count accurate
- [ ] Bundle size accurate
- [ ] Node.js version current
- [ ] Out-of-scope list has no strikethrough items
- [ ] Out-of-scope list only contains relevant exclusions
</verification>

<success_criteria>
User can read PROJECT.md without broken HTML, stale counts, or broken table rows. User can see an out-of-scope list that reflects actual current exclusions.
</success_criteria>

<output>
After completion, create `.planning/phases/116-planning-artifact-cleanup/116-02-SUMMARY.md`
</output>
