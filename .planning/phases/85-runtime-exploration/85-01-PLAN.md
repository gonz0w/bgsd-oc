---
phase: 85-runtime-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cli-tools/bun-runtime.js
  - src/lib/cli-tools/install-guidance.js
  - src/commands/runtime.js
  - src/router.js
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03
user_setup: []

must_haves:
  truths:
    - User can run `bgsd-tools runtime` to see Bun detection status
    - When Bun is available, output shows version, path, and "ready to use"
    - When Bun is not available, output shows platform-specific install instructions
    - User can run `bgsd-tools runtime benchmark` to compare Node.js vs Bun startup
    - Bun detection uses session cache (in-memory only, not persisted)
  artifacts:
    - src/lib/cli-tools/bun-runtime.js - Bun detection module with session cache
    - src/lib/cli-tools/install-guidance.js - Updated with Bun install instructions
    - src/commands/runtime.js - CLI command for runtime status and benchmark
    - bin/bgsd-tools.cjs - Bundled CLI with new runtime module
  key_links:
    - bun-runtime.js uses execFileSync with array args (per prior decision)
    - Bun detection follows Phase 82 detector.js pattern
    - Install guidance follows Phase 82 install-guidance.js pattern
---

<objective>
Implement Bun runtime detection, compatibility documentation, and startup benchmarking for the bGSD plugin.

Purpose: Enable users to detect Bun availability, view compatibility docs, and benchmark Node.js vs Bun startup performance.

Output: New runtime command (`bgsd-tools runtime`) showing Bun status and benchmark results.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-runtime-exploration/85-CONTEXT.md
@.planning/phases/85-runtime-exploration/85-RESEARCH.md
@.planning/phases/82-tool-detection-infrastructure/82-01-SUMMARY.md
@src/lib/cli-tools/detector.js
@src/lib/cli-tools/install-guidance.js
@src/commands/tools.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Bun runtime detection module</name>
  <files>src/lib/cli-tools/bun-runtime.js</files>
  <action>
Create src/lib/cli-tools/bun-runtime.js with:
- Session cache (Map, cleared on process exit - no TTL)
- detectBun() function using detection order: `bun --version` first (3s timeout), fallback to `which bun`
- Returns { available, name, version, path } with version/path when available
- benchmarkStartup(scriptPath, runs) function timing Node vs Bun
- isRunningUnderBun() function using process.versions.bun
- clearCache() function for testing

Per CONTEXT.md locked decisions:
- Version command first, then PATH lookup
- Session cache only (not persisted)
- Show full details when detected (version, path, "ready to use")
  </action>
  <verify>
node -e "const { detectBun } = require('./src/lib/cli-tools/bun-runtime'); console.log(JSON.stringify(detectBun(), null, 2));"</verify>
  <done>
Module exports detectBun(), benchmarkStartup(), isRunningUnderBun(), clearCache()
Detection uses session cache (Map, not persisted)
Version command tried first, PATH lookup as fallback
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Bun to install guidance</name>
  <files>src/lib/cli-tools/install-guidance.js</files>
  <action>
Add Bun entry to TOOL_CONFIG in install-guidance.js:
- name: 'bun'
- description: 'Fast JavaScript runtime'
- website: 'https://bun.sh'
- install:
  - darwin: 'curl -fsSL https://bun.sh/install | bash' (also 'brew install oven-sh/bun/bun')
  - linux: 'curl -fsSL https://bun.sh/install | bash' (also 'npm install -g bun')
  - win32: 'powershell -c "irm bun.sh/install.ps1|iex"' (also 'scoop install bun')
- alternatives: 'Node.js (built-in)'

Add bun to exports if needed.
  </action>
  <verify>
node -e "const { getInstallGuidance } = require('./src/lib/cli-tools/install-guidance'); console.log(getInstallGuidance('bun'));"</verify>
  <done>
Bun entry exists in TOOL_CONFIG
Platform-specific install commands provided
getInstallGuidance('bun') returns install instructions
  </done>
</task>

<task type="auto">
  <name>Task 3: Create runtime CLI command</name>
  <files>src/commands/runtime.js</files>
  <action>
Create src/commands/runtime.js with:
- cmdRuntimeStatus(cwd, raw): Shows Bun detection status
  - If available: Shows version, path, "Bun is ready to use"
  - If unavailable: Shows install instructions from getInstallGuidance
- cmdRuntimeBenchmark(cwd, raw): Runs startup benchmark
  - Uses benchmarkStartup() with a simple test script
  - Shows Node.js time, Bun time, and speedup ratio
  - Default 10 runs, configurable via CLI args

Output format similar to tools.js for consistency.
  </action>
  <verify>
node -e "const { cmdRuntimeStatus } = require('./src/commands/runtime'); cmdRuntimeStatus(process.cwd(), false);"</verify>
  <done>
cmdRuntimeStatus shows Bun availability with details or install instructions
cmdRuntimeBenchmark shows Node vs Bun timing comparison
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate runtime command</name>
  <files>src/router.js, bin/bgsd-tools.cjs</files>
  <action>
1. In src/router.js, add runtime command registration:
   - 'runtime' -> runtime.js exports
   
2. Rebuild bin/bgsd-tools.cjs with the new modules:
   node build.cjs
   
3. Verify the runtime command works:
   node bin/bgsd-tools.cjs runtime
  </action>
  <verify>
node bin/bgsd-tools.cjs runtime && node bin/bgsd-tools.cjs runtime benchmark</verify>
  <done>
'bgsd-tools runtime' shows Bun status
'bgsd-tools runtime benchmark' shows benchmark results
  </done>
</task>

</tasks>

<verification>
- Run `node bin/bgsd-tools.cjs runtime` - shows Bun detection status
- Run `node bin/bgsd-tools.cjs runtime benchmark` - shows Node vs Bun timing
- Verify session cache works (detection persists within process)
- Verify install instructions show for unavailable Bun
</verification>

<success_criteria>
- [ ] RUNT-01: Plugin detects Bun runtime availability (session cache, version+PATH)
- [ ] RUNT-02: Bun compatibility info documented in install-guidance.js
- [ ] RUNT-03: Benchmark command compares Node.js vs Bun startup time
- [ ] All new modules integrated into bundled CLI
- [ ] All commands accessible via bgsd-tools CLI
</success_criteria>

<output>
After completion, create `.planning/phases/85-runtime-exploration/85-01-SUMMARY.md`
</output>
