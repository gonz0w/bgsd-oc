---
phase: 103-direct-command-routing
plan: "03"
type: execute
wave: 3
depends_on:
  - "103-02"
files_modified:
  - .planning/phases/103-direct-command-routing/103-03-ROUTE-TEST.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03

must_haves:
  truths:
    - "All 41+ commands route to correct workflows on first attempt"
    - "Routing is deterministic - same command always routes same way"
    - "No routing failures or fallbacks occur"
  artifacts:
    - path: ".planning/phases/103-direct-command-routing/103-03-ROUTE-TEST.md"
      provides: "Test results for all commands"
      contains: "pass/fail for each command"
  key_links:
    - from: "command input"
      to: "workflow execution"
      via: "routing logic"
      pattern: "direct execution"
---

<objective>
Verify all 41 commands route correctly and test deterministic behavior.

Purpose: Confirm Phase 103 success criteria are met.
Output: Test results document.
</objective>

<execution_context>
@/mnt/raid/DEV/bgsd-oc/workflows/plan-phase.md
@/mnt/raid/DEV/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-direct-command-routing/103-01-SUMMARY.md
@.planning/phases/103-direct-command-routing/103-02-SUMMARY.md
@.planning/ROADMAP.md (Phase 103 success criteria)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create command routing test script</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
    1. Create test script that exercises each command's routing
    2. For each command group, invoke with valid subcommand
    3. Verify no prompts appear in output (check for common prompt patterns)
    4. Run same command twice to verify deterministic routing
    5. Test invalid subcommand produces clear error (not prompt)
  </action>
  <verify>
    node bin/bgsd-tools.cjs --help | head -50
  </verify>
  <done>
    Test script created and baseline run completed
  </done>
</task>

<task type="auto">
  <name>Task 2: Test each of the 41 commands</name>
  <files>test-commands.sh (temporary)</files>
  <action>
    1. Execute each command group with each subcommand
    2. Record: command, expected workflow, actual behavior, any prompts
    3. Test list from 103-01: config(4) + debug(1) + exec(4) + github-ci(1) + health(1) + milestone(4) + plan(5) + progress(1) + quick(1) + roadmap(5) + session(3) + todo(3) + util(10) + verify-work(1) = 43 total
    4. Verify deterministic: run each command twice, confirm identical behavior
    5. Document any failures
  </action>
  <verify>
    echo "Testing commands..." && for cmd in config debug exec github-ci health milestone plan progress quick roadmap session todo util verify-work; do echo "Testing bgsd $cmd..."; done
  </verify>
  <done>
    All 41+ commands tested with results documented
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify success criteria</name>
  <files>.planning/phases/103-direct-command-routing/103-03-ROUTE-TEST.md</files>
  <action>
    1. Check criteria from ROADMAP.md:
       - `/bgsd milestone new` executes new-milestone workflow without clarification prompts
       - All 41 slash commands route correctly on first attempt
       - Command routing is deterministic
    2. Run specific tests:
       - Execute `/bgsd milestone new` and confirm no prompts
       - List all 41 commands and verify each has valid route
       - Run same command multiple times, confirm identical
    3. Document results in test output file
  </action>
  <verify>
    echo "Success criteria check:" && echo "1. milestone new no prompts" && echo "2. 41 commands route" && echo "3. deterministic"
  </verify>
  <done>
    All success criteria verified and documented
  </done>
</task>

</tasks>

<verification>
- [ ] milestone new runs without prompts (tested 3 times)
- [ ] All 41+ commands have valid routes
- [ ] Deterministic routing confirmed (same command = same output 3x)
- [ ] Invalid commands fail with clear error (not prompt)
</verification>

<success_criteria>
Phase 103 success criteria met:
1. `/bgsd milestone new` executes without clarification prompts ✓
2. All 41 commands route correctly on first attempt ✓
3. Routing is deterministic ✓
</success_criteria>

<output>
After completion, create `.planning/phases/103-direct-command-routing/103-03-SUMMARY.md`
</output>
