---
phase: 103-direct-command-routing
plan: "02"
type: execute
wave: 2
depends_on:
  - "103-01"
files_modified:
  - commands/bgsd-milestone.md
  - commands/bgsd-plan.md
  - commands/bgsd-*.md
  - workflows/new-milestone.md
  - workflows/complete-milestone.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03

must_haves:
  truths:
    - "Commands route directly to workflows without clarification steps"
    - "All 41+ commands have clear routing configuration"
    - "Same command always routes to same workflow (deterministic)"
  artifacts:
    - path: "commands/bgsd-milestone.md"
      provides: "Direct routing for milestone subcommands"
      contains: "no clarification prompts"
    - path: "bin/bgsd-tools.cjs"
      provides: "CLI init commands bypass prompts"
      contains: "init:* commands return context only"
  key_links:
    - from: "commands/*.md"
      to: "workflows/*.md"
      via: "direct invocation"
      pattern: "invoke.*workflow"
---

<objective>
Implement direct command routing - eliminate clarification prompts and ensure deterministic routing.

Purpose: Make `/bgsd milestone new` execute the new-milestone workflow without any prompts.
Output: Updated command files and routing logic with direct execution.
</objective>

<execution_context>
@/mnt/raid/DEV/bgsd-oc/workflows/plan-phase.md
@/mnt/raid/DEV/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-direct-command-routing/103-01-ROUTE-MAP.md
@.planning/phases/103-direct-command-routing/103-01-SUMMARY.md
@.planning/ROADMAP.md (Phase 103 requirements: ROUTE-01, ROUTE-02, ROUTE-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove clarification prompts from init commands</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
    1. Find init:new-milestone, init:milestone-op, init:plan-phase commands in bgsd-tools.cjs
    2. Remove any interactive prompts (inquirer/confirm) from these init commands
    3. Make init commands return context JSON only - no user interaction
    4. If context cannot be determined, return defaults rather than prompt
    5. Ensure init commands are idempotent and deterministic
  </action>
  <verify>
    grep -n "inquirer\|confirm(" bin/bgsd-tools.cjs | grep -i "init:" | head -10
  </verify>
  <done>
    Init commands return context without prompting
  </done>
</task>

<task type="auto">
  <name>Task 2: Update command wrappers for direct routing</name>
  <files>commands/bgsd-milestone.md, commands/bgsd-plan.md, commands/bgsd-exec.md, commands/bgsd-roadmap.md, commands/bgsd-session.md, commands/bgsd-todo.md, commands/bgsd-util.md, commands/bgsd-config.md</files>
  <action>
    1. For each command group file, verify routing is direct
    2. Remove any "confirm" steps or conditional branching that requires user input
    3. Ensure subcommand parsing is deterministic (first arg = subcommand, no fuzzy matching)
    4. Update <process> sections to reflect direct routing
    5. Ensure unknown subcommands fail fast with clear error
  </action>
  <verify>
    grep -l "confirm\|clarification\|did you mean" commands/*.md
  </verify>
  <done>
    All command wrappers use direct routing
  </done>
</task>

<task type="auto">
  <name>Task 3: Disable NL clarification generation</name>
  <files>src/lib/nl/conversational-planner.js, src/lib/nl/help-fallback.js, src/lib/nl/requirement-extractor.js</files>
  <action>
    1. Find functions that generate clarification prompts
    2. Add configuration flag or bypass logic for direct command execution
    3. When command is explicit (not natural language), skip clarification generation
    4. Keep the functionality for Phase 104 (Zero Friction) but disable for now
    5. Comment clearly where changes made for future reference
  </action>
  <verify>
    grep -n "clarification" src/lib/nl/conversational-planner.js
  </verify>
  <done>
    NL clarification generation bypassed for explicit commands
  </done>
</task>

</tasks>

<verification>
- [ ] /bgsd milestone new runs without prompts
- [ ] /bgsd plan phase 103 runs without prompts
- [ ] No "did you mean" appears for explicit commands
- [ ] Unknown subcommands fail with clear error
</verification>

<success_criteria>
Commands execute directly:
- `/bgsd milestone new` → new-milestone workflow (no prompts)
- `/bgsd plan phase 103` → plan-phase workflow (no prompts)
- Routing is deterministic (same input = same output)
</success_criteria>

<output>
After completion, create `.planning/phases/103-direct-command-routing/103-02-SUMMARY.md`
</output>
