---
phase: 103-direct-command-routing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-direct-command-routing/103-01-ROUTE-MAP.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03

must_haves:
  truths:
    - "All 14 top-level command groups identified with their subcommands"
    - "Current routing flow documented with entry points"
    - "All locations where clarification prompts occur identified"
    - "41 total commands (including subcommands) mapped"
  artifacts:
    - path: ".planning/phases/103-direct-command-routing/103-01-ROUTE-MAP.md"
      provides: "Complete command routing map"
      contains: "command -> workflow mappings"
  key_links:
    - from: "commands/*.md"
      to: "workflows/*.md"
      via: "command execution"
      pattern: "invoke.*workflow"
---

<objective>
Map current command routing mechanism and identify where clarification prompts occur.

Purpose: Understand the current routing flow to identify where direct execution is blocked by clarification prompts.
Output: Route map document showing all commands, their routing paths, and clarification points.
</objective>

<execution_context>
@/mnt/raid/DEV/bgsd-oc/workflows/plan-phase.md
@/mnt/raid/DEV/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 103 requirements: ROUTE-01, ROUTE-02, ROUTE-03)
@.planning/REQUIREMENTS.md (ROUTE-01, ROUTE-02, ROUTE-03 definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate all command groups and subcommands</name>
  <files>commands/*.md</files>
  <action>
    1. List all files in commands/ directory to get top-level command groups
    2. Read each command file and extract subcommands from the <objective> and process sections
    3. Build complete list: bgsd config (4), bgsd debug (1), bgsd exec (4), bgsd github-ci (1), bgsd health (1), bgsd milestone (4), bgsd plan (5), bgsd progress (1), bgsd quick (1), bgsd roadmap (5), bgsd session (3), bgsd todo (3), bgsd util (10), bgsd verify-work (1)
    4. Count total: should be 41+ commands
  </action>
  <verify>
    ls commands/*.md | wc -l && for f in commands/*.md; do echo "=== $f ==="; grep -E "^-\s+\*\*" "$f"; done
  </verify>
  <done>
    Complete list of command groups with subcommands documented
  </done>
</task>

<task type="auto">
  <name>Task 2: Trace routing flow for key commands</name>
  <files>commands/bgsd-milestone.md, commands/bgsd-plan.md, workflows/new-milestone.md</files>
  <action>
    1. Read commands/bgsd-milestone.md to understand router pattern
    2. Read commands/bgsd-plan.md to see variation (has init: call)
    3. Trace /bgsd milestone new flow: command → workflow new-milestone
    4. Identify where init:* CLI commands run and what they provide
    5. Note any branching/conditional logic in routing
  </action>
  <verify>
    grep -r "init:" workflows/*.md | head -20
  </verify>
  <done>
    Routing flow documented for representative commands
  </done>
</task>

<task type="auto">
  <name>Task 3: Find all clarification prompt locations</name>
  <files>src/lib/nl/*.js, workflows/*.md</files>
  <action>
    1. Search for "clarification" in codebase - found in nl/conversational-planner.js, nl/requirement-extractor.js
    2. Search for "did you mean" patterns - found in nl/help-fallback.js
    3. Search for confirmation prompts in workflows that could block execution
    4. Check init:* commands for any interactive prompts
    5. Document each location with: file, line, what triggers it, whether it's skippable
  </action>
  <verify>
    grep -rn "clarification\|did you mean\|confirm" src/lib/nl/ | grep -v node_modules
  </verify>
  <done>
    List of all clarification prompt locations with bypass criteria
  </done>
</task>

</tasks>

<verification>
- [ ] Command group count matches (14)
- [ ] Total subcommand count >= 41
- [ ] Routing flow understood for group commands vs simple commands
- [ ] All clarification locations documented
</verification>

<success_criteria>
Complete route map document exists at .planning/phases/103-direct-command-routing/103-01-ROUTE-MAP.md with:
- All 14 command groups listed
- All subcommands enumerated (41+)
- Routing flow for each type
- Clarification prompt locations identified
</success_criteria>

<output>
After completion, create `.planning/phases/103-direct-command-routing/103-01-SUMMARY.md`
</output>
