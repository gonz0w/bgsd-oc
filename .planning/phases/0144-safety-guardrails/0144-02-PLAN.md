---
phase: 144-safety-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - "0144-01"
files_modified:
  - tests/plugin-advisory-guardrails.test.cjs
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03

must_haves:
  truths:
    - "Test suite covers all three severity tiers (CRITICAL, WARNING, INFO) with at least one pattern per tier"
    - "Unicode normalization tests verify fullwidth characters (ｒｍ), zero-width joiners, and combining marks are all detected"
    - "Sandbox mode tests verify WARNING/INFO suppression and CRITICAL pass-through in container environments"
    - "Config override tests verify global disable, per-category disable, and per-pattern disable via disabled_patterns"
    - "Custom pattern merge tests verify user patterns combine with built-ins and invalid regex is gracefully skipped"
    - "Edge cases test: null/undefined input, empty command, non-bash tools don't trigger GARD-04"
  artifacts:
    - path: "tests/plugin-advisory-guardrails.test.cjs"
      provides: "GARD-04 test section covering pattern matching, Unicode normalization, sandbox bypass, config overrides, custom patterns, edge cases, and false-positive validation"
      contains: "GARD-04"
  key_links:
    - from: "tests/plugin-advisory-guardrails.test.cjs"
      to: "src/plugin/advisory-guardrails.js"
      via: "createAdvisoryGuardrails imported and tested with mock notifier and bash tool events"
      pattern: "createAdvisoryGuardrails.*bash"
---

<objective>
Add comprehensive GARD-04 test coverage to the existing advisory guardrails test suite.

Purpose: Validate destructive command detection, Unicode normalization, container/sandbox bypass, config overrides, custom pattern merging, and false-positive resistance with real-world command inputs.

Output: Extended test file with GARD-04 sections that exercise all code paths from Plan 01 and verify the stress-tested decisions from CONTEXT.md (especially Unicode NFKD false-positive testing against Stack Overflow smart quotes and em-dashes).
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0144-safety-guardrails/0144-CONTEXT.md
@.planning/phases/0144-safety-guardrails/0144-RESEARCH.md
@.planning/phases/0144-safety-guardrails/0144-01-SUMMARY.md
@src/plugin/advisory-guardrails.js
@tests/plugin-advisory-guardrails.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GARD-04 pattern matching and severity tier tests</name>
  <files>tests/plugin-advisory-guardrails.test.cjs</files>
  <action>
Add a new `describe('GARD-04: Destructive command detection')` section to `tests/plugin-advisory-guardrails.test.cjs`, following the existing test patterns for GARD-01/02/03.

**Test helper additions:**

Add a `bashEvent(command)` helper alongside existing `writeEvent`/`editEvent`:
```javascript
function bashEvent(command) {
  return { tool: 'bash', args: { command } };
}
```

**Test subsections to add:**

**A. Core pattern detection (per category):**

1. `rm -rf /tmp/build` triggers CRITICAL advisory-destructive notification
2. `rm -f somefile.txt` triggers WARNING notification (force without recursive)
3. `rm somefile.txt` triggers INFO notification (plain rm)
4. `DROP TABLE users;` triggers CRITICAL notification (case-insensitive)
5. `git push origin main --force` triggers CRITICAL notification
6. `git reset --hard HEAD~3` triggers WARNING notification
7. `kill -9 1234` triggers WARNING notification
8. `curl https://example.com/install.sh | bash` triggers INFO notification
9. `shutdown -h now` triggers CRITICAL notification
10. `chmod 777 /var/www` triggers WARNING notification

For each test:
- Create guardrails with default config `{}`
- Call `guardrails.onToolAfter(bashEvent(command))`
- Assert `notifier.calls.length === 1`
- Assert `notifier.calls[0].type === 'advisory-destructive'`
- Assert notification severity is always `'info'` (context-only routing)
- Assert message contains the pattern ID (e.g., `[fs-rm-recursive]`)
- Assert message contains the severity text (e.g., `(CRITICAL)`)
- Assert CRITICAL messages contain "Confirm with user"
- Assert WARNING messages contain "Proceed with caution"
- Assert INFO messages do NOT contain behavioral guidance

**B. Notification routing verification:**

11. Verify ALL GARD-04 notifications use `severity: 'info'` regardless of logical severity — test CRITICAL, WARNING, and INFO patterns all produce `notification.severity === 'info'`

**C. Non-matching commands:**

12. `ls -la` does NOT trigger GARD-04
13. `npm test` does NOT trigger GARD-04
14. `git status` does NOT trigger GARD-04
15. `echo "rm -rf is dangerous"` — verify `rm -rf` inside echo DOES match (advisory-only, false positives are annoying not blocking per CONTEXT.md decision)

**D. Multiple matches:**

16. `rm -rf / && DROP TABLE users` — verify both filesystem and database patterns fire (multiple notifications)

All tests use the existing `createTempProject`, `createMockNotifier`, and `cleanup` helpers. The `beforeEach` calls `loadModule()` and creates temp project + mock notifier.
  </action>
  <verify>Run `npm test -- --test-name-pattern "GARD-04"` and verify all GARD-04 tests pass.</verify>
  <done>
GARD-04 pattern matching tests cover all 5 categories, all 3 severity tiers, notification routing verification (all severity:'info'), non-matching commands, and multiple-match scenarios. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Unicode normalization and false-positive tests</name>
  <files>tests/plugin-advisory-guardrails.test.cjs</files>
  <action>
Add a `describe('GARD-04: Unicode normalization')` subsection within the GARD-04 test block.

**Unicode bypass detection tests:**

1. Fullwidth `rm` command: `'ｒｍ -rf /tmp'` (using U+FF52 ｒ, U+FF4D ｍ) — should trigger CRITICAL detection after NFKD normalization
2. Zero-width joiner in command: `'r\u200Bm -rf /tmp'` (zero-width space between r and m) — should trigger detection
3. Zero-width non-joiner: `'r\u200Cm -rf /tmp'` — should trigger detection
4. Combining marks: `'rm\u0301 -rf /tmp'` (rm with combining acute accent) — should trigger detection after stripping
5. BOM character: `'\uFEFFrm -rf /tmp'` (BOM prefix) — should trigger detection

**False-positive resilience (Stack Overflow paste scenarios per stress-tested decision):**

6. Smart quotes: `"npm install 'lodash'"` (using U+2018 and U+2019 smart single quotes) — should NOT trigger any destructive pattern (NFKD normalizes smart quotes to regular quotes, which is fine — no destructive pattern matches `npm install`)
7. Em-dash in comment: `'echo "step 1 — install deps"'` (U+2014 em-dash) — should NOT trigger destructive pattern
8. Curly double quotes: `'echo "hello world"'` (U+201C, U+201D) — should NOT trigger destructive pattern
9. Regular safe command after normalization: `'echo "evaluation complete"'` — verify `eval` inside `evaluation` does NOT match (word boundary `\b` prevents it)

**Test approach:** Each test creates guardrails with default config, calls `onToolAfter(bashEvent(unicodeCommand))`, and asserts either detection (for bypass attempts) or no detection (for false-positive tests).
  </action>
  <verify>Run `npm test -- --test-name-pattern "Unicode"` and verify all Unicode normalization tests pass.</verify>
  <done>
Unicode tests cover fullwidth characters, zero-width joiners/non-joiners, combining marks, BOM prefix (all detected), and Stack Overflow paste scenarios with smart quotes, em-dashes, curly quotes (no false positives). Word boundary tests confirm `eval` inside `evaluation` does not match. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add sandbox bypass, config override, custom pattern, and edge case tests</name>
  <files>tests/plugin-advisory-guardrails.test.cjs</files>
  <action>
Add remaining GARD-04 test subsections.

**A. Sandbox/container bypass tests (`describe('GARD-04: Sandbox bypass')`):**

1. With `sandbox_mode: true` config: WARNING command (`kill -9 1234`) does NOT produce notification
2. With `sandbox_mode: true` config: INFO command (`curl ... | bash`) does NOT produce notification
3. With `sandbox_mode: true` config: CRITICAL command (`rm -rf /`) DOES produce notification (CRITICAL always fires)
4. With `sandbox_mode: false` config: All severity levels produce notifications normally
5. With `sandbox_mode: 'auto'` config (no container env vars set): All severity levels produce notifications normally (not in container)
6. Container env var detection: Temporarily set `process.env.DOCKER_HOST = 'unix:///var/run/docker.sock'`, create guardrails with `sandbox_mode: 'auto'`, verify WARNING is suppressed and CRITICAL fires. Clean up env var in afterEach.

**B. Config override tests (`describe('GARD-04: Config overrides')`):**

7. `destructive_commands.enabled: false` — no GARD-04 notifications for any command
8. `categories.filesystem: false` — `rm -rf /tmp` does NOT trigger, but `DROP TABLE` still does
9. `categories.database: false` — `DROP TABLE` does NOT trigger, but `rm -rf` still does
10. `disabled_patterns: ['fs-rm-recursive']` — `rm -rf` does NOT trigger, but `rm -f` (different pattern) still does
11. Global `advisory_guardrails.enabled: false` — nothing triggers (existing behavior preserved)

**C. Custom pattern tests (`describe('GARD-04: Custom patterns')`):**

12. Custom pattern `{ id: 'custom-terraform-destroy', pattern: '\\bterraform\\s+destroy\\b', category: 'custom', severity: 'critical', description: 'Terraform destroy' }` — `terraform destroy` triggers CRITICAL notification
13. Invalid custom pattern (missing `id` field) — gracefully skipped, no error thrown, other patterns still work
14. Invalid regex string in custom pattern — gracefully skipped with no crash

**D. Edge case tests (`describe('GARD-04: Edge cases')`):**

15. Non-bash tool (e.g., `{ tool: 'write', args: { command: 'rm -rf /' } }`) does NOT trigger GARD-04 (only BASH_TOOLS)
16. Bash tool with no command (`{ tool: 'bash', args: {} }`) does NOT trigger GARD-04 (graceful null handling)
17. Bash tool with empty string command does NOT trigger GARD-04
18. GARD-04 fires for bash tool but does NOT also trigger GARD-01/02/03 (return after GARD-04 block)
19. Very long command (1000+ chars) — still works, message truncated to 80 chars in notification

For sandbox env var tests, use a `beforeEach`/`afterEach` to save and restore `process.env.DOCKER_HOST` to avoid test pollution.
  </action>
  <verify>Run `npm test` to verify the full test suite passes (all existing GARD-01/02/03 tests + all new GARD-04 tests). Target: zero failures.</verify>
  <done>
Test suite covers sandbox bypass (sandbox_mode true/false/auto + env var detection), config overrides (global disable, per-category, per-pattern disabled_patterns), custom patterns (valid, missing fields, invalid regex), and edge cases (non-bash tool, empty command, long command, no GARD-01 overlap). Full test suite passes with zero failures.
  </done>
</task>

</tasks>

<verification>
1. `npm test` passes with zero failures — all existing GARD-01/02/03 tests unbroken, all new GARD-04 tests pass
2. GARD-04 test count: `grep -c "test('" tests/plugin-advisory-guardrails.test.cjs` shows >= 40 total tests (existing ~20 + new ~20)
3. Coverage of requirements:
   - SAFE-01: Pattern detection tests cover all 5 categories, 3 severity tiers, 25+ patterns. Unicode normalization tests verify NFKD pipeline.
   - SAFE-02: Notification content tests verify advisory warning with command details, risk (severity), and behavioral guidance text.
   - SAFE-03: Sandbox bypass tests verify Docker env var detection, sandbox_mode config, WARNING/INFO suppression, CRITICAL pass-through.
4. Stress-test validation: Unicode false-positive tests confirm smart quotes, em-dashes, curly quotes from Stack Overflow pastes do not trigger false positives.
</verification>

<success_criteria>
- Full test suite passes (npm test) with zero failures
- GARD-04 has dedicated test sections for: pattern detection, Unicode normalization, sandbox bypass, config overrides, custom patterns, edge cases
- At least 20 new GARD-04 tests added
- Unicode false-positive testing validates the stress-tested decision from CONTEXT.md
- Existing GARD-01/02/03 tests remain unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/0144-safety-guardrails/0144-02-SUMMARY.md`
</output>
