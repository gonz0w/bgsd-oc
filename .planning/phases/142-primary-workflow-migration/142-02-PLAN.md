---
phase: 142-primary-workflow-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/new-milestone.md
  - src/lib/questions.js
autonomous: true
requirements:
  - MIGRATE-02
must_haves:
  truths:
    - new-milestone.md uses questionTemplate() for steps 2, 3, 8, 9
    - Conversation flow preserved (goal gathering, version determination, research decision, requirements scoping)
  artifacts:
    - Question templates added to src/lib/questions.js
    - new-milestone.md updated with questionTemplate() calls
  key_links:
    - questionTemplate() in prompts.js calls getQuestionTemplate() in questions.js
    - Graceful fallback generates options at runtime if template missing
---

<objective>
Migrate new-milestone.md steps 2 (Gather Goals), 3 (Determine Version), 8 (Research), 9 (Define Requirements) to use questionTemplate() for inline questions.
</objective>

<execution_context>
@/Users/cam/DEV/bgsd-oc/workflows/new-milestone.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
@/Users/cam/DEV/bgsd-oc/src/lib/prompts.js
</execution_context>

<context>
@.planning/phases/142-primary-workflow-migration/142-01-PLAN.md
Note: Templates created in 142-01 can be referenced. If 'new-milestone-goals' or similar templates already exist from prior work, reuse them.
</context>

<tasks>

<task type="auto">
  <name>Identify inline questions in new-milestone.md steps 2, 3, 8, 9</name>
  <files>workflows/new-milestone.md</files>
  <action>
Search new-milestone.md for inline question patterns in steps 2, 3, 8, 9:

Step 2 (gather_goals, line ~27): "What do you want to build next?" with question() to explore features
Step 3 (determine_version, line ~33): Version confirmation question
Step 8 (research, line ~102): "Research the domain ecosystem?" binary question
Step 8.5 (skill_discovery, line ~135): "Install any skills?" confirmation
Step 9 (define_requirements, line ~155): Multi-select for category scoping

For each question, document: template ID, question text, options, multiSelect flag.
  </action>
  <verify>All inline questions in steps 2,3,8,9 identified</verify>
  <done>List of {templateId, questionText, options, multiSelect} for each question</done>
</task>

<task type="auto">
  <name>Add question templates to questions.js</name>
  <files>src/lib/questions.js</files>
  <action>
Add templates to OPTION_TEMPLATES:

1. 'new-milestone-goals': OPEN question - use EXPLORATION type with runtime fallback
2. 'new-milestone-version': Yes/No confirm version suggestion (BINARY)
3. 'new-milestone-research': Research first / Skip research (SINGLE_CHOICE)
4. 'new-milestone-skills': Yes/No install skills (BINARY)
5. 'new-milestone-scope-category': Multi-select with "None for this milestone" option (MULTI_CHOICE)

Follow OPTION_RULES: MIN 3, MAX 5 options, diversity, escape hatch at last position.
  </action>
  <verify>grep -c "new-milestone-" src/lib/questions.js returns 5+</verify>
  <done>5 question templates added to OPTION_TEMPLATES</done>
</task>

<task type="auto">
  <name>Update new-milestone.md to use questionTemplate() calls</name>
  <files>workflows/new-milestone.md</files>
  <action>
Replace inline question structures with questionTemplate() calls in steps 2, 3, 8, 9:

Step 2 - Gather Goals:
- Replace inline question() for exploring goals with questionTemplate('new-milestone-goals', 'EXPLORATION')

Step 3 - Determine Version:
- Replace version confirmation with questionTemplate('new-milestone-version', 'BINARY')

Step 8 - Research Decision:
- Replace "Research the domain ecosystem?" with questionTemplate('new-milestone-research', 'SINGLE_CHOICE')

Step 8.5 - Skill Discovery:
- Replace "Install any skills?" confirmation with questionTemplate('new-milestone-skills', 'BINARY')

Step 9 - Define Requirements:
- Replace category scoping multi-select with questionTemplate('new-milestone-scope-category', 'MULTI_CHOICE')

Preserve question text, headers, and multiSelect flags. Only options come from templates.
  </action>
  <verify>grep "questionTemplate" workflows/new-milestone.md | wc -l returns 5+</verify>
  <done>new-milestone.md uses questionTemplate() for steps 2,3,8,9 questions</done>
</task>

</tasks>

<verification>
- Verify: grep -c "questionTemplate" workflows/new-milestone.md returns 5+
- Verify: Steps 2, 3, 8, 9 all use questionTemplate() calls
- Verify: MultiSelect flags preserved for step 9
</verification>

<success_criteria>
- [ ] All 5 question templates added to questions.js OPTION_TEMPLATES
- [ ] new-milestone.md has questionTemplate() calls for steps 2, 3, 8, 9
- [ ] Conversation flow (goal gathering, version, research, requirements) preserved
- [ ] MultiSelect preserved for category scoping in step 9
</success_criteria>

<output>
After completion, create `.planning/phases/142-primary-workflow-migration/142-02-SUMMARY.md`
</output>
