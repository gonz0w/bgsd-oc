---
phase: 142-primary-workflow-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/plan-phase.md
  - src/lib/questions.js
autonomous: true
requirements:
  - MIGRATE-03
must_haves:
  truths:
    - plan-phase.md uses questionTemplate() for inline questions
    - Planning context injection intact (research paths, existing plans, assertions)
  artifacts:
    - Question templates added to src/lib/questions.js
    - plan-phase.md updated with questionTemplate() calls
  key_links:
    - questionTemplate() in prompts.js calls getQuestionTemplate() in questions.js
    - Graceful fallback generates options at runtime if template missing
---

<objective>
Migrate plan-phase.md to use questionTemplate() for inline questions while preserving planning context injection.
</objective>

<execution_context>
@/Users/cam/DEV/bgsd-oc/workflows/plan-phase.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
@/Users/cam/DEV/bgsd-oc/src/lib/prompts.js
</execution_context>

<tasks>

<task type="auto">
  <name>Identify inline questions in plan-phase.md</name>
  <files>workflows/plan-phase.md</files>
  <action>
Search plan-phase.md for inline question patterns:

1. Step 4 (load_context): Context without/discuss-phase first question
2. Step 6 (check_existing): Add more / View / Replan options for existing plans
3. Step 10-12 (checker): Continue / Adjust / Force options

For each question, document: template ID, question text, options, multiSelect flag.
  </action>
  <verify>All inline questions in plan-phase.md identified</verify>
  <done>List of {templateId, questionText, options} for each question</done>
</task>

<task type="auto">
  <name>Add question templates to questions.js</name>
  <files>src/lib/questions.js</files>
  <action>
Add templates to OPTION_TEMPLATES:

1. 'plan-phase-context': Continue without / Run discuss-phase first (SINGLE_CHOICE)
2. 'plan-phase-existing': Add more / View existing / Replan (SINGLE_CHOICE)  
3. 'plan-phase-checker-passed': Continue / View plans (SINGLE_CHOICE)
4. 'plan-phase-checker-issues': Force / Guidance / Abandon (SINGLE_CHOICE)

Follow OPTION_RULES: MIN 3, MAX 5 options, diversity, escape hatch at last position.
  </action>
  <verify>grep -c "plan-phase-" src/lib/questions.js returns 4+</verify>
  <done>4 question templates added to OPTION_TEMPLATES</done>
</task>

<task type="auto">
  <name>Update plan-phase.md to use questionTemplate() calls</name>
  <files>workflows/plan-phase.md</files>
  <action>
Replace inline question structures with questionTemplate() calls:

1. Step 4: Replace "continue without or run discuss-phase first" with questionTemplate('plan-phase-context', 'SINGLE_CHOICE')

2. Step 6: Replace "add more / view / replan" options with questionTemplate('plan-phase-existing', 'SINGLE_CHOICE')

3. Step 10-12: Replace checker outcome options with questionTemplate('plan-phase-checker-*' appropriately)

Preserve question text, headers, and multiSelect flags. Only options come from templates.
  </action>
  <verify>grep "questionTemplate" workflows/plan-phase.md | wc -l returns 3+</verify>
  <done>plan-phase.md uses questionTemplate() for all questions</done>
</task>

</tasks>

<verification>
- Verify: grep -c "questionTemplate" workflows/plan-phase.md returns 3+
- Verify: Planning context injection patterns still present in workflow
</verification>

<success_criteria>
- [ ] All question templates added to questions.js OPTION_TEMPLATES
- [ ] plan-phase.md has questionTemplate() calls for inline questions
- [ ] Planning context injection intact
- [ ] All multiSelect flags and headers preserved
</success_criteria>

<output>
After completion, create `.planning/phases/142-primary-workflow-migration/142-03-SUMMARY.md`
</output>
