---
phase: 142-primary-workflow-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/discuss-phase.md
  - src/lib/questions.js
autonomous: true
requirements:
  - MIGRATE-01
must_haves:
  truths:
    - discuss-phase.md uses questionTemplate() calls for all questions
    - Zero inline question text remains in discuss-phase.md
    - Conversation flow is preserved (gray area selection, Socratic discussion, stress test)
  artifacts:
    - Question templates added to src/lib/questions.js OPTION_TEMPLATES
    - discuss-phase.md updated with questionTemplate() calls
  key_links:
    - questionTemplate() in prompts.js calls getQuestionTemplate() in questions.js
    - Graceful fallback: if template missing, questionTemplate() generates runtime options
---

<objective>
Migrate discuss-phase.md to use questionTemplate() for all inline questions. Add templates for: gray area selection, existing context options, stress test response, and Socratic discussion prompts.
</objective>

<execution_context>
@/Users/cam/DEV/bgsd-oc/workflows/discuss-phase.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
@/Users/cam/DEV/bgsd-oc/src/lib/prompts.js
</execution_context>

<tasks>

<task type="auto">
  <name>Identify all inline questions in discuss-phase.md</name>
  <files>workflows/discuss-phase.md</files>
  <action>
Search discuss-phase.md for all inline question patterns:
1. Line ~58: "use question:" with existing context options
2. Line ~63: "use question:" with replan warning
3. Line ~94-99: present_gray_areas step with multiSelect question
4. Line ~117: Socratic discussion loop questions (4 per area)
5. Line ~147: stress test response question

For each question, document: template ID, question text, options, multiSelect flag.
  </action>
  <verify>All inline questions identified with their template IDs</verify>
  <done>List of {templateId, questionText, options} for each question in discuss-phase.md</done>
</task>

<task type="auto">
  <name>Add question templates to questions.js</name>
  <files>src/lib/questions.js</files>
  <action>
Add templates to OPTION_TEMPLATES for each identified question:

1. 'discuss-context-existing': Update/View/Skip options
2. 'discuss-replan-warning': Continue/View/Cancel options  
3. 'discuss-gray-areas': Dynamic based on phase gray areas (use runtime fallback initially)
4. 'discuss-stress-test-response': No changes / Revisit a decision options
5. 'discuss-socratic-continue': More questions / Next area options

Follow OPTION_RULES: MIN 3, MAX 5 options, diversity, escape hatch "Something else" at last position.
Each template should have typeHint based on question type (SINGLE_CHOICE vs MULTI_CHOICE).
  </action>
  <verify>grep -c "discuss-" src/lib/questions.js returns 5+ template entries</verify>
  <done>5 question templates added to OPTION_TEMPLATES in questions.js</done>
</task>

<task type="auto">
  <name>Update discuss-phase.md to use questionTemplate() calls</name>
  <files>workflows/discuss-phase.md</files>
  <action>
Replace inline question structures with questionTemplate() calls:

1. Replace inline options in present_gray_areas with:
   ```
   let qt = questionTemplate('discuss-gray-areas', 'MULTI_CHOICE', {phase});
   question([{
     question: "Which areas do you want to discuss for [phase name]?",
     header: "Discuss",
     multiSelect: true,
     options: qt.options
   }])
   ```

2. Replace stress test response question inline options with questionTemplate() call

3. For Socratic loop: use questionTemplate('discuss-socratic-continue', 'SINGLE_CHOICE')

4. For existing context: use questionTemplate('discuss-context-existing', 'SINGLE_CHOICE')

5. For replan warning: use questionTemplate('discuss-replan-warning', 'SINGLE_CHOICE')

Preserve all question text, headers, and multiSelect flags. Only the options come from templates.
  </action>
  <verify>grep "questionTemplate" workflows/discuss-phase.md | wc -l returns 5+</verify>
  <done>discuss-phase.md uses questionTemplate() for all questions, zero inline question text remains</done>
</task>

</tasks>

<verification>
- Run: node bin/bgsd-tools.cjs verify:validate plan-structure .planning/phases/142-primary-workflow-migration/142-01-PLAN.md
- Verify: grep -c "inline" workflows/discuss-phase.md returns 0
- Verify: grep "questionTemplate" workflows/discuss-phase.md returns 5+ matches
</verification>

<success_criteria>
- [ ] All 5 question templates added to questions.js OPTION_TEMPLATES
- [ ] discuss-phase.md has 5+ questionTemplate() calls
- [ ] Zero inline question text remains in discuss-phase.md
- [ ] All multiSelect flags and headers preserved
- [ ] Conversation flow unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/142-primary-workflow-migration/142-01-SUMMARY.md`
</output>
