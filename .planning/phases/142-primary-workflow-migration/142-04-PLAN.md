---
phase: 142-primary-workflow-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/transition.md
  - src/lib/questions.js
autonomous: true
requirements:
  - MIGRATE-04
must_haves:
  truths:
    - transition.md uses questionTemplate() for inline questions
    - Phase transition workflow unchanged
  artifacts:
    - Question templates added to src/lib/questions.js
    - transition.md updated with questionTemplate() calls
  key_links:
    - questionTemplate() in prompts.js calls getQuestionTemplate() in questions.js
    - Graceful fallback generates options at runtime if template missing
---

<objective>
Migrate transition.md to use questionTemplate() for inline questions while preserving phase transition workflow.
</objective>

<execution_context>
@/Users/cam/DEV/bgsd-oc/workflows/transition.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
@/Users/cam/DEV/bgsd-oc/src/lib/prompts.js
</execution_context>

<tasks>

<task type="auto">
  <name>Identify inline questions in transition.md</name>
  <files>workflows/transition.md</files>
  <action>
Search transition.md for inline question patterns:

Line ~45: "Phase [X] complete — all [Y] plans finished. Ready to mark done and move to Phase [X+1]?"

Line ~56-64: Incomplete plans options:
- Continue current phase
- Mark complete anyway
- Review what's left

Line ~160-171: Route A/B decision for next phase

For each question, document: template ID, question text, options, multiSelect flag.
  </action>
  <verify>All inline questions in transition.md identified</verify>
  <done>List of {templateId, questionText, options} for each question</done>
</task>

<task type="auto">
  <name>Add question templates to questions.js</name>
  <files>src/lib/questions.js</files>
  <action>
Add templates to OPTION_TEMPLATES:

1. 'transition-complete': Mark done / Cancel (SINGLE_CHOICE) - for complete phase transition
2. 'transition-incomplete': Continue / Mark complete / Review (SINGLE_CHOICE) - for incomplete plans
3. 'transition-next-route': Route A (more phases) vs Route B (milestone complete) options (SINGLE_CHOICE)

Follow OPTION_RULES: MIN 3, MAX 5 options, diversity, escape hatch at last position.
  </action>
  <verify>grep -c "transition-" src/lib/questions.js returns 3+</verify>
  <done>3 question templates added to OPTION_TEMPLATES</done>
</task>

<task type="auto">
  <name>Update transition.md to use questionTemplate() calls</name>
  <files>workflows/transition.md</files>
  <action>
Replace inline question structures with questionTemplate() calls:

1. Line ~45: Replace "Ready to mark done?" with questionTemplate('transition-complete', 'SINGLE_CHOICE')

2. Line ~56-64: Replace incomplete plans options with questionTemplate('transition-incomplete', 'SINGLE_CHOICE')

3. Line ~160-171: Replace Route A/B options with questionTemplate('transition-next-route', 'SINGLE_CHOICE')

Preserve question text, headers, and multiSelect flags. Only options come from templates.
  </action>
  <verify>grep "questionTemplate" workflows/transition.md | wc -l returns 3+</verify>
  <done>transition.md uses questionTemplate() for all questions</done>
</task>

</tasks>

<verification>
- Verify: grep -c "questionTemplate" workflows/transition.md returns 3+
- Verify: Phase transition workflow logic intact
</verification>

<success_criteria>
- [ ] All question templates added to questions.js OPTION_TEMPLATES
- [ ] transition.md has questionTemplate() calls for inline questions
- [ ] Phase transition workflow unchanged
- [ ] Safety rails preserved (incomplete plans prompt still functional)
</success_criteria>

<output>
After completion, create `.planning/phases/142-primary-workflow-migration/142-04-SUMMARY.md`
</output>
