---
phase: 58-research-orchestration
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified: [workflows/research-phase.md, bin/gsd-tools.cjs]
autonomous: true
requirements: [ORCH-03]

must_haves:
  truths:
    - "Researcher agent workflow calls research:collect before spawning the researcher subagent"
    - "Collected sources are injected into researcher prompt as additional_context when tier < 4"
    - "At tier 4 (no tools or --quick), researcher prompt is identical to current behavior — zero regression"
    - "Workflow passes --quick flag through when present in command arguments"
  artifacts:
    - path: "workflows/research-phase.md"
      provides: "research:collect integration step in researcher workflow"
      contains: "research:collect"
    - path: "bin/gsd-tools.cjs"
      provides: "Built bundle with all Phase 58 changes"
  key_links:
    - from: "workflows/research-phase.md"
      to: "src/commands/research.js"
      via: "subprocess call to research:collect"
      pattern: "research:collect"
    - from: "workflows/research-phase.md"
      to: "workflows/research-phase.md"
      via: "agent_context injection into researcher prompt"
      pattern: "agent_context"
---

<objective>
Integrate the research:collect pipeline into the existing researcher agent workflow (`workflows/research-phase.md`), so that collected sources are conditionally injected into the researcher's prompt when tools are available, with zero regression when tools are absent.

Purpose: Completes ORCH-03 — existing researcher agents use the new RAG pipeline transparently. Traces to DO-28 (deterministic context loading: agents receive pre-computed source context instead of discovering it themselves).
Output: Updated workflow with source collection step, rebuilt bundle.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-research-orchestration/58-RESEARCH.md
@.planning/phases/58-research-orchestration/58-01-SUMMARY.md
@workflows/research-phase.md
@src/commands/research.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate research:collect into researcher agent workflow</name>
  <files>workflows/research-phase.md</files>
  <action>
Modify `workflows/research-phase.md` to add a source collection step between Step 3 (Gather Phase Context) and Step 4 (Spawn Researcher).

**Add Step 3.5: Collect Research Sources**

Insert a new step after Step 3 and before Step 4. The step:

1. Calls `research:collect` with the phase description as the query:
```
COLLECT_OUTPUT=$(node __OPENCODE_CONFIG__/get-shit-done/bin/gsd-tools.cjs research:collect "${PHASE_DESCRIPTION}" 2>/dev/null)
```

2. Extracts `tier` and `agent_context` from the JSON output:
```
TIER=$(echo "$COLLECT_OUTPUT" | jq -r '.tier // 4')
AGENT_CONTEXT=$(echo "$COLLECT_OUTPUT" | jq -r '.agent_context // ""')
```

3. Documents the conditional injection logic:
   - If `TIER < 4` AND `AGENT_CONTEXT` is non-empty: include collected sources in the `<additional_context>` block of the researcher prompt (Step 4).
   - If `TIER == 4` OR `AGENT_CONTEXT` is empty: use the existing prompt unchanged. **Do NOT inject empty tags or error messages.** This ensures zero regression — the researcher works exactly as before when no tools are available.

4. If `--quick` flag was passed to the workflow command: pass `--quick` through to `research:collect` so it returns tier 4 immediately.

**Modify Step 4: Spawn Researcher**

Update the `<additional_context>` block in the Task() prompt to conditionally include collected sources:

```
<additional_context>
Phase description: {description}
If INTENT.md exists: scope research to align with stated project objective and desired outcomes. Prioritize findings relevant to P1 outcomes.

{If AGENT_CONTEXT is non-empty:}
The following sources were collected automatically from web search and YouTube. Use them to ground your research findings. Cite sources by URL when referencing specific information.

{AGENT_CONTEXT}
{End if}
</additional_context>
```

**Key constraints:**
- Do NOT change the researcher subagent type or model selection.
- Do NOT change the output path or format.
- The workflow step is additive — all existing steps remain unchanged.
- Use `2>/dev/null` on the collect command to suppress stderr status messages (they're for interactive use, not workflow parsing).
- If `research:collect` fails entirely (command not found, JSON parse error), treat as tier 4 — no sources injected, researcher proceeds normally.
  </action>
  <verify>
Read the updated `workflows/research-phase.md` and verify:
- Step 3.5 exists between Step 3 and Step 4.
- Conditional injection logic is documented clearly.
- Tier 4 case explicitly states "use existing prompt unchanged."
- No changes to Step 1, 2, 3, or 5.
  </verify>
  <done>
research-phase.md workflow includes source collection step that calls research:collect, conditionally injects sources into researcher prompt at tier < 4, and preserves identical behavior at tier 4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final build and verify zero regression</name>
  <files>bin/gsd-tools.cjs</files>
  <action>
1. Run `npm run build` to produce final bundle with all Phase 58 changes.
2. Verify bundle size is within 1500KB budget.
3. Run `node bin/gsd-tools.cjs research:collect --quick "test"` to confirm collect command still works in the final bundle.
4. Run `node bin/gsd-tools.cjs research:capabilities --pretty` to confirm existing capabilities command is unaffected.
5. Run `node bin/gsd-tools.cjs research:yt-search --help` to confirm existing yt-search help is unaffected.
6. Verify no regressions in existing research namespace commands.
  </action>
  <verify>
`npm run build` succeeds. Bundle < 1500KB. All existing research commands produce expected output. research:collect --quick returns tier 4 JSON.
  </verify>
  <done>
Final bundle built with all Phase 58 code, bundle within size budget, all existing commands verified working with zero regressions.
  </done>
</task>

</tasks>

<verification>
1. `workflows/research-phase.md` contains Step 3.5 with `research:collect` integration
2. Tier 4 path produces identical researcher prompt to current behavior (no empty tags, no error messages)
3. Tier 2/3 path includes `<collected_sources>` XML in researcher's `<additional_context>`
4. --quick flag passes through from workflow to research:collect
5. Existing research commands (capabilities, yt-search, yt-transcript) work unchanged
6. Bundle builds and size < 1500KB
</verification>

<success_criteria>
- research-phase.md workflow conditionally injects collected sources into researcher prompt
- Zero regression: tier 4 (no tools) produces byte-identical researcher behavior
- --quick flag bypasses collection in the workflow
- Final bundle builds cleanly within size budget
- All existing research namespace commands verified functional
</success_criteria>

<output>
After completion, create `.planning/phases/58-research-orchestration/58-02-SUMMARY.md`
</output>
