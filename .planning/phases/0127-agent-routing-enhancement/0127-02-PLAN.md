---
phase: 127-agent-routing-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - "0127-01"
files_modified:
  - tests/decisions.test.cjs
  - tests/enricher-decisions.test.cjs
autonomous: true
requirements:
  - AGENT-01

must_haves:
  truths:
    - "Contract tests verify resolveFileDiscoveryMode returns correct tool for all scope/availability combinations"
    - "Contract tests verify resolveSearchMode returns correct tool for all gitignore/availability combinations"
    - "Contract tests verify resolveJsonTransformMode returns correct tool for all complexity/availability combinations"
    - "Enricher test confirms tool_availability appears in bgsd-context output"
    - "All three resolve functions are in DECISION_REGISTRY and evaluated by evaluateDecisions"
  artifacts:
    - path: "tests/decisions.test.cjs"
      provides: "Contract tests for three tool routing decision functions"
      contains: "resolveFileDiscoveryMode"
    - path: "tests/enricher-decisions.test.cjs"
      provides: "Enricher integration test for tool_availability"
      contains: "tool_availability"
  key_links:
    - from: "tests/decisions.test.cjs"
      to: "src/lib/decision-rules.js"
      via: "require and test each resolve function"
      pattern: "resolveFileDiscoveryMode|resolveSearchMode|resolveJsonTransformMode"
    - from: "tests/enricher-decisions.test.cjs"
      to: "src/plugin/command-enricher.js"
      via: "enrichment output validation"
      pattern: "tool_availability"
---

<objective>
Add contract tests for all three tool routing decision functions and enricher tool_availability.

Purpose: Verify the AGENT-01 resolve functions produce correct outputs across all input combinations — ensuring hardcoded fallback chains, tool-name-only returns, and DECISION_REGISTRY integration work as designed. Traces to DO-89 (decision functions with contract tests), SC-69 (decision functions with 85%+ confidence, 100+ tests).

Output: Comprehensive test coverage for Phase 127 decision functions and enricher integration.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0127-agent-routing-enhancement/0127-01-SUMMARY.md
@src/lib/decision-rules.js
@tests/decisions.test.cjs
@tests/enricher-decisions.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract tests for tool routing decision functions</name>
  <files>tests/decisions.test.cjs</files>
  <action>
Add a new `describe('Phase 127: Tool routing decision functions', ...)` block to `tests/decisions.test.cjs` with the following test cases:

**resolveFileDiscoveryMode tests (~12 tests):**
- `scope: 'single-file'` with all tools available → returns `'node'` (no tool needed)
- `scope: 'single-file'` with no tools → returns `'node'`
- `scope: 'directory'` with fd available → returns `'fd'`
- `scope: 'directory'` with fd unavailable → returns `'node'`
- `scope: 'project-wide'` with fd available → returns `'fd'`
- `scope: 'project-wide'` with fd unavailable → returns `'node'`
- Missing scope defaults gracefully (returns `'node'`)
- Missing tool_availability defaults gracefully (returns `'node'`)
- All results have `confidence: 'HIGH'` and `rule_id: 'file-discovery-mode'`
- Return value is always a string (just the tool name, no rationale per CONTEXT.md)

**resolveSearchMode tests (~12 tests):**
- ripgrep available → returns `'ripgrep'` regardless of gitignore flag
- ripgrep unavailable, fd available, needs_gitignore_respect true → returns `'fd'`
- ripgrep unavailable, fd available, needs_gitignore_respect false → returns `'node'` (fd is for gitignore-respecting search)
- All tools unavailable → returns `'node'`
- Only fd available, no gitignore need → returns `'node'`
- Default needs_gitignore_respect (true) when not specified → ripgrep if available
- Missing tool_availability → returns `'node'`
- All results have `rule_id: 'search-mode'`
- Fallback chain is ripgrep → fd → node (hardcoded per CONTEXT.md)

**resolveJsonTransformMode tests (~10 tests):**
- `json_complexity: 'simple'` with jq available → returns `'javascript'` (no tool needed for simple ops)
- `json_complexity: 'simple'` with jq unavailable → returns `'javascript'`
- `json_complexity: 'complex'` with jq available → returns `'jq'`
- `json_complexity: 'complex'` with jq unavailable → returns `'javascript'`
- Missing json_complexity defaults gracefully (returns `'javascript'`)
- Missing tool_availability → returns `'javascript'`
- All results have `rule_id: 'json-transform-mode'`
- Fallback chain is jq → javascript for complex, always javascript for simple

**DECISION_REGISTRY integration tests (~4 tests):**
- All three rules appear in DECISION_REGISTRY (filter by category 'tool-routing')
- Each registry entry has required fields: id, name, category, inputs, outputs, confidence_range, resolve
- `evaluateDecisions('bgsd-execute-phase', { tool_availability: {...}, scope: 'project-wide' })` includes `'file-discovery-mode'` in result keys
- `evaluateDecisions` returns results for all three rules when all inputs present

Follow the existing test pattern in decisions.test.cjs (require from bin/bgsd-tools.cjs, use describe/it blocks, assert with strictEqual/deepStrictEqual).

Target: ~38 tests total for comprehensive coverage of all input combinations.
  </action>
  <verify>
Run `npm test` — all tests pass including new ones.
Run `npm test 2>&1 | grep -c "passing"` to confirm test count increased by ~38 from 1427 baseline.
  </verify>
  <done>
- ~38 new contract tests cover all input/output combinations for three resolve functions
- DECISION_REGISTRY integration tests confirm proper registration
- evaluateDecisions aggregator includes new rules
- All tests pass (1427 + ~38 = 1465+ total)
  </done>
</task>

<task type="auto">
  <name>Task 2: Enricher integration test for tool_availability</name>
  <files>tests/enricher-decisions.test.cjs</files>
  <action>
Add a new `describe('Phase 127: tool_availability enrichment', ...)` block to `tests/enricher-decisions.test.cjs` with the following test cases:

**tool_availability shape tests (~8 tests):**
- Enrichment output includes `tool_availability` key
- `tool_availability` is an object (not null, not array)
- `tool_availability` has exactly 6 keys: ripgrep, fd, jq, yq, bat, gh
- Each value in `tool_availability` is a boolean (typeof check per tool)
- `tool_availability` does NOT contain version info (per CONTEXT.md: "just true/false per tool")
- `tool_availability` does NOT contain path info
- When enricher runs, tool_availability is populated (non-empty object)
- tool_availability keys match the TOOLS constant from detector.js

**Decision evaluation with tool_availability (~4 tests):**
- When tool_availability is in enrichment state, evaluateDecisions includes tool-routing results
- tool-routing decisions all have HIGH confidence
- Decision results have correct rule_ids (file-discovery-mode, search-mode, json-transform-mode)
- Results contain string values (tool names only, per CONTEXT.md)

Follow the existing test pattern in enricher-decisions.test.cjs. If the existing tests mock the enricher, follow the same mock pattern. If they test against the built binary, follow that pattern.

Target: ~12 tests for enricher integration coverage.
  </action>
  <verify>
Run `npm test` — all tests pass including new enricher tests.
Confirm total test count is 1465+ (1427 baseline + ~38 decision tests + ~12 enricher tests = ~1477).
  </verify>
  <done>
- ~12 enricher tests confirm tool_availability shape, types, and decision integration
- tool_availability is verified as true/false only (no version or path per CONTEXT.md)
- All tests pass (1477+ total)
- SC-69 satisfied: new decision functions have contract tests
  </done>
</task>

</tasks>

<verification>
1. `npm test` — all tests pass, count is 1465+ (baseline 1427 + ~50 new)
2. Decision tests cover all input combinations for all three resolve functions
3. Enricher tests verify tool_availability shape and boolean-only values
4. DECISION_REGISTRY integration tests confirm all three rules registered
5. No regressions in existing tests
</verification>

<success_criteria>
- Contract tests exist for all three resolve functions covering all input combinations
- DECISION_REGISTRY integration tests confirm proper registration
- Enricher tests confirm tool_availability in bgsd-context output with correct shape
- Total test count increased by ~50 from 1427 baseline
- All tests pass (zero failures)
</success_criteria>

<output>
After completion, create `.planning/phases/0127-agent-routing-enhancement/0127-02-SUMMARY.md`
</output>
