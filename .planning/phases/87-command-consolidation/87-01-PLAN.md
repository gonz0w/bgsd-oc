---
phase: 87-command-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/
  - workflows/
autonomous: true
requirements:
  - CMND-01
  - CMND-02
  - CMND-03
  - CMND-04

must_haves:
  truths:
    - "Slash command surface is reduced through subcommand groups"
    - "Commands organized into logical categories (plan, exec, config, etc.)"
    - "Stale commands identified and documented for removal"
    - "Overlapping commands consolidated into single entry points"
    - "Internal-only functions not exposed as slash commands"
  artifacts:
    - path: "commands/bgsd-plan.md"
      provides: "Subcommand group wrapper for planning commands"
    - path: "commands/bgsd-exec.md"
      provides: "Subcommand group wrapper for execution commands"
    - path: "commands/bgsd-config.md"
      provides: "Subcommand group wrapper for configuration commands"
    - path: "commands/bgsd-util.md"
      provides: "Subcommand group wrapper for utility commands"
    - path: "commands/bgsd-roadmap.md"
      provides: "Subcommand group wrapper for roadmap management"
    - path: "commands/bgsd-milestone.md"
      provides: "Subcommand group wrapper for milestone commands"
    - path: "COMMAND_CONSOLIDATION.md"
      provides: "Documentation of old commands to remove"
  key_links:
    - from: "bgsd-plan.md"
      to: "workflows/plan-*.md"
      via: "subcommand routing"
    - from: "bgsd-exec.md"
      to: "workflows/execute-*.md"
      via: "subcommand routing"
---

<objective>
Create subcommand group structure for bGSD slash commands. Organize 41 commands into logical groups with wrapper commands that route to existing workflows.

Purpose: Reduce cognitive load, simplify command discovery, maintain backward compatibility through routing.

Output: 6-8 new subcommand wrapper commands, COMMAND_CONSOLIDATION.md documenting commands to remove.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@/mnt/raid/DEV/bgsd-oc/commands/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze and categorize all 41 existing commands</name>
  <files>commands/*.md</files>
  <action>
Analyze all 41 commands in commands/ directory and categorize into logical groups:
- Planning (new-project, discuss-phase, research-phase, list-phase-assumptions, plan-phase)
- Execution (execute-phase, quick, github-ci)
- Roadmap (add-phase, insert-phase, remove-phase)
- Milestone (new-milestone, complete-milestone, audit-milestone, plan-milestone-gaps)
- Session (resume-work, pause-work, progress)
- Debug (debug, verify-work)
- Todo (add-todo, check-todos)
- Config (settings, set-profile, validate-config)
- Utilities (map-codebase, cleanup, help, update, velocity, validate-deps, test-run, trace-requirement, search-decisions, search-lessons, session-diff, rollback-info, context-budget, codebase-impact, reapply-patches, health)

Document findings in COMMAND_CONSOLIDATION.md with categories and routing targets.
  </action>
  <verify>ls commands/ | wc -l (should show 41 + new wrappers)</verify>
  <done>All 41 commands categorized into 8 logical groups with routing targets identified</done>
</task>

<task type="auto">
  <name>Task 2: Create subcommand wrapper commands</name>
  <files>commands/bgsd-plan.md, commands/bgsd-exec.md, commands/bgsd-config.md, commands/bgsd-util.md</files>
  <action>
Create subcommand wrapper commands for each group. Each wrapper:
- Has description indicating it's a group command
- Uses process to route to specific workflow based on $ARGUMENTS
- Example: /bgsd plan phase 1 -> routes to /bgsd-plan-phase 1

Create these wrapper commands:
1. commands/bgsd-plan.md - Routes: project, discuss, research, assumptions, phase
2. commands/bgsd-exec.md - Routes: phase, quick, ci
3. commands/bgsd-roadmap.md - Routes: add, insert, remove
4. commands/bgsd-milestone.md - Routes: new, complete, audit, gaps
5. commands/bgsd-session.md - Routes: resume, pause, progress
6. commands/bgsd-debug.md - Routes: debug, verify
7. commands/bgsd-todo.md - Routes: add, check
8. commands/bgsd-config.md - Routes: settings, profile, validate
9. commands/bgsd-util.md - Routes: map, cleanup, help, update, velocity, validate-deps, test-run, trace, search-decisions, search-lessons, session-diff, rollback-info, context-budget, codebase-impact, reapply-patches, health
  </action>
  <verify>ls commands/bgsd-*.md | wc -l</verify>
  <done>9 new subcommand wrapper commands created with proper routing logic</done>
</task>

<task type="auto">
  <name>Task 3: Document commands to remove and mark internal-only</name>
  <files>COMMAND_CONSOLIDATION.md</files>
  <action>
Create COMMAND_CONSOLIDATION.md documenting:
1. OLD COMMANDS TO REMOVE: List all 41 original commands that are now superseded by subcommand groups (but note: some may need to remain for backward compatibility during transition)

2. INTERNAL-ONLY COMMANDS: Identify commands that should NOT be exposed as slash commands:
   - Commands only used by other agents
   - Commands that are implementation details

3. MAPPING: Complete mapping from old /bgsd-* to new /bgsd-* subcommand structure

Example mapping:
- /bgsd-plan-phase 1 -> /bgsd plan phase 1
- /bgsd-new-project -> /bgsd plan new-project
- /bgsd-execute-phase 1 -> /bgsd exec phase 1
  </action>
  <verify>cat COMMAND_CONSOLIDATION.md | head -50</verify>
  <done>COMMAND_CONSOLIDATION.md created with complete mapping and removal list</done>
</task>

</tasks>

<verification>
- Verify 9 new wrapper commands exist in commands/
- Verify each wrapper routes to correct workflow
- Verify COMMAND_CONSOLIDATION.md documents all 41 original commands
</verification>

<success_criteria>
- Subcommand groups created: plan, exec, roadmap, milestone, session, debug, todo, config, util
- Each group has a wrapper command that routes to specific workflows
- COMMAND_CONSOLIDATION.md documents old commands and mapping
- Commands organized into logical subcommand groups (CMND-01)
</success_criteria>

<output>
After completion, create `.planning/phases/87-command-consolidation/87-01-SUMMARY.md`
</output>
