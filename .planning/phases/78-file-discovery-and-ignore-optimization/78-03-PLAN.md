---
phase: 78-file-discovery-and-ignore-optimization
plan: 03
type: execute
wave: 3
depends_on:
  - 78-02
files_modified:
  - tests/codebase.test.cjs
  - tests/helpers.cjs
  - baseline.cjs
  - src/lib/adapters/discovery.js
autonomous: true
requirements:
  - SCAN-01
  - SCAN-03
intent:
  outcome_ids:
    - DO-20
    - DO-32
    - DO-38
  rationale: "Parity and benchmark gates make the scan optimization safe to keep enabled while proving measurable user-facing improvement."
must_haves:
  truths:
    - "Maintainer can prove optimized scan outputs match legacy file-selection behavior across tricky ignore scenarios."
    - "User-facing scan-heavy commands show measurable end-to-end improvement versus legacy baseline evidence."
    - "If parity mismatches appear, maintainers have deterministic evidence and rollback controls to restore legacy behavior quickly."
  artifacts:
    - path: tests/codebase.test.cjs
      provides: "Parity fixture matrix for legacy versus optimized scan outputs"
      contains: "nested .gitignore"
    - path: tests/helpers.cjs
      provides: "Reusable fixture setup utilities for parity and benchmark scenarios"
      contains: "createTempProject"
    - path: baseline.cjs
      provides: "Benchmark capture for scan optimization evidence"
      contains: "baseline"
    - path: src/lib/adapters/discovery.js
      provides: "Mismatch diagnostics and safe fallback toggles"
      contains: "fallback"
  key_links:
    - from: tests/codebase.test.cjs
      to: src/lib/adapters/discovery.js
      via: "legacy-versus-optimized parity assertions"
      pattern: "parity|legacy|optimized"
    - from: baseline.cjs
      to: src/lib/codebase-intel.js
      via: "codebase analyze benchmark run"
      pattern: "codebase analyze"
    - from: tests/helpers.cjs
      to: tests/codebase.test.cjs
      via: "shared fixture generation for ignore edge cases"
      pattern: "fixture|temp"
---

<objective>
Complete Phase 78 with explicit parity and performance evidence, then lock in safe adoption criteria for optimized scan behavior.

Purpose: Satisfy parity hard-gate requirements by proving equivalence on edge-case fixtures while documenting measurable speed improvements and rollback readiness.
Output: Dedicated parity fixture tests, benchmark evidence capture, and finalized fallback/diagnostic behavior for ongoing safety.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/78-file-discovery-and-ignore-optimization/78-RESEARCH.md
@.planning/phases/78-file-discovery-and-ignore-optimization/78-01-SUMMARY.md
@.planning/phases/78-file-discovery-and-ignore-optimization/78-02-SUMMARY.md
@tests/codebase.test.cjs
@tests/helpers.cjs
@baseline.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parity fixture matrix for ignore and traversal edge cases</name>
  <files>tests/codebase.test.cjs, tests/helpers.cjs</files>
  <action>Create parity tests that compare legacy and optimized discovery outputs for high-risk scenarios from research: nested `.gitignore`, negation rules, hidden directories/files, symlinked entries, root-level source files, and fallback-to-root behavior. Use deterministic fixture setup helpers so parity tests are reproducible across environments.</action>
  <verify>npm run test:file -- tests/codebase.test.cjs</verify>
  <done>Parity suite deterministically validates legacy and optimized source-dir/file-selection equivalence across all targeted edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: Capture benchmark evidence for scan optimization requirements</name>
  <files>baseline.cjs, tests/helpers.cjs</files>
  <action>Update benchmark capture paths to include scan-focused before/after evidence using forced legacy mode and optimized mode runs on representative large-tree fixtures. Record duration deltas and command context needed for repeatability in plan summary evidence, and fail completion if no measurable improvement is observed.</action>
  <verify>npm run baseline</verify>
  <done>Baseline tooling can generate repeatable legacy-vs-optimized scan timing evidence with explicit command metadata and percent-change reporting.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize safety controls and phase completion validation gates</name>
  <files>src/lib/adapters/discovery.js, tests/codebase.test.cjs</files>
  <action>Ensure fallback/diagnostic controls are explicit and documented in code paths used by parity triage, then run full validation gates for closure. If parity or benchmark checks fail, keep plan open and record mismatch details plus rollback mode guidance in summary instead of declaring completion.</action>
  <verify>npm run test:file -- tests/codebase.test.cjs && npm test && npm run build</verify>
  <done>Parity checks are green, benchmark evidence is captured, fallback controls remain available, and full test/build gates pass for safe phase closure.</done>
</task>

</tasks>

<verification>
- Run `npm run test:file -- tests/codebase.test.cjs` after each task to keep parity regressions localized.
- Run `npm run baseline` in legacy-forced and optimized modes and capture deltas in summary evidence.
- Run `npm test` and `npm run build` as final closure gates before marking Phase 78 complete.
</verification>

<success_criteria>
- SCAN-03 parity is proven with deterministic fixture-based legacy-versus-optimized comparisons.
- SCAN-01 performance evidence demonstrates measurable scan improvement in benchmark runs.
- Safe fallback/diagnostic controls remain available for mismatch triage without breaking user command behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/78-file-discovery-and-ignore-optimization/78-03-SUMMARY.md`.
</output>
