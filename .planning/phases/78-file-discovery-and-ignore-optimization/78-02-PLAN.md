---
phase: 78-file-discovery-and-ignore-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - src/lib/adapters/discovery.js
  - src/lib/codebase-intel.js
  - src/commands/codebase.js
  - tests/codebase.test.cjs
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
intent:
  outcome_ids:
    - DO-20
    - DO-32
    - DO-38
  rationale: "Turning on optimized discovery paths for hot commands requires explicit tests and guards so speed gains land without regressions."
must_haves:
  truths:
    - "File-heavy analyze flows run through optimized traversal by default and complete faster on large trees."
    - "Scan flows no longer pay repeated `git check-ignore` subprocess costs in normal execution paths."
    - "Maintainers can still run legacy comparison mode for diagnosis if parity or performance issues appear."
  artifacts:
    - path: src/lib/adapters/discovery.js
      provides: "Optimized-by-default execution path with diagnostics toggles"
      contains: "shadow"
    - path: src/lib/codebase-intel.js
      provides: "Codebase analysis discovery path consuming optimized adapter behavior"
      contains: "performAnalysis"
    - path: src/commands/codebase.js
      provides: "Analyze/status command compatibility over migrated discovery internals"
      contains: "cmdCodebaseAnalyze"
    - path: tests/codebase.test.cjs
      provides: "Tests asserting optimized scan behavior and no per-entry ignore subprocess usage"
      contains: "codebase analyze"
  key_links:
    - from: src/commands/codebase.js
      to: src/lib/codebase-intel.js
      via: "analyze command execution"
      pattern: "performAnalysis"
    - from: src/lib/codebase-intel.js
      to: src/lib/adapters/discovery.js
      via: "optimized discovery selection"
      pattern: "optimized|legacy|shadow"
    - from: tests/codebase.test.cjs
      to: src/lib/adapters/discovery.js
      via: "behavioral assertions for ignore/traversal path"
      pattern: "check-ignore|optimized"
---

<objective>
Enable optimized discovery as the default hot-path behavior for codebase scan flows, while preserving explicit diagnostic controls and command-level compatibility.

Purpose: Deliver tangible scan performance gains by routing normal execution to in-process traversal/ignore matching and proving that command behavior remains stable.
Output: Optimized default wiring, diagnostics/shadow controls, and command-level tests validating scan behavior.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/78-file-discovery-and-ignore-optimization/78-RESEARCH.md
@.planning/phases/78-file-discovery-and-ignore-optimization/78-01-SUMMARY.md
@src/lib/adapters/discovery.js
@src/lib/codebase-intel.js
@src/commands/codebase.js
@tests/codebase.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch hotspot discovery flows to optimized default with guard rails</name>
  <files>src/lib/adapters/discovery.js, src/lib/codebase-intel.js</files>
  <action>Promote optimized traversal/ignore mode to default for `getSourceDirs` and `walkSourceFiles` hot paths used by codebase analysis and stale detection fallback scanning. Keep explicit env/config diagnostics controls for forced legacy mode and optional shadow comparison so maintainers can triage parity/perf regressions quickly.</action>
  <verify>npm run test:file -- tests/codebase.test.cjs</verify>
  <done>Hot-path scan internals use optimized adapter behavior by default while legacy diagnostic entry points remain available.</done>
</task>

<task type="auto">
  <name>Task 2: Validate command wiring and compatibility after optimized cutover</name>
  <files>src/commands/codebase.js, src/lib/codebase-intel.js</files>
  <action>Confirm `codebase analyze`, `codebase status`, and background analysis flows remain compatible after optimized discovery default changes, including lock-file handling and stale/fresh mode behavior. Apply minimal command-layer updates only where wiring assumptions relied on legacy discovery internals.</action>
  <verify>npm run test:file -- tests/codebase.test.cjs</verify>
  <done>Command-level analysis/status behavior remains contract-stable with optimized internals enabled.</done>
</task>

<task type="auto">
  <name>Task 3: Add behavioral tests for optimized default and ignore subprocess elimination</name>
  <files>tests/codebase.test.cjs</files>
  <action>Add or update tests that assert optimized discovery path usage in analyze flows and verify repeated per-entry `git check-ignore` spawning is absent from normal scan execution. Keep assertions behavior-based (output, mode, and instrumentation evidence) so tests remain robust across refactors.</action>
  <verify>npm run test:file -- tests/codebase.test.cjs</verify>
  <done>Test suite contains explicit coverage that optimized default scan path is active and subprocess ignore overhead is removed from hot paths.</done>
</task>

</tasks>

<verification>
- Run `npm run test:file -- tests/codebase.test.cjs` after each task.
- Run `npm test` when all updates are complete to ensure whole-suite compatibility.
- Run `npm run build` to verify bundled CLI artifact integrity after command/discovery changes.
</verification>

<success_criteria>
- Optimized discovery is the default runtime path for file-heavy scan flows.
- Repeated ignore subprocess overhead is removed from normal discovery execution.
- Existing codebase command outputs and stale/fresh behavior remain backward compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/78-file-discovery-and-ignore-optimization/78-02-SUMMARY.md`.
</output>
