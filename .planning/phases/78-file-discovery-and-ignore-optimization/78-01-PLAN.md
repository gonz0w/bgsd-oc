---
phase: 78-file-discovery-and-ignore-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/adapters/discovery.js
  - src/lib/codebase-intel.js
  - src/lib/ast.js
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
intent:
  outcome_ids:
    - DO-20
    - DO-32
  rationale: "Establishing a dual-path discovery adapter reduces traversal overhead while preserving legacy behavior contracts."
must_haves:
  truths:
    - "File discovery uses in-process traversal and ignore matching instead of per-directory git subprocess checks in hot paths."
    - "Optimized and legacy discovery engines can run from one adapter seam so parity can be validated before full cutover."
    - "Callers continue receiving the same source-dir and relative-file-path shapes after adapter integration."
  artifacts:
    - path: package.json
      provides: "Discovery optimization dependencies"
      contains: "fast-glob"
    - path: src/lib/adapters/discovery.js
      provides: "Dual-path discovery adapter with optimized and legacy engines"
      contains: "getSourceDirs"
    - path: src/lib/codebase-intel.js
      provides: "Discovery hotspot migration through adapter seam"
      contains: "walkSourceFiles"
    - path: src/lib/ast.js
      provides: "Repo-map discovery path wired through shared adapter behavior"
      contains: "generateRepoMap"
  key_links:
    - from: src/lib/codebase-intel.js
      to: src/lib/adapters/discovery.js
      via: "source directory and file traversal calls"
      pattern: "getSourceDirs|walkSourceFiles"
    - from: src/lib/adapters/discovery.js
      to: package.json
      via: "runtime dependency usage"
      pattern: "fast-glob|ignore"
    - from: src/lib/ast.js
      to: src/lib/adapters/discovery.js
      via: "repo map file collection"
      pattern: "getSourceDirs|walkSourceFiles"
---

<objective>
Create a dedicated discovery adapter that supports legacy and optimized traversal/ignore engines, then wire existing discovery hotspots through the adapter without changing external command contracts.

Purpose: Establish a reversible migration seam that removes repeated ignore subprocess overhead and unlocks fast-glob traversal while preserving legacy behavior for parity checks.
Output: New discovery adapter module, dependency updates, and call-site wiring in codebase intel and repo-map generation.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/78-file-discovery-and-ignore-optimization/78-RESEARCH.md
@src/lib/codebase-intel.js
@src/lib/ast.js
@src/commands/codebase.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optimized discovery dependencies and adapter module scaffold</name>
  <files>package.json, package-lock.json, src/lib/adapters/discovery.js</files>
  <action>Add `fast-glob` and `ignore` dependencies, then create `src/lib/adapters/discovery.js` exposing stable adapter APIs for source-dir detection and source-file walking. Implement adapter options for legacy mode, optimized mode, and optional shadow-compare hooks so migration can be phased without breaking existing callers.</action>
  <verify>npm install --package-lock-only && npm run test:file -- tests/codebase.test.cjs</verify>
  <done>Adapter module exists with callable dual-path APIs and dependency metadata is present for both traversal and ignore engines.</done>
</task>

<task type="auto">
  <name>Task 2: Implement in-process ignore matching and fast traversal in adapter</name>
  <files>src/lib/adapters/discovery.js</files>
  <action>Implement optimized adapter internals that use `fast-glob` for high-fanout traversal and `ignore` for in-process `.gitignore` matching with normalized relative POSIX paths. Preserve legacy semantics identified in research: `SKIP_DIRS`, hidden-directory handling, binary filtering expectations, root-source detection, fallback-to-root behavior, and relative path output shape.</action>
  <verify>npm run test:file -- tests/codebase.test.cjs</verify>
  <done>Optimized adapter path performs traversal and ignore checks in-process while preserving output contracts needed by existing scan consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate discovery call sites to adapter seam with legacy-safe default</name>
  <files>src/lib/codebase-intel.js, src/lib/ast.js</files>
  <action>Refactor `getSourceDirs` and `walkSourceFiles` usage in `codebase-intel` and repo-map generation to route through the new adapter seam. Keep default behavior parity-safe (legacy or shadow-compare guarded mode) so rollout can continue in subsequent plans without changing public command outputs yet.</action>
  <verify>npm run test:file -- tests/codebase.test.cjs</verify>
  <done>Discovery hotspots are routed through shared adapter APIs while existing command behavior remains stable for current users.</done>
</task>

</tasks>

<verification>
- Run `npm run test:file -- tests/codebase.test.cjs` after each task to detect discovery regressions early.
- Run `npm test` after wiring is complete to confirm no cross-command behavior drift.
- Run `npm run build` to ensure single-file CLI artifact generation remains valid after dependency/wiring changes.
</verification>

<success_criteria>
- Discovery hotspot code paths are adapter-backed and support both legacy and optimized implementations.
- Optimized path removes per-directory git ignore subprocess checks in implementation.
- Existing scan-related command interfaces remain contract-compatible after migration seam introduction.
</success_criteria>

<output>
After completion, create `.planning/phases/78-file-discovery-and-ignore-optimization/78-01-SUMMARY.md`.
</output>
