---
phase: 89-runtime-bun-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/router.js
autonomous: true
requirements:
  - RUNT-02
gap_closure: true

must_haves:
  truths:
    - "When BGSD_RUNTIME=node is set, startup shows '[bGSD] Falling back to Node.js' banner"
    - "The banner reflects the effective runtime preference (env var > config)"
  artifacts:
    - "src/router.js - Updated to use effective runtime preference from detectBun()"
  key_links:
    - "detectBun() returns forced preference info; router.js uses it for banner display"
---

<objective>

Fix the runtime banner so it correctly shows "[bGSD] Falling back to Node.js" when `BGSD_RUNTIME=node` is set.

Purpose: Provide user feedback when runtime is forced via environment variable.

</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-runtime-bun-migration/89-02-SUMMARY.md
@.planning/phases/89-runtime-bun-migration/89-VERIFICATION.md

Gap 2 (Minor): Banner message for forced Node.js not showing
- Expected: `[bGSD] Falling back to Node.js` when `BGSD_RUNTIME=node`
- Actual: No message appears  
- Root cause: Router reads `configGet('runtime')` which returns 'auto' from config, not the env override value
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix runtime banner to use effective preference from detectBun</name>
  <files>src/router.js</files>
  <action>
    In router.js (lines 13-32), the runtime detection has two issues:

    1. Line 16: `const runtimePref = configGet('runtime')` reads from config file only
    2. Line 18-24: `_runtimeDetected.preference` is set from config, ignoring env var override

    The fix: Use the effective preference from detectBun() result instead of just configGet('runtime').

    Change the _runtimeDetected assignment to use bunStatus.forced and derive preference correctly:
    - If bunStatus.forced is true, use the actual runtime that was forced (from env var or config)
    - Pass this effective preference to showRuntimeBanner()

    Reference: detectBun() returns { forced: true, ... } when BGSD_RUNTIME is set.
    The env var handling is in bun-runtime.js lines 122-130.
  </action>
  <verify>
    BGSD_RUNTIME=node node bin/bgsd-tools.cjs --verbose 2>&1 | grep -i "falling back"
  </verify>
  <done>Console output shows "[bGSD] Falling back to Node.js" when BGSD_RUNTIME=node</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild bundle and verify fix</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
    Rebuild the bundle to include the router.js fix:
    npm run build

    Then verify the banner appears correctly:
    BGSD_RUNTIME=node node bin/bgsd-tools.cjs --verbose 2>&1 | head -5
  </action>
  <verify>
    BGSD_RUNTIME=node node bin/bgsd-tools.cjs --verbose 2>&1 | grep "Falling back"
  </verify>
  <done>Bundle rebuilt, banner fix verified</done>
</task>

</tasks>

<verification>
- [ ] BGSD_RUNTIME=node shows "[bGSD] Falling back to Node.js" in verbose mode
- [ ] Normal startup (no env var) still works as before
- [ ] Bundle size not significantly changed
</verification>

<success_criteria>
Banner message appears when runtime is forced via BGSD_RUNTIME env var.
</success_criteria>

<output>
After completion, create `.planning/phases/89-runtime-bun-migration/89-03-SUMMARY.md`
</output>
