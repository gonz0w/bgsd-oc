---
phase: "98"
plan: "02"
type: "execute"
wave: 1
depends_on: ["98-01"]
files_modified: []
autonomous: true
user_setup: []
requirements:
  - "NL-03"
  - "NL-04"
must_haves:
  truths:
    - "Natural language phrases resolve to commands via fuzzy matching"
    - "User sees contextual suggestions when input is unclear"
    - "Disambiguation prompts shown when confidence < 0.8"
  artifacts:
    - path: "src/lib/nl/fuzzy-resolver.js"
      provides: "Fuzzy matching using Fuse.js with threshold 0.4"
      exports: ["FuzzyResolver"]
    - path: "src/lib/nl/help-fallback.js"
      provides: "Contextual suggestions for unrecognized input"
      exports: ["getFallbackSuggestions", "generateHelp"]
  key_links:
    - from: "fuzzy-resolver.js"
      to: "command-registry.js"
      via: "PHRASES for fuzzy matching"
      pattern: "Fuse.*PHRASES"
    - from: "help-fallback.js"
      to: "command-registry.js"
      via: "INTENTS for category suggestions"
      pattern: "INTENTS"
---

# Phase 98 Plan 02: Fuzzy Matching and Fallback Help

## Objective

- **What:** Implement fuzzy matching resolver and fallback help system for natural language commands
- **Why:** Enables smart alias resolution (NL-03) and contextual suggestions when input is unclear (NL-04)
- **Output:** Working NL command resolution with disambiguation

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/98-nl-foundation/98-01-PLAN.md
@.planning/phases/98-nl-foundation/98-01-SUMMARY.md

## Requirements Addressed

| Requirement | Description |
|-------------|-------------|
| NL-03 | Smart alias resolution — fuzzy matching with threshold 0.4 |
| NL-04 | Fallback help — contextual suggestions for unclear input |

## Decisions (from context)

- Fuzzy threshold: 0.4 (moderate)
- Disambiguation: Show choices when confidence < 0.8
- Lazy load: Fuzzy resolver loads on-demand, not at startup

## Tasks

<task type="auto">
  <name>Create fuzzy-resolver.js</name>
  <files>src/lib/nl/fuzzy-resolver.js</files>
  <action>
    Create src/lib/nl/fuzzy-resolver.js with Fuse.js integration:
    - Import Fuse from fuse.js (lazy-require to avoid startup impact)
    - Create FuzzyResolver class with threshold 0.4 (from decisions)
    - Implement resolve(input) returning array of {phrase, command, score} sorted by score
    - Include confidence calculation: 1 - score (lower score = better match)
    - Cache the Fuse instance after first use (lazy + cached pattern)
  </action>
  <verify>node -e "const{FuzzyResolver}=require('./src/lib/nl/fuzzy-resolver.js');const r=new FuzzyResolver();console.log(r.resolve('plan faze'))"</verify>
  <done>FuzzyResolver.resolve("plan faze") returns match for "plan phase" with score < 0.4</done>
</task>

<task type="auto">
  <name>Create help-fallback.js</name>
  <files>src/lib/nl/help-fallback.js</files>
  <action>
    Create src/lib/nl/help-fallback.js with suggestion system:
    - getFallbackSuggestions(input, context): Returns contextual suggestions
    - generateHelp(input): Returns help text for unrecognized input
    - Group suggestions by intent category (plan, execute, verify, query)
    - Include "did you mean" suggestions when close match exists (threshold 0.6)
  </action>
  <verify>node -e "const{getFallbackSuggestions}=require('./src/lib/nl/help-fallback.js');console.log(getFallbackSuggestions('foo'))"</verify>
  <done>getFallbackSuggestions returns array with category suggestions</done>
</task>

<task type="auto">
  <name>Create main nl-parser.js entry point</name>
  <files>src/lib/nl/nl-parser.js</files>
  <action>
    Create src/lib/nl/nl-parser.js orchestrating all NL modules:
    - Import intent-classifier, parameter-extractor, fuzzy-resolver, help-fallback
    - Implement parse(input) main function returning {parsed, command, intent, params, confidence, suggestions}
    - Apply disambiguation logic: if confidence < 0.8, include choices in output
    - Fall through to help-fallback when no match found
    - Export for use by bgsd-tools.cjs
  </action>
  <verify>node -e "const{parse}=require('./src/lib/nl/nl-parser.js');console.log(parse('plan phase 5'))"</verify>
  <done>parse("plan phase 5") returns {parsed:true,command:'plan:phase',intent:'plan',params:{phase:5}}</done>
</task>

<task type="auto">
  <name>Integrate NL parser into CLI command routing</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
    Update bgsd-tools.cjs to use nl-parser.js:
    - Add NL input detection (before alias resolution)
    - Check if input contains natural language patterns
    - Use nl-parser.parse() for NL input, fall back to existing alias resolution
    - Ensure short aliases (p, e) take priority per decisions
    - Add --nl flag to force NL parsing for testing
  </action>
  <verify>Test: node bin/bgsd-tools.cjs "show progress" (should route to session:progress)</verify>
  <done>CLI accepts natural language input and resolves to correct command</done>
</task>

<task type="auto">
  <name>Test end-to-end NL parsing</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
    Run comprehensive tests of NL parsing:
    - "plan phase 98" → plan:phase (intent + params)
    - "show progress" → session:progress (fuzzy match)
    - "run the tests" → execute:quick (intent + target)
    - "verify work" → verify:work (exact phrase)
    - "foo bar" → suggestions (unrecognized, help fallback)
    - "p 5" → plan:phase (short alias priority)
  </action>
  <verify>All 6 test cases produce expected output</verify>
  <done>All test cases pass: exact matches, fuzzy matches, intent + params, help fallback, alias priority</done>
</task>

## Verification

Test cases:
1. Exact phrase: `"verify work"` → command: "verify:work"
2. Fuzzy match: `"plan faze"` → matches "plan phase" (score < 0.4)
3. Intent + params: `"execute phase 5"` → intent: "execute", phase: 5
4. Disambiguation: Low confidence input shows choices
5. Help fallback: Unrecognized input shows suggestions
6. Alias priority: `"p 5"` → "plan:phase" (short alias wins)

## Success Criteria

- [ ] fuzzy-resolver.js created with Fuse.js, threshold 0.4
- [ ] help-fallback.js created with contextual suggestions
- [ ] nl-parser.js orchestrates all modules
- [ ] CLI accepts natural language input
- [ ] Short aliases take priority over NL phrases
- [ ] Disambiguation works (confidence < 0.8 shows choices)
- [ ] Help fallback works for unrecognized input
- [ ] All 6 test cases pass
