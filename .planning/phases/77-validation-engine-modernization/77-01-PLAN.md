---
phase: 77-validation-engine-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/plugin/validation/adapter.js
  - src/plugin/validation/flags.js
  - src/plugin/tools/bgsd-plan.js
  - tests/plugin.test.cjs
autonomous: true
requirements:
  - VALD-02
  - VALD-03
intent:
  outcome_ids:
    - DO-20
    - DO-32
  rationale: "Validation hot-path migration improves responsiveness while preserving stable behavior."
must_haves:
  truths:
    - "Maintainer can force legacy validator behavior with an explicit fallback flag."
    - "bgsd_plan responses remain contract-identical for valid and invalid inputs after adapter introduction."
    - "With `GSD_DEBUG=1`, repeated identical `bgsd_plan` validations emit the same engine-selection debug marker."
  artifacts:
    - path: src/plugin/validation/adapter.js
      provides: "Dual-engine validation adapter with normalized parse result"
      contains: "export function validateArgs"
    - path: src/plugin/validation/flags.js
      provides: "Runtime flag resolution for primary/fallback validator selection"
      contains: "BGSD_DEP_VALIBOT"
    - path: src/plugin/tools/bgsd-plan.js
      provides: "Pilot tool migrated from direct Zod schema usage"
      contains: "validateArgs"
    - path: tests/plugin.test.cjs
      provides: "Parity and fallback tests for pilot migration"
      contains: "bgsd_plan"
  key_links:
    - from: src/plugin/tools/bgsd-plan.js
      to: src/plugin/validation/adapter.js
      via: "args parsing call"
      pattern: "validateArgs"
    - from: src/plugin/validation/adapter.js
      to: src/plugin/validation/flags.js
      via: "engine selection"
      pattern: "BGSD_DEP_VALIBOT|fallback"
    - from: tests/plugin.test.cjs
      to: src/plugin/tools/bgsd-plan.js
      via: "tool execute assertions"
      pattern: "bgsd_plan"
---

<objective>
Create a validator adapter seam with explicit fallback controls, then migrate one pilot plugin tool through that seam without changing user-visible behavior.

Purpose: De-risk full migration by proving adapter wiring, parity behavior, and rollback safety on a narrow blast radius before porting all tools.
Output: Adapter modules, flag wiring, pilot tool migration, and targeted parity/fallback tests.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/77-validation-engine-modernization/77-RESEARCH.md
@src/plugin/tools/bgsd-plan.js
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dual-engine validator adapter and runtime flag resolver</name>
  <files>src/plugin/validation/adapter.js, src/plugin/validation/flags.js</files>
  <action>Create `src/plugin/validation/adapter.js` that exposes a single `validateArgs(toolName, schemaSpec, input)` API returning normalized `{ ok, data, error }` results independent of underlying library. Implement valibot as primary engine and legacy Zod as fallback engine. Add `src/plugin/validation/flags.js` to resolve guard flags (`BGSD_DEP_VALIBOT`, `BGSD_DEP_VALIBOT_FALLBACK`) with safe defaults and no behavior break for existing environments. Keep error payload mapping stable so tool handlers can continue returning existing JSON envelopes.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>Adapter and flags modules exist, tool-facing validation API is stable, and fallback mode can be toggled without code edits.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate bgsd_plan to adapter-driven argument validation</name>
  <files>src/plugin/tools/bgsd-plan.js</files>
  <action>Replace direct `zod` import and schema usage in `bgsd_plan.args` path with adapter-backed schema definition and parse call. Preserve existing coercion semantics for `phase` (including optional behavior and numeric conversion), and preserve existing error branch (`validation_error`) message shape. Do not change execute-path business logic beyond validation integration.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>`bgsd_plan` no longer depends on direct Zod schema declarations and still passes no-project, valid-phase, and invalid-phase contract tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add pilot parity and fallback-switch tests</name>
  <files>tests/plugin.test.cjs, package.json, package-lock.json</files>
  <action>Add valibot dependency and lockfile update, then extend plugin tool tests to assert parity between default engine and forced fallback mode for `bgsd_plan` argument parsing. Cover valid coercion input, invalid input handling, and JSON output contract stability. Add a dedicated debug-mode assertion (`GSD_DEBUG=1`) that runs identical input twice and verifies the same engine-selection marker is emitted each time. Keep tests deterministic by setting and restoring env flags within test scope.</action>
  <verify>npm test</verify>
  <done>Test suite demonstrates identical contract behavior between primary and fallback engines for pilot scope, includes explicit debug-log determinism assertions, and passes in CI-compatible mode.</done>
</task>

</tasks>

<verification>
- Run `npm run test:file -- tests/plugin.test.cjs` after each task to keep adapter migration regressions localized.
- Run `GSD_DEBUG=1 npm run test:file -- tests/plugin.test.cjs` to verify deterministic engine-selection debug output assertions.
- Run `npm test` once all tasks are complete to validate no contract regressions across full suite.
</verification>

<success_criteria>
- Fallback path is selectable via environment flags and keeps plugin tool behavior stable.
- Pilot tool migration proves adapter seam can replace direct Zod without output contract drift.
- New parity tests fail on engine mismatch and pass when outputs remain equivalent.
</success_criteria>

<output>
After completion, create `.planning/phases/77-validation-engine-modernization/77-01-SUMMARY.md`.
</output>
