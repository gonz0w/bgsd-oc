---
phase: 77-validation-engine-modernization
plan: 02
type: execute
wave: 2
depends_on:
  - 77-01
files_modified:
  - src/plugin/tools/bgsd-status.js
  - src/plugin/tools/bgsd-context.js
  - src/plugin/tools/bgsd-validate.js
  - src/plugin/tools/bgsd-progress.js
  - src/plugin/validation/adapter.js
  - src/plugin/validation/flags.js
  - tests/plugin.test.cjs
  - build.cjs
  - plugin.js
autonomous: true
requirements:
  - VALD-01
  - VALD-02
  - VALD-03
intent:
  outcome_ids:
    - DO-20
    - DO-32
  rationale: "Complete validator migration to reduce runtime overhead while keeping output contracts and rollback safety intact."
must_haves:
  truths:
    - "Users can run plugin tools and see identical JSON output contracts after migration."
    - "Users get lower validation overhead on repeated plugin-tool calls in normal mode."
    - "Maintainer can immediately recover legacy validator behavior for all migrated tools."
  artifacts:
    - path: src/plugin/tools/bgsd-status.js
      provides: "Migrated status tool argument validation via adapter"
      contains: "validateArgs"
    - path: src/plugin/tools/bgsd-context.js
      provides: "Migrated context tool argument coercion parity"
      contains: "validateArgs"
    - path: src/plugin/tools/bgsd-validate.js
      provides: "Migrated validate tool argument handling"
      contains: "validateArgs"
    - path: src/plugin/tools/bgsd-progress.js
      provides: "Migrated enum and value validation semantics"
      contains: "validateArgs"
    - path: tests/plugin.test.cjs
      provides: "Cross-tool parity assertions for default and fallback engines"
      contains: "bgsd_progress"
  key_links:
    - from: src/plugin/tools/bgsd-status.js
      to: src/plugin/validation/adapter.js
      via: "tool arg parse path"
      pattern: "validateArgs\\(\\s*'bgsd_status'"
    - from: src/plugin/tools/bgsd-progress.js
      to: src/plugin/validation/flags.js
      via: "engine toggle behavior"
      pattern: "BGSD_DEP_VALIBOT(_FALLBACK)?"
    - from: tests/plugin.test.cjs
      to: src/plugin/tools/bgsd-context.js
      via: "coercion parity test"
      pattern: "bgsd_context.*args\\.task|assert\\.deepStrictEqual"
---

<objective>
Complete migration of remaining plugin tool validators to the adapter seam, then prove end-to-end parity and measurable overhead improvement with fallback safety retained.

Purpose: Deliver phase outcome by finishing hot-path migration while ensuring existing tool contracts remain unchanged and rollback remains immediate.
Output: Full plugin tool migration, expanded parity tests, and build/test verification for deploy safety.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/77-validation-engine-modernization/77-RESEARCH.md
@.planning/phases/77-validation-engine-modernization/77-01-SUMMARY.md
@src/plugin/tools/bgsd-status.js
@src/plugin/tools/bgsd-context.js
@src/plugin/tools/bgsd-validate.js
@src/plugin/tools/bgsd-progress.js
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate remaining plugin tools to adapter-backed validators</name>
  <files>src/plugin/tools/bgsd-status.js, src/plugin/tools/bgsd-context.js, src/plugin/tools/bgsd-validate.js, src/plugin/tools/bgsd-progress.js, src/plugin/validation/adapter.js, src/plugin/validation/flags.js</files>
  <action>Replace direct Zod schema definitions in the four remaining plugin tools with adapter-backed schema specs and parse calls. Preserve each tool's current coercion, enum, optional-field, and validation-error semantics. Ensure fallback flag applies uniformly across all migrated tools and keep adapter internals extensible for shadow-compare diagnostics when `GSD_DEBUG=1`.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>All five plugin tools route through shared validation adapter with no direct in-tool Zod schema usage and consistent fallback selection behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Expand parity and contract tests for all migrated tool args</name>
  <files>tests/plugin.test.cjs</files>
  <action>Add focused tests for known migration risk points from research: `bgsd_context.args.task` coercion parity, `bgsd_progress.args.action` enum parity, and invalid/missing arg handling envelopes. For each case, execute once in primary mode and once in forced fallback mode, then assert equivalent JSON contract outputs and stable error categories.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>Plugin tests encode cross-engine parity checks for critical argument paths and detect regressions before rollout.</done>
</task>

<task type="auto">
  <name>Task 3: Validate performance-safety rollout and build compatibility</name>
  <files>build.cjs, plugin.js, tests/plugin.test.cjs</files>
  <action>Run full build/test gates and update any plugin-size assertions or build metadata that still assume Zod-specific wording. Add an explicit VALD-01 benchmark capture using `npm run baseline` in both modes: `BGSD_DEP_VALIBOT=0 BGSD_DEP_VALIBOT_FALLBACK=1` (legacy baseline) and `BGSD_DEP_VALIBOT=1 BGSD_DEP_VALIBOT_FALLBACK=0` (modern path). Compare before/after timing from `.planning/baselines/performance.json` snapshots, require at least a 10% improvement in the modern run, and record command outputs plus percent delta in `77-02-SUMMARY.md` evidence.</action>
  <verify>npm run build && npm test && BGSD_DEP_VALIBOT=0 BGSD_DEP_VALIBOT_FALLBACK=1 npm run baseline && BGSD_DEP_VALIBOT=1 BGSD_DEP_VALIBOT_FALLBACK=0 npm run baseline</verify>
  <done>Build artifact remains deployable, full tests pass, and summary evidence documents legacy-vs-modern benchmark results with >=10% improvement for VALD-01 while rollback path remains available; if benchmark delta is below threshold, mark this plan not complete and create a revision/new plan before closure.</done>
</task>

</tasks>

<verification>
- Execute `npm run test:file -- tests/plugin.test.cjs` after migration and test-expansion tasks.
- Execute `npm run build` before full tests to ensure single-file plugin deployment path remains valid.
- Execute `npm test` as the final gate and confirm no plugin contract regressions.
- Execute `BGSD_DEP_VALIBOT=0 BGSD_DEP_VALIBOT_FALLBACK=1 npm run baseline` and `BGSD_DEP_VALIBOT=1 BGSD_DEP_VALIBOT_FALLBACK=0 npm run baseline`, then record before/after timings and percent change in `77-02-SUMMARY.md`.
</verification>

<success_criteria>
- Every plugin tool uses shared adapter validation with parity-preserving behavior.
- Cross-engine parity tests cover coercion, enum, and invalid input contracts.
- Build and full test suite pass; benchmark evidence includes explicit legacy-vs-modern command output, before/after timing, and >=10% improvement for VALD-01 with retained fallback safety (otherwise plan is not complete and requires revision/new plan).
</success_criteria>

<output>
After completion, create `.planning/phases/77-validation-engine-modernization/77-02-SUMMARY.md`.
</output>
