---
phase: 77-validation-engine-modernization
plan: 03
type: execute
wave: 3
depends_on:
  - 77-02
files_modified:
  - tests/plugin.test.cjs
  - src/plugin/tools/bgsd-context.js
  - src/plugin/tools/bgsd-progress.js
  - src/plugin/validation/adapter.js
autonomous: true
gap_closure: true
requirements:
  - VALD-01
  - VALD-02
  - VALD-03
intent:
  outcome_ids:
    - DO-20
    - DO-32
  rationale: "Close verification gaps by restoring full parity confidence and proving fallback wiring behavior in executed tool paths."
must_haves:
  truths:
    - "User sees unchanged command behavior and output contracts after validator migration, with plugin parity tests fully green."
    - "Maintainer can force legacy fallback and observe tool behavior switch through adapter-mediated wiring, including bgsd_progress paths."
    - "Validation modernization gains stay intact while parity and fallback evidence are explicit and repeatable."
  artifacts:
    - path: tests/plugin.test.cjs
      provides: "Passing parity coverage for bgsd_context fallback contract and explicit bgsd_progress fallback wiring evidence"
      contains: "bgsd_context keeps task coercion contract parity in forced fallback mode"
    - path: src/plugin/validation/adapter.js
      provides: "Single source of engine selection and fallback behavior for migrated plugin tools"
      contains: "resolveValidationFlags"
    - path: src/plugin/tools/bgsd-progress.js
      provides: "Adapter-mediated progress validation path that participates in fallback mode"
      contains: "validateArgs"
  key_links:
    - from: tests/plugin.test.cjs
      to: src/plugin/tools/bgsd-context.js
      via: "forced-fallback parity assertion"
      pattern: "bgsd_context.*forced fallback"
    - from: tests/plugin.test.cjs
      to: src/plugin/tools/bgsd-progress.js
      via: "tool-level fallback behavior evidence"
      pattern: "bgsd_progress.*runWithValidationModes"
    - from: src/plugin/tools/bgsd-progress.js
      to: src/plugin/validation/adapter.js
      via: "adapter-mediated engine selection"
      pattern: "validateArgs\(\s*'bgsd_progress'"
---

<objective>
Close Phase 77 verification gaps by fixing the failing fallback parity test and adding explicit evidence that fallback toggles reach bgsd_progress through adapter wiring.

Purpose: Remove remaining blocker/warning findings so VALD-02 and VALD-03 are fully verifiable without regressing VALD-01 performance gains.
Output: Green plugin parity tests, explicit fallback-wiring evidence in tests, and implementation adjustments (if needed) to keep adapter-mediated behavior contract-stable.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/INTENT.md
@.planning/phases/77-validation-engine-modernization/77-VERIFICATION.md
@.planning/phases/77-validation-engine-modernization/77-01-SUMMARY.md
@.planning/phases/77-validation-engine-modernization/77-02-SUMMARY.md
@src/plugin/tools/bgsd-context.js
@src/plugin/tools/bgsd-progress.js
@src/plugin/validation/adapter.js
@tests/plugin.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore bgsd_context forced-fallback parity to full pass</name>
  <files>tests/plugin.test.cjs, src/plugin/tools/bgsd-context.js, src/plugin/validation/adapter.js</files>
  <action>Reproduce the currently failing `bgsd_context` fallback parity test and identify the exact contract drift in current repository state. Implement the smallest code/test correction needed so fallback and modern validation modes produce equivalent output contracts for the coercion path (`task: '1'`) and related invalid-input envelope behavior. Preserve existing adapter API and avoid changing non-validation business logic.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>`tests/plugin.test.cjs` runs fully green with the previously failing `bgsd_context` forced-fallback parity test now passing.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit tool-level fallback evidence for bgsd_progress adapter wiring</name>
  <files>tests/plugin.test.cjs, src/plugin/tools/bgsd-progress.js, src/plugin/validation/adapter.js</files>
  <action>Add a focused parity/wiring assertion for `bgsd_progress` that executes in default and forced-fallback modes and proves fallback selection affects the tool through adapter-mediated validation (without requiring direct env-flag reads in the tool file). If current assertions are ambiguous, refine naming/assertions so evidence clearly maps to the verification gap and documents expected indirect wiring path.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs</verify>
  <done>Plugin tests contain explicit, readable evidence linking `bgsd_progress` behavior to adapter-mediated fallback toggles, closing the key-link ambiguity.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run rollout gates and record gap-closure evidence</name>
  <files>tests/plugin.test.cjs</files>
  <action>Run focused plugin parity tests and full suite gates needed to support verification closure. In the summary, capture outputs that show (1) parity suite is fully green and (2) fallback evidence for `bgsd_progress` is now explicit. Keep VALD-01 benchmark evidence intact from prior plan unless new changes materially alter validation hot path behavior.</action>
  <verify>npm run test:file -- tests/plugin.test.cjs && npm test</verify>
  <done>Verification evidence shows no remaining parity blocker, fallback wiring evidence is explicit, and full tests remain green.</done>
</task>

</tasks>

<verification>
- Run `npm run test:file -- tests/plugin.test.cjs` after each task to ensure the gap remains localized to plugin validation contracts.
- Run `npm test` as final regression gate before marking this gap-closure plan complete.
- Confirm test output no longer reports failure for `bgsd_context keeps task coercion contract parity in forced fallback mode`.
- Confirm test coverage includes explicit fallback-wiring evidence for `bgsd_progress` through adapter-mediated behavior.
</verification>

<success_criteria>
- GAP-77-01 is closed: plugin parity suite is fully passing and the fallback parity contract failure is resolved.
- GAP-77-02 is closed: fallback behavior evidence for `bgsd_progress` is explicit and traceable to adapter-mediated wiring.
- VALD-01/VALD-02/VALD-03 remain satisfied with no regression introduced by gap closure work.
</success_criteria>

<output>
After completion, create `.planning/phases/77-validation-engine-modernization/77-03-SUMMARY.md`.
</output>
