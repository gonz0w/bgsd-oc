---
phase: 106
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/deps.js
  - src/commands/features.js
  - src/lib/constants.js
  - src/lib/command-help.js
  - src/router.js
  - src/lib/commandDiscovery.js
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05

must_haves:
  truths:
    - "verify:orphans command is completely removed from CLI"
    - "All orphan detection functions removed from src/"
    - "Profiler.js is removed from the bundle"
    - "No node:test or test-related code in bundle"
    - "Bundle size is measurably smaller after cleanup"
  artifacts:
    - path: "src/lib/deps.js"
      contains: "findOrphaned"
    - path: "src/commands/features.js"
      contains: "orphaned"
    - path: "src/lib/constants.js"
      contains: "verify:orphans"
    - path: "src/lib/command-help.js"
      contains: "verify:orphans"
  key_links:
    - from: "src/router.js"
      to: "verify:orphans"
      pattern: "verify:orphans"
---

<objective>
Clean up the codebase by removing unused code, test infrastructure, and performance tools from the bundle.

Purpose: Remove verify:orphans command (one-time cleanup), remove performance profiling code, ensure test infrastructure is not bundled.

Output: Cleaned source code with removed commands, reduced bundle size.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@build.cjs

# Source files to examine
@src/router.js
@src/lib/deps.js
@src/commands/features.js
@src/lib/constants.js
@src/lib/command-help.js
@src/lib/profiler.js
@src/commands/profiler.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove verify:orphans command from CLI</name>
  <files>src/lib/deps.js, src/commands/features.js, src/lib/constants.js, src/lib/command-help.js, src/router.js, src/lib/commandDiscovery.js</files>
  <action>
1. In src/lib/deps.js: Remove all orphan detection functions (findOrphanedExports, findOrphanedFiles, findOrphanedWorkflows, findOrphanedTemplates, findOrphanedConfigs, and their helper functions)
2. In src/commands/features.js: Remove the orphan audit section and calls to orphan detection functions
3. In src/lib/constants.js: Remove the 'verify:orphans' help text entry
4. In src/lib/command-help.js: Remove 'verify:orphans' from the commands object
5. In src/router.js: Remove verify:orphans from the verify: dispatch
6. In src/lib/commandDiscovery.js: Remove 'orphans' from command list
  </action>
  <verify>
node bin/bgsd-tools.cjs verify:orphans 2>&1 | head -5</verify>
  <done>
verify:orphans command returns "Unknown command" error
</done>
</task>

<task type="auto">
  <name>Task 2: Remove performance profiler from bundle</name>
  <files>src/lib/profiler.js, src/commands/profiler.js, src/router.js, src/lib/helpers.js, src/lib/context.js, src/lib/ast.js, src/lib/git.js</files>
  <action>
1. Remove src/lib/profiler.js file (or ensure it's not imported anywhere)
2. Remove src/commands/profiler.js file
3. In src/router.js: Remove profiler subcommand dispatch (around line 1052-1072), remove lazyProfiler import
4. In src/lib/helpers.js: Remove profiler import and references (lines with startTimer, endTimer)
5. In src/lib/context.js: Remove profiler import and references
6. In src/lib/ast.js: Remove profiler import and references  
7. In src/lib/git.js: Remove profiler import and references
8. Update build.cjs if needed to exclude profiler files
  </action>
  <verify>
grep -r "profiler" src/commands/ src/lib/ | grep -v "profiler-benchmark\|plugin-benchmark" | head -5</verify>
  <done>
No profiler imports remain in source code (except cli-tools which are optional)
</done>
</task>

<task type="auto">
  <name>Task 3: Verify no test infrastructure in bundle</name>
  <files>build.cjs, src/</files>
  <action>
1. Search src/ for any node:test imports or test-related code that would be bundled
2. Ensure build.cjs does not include any test-related files
3. Verify package.json test scripts are external (not bundled)
  </action>
  <verify>
grep -r "node:test" src/</verify>
  <done>
No node:test imports found in src/
</done>
</task>

<task type="auto">
  <name>Task 4: Build and measure bundle reduction</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
1. Run build: npm run build
2. Record the bundle size before (from .planning/baselines/bundle-size.json if exists)
3. Compare with new bundle size
4. Report the reduction in bytes and percentage
  </action>
  <verify>
ls -la bin/bgsd-tools.cjs</verify>
  <done>
Build succeeds, bundle size is smaller than baseline (or at least within budget)
</done>
</task>

</tasks>

<verification>
- Run `node bin/bgsd-tools.cjs verify:orphans` - should return unknown command
- Run `node bin/bgsd-tools.cjs profiler` - should return unknown command  
- Check build output has no profiler-related code
- Verify bundle size from .planning/baselines/bundle-size.json
</verification>

<success_criteria>
- verify:orphans command is completely removed
- profiler command is completely removed
- No test infrastructure (node:test) in the bundle
- Bundle size is measurably smaller or within budget
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/106-code-audit-core/106-01-SUMMARY.md`
</output>
