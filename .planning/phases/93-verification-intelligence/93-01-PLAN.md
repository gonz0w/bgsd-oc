---
phase: 93-verification-intelligence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/verify.js
  - skills/verification-reference/SKILL.md
autonomous: true
requirements: [AGENT-04, AGENT-05, AGENT-06]

must_haves:
  truths:
    - "Verifier automatically detects regression patterns in code changes"
    - "Edge case suggestions provided based on project patterns"
    - "Coverage analysis identifies untested paths"
  artifacts:
    - path: "src/commands/verify.js"
      provides: "Enhanced verify commands with regression detection"
      contains: "regression detection logic"
    - path: "skills/verification-reference/SKILL.md"
      provides: "Verification skill with coverage and edge case guidance"
      contains: "coverage analysis patterns"
  key_links:
    - from: "verify.js"
      to: "git"
      via: "diff analysis"
      pattern: "git diff.*--stat"
    - from: "verify:regression"
      to: "verify:quality"
      via: "baseline comparison"
      pattern: "baseline.*after"
---

<objective>
Implement smarter verification with regression detection, edge case suggestions, and coverage analysis.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# From prior phase
@.planning/phases/92-planning-intelligence/92-01-SUMMARY.md
@.planning/phases/92-planning-intelligence/92-CONTEXT.md

# Reference existing verify command structure
@src/commands/verify.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression detection to verify:regression</name>
  <files>src/commands/verify.js</files>
  <action>
Enhance the existing verify:regression command to automatically detect regression patterns:

1. Read the current verify:regression implementation (around line 1260-1350 in verify.js)
2. Add automatic pattern detection:
   - Detect file-level regressions: compare test pass/fail counts for files that changed
   - Detect test count regressions: warn if test count decreased between runs
   - Detect timing regressions: flag tests that got significantly slower
3. Add --auto flag that runs regression detection without explicit baseline
4. Output structured regression_patterns array with type, severity, and affected files

Key patterns to detect:
- "test_count_decreased": fewer tests than baseline
- "previously_passing_now_failing": pass->fail transition  
- "timing_regression": test time increased >50%
- "coverage_decreased": coverage percentage dropped

The detection should work with git diff to identify which files changed, then analyze test impact.
  </action>
  <verify>node bin/bgsd-tools.cjs verify:regression --help shows new --auto flag</verify>
  <done>verify:regression --auto detects and reports regression patterns automatically</done>
</task>

<task type="auto">
  <name>Task 2: Add edge case suggestion to verify:review</name>
  <files>src/commands/verify.js</files>
  <action>
Enhance the verify:review command to suggest edge cases based on project patterns:

1. Find verify:review implementation (search for "cmdVerifyReview" or similar)
2. Add edge case analysis that:
   - Scans existing tests for patterns (null checks, empty arrays, error handling)
   - Identifies untested function signatures in changed files
   - Queries similar projects for common edge cases (via research patterns)
3. Add --suggest-edge-cases flag that outputs suggestions
4. Output structured suggestions array with category, description, and priority

Edge case categories to suggest:
- "null_undefined": parameters that could be null/undefined
- "empty_collections": empty arrays, objects, strings
- "boundary_values": min/max values, first/last items
- "error_paths": catch blocks, error conditions
- "async_edge_cases": race conditions, timeouts, retries

Integration: Use existing research patterns from skills/ to query similar projects.
  </action>
  <verify>node bin/bgsd-tools.cjs verify:review --help shows --suggest-edge-cases flag</verify>
  <done>verify:review --suggest-edge-cases outputs actionable edge case suggestions</done>
</task>

<task type="auto">
  <name>Task 3: Enhance coverage analysis in verify:quality</name>
  <files>src/commands/verify.js</files>
  <action>
Enhance verify:quality command with comprehensive coverage analysis:

1. Find verify:quality implementation (search for "cmdVerifyQuality")
2. Add coverage analysis that:
   - Parses coverage reports (lcov, json, or text summary)
   - Identifies uncovered lines, branches, and functions
   - Maps uncovered code to specific files and line ranges
   - Detects coverage gaps (code changed but no tests added)
3. Add --coverage-report flag to specify report format
4. Add --gap-detection flag to identify untested paths in changed files
5. Output structured coverage object with files, percentages, and gaps

Coverage metrics to include:
- line_coverage_percent
- branch_coverage_percent  
- function_coverage_percent
- uncovered_files (sorted by coverage gap)
- gap_files (changed but no test coverage)

Integration: Use existing coverage parsing if available, or create simple lcov/text parser.
  </action>
  <verify>node bin/bgsd-tools.cjs verify:quality --help shows --gap-detection flag</verify>
  <done>verify:quality --gap-detection reports untested paths in changed files</done>
</task>

</tasks>

<verification>
- Run each verify subcommand with --help to confirm flags exist
- Test verify:regression --auto on a sample change
- Verify the new functionality integrates with existing CLI framework
</verification>

<success_criteria>
- AGENT-04: Regression detection works automatically with --auto flag
- AGENT-05: Edge case suggestions provided based on code patterns
- AGENT-06: Coverage analysis identifies untested paths with gap detection

All three requirements satisfied: regression patterns detected, edge cases suggested, coverage gaps identified.
</success_criteria>

<output>
After completion, create `.planning/phases/93-verification-intelligence/93-01-SUMMARY.md`
</output>
