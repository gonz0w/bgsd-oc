---
phase: 64-command-workflow-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/router.js
  - commands/bgsd-join-discord.md
autonomous: true
requirements: [CMD-01, CMD-03, CMD-04]

must_haves:
  truths:
    - "All CLI commands work via namespace:command form (init:, plan:, execute:, verify:, util:, research:, cache:)"
    - "No duplicate flat-form routing exists — single switch block removed"
    - "bgsd-join-discord command is fully removed"
    - "Semantic duplicates resolved (codebase-impact consolidated)"
  artifacts:
    - path: "src/router.js"
      provides: "Namespace-only routing with no backward-compat block"
      contains: "switch (namespace)"
  key_links:
    - from: "src/router.js"
      to: "src/commands/*.js"
      via: "lazy-loaded module dispatch in namespace switch"
      pattern: "lazy\\w+\\(\\)\\.cmd"
---

<objective>
Complete the namespace routing in router.js by adding routes for ~20 commands that currently ONLY exist in the flat backward-compat switch block, then remove the entire backward-compat block (~900 lines).

Purpose: The backward-compat switch block (lines 748-1639) duplicates every namespaced route in flat form AND is the only home for ~20 commands that were never migrated to namespaces. This task makes the namespace routing authoritative and removes the duplication.
Output: router.js with all commands routed through namespace:command syntax only
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-command-workflow-cleanup/64-CONTEXT.md
@src/router.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add namespace routes for flat-only commands</name>
  <files>src/router.js</files>
  <action>
Add the following commands to their respective namespace switch blocks in router.js. These commands currently ONLY exist in the flat backward-compat switch block (lines 748-1639) and have NO namespaced route.

**Add to `verify` namespace (case 'verify'):**

1. `summary` subcommand → `lazyMisc().cmdVerifySummary(cwd, restArgs[0], countCheck, raw)` where countCheck parses `--check-count` from restArgs (default 2)
2. `validate` subcommand with sub-routing:
   - `consistency` → `lazyVerify().cmdValidateConsistency(cwd, raw)`
   - `health` → `lazyVerify().cmdValidateHealth(cwd, { repair: restArgs.includes('--repair') }, raw)`
   - `roadmap` → `lazyVerify().cmdValidateRoadmap(cwd, { repair: restArgs.includes('--repair') }, raw)`
3. `validate-dependencies` subcommand → `lazyFeatures().cmdValidateDependencies(cwd, restArgs[0], raw)`
4. `validate-config` subcommand → `lazyFeatures().cmdValidateConfig(cwd, raw)`
5. `test-coverage` subcommand → `lazyFeatures().cmdTestCoverage(cwd, raw)`

**Add to `execute` namespace (case 'execute'):**

6. `trajectory` subcommand with sub-routing matching existing flat form:
   - `checkpoint` → `lazyTrajectory().cmdTrajectoryCheckpoint(cwd, restArgs, raw)`
   - `list` → `lazyTrajectory().cmdTrajectoryList(cwd, restArgs.slice(1), raw)`
   - `pivot` → `lazyTrajectory().cmdTrajectoryPivot(cwd, restArgs, raw)`
   - `compare` → `lazyTrajectory().cmdTrajectoryCompare(cwd, restArgs, raw)`
   - `choose` → `lazyTrajectory().cmdTrajectoryChoose(cwd, restArgs, raw)`
   - `dead-ends` → `lazyTrajectory().cmdTrajectoryDeadEnds(cwd, restArgs.slice(1), raw)`

**Add to `util` namespace (case 'util'):**

7. `resolve-model` → `lazyMisc().cmdResolveModel(cwd, restArgs[0], raw)`
8. `template` subcommand with sub-routing:
   - `select` → `lazyMisc().cmdTemplateSelect(cwd, restArgs[1], raw)`
   - `fill` → `lazyMisc().cmdTemplateFill(cwd, restArgs[1], { phase, plan, name, type, wave, fields }, raw)` (parse flags from restArgs matching existing flat form)
9. `generate-slug` → `lazyMisc().cmdGenerateSlug(restArgs[0], raw)`
10. `verify-path-exists` → `lazyMisc().cmdVerifyPathExists(cwd, restArgs[0], raw)`
11. `config-ensure-section` → `lazyMisc().cmdConfigEnsureSection(cwd, raw)`
12. `config-migrate` → `lazyMisc().cmdConfigMigrate(cwd, raw)`
13. `scaffold` → `lazyMisc().cmdScaffold(cwd, restArgs[0], { phase, name }, raw)` (parse --phase, --name from restArgs)
14. `phase-plan-index` → `lazyMisc().cmdPhasePlanIndex(cwd, restArgs[0], raw)`
15. `state-snapshot` → `lazyMisc().cmdStateSnapshot(cwd, raw)`
16. `summary-extract` → `lazyMisc().cmdSummaryExtract(cwd, restArgs[0], fields, raw)` (parse --fields from restArgs)
17. `quick-summary` → `lazyFeatures().cmdQuickTaskSummary(cwd, raw)`
18. `extract-sections` → `lazyFeatures().cmdExtractSections(cwd, restArgs, raw)`
19. `git` subcommand with sub-routing matching existing flat form:
    - `log` → parse --count, --since, --until, --author, --path from restArgs → `lazyGit().structuredLog(cwd, opts)` and output via `require('./lib/output').output(result, raw)`
    - `diff-summary` → parse --from, --to, --path → `lazyGit().diffSummary(cwd, opts)`
    - `blame` → `lazyGit().blame(cwd, restArgs[1])`
    - `branch-info` → `lazyGit().branchInfo(cwd)`
    - `rewind` → parse --ref, --confirm, --dry-run → `lazyGit().selectiveRewind(cwd, opts)`
    - `trajectory-branch` → parse --phase, --slug, --push → `lazyGit().trajectoryBranch(cwd, opts)`
20. `profiler` subcommand with sub-routing:
    - `compare` → `lazyProfiler().cmdProfilerCompare(restArgs.slice(1))`
    - `cache-speedup` → `await lazyProfiler().cmdProfilerCacheSpeedup(restArgs.slice(1))`
    - (default/help) → write usage to stderr

**Resolve semantic duplicate:**
- The flat `codebase-impact` command calls `lazyFeatures().cmdCodebaseImpact()` while `util:codebase impact` calls `lazyCodebase().cmdCodebaseImpact()`. These are DIFFERENT functions. Keep the existing `util:codebase impact` route (from codebase.js). Do NOT add the features.js version to any namespace — it will be removed with the backward-compat block. Workflow `cmd-codebase-impact.md` will be migrated to `util:codebase impact` in Plan 02.

**Remove stale command:**
- Remove `bgsd-join-discord` from ANY namespace if it exists (it's currently only in the flat block, but verify). Per user decision: hard remove, no tombstone.

**Also update namespace error messages** (`default:` cases) to include newly added subcommands in the "Available:" list.

**Do NOT touch the backward-compat switch block yet** — that's Task 2.
  </action>
  <verify>
Run `npm run build` to verify router.js compiles into the bundle successfully. Then test a sample of newly namespaced commands:
- `node bin/gsd-tools.cjs util:resolve-model gsd-planner`
- `node bin/gsd-tools.cjs util:generate-slug "Test Phase Name"`
- `node bin/gsd-tools.cjs verify:validate health`
- `node bin/gsd-tools.cjs util:git branch-info`
- `node bin/gsd-tools.cjs execute:trajectory list`
All should return valid JSON or formatted output, not "Unknown subcommand" errors.
  </verify>
  <done>All 20 flat-only commands have namespaced routes in the appropriate namespace switch blocks. Semantic duplicates resolved. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Remove backward-compat switch block and stale slash command</name>
  <files>src/router.js, commands/bgsd-join-discord.md</files>
  <action>
1. **Delete the entire backward-compat flat switch block** in router.js. This is the `switch (command) {` block starting at approximately line 748 (after the `return; // Exit after handling namespaced command` line) and ending at approximately line 1639 (the closing `}` of `async function main()`). Keep the `main()` function closing and the final `main().catch()` at the end of the file.

   The block to remove starts after:
   ```
   return; // Exit after handling namespaced command
   }
   ```
   And includes every `case 'state':`, `case 'resolve-model':`, etc. through `default: error('Unknown command: ...')`.

   After removal, the code flow should be:
   ```
   // ... namespace routing ...
   return; // Exit after handling namespaced command
   }

   // No command matched any namespace — unknown
   error(`Unknown command: ${command}. Use namespace:command syntax. Available namespaces: init, plan, execute, verify, util, research, cache`);
   }
   ```

2. **Delete `commands/bgsd-join-discord.md`** — the slash command wrapper file.

3. **Run `npm run build`** to verify the bundle compiles.

4. **Run `npm test`** — expect test failures where tests invoke flat-form commands. Count the failures. These will be fixed in Plan 02. The important thing is that NO test fails for reasons OTHER than the flat-form removal (e.g., no namespace routing bugs from Task 1).

   Review any test failure that does NOT look like a flat→namespace migration issue and fix it immediately.
  </action>
  <verify>
- `npm run build` succeeds
- `node bin/gsd-tools.cjs init:plan-phase 1` works (namespace routing intact)
- `node bin/gsd-tools.cjs state` returns error "Unknown command: state" (flat form removed)
- `ls commands/bgsd-join-discord.md` returns "No such file" (deleted)
- `wc -l src/router.js` shows ~750 lines (down from ~1642 — ~900 lines removed)
- Run `npm test 2>&1 | tail -5` to capture total pass/fail count for Plan 02 baseline
  </verify>
  <done>Backward-compat switch block completely removed. Router.js reduced by ~900 lines. bgsd-join-discord.md deleted. Build succeeds. Test failure count from flat-form removal is captured.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- All namespaced commands work: `node bin/gsd-tools.cjs {namespace}:{command}` routes correctly
- Flat commands return "Unknown command" error (backward-compat removed)
- `commands/bgsd-join-discord.md` does not exist
- `src/router.js` line count reduced by ~900 lines
</verification>

<success_criteria>
Router.js routes all commands exclusively through namespace:command syntax. The ~900-line backward-compat flat switch block is deleted. All 20 previously flat-only commands have working namespace routes. bgsd-join-discord is fully removed. Build compiles successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/64-command-workflow-cleanup/64-01-SUMMARY.md`
</output>
