---
phase: 203-state-mutation-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/state-session-mutator.js
  - src/commands/state.js
  - workflows/execute-plan.md
  - tests/state.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - BUNDLE-01
  - BUNDLE-02
must_haves:
  truths:
    - "Batched completion writes stay atomic for non-sacred state and fail closed on rollback or validation drift."
    - "Sacred data writes (decisions, lessons, trajectories, requirements, session continuity) stay on the canonical single-write path."
    - "execute-plan runs `verify:state validate` after any batched state write before progression continues."
  artifacts:
    - path: "src/lib/state-session-mutator.js"
      provides: "atomic batched state mutation"
      contains: "completePlanCore"
    - path: "workflows/execute-plan.md"
      provides: "post-write validation gate"
      contains: "verify:state validate"
    - path: "tests/state.test.cjs"
      provides: "batch-safety regression coverage"
      contains: "batches core plan completion updates in one command"
  key_links:
    - from: "src/commands/state.js"
      to: "src/lib/state-session-mutator.js"
      via: "complete-plan dispatch uses the shared mutator"
      pattern: "verify:state complete-plan"
    - from: "workflows/execute-plan.md"
      to: "src/commands/state.js"
      via: "workflow gates progression on post-write validation"
      pattern: "verify:state complete-plan -> verify:state validate"
    - from: "tests/state.test.cjs"
      to: "src/commands/state.js"
      via: "regression coverage proves the contract on current behavior"
      pattern: "node --test tests/state.test.cjs"
---

<objective>
Make batched state mutation safe without changing the CLI surface.

Purpose: keep non-sacred state batching atomic, keep sacred writes isolated, and make execute-plan stop on failed validation.
Output: repaired state mutation flow, updated execute-plan gate, and regressions that prove the safety contract.
</objective>

> **TDD Decision:** Skipped — this is an orchestration/safety change anchored in existing behavior and needs targeted regressions plus workflow proof, not a full RED/GREEN/REFACTOR slice.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/203-state-mutation-safety/203-CONTEXT.md
@.planning/phases/203-state-mutation-safety/203-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the shared mutation path</name>
  <files>
    src/lib/state-session-mutator.js
    src/commands/state.js
  </files>
  <action>
Keep `completePlanCore` as the only batched completion mutation and preserve sacred data on the existing single-write helpers. Ensure the shared mutator still writes `STATE.md` atomically, persists the SQLite bundle under the project lock, and rolls back the markdown write if persistence fails. Keep the execute-plan completion dispatch aligned with that shared path and do not add a second batching mechanism.
  </action>
  <verify>
```bash
node --test tests/state.test.cjs --test-name-pattern "complete-plan|record-session|add-decision|record-metric"
```
  </verify>
  <done>The shared mutation path remains atomic for non-sacred state and sacred writes still use the canonical single-write helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Gate progression on validation</name>
  <files>
    workflows/execute-plan.md
    src/commands/state.js
  </files>
  <action>
Update the execute-plan flow so every batched state write is followed immediately by `verify:state validate` before any downstream progression continues. Keep the one-retry/fail-closed guidance explicit in the workflow text so unsafe writes stop the run instead of being treated as informational warnings.
  </action>
  <verify>
```bash
node --test tests/workflow.test.cjs --test-name-pattern "execute-plan|complete-plan|validate"
```
  </verify>
  <done>Execute-plan cannot advance past a failed post-write validation gate.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the safety contract</name>
  <files>
    tests/state.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
Add or adjust regression coverage for the happy path, rollback path, sacred-write isolation, and the workflow validation gate. Lock the exact command surfaces the phase depends on and make the failure modes observable in the tests.
  </action>
  <verify>
```bash
npm run build && node bin/bgsd-tools.cjs util:validate-commands --raw && node --test tests/state.test.cjs tests/workflow.test.cjs
```
  </verify>
  <done>The phase has passing regressions plus green build and command-validation proof.</done>
</task>

</tasks>

<verification>
1. `verify:state complete-plan` still performs the batched state completion path for non-sacred state only.
2. `verify:state validate` runs after batched writes in execute-plan before progression continues.
3. `npm run build` and `util:validate-commands --raw` stay green after the workflow changes.
</verification>

<success_criteria>
- [ ] Non-sacred state batching remains atomic and rollback-safe.
- [ ] Sacred data writes stay on the canonical single-write path.
- [ ] Execute-plan stops on failed post-write validation.
- [ ] Regression tests and command/build verification pass.
</success_criteria>

<output>
After completion, create `.planning/phases/203-state-mutation-safety/203-01-SUMMARY.md`
</output>
