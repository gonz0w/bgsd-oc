---
phase: 0110-audit-decision-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/audit.js
  - src/router.js
  - src/lib/constants.js
  - src/lib/command-help.js
  - src/lib/commandDiscovery.js
  - .planning/audit-catalog.json
  - workflows/verify-work.md
autonomous: true
requirements:
  - AUDIT-03
must_haves:
  truths:
    - "src/commands/audit.js no longer exists on disk"
    - "Router does not reference audit namespace — no case 'audit' in router.js"
    - "No audit:scan entries exist in constants.js COMMAND_HELP, command-help.js, or commandDiscovery.js"
    - ".planning/audit-catalog.json no longer exists on disk"
    - "verify-work.md severity inference section references existing severity.js classification rules"
    - "npm test passes with 762+ tests (no regressions from removal)"
    - "npm run build succeeds"
  artifacts:
    - path: "src/router.js"
      provides: "Router without audit namespace"
      contains: "no 'audit' case in namespace switch"
    - path: "src/lib/constants.js"
      provides: "Constants without audit:scan help"
      contains: "no audit:scan COMMAND_HELP entry"
  key_links:
    - from: "src/router.js"
      to: "src/commands/audit.js"
      via: "lazyAudit() removed"
      pattern: "lazyAudit"
---

<objective>
Remove old Phase 110 audit CLI artifacts and clean up a marginal LLM decision point.

Purpose: The prior Phase 110 scope built src/commands/audit.js (739-line scanner/rubric/catalog tool) that is no longer needed under the rescoped phase. Clean removal of all references prevents build errors and dead code. Additionally, strengthen verify-work.md severity inference by cross-referencing the existing severity.js module.

Output: Codebase free of audit CLI artifacts, passing build and test suite.
</objective>

<execution_context>
@/home/cam/.config/oc/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/oc/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0110-audit-decision-framework/0110-RESEARCH.md
@src/commands/audit.js
@src/router.js
@src/lib/constants.js
@src/lib/command-help.js
@src/lib/commandDiscovery.js
@workflows/verify-work.md
@src/lib/review/severity.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete audit.js and audit-catalog.json</name>
  <files>src/commands/audit.js, .planning/audit-catalog.json</files>
  <action>
Delete both files:
- `src/commands/audit.js` (739-line scanner/rubric/catalog from old scope)
- `.planning/audit-catalog.json` (generated artifact from old scanner runs, consumed by nobody)

These are artifacts of the prior Phase 110 scope (CLI audit catalog tool) that no longer serve the rescoped phase.
  </action>
  <verify>
`ls src/commands/audit.js 2>/dev/null || echo "DELETED"` → shows DELETED
`ls .planning/audit-catalog.json 2>/dev/null || echo "DELETED"` → shows DELETED
  </verify>
  <done>Both files removed from disk.</done>
</task>

<task type="auto">
  <name>Task 2: Remove audit references from router, constants, help, and discovery</name>
  <files>src/router.js, src/lib/constants.js, src/lib/command-help.js, src/lib/commandDiscovery.js</files>
  <action>
Clean up all references to the audit namespace and audit.js module across 4 files:

**src/router.js:**
- Remove the `lazyAudit()` function (line ~108: `function lazyAudit() { return _modules.audit || (_modules.audit = require('./commands/audit')); }`)
- Remove `'audit'` from the `KNOWN_NAMESPACES` array (line ~229)
- Remove the entire `case 'audit':` block in the namespace switch (lines ~1253-1258)
- Remove `'audit'` from the fallback error message listing available namespaces (line ~1265)

**src/lib/constants.js:**
- Remove the `audit` line from the general help text (line ~589: `audit                                Audit agent lifecycle coverage against RACI matrix`)
- Remove the `'audit:scan'` entry from `COMMAND_HELP` (lines ~941-958, the entire multi-line template literal)

**src/lib/command-help.js:**
- NOTE: The `'agent:audit'` entry (line ~181) is NOT part of the old audit.js — it's the `agent:audit` subcommand for auditing agent configurations, which is unrelated. Do NOT remove agent:audit references. Only remove entries that reference the `audit` namespace commands (audit:scan etc.). If no `audit:*` entries exist beyond agent:audit, no changes needed in this file.

**src/lib/commandDiscovery.js:**
- NOTE: The `'milestone': ['new', 'complete', 'audit', 'gaps']` entry (line ~352) refers to `milestone audit` (the audit-milestone workflow), NOT the old audit.js scanner. Do NOT remove it.
- The `'agent': ['audit', 'list', 'validate-contracts']` entry (line ~418) refers to `agent:audit`, NOT the old audit.js. Do NOT remove it.
- Only remove entries for the `audit` namespace if any exist (e.g., `'audit': ['scan']`). If no such entries exist, no changes needed in this file.

CRITICAL: Do NOT remove `agent:audit` references — that's a different feature (auditing agent configurations). Only remove the `audit` namespace (audit:scan) references that point to src/commands/audit.js.
  </action>
  <verify>
`npm run build` succeeds.
`grep -n 'lazyAudit\|audit:scan' src/router.js src/lib/constants.js` → no matches.
`grep -n "case 'audit'" src/router.js` → no matches for standalone audit namespace case.
`npm test` passes with 762+ tests.
  </verify>
  <done>All audit namespace references removed. agent:audit and milestone audit references preserved. Build and all tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Strengthen verify-work.md severity inference with severity.js cross-reference</name>
  <files>workflows/verify-work.md</files>
  <action>
The `<severity_inference>` block in verify-work.md (lines ~131-139) contains a simple keyword→severity table that the LLM follows during interactive verification. The same classification logic already exists in `src/lib/review/severity.js` as a proper programmatic module.

Update the `<severity_inference>` section to add a cross-reference note that the canonical severity classification rules live in `src/lib/review/severity.js`, so any future changes to severity logic stay in sync. Keep the existing keyword table in the workflow (it's documentation the LLM reads), but add a comment noting the source of truth:

```
<!-- Canonical severity rules: src/lib/review/severity.js — keep in sync -->
```

This is a LOW priority documentation improvement per the research — the LLM already handles this correctly, but the cross-reference prevents drift.
  </action>
  <verify>
`grep -c 'severity.js' workflows/verify-work.md` → at least 1 match.
  </verify>
  <done>verify-work.md severity inference section references severity.js as the canonical source of truth.</done>
</task>

</tasks>

<verification>
1. `ls src/commands/audit.js 2>/dev/null` → file does not exist
2. `ls .planning/audit-catalog.json 2>/dev/null` → file does not exist
3. `grep -rn 'lazyAudit\|audit:scan' src/` → no matches
4. `grep -c 'severity.js' workflows/verify-work.md` → at least 1
5. `npm run build` → succeeds
6. `npm test` → 762+ tests pass
</verification>

<success_criteria>
- Old audit.js module and catalog artifact deleted
- All audit namespace references cleaned from router, constants, help, discovery
- agent:audit and milestone:audit references preserved (different features)
- verify-work.md severity section cross-references severity.js
- Build succeeds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/0110-audit-decision-framework/0110-01-SUMMARY.md`
</output>
