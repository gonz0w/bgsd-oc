---
phase: 109-duplicate-code-merge
plan: "03"
type: execute
wave: 3
depends_on: ["109-02"]
files_modified: []
autonomous: true
requirements:
  - DUPE-03
user_setup: []

must_haves:
  truths:
    - "All existing tests pass after consolidation"
    - "No runtime errors from refactored code"
    - "Build succeeds without errors"
  artifacts: []
  key_links: []
---

<objective>
Verify that all functionality works correctly after consolidating duplicate code.

Purpose: Ensure the refactoring didn't break any existing functionality.
Output: Test results and verification report.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-duplicate-code-merge/109-CONTEXT.md
@.planning/phases/109-duplicate-code-merge/109-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suite to verify no regressions</name>
  <files></files>
  <action>
1. Run the full test suite: `npm test`
2. Capture test results - all tests must pass
3. If tests fail, analyze failures:
   - If caused by consolidation changes, fix the issues
   - If pre-existing failures, note them but don't block
4. Verify build succeeds: `npm run build`
</action>
  <verify>
- `npm test` passes (or failures are pre-existing and documented)
- `npm run build` succeeds without errors
</verify>
  <done>
All tests pass and build succeeds after consolidation.
</done>
</task>

<task type="auto">
  <name>Task 2: Verify CLI commands still work</name>
  <files></files>
  <action>
1. Test basic CLI functionality: `node bin/bgsd-tools.cjs util:help`
2. Test a few key commands to ensure nothing is broken:
   - `node bin/bgsd-tools.cjs util:map --help`
   - `node bin/bgsd-tools.cjs health`
3. Verify the refactored utilities are properly exported and accessible
</action>
  <verify>
- Basic CLI commands work without errors
- Utility functions are properly exported from bin/bgsd-tools.cjs
</verify>
  <done>
CLI commands function correctly after consolidation.
</done>
</task>

</tasks>

<verification>
- Test suite passes (npm test)
- Build succeeds (npm run build)
- CLI commands work (bgsd-tools.cjs commands execute)
</verification>

<success_criteria>
All tests pass, build succeeds, and CLI commands work correctly after consolidation.
</success_criteria>

<output>
After completion, create `.planning/phases/109-duplicate-code-merge/109-03-SUMMARY.md`
</output>
