---
phase: 109-duplicate-code-merge
plan: "02"
type: execute
wave: 2
depends_on: ["109-01"]
files_modified: []
autonomous: true
requirements:
  - DUPE-02
user_setup: []

must_haves:
  truths:
    - "Common utility functions extracted to shared modules"
    - "Original duplicate locations updated to use shared utilities"
    - "No functionality removed - only code reorganization"
  artifacts:
    - path: "src/lib/"
      provides: "Shared utility modules for consolidated code"
    - path: "src/commands/"
      provides: "Updated command handlers using shared utilities"
    - path: "src/plugin/"
      provides: "Updated plugin modules using shared utilities"
  key_links: []
---

<objective>
Analyze the duplicate detection report and consolidate common patterns into shared utilities.

Purpose: Reduce code duplication by extracting reusable patterns to shared modules.
Output: Refactored code with duplicates consolidated.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-duplicate-code-merge/109-CONTEXT.md
@.planning/phases/109-duplicate-code-merge/109-01-SUMMARY.md
@.planning/phases/109-duplicate-code-merge/duplicates-report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze duplicates and identify consolidation candidates</name>
  <files></files>
  <action>
1. Read the duplicates-report.md from Plan 01
2. Identify patterns that are worth consolidating (high-reuse potential):
   - Error handling patterns (try/catch, error formatting)
   - File parsing/validation logic
   - String manipulation utilities
   - Repeated helper functions
3. Skip consolidation if it would reduce clarity (per user decision)
4. Prioritize by scope priority: lib/ first, then commands/, then plugin/
5. Create a consolidation plan: list which files to modify and what to extract
</action>
  <verify>
- Consolidation candidates identified with clear extraction plan
- Skip candidates documented where consolidation reduces clarity
</verify>
  <done>
Consolidation plan created with prioritized list of patterns to extract.
</done>
</task>

<task type="auto">
  <name>Task 2: Extract common patterns to shared utilities</name>
  <files>src/lib/*.js (new and existing)</files>
  <action>
1. For each consolidation candidate:
   a. Create new shared utility in src/lib/ if doesn't exist (e.g., src/lib/error-utils.js, src/lib/string-utils.js)
   b. Extract the common function to the shared utility
   c. Update all duplicate locations to import and use the shared utility
2. Follow user decisions:
   - Similarity threshold: >70% (only consolidate substantial duplicates)
   - Scope priority: lib/ → commands/ → plugin/
   - Agent discretion: Skip if reduces clarity
3. Ensure backward compatibility - don't break existing exports
4. Keep code readable - don't over-consolidate for DRY purity
</action>
  <verify>
- New shared utility modules created in src/lib/
- Original duplicate files updated to use shared utilities
- No functionality removed, only reorganized
</verify>
  <done>
Common patterns extracted to shared utilities and all references updated.
</done>
</task>

</tasks>

<verification>
- Verify shared utilities created in src/lib/
- Verify original files updated to import from shared utilities
- Verify code still functions: basic sanity check of imports and exports
</verification>

<success_criteria>
Duplicate code patterns consolidated into shared utilities. Original locations refactored to use shared code.
</success_criteria>

<output>
After completion, create `.planning/phases/109-duplicate-code-merge/109-02-SUMMARY.md`
</output>
