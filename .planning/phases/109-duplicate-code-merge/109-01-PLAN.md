---
phase: 109-duplicate-code-merge
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - DUPE-01
user_setup: []

must_haves:
  truths:
    - "Duplicate detection runs without errors"
    - "Duplicate patterns are identified across src/ directory"
    - "Report includes file paths, line numbers, and similarity percentages"
  artifacts:
    - path: ".planning/phases/109-duplicate-code-merge/duplicates-report.md"
      provides: "Analysis of duplicate code patterns found"
      contains: "list of duplicate code with similarity >70%"
  key_links: []
---

<objective>
Run automated duplicate code detection on the src/ directory using jscpd or similar tool.

Purpose: Identify all duplicate and near-duplicate code patterns above 70% similarity threshold.
Output: Duplicate detection report with file paths, line numbers, and similarity percentages.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-duplicate-code-merge/109-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run jscpd duplicate detection on src/</name>
  <files>.planning/phases/109-duplicate-code-merge/duplicates-report.md</files>
  <action>
1. Check if jscpd is available: `npx jscpd --version` or install if needed: `npm install -g jscpd`
2. Run jscpd on src/ directory with threshold 70%: `npx jscpd src/ --threshold 70 --output .planning/phases/109-duplicate-code-merge/`
3. If jscpd fails, fall back to manual detection using grep patterns for common duplicated code (error handling, string manipulation, validation logic)
4. Parse jscpd output (XML/JSON format) and create a markdown report
5. The report should include:
   - Total duplicate blocks found
   - Files involved and line numbers
   - Similarity percentage for each
   - Categorize by priority: lib/, commands/, plugin/ per user decision
</action>
  <verify>
- jscpd report generated at .planning/phases/109-duplicate-code-merge/duplicates-report.md
- Report lists duplicates with similarity >70%
- Priority directories (lib/, commands/, plugin/) are analyzed
</verify>
  <done>
Duplicate detection report exists with identified duplicate patterns across src/ directory.
</done>
</task>

</tasks>

<verification>
- Verify jscpd ran successfully (exit code 0) or fallback detection completed
- Verify report file was created with content
- Verify report shows duplicates in priority order: lib/ first, then commands/, then plugin/
</verification>

<success_criteria>
Duplicate detection completes without errors. Report identifies all code patterns with >70% similarity.
</success_criteria>

<output>
After completion, create `.planning/phases/109-duplicate-code-merge/109-01-SUMMARY.md`
</output>
