---
phase: "135"
plan: "05"
title: "Compress audit-milestone, quick & map-codebase + Final Verification"
type: standard
requirements: ["COMP-01", "COMP-02", "COMP-03"]
depends_on: ["135-02", "135-03", "135-04"]
files_modified:
  - workflows/audit-milestone.md
  - workflows/quick.md
  - workflows/map-codebase.md
  - bin/bgsd-tools.cjs
estimated_tasks: 3
wave: 3
---

# Plan 05: Compress audit-milestone, quick & map-codebase + Final Verification

## Objective

Compress the final 3 workflows (audit-milestone 301 lines, quick 341 lines, map-codebase 303 lines), then run the full verification suite against the Phase 134 baseline to confirm 40%+ average token reduction across all 10 workflows and zero structural regressions.

## Must-Haves

```yaml
must_haves:
  truths:
    - "audit-milestone.md is reduced from ~301 lines to ~180 lines or fewer"
    - "quick.md is reduced from ~341 lines to ~205 lines or fewer"
    - "map-codebase.md is reduced from ~303 lines to ~180 lines or fewer"
    - "All 3 workflows have <!-- section: step_name --> markers at each major process step"
    - "quick.md CI quality gate replaced with <skill:ci-quality-gate />"
    - "workflow:compare shows 40%+ average token reduction across the top 10 workflows"
    - "workflow:verify-structure passes on all 44 workflows with zero regressions"
  artifacts:
    - path: "workflows/audit-milestone.md"
      provides: "Compressed audit-milestone workflow"
      contains: "<!-- section: markers, all Task() calls, requirements cross-reference logic"
    - path: "workflows/quick.md"
      provides: "Compressed quick workflow"
      contains: "<!-- section: markers, <skill:ci-quality-gate />, all 6 Task() calls"
    - path: "workflows/map-codebase.md"
      provides: "Compressed map-codebase workflow"
      contains: "<!-- section: markers, all 4 Task() calls"
  key_links:
    - from: "workflows/quick.md"
      to: "skills/ci-quality-gate/SKILL.md"
      via: "<skill:ci-quality-gate />"
      pattern: "<skill:ci-quality-gate"
```

## Tasks

### Task 1: Compress audit-milestone.md and map-codebase.md

- **Type:** auto
- **Files:**
  - `workflows/audit-milestone.md` (modify)
  - `workflows/map-codebase.md` (modify)

- **Action:**

  **audit-milestone.md:**

  Add section markers: `<!-- section: initialize -->`, `<!-- section: determine_scope -->`, `<!-- section: read_verifications -->`, `<!-- section: spawn_checker -->`, `<!-- section: collect_results -->`, `<!-- section: requirements_coverage -->`, `<!-- section: aggregate_report -->`, `<!-- section: present_results -->`, `<!-- section: offer_next -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  Replace bgsd-context initialization with `<skill:bgsd-context-init />`.

  Prose-tighten:
  - Compress the 5-substep requirements coverage section (the largest block) — keep the cross-reference logic (parse REQUIREMENTS.md traceability table, parse VERIFICATION.md requirements tables, extract SUMMARY.md frontmatter) but express as a compact algorithm instead of verbose prose per substep
  - Compress the Status Determination Matrix to a compact table
  - Compress the MILESTONE-AUDIT.md YAML template — keep required fields but remove verbose field descriptions
  - Compress the 3 result routing paths (passed/gaps/tech_debt) — keep routing logic, compress Next Up blocks
  - Compress orphan detection instructions

  Preserve: 1 Task() call (bgsd-verifier), all ~4 CLI commands, all 3 routing paths, FAIL gate logic.

  **map-codebase.md:**

  Add section markers: `<!-- section: purpose -->`, `<!-- section: init_context -->`, `<!-- section: check_existing -->`, `<!-- section: create_structure -->`, `<!-- section: spawn_agents -->`, `<!-- section: collect_confirmations -->`, `<!-- section: verify_output -->`, `<!-- section: scan_secrets -->`, `<!-- section: commit -->`, `<!-- section: offer_next -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  Replace bgsd-context initialization with `<skill:bgsd-context-init />`.

  Prose-tighten:
  - Compress purpose/philosophy blocks to 1-2 sentences
  - Compress check_existing refresh/update/skip logic to a compact decision list
  - Compress the 4 mapper spawn blocks — they follow identical patterns, express as a loop or table with dimensions (tech, arch, quality, concerns) rather than 4 separate blocks
  - Compress secret scanning to essential grep patterns
  - Compress verification (ls + wc -l) to 2 lines

  Preserve: All 4 Task() calls (4 mappers), 1 CLI command (execute:commit), run_in_background pattern.

- **Verify:** audit-milestone.md ≤180 lines, map-codebase.md ≤180 lines. All Task() calls and CLI commands preserved. Run `workflow:verify-structure` against baseline for both.

- **Done:** audit-milestone.md and map-codebase.md each reduced 40%+ with section markers. All structural elements preserved.

### Task 2: Compress quick.md

- **Type:** auto
- **Files:**
  - `workflows/quick.md` (modify)

- **Action:**

  **Add section markers**: `<!-- section: parse_args -->`, `<!-- section: initialize -->`, `<!-- section: create_task_dir -->`, `<!-- section: spawn_planner -->`, `<!-- section: plan_checker -->`, `<!-- section: spawn_executor -->`, `<!-- section: ci_quality_gate -->`, `<!-- section: verification -->`, `<!-- section: update_state -->`, `<!-- section: final_commit -->`, `<!-- section: success_criteria -->`. Close each with `<!-- /section -->`.

  **Replace shared blocks:**
  - Replace bgsd-context initialization with `<skill:bgsd-context-init />`
  - Replace CI quality gate block (~35-40 lines) with `<skill:ci-quality-gate scope="quick-{task_name}" />`
  - Replace classifyHandoffIfNeeded workaround with 1-line comment

  **Prose-tighten throughout:**
  - Compress argument parsing (--full, --ci, --no-ci) to a compact flag table
  - Compress the planner spawn Task() block — keep params, remove explanatory prose
  - Compress plan-checker loop — keep the max-2-iteration logic, remove verbose explanation
  - Compress executor spawn — keep Task() params, remove prose
  - Compress verification (--full only) — keep spawn, remove explanation
  - Compress STATE.md update (Quick Tasks Completed table) to essential instructions
  - Compress the --full conditional logic throughout — use compact `if --full:` markers

  **Preservation rules:**
  - All 6 Task() calls (planner, checker, revision planner, executor, CI gate, verifier)
  - All ~3 CLI commands (util:config-get, execute:commit)
  - --full mode conditional logic throughout
  - Plan-checker iteration loop (max 2)

- **Verify:** Count lines — target ≤205. Confirm all 6 Task() calls and CLI commands preserved. Run `workflow:verify-structure` against baseline.

- **Done:** quick.md reduced 40%+ with section markers. CI quality gate replaced with skill reference. All structural elements preserved.

### Task 3: Final verification — measure reduction and validate structure

- **Type:** auto
- **Files:**
  - `bin/bgsd-tools.cjs` (rebuild)

- **Action:**

  1. **Rebuild the binary** — Run `npm run build` to ensure the binary reflects any source changes made during the phase.

  2. **Run `workflow:compare`** against the Phase 134 baseline snapshot (the one saved in Plan 01, Task 1). Use the snapshot path saved earlier or run `workflow:compare` with no args to auto-select the pre-compression baseline vs current state.

  3. **Calculate top-10 average reduction** — From the compare output, extract the token deltas for the 10 target workflows:
     - discuss-phase, execute-phase, new-milestone, execute-plan, transition
     - new-project, audit-milestone, quick, resume-project, map-codebase
     - Compute: `average_reduction = sum(per_workflow_percent_reduction) / 10`
     - **Must be 40%+ average.** If below 40%, identify the weakest workflows and note which need further compression.

  4. **Run `workflow:verify-structure`** against the Phase 134 baseline snapshot.
     - **Must exit with code 0** (zero regressions).
     - If any workflow shows structural regressions (missing Task() calls, CLI commands, or section markers), document which elements were lost and fix them.

  5. **Verify section markers** — For each of the 10 compressed workflows, confirm `<!-- section: -->` markers exist at every major step. The `workflow:verify-structure` structural fingerprint includes section markers, so a passing result confirms this.

  6. **Verify skill references** — Confirm the following `<skill:X />` tags exist in the specified workflows:
     - `<skill:bgsd-context-init />` in all 10 workflows
     - `<skill:ci-quality-gate` in execute-phase.md and quick.md
     - `<skill:research-pipeline` in new-milestone.md and new-project.md
     - `<skill:deviation-rules` in execute-plan.md
     - `<skill:commit-protocol` in execute-plan.md
     - `<skill:checkpoint-protocol` in execute-plan.md

  7. **Document results** — Print the final comparison table showing before/after token counts per workflow and the overall reduction percentage.

- **Verify:** `workflow:compare` shows 40%+ average token reduction for top 10 workflows. `workflow:verify-structure` exits 0. All `<skill:X />` references confirmed present.

- **Done:** Phase 135 verification complete. All 10 workflows compressed 40%+ with section markers and skill references. Zero structural regressions confirmed. Measurement data ready for SUMMARY.md documentation.
