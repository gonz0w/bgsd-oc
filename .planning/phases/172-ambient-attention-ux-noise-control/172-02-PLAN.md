---
phase: 172-ambient-attention-ux-noise-control
plan: 02
type: tdd
wave: 2
depends_on:
  - 172-01
files_modified:
  - src/plugin/index.js
  - src/plugin/cmux-attention-sync.js
  - tests/plugin.test.cjs
autonomous: true
requirements:
  - CMUX-05
  - CMUX-06
  - CMUX-09
outcome_ids: []
user_setup: []
must_haves:
  truths:
    - "Attached cmux workspaces show concise one-line sidebar logs for meaningful lifecycle moments while routine refreshes stay silent"
    - "Attention-worthy moments such as checkpoints, required user input, blockers, warnings, and plan, phase, or workflow completion generate notifications without notifying for routine task completion"
    - "Sidebar snapshot refresh remains separate from edge-triggered attention sync so unchanged state refreshes do not spam logs or notifications"
  artifacts:
    - path: "src/plugin/cmux-attention-sync.js"
      provides: "Stateful edge-triggered writer that applies policy events through attached cmux log and notify calls"
      contains: "syncCmuxAttention, createAttentionMemory, lastEmittedAt, lastEventKeys"
    - path: "src/plugin/index.js"
      provides: "Plugin lifecycle wiring that refreshes sidebar snapshots and attention events through separate paths"
      contains: "refreshCmuxSidebar, refreshCmuxAttention, tool.execute.after, event.type === 'session.idle'"
    - path: "tests/plugin.test.cjs"
      provides: "Plugin-level regressions for ambient logs, notifications, and refresh-driven spam suppression"
      contains: "Plugin cmux attention sync, log-only routine completion, notify-worthy checkpoint, repeated warning suppression"
  key_links:
    - from: "src/plugin/index.js"
      to: "src/plugin/cmux-attention-sync.js"
      via: "plugin lifecycle hooks feeding edge-triggered attention sync separately from level-triggered sidebar refresh"
      pattern: "refreshCmuxAttention|syncCmuxAttention|tool.execute.after|session.idle|file.watcher.updated"
    - from: "src/plugin/cmux-attention-sync.js"
      to: "src/plugin/cmux-attention-policy.js"
      via: "stateful writer consuming pure classification results and semantic keys before calling adapter log or notify"
      pattern: "classifyAttentionEvent|shouldEmitAttentionEvent|key|notify|log"
    - from: "src/plugin/cmux-attention-sync.js"
      to: "src/plugin/cmux-targeting.js"
      via: "attached adapter delivering `log(...)` and `notify(...)` only after trust-gated policy emission"
      pattern: "adapter.log|adapter.notify|attached|suppressed"
---

<objective>
Wire low-noise ambient logs and notifications into the plugin lifecycle without turning refreshes into spam.

Purpose: once the policy and trusted notify transport exist, Phase 172 still needs one narrow integration path that emits concise logs and attention notifications only on semantic transitions. This plan preserves Phase 171's trust gate and snapshot sync, while adding a separate edge-triggered attention writer that keeps the sidebar readable and notifications exceptional.
Output: lifecycle-driven attention sync that logs meaningful moments, escalates only attention-worthy events, and suppresses unchanged refresh noise.
</objective>

> **TDD Decision:** Selected — lifecycle-triggered log and notification emission, quiet suppression, and refresh-spam avoidance are deterministic integration contracts with clear observable outcomes.

<feature>
  <name>Edge-triggered plugin ambient attention sync</name>
  <files>
    src/plugin/index.js
    src/plugin/cmux-attention-sync.js
    tests/plugin.test.cjs
  </files>
  <tdd-targets>
    <red>node --test --test-force-exit --test-name-pattern "Plugin cmux attention sync" tests/plugin.test.cjs</red>
    <green>node --test --test-force-exit --test-name-pattern "Plugin cmux attention sync" tests/plugin.test.cjs</green>
    <refactor>node --test --test-force-exit --test-name-pattern "Plugin cmux attention sync" tests/plugin.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Attached cmux workspaces receive terse log lines for meaningful lifecycle moments and notifications only for attention-worthy events, while repeated unchanged refreshes stay silent.
    Cases: planner or workflow start logs once; routine task completion logs without notifying; checkpoints, waiting-input, blockers, warnings, and plan, phase, or workflow completion log and notify; repeated unchanged warning or blocker conditions cool down; snapshot-only refreshes do not re-emit the same event.
  </behavior>
  <implementation>
    Add one narrow attention-sync helper that keeps per-workspace last-emitted semantic keys and cooldown timestamps, then call it from `src/plugin/index.js` alongside the existing sidebar refresh path. Reuse the Phase 171 trust gate and project-state inputs, keep snapshot sync level-triggered, and keep attention sync edge-triggered so attached sessions stay useful instead of chatty.
  </implementation>
</feature>

<execution_context>
@.planning/phases/172-ambient-attention-ux-noise-control/172-CONTEXT.md
@.planning/phases/172-ambient-attention-ux-noise-control/172-RESEARCH.md
@.planning/phases/172-ambient-attention-ux-noise-control/172-01-PLAN.md
</execution_context>

<context>
@src/plugin/index.js
@src/plugin/cmux-sidebar-snapshot.js
@src/plugin/idle-validator.js
@src/plugin/stuck-detector.js
@tests/plugin.test.cjs
@.planning/phases/171-ambient-workspace-status-progress/171-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add plugin integration regressions for ambient attention sync and quiet suppression</name>
  <files>tests/plugin.test.cjs</files>
  <action>
Add focused plugin-level regressions that prove Phase 172 emits ambient attention only on semantic transitions through the attached adapter boundary.

Cover at least these cases: startup or planner/executor start logs once; routine task completion is log-only; checkpoints, waiting-input, blockers, warnings, and plan, phase, or workflow completion notify plus log; unchanged repeated warning or blocker refreshes stay suppressed until state meaningfully changes; suppressed adapters remain no-op and do not break normal plugin hooks or sidebar sync.
  </action>
  <verify>rg -n "Plugin cmux attention sync|log-only|checkpoint|phase-complete|repeated warning suppression|suppressed" tests/plugin.test.cjs</verify>
  <done>`tests/plugin.test.cjs` proves ambient attention follows the locked channel split, completion escalation rules, and refresh-spam suppression contract</done>
</task>

<task type="auto">
  <name>Wire edge-triggered attention sync through plugin lifecycle hooks</name>
  <files>
src/plugin/index.js
src/plugin/cmux-attention-sync.js
  </files>
  <action>
Implement one stateful attention-sync helper that consumes the Plan 01 policy output, tracks per-workspace semantic keys and cooldown timestamps, and writes concise logs plus notifications through `cmuxAdapter.log(...)` and `cmuxAdapter.notify(...)` only when meaningfully new events occur.

Call this helper from plugin startup and the existing `file.watcher.updated`, `session.idle`, and `tool.execute.after` refresh points without collapsing it into `refreshCmuxSidebar()`. Keep the two paths separate: sidebar snapshots remain idempotent current-state sync, while ambient attention remains edge-triggered and quiet by default.
  </action>
  <verify>node --test --test-force-exit --test-name-pattern "Plugin cmux attention sync" tests/plugin.test.cjs</verify>
  <done>The plugin has one centralized attention sync path that logs meaningful moments, escalates only attention-worthy events, and suppresses refresh-driven duplicates</done>
</task>

</tasks>

<verification>
- Run: npm run build
</verification>

<success_criteria>
- [ ] Attached sessions emit concise one-line sidebar logs for meaningful lifecycle events without writing on every snapshot refresh
- [ ] Notifications are reserved for checkpoints, required input, blockers, warnings, and plan, phase, or workflow completion moments
- [ ] Routine task completion stays log-only unless it crosses a plan, phase, or final workflow boundary
- [ ] Repeated unchanged non-essential events cool down or stay suppressed until state meaningfully changes
</success_criteria>

<output>
After completion, create `.planning/phases/172-ambient-attention-ux-noise-control/172-02-SUMMARY.md`
</output>
