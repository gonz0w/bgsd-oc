---
phase: 172-ambient-attention-ux-noise-control
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/plugin/cmux-targeting.js
  - src/plugin/cmux-attention-policy.js
  - tests/plugin-cmux-targeting.test.cjs
  - tests/plugin-cmux-attention-policy.test.cjs
autonomous: true
requirements:
  - CMUX-06
  - CMUX-09
outcome_ids: []
user_setup: []
must_haves:
  truths:
    - "Attached cmux sessions classify lifecycle moments into log-only, notify-plus-log, or suppress outcomes without notifying for routine starts or routine task completion"
    - "Repeated non-essential events reuse semantic workspace-scoped keys so unchanged warnings, blockers, or refresh-only events cool down instead of spamming the user"
    - "Suppressed or unattached sessions stay quiet because notify delivery stays behind the existing write-proven cmux adapter boundary"
  artifacts:
    - path: "src/plugin/cmux-attention-policy.js"
      provides: "Pure attention policy that normalizes lifecycle moments into semantic events, dedupe keys, and escalation decisions"
      contains: "classifyAttentionEvent, buildAttentionEventKey, shouldEmitAttentionEvent"
    - path: "src/plugin/cmux-targeting.js"
      provides: "Attached cmux adapter contract extended with workspace-scoped notify delivery"
      contains: "notify, createAttachedCmuxAdapter, createNoopCmuxAdapter"
    - path: "tests/plugin-cmux-attention-policy.test.cjs"
      provides: "Regression coverage for quiet-by-default escalation and semantic dedupe behavior"
      contains: "task-complete log-only, checkpoint notify, plan-complete notify, semantic dedupe key, cooldown"
  key_links:
    - from: "src/plugin/cmux-attention-policy.js"
      to: "src/plugin/cmux-targeting.js"
      via: "policy output feeding adapter-safe log and notify delivery without bypassing attachment trust gates"
      pattern: "notify|log|workspaceId|key|cooldownMs"
    - from: "src/plugin/cmux-attention-policy.js"
      to: "src/plugin/notification.js"
      via: "noise-control mechanics borrowing semantic dedupe and cooldown ideas from existing local notification history"
      pattern: "history|getHistory|severity|dedupe|rate"
    - from: "tests/plugin-cmux-targeting.test.cjs"
      to: "src/plugin/cmux-targeting.js"
      via: "adapter regressions proving attached notify calls are workspace-scoped and suppressed adapters stay inert"
      pattern: "notify:workspace|suppressed|not-attached"
---

<objective>
Create the attention-policy contract and trusted notify transport that Phase 172 will build on.

Purpose: Phase 172 needs one pure layer that decides which lifecycle moments deserve logs, notifications, or silence before plugin hooks start emitting attention side effects. This plan keeps the quiet-by-default policy centralized, reuses the attached cmux boundary from Phase 170, and prevents notify support from bypassing the existing trust gate.
Output: a pure attention-policy helper plus attached-adapter notify support with deterministic regressions for escalation, semantic dedupe keys, and cooldown behavior.
</objective>

> **TDD Decision:** Selected — event classification, semantic dedupe keys, and attached-versus-suppressed notify transport are deterministic behavior contracts with clear pass/fail outcomes.

<feature>
  <name>Semantic ambient attention policy and trusted notify transport</name>
  <files>
    src/plugin/cmux-targeting.js
    src/plugin/cmux-attention-policy.js
    tests/plugin-cmux-targeting.test.cjs
    tests/plugin-cmux-attention-policy.test.cjs
  </files>
  <tdd-targets>
    <red>npm run test:file -- tests/plugin-cmux-targeting.test.cjs tests/plugin-cmux-attention-policy.test.cjs</red>
    <green>npm run test:file -- tests/plugin-cmux-targeting.test.cjs tests/plugin-cmux-attention-policy.test.cjs</green>
    <refactor>npm run test:file -- tests/plugin-cmux-targeting.test.cjs tests/plugin-cmux-attention-policy.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Attached cmux sessions can normalize lifecycle moments into a small canonical event set and know whether each moment should log, notify, or stay suppressed.
    Cases: planner or executor start logs without notifying; routine task completion stays log-only; checkpoints, waiting-input, blockers, warnings, plan completion, phase completion, and workflow completion notify on first occurrence; unchanged repeated events reuse semantic keys and cooldowns so refreshes do not re-alert the user.
  </behavior>
  <implementation>
    Add one pure helper that turns project lifecycle signals into semantic event objects with kind, dedupe key, log payload, optional notify payload, and cooldown metadata. Extend `src/plugin/cmux-targeting.js` with `notify(...)` support behind the same attached-or-suppressed adapter contract instead of introducing a new write path.
  </implementation>
</feature>

<execution_context>
@.planning/phases/172-ambient-attention-ux-noise-control/172-CONTEXT.md
@.planning/phases/172-ambient-attention-ux-noise-control/172-RESEARCH.md
</execution_context>

<context>
@src/plugin/cmux-targeting.js
@src/plugin/notification.js
@src/plugin/index.js
@tests/plugin-cmux-targeting.test.cjs
@.planning/phases/170-cmux-workspace-detection-safe-targeting/170-03-SUMMARY.md
@.planning/phases/171-ambient-workspace-status-progress/171-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add regressions for notify transport and quiet-by-default attention policy</name>
  <files>
tests/plugin-cmux-targeting.test.cjs
tests/plugin-cmux-attention-policy.test.cjs
  </files>
  <action>
Add focused regressions that lock the Phase 172 event contract before implementation exists.

Cover at least these cases: attached adapters expose workspace-scoped `notify(...)`; suppressed adapters keep notify inert; `planner-start`, `executor-start`, and routine `task-complete` classify as log-only; `checkpoint`, `waiting-input`, `blocker`, `warning`, `plan-complete`, `phase-complete`, and `workflow-complete` classify as notify-plus-log; repeated unchanged warning or blocker events reuse one semantic key and remain suppressed until cooldown expiry or meaningful identity change.
  </action>
  <verify>rg -n "notify|task-complete|checkpoint|plan-complete|phase-complete|semantic dedupe|cooldown" tests/plugin-cmux-targeting.test.cjs tests/plugin-cmux-attention-policy.test.cjs</verify>
  <done>The new regressions name the locked notify matrix, semantic dedupe-key behavior, and attached-versus-suppressed transport contract that Phase 172 must preserve</done>
</task>

<task type="auto">
  <name>Implement the pure attention policy and attached notify boundary</name>
  <files>
src/plugin/cmux-targeting.js
src/plugin/cmux-attention-policy.js
  </files>
  <action>
Create a pure attention-policy helper that emits canonical event objects with stable semantic keys, concise action-first log text, optional notify payloads, and cooldown metadata keyed by workspace and event meaning.

Extend `src/plugin/cmux-targeting.js` so attached adapters can call `notify(...)` through the existing trusted workspace boundary while suppressed adapters stay fail-open and quiet. Keep the policy helper pure: it should classify events and dedupe identities, not perform cmux writes or re-derive Phase 171 state truth.
  </action>
  <verify>npm run test:file -- tests/plugin-cmux-targeting.test.cjs tests/plugin-cmux-attention-policy.test.cjs</verify>
  <done>The repo has one pure attention-policy contract and one trusted notify transport path that later plugin wiring can consume without bypassing cmux attachment safety</done>
</task>

</tasks>

<verification>
- Run: npm run build
</verification>

<success_criteria>
- [ ] One pure helper classifies lifecycle moments into log-only, notify-plus-log, or suppress outcomes that match the locked quiet-by-default policy
- [ ] Semantic event keys include workspace and stable event identity so refresh-only repeats do not alert again
- [ ] Attached adapters can deliver workspace-scoped `notify(...)` without adding a second cmux targeting path
- [ ] Suppressed and unattached adapters remain quiet and unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/172-ambient-attention-ux-noise-control/172-01-SUMMARY.md`
</output>
