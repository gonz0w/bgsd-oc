---
phase: 0124-tool-detection-infrastructure
plan: 02
type: tdd
wave: 2
depends_on: ["01"]
files_modified:
  - tests/cli-tools.test.cjs
autonomous: true
requirements:
  - TOOL-DET-01
intent:
  outcome_ids:
    - DO-86
must_haves:
  truths:
    - "All detection functions have unit tests covering happy path and error cases"
    - "File cache read/write/expiry has dedicated tests with temp directories"
    - "Version parsing covers all 6 tool output formats plus edge cases"
    - "Cross-platform resolveToolPath logic is tested for win32 and posix branches"
    - "detect:tools CLI command output format matches spec (flat JSON array)"
  artifacts:
    - path: "tests/cli-tools.test.cjs"
      provides: "Comprehensive test suite for tool detection infrastructure"
  key_links:
    - from: "tests/cli-tools.test.cjs"
      to: "src/lib/cli-tools/detector.js"
      via: "require and test all exports"
      pattern: "detectTool|getToolStatus|parseVersion|meetsMinVersion|clearCache"
    - from: "tests/cli-tools.test.cjs"
      to: "src/lib/cli-tools/install-guidance.js"
      via: "require and test guidance functions"
      pattern: "getInstallGuidance|getInstallCommand"
    - from: "tests/cli-tools.test.cjs"
      to: "src/lib/cli-tools/fallback.js"
      via: "require and test fallback wrapper"
      pattern: "withToolFallback|isToolAvailable"
---

<objective>
Create comprehensive TDD test suite for the tool detection infrastructure covering detector, install guidance, fallback wrapper, and CLI command output.

Purpose: Phase 124's detection code currently has zero tests. This plan adds test coverage for all public APIs ensuring correctness and preventing regressions.
Output: `tests/cli-tools.test.cjs` with 40+ tests covering detection, caching, version parsing, cross-platform logic, install guidance, and CLI output format.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/0124-tool-detection-infrastructure/0124-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core detector and cache tests</name>
  <files>
    tests/cli-tools.test.cjs
  </files>
  <action>
Create `tests/cli-tools.test.cjs` with the project's existing test framework (Node.js built-in `node:test` + `assert`). Check an existing test file like `tests/misc.test.cjs` for the exact pattern.

**Test groups to write:**

**1. detector.js — detectTool()**
- `detectTool('ripgrep')` returns object with `available` (boolean), `name`, `description`
- `detectTool('rg')` resolves alias to ripgrep
- `detectTool('unknown-tool-xyz')` returns `{ available: false, error: 'Unknown tool' }`
- `detectTool` returns `path` string when tool is available
- `detectTool` returns `version` string when tool is available
- Repeated calls return cached result (same object reference or timestamp check)

**2. detector.js — getToolStatus()**
- Returns object with all 6 tool keys: ripgrep, fd, jq, yq, bat, gh
- Each entry has `available`, `path`, `name`, `description`, `version` fields
- No entry is missing any required field (even if null)

**3. detector.js — parseVersion()**
- `parseVersion('ripgrep 15.1.0')` → `{ major: 15, minor: 1, patch: 0 }`
- `parseVersion('jq-1.8.1')` → `{ major: 1, minor: 8, patch: 1 }`
- `parseVersion('fd 10.4.2')` → `{ major: 10, minor: 4, patch: 2 }`
- `parseVersion('gh version 2.88.0 (2026-03-11)')` → `{ major: 2, minor: 88, patch: 0 }`
- `parseVersion('bat 0.26.1 (v0.25.0-402-g979ba226)')` → `{ major: 0, minor: 26, patch: 1 }`
- `parseVersion('yq 3.4.3')` → `{ major: 3, minor: 4, patch: 3 }`
- `parseVersion('no version here')` → `null`
- `parseVersion(null)` → `null`
- `parseVersion('')` → `null`

**4. detector.js — meetsMinVersion()**
- Tool available with version above minimum → `{ meets: true, current: '...', required: '...' }`
- Tool available with version below minimum → `{ meets: false, current: '...', required: '...' }`
- Tool available with exact minimum version → `{ meets: true, ... }`
- Tool not available → `{ meets: false, current: null, required: '...' }`

**5. detector.js — file cache**
- Use `setCachePath(tempDir + '/tools.json')` to redirect cache to temp dir
- After `detectTool()`, cache file exists on disk
- `clearCache()` removes both in-memory and file cache
- Expired file cache (modify timestamp to >5min ago) triggers fresh detection
- Corrupted cache file (write invalid JSON) falls back gracefully

**6. detector.js — clearCache()**
- After `clearCache()`, in-memory Map is empty
- After `clearCache()`, file cache is deleted

**7. TOOLS constant**
- Has exactly 6 entries
- Each has `name`, `aliases`, `description`
  </action>
  <verify>
```bash
npm run build && npm test 2>&1 | tail -20
```
All new tests pass alongside existing test suite.
  </verify>
  <done>tests/cli-tools.test.cjs exists with 25+ tests covering detectTool, getToolStatus, parseVersion, meetsMinVersion, file cache, and clearCache. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Install guidance, fallback wrapper, and CLI output tests</name>
  <files>
    tests/cli-tools.test.cjs
  </files>
  <action>
Add additional test groups to `tests/cli-tools.test.cjs`:

**8. install-guidance.js — getInstallGuidance()**
- Returns object for known tool ('ripgrep') with `name`, `installCommand`, `platform`
- Returns `null` for unknown tool
- Resolves aliases (e.g., 'rg' → ripgrep guidance)
- `installCommand` is a non-empty string
- `platform` is one of 'darwin', 'linux', 'win32'

**9. install-guidance.js — getInstallCommand()**
- Returns string for known tool
- Returns null for unknown tool

**10. install-guidance.js — getAllToolConfig()**
- Returns object with at least 6 tool entries (ripgrep, fd, jq, yq, bat, gh)
- Each tool has `install` object with `darwin`, `linux`, `win32` keys

**11. fallback.js — withToolFallback()**
- When tool is available: cliFn is called, result returned, `usedFallback: false`
- When tool is unavailable: fallbackFn is called, result returned, `usedFallback: true`, `guidance` present
- When cliFn throws: returns `{ success: false, error: '...' }`
- When fallbackFn throws: returns `{ success: false, usedFallback: true, error: '...' }`

**12. fallback.js — isToolAvailable()**
- Returns boolean for known tools
- Returns false for unknown tools

**13. CLI output format — detect:tools**
- Run `node bin/bgsd-tools.cjs detect:tools` via execSync
- Parse output as JSON — must be valid JSON
- Result is an array (not object)
- Each element has `name` (string), `cmd` (string), `available` (boolean)
- Array has exactly 6 elements
- Available tools have `path` and `version` fields
- Unavailable tools have `install` field

**14. CLI backward compat — util:tools**
- Run `node bin/bgsd-tools.cjs util:tools` via execSync
- Parse output as JSON — must be valid JSON
- Result has `available` and `unavailable` arrays and `summary` object
  </action>
  <verify>
```bash
npm run build && npm test 2>&1 | tail -20
```
All tests pass. Total test count increased by 40+.
  </verify>
  <done>tests/cli-tools.test.cjs has 40+ tests covering detector, install guidance, fallback, and CLI output format. All pass alongside existing suite.</done>
</task>

</tasks>

<verification>
1. `npm test` passes with all new and existing tests
2. Test file covers all public exports from detector.js, install-guidance.js, fallback.js
3. CLI output format tests validate the `detect:tools` contract
4. File cache tests use temp directories (no pollution of real cache)
5. Total test count increased by 40+
</verification>

<success_criteria>
- [ ] `tests/cli-tools.test.cjs` exists with 40+ tests
- [ ] detectTool, getToolStatus, parseVersion, meetsMinVersion all have unit tests
- [ ] File cache read/write/expiry tested with temp directories
- [ ] Install guidance and fallback wrapper tested
- [ ] CLI `detect:tools` output format validated as flat JSON array
- [ ] `npm test` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/0124-tool-detection-infrastructure/0124-02-SUMMARY.md`
</output>
