---
phase: 104-zero-friction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [FRIC-01, FRIC-02, FRIC-03]
user_setup: []

must_haves:
  truths:
    - "Commands execute without 'did you mean X?' clarification prompts when confidence >= 60%"
    - "Help text accurately reflects command routing - what help shows works on first try"
    - "Users can override with --exact flag to force exact matching"
    - "Context-based defaults apply when project state is clear"
  artifacts:
    - path: "src/lib/nl/conversational-planner.js"
      provides: "Goal parsing with bypassClarification option"
    - path: "src/lib/nl/help-fallback.js"
      provides: "Fallback suggestions with bypass option"
  key_links:
    - from: "command wrappers"
      to: "NL modules"
      via: "bypass flags"
      pattern: "bypassClarification.*true"
---

<objective>
Implement help-command alignment so that what help displays matches what command routing executes. This addresses the core pain point: commands typed exactly as shown in help should work on first try.

Purpose: Eliminate the "figure it out" phase where users must hunt through options or retry commands.
Output: Help text synchronized with command registry, validation system to detect drift.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 104 requirements)
@.planning/PROJECT.md (v11.1 milestone goals)
@.planning/phases/104-zero-friction/104-CONTEXT.md
@.planning/phases/103-direct-command-routing/103-01-ROUTE-MAP.md
@.planning/phases/103-direct-command-routing/103-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit help-routing alignment</name>
  <files>[]</files>
  <action>Audit all 41 slash commands in commands/ directory. For each command, verify:
1. The command path matches what help text displays
2. The init:* command it routes to is documented
3. No discrepancies between help output and actual routing
Create a findings document listing any misalignments found.</action>
  <verify>Run `node bin/bgsd-tools.cjs util:help --all 2>/dev/null | head -100` and verify help output matches command files</verify>
  <done>Audit complete with findings document showing all 41 commands and their routing status</done>
</task>

<task type="auto">
  <name>Task 2: Implement command registry validation</name>
  <files>[bin/bgsd-tools.cjs]</files>
  <action>Add validation in the CLI startup that compares:
1. Command files in commands/ directory
2. Help text generated from those commands
3. Init routes they connect to
Fail with actionable error if mismatch detected. This ensures CI catches drift before deployment.</action>
  <verify>Run validation and confirm it passes for all 41 commands</verify>
  <done>Validation runs on every CLI invocation and detects help-routing mismatches</done>
</task>

<task type="auto">
  <name>Task 3: Add exact-match override flag</name>
  <files>[bin/bgsd-tools.cjs]</files>
  <action>Add --exact flag support to the main CLI that bypasses fuzzy matching and requires exact command match. When --exact is provided:
1. Reject any command that doesn't match exactly
2. Show closest alternatives if no exact match
3. Log the exact-match attempt for learning system
This gives users explicit override capability per CONTEXT.md decisions.</action>
  <verify>Test: `node bin/bgsd-tools.cjs plan:phase --exact` should fail gracefully, `node bin/bgsd-tools.cjs plan phase --exact` should work</verify>
  <done>--exact flag works to force exact matching and reject fuzzy matches</done>
</task>

</tasks>

<verification>
- Run full test suite to ensure no regressions
- Verify 41 commands all pass validation
- Test --exact flag on ambiguous commands
</verification>

<success_criteria>
Help text accurately reflects command routing for all 41 commands. Validation catches any drift automatically. Users can override with --exact when needed.
</success_criteria>

<output>
After completion, create `.planning/phases/104-zero-friction/104-01-SUMMARY.md`
</output>
