---
phase: 104-zero-friction
plan: "02"
type: execute
wave: 2
depends_on: [104-01]
files_modified: []
autonomous: true
requirements: [FRIC-01, FRIC-02, FRIC-03]
user_setup: []

must_haves:
  truths:
    - "Commands with 60%+ confidence execute automatically without prompting"
    - "Ambiguous commands (< 60% confidence) prompt with top option + override"
    - "System learns from user choices to improve future context-based defaults"
  artifacts:
    - path: "src/lib/nl/conversational-planner.js"
      provides: "Confidence scoring with threshold-based auto-execution"
    - path: "src/lib/nl/help-fallback.js"
      provides: "Ambiguity resolution with user choice learning"
  key_links:
    - from: "NL parser"
      to: "confidence threshold"
      via: "60% cutoff logic"
      pattern: "confidence.*0\\.6"
---

<objective>
Implement threshold-based ambiguity handling: auto-execute when confident, prompt when ambiguous. This removes "did you mean X?" loops while preserving user control.

Purpose: Commands execute immediately when clear, prompt when genuinely ambiguous.
Output: Confidence scoring system with threshold-based auto-execution and learning capability.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 104 success criteria)
@.planning/phases/104-zero-friction/104-CONTEXT.md
@.planning/phases/104-zero-friction/104-01-SUMMARY.md
@.planning/phases/103-direct-command-routing/103-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement confidence threshold system</name>
  <files>[src/lib/nl/conversational-planner.js]</files>
  <action>Enhance the confidence scoring in conversational-planner.js:
1. Add confidence score (0-100) to each parsing result
2. Implement 60% threshold: >= 60% = auto-execute, < 60% = prompt user
3. Use bypassClarification flag from Phase 103 to bypass prompts when called from direct routing
4. Return confidence score in parse result for downstream decision-making
This implements FRIC-01 and FRIC-02 from the roadmap.</action>
  <verify>Test: Parse various commands and verify confidence scores are returned. Verify >=60% bypasses prompts, <60% triggers prompt</verify>
  <done>Confidence threshold system implemented with 60% cutoff for auto-execution</done>
</task>

<task type="auto">
  <name>Task 2: Add context-based default selection</name>
  <files>[src/lib/nl/conversational-planner.js]</files>
  <action>Implement context-aware default selection:
1. Read project state (current phase, milestone, recent commands) from STATE.md
2. Use context to boost confidence when command aligns with current context
3. For example: if current phase is 103, "exec phase" should have higher confidence for phase 103
4. Cache context for fast lookup during parsing
This implements FRIC-02 (context-based defaults) from CONTEXT.md.</action>
  <verify>Test: Set different phases in STATE.md and verify command confidence scores change accordingly</verify>
  <done>Context-based defaults boost confidence when commands align with current project state</done>
</task>

<task type="auto">
  <name>Task 3: Implement user choice learning</name>
<files>[src/lib/nl/help-fallback.js]</files>
  <action>Add learning capability:
1. When user overrides a suggestion, record the choice locally (config file)
2. On future ambiguous commands, boost confidence for user's previously chosen option
3. Provide command to clear learned preferences (for user control)
4. Store in local config only - never sent externally
This implements the learning portion of CONTEXT.md ambiguity handling decisions.</action>
  <verify>Test: Override a suggestion, verify it's remembered. Run clear-learned command, verify preferences reset</verify>
  <done>System learns from user overrides and improves future suggestions</done>
</task>

</tasks>

<verification>
- Run test suite to verify confidence scoring works
- Manually test context boosting with different project states
- Verify learning system records and recalls user preferences
</verification>

<success_criteria>
Commands execute automatically when confidence >= 60%. Ambiguous commands show top option with override. System learns from user choices to improve future defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/104-zero-friction/104-02-SUMMARY.md`
</output>
