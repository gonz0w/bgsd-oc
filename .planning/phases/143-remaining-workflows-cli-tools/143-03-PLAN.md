---
phase: 143-remaining-workflows-cli-tools
plan: 03
type: execute
wave: 1
depends_on:
  - 143-02
files_modified:
  - workflows/settings.md
  - src/lib/questions.js
autonomous: true
requirements:
  - MIGRATE-08
must_haves:
  truths:
    - settings.md uses questionTemplate() calls for all questions
    - Zero inline question text remains in settings.md
    - Conversation flow preserved (model selection, workflow toggles, branching strategy)
  artifacts:
    - Question templates added to src/lib/questions.js OPTION_TEMPLATES
    - settings.md updated with questionTemplate() calls
  key_links:
    - questionTemplate() in prompts.js calls getQuestionTemplate() in questions.js
    - Graceful fallback: if template missing, questionTemplate() generates runtime options
---

<objective>
Migrate settings.md to use questionTemplate() for all inline questions. settings.md has 7 inline questions in 2 question() arrays - one for present_settings (6 questions) and one for save_as_defaults (1 question).
</objective>

<execution_context>
@/Users/cam/DEV/bgsd-oc/workflows/settings.md
@/Users/cam/DEV/bgsd-oc/src/lib/questions.js
@/Users/cam/DEV/bgsd-oc/src/lib/prompts.js
</execution_context>

<context>
@.planning/phases/143-remaining-workflows-cli-tools/143-WORKFLOW-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Identify all inline questions in settings.md</name>
  <files>workflows/settings.md</files>
  <action>
From settings.md, identify all inline questions:

present_settings step (lines 38-96) - 6 questions:
1. "Which model profile for agents?" - Model, multiSelect: false, 3 options (Quality/Balanced/Budget)
2. "Spawn Plan Researcher?" - Research, multiSelect: false, 2 options (Yes/No)
3. "Spawn Plan Checker?" - Plan Check, multiSelect: false, 2 options (Yes/No)
4. "Spawn Execution Verifier?" - Verifier, multiSelect: false, 2 options (Yes/No)
5. "Auto-advance pipeline?" - Auto, multiSelect: false, 2 options (No/Yes)
6. "Git branching strategy?" - Branching, multiSelect: false, 3 options (None/Per Phase/Per Milestone)

save_as_defaults step (lines 126-136) - 1 question:
1. "Save these as default settings for all new projects?" - Defaults, multiSelect: false, 2 options (Yes/No)

For each, document: template ID, question text, options, multiSelect flag.
</action>
  <verify>All 7 inline questions identified with template IDs</verify>
  <done>List of 7 {templateId, questionText, options} for settings.md</done>
</task>

<task type="auto">
  <name>Add question templates to questions.js</name>
  <files>src/lib/questions.js</files>
  <action>
Add templates to OPTION_TEMPLATES for each identified question:

1. 'settings-model-profile': Model profile selection (SINGLE_CHOICE)
   options: Quality / Balanced (Recommended) / Budget

2. 'settings-plan-researcher': Plan researcher toggle (BINARY)
   options: Yes / No

3. 'settings-plan-checker': Plan checker toggle (BINARY)
   options: Yes / No

4. 'settings-execution-verifier': Execution verifier toggle (BINARY)
   options: Yes / No

5. 'settings-auto-advance': Auto-advance toggle (BINARY)
   options: No (Recommended) / Yes

6. 'settings-branching-strategy': Branching strategy selection (SINGLE_CHOICE)
   options: None (Recommended) / Per Phase / Per Milestone

7. 'settings-save-defaults': Save defaults confirmation (BINARY)
   options: Yes / No

Follow OPTION_RULES: MIN 3, MAX 5 options, diversity, escape hatch "Something else" at last position.
Note: Some have only 2 options - use BINARY type which allows 2 options per TAXONOMY rules.
</action>
  <verify>grep -c "settings-" src/lib/questions.js returns 7</verify>
  <done>7 question templates added to OPTION_TEMPLATES</done>
</task>

<task type="auto">
  <name>Update settings.md to use questionTemplate() calls</name>
  <files>workflows/settings.md</files>
  <action>
Replace inline question structures in present_settings step (lines 38-96) with questionTemplate() calls:

For each question, replace:
```
question([
  {
    question: "Which model profile for agents?",
    header: "Model",
    multiSelect: false,
    options: [
      { label: "Quality", description: "..." },
      ...
    ]
  }
])
```

With:
```
let qt = questionTemplate('settings-model-profile', 'SINGLE_CHOICE');
question([{
  question: "Which model profile for agents?",
  header: "Model",
  multiSelect: false,
  options: qt.options
}])
```

Do NOT inline the question text - question TEXT stays inline, only OPTIONS come from templates.

Preserve all question text, headers, and multiSelect flags.
</action>
  <verify>grep "questionTemplate" workflows/settings.md | wc -l returns 7+</verify>
  <done>settings.md uses questionTemplate() for all questions, zero inline question([) remains</done>
</task>

</tasks>

<verification>
- Run: node bin/bgsd-tools.cjs verify:validate plan-structure .planning/phases/143-remaining-workflows-cli-tools/143-03-PLAN.md
- Verify: grep -c "questionTemplate" workflows/settings.md returns 7+
- Verify: grep -c "question(\[" workflows/settings.md returns 0
- Verify: All question text preserved (headers, multiSelect flags intact)
</verification>

<success_criteria>
- [ ] All 7 question templates added to questions.js OPTION_TEMPLATES
- [ ] settings.md has 7+ questionTemplate() calls
- [ ] Zero question([ inline arrays remain in settings.md
- [ ] All question text, headers, multiSelect flags preserved
- [ ] Conversation flow (model selection, workflow toggles, branching) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/143-remaining-workflows-cli-tools/143-03-SUMMARY.md`
</output>
