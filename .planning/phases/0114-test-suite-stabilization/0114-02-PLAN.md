---
phase: 0114-test-suite-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - 0114-01
files_modified:
  - tests/infra.test.cjs
  - tests/env.test.cjs
  - tests/verify.test.cjs
  - tests/codebase.test.cjs
  - tests/plugin.test.cjs
  - tests/misc.test.cjs
  - tests/integration.test.cjs
  - tests/init.test.cjs
  - tests/state.test.cjs
  - tests/plan.test.cjs
  - tests/contracts.test.cjs
  - tests/memory.test.cjs
  - tests/intent.test.cjs
  - tests/trajectory.test.cjs
  - tests/worktree.test.cjs
  - tests/agent.test.cjs
  - tests/orchestration.test.cjs
  - tests/decisions.test.cjs
  - tests/enricher-decisions.test.cjs
  - tests/summary-generate.test.cjs
  - tests/plugin-advisory-guardrails.test.cjs
  - bin/bgsd-tools.cjs
  - src/router.js
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "User can run `npm test` and see zero test failures across all 21 test files"
    - "Plugin tests pass when run in isolation (no project context dependency)"
    - "Config migration test assertions match current CLI output schema"
    - "Infrastructure tests match the actual structure of the built CLI (no stale string checks)"
    - "Env edge case tests (permissions, symlinks, manifests) all pass"
  artifacts:
    - path: "tests/infra.test.cjs"
      provides: "Updated infrastructure assertions matching minified build"
    - path: "tests/env.test.cjs"
      provides: "Fixed edge case tests for permissions, symlinks, manifest"
  key_links:
    - from: "tests/infra.test.cjs"
      to: "bin/bgsd-tools.cjs"
      via: "Source string inspection tests"
      pattern: "readFileSync.*TOOLS_PATH"
    - from: "tests/env.test.cjs"
      to: "bin/bgsd-tools.cjs"
      via: "CLI invocation for env scan"
      pattern: "runGsdTools.*env scan"
---

<objective>
Fix all remaining test failures after the banner fix — targeting zero failures across the entire suite.

Purpose: After Plan 01 eliminates ~530 banner failures, ~70 residual failures remain from stale assertions, missing modules, plugin isolation issues, config migration mismatches, and env edge cases. This plan fixes every one of them to achieve the phase goal: absolute zero test failures.
Output: All 21 test files passing, `npm test` fully green
</objective>

<execution_context>
@/home/cam/.config/oc/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/oc/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0114-test-suite-stabilization/0114-CONTEXT.md
@.planning/phases/0114-test-suite-stabilization/0114-01-SUMMARY.md
@tests/infra.test.cjs
@tests/env.test.cjs
@tests/verify.test.cjs
@tests/helpers.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage remaining failures and fix by root cause</name>
  <files>tests/infra.test.cjs, tests/env.test.cjs, tests/codebase.test.cjs, tests/verify.test.cjs, tests/plugin.test.cjs, tests/misc.test.cjs, tests/integration.test.cjs, tests/init.test.cjs, tests/state.test.cjs, tests/plan.test.cjs, tests/contracts.test.cjs, tests/memory.test.cjs, tests/intent.test.cjs, tests/trajectory.test.cjs, tests/worktree.test.cjs, tests/agent.test.cjs, tests/orchestration.test.cjs, tests/decisions.test.cjs, tests/enricher-decisions.test.cjs, tests/summary-generate.test.cjs, tests/plugin-advisory-guardrails.test.cjs</files>
  <action>
After Plan 01's banner fix and rebuild, run `npm test` and capture the full output. Group remaining failures by root cause. The expected residual categories are:

**Category A: Stale infrastructure assertions in `tests/infra.test.cjs`**
- `_tmpFiles tracking is wired into output pipeline` (line ~204): Test checks for `_tmpFiles.push` inside a `function outputJSON(` section of the built CLI. The build is minified — these literal strings don't exist. Fix: Rewrite the test to verify the behavior rather than the source code. Option 1: Check that `outputJSON` or `_tmpFiles` appears anywhere in the source (minifier may preserve identifiers). Option 2: Delete the source-inspection test and keep only the behavioral test at line ~217 (`no temp files remain after CLI invocation`) which already verifies the same thing. Per user decision: "Delete tests for dead/removed features."
- `bgsd-tools.cjs should track temp files` (line ~198): Same issue — checking for `_tmpFiles` literal in minified source. Fix same way.
- `COMMAND_HELP covers major commands` (line ~279): Test parses help output incorrectly — it looks for a single line of comma-separated commands. The actual help output format may have changed. Fix: Update the parsing logic to match current help output format, or relax the assertion.
- `help text does not contaminate stdout` (line ~270): The banner was printing to stdout with --help. After Plan 01's TTY guard, this should be fixed. Verify — if still failing, investigate.

**Category B: Env test failures in `tests/env.test.cjs`**
- `permission-denied directory does not crash` (line ~349): `envScanParsed()` returns `{ success: false }` because the env scan fails with banner JSON parse error. After banner fix, this should pass. If still failing, the permission test itself may have an issue (e.g., running as root).
- `deeply nested symlink does not cause infinite loop` (line ~360): Same — likely fixed by banner fix. Verify.
- `env scan does NOT write manifest when .planning/ does not exist` (line ~430): Env scan JSON parse failure from banner. After banner fix, verify.

**Category C: Any remaining JSON parse failures**
After the banner fix, some tests may still fail if there are OTHER sources of stdout pollution. Run `npm test 2>&1 | grep "SyntaxError"` to check. If any remain, trace them to their source and fix.

**Category D: Stale assertion values**
Any test where the expected output format changed but tests weren't updated. Per user decision: "CLI output is the source of truth — update tests to match current CLI behavior."

**Category E: Plugin isolation issues (TEST-03)**
Check `tests/plugin.test.cjs` and `tests/plugin-advisory-guardrails.test.cjs`. If they fail because they can't find plugin context files, add graceful fallbacks or mock the missing context.

**Category F: Config migration tests (TEST-05)**
Check config-migrate tests in `tests/infra.test.cjs`. If expected migration outputs don't match current schema, update the expected values.

For each failure: read the test, understand what it expects, check what the CLI actually outputs, and either update the test to match current behavior or fix the CLI if it's genuinely broken.

If a test failure reveals a genuine CLI bug, fix the bug (per user decision).
If a test is for a removed feature, delete it.
If a test assertion is stale, update it to match current CLI output.
  </action>
  <verify>Run `npm test 2>&1 | tail -10` and confirm zero failures. Run `npm test 2>&1 | grep "✖"` and confirm no output.</verify>
  <done>All remaining test failures fixed. `npm test` shows 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification — full green suite</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
1. Run `npm run build` to ensure the CLI is rebuilt with any source fixes from Task 1.
2. Run `npm test` end-to-end and capture the full summary.
3. Verify: `fail 0` in the output.
4. If any tests still fail, fix them — repeat until zero.
5. Record the final test count (should be ~1010-1014 depending on deleted dead tests).

Per user decision: "Target absolute zero failures — every single test must pass, no exceptions."
  </action>
  <verify>Run `npm test` and confirm the summary line shows `fail 0`. Run `npm test 2>&1 | grep "✖"` and confirm empty output.</verify>
  <done>`npm test` produces `fail 0`. Every test in the suite passes. The test suite provides reliable signal for all subsequent changes.</done>
</task>

</tasks>

<verification>
- `npm test` exits with `fail 0`
- `npm test 2>&1 | grep "✖"` produces no output
- No SyntaxError, no MODULE_NOT_FOUND, no ENOENT errors in test output
- Total test count is documented (may differ from 1014 if dead tests deleted)
</verification>

<success_criteria>
- Zero test failures across all 21 test files
- Plugin tests pass in isolation
- Config migration tests match current schema
- Infrastructure tests match actual CLI build structure
- Env edge case tests all pass
- Final test count documented
</success_criteria>

<output>
After completion, create `.planning/phases/0114-test-suite-stabilization/0114-02-SUMMARY.md`
</output>
