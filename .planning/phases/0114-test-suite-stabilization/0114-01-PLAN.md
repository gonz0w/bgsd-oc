---
phase: 0114-test-suite-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/router.js
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Running `npm test` produces zero SyntaxError failures caused by '[bGSD] Running with Bun' banner polluting stdout"
    - "CLI commands that return JSON produce clean JSON on stdout with no banner prefix"
    - "Tests referencing src/lib/profiler.js no longer fail with MODULE_NOT_FOUND"
  artifacts:
    - path: "src/router.js"
      provides: "Banner suppression in piped/non-TTY mode"
      contains: "isTTY"
    - path: "bin/bgsd-tools.cjs"
      provides: "Rebuilt CLI bundle with banner fix"
  key_links:
    - from: "src/router.js"
      to: "bin/bgsd-tools.cjs"
      via: "npm run build"
      pattern: "isTTY"
---

<objective>
Eliminate the ~526 Bun runtime banner failures and the ~4 missing profiler module failures by fixing the banner at source and handling the dead profiler reference.

Purpose: The banner fix alone resolves ~88% of all 600 test failures. This unblocks accurate triage of the remaining ~70 residual failures in Plan 02.
Output: Patched src/router.js, rebuilt bin/bgsd-tools.cjs, profiler tests fixed
</objective>

<execution_context>
@/home/cam/.config/oc/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/oc/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0114-test-suite-stabilization/0114-CONTEXT.md
@src/router.js
@tests/helpers.cjs
@tests/infra.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress Bun runtime banner in non-TTY mode</name>
  <files>src/router.js</files>
  <action>
In `src/router.js`, modify the `showRuntimeBanner()` function (around line 50-70) to exit early when stdout is not a TTY. Add `if (!process.stdout.isTTY) return;` as the first line of the function body. This ensures the banner never prints when stdout is piped (which is how all tests invoke the CLI via `execSync`).

Per user decision: "Suppress the banner at source — remove stdout output entirely (or guard with isTTY)". The isTTY guard is the right approach because it preserves the banner for interactive use while silencing it for programmatic consumption.

Also verify the call site at line ~173-176 (`if (showBanner) { showRuntimeBanner(...) }`) — no changes needed there since the guard is inside the function.
  </action>
  <verify>Run `node src/router.js util:current-timestamp 2>/dev/null` and confirm stdout contains only JSON (no `[bGSD]` prefix). Then run `node src/router.js util:current-timestamp` in a terminal and confirm the banner still appears for interactive use (optional — TTY detection in CI may differ).</verify>
  <done>showRuntimeBanner() returns early when process.stdout.isTTY is falsy. No banner text appears on stdout in piped mode.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild CLI bundle and fix profiler tests</name>
  <files>bin/bgsd-tools.cjs, tests/infra.test.cjs</files>
  <action>
1. Run `npm run build` to rebuild `bin/bgsd-tools.cjs` with the banner fix from Task 1.

2. Fix the profiler tests in `tests/infra.test.cjs` (around line 876-981). The `src/lib/profiler.js` module does not exist — it was removed. Per user decision: "fix the import path if the module moved, delete the test if the module was intentionally removed."

   - The `profiler` describe block has 4 tests: `profiler disabled by default`, `profiler enabled writes baseline`, `profiler baseline JSON structure`, and `profiler zero-cost when disabled`.
   - Since `src/lib/profiler.js` doesn't exist and there's no profiler module anywhere in `src/lib/`, delete the entire `describe('profiler', ...)` block (lines ~876-982).
   - Also check `tests/codebase.test.cjs` around line 1064 which references `src/lib/profiler.js` as a test fixture file path for codebase context — update that test to use a file that actually exists (e.g., `src/lib/cache.js` or `src/lib/debug.js`).

3. Verify the build succeeded and the rebuilt CLI doesn't emit the banner when piped.
  </action>
  <verify>Run `npm run build` (must exit 0). Run `node bin/bgsd-tools.cjs util:current-timestamp 2>/dev/null | node -e "JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'))"` to confirm clean JSON output. Run `npm test -- tests/infra.test.cjs 2>&1 | grep profiler` and confirm no profiler test failures (tests should be gone). Run a quick `npm test 2>&1 | tail -10` to get the new pass/fail counts.</verify>
  <done>bin/bgsd-tools.cjs rebuilt with banner fix. Profiler tests removed (dead module). Test pass count increases by ~530+ (from 414 to ~940+).</done>
</task>

</tasks>

<verification>
- `npm test 2>&1 | grep "SyntaxError.*bGSD"` returns zero matches
- `node bin/bgsd-tools.cjs util:current-timestamp 2>/dev/null` outputs clean JSON
- `npm test 2>&1 | grep "profiler"` shows no failures
- Test pass count is ~940+ (up from 414)
</verification>

<success_criteria>
- Zero test failures caused by Bun runtime banner stdout pollution
- Zero test failures caused by missing profiler module
- bin/bgsd-tools.cjs rebuilt and passing JSON parse tests
- Remaining failures are small enough to categorize and fix individually in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/0114-test-suite-stabilization/0114-01-SUMMARY.md`
</output>
