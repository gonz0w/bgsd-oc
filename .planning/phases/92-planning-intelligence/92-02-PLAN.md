---
phase: 92-planning-intelligence
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03

must_haves:
  truths:
    - CLI commands exist for dependency analysis
    - Task sizing estimates available via CLI
    - Parallelization warnings in output
  artifacts:
    - New CLI subcommands in bgsd-tools
  key_links:
    - Commands invoked by planner during validation
    - Commands invoked by execute-phase for wave analysis
---

<objective>
Add CLI commands to bgsd-tools that implement automatic dependency detection, task sizing, and parallelization analysis.

Purpose: Provide tools that help the planner automatically analyze plans and suggest improvements.

Output: New CLI commands in bgsd-tools.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-planning-intelligence/92-CONTEXT.md
@/home/cam/.config/opencode/agents/bgsd-planner.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add util:analyze-deps command</name>
  <files>
    - bin/bgsd-tools.cjs
  </files>
  <action>
Add new CLI command `util:analyze-deps` to bgsd-tools.cjs:

1. Register command: "util:analyze-deps" 
2. Implementation:
   - Takes phase directory as argument
   - Reads all PLAN.md files in phase
   - Extracts files_modified from each plan
   - Analyzes file overlaps between plans
   - For each overlap, suggests dependency with confidence score
   - Confidence: 100% if file written in plan A and read in plan B, lower for patterns

3. Output format:
   ```json
   {
     "phase": "92",
     "suggestions": [
       {
         "from": "01",
         "to": "02", 
         "file": "src/models/user.ts",
         "confidence": 100,
         "reason": "Plan 01 writes, Plan 02 reads"
       }
     ]
   }
   ```

4. Add to CLI help text

Location: Find similar util commands and add nearby. Search for "util:" commands pattern.
  </action>
  <verify>
node bin/bgsd-tools.cjs util:analyze-deps .planning/phases/92-planning-intelligence 2>&1 | head -20
</verify>
  <done>
Command exists and outputs dependency suggestions with confidence scores.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add util:estimate-scope command</name>
  <files>
    - bin/bgsd-tools.cjs
  </files>
  <action>
Add new CLI command `util:estimate-scope` to bgsd-tools.cjs:

1. Register command: "util:estimate-scope"
2. Implementation:
   - Takes plan file path as argument  
   - Reads PLAN.md, extracts tasks and files_modified
   - Estimates complexity based on:
     - Number of tasks (3+ = higher)
     - File count (5+ = higher)
     - Cross-subsystem patterns (api + models + ui = very high)
   - Estimates context impact using formula: base * complexity_factor
   - Compares to 50% budget target

3. Output format:
   ```json
   {
     "plan": "92-01",
     "tasks": 3,
     "files": 8,
     "complexity": "high",
     "context_estimate": "45%",
     "within_budget": true,
     "recommendations": ["Consider splitting if >50%"]
   }
   ```

4. Add to CLI help text
  </action>
<verify>
node bin/bgsd-tools.cjs util:estimate-scope .planning/phases/92-planning-intelligence/92-01-PLAN.md 2>&1 | head -20
</verify>
  <done>
Command exists and outputs scope estimates with budget analysis.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance verify:plan-wave with parallelization warnings</name>
  <files>
    - bin/bgsd-tools.cjs
  </files>
  <action>
Enhance existing `verify:plan-wave` command with better parallelization analysis:

1. Current command already detects file conflicts within waves
2. Enhance output to include:
   - Clear "parallelization warnings" section
   - List of plans that CANNOT run in parallel
   - Specific file conflicts causing serialization

3. Add to output:
   ```json
   {
     "phase": "92",
     "waves": {...},
     "conflicts": [...],
     "parallelization_warnings": [
       {
         "wave": 1,
         "reason": "Plans 01 and 02 both modify bin/bgsd-tools.cjs",
         "recommendation": "Run sequentially or merge into single plan"
       }
     ],
     "safe_to_parallelize": ["01", "02", "03"]
   }
   ```

4. Enhance the human-readable output with color-coded warnings
  </action>
<verify>
node bin/bgsd-tools.cjs verify:plan-wave .planning/phases/92-planning-intelligence 2>&1 | grep -i "warning\|conflict\|parallel"
</verify>
  <done>
Command enhanced with parallelization warnings in output.
  </done>
</task>

</tasks>

<verification>
- All three CLI commands exist and function
- analyze-deps shows dependency suggestions
- estimate-scope shows scope estimates  
- plan-wave shows parallelization warnings
</verification>

<success_criteria>
CLI commands implemented:
- util:analyze-deps: Dependency detection with confidence scores
- util:estimate-scope: Task sizing estimates with budget analysis
- verify:plan-wave: Enhanced with parallelization warnings
</success_criteria>

<output>
After completion, create `.planning/phases/92-planning-intelligence/92-02-SUMMARY.md`
</output>
