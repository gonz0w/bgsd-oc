---
phase: 141-taxonomy-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/decision-rules.js
autonomous: true
requirements:
  - TAX-04
  - TAX-05
user_setup: []

must_haves:
  truths:
    - resolveQuestionType(workflow, step) follows existing pure-function pattern: (state) => { value, confidence, rule_id }
    - resolveOptionGeneration(questionType, context) follows existing pure-function pattern
    - Both functions registered in DECISION_REGISTRY with category 'question-routing'
    - Functions run at execution time (not pre-computed at enrichment)
    - Returns HIGH confidence for all cases
  artifacts:
    - Two new decision functions added to decision-rules.js
    - DECISION_REGISTRY updated with new entries
  key_links:
    - resolveQuestionType provides routing to TAXONOMY types
    - resolveOptionGeneration determines pre-authored vs runtime generation
---

<objective>
Add resolveQuestionType and resolveOptionGeneration decision functions to DECISION_REGISTRY. These enable workflow step routing to correct question types and option generation strategies.
</objective>

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/141-taxonomy-infrastructure/141-CONTEXT.md
@.planning/phases/141-taxonomy-infrastructure/141-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/lib/decision-rules.js
@src/lib/questions.js
</context>

<tasks>

<task type="auto">
  <name>Add resolveQuestionType and resolveOptionGeneration to decision-rules.js</name>
  <files>src/lib/decision-rules.js</files>
  <action>
Add to decision-rules.js:
1. resolveQuestionType(state) function:
   - Inputs: workflow_id, step_id
   - Uses questions.js TAXONOMY to determine type
   - Returns { value: TAXONOMY type, confidence: 'HIGH', rule_id: 'resolve-question-type' }
   - Default to SINGLE_CHOICE if workflow/step not recognized (graceful fallback)

2. resolveOptionGeneration(questionType, context) function:
   - Inputs: questionType (from TAXONOMY), context (optional additional info)
   - Returns { value: 'pre-authored' | 'runtime', confidence: 'HIGH', rule_id: 'resolve-option-generation' }
   - 'pre-authored' when template exists in OPTION_TEMPLATES for known question types
   - 'runtime' for edge cases or when pre-authored template not available
   - Uses questions.js generateRuntimeOptions as fallback

3. Add to DECISION_REGISTRY array:
   {
     id: 'resolve-question-type',
     name: 'Question Type Resolution',
     category: 'question-routing',
     description: 'Determines TAXONOMY type for a workflow step',
     inputs: ['workflow_id', 'step_id'],
     outputs: ['TAXONOMY enum value'],
     confidence_range: ['HIGH'],
     resolve: resolveQuestionType,
   },
   {
     id: 'resolve-option-generation',
     name: 'Option Generation Strategy',
     category: 'question-routing',
     description: 'Determines pre-authored vs runtime generation approach',
     inputs: ['questionType', 'context'],
     outputs: ['pre-authored' | 'runtime'],
     confidence_range: ['HIGH'],
     resolve: resolveOptionGeneration,
   }

4. Add exports for the new functions
  </action>
  <verify>
node -e "const dr = require('./src/lib/decision-rules.js'); console.log('resolveQuestionType exists:', typeof dr.resolveQuestionType === 'function'); console.log('resolveOptionGeneration exists:', typeof dr.resolveOptionGeneration === 'function'); const result1 = dr.resolveQuestionType({ workflow_id: 'discuss-phase', step_id: 'area-select' }); console.log('resolveQuestionType result:', JSON.stringify(result1)); const result2 = dr.resolveOptionGeneration({ questionType: 'SINGLE_CHOICE', context: {} }); console.log('resolveOptionGeneration result:', JSON.stringify(result2));"
  </verify>
  <done>
- resolveQuestionType(workflow, step) returns { value, confidence, rule_id }
- resolveOptionGeneration(questionType, context) returns { value, confidence, rule_id }
- Both registered in DECISION_REGISTRY with category 'question-routing'
- Functions exported from decision-rules.js
- Returns HIGH confidence
</done>
</task>

</tasks>

<verification>
Decision functions exist, follow pure-function pattern, registered in DECISION_REGISTRY with 'question-routing' category.
</verification>

<success_criteria>
- resolveQuestionType callable and returns expected structure
- resolveOptionGeneration callable and returns expected structure
- Both entries exist in DECISION_REGISTRY array
- Functions exported
</success_criteria>

<output>
After completion, create `.planning/phases/141-taxonomy-infrastructure/141-03-SUMMARY.md`
</output>
