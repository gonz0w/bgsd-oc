---
phase: 54-command-consolidation
plan: 03
type: execute
wave: 2
depends_on: [54-01]
files_modified: [workflows/*.md, commands/*.md]
autonomous: true
requirements: [CMD-02]

must_haves:
  truths:
    - "All workflow references use namespace:command format"
    - "Slash command files renamed to bgsd-*.md"
    - "No old flat command names remain in workflows or commands/"
  artifacts:
    - path: "workflows/*.md"
      contains: "plan:intent"
    - path: "commands/bgsd-*.md"
      contains: "plan:intent"
  key_links: []
---

<objective>
Update all workflow and slash command references to use new namespaced command names.
</objective>

<execution_context>
@/home/cam/.config/oc/get-shit-done/workflows/execute-plan.md
@/home/cam/.config/oc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-command-consolidation/54-CONTEXT.md
@.planning/phases/54-command-consolidation/54-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Update workflow references</name>
  <files>workflows/*.md</files>
  <action>
1. List all workflows/*.md files
2. For each workflow, grep for gsd-tools.cjs invocations (node ...gsd-tools.cjs ...)
3. Update command names from flat to namespaced:
   - intent → plan:intent
   - requirements → plan:requirements
   - roadmap → plan:roadmap
   - phase → plan:phase
   - commit → execute:commit
   - verify → verify:verify
   - state → verify:state
   - etc.
4. Verify no old command names remain: grep -r "gsd-tools.cjs intent" workflows/
  </action>
  <verify>grep -r "gsd-tools.cjs [a-z]* " workflows/ returns no matches (only namespaced)</verify>
  <done>All workflow references use namespace:command format</done>
</task>

<task type="auto">
  <name>Update slash command files</name>
  <files>commands/*.md</files>
  <action>
1. List all commands/*.md files
2. Rename files from gsd-*.md to bgsd-*.md (unbranding per CONTEXT.md)
3. Update command references inside each file from flat to namespaced
4. Verify: node bin/gsd-tools.cjs --help shows new command names work
  </action>
  <verify>All slash commands use bgsd- prefix and reference namespaced commands</verify>
  <done>Slash command files renamed and updated</done>
</task>

</tasks>

<verification>
- [ ] No flat command names in workflows/*.md
- [ ] commands/*.md renamed to bgsd-*.md
- [ ] All command references in workflows use namespace:command
- [ ] No references to old flat command names anywhere in workflows or commands/
</verification>

<success_criteria>
All workflow and command file references updated to namespaced commands.
</success_criteria>

<output>
After completion, create `.planning/phases/54-command-consolidation/54-03-SUMMARY.md`
</output>
