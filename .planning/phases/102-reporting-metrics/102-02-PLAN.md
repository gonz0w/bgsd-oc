---
phase: 102-reporting-metrics
plan: 02
type: execute
wave: 2
depends_on: [102-01]
files_modified: []
autonomous: true
requirements: [VIS-08]
user_setup: []

must_haves:
  truths:
    - "User can view computed velocity metrics showing tasks completed per session"
    - "Velocity metrics include tasks per session, average duration, trend indicator"
    - "Velocity sparkline visualization displays with session output"
    - "On-demand velocity command accessible via /bgsd util velocity"
  artifacts:
    - path: "src/lib/reports/velocity-metrics.js"
      provides: "Velocity metrics computation and display"
      exports: ["calculateVelocityMetrics", "renderVelocityReport", "getVelocityTrend"]
    - path: "src/lib/viz/index.js"
      provides: "Export velocity metrics module"
      contains: "velocity-metrics"
    - path: "src/commands/milestone.js"
      provides: "milestone summary CLI command"
      contains: "summary"
    - path: "src/commands/velocity.js" (or add to features.js)
      provides: "velocity CLI command"
      contains: "velocity"
  key_links:
    - from: "src/lib/reports/velocity-metrics.js"
      to: "src/lib/viz/sparkline.js"
      via: "calculateVelocityTrend, renderSparkline"
    - from: "src/lib/reports/velocity-metrics.js"
      to: "src/lib/format.js"
      via: "formatTable, sectionHeader"
    - from: "src/commands/velocity.js"
      to: "src/lib/reports/velocity-metrics.js"
      via: "calculateVelocityMetrics, renderVelocityReport"
---

<objective>
Create velocity metrics computation module and add CLI commands for on-demand access
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-rich-visualization/101-02-SUMMARY.md (sparkline.js)
@src/lib/viz/sparkline.js (existing sparkline for VIS-05)
@src/lib/format.js (table rendering)
@src/lib/reports/milestone-summary.js (from 102-01)
</context>

<tasks>

<task type="auto">
  <name>Create velocity metrics computation module</name>
  <files>src/lib/reports/velocity-metrics.js</files>
  <action>
Create src/lib/reports/velocity-metrics.js with:
- calculateVelocityMetrics(sessionHistory) - Compute velocity from session data
  - Input: Array of { date, tasksCompleted, duration (minutes), phase } objects
  - Output: { avgTasksPerSession, avgDuration, trend, trendDirection, totalSessions, totalTasks, sparklineData }
- renderVelocityReport(metrics, options) - Format velocity report for display
  - options: { format: 'console' | 'json', showSparkline: boolean }
  - Include trend indicator (improving/stable/declining)
  - Use sparkline.js from VIS-05 for trend visualization
- getVelocityTrend(velocityHistory) - Calculate trend direction
  - Compare recent sessions vs earlier sessions
  - Return: 'improving' | 'stable' | 'declining'

Data sources:
- .planning/STATE.md - Session history from performance metrics section
- Git history - Commits to .planning/ directory with timestamps
- Phase SUMMARY.md files - Task completion data

Velocity calculation:
- tasks per session = tasksCompleted / session count
- average duration = sum(duration) / session count
- trend = linear regression on last N sessions, or compare last 3 vs previous 3
  </action>
  <verify>File created at src/lib/reports/velocity-metrics.js with all exports</verify>
  <done>Velocity metrics module exists with calculateVelocityMetrics, renderVelocityReport, getVelocityTrend exports</done>
</task>

<task type="auto">
  <name>Export velocity metrics from viz index</name>
  <files>src/lib/viz/index.js</files>
  <action>
Add velocity-metrics export to src/lib/viz/index.js:
- Require('./reports/velocity-metrics')
- Export velocityMetrics object with key functions
  </action>
  <verify>grep -n "velocity-metrics" src/lib/viz/index.js</verify>
  <done>Velocity metrics module exported from viz index</done>
</task>

<task type="auto">
  <name>Add milestone summary CLI command</name>
  <files>src/commands/milestone.js</files>
  <action>
Add milestone summary command to src/commands/milestone.js:
- Add 'summary' subcommand to milestone namespace
- Usage: /bgsd milestone summary [version]
- Options:
  - version: Optional milestone version (default: current milestone)
  - --format json: JSON output instead of console
  - --save: Save to file
  - --file <path>: Save to specific path
- Uses milestone-summary.js from 102-01
- Output formatted report to console or file
  </action>
  <verify>grep -n "summary" src/commands/milestone.js | head -5</verify>
  <done>Milestone summary command added to milestone.js</done>
</task>

<task type="auto">
  <name>Add velocity CLI command</name>
  <files>src/commands/features.js</files>
  <action>
Add velocity command to util namespace (or enhance existing):
- Add 'velocity' subcommand to util namespace
- Usage: /bgsd util velocity
- Options:
  - --sessions N: Show last N sessions (default: 10)
  - --format json: JSON output instead of console
  - --sparkline: Show inline sparkline (default: true for console)
- Uses velocity-metrics.js module
- Displays: tasks/session, avg duration, trend, sparkline
  </action>
  <verify>grep -n "velocity" src/commands/features.js | head -10</verify>
  <done>Velocity command accessible via /bgsd util velocity</done>
</task>

</tasks>

<verification>
- Velocity metrics module computes accurate data from session history
- Sparkline visualization displays correctly (using VIS-05)
- Both CLI commands accessible and functional
- Output matches existing bGSD formatting conventions
</verification>

<success_criteria>
VIS-08: User can view computed velocity metrics showing tasks completed per session
</success_criteria>

<output>
After completion, create `.planning/phases/102-reporting-metrics/102-02-SUMMARY.md`
</output>
