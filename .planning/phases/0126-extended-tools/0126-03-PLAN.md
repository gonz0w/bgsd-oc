---
phase: 0126-extended-tools
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - tests/cli-tools-integration.test.cjs
autonomous: true
requirements: [TOOL-04, TOOL-05, TOOL-06]

must_haves:
  truths:
    - "Integration tests verify yq parseYAML works correctly for docker-compose and pnpm-workspace YAML"
    - "Integration tests verify bat catWithHighlight returns content with and without bat installed"
    - "Integration tests verify isGhUsable blocks exactly 2.88.0 and allows 2.88.1, 2.87.0, 2.89.0"
    - "Integration tests verify detect:gh-preflight returns structured JSON with usable/authenticated/version/errors"
    - "Config toggle tests confirm tools_yq=false, tools_bat=false, tools_gh=false cause tools to be skipped"
    - "All integration tests pass regardless of which CLI tools are installed on the test machine"
  artifacts:
    - path: "tests/cli-tools-integration.test.cjs"
      provides: "Extended integration tests for Phase 126 tool integrations"
      contains: "isGhUsable"
  key_links:
    - from: "tests/cli-tools-integration.test.cjs"
      to: "src/lib/cli-tools/gh.js"
      via: "require('../src/lib/cli-tools')"
      pattern: "isGhUsable"
    - from: "tests/cli-tools-integration.test.cjs"
      to: "src/lib/cli-tools/yq.js"
      via: "require('../src/lib/cli-tools')"
      pattern: "parseYAML"
    - from: "tests/cli-tools-integration.test.cjs"
      to: "src/lib/cli-tools/bat.js"
      via: "require('../src/lib/cli-tools')"
      pattern: "catWithHighlight"
---

<objective>
Create integration tests validating all Phase 126 tool integrations — yq YAML parsing, bat syntax highlighting, gh version blocklist, and preflight validation.

Purpose: Verify that yq, bat, and gh integrations produce correct results regardless of tool availability, that config toggles work for all three tools, that the gh 2.88.0 version blocklist is exact (not blocking 2.88.1+), and that detect:gh-preflight returns structured output. This ensures Phase 126 requirements TOOL-04, TOOL-05, and TOOL-06 are all verified.

Output: Extended integration tests (30+ new tests) added to the existing cli-tools-integration.test.cjs file.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0126-extended-tools/0126-CONTEXT.md
@.planning/phases/0126-extended-tools/0126-01-SUMMARY.md
@.planning/phases/0126-extended-tools/0126-02-SUMMARY.md
@tests/cli-tools-integration.test.cjs
@tests/cli-tools.test.cjs
@src/lib/cli-tools/yq.js
@src/lib/cli-tools/bat.js
@src/lib/cli-tools/gh.js
@src/lib/cli-tools/fallback.js
@src/lib/constants.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add yq and bat integration tests</name>
  <files>tests/cli-tools-integration.test.cjs</files>
  <action>
Add new test sections to the existing `tests/cli-tools-integration.test.cjs` file (this file already has Phase 125 integration tests — add Phase 126 tests after the existing ones).

**Config toggle tests for yq, bat, gh (6 tests):**
1. CONFIG_SCHEMA has `tools_yq` entry with type 'boolean' and default true
2. CONFIG_SCHEMA has `tools_bat` entry with type 'boolean' and default true
3. CONFIG_SCHEMA has `tools_gh` entry with type 'boolean' and default true
4. CONFIG_SCHEMA tools_yq has nested `{ section: 'tools', field: 'yq' }`
5. CONFIG_SCHEMA tools_bat has nested `{ section: 'tools', field: 'bat' }`
6. CONFIG_SCHEMA tools_gh has nested `{ section: 'tools', field: 'gh' }`

**yq integration tests (8 tests):**
1. `parseYAML('key: value')` returns `{ success: true, result }` shape with result containing the parsed key
2. `parseYAML` with multi-line YAML (key: value pairs) returns object with all keys
3. `parseYAML` with list YAML (`- item1\n- item2`) handles arrays
4. `parseYAML` with empty input returns graceful result (not crash)
5. `parseYAML` with invalid YAML returns `{ success: false }` or `{ result: null }`
6. `transformYAML(yamlStr, '.key')` returns the value at key (basic expression)
7. `YAMLtoJSON('key: value')` returns JSON string representation
8. `isYqAvailable()` returns boolean (not null/undefined)

**bat integration tests (8 tests):**
1. `catWithHighlight(filePath)` returns `{ success: true, result }` shape for existing file
2. `catWithHighlight` result contains file content (verify by checking known content in a test fixture or this test file itself)
3. `catWithHighlight` with `language: 'diff'` option returns content
4. `catWithHighlight` with `style: 'numbers,grid'` option returns content
5. `catWithHighlight` for non-existent file returns `{ success: false }` with error
6. `getLanguage('test.js')` returns `{ success: true, result }` with 'javascript' or 'auto'
7. `isBatAvailable()` returns boolean (not null/undefined)
8. `getStylePresets()` returns object with known preset keys (full, header, numbers, grid, none)

**Output parity tests (4 tests):**
1. parseYAML result shape is identical whether yq CLI or JS fallback runs (has success, usedFallback, result)
2. catWithHighlight result shape is identical whether bat CLI or JS fallback runs
3. parseYAML JS fallback produces valid parsed object for simple key:value YAML
4. catWithHighlight JS fallback produces file content string

Use the project's own source files as test fixtures where file reading is needed (e.g., use this test file itself for catWithHighlight). Create inline YAML strings for parseYAML tests. All tests must pass regardless of whether yq/bat are installed.
  </action>
  <verify>
Run `npm test` — all tests pass including new yq/bat integration tests. Count new tests with `node --test tests/cli-tools-integration.test.cjs 2>&1 | tail -5`.
  </verify>
  <done>26+ new integration tests for yq config toggles, parseYAML, transformYAML, bat catWithHighlight, getLanguage, output parity. All pass regardless of tool installation.</done>
</task>

<task type="auto">
  <name>Task 2: Add gh version blocklist and preflight tests</name>
  <files>tests/cli-tools-integration.test.cjs</files>
  <action>
Add to the existing `tests/cli-tools-integration.test.cjs` file:

**isGhUsable version blocklist tests (8 tests):**
1. `isGhUsable` is a function exported from cli-tools
2. `isGhUsable()` returns object with `usable` boolean property
3. The BLOCKED_VERSIONS array in gh.js contains an entry for 2.88.0
4. Version parsing: `parseVersion('2.88.0')` returns `{ major: 2, minor: 88, patch: 0 }`
5. Version parsing: `parseVersion('2.88.1')` returns `{ major: 2, minor: 88, patch: 1 }`
6. Blocked version check logic: Given parseVersion output for 2.88.0, verify it matches the blocked version criteria (major===2 && minor===88 && patch===0)
7. Non-blocked version check logic: Given parseVersion output for 2.88.1, verify it does NOT match blocked version criteria
8. Non-blocked version check logic: Given parseVersion output for 2.87.0, verify it does NOT match blocked version criteria

   NOTE: Tests 6-8 test the LOGIC of version comparison directly, not by calling isGhUsable() (which would try to detect the actual gh binary). Extract the BLOCKED_VERSIONS constant and parseVersion function, then verify the comparison logic in pure unit-test fashion.

**detect:gh-preflight CLI tests (4 tests):**
1. Running `node bin/bgsd-tools.cjs detect:gh-preflight` returns valid JSON (parse doesn't throw)
2. Preflight output has `usable` boolean field
3. Preflight output has `authenticated` boolean field
4. Preflight output has `errors` array field

   NOTE: The preflight command exercises the real system — if gh is not installed, `usable` will be false and errors will contain messages. Tests should verify the OUTPUT SHAPE, not the specific values (which depend on the test environment).

**gh error-and-stop behavior tests (4 tests):**
1. `checkAuth` fallback throws Error (not returns graceful result) — gh has no JS fallback
2. `listPRs` fallback throws Error — no JS fallback for PR operations
3. `isGhAvailable()` returns boolean
4. gh tool detection result shape has `available` boolean

**Graceful degradation summary tests (4 tests):**
1. yq fallback: `parseYAML('key: value')` returns `{ success: true }` even when yq unavailable
2. bat fallback: `catWithHighlight(existing_file)` returns `{ success: true }` even when bat unavailable
3. gh error: gh wrapper fallback functions throw errors (confirming no silent degradation for gh)
4. All 6 CONFIG_SCHEMA tool entries have type 'boolean' and default true

Ensure all 20+ new tests are self-contained and pass regardless of gh/yq/bat installation. For version blocklist logic tests, use parseVersion directly rather than depending on installed gh version.
  </action>
  <verify>
Run `npm test` — all tests pass. Verify total new test count: `node --test tests/cli-tools-integration.test.cjs 2>&1 | tail -5` shows combined Phase 125 + Phase 126 tests all passing. Run `npm run build` — build succeeds.
  </verify>
  <done>20+ new gh tests covering version blocklist logic, preflight CLI output shape, error-and-stop behavior. Combined with Task 1, 46+ new Phase 126 tests total. All pass regardless of tool installation. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npm test` — full test suite passes (1350+ baseline + Phase 125 integration + 46+ new Phase 126 tests)
2. `npm run build` — build succeeds
3. Phase 126 tests cover all three requirements: yq (TOOL-04), bat (TOOL-05), gh (TOOL-06)
4. Version blocklist tests verify exact match on 2.88.0 (not 2.88.x)
5. Tests pass on machines without yq, bat, or gh installed (pure fallback mode)
6. Preflight CLI command returns valid structured JSON
7. Config toggle tests verify all 6 tool entries in CONFIG_SCHEMA
</verification>

<success_criteria>
- 46+ new integration tests covering all Phase 126 requirements
- Tests pass regardless of CLI tool availability on test machine
- Version blocklist logic verified for exact 2.88.0 match
- Preflight output shape validated
- Error-and-stop behavior confirmed for gh (no silent fallback)
- Full test suite (baseline + Phase 125 + Phase 126) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/0126-extended-tools/0126-03-SUMMARY.md`
</output>
