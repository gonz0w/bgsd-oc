---
phase: 202-parallelization-safety
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/planning-cache.js
  - src/lib/lifecycle.js
  - src/lib/jj-workspace.js
  - src/commands/codebase.js
  - tests/mutation-primitives.test.cjs
  - tests/codebase.test.cjs
  - tests/workspace.test.cjs
autonomous: true
requirements:
  - PARALLEL-01
  - PARALLEL-02
  - PARALLEL-03
  - PARALLEL-04
must_haves:
  truths:
    - Shared cache mutations are serialized under project-lock discipline so same-key invalidation cannot race under parallel execution.
    - Lifecycle wave ordering is verified in `src/lib/lifecycle.js` before any parallel dispatch depends on it.
    - `collectWorkspaceProof()` remains the hard gate for accelerated paths and parallel mode only proceeds when proof is authoritative.
    - Independent stage runners use `child_process.spawn` and `Promise.all` fan-in without letting shared writes escape the serialized section.
  artifacts:
    - path: src/lib/planning-cache.js
      provides: Shared cache mutation paths guarded by narrow project-lock discipline
    - path: src/lib/lifecycle.js
      provides: Deterministic Kahn-sort verification for lifecycle chains and waves
    - path: src/lib/jj-workspace.js
      provides: Workspace proof data with `parallel_allowed` as the dispatch gate
    - path: src/commands/codebase.js
      provides: Spawn-based parallel stage orchestration with Promise.all fan-in
    - path: tests/mutation-primitives.test.cjs
      provides: Lock-release and serialization regressions for shared cache mutation
    - path: tests/codebase.test.cjs
      provides: Lifecycle ordering and parallel orchestration regressions
    - path: tests/workspace.test.cjs
      provides: Workspace proof-gate regressions for accelerated paths
  key_links:
    - from: src/lib/planning-cache.js
      to: src/lib/project-lock.js
      via: withProjectLock discipline around shared cache mutation
      pattern: withProjectLock
    - from: src/lib/lifecycle.js
      to: src/commands/codebase.js
      via: Kahn-order verification before parallel wave dispatch
      pattern: buildLifecycleGraph
    - from: src/lib/jj-workspace.js
      to: src/commands/codebase.js
      via: parallel_allowed proof gate before accelerated execution
      pattern: collectWorkspaceProof
    - from: src/commands/codebase.js
      to: spawned child processes
      via: child_process.spawn + Promise.all fan-in
      pattern: spawn|Promise.all
---

<objective>
Implement Phase 202's safe parallelization slice: serialize shared cache mutation, keep lifecycle ordering deterministic, preserve workspace proof gating, and fan out independent stages with spawned children and Promise.all fan-in.

Purpose: Phase 202 protects the accelerated path from cache races and ordering bugs while keeping the existing proof gate intact.
Output: Code changes in the shared cache, lifecycle, workspace proof, and codebase orchestration layers, plus focused regression tests for each safety boundary.
</objective>

> **TDD Decision:** Selected — the phase changes observable behavior in cache mutation, ordering verification, proof gating, and parallel orchestration, so the implementation should be driven by tests first.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/202-parallelization-safety/202-CONTEXT.md
@.planning/phases/202-parallelization-safety/202-RESEARCH.md
@src/lib/planning-cache.js
@src/lib/lifecycle.js
@src/lib/jj-workspace.js
@src/commands/codebase.js
@tests/mutation-primitives.test.cjs
@tests/codebase.test.cjs
@tests/workspace.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialize shared cache mutation</name>
  <files>
    src/lib/planning-cache.js
    tests/mutation-primitives.test.cjs
  </files>
  <action>
Harden the shared cache mutation paths so parallel stages cannot race on the same entry.

Use the existing `withProjectLock()` discipline from `src/lib/project-lock.js` as the narrow serialization boundary for the cache mutation points that matter to parallel stage safety. Keep the existing SQLite transaction behavior intact; the lock is for cross-stage coordination, not a replacement for the transaction.

Focus on the mutation surfaces that can overlap during accelerated work, especially roadmap/plan writes and any shared invalidation path that can refresh the same key from two callers.

Extend `tests/mutation-primitives.test.cjs` with regressions that prove:
- the lock is released after a failing protected mutation
- back-to-back protected writes do not leave the lock held
- the new cache mutation path still behaves correctly under failure and cleanup
  </action>
  <verify>node --test tests/mutation-primitives.test.cjs</verify>
  <done>Shared cache mutation is serialized by project-lock discipline and the lock-release regressions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Keep lifecycle ordering deterministic</name>
  <files>
    src/lib/lifecycle.js
    tests/codebase.test.cjs
  </files>
  <action>
Make the lifecycle graph the single source of truth for Kahn-style ordering verification.

Keep the verification in `src/lib/lifecycle.js` rather than adding a separate command-layer verifier. Tighten the chain-building path so ordering is computed before any downstream parallel dispatch can rely on it, and make sure the graph output remains deterministic for the same lifecycle input.

Extend `tests/codebase.test.cjs` to prove:
- the topological ordering still produces the expected chain order
- the lifecycle cache remains populated with the verified graph shape
- the behavior stays stable for projects with and without lifecycle patterns
  </action>
  <verify>node --test tests/codebase.test.cjs</verify>
  <done>Lifecycle ordering verification stays in lifecycle.js and the lifecycle regression coverage passes.</done>
</task>

<task type="auto">
  <name>Task 3: Gate and fan out parallel stages</name>
  <files>
    src/lib/jj-workspace.js
    src/commands/codebase.js
    tests/workspace.test.cjs
    tests/codebase.test.cjs
  </files>
  <action>
Add the proof-gated parallel execution path.

Use `collectWorkspaceProof()` as the hard precondition for accelerated execution. Only dispatch the parallel branch when `parallel_allowed` is true, and keep the sequential fallback intact when proof is missing or mismatched.

Implement the independent stage runner in `src/commands/codebase.js` with `child_process.spawn` and `Promise.all` fan-in. Keep shared writes serialized outside the parallel section. Use the deferred cleanup decision from context: do not invent a new orphan-kill policy in this phase.

Extend tests so they prove:
- workspace proof still unlocks parallel mode only when intended root, cwd, and jj root all match
- the accelerated path stays blocked when proof is missing or mismatched
- the parallel orchestration reports the same observable success/fallback behavior as the sequential path for the approved wave
  </action>
  <verify>node --test tests/workspace.test.cjs tests/codebase.test.cjs</verify>
  <done>Parallel dispatch remains proof-gated, uses spawn + Promise.all fan-in, and retains the sequential fallback contract.</done>
</task>

</tasks>

<verification>
1. `node --test tests/mutation-primitives.test.cjs tests/codebase.test.cjs tests/workspace.test.cjs` passes
2. Shared cache mutation is serialized without breaking existing transaction behavior
3. Lifecycle ordering is verified in `src/lib/lifecycle.js` and remains deterministic
4. Parallel dispatch only starts when workspace proof is authoritative
5. Spawn-based fan-out/fan-in does not move shared writes into the parallel section
</verification>

<success_criteria>
- [ ] Phase 202 is validated against PARALLEL-01..PARALLEL-04
- [ ] Shared cache mutations use narrow project-lock serialization
- [ ] Lifecycle Kahn ordering verification stays in `src/lib/lifecycle.js`
- [ ] Workspace proof remains the hard gate for accelerated paths
- [ ] Parallel stage orchestration uses `child_process.spawn` with `Promise.all` fan-in
- [ ] Targeted regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/202-parallelization-safety/202-01-SUMMARY.md`
</output>
