---
phase: 105-polish-and-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [POLY-01, POLY-02, POLY-03]

must_haves:
  truths:
    - "User typing a typo command gets actionable 'did you mean X?' suggestion"
    - "User can bypass unnecessary prompts with --defaults flag"
    - "User sees brief one-liner error with actionable recovery suggestion"
  artifacts:
    - path: "src/lib/commandDiscovery.js"
      provides: "getSimilarCommands with confidence scoring"
      contains: "threshold, confidence percentage"
    - path: "src/router.js"
      provides: "Command routing with smart defaults"
      contains: "--defaults flag handling"
    - path: "src/lib/error.js"
      provides: "Enhanced error formatting"
      contains: "oneLine, suggestion, examples"
  key_links:
    - from: "src/router.js"
      to: "src/lib/commandDiscovery.js"
      via: "getSimilarCommands call"
      pattern: "getSimilarCommands.*confidence"
    - from: "src/router.js"
      to: "src/lib/error.js"
      via: "error function with suggestion"
      pattern: "error.*suggestion|Try:.*"
---

<objective>
Polish edge cases in command routing, streamline workflow execution with smart defaults, and enhance error messages with actionable suggestions.

Purpose: Complete the v11.1 milestone by ensuring commands handle confusion gracefully, remove unnecessary steps, and provide helpful error recovery.
Output: Enhanced command routing with confidence thresholds, --defaults flag, and improved error formatting.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-polish-and-fixes/105-CONTEXT.md

# Prior phase summaries inform polish approach
@.planning/phases/104-zero-friction/104-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance command confusion handling with confidence threshold</name>
  <files>src/lib/commandDiscovery.js</files>
  <action>
Enhance getSimilarCommands() to return confidence percentage alongside suggestions. Add threshold parameter (default 90%) to only suggest when confidence is high. Modify function signature to: getSimilarCommands(input, options) where options = { threshold: 0.9, thresholdType: 'high'|'low' }. High threshold (90%) = only suggest when very confident. Return format: { command, distance, confidence: 0-100, suggestion: "Did you mean: X?" }.
  </action>
  <verify>
node -e "const d = require('./src/lib/commandDiscovery'); const r = d.getSimilarCommands('bgsd plan fase', { threshold: 0.9, thresholdType: 'high' }); console.log(JSON.stringify(r, null, 2));"
  </verify>
  <done>
getSimilarCommands returns confidence percentage and filters by threshold. Commands with distance >2 or confidence <90% (for high threshold) are not suggested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement --defaults flag for smart defaults bypass</name>
  <files>src/router.js</files>
  <action>
Add --defaults flag handling in command routing. When --defaults is present, use smart defaults for any optional parameters instead of prompting. Look for places where user is prompted for optional input (grep for 'prompt\|readline\|confirm'). Add defaultsMap: { phase: 'next', force: false, verbose: false }. Apply defaults before any clarification logic. Test with: plan:phase with --defaults should use next incomplete phase.
  </action>
  <verify>
node bin/bgsd-tools.cjs plan:phase --defaults 2>&1 | head -20
  </verify>
  <done>
--defaults flag bypasses all prompts and uses smart defaults. User is never asked "did you mean?" when --defaults is used.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance error messages with brief format and examples</name>
  <files>src/lib/error.js</files>
  <action>
Enhance error formatting to include: (1) oneLine: brief single-line summary for terminal status, (2) detailed: full error with context and examples, (3) suggestion: actionable "Try:" command. Add formatErrorBrief() function that returns just the one-liner. Modify BgsdError to accept options: { oneLine, examples: [], suggestion }. In router.js error calls, add brief format option. Example: error("Invalid phase") -> prints "[ERROR] Invalid phase: X must be Y. Try: bgsd plan phase 05"
  </action>
  <verify>
node -e "const { BgsdError } = require('./src/lib/error'); const e = new BgsdError('test', { suggestion: 'Try: bgsd plan phase', examples: ['bgsd plan phase 05'] }); console.log(require('./src/lib/error').formatError(e));"
  </verify>
  <done>
Errors now show: brief one-line summary, detailed message with examples, and actionable "Try:" suggestion. All error paths include suggestion when recovery action exists.
  </done>
</task>

</tasks>

<verification>
Run test commands to verify all three success criteria:
1. Typo command shows "did you mean" with high confidence threshold
2. Command with --defaults uses smart defaults without prompting
3. Error messages include "Try:" with actionable suggestion
</verification>

<success_criteria>
After execution:
- POLY-01: Command confusion shows suggestions only when 90%+ confidence
- POLY-02: --defaults flag bypasses prompts with smart defaults
- POLY-03: All errors include actionable "Try:" suggestion
</success_criteria>

<output>
After completion, create `.planning/phases/105-polish-and-fixes/105-01-SUMMARY.md`
</output>
