---
phase: 117-intent-archival-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/phase.js
  - workflows/complete-milestone.md
  - src/plugin/advisory-guardrails.js
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04

must_haves:
  truths:
    - "User completes milestone and INTENT.md outcomes automatically snapshot to archive file"
    - "User completes milestone and completed outcomes are stripped from active INTENT.md"
    - "User adds new outcomes after archival without ID collisions"
    - "User completes milestone and history section is archived alongside outcomes"
    - "Active INTENT.md stays lean (under 100 lines)"
  artifacts:
    - path: ".planning/archive/INTENT-v{version}.md"
      provides: "Archived snapshot of INTENT.md at milestone completion"
    - path: "src/commands/phase.js"
      provides: "Intent archival logic integrated into cmdMilestoneComplete"
  key_links:
    - from: "workflows/complete-milestone.md"
      to: "src/commands/phase.js"
      via: "plan:milestone complete CLI call"
---

<objective>
Implement automatic INTENT.md archival during milestone completion — completed outcomes archived to per-milestone files, active file stays lean with only current objective and pending outcomes.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/INTENT.md
@.planning/phases/117-intent-archival-system/117-CONTEXT.md
@src/commands/phase.js
@workflows/complete-milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intent archival logic to cmdMilestoneComplete</name>
  <files>src/commands/phase.js</files>
  <action>
Add intent archival function to src/commands/phase.js and integrate into cmdMilestoneComplete:

1. Add new helper function `archiveIntent(cwd, version)` that:
   - Reads current INTENT.md
   - Creates `.planning/archive/` directory if missing
   - Creates archive file `INTENT-v{version}.md` with full INTENT.md content
   - Parses outcomes: keeps only those without [P1]/[P2] completion markers or marked as pending
   - Strips criteria, constraints, health sections (archived with milestone)
   - Tracks highest-ever outcome ID in a comment to prevent future ID collisions
   - Rewrites active INTENT.md with only: revision, created, updated, objective, users, pending outcomes

2. Call `archiveIntent(cwd, version)` at end of cmdMilestoneComplete function (before final output)

The archival runs automatically as part of `plan:milestone complete` — no separate CLI command needed.
  </action>
  <verify>
node -e "
const fs = require('fs');
const content = fs.readFileSync('src/commands/phase.js', 'utf8');
console.log('archiveIntent function:', content.includes('function archiveIntent'));
console.log('cmdMilestoneComplete calls archiveIntent:', content.includes('archiveIntent(cwd'));
"
</verify>
  <done>archiveIntent function exists in phase.js and is called from cmdMilestoneComplete</done>
</task>

<task type="auto">
  <name>Task 2: Update milestone complete workflow documentation</name>
  <files>workflows/complete-milestone.md</files>
  <action>
Update workflows/complete-milestone.md:

1. In success_criteria section (line 213+), add:
   - "[ ] INTENT.md archived to .planning/archive/INTENT-v{version}.md"
   - "[ ] Active INTENT.md contains only objective + pending outcomes"

2. In the archive_milestone step comments, note that INTENT.md is also archived automatically

This is informational — the actual archival happens automatically in the CLI.
  </action>
  <verify>
grep -n "INTENT.md" workflows/complete-milestone.md
</verify>
  <done>complete-milestone.md documents intent archival in success criteria</done>
</task>

<task type="auto">
  <name>Task 3: Add /bgsd-complete-milestone to advisory guardrails allowlist</name>
  <files>src/plugin/advisory-guardrails.js</files>
  <action>
Update src/plugin/advisory-guardrails.js line 109:
Change: `'INTENT.md': ['/bgsd-new-project', '/bgsd-new-milestone'],`
To: `'INTENT.md': ['/bgsd-new-project', '/bgsd-new-milestone', '/bgsd-complete-milestone'],`

This prevents guardrails from flagging INTENT.md modifications during milestone completion.
  </action>
  <verify>
grep "INTENT.md" src/plugin/advisory-guardrails.js | grep complete-milestone
</verify>
  <done>advisory-guardrails.js allows /bgsd-complete-milestone to modify INTENT.md</done>
</task>

</tasks>

<verification>
1. Run `npm run build` — verify build succeeds
2. Run tests: `npm test` — verify all tests pass
3. Verify archiveIntent function exists in phase.js
4. Check advisory-guardrails allowlist updated
</verification>

<success_criteria>
- [ ] INTENT.md automatically archived to .planning/archive/INTENT-v{version}.md on milestone complete
- [ ] Active INTENT.md contains only objective + pending outcomes (no completed outcomes, criteria, constraints, health)
- [ ] Outcome IDs continue monotonically after archival (no collisions)
- [ ] History section archived alongside outcomes
- [ ] /bgsd-complete-milestone added to advisory guardrails allowlist
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/117-intent-archival-system/117-01-SUMMARY.md`
</output>
