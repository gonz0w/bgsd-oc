---
phase: 0125-core-tools-integration
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - tests/cli-tools-integration.test.cjs
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03, TOOL-DEGR-01]

must_haves:
  truths:
    - "Integration tests verify ripgrep, fd, and jq work correctly when tools are available"
    - "Integration tests verify all three tools fall back gracefully when unavailable"
    - "Config toggle tests confirm tools_ripgrep=false causes ripgrep to be skipped"
    - "Output parity tests confirm CLI and fallback paths produce identical results"
    - "All integration tests pass on CI regardless of which CLI tools are installed"
  artifacts:
    - path: "tests/cli-tools-integration.test.cjs"
      provides: "Integration test suite for Phase 125 tool integrations"
      contains: "searchRipgrep"
  key_links:
    - from: "tests/cli-tools-integration.test.cjs"
      to: "src/lib/cli-tools/fallback.js"
      via: "require('../src/lib/cli-tools/fallback')"
      pattern: "isToolEnabled"
    - from: "tests/cli-tools-integration.test.cjs"
      to: "src/lib/adapters/discovery.js"
      via: "require('../src/lib/adapters/discovery')"
      pattern: "walkSourceFiles"
---

<objective>
Create a comprehensive integration test suite validating all Phase 125 tool integrations work correctly with both CLI backends and Node.js fallbacks.

Purpose: Verify that ripgrep, fd, and jq integrations produce correct results regardless of tool availability, that config toggles work, and that output parity is maintained between CLI and fallback paths. This ensures Phase 125 requirements TOOL-01, TOOL-02, TOOL-03, and TOOL-DEGR-01 are all verified.

Output: Integration test file with 30+ tests covering config toggles, ripgrep search, fd discovery, jq transformation, fallback parity, and graceful degradation.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0125-core-tools-integration/0125-CONTEXT.md
@.planning/phases/0125-core-tools-integration/0125-01-SUMMARY.md
@.planning/phases/0125-core-tools-integration/0125-02-SUMMARY.md
@tests/cli-tools.test.cjs
@src/lib/cli-tools/fallback.js
@src/lib/cli-tools/ripgrep.js
@src/lib/cli-tools/fd.js
@src/lib/cli-tools/jq.js
@src/lib/constants.js
@src/lib/adapters/discovery.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test suite for config toggles and ripgrep integration</name>
  <files>tests/cli-tools-integration.test.cjs</files>
  <action>
Create `tests/cli-tools-integration.test.cjs` using `node:test` and `node:assert` (matching project conventions from Phase 124's test file).

**Config toggle tests (8+ tests):**
1. CONFIG_SCHEMA has tools_ripgrep, tools_fd, tools_jq entries with type 'boolean' and default true
2. CONFIG_SCHEMA tools entries have nested section 'tools' for nested config lookup
3. `isToolEnabled('ripgrep')` returns boolean (not null/undefined)
4. `isToolEnabled('fd')` returns boolean
5. `isToolEnabled('jq')` returns boolean
6. `isToolEnabled` with unknown tool name returns false or relies on detection
7. `withToolFallback` for a disabled tool goes to fallback path (mock via config)
8. `isToolEnabled` is exported from fallback.js module

**ripgrep integration tests (8+ tests):**
1. `searchRipgrep(pattern, { paths })` returns `{ success, usedFallback, result }` shape
2. Search with no matches returns empty array (not error), testing exit code 1 handling
3. Search with matches returns array of `{ path, lineNumber, line }` objects
4. `parseRipgrepJson` handles empty input gracefully
5. `parseRipgrepJson` handles valid JSON lines output
6. `parseRipgrepJson` skips non-match type JSON lines
7. Search with `maxCount: 1` limits results
8. Search with `ignoreCase: true` finds case-insensitive matches

**Output parity tests (4+ tests):**
1. searchRipgrep result shape is identical whether CLI or fallback runs
2. Result array elements have same properties (path, lineNumber, line, offset, _raw)
3. findFiles result shape is identical whether CLI or fallback runs  
4. transformJson result shape is identical whether CLI or fallback runs

Use the project's own source files as test corpus (e.g., search for 'require' in src/lib/ directory, find .js files, transform a known JSON object). This ensures tests work regardless of which tools are installed on the CI machine.
  </action>
  <verify>
Run `npm test` — all tests pass including new integration tests. Count new tests: `node --test tests/cli-tools-integration.test.cjs 2>&1 | tail -5`.
  </verify>
  <done>Integration test file created with 20+ tests covering config toggles, ripgrep search, output parity. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add fd discovery and jq transformation integration tests</name>
  <files>tests/cli-tools-integration.test.cjs</files>
  <action>
Add to the existing `tests/cli-tools-integration.test.cjs` file:

**fd integration tests (8+ tests):**
1. `findFiles('', { type: 'f' })` returns `{ success, usedFallback, result }` shape
2. `findFiles` result is an array of file paths (strings)
3. `findByExtension('js')` returns only .js files
4. `findDirectories('', { maxDepth: 1 })` returns directories
5. fd results don't include node_modules or .git entries (gitignore respected)
6. Discovery adapter `walkSourceFiles` returns array of relative file paths
7. Discovery adapter `getSourceDirs` returns array of directory names
8. Discovery still works when fd unavailable (fallback to optimized/legacy)

**jq integration tests (8+ tests):**
1. `transformJson(obj, '.key')` returns the value at key
2. `transformJson(arr, '.[0]')` returns first element
3. `transformJson(obj, 'keys')` returns array of keys
4. `transformJson(arr, 'length')` returns array length
5. `transformJson` with `select` filter works (e.g., `[.[] | select(.x > 1)]`)
6. `transformJson` with pipe chain works (e.g., `.[] | .name`)
7. `transformJson` with compact option returns single-line JSON
8. `transformJson` with unknown filter doesn't crash (graceful handling)
9. FILTER_PRESETS has corrected mappings (keys → 'keys', not '.[]')

**Graceful degradation tests (4+ tests):**
1. All three tool wrappers return `{ success: true }` even when tools unavailable
2. `usedFallback` is `true` when tool not installed
3. No user-visible error messages in fallback path output (silent degradation per CONTEXT.md)
4. `isToolEnabled` returns false when config explicitly disables a tool

Ensure all tests are self-contained and pass regardless of which CLI tools are installed on the test machine. Each test should work with BOTH the CLI path and fallback path.
  </action>
  <verify>
Run `npm test` — all tests pass. Verify total count: `node --test tests/cli-tools-integration.test.cjs 2>&1 | tail -5` shows 30+ tests. Run `npm run build` — build succeeds.
  </verify>
  <done>Integration test suite complete with 30+ tests covering fd discovery, jq transformation, graceful degradation. All tests pass. Total test suite (1350 baseline + 30+ new) all green.</done>
</task>

</tasks>

<verification>
1. `npm test` — full test suite passes (1350+ baseline + 30+ new integration tests)
2. `npm run build` — build succeeds
3. Integration test file exists: `ls tests/cli-tools-integration.test.cjs`
4. Test count: `node --test tests/cli-tools-integration.test.cjs 2>&1 | grep -c "# pass"` ≥ 30
5. Tests cover all four requirements: grep for TOOL-01, TOOL-02, TOOL-03, TOOL-DEGR-01 coverage in test descriptions
6. Tests run cleanly without any CLI tools installed (pure fallback mode)
</verification>

<success_criteria>
- 30+ integration tests covering all Phase 125 requirements
- Tests pass regardless of CLI tool availability on test machine
- Config toggle tests verify enable/disable behavior
- Output parity tests confirm CLI and fallback produce identical shapes
- Full test suite (baseline + new) remains green
</success_criteria>

<output>
After completion, create `.planning/phases/0125-core-tools-integration/0125-03-SUMMARY.md`
</output>
