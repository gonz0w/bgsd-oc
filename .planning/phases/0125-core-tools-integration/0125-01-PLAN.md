---
phase: 0125-core-tools-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.js
  - src/lib/cli-tools/fallback.js
  - src/lib/conventions.js
  - src/lib/deps.js
  - src/commands/env.js
autonomous: true
requirements: [TOOL-01, TOOL-DEGR-01]

must_haves:
  truths:
    - "Per-tool config toggles (tools_ripgrep, tools_fd, tools_jq) exist in CONFIG_SCHEMA with boolean type and default true"
    - "Setting tools_ripgrep to false in config.json causes ripgrep to be skipped even if installed"
    - "Convention detection (extractPatterns for Phoenix) uses ripgrep when available for content search across .ex files"
    - "Dependency parsing pre-filters files via ripgrep when available before reading content"
    - "Infrastructure service detection in env.js uses ripgrep for docker-compose parsing when available"
    - "All fallback paths produce identical output format — callers cannot distinguish which backend ran"
  artifacts:
    - path: "src/lib/constants.js"
      provides: "Config schema entries for tools_ripgrep, tools_fd, tools_jq"
      contains: "tools_ripgrep"
    - path: "src/lib/cli-tools/fallback.js"
      provides: "Config-aware tool resolution"
      exports: ["isToolEnabled", "withToolFallback"]
    - path: "src/lib/conventions.js"
      provides: "ripgrep-backed convention pattern extraction"
      contains: "searchRipgrep"
    - path: "src/lib/deps.js"
      provides: "ripgrep-backed import pre-filtering"
      contains: "searchRipgrep"
    - path: "src/commands/env.js"
      provides: "ripgrep-backed infrastructure detection"
      contains: "searchRipgrep"
  key_links:
    - from: "src/lib/cli-tools/fallback.js"
      to: "src/lib/config.js"
      via: "loadConfig() call in isToolEnabled()"
      pattern: "loadConfig"
    - from: "src/lib/conventions.js"
      to: "src/lib/cli-tools/index.js"
      via: "require('./cli-tools')"
      pattern: "searchRipgrep"
    - from: "src/lib/deps.js"
      to: "src/lib/cli-tools/index.js"
      via: "require('./cli-tools')"
      pattern: "searchRipgrep"
---

<objective>
Add per-tool config toggles and integrate ripgrep into all content search callsites across bgsd-tools.

Purpose: Enable config-driven tool enablement/disablement and replace file-by-file readFileSync+regex patterns with ripgrep-backed search for 50x+ speedup on large codebases. This is the foundation for all Phase 125 tool integrations.

Output: Config schema entries, config-aware fallback wrapper, and ripgrep wired into conventions.js, deps.js, and env.js with identical output format from both backends.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/0125-core-tools-integration/0125-CONTEXT.md
@.planning/phases/0125-core-tools-integration/0125-RESEARCH.md
@.planning/phases/0124-tool-detection-infrastructure/0124-01-SUMMARY.md
@src/lib/constants.js
@src/lib/cli-tools/fallback.js
@src/lib/cli-tools/ripgrep.js
@src/lib/cli-tools/index.js
@src/lib/conventions.js
@src/lib/deps.js
@src/commands/env.js
@src/lib/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config toggles and config-aware tool resolution</name>
  <files>src/lib/constants.js, src/lib/cli-tools/fallback.js</files>
  <action>
1. In `src/lib/constants.js`, add three entries to CONFIG_SCHEMA after the existing `optimization_sqlite_cache` entry:
   - `tools_ripgrep: { type: 'boolean', default: true, description: 'Enable ripgrep for content search', aliases: [], nested: { section: 'tools', field: 'ripgrep' } }`
   - `tools_fd: { type: 'boolean', default: true, description: 'Enable fd for file discovery', aliases: [], nested: { section: 'tools', field: 'fd' } }`
   - `tools_jq: { type: 'boolean', default: true, description: 'Enable jq for JSON transformation', aliases: [], nested: { section: 'tools', field: 'jq' } }`

2. In `src/lib/cli-tools/fallback.js`:
   - Add `const { loadConfig } = require('../config');` at the top
   - Create new `isToolEnabled(toolName)` function that:
     a. Calls `loadConfig(process.cwd())` in a try/catch (config may not exist)
     b. Checks `config[`tools_${toolName}`]` — if explicitly `false`, return `false`
     c. Falls through to existing `isToolAvailable(toolName)` for detection check
   - Update `withToolFallback()` to call `isToolEnabled()` instead of `detectTool()` directly:
     Replace the `detectTool(toolName)` call with a two-step check: first `isToolEnabled()`, and if `false` go straight to fallback. If `true`, proceed with `detectTool()` check and CLI execution.
     IMPORTANT: Keep the existing `detectTool()` call for when config says enabled — `isToolEnabled()` checks config then detection, `withToolFallback()` uses `isToolEnabled()` to decide.
   - Export `isToolEnabled` alongside existing exports

  Note: The `loadConfig` import creates a deliberate dependency on config module. When config.json doesn't exist (e.g., no .planning/ directory), the try/catch falls through to detection-only mode, preserving backward compatibility.
  </action>
  <verify>
Run `npm test` — all 1350 existing tests pass. Verify `isToolEnabled` is exported from fallback.js. Verify CONFIG_SCHEMA has 3 new entries with `grep -c 'tools_' src/lib/constants.js` showing 3.
  </verify>
  <done>CONFIG_SCHEMA has tools_ripgrep, tools_fd, tools_jq entries. fallback.js exports isToolEnabled(). withToolFallback() respects config toggles. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ripgrep into conventions.js, deps.js, and env.js</name>
  <files>src/lib/conventions.js, src/lib/deps.js, src/commands/env.js</files>
  <action>
**conventions.js** (Phoenix extractPatterns — lines 349-449):
1. Add `const { searchRipgrep } = require('./cli-tools');` at the top of the file
2. In `extractPatterns()` for the Phoenix detector, replace the file-by-file read+regex pattern with ripgrep calls. For each pattern detection:
   - Pattern 1 (Phoenix routes, lines 354-376): Instead of iterating `routerFiles` and reading each, use `searchRipgrep('pipe_through|\\b(get|post|put|delete|patch)\\s', { paths: routerFiles.map(f => path.join(cwd, f)) })`. Parse results to build the same `routeEvidence` array and `hasPipeThrough` boolean. If `result.usedFallback`, fall through to existing readFile logic (the wrapper handles this automatically).
   - Pattern 2 (Ecto schemas, lines 379-395): Use `searchRipgrep('use Ecto\\.Schema', { paths: schemaFiles.map(f => path.join(cwd, f)), maxCount: 1 })`. Collect matching file paths into `ectoSchemaFiles`.
   - Pattern 3 (Plugs, lines 398-413): Use `searchRipgrep('use Plug\\b|import Plug\\.Conn', { paths: schemaFiles.map(f => path.join(cwd, f)), maxCount: 1 })`. Collect matching file paths.
   
   CRITICAL: The searchRipgrep wrapper already handles fallback transparently — when ripgrep unavailable, it runs the Node.js regex fallback internally. So replace the manual read+regex loops with searchRipgrep calls; the fallback path inside the wrapper provides identical output format. Callers don't need dual logic.

   IMPORTANT: ripgrep exit code 1 means "no matches" not "error". The ripgrep.js wrapper already handles this in its try/catch. But verify: if searchRipgrep returns `{ success: false }` for no matches, handle it as empty results, not an error.

**deps.js** (import parsing — line 494):
3. Add `const { searchRipgrep } = require('./cli-tools');` at the top
4. In the file reading section around line 494, add a ripgrep pre-filter optimization: before reading each file's full content, use ripgrep to quickly check if the file contains import/require patterns. Only read the full file if ripgrep confirms matches exist. This is an optimization — the existing readFileSync+parse logic remains as the actual parser; ripgrep just pre-filters files that definitely have imports.
   - Use `searchRipgrep('^(import |from |require\\(|use |using |package )', { paths: [absPath], maxCount: 1 })` as a quick check before the full read.
   - If ripgrep finds no matches, skip that file (it has no imports).
   - If ripgrep returns usedFallback=true, skip the pre-filter and read the file as before.

**env.js** (detectInfraServices — lines 536-563):
5. Add `const { searchRipgrep } = require('../lib/cli-tools');` at the top
6. In `detectInfraServices()`, for the docker-compose file parsing, replace the readFileSync+regex with a ripgrep call:
   - Use `searchRipgrep('image:|build:', { paths: [filePath] })` to extract service lines from docker-compose files.
   - Parse matched lines to extract service names (same logic, just faster file reading).
   - The fallback handles unavailability automatically.

IMPORTANT per CONTEXT.md: Silent fallback — no user-visible messages. The withToolFallback wrapper may set `guidance` on fallback, but callers must NOT print it. The convention is: use the result silently, ignore the `usedFallback` and `guidance` fields in normal operation.
  </action>
  <verify>
Run `npm test` — all tests pass. Run `npm run build` — build succeeds. Verify ripgrep import exists in all three files with `grep -l "searchRipgrep" src/lib/conventions.js src/lib/deps.js src/commands/env.js`.
  </verify>
  <done>conventions.js Phoenix pattern detection uses ripgrep for content search. deps.js pre-filters import-containing files via ripgrep. env.js detectInfraServices uses ripgrep for docker-compose parsing. All fallbacks transparent. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npm test` — all 1350+ tests pass (no regressions)
2. `npm run build` — build succeeds 
3. CONFIG_SCHEMA has tools_ripgrep, tools_fd, tools_jq entries: `node -e "const c = require('./src/lib/constants'); console.log(Object.keys(c.CONFIG_SCHEMA).filter(k => k.startsWith('tools_')))"` → ['tools_ripgrep', 'tools_fd', 'tools_jq']
4. isToolEnabled exported from fallback.js: `node -e "const f = require('./src/lib/cli-tools/fallback'); console.log(typeof f.isToolEnabled)"` → function
5. ripgrep integration present: `grep -l searchRipgrep src/lib/conventions.js src/lib/deps.js src/commands/env.js` → lists all 3 files
6. No shell injection: `grep -r "execSync(" src/lib/conventions.js src/lib/deps.js src/commands/env.js` → no new execSync calls (all tool calls go through wrappers)
</verification>

<success_criteria>
- Per-tool config toggles exist and control tool availability
- ripgrep integrated into 3 consumer files with transparent fallback
- All existing tests pass with zero regressions
- Build succeeds
- No shell injection vulnerabilities (all tool calls via execFileSync array args through wrappers)
</success_criteria>

<output>
After completion, create `.planning/phases/0125-core-tools-integration/0125-01-SUMMARY.md`
</output>
