---
phase: 108-dead-code-removal
plan: 02
type: execute
wave: 2
depends_on:
  - 108-01
files_modified: []
autonomous: true
requirements:
  - DEAD-03

must_haves:
  truths:
    - "Verified dead code is removed from source files"
    - "All tests pass after removal"
    - "CLI commands still work after removal"
    - "Rollback info saved for removed code"
  artifacts:
    - path: ".planning/phases/108-dead-code-removal/removed-code.json"
      provides: "Log of all removed code for potential rollback"
      contains: "Array of {file, lines_removed, code, reason}"
  key_links:
    - from: "unreachable-report.json"
      to: "removed-code.json"
      via: "Manual verification and removal"
      pattern: "removed.*json"
---

<objective>
Remove verified dead code from the codebase based on Plan 01 analysis.

Purpose: Remove confirmed unreachable code after human verification
Output: Cleaned source files, rollback info, verified functionality

Following the "mark first, then remove" approach from context.
Human verification required for each removal.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/108-dead-code-removal/108-CONTEXT.md
@.planning/phases/108-dead-code-removal/108-01-PLAN.md
@.planning/phases/108-dead-code-removal/unreachable-report.json

# Key decisions from CONTEXT:
- Mark first, then remove after verification
- Human verification required for each removal
- Mixed approach - depends on confidence level
- Per-phase file for rollback info
- Conservative: leave uncertain code
- Run tests and CLI commands after each removal batch
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Review high-confidence dead code</name>
  <files>.planning/phases/108-dead-code-removal/unreachable-report.json</files>
  <action>
# Present high-confidence findings for verification
1. Filter unreachable-report.json for confidence: "high"
2. Present each finding to user for verification
3. User confirms: remove / skip / investigate further

For each high-confidence item:
- Show file, line number, code snippet
- Explain why it's dead (e.g., "code after return statement")
- User decides: remove, skip, or needs more investigation

This is the human verification checkpoint per context requirements.
  </action>
  <verify>
# After user confirms removals:
# - Create list of confirmed removals
# - Create list of items to skip
</verify>
  <done>
- List of user-confirmed removals
- List of items to skip
- Proceed to removal tasks
</done>
</task>

<task type="auto">
  <name>Task 2: Remove confirmed dead code</name>
  <files>src/**/*.js (files with confirmed dead code)</files>
  <action>
# Remove confirmed dead code from source files
1. For each confirmed removal:
   a. Read the source file
   b. Remove the dead code lines
   c. Save original code to removed-code.json for rollback
   d. Write the cleaned file
   
2. Commit strategy: per-file commits after each removal
   - Allows easy rollback if issues found
   
3. After each removal:
   - Run npm test to verify no breakage
   - If tests fail, rollback that specific removal

removed-code.json format:
{
  "file": "src/lib/utils.js",
  "lines_removed": "15-20",
  "code": "/* removed code here */",
  "reason": "code after return statement",
  "removed_at": "2026-03-12T..."
}
  </action>
  <verify>
# Run tests after removals
npm test 2>&1 | tail -20
</verify>
  <done>
- Confirmed dead code removed from source
- removed-code.json updated with rollback info
- Tests pass after removal
</done>
</task>

<task type="auto">
  <name>Task 3: Verify CLI commands still work</name>
  <files>bin/bgsd-tools.cjs</files>
  <action>
# Verify CLI functionality after removal
Run key CLI commands to ensure nothing broke:

1. node bin/bgsd-tools.cjs util:help
2. node bin/bgsd-tools.cjs verify:state --help
3. node bin/bgsd-tools.cjs plan:roadmap --help

If any command fails, investigate and potentially rollback.
  </action>
  <verify>
# All commands return without errors
node bin/bgsd-tools.cjs util:help > /dev/null 2>&1 && echo "util:help OK"
node bin/bgsd-tools.cjs verify:state --help > /dev/null 2>&1 && echo "verify:state OK"
</verify>
  <done>
- All key CLI commands work
- No runtime errors after removal
</done>
</task>

<task type="auto">
  <name>Task 4: Final verification and cleanup</name>
  <files>.planning/phases/108-dead-code-removal/</files>
  <action>
# Final verification step
1. Run full test suite: npm test
2. Check for any new warnings or errors
3. Update phase status in ROADMAP.md
4. Mark DEAD-01, DEAD-02, DEAD-03 as complete in REQUIREMENTS.md

If tests pass and CLI works:
- Phase complete
- Summary created
  </action>
  <verify>
npm test 2>&1 | grep -E "(PASS|FAIL|Tests:)"
</verify>
  <done>
- All tests pass
- All CLI commands work
- Requirements marked complete
- Summary created
</done>
</task>

</tasks>

<verification>
- All confirmed dead code removed
- npm test passes
- All CLI commands work
- Rollback info saved
</verification>

<success_criteria>
- Dead code removed after human verification
- All tests pass
- CLI commands work
- Rollback info preserved for each removal
- DEAD-01, DEAD-02, DEAD-03 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/108-dead-code-removal/108-02-SUMMARY.md`
</output>
