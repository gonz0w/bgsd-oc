---
phase: 182-risk-routed-hardening-proof-policy
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/decision-rules.js
  - src/plugin/command-enricher.js
  - workflows/execute-phase.md
  - tests/decisions.test.cjs
  - tests/enricher-decisions.test.cjs
  - tests/workflow.test.cjs
autonomous: true
requirements:
  - TEST-01
  - TEST-02
verification_route: full
verification_route_reason: "Touches runtime decision logic, plugin-enriched execution context, and the executor workflow contract that determine how hardening proof is routed."
must_haves:
  truths:
    - "Implementation slices can carry an explicit risk-based `verification_route` contract instead of inheriting proof cost from task/file-count heuristics."
    - "Runtime, shared-state, and plugin-facing work default to `full`, docs/workflow/template guidance can default lighter, and generated-artifact work follows source risk with explicit downgrade rationale when kept below `full`."
    - "Execution guidance receives the same normalized route contract and proof expectations that planning selected, so broad regression runs happen once only when the slice risk justifies them."
  artifacts:
    - path: "src/lib/decision-rules.js"
      provides: "Policy-first verification-route normalizer"
      contains: "required_proof"
    - path: "src/plugin/command-enricher.js"
      provides: "Execution-context routing inputs derived from plan metadata instead of plan size"
      contains: "verification-routing"
    - path: "workflows/execute-phase.md"
      provides: "Executor contract for route-aware proof expectations"
      contains: "Verification route:"
  key_links:
    - from: "src/plugin/command-enricher.js"
      to: "src/lib/decision-rules.js"
      via: "Phase/plan metadata and change-class inputs feed one shared route decision shape instead of parallel heuristics"
      pattern: "verification-routing|required_proof|default_reason|downgrade"
    - from: "workflows/execute-phase.md"
      to: "src/lib/decision-rules.js"
      via: "Executor guidance consumes the same route value and proof bundle the policy resolver emits"
      pattern: "Verification route:|skip|light|full|regression"
---

<objective>
Replace the current size-based verification-routing heuristic with one normalized risk policy that planners and executors can both trust for this milestone's hardening work.

Purpose: Phase 182 fails if routing still depends on plan size or prose-only judgment. This slice makes the locked `skip` / `light` / `full` policy durable at the runtime decision layer and carries it into execution context.
Output: a shared route normalizer in `src/lib/decision-rules.js`, richer routing inputs from `src/plugin/command-enricher.js`, updated execute-phase wording, and focused regressions that lock the policy contract.
</objective>

> **TDD Decision:** Selected — route selection and proof-bundle normalization are deterministic behavior with clear pass/fail expectations, so RED tests should lock the milestone defaults and override rules before the runtime policy changes.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-CONTEXT.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-RESEARCH.md
@.planning/phases/181-workspace-root-truth-safe-fallback/181-02-SUMMARY.md
@src/lib/decision-rules.js
@src/plugin/command-enricher.js
@workflows/execute-phase.md
@tests/decisions.test.cjs
@tests/enricher-decisions.test.cjs
@tests/workflow.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for policy-first verification routing and proof bundles</name>
  <files>
    tests/decisions.test.cjs
    tests/enricher-decisions.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
  Extend the routing regressions before touching implementation so the locked Phase 182 policy is executable rather than interpretive.

  Add failing cases that reject the current task/file-count heuristic: runtime, shared-state, and plugin slices should default to `full`; docs/workflow/template-only slices should stay eligible for `skip` or `light`; generated-artifact changes should derive their route from touched source risk and require an explicit rationale when kept below `full`; and downward overrides should require written justification while upward overrides remain allowed. Lock the normalized proof-bundle shape as part of the same contract so the resolver returns both the selected route and required proof expectations instead of only a bare string.
  </action>
  <verify>node --test tests/decisions.test.cjs tests/enricher-decisions.test.cjs</verify>
  <done>The targeted routing tests fail on current code because verification routing still keys off plan size and does not emit the locked proof-bundle metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the shared route normalizer and carry it into execution context</name>
  <files>
    src/lib/decision-rules.js
    src/plugin/command-enricher.js
    workflows/execute-phase.md
    tests/decisions.test.cjs
    tests/enricher-decisions.test.cjs
    tests/workflow.test.cjs
  </files>
  <action>
  Replace the current heuristic in `src/lib/decision-rules.js` with one policy-aligned normalizer that chooses `skip`, `light`, or `full` from change class plus blast radius, then returns the route alongside durable explanation metadata such as default reason, downgrade justification, and required proof buckets.

  Feed that resolver with richer metadata from `src/plugin/command-enricher.js` using plan/frontmatter and touched-surface signals rather than only task/file counts. Then update `workflows/execute-phase.md` so executor prompts consume the normalized route contract explicitly: `skip` = structural proof only, `light` = named focused proof plus smoke regression, `full` = named focused proof plus one broad regression gate. Keep the locked context decisions intact: generated artifacts do not auto-escalate solely because they are generated, and lighter-than-default artifact work must carry written rationale.
  </action>
  <verify>npm run build</verify>
  <done>The runtime decision layer emits one reusable policy-first route contract, the enricher supplies the needed routing inputs, and execute-phase guidance reflects the same proof expectations without falling back to size-based language.</done>
</task>

</tasks>

<feature>
  <name>Policy-first verification-route normalization for hardening work</name>
  <files>
    src/lib/decision-rules.js
    src/plugin/command-enricher.js
    workflows/execute-phase.md
    tests/decisions.test.cjs
    tests/enricher-decisions.test.cjs
    tests/workflow.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/decisions.test.cjs tests/enricher-decisions.test.cjs</red>
    <green>npm run build && node --test tests/decisions.test.cjs tests/enricher-decisions.test.cjs tests/workflow.test.cjs</green>
    <refactor>node --test tests/decisions.test.cjs tests/enricher-decisions.test.cjs tests/workflow.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Verification routing follows the locked hardening policy instead of plan-size heuristics.

    Cases:
      - runtime, shared-state, and plugin-impacting slices default to `full` with focused proof plus broad regression
      - docs, workflow, template, and guidance-only slices stay eligible for lighter routes without automatic broad-suite reruns
      - generated-artifact work derives its route from the touched source risk and must record explicit low-risk rationale when kept below `full`
      - the shared route decision exposes the selected route plus required proof buckets so executor prompts can distinguish `required` from `not required`
  </behavior>
  <implementation>
    Reuse the existing decision-rules and command-enricher surfaces instead of introducing a second policy engine. Strengthen those runtime inputs so the same normalized route contract can travel from planning metadata into executor guidance.
  </implementation>
</feature>

<verification>
```bash
npm run build && node --test tests/decisions.test.cjs tests/enricher-decisions.test.cjs tests/workflow.test.cjs
```
</verification>

<success_criteria>
- [ ] `resolveVerificationRouting` is policy-first and no longer classifies slices primarily by task/file-count thresholds
- [ ] Execution context can read one normalized route contract with explicit proof expectations for `skip`, `light`, and `full`
- [ ] Generated-artifact routing honors planner judgment and requires written rationale when a below-default route is allowed
</success_criteria>

<output>
After completion, create `.planning/phases/182-risk-routed-hardening-proof-policy/182-01-SUMMARY.md`
</output>
