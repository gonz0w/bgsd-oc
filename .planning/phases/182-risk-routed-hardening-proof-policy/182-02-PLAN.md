---
phase: 182-risk-routed-hardening-proof-policy
plan: 02
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - src/commands/misc/frontmatter.js
  - src/commands/misc/templates.js
  - src/commands/verify/quality.js
  - src/commands/scaffold.js
  - templates/verification-report.md
  - workflows/verify-work.md
  - tests/verify.test.cjs
  - tests/workflow.test.cjs
  - tests/guidance-intent-alignment.test.cjs
autonomous: true
requirements:
  - TEST-01
  - TEST-03
  - TEST-04
verification_route: full
verification_route_reason: "Touches planner validation, verifier/report scaffolds, and verification workflow guidance that define required vs exempt proof across the repo."
must_haves:
  truths:
    - "Planner-facing artifacts require an explicit `verification_route` and can describe why lighter proof is allowed instead of leaving proof scope implicit."
    - "Verifier-facing artifacts and workflow guidance distinguish missing behavior proof, missing regression proof, and missing human verification, while route-exempt buckets read as `not required`."
    - "Docs-, workflow-, template-, and guidance-only slices can stay on structural or focused proof paths without being framed as broad-regression failures."
  artifacts:
    - path: "src/commands/verify/quality.js"
      provides: "Plan-structure enforcement for explicit verification-route metadata"
      contains: "verification_route"
    - path: "src/commands/scaffold.js"
      provides: "Route-aware verification scaffold with explicit proof buckets"
      contains: "not required"
    - path: "templates/verification-report.md"
      provides: "Canonical report wording for required versus exempt proof buckets"
      contains: "not required"
  key_links:
    - from: "src/commands/misc/templates.js"
      to: "src/commands/verify/quality.js"
      via: "Plan templates and validators share the same explicit verification_route requirement"
      pattern: "verification_route|verification_route_reason"
    - from: "workflows/verify-work.md"
      to: "templates/verification-report.md"
      via: "Verification workflow and final report keep behavior, regression, and human-proof buckets separate while surfacing route exemptions honestly"
      pattern: "behavior proof|regression proof|human verification|not required"
---

<objective>
Carry the normalized verification-route contract into planner and verifier artifacts so approval checks, generated scaffolds, and final verification output all agree on what proof is required.

Purpose: The route policy is not trustworthy if plans can omit it or verifier output still blurs omission with exemption. This slice makes the route visible in planning artifacts and makes verifier/report surfaces say exactly which proof bucket is missing versus not required.
Output: plan-template and plan-structure enforcement for `verification_route`, route-aware verification scaffolds/templates, updated verify-work guidance, and regressions that lock bucket-separated reporting.
</objective>

> **TDD Decision:** Selected — planner validation and verifier reporting are deterministic contract behavior with exact expected output, so RED tests should lock the route metadata and bucket wording before those surfaces are updated.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
@/Users/cam/.config/opencode/bgsd-oc/skills/tdd-execution/SKILL.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-CONTEXT.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-RESEARCH.md
@.planning/phases/182-risk-routed-hardening-proof-policy/182-01-PLAN.md
@src/commands/misc/frontmatter.js
@src/commands/misc/templates.js
@src/commands/verify/quality.js
@src/commands/scaffold.js
@templates/verification-report.md
@workflows/verify-work.md
@tests/verify.test.cjs
@tests/workflow.test.cjs
@tests/guidance-intent-alignment.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED coverage for explicit route metadata in plan and verification artifacts</name>
  <files>
    tests/verify.test.cjs
    tests/workflow.test.cjs
    tests/guidance-intent-alignment.test.cjs
  </files>
  <action>
  Extend the planner/verifier contract tests before touching implementation so the remaining Phase 182 gaps are concrete.

  Add failing coverage that rejects plans missing an explicit `verification_route`, requires written rationale when a below-default route is allowed, and locks verification/report surfaces to show separate behavior-proof, regression-proof, and human-verification buckets with route-exempt buckets rendered as `not required`. Also lock the workflow/template guidance that docs-, workflow-, template-, and guidance-only slices may stay structural or focused without being presented as missing broad regression.
  </action>
  <verify>node --test tests/verify.test.cjs tests/workflow.test.cjs tests/guidance-intent-alignment.test.cjs</verify>
  <done>The targeted planner/verifier tests fail on current code because plan validation does not require route metadata and verification/report surfaces do not yet make required-versus-exempt proof explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Implement route-aware planner validation and verifier-report scaffolds</name>
  <files>
    src/commands/misc/frontmatter.js
    src/commands/misc/templates.js
    src/commands/verify/quality.js
    src/commands/scaffold.js
    templates/verification-report.md
    workflows/verify-work.md
    tests/verify.test.cjs
    tests/workflow.test.cjs
    tests/guidance-intent-alignment.test.cjs
  </files>
  <action>
  Update planner-facing surfaces so new plans and plan validation require explicit route metadata that matches the Phase 182 policy rather than leaving proof routing implicit.

  Extend `src/commands/misc/templates.js`, `src/commands/misc/frontmatter.js`, and `src/commands/verify/quality.js` so plan templates, frontmatter validation, and `verify:verify plan-structure` all recognize `verification_route` and route rationale fields. Then update `src/commands/scaffold.js`, `templates/verification-report.md`, and `workflows/verify-work.md` so verification artifacts keep separate behavior/regression/human-proof buckets, mark route-exempt proof as `not required`, and preserve the locked lighter-route behavior for docs/workflow/template/guidance-only slices without implying that a broad regression gate is missing.
  </action>
  <verify>npm run build</verify>
  <done>Planner artifacts expose explicit route metadata, verification scaffolds/reporting describe required and exempt proof honestly, and verifier guidance preserves proportionate proof for low-risk documentation or workflow slices.</done>
</task>

</tasks>

<feature>
  <name>Route-aware planner and verifier artifact contract</name>
  <files>
    src/commands/misc/frontmatter.js
    src/commands/misc/templates.js
    src/commands/verify/quality.js
    src/commands/scaffold.js
    templates/verification-report.md
    workflows/verify-work.md
    tests/verify.test.cjs
    tests/workflow.test.cjs
    tests/guidance-intent-alignment.test.cjs
  </files>
  <tdd-targets>
    <red>node --test tests/verify.test.cjs tests/workflow.test.cjs tests/guidance-intent-alignment.test.cjs</red>
    <green>npm run build && node --test tests/verify.test.cjs tests/workflow.test.cjs tests/guidance-intent-alignment.test.cjs</green>
    <refactor>node --test tests/verify.test.cjs tests/workflow.test.cjs tests/guidance-intent-alignment.test.cjs</refactor>
  </tdd-targets>
  <behavior>
    Planner and verifier artifacts share one explicit proof-routing contract.

    Cases:
      - plan templates and plan-structure validation require `verification_route` metadata and preserve explicit rationale when a route is lower than the default expectation
      - verification/report scaffolds always show behavior proof, regression proof, and human verification as separate buckets
      - route-exempt buckets render as `not required` so `skip` and `light` work do not look like broad-regression failures
      - docs-, workflow-, template-, and guidance-only slices can keep structural or focused proof expectations without drifting into ambiguous verifier prose
  </behavior>
  <implementation>
    Reuse existing template, scaffold, validator, and workflow surfaces rather than inventing a second report taxonomy. Align their wording and required fields to the normalized route contract from Plan 01.
  </implementation>
</feature>

<verification>
```bash
npm run build && node --test tests/verify.test.cjs tests/workflow.test.cjs tests/guidance-intent-alignment.test.cjs
```
</verification>

<success_criteria>
- [ ] Plan templates and plan-structure validation require explicit `verification_route` metadata for implementation work
- [ ] Verification/report surfaces distinguish missing behavior proof, missing regression proof, and missing human verification while marking route-exempt buckets `not required`
- [ ] Docs-, workflow-, template-, and guidance-only slices can stay proportionate without verifier output implying a missing broad regression gate
</success_criteria>

<output>
After completion, create `.planning/phases/182-risk-routed-hardening-proof-policy/182-02-SUMMARY.md`
</output>
