---
phase: 138-workflow-agent-tool-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/execute-plan.md
  - workflows/execute-phase.md
  - workflows/map-codebase.md
  - workflows/github-ci.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - GH-01
outcome_ids:
  - DO-100
  - DO-102

must_haves:
  truths:
    - "execute-plan.md parses file-discovery-mode and search-mode from bgsd-context decisions and emits different search/discovery instructions to executor agents based on tool presence vs absence"
    - "execute-phase.md passes a tool capability hint (capability level from handoff_tool_context) in the Task() prompt when spawning executor agents"
    - "map-codebase.md passes tool routing guidance to the 4 mapper agent Task() spawns so mapper agents know which CLI tools are available"
    - "github-ci.md uses detect:gh-preflight JSON output for authentication validation instead of raw gh auth status"
  artifacts:
    - path: "workflows/execute-plan.md"
      provides: "Tool-aware search/discovery instructions for executors"
      contains: "file-discovery-mode, search-mode"
    - path: "workflows/execute-phase.md"
      provides: "Tool capability hint in executor Task() prompt"
      contains: "capability_level, handoff_tool_context"
    - path: "workflows/map-codebase.md"
      provides: "Tool routing guidance in mapper Task() prompts"
      contains: "tool_availability, capability_level"
    - path: "workflows/github-ci.md"
      provides: "gh-preflight decision consumption"
      contains: "detect:gh-preflight"
  key_links:
    - from: "workflows/execute-plan.md"
      to: "bgsd-context decisions"
      via: "Pre-computed decision pattern for file-discovery-mode and search-mode"
      pattern: "decisions\\.file-discovery-mode|decisions\\.search-mode"
    - from: "workflows/execute-phase.md"
      to: "bgsd-context handoff_tool_context"
      via: "capability_level extraction and Task() prompt injection"
      pattern: "capability_level|handoff_tool_context"
    - from: "workflows/github-ci.md"
      to: "detect:gh-preflight"
      via: "Replaces raw gh auth status with structured preflight check"
      pattern: "detect:gh-preflight"
---

<objective>
Wire tool detection decisions from bgsd-context into 4 workflow files so they emit tool-aware instructions when spawning agents and validate GitHub CLI via structured preflight.

Purpose: Workflows currently ignore tool_availability decisions. After this plan, execute-plan emits tool-specific search commands, execute-phase passes capability level to executors, map-codebase informs mapper agents of available tools, and github-ci uses detect:gh-preflight for auth validation.
Output: 4 workflow files updated to consume tool routing decisions from bgsd-context using the established pre-computed decision pattern.
</objective>

<execution_context>
@/home/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/home/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-workflow-agent-tool-routing/138-CONTEXT.md
@workflows/execute-plan.md
@workflows/execute-phase.md
@workflows/map-codebase.md
@workflows/github-ci.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tool-aware search/discovery instructions to execute-plan.md</name>
  <files>workflows/execute-plan.md</files>
  <action>
In execute-plan.md, make two changes:

**1. Parse tool decisions from bgsd-context (init_context step, around line 15):**
Add `file-discovery-mode`, `search-mode`, `tool_availability`, `capability_level` (from `handoff_tool_context`) to the list of parsed fields from `<bgsd-context>` JSON.

**2. Add a "Tool-Aware Execution Guidance" section (after the existing execution pattern logic, around line 55-60):**
Add a new section that uses the pre-computed decision pattern (same pattern as `decisions.execution-pattern` at line 49) to emit tool-specific instructions:

```markdown
## Tool-Aware Execution Guidance

**Pre-computed decision:** If `decisions.file-discovery-mode` exists in `<bgsd-context>`, use its `.value`:
- `"fd"` → Executor agents should prefer `fd` for file discovery (e.g., `fd -e ts -e tsx`)
- `"node"` → Executor agents should use Glob tool or `node bgsd-tools.cjs` for file discovery

**Pre-computed decision:** If `decisions.search-mode` exists in `<bgsd-context>`, use its `.value`:
- `"ripgrep"` → Executor agents should prefer `rg` for content search (e.g., `rg "pattern" --type ts`)
- `"node"` → Executor agents should use Grep tool or node-based search

Include the resolved tool guidance in the executor Task() prompt context as a brief "Preferred tools" line. Do not duplicate full tool_availability — agents receive their own bgsd-context injection with complete details.
```

This follows the established pre-computed decision pattern used by 8+ existing workflows. The guidance applies to the executor spawn prompts in Patterns A/B/C (lines 62+).
  </action>
  <verify>
Read workflows/execute-plan.md and confirm:
1. `file-discovery-mode` and `search-mode` appear in the init_context field list
2. A "Tool-Aware Execution Guidance" section exists with pre-computed decision patterns for both file-discovery-mode and search-mode
3. The section references Patterns A/B/C executor spawns
  </verify>
  <done>execute-plan.md emits different search/discovery instructions when tools are present vs absent — "use fd/rg" when available, "use Glob/Grep tool" when not</done>
</task>

<task type="auto">
  <name>Task 2: Pass tool capability hint in execute-phase.md Task() prompts</name>
  <files>workflows/execute-phase.md</files>
  <action>
In execute-phase.md, make two changes:

**1. Parse tool context from bgsd-context (initialize step, around line 31):**
Add `handoff_tool_context`, `capability_level` (from `handoff_tool_context.capability_level`) to the list of parsed fields.

**2. Add a one-line tool capability hint to the Task() spawn prompt (execute_waves step, lines 131-168 for Mode B, and line 117 for Mode A):**

In both Mode A (worktree) and Mode B (sequential) Task() prompts, add a single line after the execution context block:

```
Tool capability: {capability_level} — agent receives full tool decisions via bgsd-context injection.
```

Where `{capability_level}` is `HIGH`, `MEDIUM`, or `LOW` from `handoff_tool_context.capability_level`.

This follows the context decision: "Minimal one-line hint in Task() prompt, full details in bgsd-context. Single source of truth." No duplication of tool_availability data — spawned executors get their own bgsd-context with complete tool decisions.
  </action>
  <verify>
Read workflows/execute-phase.md and confirm:
1. `handoff_tool_context` or `capability_level` appears in the initialize field list
2. Mode A and Mode B Task() prompts both contain a "Tool capability:" line
3. The hint is exactly one line — no duplication of tool_availability details
  </verify>
  <done>execute-phase.md passes tool routing guidance (capability level) to spawned executor agents in the Task() prompt</done>
</task>

<task type="auto">
  <name>Task 3: Add tool routing guidance to map-codebase.md mapper spawns</name>
  <files>workflows/map-codebase.md</files>
  <action>
In map-codebase.md, make two changes:

**1. Parse tool context from bgsd-context (init_context step, around line 14):**
Add `tool_availability`, `capability_level` (from `handoff_tool_context.capability_level`) to the extracted fields.

**2. Add tool guidance to the 4 mapper agent Task() spawn prompts (spawn_agents step, lines 42-54):**

Add a "Preferred tools" block to each mapper Task() prompt that provides resolved commands based on tool_availability:

```
Tool capability: {capability_level}.
{If fd available: "File discovery: fd -e ts -e tsx -e js -e jsx"}
{If ripgrep available: "Content search: rg 'pattern' --type ts"}
{If neither: "Use Glob and Grep MCP tools for file discovery and content search"}
```

The mapper agent (bgsd-codebase-mapper.md) has `tool_dependency_level: 'high'` so it already receives full tool data via bgsd-context. The Task() prompt supplements this with resolved commands specific to the mapping use case.

Note: The mapper agent prompt updates happen in Plan 02. This task only modifies the workflow spawning side.
  </action>
  <verify>
Read workflows/map-codebase.md and confirm:
1. `tool_availability` or `capability_level` appears in the init_context field list
2. Each of the 4 mapper Task() prompts includes tool guidance
3. Guidance uses resolved concrete commands (not abstract variables)
  </verify>
  <done>map-codebase.md passes tool routing guidance to spawned mapper agents so they know which CLI tools are available for file discovery and content search</done>
</task>

<task type="auto">
  <name>Task 4: Replace gh auth status with detect:gh-preflight in github-ci.md</name>
  <files>workflows/github-ci.md</files>
  <action>
In github-ci.md, replace the raw `gh auth status` call at line 40 with the structured `detect:gh-preflight` command.

**Current code (line 40 area):**
```bash
gh auth status 2>&1
```

**Replace with:**
```bash
node __OPENCODE_CONFIG__/bgsd-oc/bin/bgsd-tools.cjs detect:gh-preflight
```

**Update the validation logic around lines 40-50:**
Instead of parsing raw `gh auth status` output, parse the JSON output from `detect:gh-preflight`:
- Check `usable` field (boolean) — if true, gh CLI is ready
- On failure: display `error` field with actionable fix: "gh CLI not usable: {reason}. Run `{fix_command}` to resolve."
- Check `warnings` array — surface any version-specific warnings (e.g., known-bad gh versions)

The error UX should follow the context decision: **error + actionable fix** format, not a generic "not usable" message. Surface the exact problem and exact fix from the preflight JSON.

Also add `detect:gh-preflight` to the bgsd-context field parsing in the init section (around line 105-109) if there's a pre-computed `decisions.gh-preflight` value available.
  </action>
  <verify>
Read workflows/github-ci.md and confirm:
1. No raw `gh auth status` call remains (the old call at line 40 is replaced)
2. `detect:gh-preflight` command or decision is used instead
3. Error handling provides actionable fix messages, not generic errors
4. Warnings from preflight JSON are surfaced
  </verify>
  <done>github-ci.md uses detect:gh-preflight JSON output for authentication validation with actionable error messages instead of raw gh auth status</done>
</task>

</tasks>

<verification>
After all 4 tasks, verify the complete workflow routing surface:
1. `execute-plan.md` — has tool-aware search/discovery section with pre-computed decision pattern
2. `execute-phase.md` — has capability_level hint in both Mode A and Mode B Task() prompts
3. `map-codebase.md` — has tool guidance in all 4 mapper Task() spawn prompts
4. `github-ci.md` — uses detect:gh-preflight instead of raw gh auth status
5. All changes follow the pre-computed decision pattern established in existing workflows
6. No workflow duplicates full tool_availability data in Task() prompts (minimal hint only)
</verification>
