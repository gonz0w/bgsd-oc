---
phase: 176-command-hotspot-simplification-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - "176-01"
  - "176-02"
  - "176-03"
files_modified:
  - bin/bgsd-tools.cjs
autonomous: true
requirements:
  - SAFE-01
must_haves:
  truths:
    - "All canonical command routes dispatch correctly after cleanup."
    - "Planning workflows (plan:phase, plan:milestone) produce expected output."
    - "Verify and review workflows dispatch correctly."
  artifacts:
    - path: "bin/bgsd-tools.cjs"
      provides: "Rebuilt CLI with all cleanup changes"
      contains: "verify|misc"
    - path: "tests/validate-commands.test.cjs"
      provides: "Command integrity regression coverage"
      contains: "plan.*phase|discuss.*phase"
  key_links:
    - from: "bin/bgsd-tools.cjs"
      to: "src/router.js"
      via: "CLI dispatch uses router"
      pattern: "lazyVerify|lazyMisc"
    - from: "tests/validate-commands.test.cjs"
      to: "src/commands/verify.js"
      via: "Regression tests exercise verify command"
      pattern: "verify.*plan"
---

<objective>
Run the full integration test suite to prove that all canonical command routes still work correctly after the output-context encapsulation (plan 01), verify.js subdomain extraction (plan 02), and misc.js subdomain extraction (plan 03).

Purpose: SAFE-01 requires users can run supported planning and settings workflows after cleanup with regression proof. This plan serves as the integration gate before closing Phase 176.
Output: Full test suite pass with coverage report showing canonical routes are functional.
</objective>

> **TDD Decision:** Skipped — this is verification-only (running existing test suite) not introducing new behavior.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-CONTEXT.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-RESEARCH.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-01-PLAN.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-02-PLAN.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild CLI and run full test suite</name>
  <files>
    bin/bgsd-tools.cjs
  </files>
  <action>
Rebuild the CLI with all changes from plans 01-03, then run the full test suite:

1. `npm run build` — rebuild bin/bgsd-tools.cjs from source
2. `npm test` — run full test suite (762+ tests)

Capture test output and verify all tests pass. If any tests fail, diagnose the root cause and determine if it requires a fix in plans 01-03.
  </action>
  <verify>npm test 2>&1 | tail -30</verify>
  <done>Full test suite passes after all cleanup changes.</done>
</task>

<task type="auto">
  <name>Task 2: Run canonical route smoke tests</name>
  <files>
    bin/bgsd-tools.cjs
  </files>
  <action>
Run targeted smoke tests for canonical planning and verify routes:

1. `node bin/bgsd-tools.cjs plan:phase --help` — verify plan:phase route
2. `node bin/bgsd-tools.cjs plan:milestone --help` — verify plan:milestone route
3. `node bin/bgsd-tools.cjs verify:verify --help` — verify verify:verify route
4. `node bin/bgsd-tools.cjs verify:state --help` — verify verify:state route
5. `node bin/bgsd-tools.cjs review:scan --help` — verify review route
6. `node bin/bgsd-tools.cjs security:scan --help` — verify security route

Each command should display help without crashing.
  </action>
  <verify>node bin/bgsd-tools.cjs verify:verify --help 2>&1 | head -5</verify>
  <done>All canonical routes display help without errors.</done>
</task>

</tasks>

<verification>
npm test 2>&1 | grep -E "^(✓|✗|tests|pass|fail)" | tail -20
</verification>

<success_criteria>
- [ ] `npm run build` succeeds
- [ ] `npm test` passes (all 762+ tests)
- [ ] Canonical planning routes (plan:phase, plan:milestone) dispatch correctly
- [ ] Verify routes (verify:verify, verify:state) dispatch correctly
- [ ] Review and security routes dispatch correctly
</success_criteria>

<output>
After completion, create `.planning/phases/176-command-hotspot-simplification-hardening/176-04-SUMMARY.md`
</output>
