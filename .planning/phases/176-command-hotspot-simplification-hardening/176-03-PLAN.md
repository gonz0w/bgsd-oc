---
phase: 176-command-hotspot-simplification-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "176-01"
files_modified:
  - src/commands/misc.js
  - src/commands/misc/
autonomous: true
requirements:
  - CLI-03
must_haves:
  truths:
    - "misc.js is refactored into a subdomain directory with barrel re-export at the original path."
    - "Internal imports within misc/ subdomain use relative requires without touching globals."
    - "All existing `require('./commands/misc')` calls still resolve after the split."
  artifacts:
    - path: "src/commands/misc.js"
      provides: "Barrel re-export from misc/ subdomain"
      contains: "require.*misc/"
    - path: "src/commands/misc/index.js"
      provides: "Misc subdomain barrel index"
      contains: "module.exports"
    - path: "src/commands/misc/frontmatter.js"
      provides: "Frontmatter extraction helpers"
      contains: "extractFrontmatter|parseMustHavesBlock"
    - path: "src/commands/misc/templates.js"
      provides: "Template generation and loading"
      contains: "loadTemplate|renderTemplate"
    - path: "src/commands/misc/history-examples.js"
      provides: "History example generation"
      contains: "generateHistoryExample|renderHistoryLine"
    - path: "src/commands/misc/git-helpers.js"
      provides: "Git integration helpers"
      contains: "getGitBranch|getGitStatus"
    - path: "src/commands/misc/recovery.js"
      provides: "Recovery and rollback helpers"
      contains: "Recovery-related functions from misc.js"
    - path: "src/commands/misc/config-utils.js"
      provides: "Config utilities"
      contains: "loadConfig|saveConfig"
  key_links:
    - from: "src/commands/misc.js"
      to: "src/commands/misc/index.js"
      via: "barrel re-export preserves import contract"
      pattern: "module.exports = require"
---

<objective>
Extract `src/commands/misc.js` (2938 LOC) into a `src/commands/misc/` subdomain directory while maintaining backward compatibility via barrel re-export at the original path.

Purpose: CLI-03 requires maintainers to work within smaller command subdomains. misc.js is the second-largest bucket module and contains frontmatter helpers, templates, history examples, git helpers, recovery, and config utilities that can be cleanly separated.
Output: `src/commands/misc/` subdomain with barrel re-export, plus all internal references updated.
</objective>

> **TDD Decision:** Skipped — structural refactoring (file split with barrel re-export) preserves behavioral contract; verification via existing test suite proves correctness.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-CONTEXT.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-RESEARCH.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-01-PLAN.md
@src/commands/misc.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create misc/ subdomain and convert misc.js to barrel re-export</name>
  <files>
    src/commands/misc/
    src/commands/misc.js
  </files>
  <action>
Analyze `src/commands/misc.js` to identify functional clusters, then create `src/commands/misc/` directory with subdomain files.

Based on the research, misc.js contains: frontmatter helpers, templates, history examples, git helpers, recovery, and config utilities.

Extract into subdomain files following the pattern from plan 02. Create:
- `src/commands/misc/index.js` — barrel re-export
- `src/commands/misc/frontmatter.js` — frontmatter helpers
- `src/commands/misc/templates.js` — template generation and loading
- `src/commands/misc/history-examples.js` — history example generation
- `src/commands/misc/git-helpers.js` — git integration helpers
- `src/commands/misc/recovery.js` — recovery and rollback helpers
- `src/commands/misc/config-utils.js` — config utilities

Then replace the contents of `src/commands/misc.js` with a barrel re-export similar to the verify.js pattern.

Keep imports self-contained within the misc/ subdomain where possible.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>misc/ subdomain exists with barrel re-export; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Verify misc subdomain extraction and run tests</name>
  <files>
    src/commands/misc/
    src/commands/misc.js
  </files>
  <action>
Run the existing test suite focused on misc-related tests to ensure the refactor preserved behavior:

1. `npm run build` to rebuild the CLI
2. `npm test -- --test-name-pattern="misc" 2>&1 | tail -30`

If tests fail, diagnose whether the issue is a missing export or incorrect barrel re-export.
  </action>
  <verify>npm test -- --test-name-pattern="misc" 2>&1 | tail -15</verify>
  <done>All misc-related tests pass after the subdomain extraction.</done>
</task>

</tasks>

<verification>
npm run build && npm test 2>&1 | tail -20
</verification>

<success_criteria>
- [ ] `src/commands/misc/` subdomain directory exists with subdomain files
- [ ] `src/commands/misc.js` is a barrel re-export preserving all original exports
- [ ] All `require('./commands/misc')` calls still resolve correctly
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/176-command-hotspot-simplification-hardening/176-03-SUMMARY.md`
</output>
