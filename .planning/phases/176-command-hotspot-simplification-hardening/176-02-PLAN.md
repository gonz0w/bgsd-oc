---
phase: 176-command-hotspot-simplification-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "176-01"
files_modified:
  - src/commands/verify.js
  - src/commands/verify/
autonomous: true
requirements:
  - CLI-03
must_haves:
  truths:
    - "verify.js is refactored into a subdomain directory with barrel re-export at the original path."
    - "Internal imports within verify/ subdomain use relative requires without touching globals."
    - "All existing `require('./commands/verify')` calls still resolve after the split."
  artifacts:
    - path: "src/commands/verify.js"
      provides: "Barrel re-export from verify/ subdomain"
      contains: "require.*verify/"
    - path: "src/commands/verify/index.js"
      provides: "Verify subdomain barrel index"
      contains: "module.exports"
    - path: "src/commands/verify/quality.js"
      provides: "Quality verification functions"
      contains: "validateModelSettingsContract|extractCanonicalTddDecision|getMissingMetadataMessage"
    - path: "src/commands/verify/references.js"
      provides: "References and artifacts verification"
      contains: "getArchivedPhaseDirs|getMilestoneInfo|getPhaseTree"
    - path: "src/commands/verify/search.js"
      provides: "Search validation functions"
      contains: "findPhaseInternal|normalizePhaseName"
    - path: "src/commands/verify/health.js"
      provides: "Health and freshness validation"
      contains: "getRuntimeFreshness|getPlanMetadataContext"
  key_links:
    - from: "src/commands/verify.js"
      to: "src/commands/verify/index.js"
      via: "barrel re-export preserves import contract"
      pattern: "module.exports = require"
    - from: "src/commands/verify/"
      to: "src/lib/helpers"
      via: "verify subdomain imports helpers normally"
      pattern: "require.*lib/helpers"
---

<objective>
Extract `src/commands/verify.js` (3383 LOC, largest bucket module) into a `src/commands/verify/` subdomain directory while maintaining backward compatibility via barrel re-export at the original path.

Purpose: CLI-03 requires maintainers to work within smaller command subdomains. This plan breaks up the highest-friction bucket file first as a pilot, then subsequent plans apply the same pattern to misc.js, init.js, and features.js.
Output: `src/commands/verify/` subdomain with barrel re-export, plus all internal references updated.
</objective>

> **TDD Decision:** Skipped — structural refactoring (file split with barrel re-export) preserves behavioral contract; verification via existing test suite proves correctness.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-CONTEXT.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-RESEARCH.md
@.planning/phases/176-command-hotspot-simplification-hardening/176-01-PLAN.md
@src/commands/verify.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify/ subdomain and convert verify.js to barrel re-export</name>
  <files>
    src/commands/verify/
    src/commands/verify.js
  </files>
  <action>
Create `src/commands/verify/` directory and populate with subdomain files:

1. `src/commands/verify/index.js` — barrel re-export that requires all subdomain files and merges their exports

2. `src/commands/verify/quality.js` — extract: validateModelSettingsContract, extractCanonicalTddDecision, getMissingMetadataMessage, getInconclusiveMetadataMessage, and related quality/assertion functions

3. `src/commands/verify/references.js` — extract: getArchivedPhaseDirs, getMilestoneInfo, getPhaseTree, and references/artifacts functions

4. `src/commands/verify/search.js` — extract: findPhaseInternal, normalizePhaseName, and search-related functions

5. `src/commands/verify/health.js` — extract: getRuntimeFreshness, getPlanMetadataContext, createPlanMetadataContext, and health/freshness functions

Then replace the contents of `src/commands/verify.js` with a barrel re-export:

```javascript
'use strict';
module.exports = Object.assign(
  {},
  require('./verify/quality'),
  require('./verify/references'),
  require('./verify/search'),
  require('./verify/health')
);
```

This preserves the original export contract so all existing `require('./commands/verify')` calls still work.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>verify/ subdomain exists with barrel re-export; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Verify subdomain extraction and run tests</name>
  <files>
    src/commands/verify/
    src/commands/verify.js
  </files>
  <action>
Run the existing test suite focused on verify-related tests to ensure the refactor preserved behavior:

1. `npm run build` to rebuild the CLI
2. `npm test -- --test-name-pattern="verify" 2>&1 | tail -30`

If tests fail, diagnose whether the issue is a missing export or incorrect barrel re-export.
  </action>
  <verify>npm test -- --test-name-pattern="verify" 2>&1 | tail -15</verify>
  <done>All verify-related tests pass after the subdomain extraction.</done>
</task>

</tasks>

<verification>
npm run build && npm test 2>&1 | tail -20
</verification>

<success_criteria>
- [ ] `src/commands/verify/` subdomain directory exists with 5 subdomain files
- [ ] `src/commands/verify.js` is a barrel re-export preserving all original exports
- [ ] All `require('./commands/verify')` calls still resolve correctly
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/176-command-hotspot-simplification-hardening/176-02-SUMMARY.md`
</output>
