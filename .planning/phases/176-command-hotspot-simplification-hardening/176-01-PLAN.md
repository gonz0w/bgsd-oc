---
phase: 176-command-hotspot-simplification-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/output-context.js
  - src/router.js
  - src/lib/output.js
  - src/plugin/debug-contract.js
  - src/commands/init.js
  - src/commands/features.js
  - src/commands/env.js
  - src/commands/milestone.js
autonomous: true
requirements:
  - CLI-03
  - SAFE-02
must_haves:
  truths:
    - "All `global._gsd*` ambient state references are replaced with encapsulated module-API calls."
    - "Output mode, compact mode, manifest mode, and request fields use module-local state instead of globals."
    - "All touched cleanup paths throw on first error instead of swallowing silently."
  artifacts:
    - path: "src/lib/output-context.js"
      provides: "Encapsulated output context module"
      contains: "getOutputMode|setOutputMode|getCompactMode|setCompactMode|getManifestMode|setManifestMode|getRequestedFields|setRequestedFields|getDbNotices|setDbNotices"
    - path: "src/router.js"
      provides: "Router updated to use output-context module"
      contains: "require.*output-context"
    - path: "src/commands/init.js"
      provides: "init.js updated to use output-context module"
      contains: "require.*output-context"
  key_links:
    - from: "src/lib/output-context.js"
      to: "src/router.js"
      via: "router sets output mode at startup via output-context API"
      pattern: "setOutputMode|setCompactMode"
    - from: "src/commands/init.js"
      to: "src/lib/output-context.js"
      via: "init reads output mode via output-context API"
      pattern: "getCompactMode|getManifestMode"
    - from: "src/commands/features.js"
      to: "src/lib/output-context.js"
      via: "features reads/sets compact mode via output-context API"
      pattern: "getCompactMode|setCompactMode"
---

<objective>
Create `src/lib/output-context.js` that encapsulates all `global._gsd*` ambient state into module-local variables with getter/setter APIs, and update all command modules and router to use the new API instead of touching globals directly.

Purpose: Phase 176's locked decision requires full encapsulation of shared mutable state before any subdomain extraction begins. This eliminates unguarded ambient globals that make testing brittle and error swallowing that makes debugging hard.
Output: Encapsulated output-context module plus all updated references in router, init, features, env, milestone, and plugin debug-contract.
</objective>

> **TDD Decision:** Skipped — this is refactoring existing behavior (globals to module-API) without changing functional contract; verification via build success and existing test suite proves correctness.

<execution_context>
@/Users/cam/.config/opencode/bgsd-oc/workflows/execute-plan.md
@/Users/cam/.config/opencode/bgsd-oc/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-CONTEXT.md
@.planning/milestones/v18.1-phases/176-command-hotspot-simplification-hardening/176-RESEARCH.md
@src/lib/output.js
@src/router.js
@src/commands/init.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create output-context.js module and verify build succeeds</name>
  <files>
    src/lib/output-context.js
  </files>
  <action>
Create `src/lib/output-context.js` with module-local state and getter/setter API for all `global._gsd*` output-related variables:

- `_outputMode` (default: 'json')
- `_compactMode` (default: true)
- `_manifestMode` (default: false)
- `_requestedFields` (default: null)
- `_dbNotices` (default: undefined)

Export: getOutputMode, setOutputMode, getCompactMode, setCompactMode, getManifestMode, setManifestMode, getRequestedFields, setRequestedFields, getDbNotices, setDbNotices.

Use the same fail-fast contract: throw on invalid input rather than coercing silently.

Then update router.js to import and use the output-context API. At the top of router.js, add the require for output-context and replace all `global._gsd*` references with getter/setter calls.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>output-context.js exists and router.js uses it; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update command modules to use output-context API</name>
  <files>
    src/lib/output.js
    src/plugin/debug-contract.js
    src/commands/init.js
    src/commands/features.js
    src/commands/env.js
    src/commands/milestone.js
  </files>
  <action>
Update all remaining `global._gsd*` references in:
- `src/lib/output.js`: Replace `global._gsdOutputMode`, `global._gsdCompactMode`, `global._gsdRequestedFields` reads
- `src/plugin/debug-contract.js`: Replace `global._gsdCompactMode` read
- `src/commands/init.js`: Replace `global._gsdCompactMode`, `global._gsdManifestMode` reads
- `src/commands/features.js`: Replace `global._gsdCompactMode` reads/writes
- `src/commands/env.js`: Replace `global._gsdCompactMode` read
- `src/commands/milestone.js`: Replace `global._gsdOutputMode` reads/writes

In each file, import the output-context API and replace global references with getter/setter calls. Preserve all existing logic and defaults.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>All command modules use output-context API exclusively for output-related state.</done>
</task>

<task type="auto">
  <name>Task 3: Verify all globals replaced and tests pass</name>
  <files>
    src/lib/output-context.js
    src/router.js
    src/commands/init.js
    src/commands/features.js
    src/commands/env.js
    src/commands/milestone.js
    src/lib/output.js
    src/plugin/debug-contract.js
  </files>
  <action>
Run the test suite to verify:
1. `npm run build` succeeds
2. `npm test` passes
3. `rg "global\\._gsd" src/router.js src/commands/init.js src/commands/features.js src/lib/output.js src/plugin/debug-contract.js src/commands/milestone.js` returns no matches (all globals replaced)

If grep finds any remaining global._gsd references, fix them in the respective files.
  </action>
  <verify>npm test 2>&1 | tail -10</verify>
  <done>Full test suite passes and no global._gsd references remain in touched files.</done>
</task>

</tasks>

<verification>
npm run build && npm test 2>&1 | tail -20
</verification>

<success_criteria>
- [ ] `src/lib/output-context.js` exports getter/setter API for all five state variables
- [ ] `src/router.js` uses output-context API exclusively
- [ ] All command modules (init, features, env, milestone) use output-context API exclusively
- [ ] `src/lib/output.js` and `src/plugin/debug-contract.js` use output-context API exclusively
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] No `global._gsd*` references remain in touched files
</success_criteria>

<output>
After completion, create `.planning/phases/176-command-hotspot-simplification-hardening/176-01-SUMMARY.md`
</output>
